(function attachRendering(global) {
    const GROUND_TILE_OVERLAP_SCALE = 1.5;
    const GROUND_TILE_CACHE_LIMIT = 6000;
    const GROUND_TILE_TRIM_CHUNK_SIZE = 250;
    const GROUND_TILE_POOL_LIMIT = 1024;
    const TREE_ALPHA_CUTOFF = 0.08;
    const LOS_NEAR_REVEAL_RADIUS = 1.0;
    const LOS_THROTTLE_MS = 33;
    const LOS_BINS = 3600;
    const MAZE_MODE_ACTIVATION_SKIP_REVEAL_MS = 700;
    const FLOOR_VISUAL_FILL = 0x746b4d;
    const FLOOR_VISUAL_FILL_ACTIVE = 0x8c7f54;
    const FLOOR_VISUAL_HOLE_FILL = 0x050505;
    const FLOOR_VISUAL_UPPER_FILL = 0xb89a68;
    const FLOOR_VISUAL_CAVE_TEXTURE_PATH = "/assets/images/flooring/cave.jpg";
    const FLOOR_VISUAL_TEXTURE_WORLD_SCALE = 0.1;
    const FLOORING_TEXTURE_CONFIG_URL = "/assets/images/flooring/items.json";
    const FLOOR_LAYER_DEFAULT_HEIGHT_UNITS = 3;
    const FLOOR_LEVEL0_SURFACE_TEXTURE_PX_PER_WORLD = 128;
    const FLOOR_LEVEL0_SURFACE_TEXTURE_MAX_SIZE = 4096;
    const FLOOR_LEVEL0_TERRAIN_DEPTH_BIAS_STEP_UNITS = 0.01;
    const FLOOR_LEVEL0_TERRAIN_ALPHA_BLEND_WIDTH_UNITS = 1 / 3;
    // Terrain polygons are rendered live through floorVisual entries. The level-0
    // baked-surface path below still exists for legacy ground/road surface work,
    // but it is not the source of truth for painted terrain polygon visibility.
    const FLOOR_LEVEL0_BAKED_SURFACE_ENABLED = true;
    const FLOOR_LEVEL0_FORCE_BAKED_SURFACE = true;
    const FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED = true;
    const FLOOR_LEVEL0_GRASS_MATERIAL_PATH = "/assets/images/terrain/materials/grass.png";
    const FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED = true;
    const FLOOR_LEVEL0_BAKE_ROAD_PATHS = false;
    const FLOOR_LEVEL0_CHUNK_TEXTURE_SIZE = 1024;
    const FLOOR_LEVEL0_CHUNK_TEXTURE_PX_PER_WORLD = 32;
    const FLOOR_LEVEL0_CHUNK_BUILDS_PER_FRAME = 1;
    const FLOOR_LEVEL0_CHUNK_CACHE_LIMIT = 96;
    const FLOOR_LEVEL0_SEAM_BLEED_UNITS = 0.16;
    const FLOOR_LEVEL0_TERRAIN_BOUNDARY_MODEL_VERSION = 4;
    const FLOOR_LEVEL0_TERRAIN_FADE_CLIP_WARNING_LIMIT = 50;
    const FLOOR_VISUAL_DEPTH_NEAR_METRIC = -128;
    const FLOOR_VISUAL_DEPTH_FAR_METRIC = 256;
    const FLOOR_VISUAL_DEPTH_BIAS_UNITS = 0.001;
    const LEVEL0_TERRAIN_TOP_DEPTH_BIAS_UNITS = FLOOR_VISUAL_DEPTH_BIAS_UNITS + 0.002 + (4 * FLOOR_LEVEL0_TERRAIN_DEPTH_BIAS_STEP_UNITS);
    const ROAD_PATH_DEPTH_BIAS_UNITS = LEVEL0_TERRAIN_TOP_DEPTH_BIAS_UNITS + 0.004;
    const GROUND_SHADOW_DEPTH_BIAS_UNITS = ROAD_PATH_DEPTH_BIAS_UNITS + 0.004;
    const FLOOR_VISUAL_HOLE_DEPTH_BIAS_UNITS = 0.02;
    const FLOOR_BELOW_CURRENT_DARKNESS_MULTIPLIER = 0.8;
    const BUILDING_CUTAWAY_GHOST_ALPHA = 0;
    const BUILDING_CUTAWAY_INTERIOR_ALPHA = 0.5;
    const PROTOTYPE_BUILDING_EXTERIOR_CUTAWAY_ALPHA_THRESHOLD = 8;
    const BUILDING_CUTAWAY_COMPOSITE_VERSION = 5;
    const BUILDING_CUTAWAY_CACHE_PX_PER_WORLD = 64;
    const BUILDING_CUTAWAY_CACHE_MAX_TEXTURE_SIZE = 4096;
    const BUILDING_CUTAWAY_DATA_MIN_Z = -64;
    const BUILDING_CUTAWAY_DATA_MAX_Z = 192;
    const BUILDING_CUTAWAY_ENTRY_FADE_MS = 500;
    const BUILDING_CUTAWAY_INTERIOR_FLOOR_FADE_MS = 500;
    const BUILDING_CUTAWAY_INTERIOR_FLOOR_SWITCH_BELOW_UNITS = 1;
    const BUILDING_DOORWAY_TRANSITION_ENTER_DISTANCE = 0.45;
    const BUILDING_DOORWAY_TRANSITION_EXIT_DISTANCE = 0.65;
    const BUILDING_DOORWAY_TRANSITION_SPAN_PADDING = 0.45;
    const BUILDING_DOORWAY_TRANSITION_EXIT_SPAN_PADDING = 0.6;
    const BUILDING_INTERIOR_OVERLAY_Z = 2147483648;
    const BUILDING_INTERIOR_FOREGROUND_Z = 2147483650;
    const BUILDING_INTERIOR_WIZARD_BODY_Z = BUILDING_INTERIOR_FOREGROUND_Z - 1;
    const BUILDING_INTERIOR_WIZARD_SHADOW_Z = BUILDING_INTERIOR_WIZARD_BODY_Z - 0.001;
    const BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS = 32;
    const BUILDING_INTERIOR_LOWER_FLOOR_LIGHT_FACTOR = 0.62;
    const BUILDING_INTERIOR_WIZARD_HAT_Z = BUILDING_INTERIOR_FOREGROUND_Z + 1;
    const LOS_SHADOW_DEPTH_BIAS_UNITS = GROUND_SHADOW_DEPTH_BIAS_UNITS;
    const WIZARD_SHADOW_DEPTH_BIAS_UNITS = GROUND_SHADOW_DEPTH_BIAS_UNITS;
    const WIZARD_HAT_LIFT_UNITS = 0.15;
    const WIZARD_BODY_VISUAL_LOWER_UNITS = 0.25;
    const WIZARD_BODY_GROUND_DEPTH_BIAS_UNITS = 0.1;
    const WIZARD_WATER_DEPTH_SLOPE = 2 / 3;
    const WIZARD_WATER_MAX_DEPTH_UNITS = 2 / 3;
    const WIZARD_WATER_MIN_VISIBLE_BODY_RATIO = 1 / 3;
    const BUILDING_INTERIOR_WIZARD_BODY_DEPTH_LIFT_UNITS = WIZARD_BODY_VISUAL_LOWER_UNITS + 0.02;
    const FLOOR_VISUAL_DEPTH_VS = `
precision highp float;
attribute vec2 aVertexPosition;
attribute vec2 aUvs;
attribute float aEdgeAlpha;
uniform vec2 uScreenSize;
uniform vec2 uCameraWorld;
uniform float uCameraZ;
uniform float uBaseZ;
uniform float uDepthBias;
uniform float uViewScale;
uniform float uXyRatio;
uniform vec2 uDepthRange;
varying vec2 vUvs;
varying float vWorldZ;
varying float vEdgeAlpha;
void main(void) {
    float camDx = aVertexPosition.x - uCameraWorld.x;
    float camDy = aVertexPosition.y - uCameraWorld.y;
    float camDz = uBaseZ - uCameraZ;
    float sx = max(1.0, uScreenSize.x);
    float sy = max(1.0, uScreenSize.y);
    float screenX = camDx * uViewScale;
    float screenY = (camDy - camDz) * uViewScale * uXyRatio;
    float depthMetric = camDy + camDz + uDepthBias;
    float farMetric = uDepthRange.x;
    float invSpan = max(1e-6, uDepthRange.y);
    float nd = clamp((farMetric - depthMetric) * invSpan, 0.0, 1.0);
    vec2 clip = vec2(
        (screenX / sx) * 2.0 - 1.0,
        1.0 - (screenY / sy) * 2.0
    );
    gl_Position = vec4(clip, nd * 2.0 - 1.0, 1.0);
    vUvs = aUvs;
    vWorldZ = uBaseZ;
    vEdgeAlpha = clamp(aEdgeAlpha, 0.0, 1.0);
}
`;
    const ROAD_PATH_DEPTH_VS = `
precision highp float;
attribute vec2 aVertexPosition;
attribute vec2 aUvs;
attribute float aEdgeAlpha;
uniform vec2 uScreenSize;
uniform vec2 uCameraWorld;
uniform float uCameraZ;
uniform float uBaseZ;
uniform float uDepthBias;
uniform float uViewScale;
uniform float uXyRatio;
uniform vec2 uDepthRange;
varying vec2 vUvs;
varying float vWorldZ;
varying float vEdgeAlpha;
void main(void) {
    float camDx = aVertexPosition.x - uCameraWorld.x;
    float camDy = aVertexPosition.y - uCameraWorld.y;
    float camDz = uBaseZ - uCameraZ;
    float sx = max(1.0, uScreenSize.x);
    float sy = max(1.0, uScreenSize.y);
    float screenX = camDx * uViewScale;
    float screenY = (camDy - camDz) * uViewScale * uXyRatio;
    float depthMetric = camDy + camDz + uDepthBias;
    float farMetric = uDepthRange.x;
    float invSpan = max(1e-6, uDepthRange.y);
    float nd = clamp((farMetric - depthMetric) * invSpan, 0.0, 1.0);
    vec2 clip = vec2(
        (screenX / sx) * 2.0 - 1.0,
        1.0 - (screenY / sy) * 2.0
    );
    gl_Position = vec4(clip, nd * 2.0 - 1.0, 1.0);
    vUvs = aUvs;
    vWorldZ = uBaseZ;
    vEdgeAlpha = clamp(aEdgeAlpha, 0.0, 1.0);
}
`;
    const warnedTerrainFadeClipFailures = new Set();
    let suppressedTerrainFadeClipWarnings = 0;
    const LOS_SHADOW_DEPTH_VS = `
precision highp float;
attribute vec2 aWorldPosition;
uniform vec2 uScreenSize;
uniform vec2 uCameraWorld;
uniform float uCameraZ;
uniform float uBaseZ;
uniform float uDepthBias;
uniform float uViewScale;
uniform float uXyRatio;
uniform vec2 uDepthRange;
void main(void) {
    float camDx = aWorldPosition.x - uCameraWorld.x;
    float camDy = aWorldPosition.y - uCameraWorld.y;
    float camDz = uBaseZ - uCameraZ;
    float sx = max(1.0, uScreenSize.x);
    float sy = max(1.0, uScreenSize.y);
    float screenX = camDx * uViewScale;
    float screenY = (camDy - camDz) * uViewScale * uXyRatio;
    float depthMetric = camDy + camDz + uDepthBias;
    float farMetric = uDepthRange.x;
    float invSpan = max(1e-6, uDepthRange.y);
    float nd = clamp((farMetric - depthMetric) * invSpan, 0.0, 1.0);
    vec2 clip = vec2(
        (screenX / sx) * 2.0 - 1.0,
        1.0 - (screenY / sy) * 2.0
    );
    gl_Position = vec4(clip, nd * 2.0 - 1.0, 1.0);
}
`;
    const LOS_SHADOW_DEPTH_FS = `
precision highp float;
uniform vec4 uTint;
void main(void) {
    gl_FragColor = uTint;
}
`;
    const FLOOR_VISUAL_DEPTH_FS = `
precision highp float;
varying vec2 vUvs;
varying float vWorldZ;
varying float vEdgeAlpha;
uniform sampler2D uSampler;
uniform vec4 uTint;
uniform float uAlphaCutoff;
uniform float uBuildingCutawayDataPass;
uniform vec2 uBuildingCutawayDataZRange;
void main(void) {
    vec4 outColor = texture2D(uSampler, fract(vUvs)) * uTint;
    float edgeAlpha = clamp(vEdgeAlpha, 0.0, 1.0);
    outColor = vec4(outColor.rgb * edgeAlpha, outColor.a * edgeAlpha);
    if (outColor.a < uAlphaCutoff) discard;
    if (uBuildingCutawayDataPass > 0.5) {
        float minZ = uBuildingCutawayDataZRange.x;
        float invSpan = uBuildingCutawayDataZRange.y;
        float encodedZ = clamp((vWorldZ - minZ) * invSpan, 0.0, 1.0);
        gl_FragColor = vec4(encodedZ, 0.0, 0.0, 1.0);
        return;
    }
    gl_FragColor = outColor;
}
`;
    const FLOOR_VISUAL_WATER_DEPTH_FS = `
precision highp float;
varying vec2 vUvs;
varying float vWorldZ;
varying float vEdgeAlpha;
uniform sampler2D uSampler;
uniform vec4 uTint;
uniform vec2 uPhaseOffset0;
uniform vec2 uPhaseOffset1;
uniform vec2 uPhaseOffset2;
uniform vec3 uPhaseWeights;
uniform float uSpatialPhase;
uniform float uSpatialFrequency;
uniform float uSpatialStrength;
uniform float uAlphaCutoff;
uniform float uBuildingCutawayDataPass;
uniform vec2 uBuildingCutawayDataZRange;
void main(void) {
    if (uBuildingCutawayDataPass > 0.5) {
        float minZ = uBuildingCutawayDataZRange.x;
        float invSpan = uBuildingCutawayDataZRange.y;
        float encodedZ = clamp((vWorldZ - minZ) * invSpan, 0.0, 1.0);
        gl_FragColor = vec4(encodedZ, 0.0, 0.0, 1.0);
        return;
    }
    vec3 w = max(uPhaseWeights, vec3(0.0));
    w *= smoothstep(vec3(0.0), vec3(0.08), uPhaseWeights);
    float weightSum = max(0.0001, w.x + w.y + w.z);
    w /= weightSum;
    vec2 uv0 = vUvs;
    vec2 uv1 = mat2(0.7986355, -0.6018150, 0.6018150, 0.7986355) * vUvs;
    vec2 uv2 = mat2(0.2756374, -0.9612617, 0.9612617, 0.2756374) * vUvs;
    vec3 c0 = texture2D(uSampler, fract(uv0 + uPhaseOffset0)).rgb;
    vec3 c1 = texture2D(uSampler, fract(uv1 + uPhaseOffset1)).rgb;
    vec3 c2 = texture2D(uSampler, fract(uv2 + uPhaseOffset2)).rgb;
    float alpha = clamp(uTint.a * vEdgeAlpha, 0.0, 1.0);
    if (alpha < uAlphaCutoff) discard;
    gl_FragColor = vec4((c0 * w.x + c1 * w.y + c2 * w.z) * uTint.rgb * alpha, alpha);
}
`;
    const ROAD_PATH_DEPTH_FS = `
precision highp float;
varying vec2 vUvs;
varying float vWorldZ;
varying float vEdgeAlpha;
uniform sampler2D uSampler;
uniform vec4 uTint;
uniform float uAlphaCutoff;
uniform float uBuildingCutawayDataPass;
uniform vec2 uBuildingCutawayDataZRange;
void main(void) {
    vec4 sampleColor = texture2D(uSampler, fract(vUvs));
    float fadeAlpha = clamp(uTint.a * vEdgeAlpha, 0.0, 1.0);
    vec4 outColor = vec4(sampleColor.rgb * uTint.rgb * fadeAlpha, sampleColor.a * fadeAlpha);
    if (outColor.a < uAlphaCutoff) discard;
    if (uBuildingCutawayDataPass > 0.5) {
        float minZ = uBuildingCutawayDataZRange.x;
        float invSpan = uBuildingCutawayDataZRange.y;
        float encodedZ = clamp((vWorldZ - minZ) * invSpan, 0.0, 1.0);
        gl_FragColor = vec4(encodedZ, 0.0, 0.0, 1.0);
        return;
    }
    gl_FragColor = outColor;
}
`;
    const STAIR_DEPTH_VS = `
precision highp float;
attribute vec3 aWorldPosition;
attribute vec2 aUvs;
attribute float aShade;
uniform vec2 uScreenSize;
uniform vec2 uCameraWorld;
uniform float uCameraZ;
uniform float uDepthBias;
uniform float uViewScale;
uniform float uXyRatio;
uniform vec2 uDepthRange;
varying vec2 vUvs;
varying float vShade;
void main(void) {
    float camDx = aWorldPosition.x - uCameraWorld.x;
    float camDy = aWorldPosition.y - uCameraWorld.y;
    float camDz = aWorldPosition.z - uCameraZ;
    float sx = max(1.0, uScreenSize.x);
    float sy = max(1.0, uScreenSize.y);
    float screenX = camDx * uViewScale;
    float screenY = (camDy - camDz) * uViewScale * uXyRatio;
    float depthMetric = camDy + camDz + uDepthBias;
    float farMetric = uDepthRange.x;
    float invSpan = max(1e-6, uDepthRange.y);
    float nd = clamp((farMetric - depthMetric) * invSpan, 0.0, 1.0);
    vec2 clip = vec2(
        (screenX / sx) * 2.0 - 1.0,
        1.0 - (screenY / sy) * 2.0
    );
    gl_Position = vec4(clip, nd * 2.0 - 1.0, 1.0);
    vUvs = aUvs;
    vShade = aShade;
}
`;
    const STAIR_DEPTH_FS = `
precision highp float;
varying vec2 vUvs;
varying float vShade;
uniform sampler2D uSampler;
uniform vec4 uTint;
uniform float uAlphaCutoff;
void main(void) {
    vec4 color = texture2D(uSampler, vUvs) * uTint;
    color.rgb *= clamp(vShade, 0.0, 1.25);
    if (color.a < uAlphaCutoff) discard;
    gl_FragColor = color;
}
`;
    if (typeof global.renderingShowPickerScreen !== "boolean") {
        global.renderingShowPickerScreen = false;
    }

    function getShowPickerScreenFlag() {
        return !!global.renderingShowPickerScreen;
    }

    function setShowPickerScreenFlag(enabled) {
        const next = !!enabled;
        global.renderingShowPickerScreen = next;
        return next;
    }

    function getFloorEarcut() {
        if (global.earcut) {
            if (typeof global.earcut === "function") return global.earcut;
            if (typeof global.earcut.default === "function") return global.earcut.default;
        }
        if (typeof PIXI !== "undefined" && PIXI.utils && typeof PIXI.utils.earcut === "function") {
            return PIXI.utils.earcut;
        }
        return null;
    }

    function normalizeFloorVisualPointList(points) {
        if (!Array.isArray(points)) return [];
        const out = [];
        for (let i = 0; i < points.length; i++) {
            const point = points[i];
            const x = Number(point && point.x);
            const y = Number(point && point.y);
            if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
            out.push({ x, y });
        }
        return out;
    }

    function buildFloorVisualSignature(outer, holes) {
        const parts = [];
        const appendRing = (ring) => {
            parts.push("[");
            for (let i = 0; i < ring.length; i++) {
                const pt = ring[i];
                parts.push(`${Math.round(pt.x * 1000)},${Math.round(pt.y * 1000)};`);
            }
            parts.push("]");
        };
        appendRing(outer);
        const normalizedHoles = Array.isArray(holes) ? holes : [];
        for (let i = 0; i < normalizedHoles.length; i++) {
            appendRing(normalizeFloorVisualPointList(normalizedHoles[i]));
        }
        return parts.join("");
    }

    function buildFloorVisualClipBoundarySignature(rings) {
        const normalized = Array.isArray(rings) ? rings : [];
        if (normalized.length === 0) return "";
        return normalized.map(ring => buildFloorVisualSignature(normalizeFloorVisualPointList(ring), [])).join("|");
    }

    function floorVisualPointLiesOnSegment(point, a, b, epsilon = 1e-7) {
        const px = Number(point && point.x);
        const py = Number(point && point.y);
        const ax = Number(a && a.x);
        const ay = Number(a && a.y);
        const bx = Number(b && b.x);
        const by = Number(b && b.y);
        if (!Number.isFinite(px) || !Number.isFinite(py) || !Number.isFinite(ax) || !Number.isFinite(ay) || !Number.isFinite(bx) || !Number.isFinite(by)) return false;
        const dx = bx - ax;
        const dy = by - ay;
        const lengthSq = dx * dx + dy * dy;
        if (!(lengthSq > epsilon * epsilon)) return Math.hypot(px - ax, py - ay) <= epsilon;
        const cross = (px - ax) * dy - (py - ay) * dx;
        if (Math.abs(cross) > epsilon * Math.sqrt(lengthSq)) return false;
        const dot = (px - ax) * dx + (py - ay) * dy;
        return dot >= -epsilon && dot <= lengthSq + epsilon;
    }

    function floorVisualSegmentLiesOnBoundaryRings(a, b, rings, epsilon = 1e-7) {
        const normalized = Array.isArray(rings) ? rings : [];
        for (let r = 0; r < normalized.length; r++) {
            const ring = normalizeFloorVisualPointList(normalized[r]);
            if (ring.length < 2) continue;
            for (let i = 0; i < ring.length; i++) {
                const c = ring[i];
                const d = ring[(i + 1) % ring.length];
                if (
                    floorVisualPointLiesOnSegment(a, c, d, epsilon) &&
                    floorVisualPointLiesOnSegment(b, c, d, epsilon)
                ) {
                    return true;
                }
            }
        }
        return false;
    }

    function cloneFloorVisualDiagnosticRing(points) {
        return normalizeFloorVisualPointList(points).map(point => ({
            x: Math.round(Number(point.x) * 1000000) / 1000000,
            y: Math.round(Number(point.y) * 1000000) / 1000000
        }));
    }

    function summarizeFloorVisualRingEdges(points) {
        const ring = normalizeFloorVisualPointList(points);
        const zeroLengthEdges = [];
        let minEdgeLength = Infinity;
        let maxEdgeLength = 0;
        let totalEdgeLength = 0;
        for (let i = 0; i < ring.length; i++) {
            const a = ring[i];
            const b = ring[(i + 1) % ring.length];
            const length = Math.hypot(Number(b.x) - Number(a.x), Number(b.y) - Number(a.y));
            if (Number.isFinite(length)) {
                minEdgeLength = Math.min(minEdgeLength, length);
                maxEdgeLength = Math.max(maxEdgeLength, length);
                totalEdgeLength += length;
                if (!(length > 1e-9)) {
                    zeroLengthEdges.push({
                        edgeIndex: i,
                        start: cloneFloorVisualDiagnosticRing([a])[0] || null,
                        end: cloneFloorVisualDiagnosticRing([b])[0] || null
                    });
                }
            }
        }
        return {
            pointCount: ring.length,
            signedArea: Math.round(getFloorVisualRingSignedArea(ring) * 1000000) / 1000000,
            minEdgeLength: Number.isFinite(minEdgeLength) ? Math.round(minEdgeLength * 1000000) / 1000000 : null,
            maxEdgeLength: Math.round(maxEdgeLength * 1000000) / 1000000,
            totalEdgeLength: Math.round(totalEdgeLength * 1000000) / 1000000,
            zeroLengthEdges
        };
    }

    function throwTerrainAlphaBlendDiagnosticError(message, diagnostic) {
        const err = new Error(message);
        err.diagnostic = diagnostic || {};
        if (typeof console !== "undefined" && console && typeof console.error === "function") {
            console.error("[terrain alpha blend diagnostic]", {
                message,
                diagnostic: err.diagnostic
            });
        }
        throw err;
    }

    function warnTerrainFadeClipApproximation(err, diagnostic) {
        const key = [
            diagnostic && diagnostic.entryKey ? diagnostic.entryKey : "",
            diagnostic && diagnostic.ringKind ? diagnostic.ringKind : "",
            diagnostic && Number.isFinite(diagnostic.holeIndex) ? diagnostic.holeIndex : "",
            diagnostic && Number.isFinite(diagnostic.edgeIndex) ? diagnostic.edgeIndex : "",
            diagnostic && diagnostic.bandSignature ? diagnostic.bandSignature : "",
            diagnostic && diagnostic.clipSignature ? diagnostic.clipSignature : "",
            err && err.message ? String(err.message) : String(err)
        ].join("|");
        if (warnedTerrainFadeClipFailures.has(key)) return;
        if (warnedTerrainFadeClipFailures.size >= FLOOR_LEVEL0_TERRAIN_FADE_CLIP_WARNING_LIMIT) {
            suppressedTerrainFadeClipWarnings += 1;
            if (suppressedTerrainFadeClipWarnings === 1) {
                console.warn("[terrain fade clip approximation] warning limit reached", {
                    limit: FLOOR_LEVEL0_TERRAIN_FADE_CLIP_WARNING_LIMIT,
                    message: "Additional distinct terrain fade clipping failures are suppressed."
                });
            }
            return;
        }
        warnedTerrainFadeClipFailures.add(key);
        console.warn("[terrain fade clip approximation]", {
            message: "Polygon clipping failed while clipping a terrain edge fade band; drawing the unclipped fade band as a diagnostic approximation.",
            error: err && err.message ? String(err.message) : String(err),
            approximation: "unclipped fade band",
            entryKey: diagnostic && diagnostic.entryKey ? diagnostic.entryKey : "",
            terrainType: diagnostic && diagnostic.terrainType ? diagnostic.terrainType : "",
            sectionKey: diagnostic && diagnostic.sectionKey ? diagnostic.sectionKey : "",
            groupIndex: diagnostic && Number.isFinite(diagnostic.groupIndex) ? diagnostic.groupIndex : null,
            polygonIndex: diagnostic && Number.isFinite(diagnostic.polygonIndex) ? diagnostic.polygonIndex : null,
            clippedPieceIndex: diagnostic && Number.isFinite(diagnostic.clippedPieceIndex) ? diagnostic.clippedPieceIndex : null,
            ringKind: diagnostic && diagnostic.ringKind ? diagnostic.ringKind : "",
            holeIndex: diagnostic && Number.isFinite(diagnostic.holeIndex) ? diagnostic.holeIndex : null,
            edgeIndex: diagnostic && Number.isFinite(diagnostic.edgeIndex) ? diagnostic.edgeIndex : null,
            blendWidth: diagnostic && Number.isFinite(diagnostic.blendWidth) ? diagnostic.blendWidth : null,
            sourceSignature: diagnostic && diagnostic.sourceSignature ? diagnostic.sourceSignature : "",
            bandSignature: diagnostic && diagnostic.bandSignature ? diagnostic.bandSignature : "",
            clipSignature: diagnostic && diagnostic.clipSignature ? diagnostic.clipSignature : "",
            edgeStart: diagnostic && diagnostic.edgeStart ? diagnostic.edgeStart : null,
            edgeEnd: diagnostic && diagnostic.edgeEnd ? diagnostic.edgeEnd : null,
            bandOuter: diagnostic && Array.isArray(diagnostic.bandOuter) ? diagnostic.bandOuter : [],
            sourceOuter: diagnostic && Array.isArray(diagnostic.sourceOuter) ? diagnostic.sourceOuter : [],
            sourceHoles: diagnostic && Array.isArray(diagnostic.sourceHoles) ? diagnostic.sourceHoles : [],
            clipRings: diagnostic && Array.isArray(diagnostic.clipRings) ? diagnostic.clipRings : []
        });
    }

    function triangulateFloorVisualPolygon(outer, holes) {
        const earcut = getFloorEarcut();
        if (!earcut || outer.length < 3) return null;
        const vertices = [];
        const holeIndices = [];
        const allPoints = [];
        const pushRing = (ring) => {
            for (let i = 0; i < ring.length; i++) {
                const pt = ring[i];
                vertices.push(pt.x, pt.y);
                allPoints.push(pt);
            }
        };
        pushRing(outer);
        const normalizedHoles = Array.isArray(holes) ? holes : [];
        for (let i = 0; i < normalizedHoles.length; i++) {
            const ring = normalizeFloorVisualPointList(normalizedHoles[i]);
            if (ring.length < 3) continue;
            holeIndices.push(allPoints.length);
            pushRing(ring);
        }
        if (allPoints.length < 3) return null;
        const indices = earcut(vertices, holeIndices.length > 0 ? holeIndices : null, 2);
        if (!indices || indices.length < 3) return null;
        const maxIndex = allPoints.length - 1;
        const IndexArray = maxIndex > 65535 ? Uint32Array : Uint16Array;
        return {
            points: allPoints,
            indices: new IndexArray(indices),
            vertexCount: allPoints.length
        };
    }

    function getFloorVisualRingSignedArea(points) {
        const ring = normalizeFloorVisualPointList(points);
        if (ring.length < 3) return 0;
        let area = 0;
        for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {
            area += (ring[j].x * ring[i].y) - (ring[i].x * ring[j].y);
        }
        return area * 0.5;
    }

    function offsetFloorVisualRing(points, amount, diagnosticContext = null) {
        const ring = normalizeFloorVisualPointList(points);
        const distance = Number(amount);
        if (ring.length < 3 || !Number.isFinite(distance) || Math.abs(distance) <= 1e-9) return ring;
        const signedArea = getFloorVisualRingSignedArea(ring);
        if (!Number.isFinite(signedArea) || Math.abs(signedArea) <= 1e-9) {
            throw new Error("terrain alpha blend requires a non-degenerate polygon ring");
        }
        const outwardSign = signedArea >= 0 ? 1 : -1;
        const edgeNormal = (a, b) => {
            const dx = Number(b.x) - Number(a.x);
            const dy = Number(b.y) - Number(a.y);
            const length = Math.hypot(dx, dy);
            if (!(length > 1e-9)) return null;
            return {
                x: outwardSign * dy / length,
                y: -outwardSign * dx / length
            };
        };
        const intersectLines = (p1, p2, p3, p4) => {
            const x1 = Number(p1.x);
            const y1 = Number(p1.y);
            const x2 = Number(p2.x);
            const y2 = Number(p2.y);
            const x3 = Number(p3.x);
            const y3 = Number(p3.y);
            const x4 = Number(p4.x);
            const y4 = Number(p4.y);
            const den = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);
            if (Math.abs(den) <= 1e-9) return null;
            const det12 = x1 * y2 - y1 * x2;
            const det34 = x3 * y4 - y3 * x4;
            return {
                x: (det12 * (x3 - x4) - (x1 - x2) * det34) / den,
                y: (det12 * (y3 - y4) - (y1 - y2) * det34) / den
            };
        };
        const out = [];
        const maxMiter = Math.max(Math.abs(distance) * 6, Math.abs(distance) + 0.001);
        for (let i = 0; i < ring.length; i++) {
            const prev = ring[(i + ring.length - 1) % ring.length];
            const cur = ring[i];
            const next = ring[(i + 1) % ring.length];
            const prevNormal = edgeNormal(prev, cur);
            const nextNormal = edgeNormal(cur, next);
            if (!prevNormal || !nextNormal) {
                const prevEdgeLength = Math.hypot(Number(cur.x) - Number(prev.x), Number(cur.y) - Number(prev.y));
                const nextEdgeLength = Math.hypot(Number(next.x) - Number(cur.x), Number(next.y) - Number(cur.y));
                throwTerrainAlphaBlendDiagnosticError(
                    "terrain alpha blend cannot offset a polygon ring with zero-length edges",
                    {
                        ...(diagnosticContext && typeof diagnosticContext === "object" ? diagnosticContext : {}),
                        offsetAmount: distance,
                        vertexIndex: i,
                        previousEdgeIndex: (i + ring.length - 1) % ring.length,
                        nextEdgeIndex: i,
                        previousEdgeLength: Number.isFinite(prevEdgeLength) ? prevEdgeLength : null,
                        nextEdgeLength: Number.isFinite(nextEdgeLength) ? nextEdgeLength : null,
                        previousPoint: cloneFloorVisualDiagnosticRing([prev])[0] || null,
                        currentPoint: cloneFloorVisualDiagnosticRing([cur])[0] || null,
                        nextPoint: cloneFloorVisualDiagnosticRing([next])[0] || null,
                        ringSummary: summarizeFloorVisualRingEdges(ring),
                        ring: cloneFloorVisualDiagnosticRing(ring)
                    }
                );
            }
            const prevA = { x: prev.x + prevNormal.x * distance, y: prev.y + prevNormal.y * distance };
            const prevB = { x: cur.x + prevNormal.x * distance, y: cur.y + prevNormal.y * distance };
            const nextA = { x: cur.x + nextNormal.x * distance, y: cur.y + nextNormal.y * distance };
            const nextB = { x: next.x + nextNormal.x * distance, y: next.y + nextNormal.y * distance };
            let point = intersectLines(prevA, prevB, nextA, nextB);
            if (!point) {
                const nx = prevNormal.x + nextNormal.x;
                const ny = prevNormal.y + nextNormal.y;
                const length = Math.hypot(nx, ny);
                point = length > 1e-9
                    ? { x: cur.x + (nx / length) * distance, y: cur.y + (ny / length) * distance }
                    : { x: cur.x + nextNormal.x * distance, y: cur.y + nextNormal.y * distance };
            }
            const dx = point.x - cur.x;
            const dy = point.y - cur.y;
            const miterLength = Math.hypot(dx, dy);
            if (miterLength > maxMiter) {
                point = {
                    x: cur.x + (dx / miterLength) * maxMiter,
                    y: cur.y + (dy / miterLength) * maxMiter
                };
            }
            if (!Number.isFinite(point.x) || !Number.isFinite(point.y)) {
                throw new Error("terrain alpha blend produced a non-finite offset vertex");
            }
            out.push(point);
        }
        return out;
    }

    function triangulateFloorVisualTerrainBlendPolygon(outer, holes, blendWidth, options = null) {
        const sourceOuter = normalizeFloorVisualPointList(outer);
        if (sourceOuter.length < 3) return null;
        const width = Number(blendWidth);
        if (!Number.isFinite(width) || !(width > 0)) {
            return triangulateFloorVisualPolygon(sourceOuter, holes);
        }
        const base = triangulateFloorVisualPolygon(sourceOuter, holes);
        if (!base) return null;
        const points = base.points.slice();
        const edgeAlpha = new Array(points.length).fill(1);
        const indices = Array.from(base.indices);
        const emitVertex = (point, alpha) => {
            if (!point || !Number.isFinite(point.x) || !Number.isFinite(point.y)) {
                throw new Error("terrain alpha blend cannot emit a non-finite vertex");
            }
            points.push({ x: Number(point.x), y: Number(point.y) });
            edgeAlpha.push(Math.max(0, Math.min(1, Number(alpha))));
            return points.length - 1;
        };
        const emitTriangle = (a, b, c) => {
            indices.push(a, b, c);
        };
        const clipBoundaryRings = Array.isArray(options && options.edgeFadeClipRings)
            ? options.edgeFadeClipRings.map(ring => normalizeFloorVisualPointList(ring)).filter(ring => ring.length >= 3)
            : [];
        const skipBoundaryRings = Array.isArray(options && options.edgeFadeSkipRings)
            ? options.edgeFadeSkipRings.map(ring => normalizeFloorVisualPointList(ring)).filter(ring => ring.length >= 3)
            : clipBoundaryRings;
        const clipBoundaryOuter = clipBoundaryRings.length > 0 ? clipBoundaryRings[0] : null;
        const clipBoundaryHoles = clipBoundaryRings.length > 1 ? clipBoundaryRings.slice(1) : [];
        const sourceHoles = Array.isArray(holes)
            ? holes.map(hole => normalizeFloorVisualPointList(hole)).filter(hole => hole.length >= 3)
            : [];
        const diagnosticBase = options && typeof options.edgeFadeDiagnosticContext === "object" && options.edgeFadeDiagnosticContext
            ? options.edgeFadeDiagnosticContext
            : {};
        const sourceSignature = buildFloorVisualSignature(sourceOuter, sourceHoles);
        const clipSignature = buildFloorVisualClipBoundarySignature(clipBoundaryRings);
        const skipClipBoundaryFade = options && options.skipClipBoundaryFade === true;
        const clipFadeBandPolygon = (bandOuter, bandContext = null) => {
            if (!clipBoundaryOuter) return [{ outer: bandOuter, holes: [] }];
            const api = getFloorVisualPolygonClippingApi();
            if (!api || typeof api.intersection !== "function") {
                throw new Error("terrain alpha blend clipping requires polygon clipping intersection");
            }
            const bandGeometry = floorVisualClipMultiPolygonFromRings(bandOuter, []);
            const clipGeometry = floorVisualClipMultiPolygonFromRings(clipBoundaryOuter, clipBoundaryHoles);
            if (bandGeometry.length === 0 || clipGeometry.length === 0) return [];
            try {
                return floorVisualClipGeometryToPolygons(api.intersection(bandGeometry, clipGeometry));
            } catch (err) {
                warnTerrainFadeClipApproximation(err, {
                    ...diagnosticBase,
                    ringKind: bandContext && bandContext.ringKind ? bandContext.ringKind : "",
                    holeIndex: bandContext && Number.isFinite(bandContext.holeIndex) ? bandContext.holeIndex : null,
                    edgeIndex: bandContext && Number.isFinite(bandContext.edgeIndex) ? bandContext.edgeIndex : null,
                    edgeStart: bandContext && bandContext.edgeStart ? bandContext.edgeStart : null,
                    edgeEnd: bandContext && bandContext.edgeEnd ? bandContext.edgeEnd : null,
                    blendWidth: width,
                    sourceSignature,
                    bandSignature: buildFloorVisualSignature(bandOuter, []),
                    clipSignature,
                    bandOuter: cloneFloorVisualDiagnosticRing(bandOuter),
                    sourceOuter: cloneFloorVisualDiagnosticRing(sourceOuter),
                    sourceHoles: sourceHoles.map(hole => cloneFloorVisualDiagnosticRing(hole)),
                    clipRings: clipBoundaryRings.map(ring => cloneFloorVisualDiagnosticRing(ring))
                });
                return [{ outer: bandOuter, holes: [] }];
            }
        };
        const getFadeBandAlpha = (point, innerA, innerB, outerA, outerB) => {
            const ax = Number(innerA && innerA.x);
            const ay = Number(innerA && innerA.y);
            const bx = Number(innerB && innerB.x);
            const by = Number(innerB && innerB.y);
            const px = Number(point && point.x);
            const py = Number(point && point.y);
            if (!Number.isFinite(ax) || !Number.isFinite(ay) || !Number.isFinite(bx) || !Number.isFinite(by) || !Number.isFinite(px) || !Number.isFinite(py)) {
                throw new Error("terrain alpha blend clipping produced a non-finite alpha sample");
            }
            const edgeX = bx - ax;
            const edgeY = by - ay;
            const edgeLengthSq = edgeX * edgeX + edgeY * edgeY;
            const edgeT = edgeLengthSq > 1e-9
                ? Math.max(0, Math.min(1, ((px - ax) * edgeX + (py - ay) * edgeY) / edgeLengthSq))
                : 0;
            const innerX = ax + edgeX * edgeT;
            const innerY = ay + edgeY * edgeT;
            const outerX = Number(outerA.x) + (Number(outerB.x) - Number(outerA.x)) * edgeT;
            const outerY = Number(outerA.y) + (Number(outerB.y) - Number(outerA.y)) * edgeT;
            const fadeX = outerX - innerX;
            const fadeY = outerY - innerY;
            const fadeLengthSq = fadeX * fadeX + fadeY * fadeY;
            if (!(fadeLengthSq > 1e-9)) return 1;
            const fadeT = ((px - innerX) * fadeX + (py - innerY) * fadeY) / fadeLengthSq;
            return 1 - Math.max(0, Math.min(1, fadeT));
        };
        const emitAlphaPolygon = (polygon, alphaForPoint) => {
            if (!polygon || !Array.isArray(polygon.outer) || polygon.outer.length < 3) return;
            const clippedTriangulation = triangulateFloorVisualPolygon(polygon.outer, polygon.holes);
            if (!clippedTriangulation) return;
            const baseIndex = points.length;
            for (let i = 0; i < clippedTriangulation.points.length; i++) {
                emitVertex(clippedTriangulation.points[i], alphaForPoint(clippedTriangulation.points[i]));
            }
            for (let i = 0; i + 2 < clippedTriangulation.indices.length; i += 3) {
                emitTriangle(
                    baseIndex + clippedTriangulation.indices[i],
                    baseIndex + clippedTriangulation.indices[i + 1],
                    baseIndex + clippedTriangulation.indices[i + 2]
                );
            }
        };
        const emitFadeBand = (innerRing, outerRing, ringContext = null) => {
            const inner = normalizeFloorVisualPointList(innerRing);
            const outerBand = normalizeFloorVisualPointList(outerRing);
            if (inner.length !== outerBand.length || inner.length < 3) {
                throw new Error("terrain alpha blend requires matching inner and outer fade rings");
            }
            for (let i = 0; i < inner.length; i++) {
                const next = (i + 1) % inner.length;
                if (
                    skipClipBoundaryFade &&
                    skipBoundaryRings.length > 0 &&
                    floorVisualSegmentLiesOnBoundaryRings(inner[i], inner[next], skipBoundaryRings)
                ) {
                    continue;
                }
                const bandOuter = [inner[i], inner[next], outerBand[next], outerBand[i]];
                const clippedBands = clipFadeBandPolygon(bandOuter, {
                    edgeIndex: i,
                    ringKind: ringContext && ringContext.ringKind ? ringContext.ringKind : "outer",
                    holeIndex: ringContext && Number.isFinite(ringContext.holeIndex) ? ringContext.holeIndex : null,
                    edgeStart: cloneFloorVisualDiagnosticRing([inner[i]])[0] || null,
                    edgeEnd: cloneFloorVisualDiagnosticRing([inner[next]])[0] || null
                });
                for (let b = 0; b < clippedBands.length; b++) {
                    emitAlphaPolygon(clippedBands[b], point => (
                        getFadeBandAlpha(point, inner[i], inner[next], outerBand[i], outerBand[next])
                    ));
                }
            }
        };
        emitFadeBand(sourceOuter, offsetFloorVisualRing(sourceOuter, width, {
            ...diagnosticBase,
            ringKind: "outer",
            holeIndex: null,
            blendWidth: width,
            sourceSignature,
            clipSignature,
            sourceOuter: cloneFloorVisualDiagnosticRing(sourceOuter),
            sourceHoles: sourceHoles.map(hole => cloneFloorVisualDiagnosticRing(hole)),
            clipRings: clipBoundaryRings.map(ring => cloneFloorVisualDiagnosticRing(ring))
        }), { ringKind: "outer" });
        const normalizedHoles = Array.isArray(holes) ? holes : [];
        for (let h = 0; h < normalizedHoles.length; h++) {
            const hole = normalizeFloorVisualPointList(normalizedHoles[h]);
            if (hole.length < 3) continue;
            emitFadeBand(hole, offsetFloorVisualRing(hole, -width, {
                ...diagnosticBase,
                ringKind: "hole",
                holeIndex: h,
                blendWidth: width,
                sourceSignature,
                clipSignature,
                sourceOuter: cloneFloorVisualDiagnosticRing(sourceOuter),
                sourceHoles: sourceHoles.map(sourceHole => cloneFloorVisualDiagnosticRing(sourceHole)),
                clipRings: clipBoundaryRings.map(ring => cloneFloorVisualDiagnosticRing(ring))
            }), { ringKind: "hole", holeIndex: h });
        }
        const IndexArray = points.length > 65535 ? Uint32Array : Uint16Array;
        return {
            points,
            indices: new IndexArray(indices),
            vertexCount: points.length,
            edgeAlpha: new Float32Array(edgeAlpha)
        };
    }

    function expandFloorVisualPolygonFromCentroid(points, amount) {
        const ring = normalizeFloorVisualPointList(points);
        const delta = Number(amount);
        if (ring.length < 3 || !Number.isFinite(delta) || delta <= 0) return ring;
        let cx = 0;
        let cy = 0;
        for (let i = 0; i < ring.length; i++) {
            cx += ring[i].x;
            cy += ring[i].y;
        }
        cx /= ring.length;
        cy /= ring.length;
        const out = [];
        for (let i = 0; i < ring.length; i++) {
            const pt = ring[i];
            const dx = pt.x - cx;
            const dy = pt.y - cy;
            const length = Math.hypot(dx, dy);
            if (length <= 1e-6) {
                out.push({ x: pt.x, y: pt.y });
                continue;
            }
            out.push({
                x: pt.x + (dx / length) * delta,
                y: pt.y + (dy / length) * delta
            });
        }
        return out;
    }

    function getFloorVisualPointBounds(points) {
        const ring = normalizeFloorVisualPointList(points);
        if (ring.length === 0) return null;
        let minX = Infinity;
        let minY = Infinity;
        let maxX = -Infinity;
        let maxY = -Infinity;
        for (let i = 0; i < ring.length; i++) {
            minX = Math.min(minX, ring[i].x);
            minY = Math.min(minY, ring[i].y);
            maxX = Math.max(maxX, ring[i].x);
            maxY = Math.max(maxY, ring[i].y);
        }
        if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) return null;
        return { minX, minY, maxX, maxY, width: maxX - minX, height: maxY - minY };
    }

    function clipFloorVisualPolygonToRect(points, rect) {
        let polygon = normalizeFloorVisualPointList(points);
        if (polygon.length < 3 || !rect) return [];
        const EPS = 1e-7;
        const clipEdge = (input, inside, intersect) => {
            if (!Array.isArray(input) || input.length === 0) return [];
            const output = [];
            for (let i = 0; i < input.length; i++) {
                const current = input[i];
                const previous = input[(i + input.length - 1) % input.length];
                const currentInside = inside(current);
                const previousInside = inside(previous);
                if (currentInside) {
                    if (!previousInside) output.push(intersect(previous, current));
                    output.push(current);
                } else if (previousInside) {
                    output.push(intersect(previous, current));
                }
            }
            return output;
        };
        polygon = clipEdge(
            polygon,
            (pt) => pt.x >= rect.minX - EPS,
            (a, b) => {
                const t = (rect.minX - a.x) / ((b.x - a.x) || EPS);
                return { x: rect.minX, y: a.y + (b.y - a.y) * t };
            }
        );
        polygon = clipEdge(
            polygon,
            (pt) => pt.x <= rect.maxX + EPS,
            (a, b) => {
                const t = (rect.maxX - a.x) / ((b.x - a.x) || EPS);
                return { x: rect.maxX, y: a.y + (b.y - a.y) * t };
            }
        );
        polygon = clipEdge(
            polygon,
            (pt) => pt.y >= rect.minY - EPS,
            (a, b) => {
                const t = (rect.minY - a.y) / ((b.y - a.y) || EPS);
                return { x: a.x + (b.x - a.x) * t, y: rect.minY };
            }
        );
        polygon = clipEdge(
            polygon,
            (pt) => pt.y <= rect.maxY + EPS,
            (a, b) => {
                const t = (rect.maxY - a.y) / ((b.y - a.y) || EPS);
                return { x: a.x + (b.x - a.x) * t, y: rect.maxY };
            }
        );
        return polygon.length >= 3 ? polygon : [];
    }

    function getFloorVisualPolygonClippingApi() {
        return (global && global.polygonClipping) ? global.polygonClipping : null;
    }

    function floorVisualPointsToClipRing(points) {
        const normalized = normalizeFloorVisualPointList(points);
        if (normalized.length < 3) return null;
        const ring = normalized.map(point => [point.x, point.y]);
        const first = ring[0];
        const last = ring[ring.length - 1];
        if (!last || Math.abs(first[0] - last[0]) > 1e-9 || Math.abs(first[1] - last[1]) > 1e-9) {
            ring.push([first[0], first[1]]);
        }
        return ring;
    }

    function floorVisualClipRingToPoints(ring) {
        if (!Array.isArray(ring)) return [];
        const points = [];
        for (let i = 0; i < ring.length; i++) {
            const pair = ring[i];
            const x = Number(pair && pair[0]);
            const y = Number(pair && pair[1]);
            if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
            if (
                i === ring.length - 1 &&
                points.length > 0 &&
                Math.abs(points[0].x - x) <= 1e-9 &&
                Math.abs(points[0].y - y) <= 1e-9
            ) {
                continue;
            }
            points.push({ x, y });
        }
        return points;
    }

    function floorVisualClipMultiPolygonFromRings(outer, holes) {
        const outerRing = floorVisualPointsToClipRing(outer);
        if (!outerRing) return [];
        const polygon = [outerRing];
        const normalizedHoles = Array.isArray(holes) ? holes : [];
        for (let i = 0; i < normalizedHoles.length; i++) {
            const holeRing = floorVisualPointsToClipRing(normalizedHoles[i]);
            if (holeRing) polygon.push(holeRing);
        }
        return [polygon];
    }

    function floorVisualClipGeometryToPolygons(geometry) {
        if (!Array.isArray(geometry) || geometry.length === 0) return [];
        const out = [];
        for (let i = 0; i < geometry.length; i++) {
            const polygon = geometry[i];
            if (!Array.isArray(polygon) || polygon.length === 0) continue;
            const outer = floorVisualClipRingToPoints(polygon[0]);
            if (outer.length < 3) continue;
            const holes = [];
            for (let h = 1; h < polygon.length; h++) {
                const hole = floorVisualClipRingToPoints(polygon[h]);
                if (hole.length >= 3) holes.push(hole);
            }
            out.push({ outer, holes });
        }
        return out;
    }

    function unionFloorVisualPolygons(polygons) {
        const normalized = Array.isArray(polygons)
            ? polygons.filter(poly => Array.isArray(poly) && poly.length >= 3)
            : [];
        if (normalized.length === 0) return [];
        const api = getFloorVisualPolygonClippingApi();
        if (!api || typeof api.union !== "function") {
            return normalized.map(outer => ({ outer, holes: [] }));
        }
        const geometries = normalized
            .map(outer => floorVisualClipMultiPolygonFromRings(outer, []))
            .filter(geometry => Array.isArray(geometry) && geometry.length > 0);
        if (geometries.length === 0) return [];
        try {
            const union = api.union(...geometries);
            const converted = floorVisualClipGeometryToPolygons(union);
            return converted.length > 0 ? converted : normalized.map(outer => ({ outer, holes: [] }));
        } catch (_err) {
            return normalized.map(outer => ({ outer, holes: [] }));
        }
    }

    function intersectFloorVisualPolygonWithPolygon(outer, holes, clipPolygon) {
        const normalizedOuter = normalizeFloorVisualPointList(outer);
        const clipOuter = normalizeFloorVisualPointList(clipPolygon && clipPolygon.outer);
        if (normalizedOuter.length < 3 || clipOuter.length < 3) return [];
        const api = getFloorVisualPolygonClippingApi();
        if (!api || typeof api.intersection !== "function") {
            return floorVisualPolygonsContainPoint([{
                outer: clipOuter,
                holes: Array.isArray(clipPolygon && clipPolygon.holes) ? clipPolygon.holes : []
            }], normalizedOuter[0].x, normalizedOuter[0].y)
                ? [{ outer: normalizedOuter, holes: Array.isArray(holes) ? holes : [] }]
                : [];
        }
        const subject = floorVisualClipMultiPolygonFromRings(normalizedOuter, holes);
        const clip = floorVisualClipMultiPolygonFromRings(clipOuter, Array.isArray(clipPolygon && clipPolygon.holes) ? clipPolygon.holes : []);
        if (!Array.isArray(subject) || subject.length === 0 || !Array.isArray(clip) || clip.length === 0) return [];
        try {
            return floorVisualClipGeometryToPolygons(api.intersection(subject, clip));
        } catch (_err) {
            return [];
        }
    }

    function intersectFloorVisualPolygonWithPolygonStrict(outer, holes, clipPolygon, label = "floor visual polygon") {
        const normalizedOuter = normalizeFloorVisualPointList(outer);
        const clipOuter = normalizeFloorVisualPointList(clipPolygon && clipPolygon.outer);
        if (normalizedOuter.length < 3 || clipOuter.length < 3) return [];
        const api = getFloorVisualPolygonClippingApi();
        if (!api || typeof api.intersection !== "function") {
            throw new Error(`${label} clipping requires polygon-clipping intersection support`);
        }
        const subject = floorVisualClipMultiPolygonFromRings(normalizedOuter, holes);
        const clip = floorVisualClipMultiPolygonFromRings(clipOuter, Array.isArray(clipPolygon && clipPolygon.holes) ? clipPolygon.holes : []);
        if (!Array.isArray(subject) || subject.length === 0 || !Array.isArray(clip) || clip.length === 0) return [];
        try {
            return floorVisualClipGeometryToPolygons(api.intersection(subject, clip));
        } catch (err) {
            throw new Error(`${label} clipping failed: ${err && err.message ? err.message : err}`);
        }
    }

    function floorVisualPolygonsContainPoint(polygons, x, y) {
        const normalized = Array.isArray(polygons) ? polygons : [];
        for (let i = 0; i < normalized.length; i++) {
            const polygon = normalized[i];
            if (!polygon || !Array.isArray(polygon.outer) || polygon.outer.length < 3) continue;
            if (!pointInFloorVisualPolygon2D(x, y, polygon.outer)) continue;
            let inHole = false;
            const holes = Array.isArray(polygon.holes) ? polygon.holes : [];
            for (let h = 0; h < holes.length; h++) {
                if (pointInFloorVisualPolygon2D(x, y, holes[h])) {
                    inHole = true;
                    break;
                }
            }
            if (!inHole) return true;
        }
        return false;
    }

    function floorVisualClipGeometryFromRect(rect) {
        if (!rect) return [];
        const minX = Number(rect.minX);
        const minY = Number(rect.minY);
        const maxX = Number(rect.maxX);
        const maxY = Number(rect.maxY);
        if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) return [];
        if (!(maxX > minX) || !(maxY > minY)) return [];
        return floorVisualClipMultiPolygonFromRings([
            { x: minX, y: minY },
            { x: maxX, y: minY },
            { x: maxX, y: maxY },
            { x: minX, y: maxY }
        ], []);
    }

    function collectFloorVisualClippedPolygonsForRect(outer, holes, rect) {
        const normalizedHoles = Array.isArray(holes) ? holes : [];
        if (normalizedHoles.length === 0) {
            const clippedOuter = clipFloorVisualPolygonToRect(outer, rect);
            return clippedOuter.length >= 3 ? [{ outer: clippedOuter, holes: [] }] : [];
        }
        const api = getFloorVisualPolygonClippingApi();
        if (!api || typeof api.intersection !== "function") return null;
        const subject = floorVisualClipMultiPolygonFromRings(outer, normalizedHoles);
        const clip = floorVisualClipGeometryFromRect(rect);
        if (!Array.isArray(subject) || subject.length === 0 || !Array.isArray(clip) || clip.length === 0) return [];
        let intersection = [];
        try {
            intersection = api.intersection(subject, clip);
        } catch (err) {
            if (global && global.renderingDiagnostics && global.renderingDiagnostics.floorChunkClipLogging === true) {
                console.warn("[level0 floor chunk clip failed]", err);
            }
            return null;
        }
        if (!Array.isArray(intersection) || intersection.length === 0) return [];
        const out = [];
        for (let i = 0; i < intersection.length; i++) {
            const polygon = intersection[i];
            if (!Array.isArray(polygon) || polygon.length === 0) continue;
            const clippedOuter = floorVisualClipRingToPoints(polygon[0]);
            if (clippedOuter.length < 3) continue;
            const clippedHoles = [];
            for (let h = 1; h < polygon.length; h++) {
                const hole = floorVisualClipRingToPoints(polygon[h]);
                if (hole.length >= 3) clippedHoles.push(hole);
            }
            out.push({ outer: clippedOuter, holes: clippedHoles });
        }
        return out;
    }

    function isFloorEditIsolationActive() {
        return false;
    }

    function isPointSupportedByFloorFragment(fragment, x, y) {
        if (!fragment || !Array.isArray(fragment.outerPolygon) || fragment.outerPolygon.length < 3) return false;
        if (typeof pointInPolygon2D !== "function" || !pointInPolygon2D(x, y, fragment.outerPolygon)) return false;
        const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
        for (let i = 0; i < holes.length; i++) {
            const hole = holes[i];
            if (Array.isArray(hole) && hole.length >= 3 && typeof pointInPolygon2D === "function" && pointInPolygon2D(x, y, hole)) {
                return false;
            }
        }
        return true;
    }

    function pointNearFloorVisualSegment2D(px, py, ax, ay, bx, by, epsilon = 0.001) {
        const abx = bx - ax;
        const aby = by - ay;
        const apx = px - ax;
        const apy = py - ay;
        const abLen2 = abx * abx + aby * aby;
        if (abLen2 <= 1e-9) return Math.hypot(px - ax, py - ay) <= epsilon;
        const t = Math.max(0, Math.min(1, (apx * abx + apy * aby) / abLen2));
        const cx = ax + abx * t;
        const cy = ay + aby * t;
        return Math.hypot(px - cx, py - cy) <= epsilon;
    }

    function closestFloorVisualSegmentPoint2D(px, py, ax, ay, bx, by) {
        const abx = bx - ax;
        const aby = by - ay;
        const apx = px - ax;
        const apy = py - ay;
        const abLen2 = abx * abx + aby * aby;
        if (abLen2 <= 1e-9) {
            const dx = px - ax;
            const dy = py - ay;
            return { x: ax, y: ay, t: 0, distanceSq: dx * dx + dy * dy };
        }
        const t = Math.max(0, Math.min(1, (apx * abx + apy * aby) / abLen2));
        const x = ax + abx * t;
        const y = ay + aby * t;
        const dx = px - x;
        const dy = py - y;
        return { x, y, t, distanceSq: dx * dx + dy * dy };
    }

    function distanceToFloorVisualRing2D(x, y, points) {
        const ring = normalizeFloorVisualPointList(points);
        if (ring.length === 0 || !Number.isFinite(x) || !Number.isFinite(y)) return null;
        let best = null;
        for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {
            const closest = closestFloorVisualSegmentPoint2D(x, y, ring[j].x, ring[j].y, ring[i].x, ring[i].y);
            if (!closest || !Number.isFinite(closest.distanceSq)) continue;
            if (!best || closest.distanceSq < best.distanceSq) {
                best = {
                    distanceSq: closest.distanceSq,
                    distance: Math.sqrt(Math.max(0, closest.distanceSq)),
                    closestPoint: { x: closest.x, y: closest.y },
                    edgeIndex: j
                };
            }
        }
        return best;
    }

    function pointInFloorVisualPolygon2D(x, y, points, options = {}) {
        const ring = normalizeFloorVisualPointList(points);
        if (ring.length < 3 || !Number.isFinite(x) || !Number.isFinite(y)) return false;
        const includeBoundary = options.includeBoundary !== false;
        if (includeBoundary) {
            const epsilon = Number.isFinite(options.epsilon) ? Math.max(0, Number(options.epsilon)) : 0.001;
            for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {
                if (pointNearFloorVisualSegment2D(x, y, ring[j].x, ring[j].y, ring[i].x, ring[i].y, epsilon)) {
                    return true;
                }
            }
        }
        let inside = false;
        for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {
            const xi = ring[i].x;
            const yi = ring[i].y;
            const xj = ring[j].x;
            const yj = ring[j].y;
            const intersect = ((yi > y) !== (yj > y))
                && (x < ((xj - xi) * (y - yi)) / ((yj - yi) || 1e-7) + xi);
            if (intersect) inside = !inside;
        }
        return inside;
    }

    function distanceToFloorVisualPolygon2D(x, y, polygon) {
        if (!polygon || !Number.isFinite(x) || !Number.isFinite(y)) return null;
        const outer = Array.isArray(polygon.outer)
            ? polygon.outer
            : (Array.isArray(polygon) ? polygon : []);
        if (outer.length < 3) return null;
        const outerDistance = distanceToFloorVisualRing2D(x, y, outer);
        const insideOuter = pointInFloorVisualPolygon2D(x, y, outer);
        const holes = Array.isArray(polygon.holes) ? polygon.holes : [];
        let insideHole = false;
        let bestHoleDistance = null;
        for (let h = 0; h < holes.length; h++) {
            const hole = holes[h];
            if (!Array.isArray(hole) || hole.length < 3) continue;
            const holeDistance = distanceToFloorVisualRing2D(x, y, hole);
            if (holeDistance && (!bestHoleDistance || holeDistance.distanceSq < bestHoleDistance.distanceSq)) {
                bestHoleDistance = holeDistance;
            }
            if (pointInFloorVisualPolygon2D(x, y, hole)) insideHole = true;
        }
        const inside = insideOuter && !insideHole;
        if (inside) {
            return { inside: true, distance: 0, distanceSq: 0, closestPoint: { x, y } };
        }
        const best = insideHole && bestHoleDistance
            ? bestHoleDistance
            : (outerDistance || bestHoleDistance);
        if (!best) return null;
        return {
            inside: false,
            distance: best.distance,
            distanceSq: best.distanceSq,
            closestPoint: best.closestPoint || null
        };
    }

    function floorVisualPolygonsDistanceToPoint(polygons, x, y) {
        const normalized = Array.isArray(polygons) ? polygons : [];
        let best = null;
        for (let i = 0; i < normalized.length; i++) {
            const distance = distanceToFloorVisualPolygon2D(x, y, normalized[i]);
            if (!distance) continue;
            if (distance.inside) return distance;
            if (!best || distance.distanceSq < best.distanceSq) best = distance;
        }
        return best;
    }

    function isPointInsideFloorVisibilityFragment(fragment, x, y) {
        if (!fragment) return false;
        if (!pointInFloorVisualPolygon2D(x, y, fragment.outerPolygon)) return false;
        const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
        for (let i = 0; i < holes.length; i++) {
            if (pointInFloorVisualPolygon2D(x, y, holes[i])) return false;
        }
        return true;
    }

    function findNearestSupportedFloorLayer(map, x, y, startLayer) {
        if (!(map && map.floorsById instanceof Map)) return null;
        const byLevel = new Map();
        for (const fragment of map.floorsById.values()) {
            if (!fragment) continue;
            const level = Number.isFinite(fragment.level) ? Math.round(Number(fragment.level)) : 0;
            if (!byLevel.has(level)) byLevel.set(level, []);
            byLevel.get(level).push(fragment);
        }
        const requestedLevel = Number.isFinite(startLayer) ? Math.round(Number(startLayer)) : 0;
        const levels = Array.from(byLevel.keys()).sort((a, b) => b - a);
        for (let i = 0; i < levels.length; i++) {
            const level = levels[i];
            if (level > requestedLevel) continue;
            const fragments = byLevel.get(level) || [];
            for (let j = 0; j < fragments.length; j++) {
                if (isPointSupportedByFloorFragment(fragments[j], x, y)) {
                    return level;
                }
            }
        }
        return null;
    }

    // Keep expensive renderer diagnostics available for triage, but leave them
    // off in normal gameplay. Re-enable only when chasing invalid PIXI textures
    // or ground tile lifetime bugs.
    function ensureRenderingDiagnosticsConfig() {
        const defaults = {
            textureSanitizer: false,
            pixiSpriteCrashDiagnostics: false,
            roadTextureLifecycleDiagnostics: false,
            groundTileProfiling: false,
            drawPassBreakdown: true,
            scenePickerHoverProfiling: false
        };
        if (!global.renderingDiagnostics || typeof global.renderingDiagnostics !== "object") {
            global.renderingDiagnostics = { ...defaults };
            return global.renderingDiagnostics;
        }
        const config = global.renderingDiagnostics;
        for (const key of Object.keys(defaults)) {
            if (typeof config[key] !== "boolean") {
                config[key] = defaults[key];
            }
        }
        return config;
    }

    function isTextureSanitizerEnabled() {
        return !!ensureRenderingDiagnosticsConfig().textureSanitizer;
    }

    function isPixiSpriteCrashDiagnosticsEnabled() {
        return !!ensureRenderingDiagnosticsConfig().pixiSpriteCrashDiagnostics;
    }

    function isRoadTextureLifecycleDiagnosticsEnabled() {
        return !!ensureRenderingDiagnosticsConfig().roadTextureLifecycleDiagnostics;
    }

    function isGroundTileProfilingEnabled() {
        return !!ensureRenderingDiagnosticsConfig().groundTileProfiling;
    }

    function isDrawPassBreakdownEnabled() {
        return !!ensureRenderingDiagnosticsConfig().drawPassBreakdown;
    }

    if (typeof global.RenderingDiagnostics !== "object" || !global.RenderingDiagnostics) {
        global.RenderingDiagnostics = {
            getFlags() {
                return { ...ensureRenderingDiagnosticsConfig() };
            },
            setTextureSanitizerEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.textureSanitizer = !!enabled;
                return config.textureSanitizer;
            },
            setPixiSpriteCrashDiagnosticsEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.pixiSpriteCrashDiagnostics = !!enabled;
                if (config.pixiSpriteCrashDiagnostics) {
                    installPixiSpriteRenderDiagnostics();
                }
                return config.pixiSpriteCrashDiagnostics;
            },
            setRoadTextureLifecycleDiagnosticsEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.roadTextureLifecycleDiagnostics = !!enabled;
                return config.roadTextureLifecycleDiagnostics;
            },
            setGroundTileProfilingEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.groundTileProfiling = !!enabled;
                return config.groundTileProfiling;
            },
            setDrawPassBreakdownEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.drawPassBreakdown = !!enabled;
                return config.drawPassBreakdown;
            },
            setScenePickerHoverProfilingEnabled(enabled) {
                const config = ensureRenderingDiagnosticsConfig();
                config.scenePickerHoverProfiling = !!enabled;
                return config.scenePickerHoverProfiling;
            }
        };
    }

    function isRenderablePixiTexture(texture) {
        if (!texture || texture === PIXI.Texture.EMPTY) return false;
        const baseTexture = texture.baseTexture || null;
        const orig = texture.orig || null;
        const hasUvs = !!(texture._uvs && texture._uvs.uvsFloat32);
        const hasOrigSize = !!(
            orig &&
            Number.isFinite(orig.width) &&
            Number.isFinite(orig.height)
        );
        return !!(baseTexture && hasUvs && hasOrigSize);
    }

    function sanitizeDisplayTreeTextures(root, options = {}) {
        if (!root) return { repaired: 0, samples: [] };
        const maxSamples = Number.isFinite(options.maxSamples) ? Math.max(1, Number(options.maxSamples)) : 8;
        const samples = [];
        let repaired = 0;
        const stack = [root];
        while (stack.length > 0) {
            const current = stack.pop();
            if (!current) continue;
            const texture = current.texture;
            if (Object.prototype.hasOwnProperty.call(current, "texture") && !isRenderablePixiTexture(texture)) {
                if (samples.length < maxSamples) {
                    samples.push({
                        name: typeof current.name === "string" ? current.name : "",
                        ctor: current.constructor && current.constructor.name ? current.constructor.name : "",
                        textureValid: !!texture,
                        hasBaseTexture: !!(texture && texture.baseTexture),
                        hasUvs: !!(texture && texture._uvs && texture._uvs.uvsFloat32),
                        origWidth: texture && texture.orig ? Number(texture.orig.width) : null,
                        origHeight: texture && texture.orig ? Number(texture.orig.height) : null
                    });
                }
                current.texture = PIXI.Texture.WHITE;
                repaired += 1;
            }
            const children = Array.isArray(current.children) ? current.children : null;
            if (!children) continue;
            for (let i = children.length - 1; i >= 0; i--) {
                stack.push(children[i]);
            }
        }
        return { repaired, samples };
    }

    function summarizePixiTexture(texture) {
        if (!texture) {
            return {
                exists: false
            };
        }
        const baseTexture = texture.baseTexture || null;
        const orig = texture.orig || null;
        return {
            exists: true,
            hasBaseTexture: !!baseTexture,
            baseTextureValid: !!(baseTexture && baseTexture.valid),
            hasUvs: !!(texture._uvs && texture._uvs.uvsFloat32),
            origWidth: orig && Number.isFinite(orig.width) ? Number(orig.width) : null,
            origHeight: orig && Number.isFinite(orig.height) ? Number(orig.height) : null,
            frameWidth: texture.frame && Number.isFinite(texture.frame.width) ? Number(texture.frame.width) : null,
            frameHeight: texture.frame && Number.isFinite(texture.frame.height) ? Number(texture.frame.height) : null
        };
    }

    function summarizePixiDisplayObject(displayObj) {
        if (!displayObj) return null;
        const parent = displayObj.parent || null;
        const scale = displayObj.scale || null;
        const anchor = displayObj.anchor || null;
        return {
            ctor: displayObj.constructor && displayObj.constructor.name ? displayObj.constructor.name : "",
            name: typeof displayObj.name === "string" ? displayObj.name : "",
            destroyed: displayObj.destroyed === true,
            visible: displayObj.visible !== false,
            renderable: displayObj.renderable !== false,
            x: Number.isFinite(displayObj.x) ? Number(displayObj.x) : null,
            y: Number.isFinite(displayObj.y) ? Number(displayObj.y) : null,
            scaleX: scale && Number.isFinite(scale.x) ? Number(scale.x) : null,
            scaleY: scale && Number.isFinite(scale.y) ? Number(scale.y) : null,
            anchorX: anchor && Number.isFinite(anchor.x) ? Number(anchor.x) : null,
            anchorY: anchor && Number.isFinite(anchor.y) ? Number(anchor.y) : null,
            alpha: Number.isFinite(displayObj.alpha) ? Number(displayObj.alpha) : null,
            parentName: parent && typeof parent.name === "string" ? parent.name : "",
            roadTextureCacheKey: (typeof displayObj._roadTextureCacheKey === "string") ? displayObj._roadTextureCacheKey : "",
            texture: summarizePixiTexture(displayObj.texture)
        };
    }

    function getWizardBodyFrameIndex(wizard, ctx = {}) {
        if (!wizard) return 0;
        const visualSpeed = Math.hypot(
            Number(wizard?.movementVector?.x) || 0,
            Number(wizard?.movementVector?.y) || 0
        );
        const activeAuras = Array.isArray(wizard.activeAuras)
            ? wizard.activeAuras
            : (typeof wizard.activeAura === "string" ? [wizard.activeAura] : []);
        const speedAuraAnimationMultiplier = activeAuras.includes("speed") ? 0.5 : 1;
        const wizardDead = !!wizard.dead;
        const isVisuallyMoving = !wizardDead && (!!wizard.moving || visualSpeed > 0.02);
        const rowIndex = Number.isInteger(wizard.lastDirectionRow)
            ? ((wizard.lastDirectionRow % 12) + 12) % 12
            : 0;

        let frameIndex = rowIndex * 9;
        if (!wizardDead && wizard.isJumping) {
            frameIndex = rowIndex * 9 + 2;
        } else if (isVisuallyMoving) {
            const speedRatio = (wizard.speed > 0)
                ? (visualSpeed / wizard.speed)
                : 0;
            const nowMs = Number.isFinite(ctx.renderNowMs) ? ctx.renderNowMs : performance.now();
            const simFrameRate = Number.isFinite(ctx.frameRate) ? ctx.frameRate : 60;
            const animSpeed = Number.isFinite(wizard.animationSpeedMultiplier)
                ? wizard.animationSpeedMultiplier
                : 1;
            const simTicks = (nowMs / 1000) * simFrameRate;
            const animFrame = Math.floor(simTicks * animSpeed * speedAuraAnimationMultiplier * speedRatio / 2) % 8;
            const effectiveAnimFrame = wizard.isMovingBackward ? (7 - animFrame) : animFrame;
            frameIndex = rowIndex * 9 + 1 + effectiveAnimFrame;
        }

        return frameIndex;
    }

    function getWizardWaterBodyRenderState(immersion = null, bodyWorldHeight = 1, viewscale = 1) {
        const inWater = !!(immersion && immersion.inWater === true);
        const submergedDepth = inWater && Number.isFinite(Number(immersion.submergedDepth))
            ? Math.max(0, Number(immersion.submergedDepth))
            : 0;
        const heightWorld = Number.isFinite(Number(bodyWorldHeight))
            ? Math.max(1e-6, Math.abs(Number(bodyWorldHeight)))
            : 1;
        const maxHiddenRatio = Math.max(0, Math.min(1, 1 - WIZARD_WATER_MIN_VISIBLE_BODY_RATIO));
        const hiddenRatio = inWater
            ? Math.max(0, Math.min(maxHiddenRatio, submergedDepth / heightWorld))
            : 0;
        const visibleRatio = Math.max(WIZARD_WATER_MIN_VISIBLE_BODY_RATIO, 1 - hiddenRatio);
        const screenHeight = Number.isFinite(Number(viewscale)) ? Math.max(0, Math.abs(Number(viewscale))) : 0;
        return {
            inWater,
            visibleRatio,
            hiddenRatio,
            hiddenScreenPx: hiddenRatio * screenHeight,
            submergedDepth,
            distanceToShore: immersion && Number.isFinite(Number(immersion.distanceToShore))
                ? Math.max(0, Number(immersion.distanceToShore))
                : 0
        };
    }

    function buildPixiDisplayObjectCrashSignature(summary) {
        if (!summary) return "unknown";
        const tex = summary.texture || {};
        return [
            summary.ctor || "",
            summary.name || "",
            summary.parentName || "",
            tex.hasBaseTexture ? "bt1" : "bt0",
            tex.hasUvs ? "uv1" : "uv0",
            tex.origWidth === null ? "owx" : `ow${tex.origWidth}`,
            tex.origHeight === null ? "ohx" : `oh${tex.origHeight}`
        ].join("|");
    }

    function syncRoadRenderSpriteTextureRetention(sprite, road) {
        if (!sprite) return;
        const RoadClass = (typeof global !== "undefined" && global && global.Road) ? global.Road : null;
        const nextKey = (road && typeof road._roadTextureCacheKey === "string" && road._roadTextureCacheKey.length > 0)
            ? road._roadTextureCacheKey
            : "";
        const currentKey = (typeof sprite._roadTextureCacheKey === "string") ? sprite._roadTextureCacheKey : "";
        if (currentKey === nextKey) return;
        if (currentKey && RoadClass && typeof RoadClass._releaseTextureCacheEntry === "function") {
            RoadClass._releaseTextureCacheEntry(currentKey);
        }
        sprite._roadTextureCacheKey = "";
        if (nextKey && RoadClass && typeof RoadClass._retainTextureCacheEntry === "function") {
            RoadClass._retainTextureCacheEntry(nextKey);
            sprite._roadTextureCacheKey = nextKey;
        }
    }

    function installPixiSpriteRenderDiagnostics() {
        if (typeof PIXI === "undefined" || !PIXI || !PIXI.Sprite || !PIXI.Sprite.prototype) return;
        if (PIXI.Sprite.prototype._survivorTextureDiagInstalled === true) return;
        PIXI.Sprite.prototype._survivorTextureDiagInstalled = true;
        const loggedCrashSignatures = new Set();

        const originalCalculateVertices = PIXI.Sprite.prototype.calculateVertices;
        if (typeof originalCalculateVertices === "function") {
            PIXI.Sprite.prototype.calculateVertices = function survivorDiagnoseCalculateVertices(...args) {
                try {
                    return originalCalculateVertices.apply(this, args);
                } catch (err) {
                    const message = err && err.message ? String(err.message) : "";
                    const textureState = summarizePixiDisplayObject(this);
                    const signature = buildPixiDisplayObjectCrashSignature(textureState);
                    if (isPixiSpriteCrashDiagnosticsEnabled() && !loggedCrashSignatures.has(signature)) {
                        loggedCrashSignatures.add(signature);
                        console.error("[pixi sprite calculateVertices crash]", {
                            message,
                            signature,
                            sprite: textureState
                        });
                        try {
                            console.error("[pixi sprite calculateVertices crash json]", JSON.stringify({
                                message,
                                signature,
                                sprite: textureState
                            }));
                        } catch (_jsonErr) {
                            // ignore JSON serialization failures
                        }
                    }
                    if (!isRenderablePixiTexture(this.texture)) {
                        this.texture = PIXI.Texture.WHITE;
                        try {
                            return originalCalculateVertices.apply(this, args);
                        } catch (_retryErr) {
                            // fall through to original error
                        }
                    }
                    throw err;
                }
            };
        }
    }

    class RenderingImpl {
        constructor() {
            installPixiSpriteRenderDiagnostics();
            const CameraCtor = global.RenderingCamera;
            const LayersCtor = global.RenderingLayers;
            const MazeModeCtor = global.RenderingMazeMode;
            this.camera = new CameraCtor();
            this.layers = new LayersCtor();
            this.mazeModeRenderer = (MazeModeCtor && typeof MazeModeCtor === "function")
                ? new MazeModeCtor()
                : null;
            this.mazeModeOverlayActive = false;
            this.mazeModeJustActivatedFrame = false;
            this.mazeModeActivatedAtMs = null;
            this.lastMazeModeSettingEnabled = null;
            this.mazeModeSuppressRevealAnimation = false;
            this.initialized = false;
            this.wizardSprite = null;
            this.wizardGhostSprite = null;
            this.wizardShadowSprite = null;
            this.wizardShadowProxy = null;
            this.wizardDrowningHatMaskGraphics = null;
            this.wizardWaterCroppedTextureCache = new WeakMap();
            this.placeObjectPreviewSprite = null;
            this.placeObjectPreviewTexturePath = "";
            this.placeObjectPreviewDisplayObject = null;
            this.placeObjectPreviewItem = null;
            this.placeObjectCenterSnapGuideGraphics = null;
            this.powerupPlacementPreviewSprite = null;
            this.powerupPlacementPreviewTexturePath = "";
            this.powerupPlacementPreviewDisplayObject = null;
            this.powerupPlacementPreviewItem = null;
            this.wallPlacementPreviewGraphics = null;
            this.floorEditorPolygonOverlayGraphics = null;
            this.terrainPolygonDiagnosticGraphics = null;
            this.layerCutawayDebugGraphics = null;
            this.buildingCutawayGroundMaskGraphics = null;
            this.buildingCutawayGroundMaskMeshes = new Map();
            this.buildingCutawayGroundMaskState = null;
            this.buildingCutawayCompositeRenderTexture = null;
            this.buildingCutawayCompositeDataRenderTexture = null;
            this.buildingCutawayCompositeSprite = null;
            this.buildingCutawayCompositeTexture = null;
            this.buildingCutawayCompositeProxy = null;
            this.buildingCutawayCompositeSize = null;
            this.buildingCutawayCompositeCache = null;
            this.buildingCutawayCompositeBillboardState = null;
            this.prototypeBuildingExteriorDepthState = null;
            this.prototypeBuildingExteriorTransparentDepthState = null;
            this.buildingCutawayCompositePendingBaseTextures = new Set();
            this.layerTransitionSnapshotRenderTexture = null;
            this.layerTransitionSnapshotSprite = null;
            this.layerTransitionSnapshotSize = null;
            this.layerTransitionSnapshot = null;
            this.prototypeBuildingInteriorFloorSnapshotRenderTexture = null;
            this.prototypeBuildingInteriorFloorSnapshotSprite = null;
            this.prototypeBuildingInteriorFloorSnapshotSize = null;
            this.prototypeBuildingInteriorFloorSnapshot = null;
            this.buildingCutawayEntryTransitions = new Map();
            this.buildingInteriorFloorCutawayLevels = new Map();
            this.buildingInteriorFloorTransitions = new Map();
            this.buildingDoorwayPresentationTransition = null;
            this.lastBuildingDoorwayPresentationState = null;
            this.buildingInteriorForegroundState = null;
            this.buildingInteriorOverlayContainer = null;
            this.buildingInteriorOverlayFloorMeshes = new Map();
            this.buildingInteriorPresentationUiState = null;
            this.buildingInteriorTransparentWindowDepthState = null;
            this.buildingInteriorDepthClearObject = null;
            this.buildingInteriorView = null;
            this._buildingInteriorForegroundPromotions = [];
            this._cutawayInteriorOverlayWallSections = new Set();
            this._layerCutawayFrameId = 0;
            this._drawFrameId = 0;
            this._activeDrawFrameId = 0;
            this._layerCutawayPreparedDrawFrameId = 0;
            this.prototypeSectionSeamGraphics = null;
            this.hexGridTexture = null;
            this.hexGridSprites = [];
            this.hexGridContainer = null;
            this.hexGridPickerBackdrop = null;
            this.groundTileContainer = null;
            this.hexGridLastViewscale = 0;
            this.hexGridLastXyratio = 0;
            this.groundSpriteByNodeKey = new Map();
            this.groundVisibleNodeKeys = new Set();
            this.groundSpritePool = [];
            this.floorVisualContainer = null;
            this.roadPathDepthContainer = null;
            this.floorVisualMeshByKey = new Map();
            this.floorVisualVisibleKeys = new Set();
            this.floorVisualCaveTexture = null;
            this.floorVisualTextureByPath = new Map();
            this.floorVisualTextureConfigCache = null;
            this.floorVisualTextureConfigPromise = null;
            this.floorVisualDepthState = null;
            this.grassDepthRenderer = null;
            this.roadPathDepthState = null;
            this.stairRenderObjectById = new Map();
            this.stairMeshById = new Map();
            this.stairDepthState = null;
            this.losShadowDepthMesh = null;
            this.losShadowDepthMaskGraphics = null;
            this.losShadowDepthState = null;
            this.level0GroundSurfaceCache = new Map();
            this.level0GroundSurfaceBakeNodeCache = new Map();
            this.level0GroundSurfaceChunkCache = new Map();
            this.level0GroundSurfaceChunkTick = 0;
            this.level0GroundCoverageVersion = 1;
            this.level0GroundSurfaceChunkBuildsThisFrame = 0;
            this.level0AnimatedWaterChunkEntryCache = new Map();
            this.level0AnimatedWaterChunkTick = 0;
            this.floorVisualChunkClipCache = new Map();
            this.floorVisualChunkClipTick = 0;
            this.level0TerrainPolygonEntryCache = new Map();
            this.level0TerrainPolygonEntryTick = 0;
            this.bakedLevel0SectionKeys = new Set();
            this.bakedLevel0SectionSignature = "";
            this.level0GroundSurfacePendingLoads = new Set();
            this.roadSpriteByObject = new Map();
            this.roadPathMeshByObject = new Map();
            this.lastSectionInputItems = [];
            this.activeObjectDisplayObjects = new Set();
            this.activeDepthBillboardMeshes = new Set();
            this.activeDepthBillboardItems = new Set();
            this.activeAnimalHealthBarItems = new Set();
            this.activeTreeHealthBarItems = new Set();
            this.activePowerupDisplayObjects = new Set();
            this.activeProjectileDisplayObjects = new Set();
            this.scriptMessageTextObjects = new Map();
            this.scriptScreenColorOverlay = null;
            this.scriptScreenColorOverlayGraphics = null;
            this.scriptScreenColorOverlayRefreshHandle = null;
            this.wizardThoughtBubble = null;
            this.wizardThoughtBubbleSprite = null;
            this.wizardThoughtBubbleTexture = null;
            this.wizardThoughtBubbleRefreshHandle = null;
            this.wizardThoughtBubblePendingBaseTextures = new Set();
            this.pickRenderItems = [];
            this._buildingInteriorPickerFrame = null;
            this.losShadowGraphics = null;
            this.currentLosState = null;
            this.lastLosWizardX = null;
            this.lastLosWizardY = null;
            this.lastLosWizardLayer = null;
            this.lastLosWizardBaseZ = null;
            this.lastLosFacingAngle = null;
            this.lastLosCandidateCount = -1;
            this.lastLosCandidateHash = 0;
            this.lastLosComputeAtMs = 0;
            this.nextLosObjectId = 1;
            this.drawPassProfiler = {
                startMs: null,
                deadlineMs: null,
                frameCount: 0,
                totalFrameMs: 0,
                maxFrameMs: 0,
                sections: Object.create(null),
                metrics: Object.create(null),
                printed: false
            };
            this.currentFrameDrawSections = Object.create(null);
            this.groundTileProfiler = {
                startMs: null,
                deadlineMs: null,
                frameCount: 0,
                printed: false,
                totals: {
                    totalMs: 0,
                    activeKeyBuildMs: 0,
                    visibleSetMs: 0,
                    createSpriteMs: 0,
                    parentAttachMs: 0,
                    textureResolveMs: 0,
                    positionSizeMs: 0,
                    cleanupMs: 0
                },
                counts: {
                    visibleNodes: 0,
                    createdSprites: 0,
                    attachedSprites: 0,
                    cleanedSprites: 0,
                    evictedSprites: 0,
                    reusedSprites: 0
                }
            };
            this._lastTextureSanitizerLogAtMs = 0;
            const ScenePickerCtor = global.RenderingScenePicker;
            this.scenePicker = (ScenePickerCtor && typeof ScenePickerCtor === "function")
                ? new ScenePickerCtor()
                : null;
        }

        resetPickRenderItems() {
            this.pickRenderItems.length = 0;
            if (this._buildingInteriorPickerFrame) {
                this._buildingInteriorPickerFrame.active = false;
                this._buildingInteriorPickerFrame.buildingItems.clear();
                this._buildingInteriorPickerFrame.allowedItems.clear();
                this._buildingInteriorPickerFrame.triggers = [];
                this._buildingInteriorPickerFrame.mapRef = null;
            }
        }

        addPickRenderItem(item, displayObj, options = null) {
            if (!item || !displayObj) return;
            if (item.gone || item.vanishing) return;
            if (!this.shouldIncludeBuildingInteriorPickRenderItem(item)) return;
            const opts = options && typeof options === "object" ? options : {};
            const forceInclude = !!opts.forceInclude;
            if (!forceInclude && !displayObj.visible) return;
            if (!displayObj.parent && !forceInclude) return;
            this.pickRenderItems.push({ item, displayObj, forceInclude });
        }

        getGroundHitboxPickProxyForItem(item, layerRank = 1) {
            if (!item) return null;
            const hitbox = item.shadowBox || item.touchBox || item.hitbox || null;
            if (!hitbox) return null;
            if (!(this.groundHitboxPickProxyByObject instanceof Map)) {
                this.groundHitboxPickProxyByObject = new Map();
            }
            let proxy = this.groundHitboxPickProxyByObject.get(item) || null;
            if (!proxy) {
                proxy = {
                    name: "renderingGroundHitboxPickProxy",
                    visible: true,
                    destroyed: false,
                    _pickGroundHitboxProxy: true
                };
                this.groundHitboxPickProxyByObject.set(item, proxy);
            }
            proxy.visible = true;
            proxy.destroyed = false;
            proxy._pickLayerRank = Number.isFinite(layerRank) ? Number(layerRank) : 1;
            return proxy;
        }

        getBuildingInteriorPickerFrameState() {
            if (!this._buildingInteriorPickerFrame) {
                this._buildingInteriorPickerFrame = {
                    active: false,
                    buildingItems: new Set(),
                    allowedItems: new Set(),
                    triggers: [],
                    mapRef: null
                };
            }
            return this._buildingInteriorPickerFrame;
        }

        shouldIncludeBuildingInteriorPickRenderItem(item) {
            const state = this._buildingInteriorPickerFrame;
            if (!state || state.active !== true || !item) return true;
            if (state.allowedItems.has(item)) return true;
            if (this.shouldAllowPrototypeBuildingInteriorLiveRenderItem(item, state, state.mapRef || global.map || null)) return true;
            if (state.buildingItems.has(item)) return false;
            const mapRef = state.mapRef || global.map || null;
            const refs = this.collectFloorRefsForRenderItem(item, mapRef);
            if (this.hasBuildingInteriorFloorRefs(refs)) {
                const triggers = Array.isArray(state.triggers) ? state.triggers : [];
                for (let i = 0; i < triggers.length; i++) {
                    const trigger = triggers[i];
                    if (!trigger || !trigger.building || !trigger.activeInteriorRegion) continue;
                    if (!this.renderItemMatchesCutawayTrigger(item, trigger, mapRef)) continue;
                    return this.renderItemMatchesActiveBuildingInteriorRegion(item, trigger, mapRef, refs);
                }
            }
            return true;
        }

        getBuildingInteriorEntryLayer(item, entry = null, fallback = 0) {
            const mountedWallLayer = this.getMountedWallLayerIndexForItem(item, fallback);
            if (Number.isFinite(mountedWallLayer)) {
                return this.getLayerIndexFromValue(mountedWallLayer, fallback);
            }
            if (Number.isFinite(entry && entry.level)) {
                return this.getLayerIndexFromValue(entry.level, fallback);
            }
            if (item && item.type === "roof") {
                return this.getLayerIndexForRoof(item, fallback);
            }
            return this.getLayerIndexForObject(item, fallback);
        }

        prepareBuildingInteriorPickerFrame(ctx, cutawayState, foregroundPlan = null) {
            const state = this.getBuildingInteriorPickerFrameState();
            state.active = false;
            state.buildingItems.clear();
            state.allowedItems.clear();
            state.triggers = [];
            state.mapRef = null;

            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            if (triggers.length === 0) return state;

            let hasActiveInterior = false;
            const plan = foregroundPlan || this.buildBuildingInteriorRenderPlan(ctx, cutawayState);
            if (plan && plan.items instanceof Set) {
                plan.items.forEach(item => {
                    if (item) state.allowedItems.add(item);
                });
            }

            const mapRef = (ctx && ctx.map) || global.map || null;
            state.triggers = triggers;
            state.mapRef = mapRef;
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            const dynamicCharacters = this.getBuildingInteriorDynamicCharacterCandidates(ctx, wizardRef);

            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const activeRegion = trigger && trigger.activeInteriorRegion;
                if (!activeRegion) continue;
                const renderCache = trigger.renderCache || null;
                if (!renderCache) {
                    throw new Error("missing building interior picker render cache");
                }
                hasActiveInterior = true;
                const activeLevel = this.getLayerIndexFromValue(activeRegion.level, 0);
                const entries = Array.isArray(renderCache.renderItems) ? renderCache.renderItems : [];
                for (let j = 0; j < entries.length; j++) {
                    const entry = entries[j];
                    const item = entry && entry.item;
                    if (!item || item.gone || item.vanishing) continue;
                    state.buildingItems.add(item);
                    const entryLevel = this.getBuildingInteriorEntryLayer(item, entry, activeLevel);
                    if (entryLevel === activeLevel) {
                        const refs = Array.isArray(entry && entry.refs)
                            ? entry.refs
                            : this.collectFloorRefsForRenderItem(item, mapRef);
                        if (
                            this.isBuildingCutawayStructuralItem(item) ||
                            !this.hasBuildingInteriorFloorRefs(refs) ||
                            this.renderItemMatchesActiveBuildingInteriorRegion(item, trigger, mapRef, refs)
                        ) {
                            state.allowedItems.add(item);
                        }
                    }
                }
                for (let j = 0; j < dynamicCharacters.length; j++) {
                    const character = dynamicCharacters[j];
                    if (!character || character.gone || character.vanishing) continue;
                    const characterLevel = this.getLayerIndexForObject(character, activeLevel);
                    if (characterLevel !== activeLevel) {
                        if (this.renderItemMatchesBuildingInteriorRegion(renderCache, character, characterLevel, mapRef)) {
                            state.buildingItems.add(character);
                        }
                        continue;
                    }
                    if (this.renderItemMatchesBuildingInteriorRegion(renderCache, character, characterLevel, mapRef)) {
                        state.buildingItems.add(character);
                        state.allowedItems.add(character);
                    }
                }
                this.preparePrototypeBuildingInteriorBitmapPickerOverlay(ctx, cutawayState, trigger, state, mapRef);
            }
            state.active = hasActiveInterior;
            return state;
        }

        preparePrototypeBuildingInteriorBitmapPickerOverlay(ctx, cutawayState, trigger, pickerState, mapRef = null) {
            if (!trigger || !trigger.activeInteriorRegion || !this.isPrototypeBuildingCutawayTrigger(trigger)) return 0;
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            if (!map || typeof map.getPrototypeBuildingInteriorBitmap !== "function") return 0;
            const placement = trigger.building && trigger.building._prototypeBuildingPlacement;
            if (!placement || !placement.id) return 0;
            const floorId = this.getPrototypeBuildingInteriorSourceFloorId(trigger, { required: false });
            if (!floorId || !this.isPrototypeBuildingInteriorBitmapSourceFloorId(floorId)) return 0;
            const cache = map.getPrototypeBuildingInteriorBitmap(placement.id, floorId);
            if (!cache || cache.status !== "ready") return 0;
            this.ensurePrototypeBuildingInteriorBitmapCoverageMetadata(ctx, trigger, cache);
            if (!(cache.coveredObjectSet instanceof Set) || cache.coveredObjectSet.size === 0) return 0;
            if (!(cache.objectRenderSignatures instanceof Map) || !(cache.objectPickerSignatures instanceof Map)) {
                throw new Error(`prototype building interior ${cache.id || placement.id} coverage is missing object signatures`);
            }
            const region = trigger.activeInteriorRegion;
            const objects = this.collectBuildingInteriorFloorRenderItems(ctx, region, map, trigger);
            let added = 0;
            for (let i = 0; i < objects.length; i++) {
                const item = objects[i];
                if (!item || item.gone || item.vanishing) continue;
                if (!cache.coveredObjectSet.has(item)) continue;
                if (
                    cache.objectRenderSignatures.get(item) !== this.getFloorObjectRenderSignature(item) ||
                    cache.objectPickerSignatures.get(item) !== this.getFloorObjectPickerSignature(item)
                ) {
                    continue;
                }
                if (pickerState && pickerState.buildingItems instanceof Set) pickerState.buildingItems.add(item);
                if (pickerState && pickerState.allowedItems instanceof Set) pickerState.allowedItems.add(item);
                if (this.addPrototypeBuildingInteriorBitmapPickerObject(ctx, item, trigger, cache, map)) {
                    added += 1;
                }
            }
            return added;
        }

        addPrototypeBuildingInteriorBitmapPickerObject(ctx, item, trigger, cache, mapRef = null) {
            if (!item || item.gone || item.vanishing) return false;
            if (!this.camera || typeof item.updateDepthBillboardMesh !== "function") return false;
            if (typeof item.ensureSprite === "function") item.ensureSprite();
            if (!item.pixiSprite) return false;
            const region = trigger && trigger.activeInteriorRegion;
            const activeLevel = this.getLayerIndexFromValue(region && region.level, this.getLayerIndexForObject(item, 0));
            item._renderLayerIndex = activeLevel;
            item._renderLayerFadeAlpha = 1;
            item._renderLayerAlpha = 1;
            item._renderLayerBaseZ = this.getLayerBaseZForObject(item, activeLevel);
            const baseDepthBias = Number.isFinite(Number(item.renderDepthOffset)) ? Number(item.renderDepthOffset) : 0;
            item._renderDepthBias = baseDepthBias + BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS;
            if (
                typeof PIXI !== "undefined" &&
                PIXI.Sprite &&
                this.isPlacedObjectEntity(item) &&
                item.rotationAxis !== "spatial" &&
                item.pixiSprite instanceof PIXI.Sprite
            ) {
                const lodTexturePath = this.resolvePlacedObjectLodTexturePath(item);
                if (typeof lodTexturePath === "string" && lodTexturePath.length > 0 && lodTexturePath !== item._activeLodTexturePath) {
                    item.pixiSprite.texture = PIXI.Texture.from(lodTexturePath);
                    item._activeLodTexturePath = lodTexturePath;
                }
            }
            this.applySpriteTransform(item);
            const mesh = item.updateDepthBillboardMesh(ctx, this.camera, { alphaCutoff: TREE_ALPHA_CUTOFF });
            if (!mesh) return false;
            if (mesh.parent && typeof mesh.parent.removeChild === "function") {
                mesh.parent.removeChild(mesh);
            }
            mesh.visible = false;
            if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                mesh.renderable = false;
            }
            mesh.alpha = 0;
            item._prototypeInteriorBitmapPickerMesh = mesh;
            this.addPickRenderItem(item, mesh, { forceInclude: true });
            return true;
        }

        getLayerIndexFromValue(value, fallback = 0) {
            const n = Number(value);
            if (!Number.isFinite(n)) return Number(fallback) || 0;
            return Math.round(n);
        }

        getLayerIndexForNode(node) {
            if (!node) return 0;
            if (Number.isFinite(node.traversalLayer)) return this.getLayerIndexFromValue(node.traversalLayer, 0);
            if (Number.isFinite(node.level)) return this.getLayerIndexFromValue(node.level, 0);
            return 0;
        }

        getLayerBaseZForLevel(level) {
            return this.getLayerIndexFromValue(level, 0) * FLOOR_LAYER_DEFAULT_HEIGHT_UNITS;
        }

        getLayerBaseZForNode(node) {
            if (node && Number.isFinite(node.baseZ)) return Number(node.baseZ);
            return this.getLayerBaseZForLevel(this.getLayerIndexForNode(node));
        }

        getLayerIndexForObject(item, fallback = 0) {
            if (!item) return this.getLayerIndexFromValue(fallback, 0);
            const membership = this.normalizeRenderFloorMembership(item._floorMembership) ||
                this.normalizeRenderFloorMembership(item.floorMembership);
            if (membership && membership.ownerType === "building") {
                const floorSupportApi = global && global.FloorSupport ? global.FloorSupport : null;
                const mapRef = global && global.map ? global.map : null;
                if (floorSupportApi && typeof floorSupportApi.resolvePrototypeBuildingFloorFragment === "function") {
                    const fragment = floorSupportApi.resolvePrototypeBuildingFloorFragment(mapRef, membership, { required: false });
                    if (fragment && Number.isFinite(Number(fragment.level))) {
                        return this.getLayerIndexFromValue(fragment.level, fallback);
                    }
                }
                if (Number.isFinite(Number(membership.level))) {
                    return this.getLayerIndexFromValue(membership.level, fallback);
                }
            }
            if (item.type === "wallSection" && item._prototypeBuildingPlacementId && Number.isFinite(item.traversalLayer)) {
                return this.getLayerIndexFromValue(item.traversalLayer, fallback);
            }
            if (item.type === "wallSection" && Number.isFinite(item.bottomZ)) {
                const layerHeight = (typeof FLOOR_LAYER_DEFAULT_HEIGHT_UNITS !== "undefined" && Number.isFinite(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS) && FLOOR_LAYER_DEFAULT_HEIGHT_UNITS > 0)
                    ? Number(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS)
                    : 3;
                return this.getLayerIndexFromValue(Math.floor((Number(item.bottomZ) / layerHeight) + 1e-6), fallback);
            }
            if (Number.isFinite(item.traversalLayer)) {
                return this.getLayerIndexFromValue(item.traversalLayer, fallback);
            }
            if (Number.isFinite(item.level)) {
                return this.getLayerIndexFromValue(item.level, fallback);
            }
            if (Number.isFinite(item.currentLayer)) {
                return this.getLayerIndexFromValue(item.currentLayer, fallback);
            }
            return this.getLayerIndexFromValue(fallback, 0);
        }

        getLayerBaseZForObject(item, fallback = 0) {
            if (this.isCharacterRenderItem(item)) {
                return 0;
            }
            const support = item && item.currentMovementSupport && typeof item.currentMovementSupport === "object"
                ? item.currentMovementSupport
                : null;
            if (support && Number.isFinite(Number(support.baseZ))) return Number(support.baseZ);
            if (item && Number.isFinite(Number(item.currentLayerBaseZ))) return Number(item.currentLayerBaseZ);
            if (item && Number.isFinite(Number(item._floorBaseZ))) return Number(item._floorBaseZ);
            if (item && item.node && Number.isFinite(Number(item.node.baseZ))) return Number(item.node.baseZ);
            const fragmentId = item && typeof item.fragmentId === "string" && item.fragmentId.length > 0
                ? item.fragmentId
                : (item && typeof item.node?.fragmentId === "string" ? item.node.fragmentId : "");
            const mapRef = (item && item.map) || this.map || (typeof global !== "undefined" ? global.map : null);
            const fragment = fragmentId && mapRef && mapRef.floorsById instanceof Map
                ? mapRef.floorsById.get(fragmentId) || null
                : null;
            if (fragment && Number.isFinite(Number(fragment.nodeBaseZ))) return Number(fragment.nodeBaseZ);
            if (item && Number.isFinite(Number(item._renderLayerBaseZ))) return Number(item._renderLayerBaseZ);
            return this.getLayerBaseZForLevel(this.getLayerIndexForObject(item, fallback));
        }

        getPlacementLayerBaseZForWizard(wizardRef = null, mapRef = null, placementLayer = 0, referencePoint = null) {
            const layer = this.getLayerIndexFromValue(placementLayer, 0);
            const wizardLayer = this.getLayerIndexFromValue(
                wizardRef && Number.isFinite(wizardRef.currentLayer)
                    ? wizardRef.currentLayer
                    : (wizardRef && Number.isFinite(wizardRef.traversalLayer) ? wizardRef.traversalLayer : layer),
                layer
            );
            const support = wizardRef && wizardRef.currentMovementSupport && typeof wizardRef.currentMovementSupport === "object"
                ? wizardRef.currentMovementSupport
                : null;
            const supportLayer = support && Number.isFinite(Number(support.layer))
                ? this.getLayerIndexFromValue(support.layer, wizardLayer)
                : wizardLayer;
            if (support && supportLayer === layer && Number.isFinite(Number(support.baseZ))) {
                return Number(support.baseZ);
            }
            if (wizardLayer === layer && wizardRef && Number.isFinite(Number(wizardRef.currentLayerBaseZ))) {
                return Number(wizardRef.currentLayerBaseZ);
            }
            const node = wizardRef && wizardRef.node && typeof wizardRef.node === "object" ? wizardRef.node : null;
            const nodeLayer = node ? this.getLayerIndexForNode(node) : null;
            if (node && nodeLayer === layer && Number.isFinite(Number(node.baseZ))) {
                return Number(node.baseZ);
            }
            const pointX = Number.isFinite(referencePoint && referencePoint.x)
                ? Number(referencePoint.x)
                : (wizardRef && Number.isFinite(wizardRef.x) ? Number(wizardRef.x) : NaN);
            const pointY = Number.isFinite(referencePoint && referencePoint.y)
                ? Number(referencePoint.y)
                : (wizardRef && Number.isFinite(wizardRef.y) ? Number(wizardRef.y) : NaN);
            if (mapRef && mapRef.floorsById instanceof Map && Number.isFinite(pointX) && Number.isFinite(pointY)) {
                let best = null;
                for (const fragment of mapRef.floorsById.values()) {
                    if (!fragment) continue;
                    const fragmentLevel = this.getLayerIndexFromValue(fragment.level, 0);
                    if (fragmentLevel !== layer) continue;
                    if (!Number.isFinite(Number(fragment.nodeBaseZ))) continue;
                    if (!isPointInsideFloorVisibilityFragment(fragment, pointX, pointY)) continue;
                    const baseZ = Number(fragment.nodeBaseZ);
                    if (!best || baseZ > best.baseZ) best = { baseZ };
                }
                if (best) return best.baseZ;
            }
            return this.getLayerBaseZForLevel(layer);
        }

        getRendererScreenSize(ctx = null) {
            const appRef = (ctx && ctx.app) || (typeof app !== "undefined" ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            const screen = appRef && appRef.screen ? appRef.screen : null;
            const width = Math.max(1, Math.round(
                Number(renderer && renderer.width) ||
                Number(screen && screen.width) ||
                Number(global && global.innerWidth) ||
                1
            ));
            const height = Math.max(1, Math.round(
                Number(renderer && renderer.height) ||
                Number(screen && screen.height) ||
                Number(global && global.innerHeight) ||
                1
            ));
            return { width, height };
        }

        getMountedWallLayerIndexForItem(item, fallback = 0) {
            if (!this.isWallMountedSpatialItem(item)) return null;
            const mountedSection = this.resolveMountedWallSectionForItem(item);
            if (!mountedSection || mountedSection.type !== "wallSection") return null;
            const wallBottomZ = Number.isFinite(mountedSection.bottomZ) ? Number(mountedSection.bottomZ) : null;
            if (!Number.isFinite(wallBottomZ)) return null;
            const layerHeight = (typeof FLOOR_LAYER_DEFAULT_HEIGHT_UNITS !== "undefined" && Number.isFinite(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS) && FLOOR_LAYER_DEFAULT_HEIGHT_UNITS > 0)
                ? Number(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS)
                : 3;
            // Wall bottoms can carry custom positive offsets; classify by floor band,
            // not nearest integer, so layer-1 walls (e.g. z=4.5) stay in layer 1.
            const derivedLayer = Math.floor((wallBottomZ / layerHeight) + 1e-6);
            return this.getLayerIndexFromValue(derivedLayer, fallback);
        }

        getLayerIndexForRoof(roof, fallback = 0) {
            if (!roof) return this.getLayerIndexFromValue(fallback, 0);
            if (Number.isFinite(roof.traversalLayer)) {
                return this.getLayerIndexFromValue(Number(roof.traversalLayer) + 1, fallback);
            }
            if (Number.isFinite(roof.level)) {
                return this.getLayerIndexFromValue(Number(roof.level) + 1, fallback);
            }
            // Most legacy roofs are level 0. Only infer negatives from z.
            const roofZ = Number.isFinite(roof.z)
                ? Number(roof.z)
                : (Number.isFinite(roof.heightFromGround) ? Number(roof.heightFromGround) : 0);
            if (
                roofZ > 0 &&
                (
                    (Array.isArray(roof.wallLoopSectionIds) && roof.wallLoopSectionIds.length > 0) ||
                    (Array.isArray(roof.vertices) && roof.vertices.length >= 3)
                )
            ) {
                const layerHeight = (typeof FLOOR_LAYER_DEFAULT_HEIGHT_UNITS !== "undefined" && Number.isFinite(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS) && FLOOR_LAYER_DEFAULT_HEIGHT_UNITS > 0)
                    ? Number(FLOOR_LAYER_DEFAULT_HEIGHT_UNITS)
                    : 3;
                return this.getLayerIndexFromValue(Math.floor((roofZ / layerHeight) + 1e-6) + 1, fallback);
            }
            if (roofZ < 0) {
                return Math.floor(roofZ / FLOOR_LAYER_DEFAULT_HEIGHT_UNITS);
            }
            return this.getLayerIndexFromValue(fallback, 0);
        }

        syncLayerTransitionState(ctx) {
            const wizard = ctx && ctx.wizard ? ctx.wizard : null;
            if (!wizard) return;
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : Date.now();
            const currentLayer = this.getLayerIndexFromValue(wizard.currentLayer, 0);

            if (!Number.isFinite(this._lastRenderedWizardLayer)) {
                this._lastRenderedWizardLayer = currentLayer;
            }

            // During a downward fall, reveal the lower layer as soon as the fall
            // starts so the player sees where they are falling.
            this._fallRevealLayer = null;
            this._fallRevealHiddenLayer = null;
            const fallState = wizard && wizard._floorFallState;
            if (fallState && fallState.active && Number.isFinite(fallState.targetLayer)) {
                const fromLayer = this.getLayerIndexFromValue(fallState.fromLayer, currentLayer);
                const toLayer = this.getLayerIndexFromValue(fallState.targetLayer, fromLayer);
                if (toLayer < fromLayer) {
                    this._fallRevealLayer = toLayer;
                    this._fallRevealHiddenLayer = fromLayer;
                    if (!fallState._layerRevealTransitionStarted) {
                        const durationMs = 500;
                        this.captureLayerTransitionSnapshot(ctx, {
                            fromLayer,
                            toLayer,
                            fadingLayer: fromLayer,
                            startedAtMs: nowMs,
                            durationMs
                        });
                        this._layerFadeTransition = {
                            fromLayer,
                            toLayer,
                            fadingLayer: fromLayer,
                            startedAtMs: nowMs,
                            durationMs
                        };
                        fallState._layerRevealTransitionStarted = true;
                        wizard._floorFallLayerRevealTransition = {
                            fromLayer,
                            toLayer,
                            startedAtMs: nowMs
                        };
                    }
                    this._lastRenderedWizardLayer = toLayer;
                }
            }

            const pending = wizard._pendingLayerTransition;
            if (pending && typeof pending === "object" && pending.active) {
                const fromLayer = this.getLayerIndexFromValue(pending.fromLevel, this._lastRenderedWizardLayer);
                const toLayer = this.getLayerIndexFromValue(pending.toLevel, currentLayer);
                const fallReveal = wizard._floorFallLayerRevealTransition;
                const alreadyRevealedByFall = !!(
                    fallReveal &&
                    this.getLayerIndexFromValue(fallReveal.fromLayer, fromLayer) === fromLayer &&
                    this.getLayerIndexFromValue(fallReveal.toLayer, toLayer) === toLayer
                );
                if (!alreadyRevealedByFall) {
                    const durationMs = Number.isFinite(pending.durationMs) ? Math.max(60, Number(pending.durationMs)) : 320;
                    this._layerFadeTransition = {
                        fromLayer,
                        toLayer,
                        fadingLayer: Math.max(fromLayer, toLayer),
                        startedAtMs: Number.isFinite(pending.startedAtMs) ? Number(pending.startedAtMs) : nowMs,
                        durationMs
                    };
                } else {
                    wizard._floorFallLayerRevealTransition = null;
                }
                pending.active = false;
                this._lastRenderedWizardLayer = toLayer;
            } else if (!Number.isFinite(this._fallRevealLayer) && currentLayer !== this._lastRenderedWizardLayer) {
                const fromLayer = this._lastRenderedWizardLayer;
                const toLayer = currentLayer;
                this._layerFadeTransition = {
                    fromLayer,
                    toLayer,
                    fadingLayer: Math.max(fromLayer, toLayer),
                    startedAtMs: nowMs,
                    durationMs: 320
                };
                this._lastRenderedWizardLayer = toLayer;
            }

            const active = this._layerFadeTransition;
            if (!active) return;
            const elapsedMs = nowMs - Number(active.startedAtMs || 0);
            if (!(elapsedMs < Number(active.durationMs || 0))) {
                this._layerFadeTransition = null;
            }
        }

        getLayerFadeMultiplier(level, nowMs = null) {
            const layer = this.getLayerIndexFromValue(level, 0);
            const active = this._layerFadeTransition;
            if (!active) {
                return Number.isFinite(this._fallRevealHiddenLayer) &&
                    layer === this.getLayerIndexFromValue(this._fallRevealHiddenLayer, 0)
                    ? 0
                    : 1;
            }
            if (layer !== this.getLayerIndexFromValue(active.fadingLayer, 0)) return 1;
            const currentNowMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const startedAtMs = Number(active.startedAtMs) || 0;
            const durationMs = Math.max(1, Number(active.durationMs) || 320);
            const progress = Math.max(0, Math.min(1, (currentNowMs - startedAtMs) / durationMs));
            const fromLayer = this.getLayerIndexFromValue(active.fromLayer, layer);
            const toLayer = this.getLayerIndexFromValue(active.toLayer, layer);
            if (toLayer > fromLayer) return progress;
            return 1 - progress;
        }

        isLayerTransitionSnapshotActive(nowMs = null) {
            const snapshot = this.layerTransitionSnapshot;
            if (!snapshot || snapshot.active !== true) return false;
            const currentNowMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const startedAtMs = Number(snapshot.startedAtMs) || 0;
            const durationMs = Math.max(1, Number(snapshot.durationMs) || 500);
            return (currentNowMs - startedAtMs) < durationMs;
        }

        isLayerTransitionSnapshotReplacingLayer(level, nowMs = null) {
            if (!this.isLayerTransitionSnapshotActive(nowMs)) return false;
            const snapshot = this.layerTransitionSnapshot;
            return this.getLayerIndexFromValue(level, 0) === this.getLayerIndexFromValue(snapshot && snapshot.fadingLayer, 0);
        }

        getLiveLayerFadeMultiplier(level, nowMs = null) {
            if (this.isLayerTransitionSnapshotReplacingLayer(level, nowMs)) return 0;
            return this.getLayerFadeMultiplier(level, nowMs);
        }

        isFallRevealHiddenLayer(level) {
            return Number.isFinite(this._fallRevealHiddenLayer) &&
                this.getLayerIndexFromValue(level, 0) === this.getLayerIndexFromValue(this._fallRevealHiddenLayer, 0);
        }

        getWizardVisualLayerIndex(wizardRef = null, fallback = 0) {
            const wizard = wizardRef || global.wizard || null;
            const currentLayer = this.getLayerIndexFromValue(
                wizard && Number.isFinite(wizard.currentLayer)
                    ? wizard.currentLayer
                    : (wizard && Number.isFinite(wizard.traversalLayer) ? wizard.traversalLayer : fallback),
                fallback
            );
            const fallState = wizard && wizard._floorFallState;
            if (fallState && fallState.active && Number.isFinite(fallState.targetLayer)) {
                const fromLayer = this.getLayerIndexFromValue(fallState.fromLayer, currentLayer);
                const toLayer = this.getLayerIndexFromValue(fallState.targetLayer, fromLayer);
                if (toLayer < fromLayer) return toLayer;
            }
            return currentLayer;
        }

        getWizardVisualLayerBaseZ(wizardRef = null, fallbackLayer = 0) {
            const wizard = wizardRef || global.wizard || null;
            const visualLayer = this.getWizardVisualLayerIndex(wizard, fallbackLayer);
            const physicalLayer = this.getLayerIndexFromValue(
                wizard && Number.isFinite(wizard.currentLayer) ? wizard.currentLayer : visualLayer,
                visualLayer
            );
            if (visualLayer !== physicalLayer) {
                return this.getLayerBaseZForLevel(visualLayer);
            }
            const support = wizard && wizard.currentMovementSupport && typeof wizard.currentMovementSupport === "object"
                ? wizard.currentMovementSupport
                : null;
            return Number.isFinite(support && support.baseZ)
                ? Number(support.baseZ)
                : Number.isFinite(wizard && wizard.currentLayerBaseZ)
                ? Number(wizard.currentLayerBaseZ)
                : this.getLayerBaseZForLevel(visualLayer);
        }

        getWizardContinuousWorldZ(wizardRef = null, fallbackBaseZ = 0) {
            const wizard = wizardRef || global.wizard || null;
            const support = wizard && wizard.currentMovementSupport && typeof wizard.currentMovementSupport === "object"
                ? wizard.currentMovementSupport
                : null;
            const baseZ = Number.isFinite(support && support.baseZ)
                ? Number(support.baseZ)
                : (Number.isFinite(wizard && wizard.currentLayerBaseZ)
                    ? Number(wizard.currentLayerBaseZ)
                    : (Number.isFinite(fallbackBaseZ) ? Number(fallbackBaseZ) : 0));
            if (support && support.type === "stair") {
                if (Number.isFinite(Number(support.continuousBaseZ))) {
                    return Number(support.continuousBaseZ);
                }
                if (Number.isFinite(Number(support.continuousLocalZ))) {
                    return baseZ + Number(support.continuousLocalZ);
                }
                if (Number.isFinite(Number(support.localZ))) {
                    return baseZ + Number(support.localZ);
                }
            }
            return baseZ + (Number.isFinite(Number(wizard && wizard.z)) ? Number(wizard.z) : 0);
        }

        getWizardStairRenderSurfaceLocalZ(support = null, mapRef = null, fallbackLocalZ = NaN) {
            if (!support || support.type !== "stair") return Number(fallbackLocalZ);
            const fallback = Number.isFinite(Number(fallbackLocalZ))
                ? Number(fallbackLocalZ)
                : (Number.isFinite(Number(support.localZ)) ? Number(support.localZ) : 0);
            const stair = support.stair || (
                mapRef &&
                mapRef.stairsById instanceof Map &&
                typeof support.stairId === "string"
                    ? mapRef.stairsById.get(support.stairId)
                    : null
            );
            if (!stair || typeof stair !== "object") {
                return Number.isFinite(Number(support.localZ)) ? Number(support.localZ) : fallback;
            }
            const lowerZ = Number(stair.lowerZ);
            const higherZ = Number(stair.higherZ);
            const baseZ = Number(support.baseZ);
            if (!Number.isFinite(lowerZ) || !Number.isFinite(higherZ) || !Number.isFinite(baseZ)) {
                return Number.isFinite(Number(support.localZ)) ? Number(support.localZ) : fallback;
            }
            const stepCount = Number.isFinite(Number(stair.stepCount))
                ? Math.max(1, Math.round(Number(stair.stepCount)))
                : 1;
            const treadIndex = Number.isFinite(Number(support.treadIndex))
                ? Math.max(0, Math.min(stepCount - 1, Math.round(Number(support.treadIndex))))
                : Math.max(0, Math.min(stepCount - 1, Math.floor(Math.max(0, Math.min(1, Number(support.upDown) || 0)) * stepCount)));
            let surfaceZ = NaN;
            if (stair.stairKind === "straight" || support.stairKind === "straight") {
                surfaceZ = lowerZ + ((higherZ - lowerZ) * ((treadIndex + 1) / stepCount));
            } else if (stair.stairKind === "treadPath" || support.stairKind === "treadPath") {
                surfaceZ = lowerZ + ((higherZ - lowerZ) * ((treadIndex + 1) / stepCount));
            }
            return Number.isFinite(surfaceZ)
                ? surfaceZ - baseZ
                : (Number.isFinite(Number(support.localZ)) ? Number(support.localZ) : fallback);
        }

        getWizardWaterImmersionState(mapRef = null, renderPos = null, wizardLayer = 0, wizard = null) {
            const dryState = {
                inWater: false,
                distanceToShore: 0,
                submergedDepth: 0,
                slope: WIZARD_WATER_DEPTH_SLOPE,
                maxDepth: WIZARD_WATER_MAX_DEPTH_UNITS
            };
            if (!mapRef) return dryState;
            if (typeof mapRef.getGroundTerrainWaterImmersionAtPoint !== "function") {
                throw new Error("wizard water rendering requires map water immersion query");
            }
            if (!renderPos || !Number.isFinite(renderPos.x) || !Number.isFinite(renderPos.y)) {
                throw new Error("wizard water rendering requires a finite render position");
            }
            const layer = Number.isFinite(Number(wizardLayer)) ? Math.round(Number(wizardLayer)) : 0;
            if (layer !== 0) return dryState;
            if (
                wizard &&
                typeof mapRef.isActorOnGroundBridge === "function" &&
                mapRef.isActorOnGroundBridge(wizard, renderPos.x, renderPos.y)
            ) {
                return dryState;
            }
            if (
                wizard &&
                typeof wizard.getFlyingSpellLevel === "function" &&
                wizard.getFlyingSpellLevel() >= 2
            ) {
                return dryState;
            }
            const immersion = mapRef.getGroundTerrainWaterImmersionAtPoint(renderPos.x, renderPos.y, {
                slope: WIZARD_WATER_DEPTH_SLOPE,
                maxDepth: WIZARD_WATER_MAX_DEPTH_UNITS,
                traversalLayer: layer
            });
            if (!immersion || immersion.inWater !== true) return dryState;
            return immersion;
        }

        getWizardWaterBodyRenderState(immersion = null, bodyWorldHeight = 1) {
            return getWizardWaterBodyRenderState(
                immersion,
                bodyWorldHeight,
                this.camera && Number.isFinite(this.camera.viewscale) ? this.camera.viewscale : 1
            );
        }

        getWizardWaterCroppedTexture(sourceTexture, visibleRatio = 1) {
            const originalTexture = sourceTexture && sourceTexture._wizardWaterSourceTexture
                ? sourceTexture._wizardWaterSourceTexture
                : sourceTexture;
            const ratio = Number.isFinite(Number(visibleRatio))
                ? Math.max(WIZARD_WATER_MIN_VISIBLE_BODY_RATIO, Math.min(1, Number(visibleRatio)))
                : 1;
            if (ratio >= 0.999) return originalTexture;
            if (
                typeof PIXI === "undefined" ||
                !PIXI ||
                typeof PIXI.Texture !== "function" ||
                typeof PIXI.Rectangle !== "function"
            ) {
                throw new Error("wizard water immersion requires Pixi texture frame support");
            }
            if (!originalTexture || !originalTexture.baseTexture) {
                throw new Error("wizard water immersion requires a source body texture");
            }
            const baseTexture = originalTexture.baseTexture;
            const baseW = Number(baseTexture.realWidth || baseTexture.width || 0);
            const baseH = Number(baseTexture.realHeight || baseTexture.height || 0);
            const frame = originalTexture.frame || originalTexture.orig || null;
            const frameX = Number(frame && frame.x);
            const frameY = Number(frame && frame.y);
            const frameW = Number(frame && frame.width);
            const frameH = Number(frame && frame.height);
            if (!(baseW > 0) || !(baseH > 0) || !(frameW > 0) || !(frameH > 0)) {
                throw new Error("wizard water immersion requires a loaded body texture frame");
            }
            const cache = this.wizardWaterCroppedTextureCache instanceof WeakMap
                ? this.wizardWaterCroppedTextureCache
                : (this.wizardWaterCroppedTextureCache = new WeakMap());
            let byRatio = cache.get(originalTexture);
            if (!byRatio) {
                byRatio = new Map();
                cache.set(originalTexture, byRatio);
            }
            const key = ratio.toFixed(3);
            const cached = byRatio.get(key);
            if (cached && cached.baseTexture === baseTexture) return cached;
            const croppedHeight = Math.max(1, Math.min(frameH, frameH * ratio));
            const croppedFrame = new PIXI.Rectangle(
                Number.isFinite(frameX) ? frameX : 0,
                Number.isFinite(frameY) ? frameY : 0,
                frameW,
                croppedHeight
            );
            const croppedTexture = new PIXI.Texture(baseTexture, croppedFrame);
            croppedTexture._wizardWaterSourceTexture = originalTexture;
            byRatio.set(key, croppedTexture);
            return croppedTexture;
        }

        ensureWizardDrowningHatMaskGraphics(parent) {
            if (!parent) {
                throw new Error("wizard drowning hat mask requires a parent container");
            }
            if (typeof PIXI === "undefined" || !PIXI || typeof PIXI.Graphics !== "function") {
                throw new Error("wizard drowning hat mask requires Pixi graphics");
            }
            if (!this.wizardDrowningHatMaskGraphics) {
                this.wizardDrowningHatMaskGraphics = new PIXI.Graphics();
                this.wizardDrowningHatMaskGraphics.name = "renderingWizardDrowningHatMask";
                this.wizardDrowningHatMaskGraphics.interactive = false;
                this.wizardDrowningHatMaskGraphics.visible = false;
            }
            const maskGraphics = this.wizardDrowningHatMaskGraphics;
            if (maskGraphics.parent !== parent) {
                parent.addChild(maskGraphics);
            }
            return maskGraphics;
        }

        clearWizardDrowningHatMask(hat = null) {
            if (hat && this.wizardDrowningHatMaskGraphics && hat.mask === this.wizardDrowningHatMaskGraphics) {
                hat.mask = null;
            }
            if (!this.wizardDrowningHatMaskGraphics) return;
            this.wizardDrowningHatMaskGraphics.clear();
            this.wizardDrowningHatMaskGraphics.visible = false;
            if (Object.prototype.hasOwnProperty.call(this.wizardDrowningHatMaskGraphics, "renderable")) {
                this.wizardDrowningHatMaskGraphics.renderable = false;
            }
        }

        projectWorldPointToCutawayPlane(x, y, z = 0) {
            const px = Number(x);
            const py = Number(y);
            const pz = Number(z) || 0;
            if (!Number.isFinite(px) || !Number.isFinite(py)) return null;
            return { x: px, y: py - pz };
        }

        projectWorldRingToCutawayPlane(points, baseZ = 0) {
            const ring = normalizeFloorVisualPointList(points);
            if (ring.length < 3) return [];
            const out = [];
            for (let i = 0; i < ring.length; i++) {
                const projected = this.projectWorldPointToCutawayPlane(ring[i].x, ring[i].y, baseZ);
                if (!projected) return [];
                out.push(projected);
            }
            return out;
        }

        getBuildingFragments(map, building) {
            if (building && Array.isArray(building.fragments)) {
                return building.fragments.filter(Boolean);
            }
            if (!(map && map.floorsById instanceof Map) || !building) return [];
            const ids = building.fragmentIds instanceof Set
                ? Array.from(building.fragmentIds)
                : (Array.isArray(building.fragmentIds) ? building.fragmentIds : []);
            const out = [];
            for (let i = 0; i < ids.length; i++) {
                const fragment = map.floorsById.get(ids[i]);
                if (fragment) out.push(fragment);
            }
            return out;
        }

        getWizardStandingBuildingLevel(map, building, x, y, wizardLayer) {
            const fragments = this.getBuildingFragments(map, building);
            const targetLayer = this.getLayerIndexFromValue(wizardLayer, 0);
            for (let i = 0; i < fragments.length; i++) {
                const fragment = fragments[i];
                if (!fragment || this.getLayerIndexFromValue(fragment.level, 0) !== targetLayer) continue;
                if (isPointInsideFloorVisibilityFragment(fragment, x, y) || isPointSupportedByFloorFragment(fragment, x, y)) {
                    return targetLayer;
                }
            }
            return null;
        }

        getLowestBuildingFragments(map, building) {
            const fragments = this.getBuildingFragments(map, building);
            let minLevel = Infinity;
            const out = [];
            for (let i = 0; i < fragments.length; i++) {
                const fragment = fragments[i];
                if (!fragment) continue;
                const level = this.getLayerIndexFromValue(fragment.level, 0);
                if (level < minLevel) {
                    minLevel = level;
                    out.length = 0;
                }
                if (level === minLevel) out.push(fragment);
            }
            return out;
        }

        getBuildingGroundProjectionPolygons(map, building) {
            if (!building) return [];
            const floorVersion = Number(map && map._floorBuildingVersion) || 0;
            const cacheKey = `${floorVersion}`;
            if (
                building._groundProjectionCacheKey === cacheKey &&
                Array.isArray(building.groundProjectionPolygons)
            ) {
                return building.groundProjectionPolygons;
            }
            const fragments = this.getLowestBuildingFragments(map, building);
            const polygons = [];
            for (let i = 0; i < fragments.length; i++) {
                const fragment = fragments[i];
                const outer = normalizeFloorVisualPointList(fragment.outerPolygon);
                if (outer.length < 3) continue;
                polygons.push({ outer });
            }
            building._groundProjectionCacheKey = cacheKey;
            building.groundProjectionPolygons = polygons;
            return polygons;
        }

        getWizardGroundProjectionBuildingLevel(map, building, x, y, wizardLayer) {
            const targetLayer = this.getLayerIndexFromValue(wizardLayer, 0);
            if (targetLayer !== 0) return null;
            const polygons = this.getBuildingGroundProjectionPolygons(map, building);
            return floorVisualPolygonsContainPoint(polygons, x, y) ? 0 : null;
        }

        getBuildingRenderCacheKey(ctx, map) {
            const floorVersion = Number(map && map._floorBuildingVersion) || 0;
            const renderVersion = Number(map && map._buildingRenderCacheVersion) || 0;
            const roofVersion = Number(ctx && ctx.roofVersion) || Number(global.roofVersion) || 0;
            const objectState = map && map._prototypeObjectState ? map._prototypeObjectState : null;
            const objectSignature = objectState && typeof objectState.activeRecordSignature === "string"
                ? objectState.activeRecordSignature
                : "";
            const activeObjectCount = objectState && objectState.activeRuntimeObjectsByRecordId instanceof Map
                ? objectState.activeRuntimeObjectsByRecordId.size
                : "";
            const wallState = map && map._prototypeWallState ? map._prototypeWallState : null;
            const wallSignature = wallState && typeof wallState.activeRecordSignature === "string"
                ? wallState.activeRecordSignature
                : "";
            const activeWallCount = wallState && wallState.activeRuntimeWallsByRecordId instanceof Map
                ? wallState.activeRuntimeWallsByRecordId.size
                : "";
            const buildingState = map && map._prototypeBuildingState ? map._prototypeBuildingState : null;
            const prototypeBuildingVersion = buildingState ? (Number(buildingState.contentVersion) || 0) : 0;
            return `${floorVersion}|${renderVersion}|${roofVersion}|${activeObjectCount}|${objectSignature}|${activeWallCount}|${wallSignature}|${prototypeBuildingVersion}`;
        }

        getBuildingRenderObjectCandidates(ctx, map) {
            const out = [];
            const seen = new Set();
            const pushItem = (item) => {
                if (!item || item.gone || item.vanishing || seen.has(item)) return;
                if (map && item.map && item.map !== map) return;
                seen.add(item);
                out.push(item);
            };
            if (map && typeof map.getGameObjects === "function") {
                const objects = map.getGameObjects({ refresh: true });
                if (Array.isArray(objects)) {
                    for (let i = 0; i < objects.length; i++) pushItem(objects[i]);
                }
            }
            const wallCtor = global.WallSectionUnit;
            const allSections = wallCtor && wallCtor._allSections instanceof Map ? wallCtor._allSections : null;
            if (allSections) {
                for (const section of allSections.values()) pushItem(section);
            }
            const roofs = this.getRoofsList(ctx);
            if (Array.isArray(roofs)) {
                for (let i = 0; i < roofs.length; i++) pushItem(roofs[i]);
            }
            return out;
        }

        getFloorFragmentInteriorPolygon(fragment) {
            if (!fragment) return null;
            const outer = normalizeFloorVisualPointList(fragment.outerPolygon);
            if (outer.length < 3) return null;
            const holes = [];
            const sourceHoles = Array.isArray(fragment.holes) ? fragment.holes : [];
            for (let i = 0; i < sourceHoles.length; i++) {
                const hole = normalizeFloorVisualPointList(sourceHoles[i]);
                if (hole.length >= 3) holes.push(hole);
            }
            return { outer, holes };
        }

        buildBuildingInteriorRegions(fragments, groundProjectionPolygons) {
            const regions = [];
            const addRegion = (region) => {
                if (!region || !region.polygon || !Array.isArray(region.polygon.outer) || region.polygon.outer.length < 3) return;
                region.staticObjects = [];
                region._staticObjectSet = new Set();
                regions.push(region);
            };
            const list = Array.isArray(fragments) ? fragments : [];
            for (let i = 0; i < list.length; i++) {
                const fragment = list[i];
                const polygon = this.getFloorFragmentInteriorPolygon(fragment);
                if (!polygon) continue;
                const fragmentId = typeof fragment.fragmentId === "string" ? fragment.fragmentId : "";
                const surfaceId = typeof fragment.surfaceId === "string" ? fragment.surfaceId : "";
                addRegion({
                    id: `fragment:${fragmentId || i}`,
                    kind: "floorFragment",
                    level: this.getLayerIndexFromValue(fragment.level, 0),
                    fragment,
                    fragmentId,
                    surfaceId,
                    polygon
                });
            }
            const groundPolygons = Array.isArray(groundProjectionPolygons) ? groundProjectionPolygons : [];
            for (let i = 0; i < groundPolygons.length; i++) {
                const polygon = groundPolygons[i];
                if (!polygon || !Array.isArray(polygon.outer) || polygon.outer.length < 3) continue;
                addRegion({
                    id: `groundFootprint:${i}`,
                    kind: "groundFootprint",
                    level: 0,
                    polygon
                });
            }
            return regions;
        }

        addBuildingInteriorRegionObject(region, entry) {
            if (!region || !entry || !entry.item) return false;
            if (!(region._staticObjectSet instanceof Set)) region._staticObjectSet = new Set();
            if (region._staticObjectSet.has(entry.item)) return false;
            region._staticObjectSet.add(entry.item);
            if (!Array.isArray(region.staticObjects)) region.staticObjects = [];
            region.staticObjects.push(entry);
            return true;
        }

        renderItemRefsMatchBuildingInteriorRegion(refs, region) {
            if (!region || !Array.isArray(refs) || refs.length === 0) return false;
            const regionFragmentId = typeof region.fragmentId === "string" ? region.fragmentId : "";
            const regionSurfaceId = typeof region.surfaceId === "string" ? region.surfaceId : "";
            for (let i = 0; i < refs.length; i++) {
                const ref = refs[i];
                if (!ref) continue;
                if (ref.fragmentId && regionFragmentId) {
                    if (regionFragmentId === ref.fragmentId) return true;
                    continue;
                }
                if (regionSurfaceId && ref.surfaceId && regionSurfaceId === ref.surfaceId) return true;
            }
            return false;
        }

        normalizeRenderFloorMembership(membership) {
            if (!membership || typeof membership !== "object") return null;
            const ownerType = typeof membership.ownerType === "string" ? membership.ownerType : "";
            const ownerId = typeof membership.ownerId === "string" ? membership.ownerId : "";
            const floorId = typeof membership.floorId === "string" && membership.floorId.length > 0
                ? membership.floorId
                : (typeof membership.sourceFloorId === "string" ? membership.sourceFloorId : "");
            if (!ownerType || !ownerId || !floorId) return null;
            const normalized = { ownerType, ownerId, floorId };
            if (Number.isFinite(Number(membership.level))) {
                normalized.level = this.getLayerIndexFromValue(membership.level, 0);
            }
            return normalized;
        }

        getRenderItemFloorMembership(item, mapRef = null, refs = null) {
            if (!item) return null;
            const floorSupportApi = global && global.FloorSupport ? global.FloorSupport : null;
            if (floorSupportApi && typeof floorSupportApi.getEntityFloorMembership === "function") {
                const membership = floorSupportApi.getEntityFloorMembership(item, { map: mapRef || global.map || null });
                if (membership) return this.normalizeRenderFloorMembership(membership);
            }
            return this.normalizeRenderFloorMembership(item._floorMembership) ||
                this.normalizeRenderFloorMembership(item.floorMembership);
        }

        getBuildingInteriorRegionFloorMembership(region, trigger = null) {
            if (!region) return null;
            const floorSupportApi = global && global.FloorSupport ? global.FloorSupport : null;
            const triggerBuildingId = typeof (trigger && trigger.buildingId) === "string" && trigger.buildingId.length > 0
                ? trigger.buildingId
                : (typeof (trigger && trigger.building && trigger.building.buildingId) === "string" && trigger.building.buildingId.length > 0
                    ? trigger.building.buildingId
                    : (typeof (trigger && trigger.building && trigger.building._prototypeBuildingPlacement && trigger.building._prototypeBuildingPlacement.id) === "string"
                        ? trigger.building._prototypeBuildingPlacement.id
                        : ""));
            const fragment = region.fragment && typeof region.fragment === "object" ? region.fragment : null;
            const ownerId = typeof (fragment && fragment.ownerId) === "string" && fragment.ownerId.length > 0
                ? fragment.ownerId
                : triggerBuildingId;
            const ownerType = typeof (fragment && fragment.ownerType) === "string" && fragment.ownerType.length > 0
                ? fragment.ownerType
                : (ownerId && ownerId.startsWith("building:") ? "building" : "");
            if (floorSupportApi && typeof floorSupportApi.createFloorMembership === "function") {
                return this.normalizeRenderFloorMembership(floorSupportApi.createFloorMembership({
                    fragment,
                    ownerType,
                    ownerId,
                    fragmentId: typeof region.fragmentId === "string" ? region.fragmentId : "",
                    surfaceId: typeof region.surfaceId === "string" ? region.surfaceId : "",
                    floorId: typeof region._prototypeBuildingSourceFragmentId === "string" ? region._prototypeBuildingSourceFragmentId : ""
                }));
            }
            const floorId = typeof (fragment && fragment._prototypeBuildingSourceFragmentId) === "string" && fragment._prototypeBuildingSourceFragmentId.length > 0
                ? fragment._prototypeBuildingSourceFragmentId
                : (typeof region._prototypeBuildingSourceFragmentId === "string" ? region._prototypeBuildingSourceFragmentId : "");
            return this.normalizeRenderFloorMembership({ ownerType, ownerId, floorId });
        }

        floorMembershipsMatchForRender(itemMembership, regionMembership) {
            const itemFloor = this.normalizeRenderFloorMembership(itemMembership);
            const regionFloor = this.normalizeRenderFloorMembership(regionMembership);
            if (!itemFloor || !regionFloor) return false;
            if (itemFloor.ownerType !== regionFloor.ownerType) return false;
            if (itemFloor.ownerId !== regionFloor.ownerId) return false;
            if (itemFloor.floorId !== regionFloor.floorId) return false;
            return true;
        }

        renderItemMembershipMatchesCutawayTrigger(item, trigger, mapRef = null, refs = null) {
            const membership = this.getRenderItemFloorMembership(item, mapRef || global.map || null, refs);
            if (!membership || membership.ownerType !== "building") return false;
            const triggerBuildingId = typeof (trigger && trigger.buildingId) === "string" && trigger.buildingId.length > 0
                ? trigger.buildingId
                : (typeof (trigger && trigger.building && trigger.building.buildingId) === "string" && trigger.building.buildingId.length > 0
                    ? trigger.building.buildingId
                    : (typeof (trigger && trigger.building && trigger.building._prototypeBuildingPlacement && trigger.building._prototypeBuildingPlacement.id) === "string"
                        ? trigger.building._prototypeBuildingPlacement.id
                        : ""));
            return !!triggerBuildingId && membership.ownerId === triggerBuildingId;
        }

        renderItemMembershipMatchesBuildingInteriorRegion(item, trigger, region, mapRef = null, refs = null) {
            return this.floorMembershipsMatchForRender(
                this.getRenderItemFloorMembership(item, mapRef || global.map || null, refs),
                this.getBuildingInteriorRegionFloorMembership(region, trigger)
            );
        }

        hasBuildingInteriorFloorRefs(refs) {
            if (!Array.isArray(refs)) return false;
            for (let i = 0; i < refs.length; i++) {
                const ref = refs[i];
                if (ref && (ref.fragmentId || ref.surfaceId)) return true;
            }
            return false;
        }

        isBuildingInteriorOverlaySurfaceItem(item) {
            if (!item) return false;
            const type = typeof item.type === "string" ? item.type.trim().toLowerCase() : "";
            const category = typeof item.category === "string" ? item.category.trim().toLowerCase() : "";
            const objectType = typeof item.objectType === "string" ? item.objectType.trim().toLowerCase() : "";
            if (item.isTriggerArea === true) return true;
            const isSurfaceType = (value) => (
                value === "road" ||
                value === "floor" ||
                value === "floortile" ||
                value === "flooring" ||
                value === "ground" ||
                value === "tile" ||
                value === "triggerarea" ||
                value === "firewall"
            );
            return (
                isSurfaceType(type) ||
                isSurfaceType(objectType) ||
                category === "floor" ||
                category === "floors" ||
                category === "flooring" ||
                category === "ground" ||
                category === "roads" ||
                category === "tiles"
            );
        }

        populateBuildingInteriorRegionMembership(cache, item, entry, refs, map) {
            if (!cache || !item || item.type === "roof" || !entry) return;
            if (item.type !== "wallSection" && (
                this.isBuildingCutawayDoorItem(item) ||
                this.isBuildingInteriorOverlaySurfaceItem(item)
            )) {
                return;
            }
            const regions = Array.isArray(cache.interiorRegions) ? cache.interiorRegions : [];
            if (regions.length === 0) return;
            const itemLevel = this.getLayerIndexFromValue(entry.level, 0);
            const refList = Array.isArray(refs) ? refs : [];
            const hasFloorRefs = this.hasBuildingInteriorFloorRefs(refList);
            for (let i = 0; i < regions.length; i++) {
                const region = regions[i];
                if (!region || this.getLayerIndexFromValue(region.level, 0) !== itemLevel) continue;
                let matches = this.renderItemRefsMatchBuildingInteriorRegion(refList, region);
                if (!matches && hasFloorRefs && region.kind === "floorFragment") continue;
                if (!matches && hasFloorRefs && region.kind !== "floorFragment" && item.type !== "wallSection") continue;
                if (!matches && region.polygon) {
                    matches = this.renderItemSamplesMatchPolygons(item, [region.polygon], map);
                }
                if (matches) this.addBuildingInteriorRegionObject(region, entry);
            }
        }

        renderItemMatchesBuildingInteriorRegion(cache, item, level, map, refs = null) {
            if (!cache || !item || item.type === "roof") return false;
            if (this.isBuildingInteriorOverlaySurfaceItem(item)) return false;
            const regions = Array.isArray(cache.interiorRegions) ? cache.interiorRegions : [];
            if (regions.length === 0) return false;
            const itemLevel = this.getLayerIndexFromValue(level, 0);
            const refList = Array.isArray(refs) ? refs : this.collectFloorRefsForRenderItem(item, map);
            const hasFloorRefs = this.hasBuildingInteriorFloorRefs(refList);
            for (let i = 0; i < regions.length; i++) {
                const region = regions[i];
                if (!region || this.getLayerIndexFromValue(region.level, 0) !== itemLevel) continue;
                if (this.renderItemRefsMatchBuildingInteriorRegion(refList, region)) return true;
                if (hasFloorRefs && region.kind === "floorFragment") continue;
                if (hasFloorRefs && region.kind !== "floorFragment" && item.type !== "wallSection") continue;
                if (this.renderItemSamplesMatchPolygons(item, [region.polygon], map)) return true;
            }
            return false;
        }

        finalizeBuildingInteriorRegions(cache) {
            const regions = Array.isArray(cache && cache.interiorRegions) ? cache.interiorRegions : [];
            for (let i = 0; i < regions.length; i++) {
                if (regions[i]) delete regions[i]._staticObjectSet;
            }
        }

        getBuildingInteriorRegionAtPoint(renderCache, x, y, level) {
            const regions = Array.isArray(renderCache && renderCache.interiorRegions) ? renderCache.interiorRegions : [];
            const targetLevel = this.getLayerIndexFromValue(level, 0);
            for (let i = 0; i < regions.length; i++) {
                const region = regions[i];
                if (!region || this.getLayerIndexFromValue(region.level, 0) !== targetLevel) continue;
                const contains = region.kind === "floorFragment" && region.fragment
                    ? (isPointInsideFloorVisibilityFragment(region.fragment, x, y) || isPointSupportedByFloorFragment(region.fragment, x, y))
                    : floorVisualPolygonsContainPoint([region.polygon], x, y);
                if (contains) return region;
            }
            return null;
        }

        buildingInteriorRegionContainsFootprintPoint(region, x, y) {
            if (!region || !region.polygon) return false;
            if (region.kind === "floorFragment" && region.fragment) {
                const outer = Array.isArray(region.polygon.outer) ? region.polygon.outer : region.fragment.outerPolygon;
                return floorVisualPolygonsContainPoint([{ outer, holes: [] }], x, y);
            }
            return floorVisualPolygonsContainPoint([region.polygon], x, y);
        }

        getBuildingInteriorRegionBaseZ(region) {
            if (region && region.fragment && Number.isFinite(Number(region.fragment.nodeBaseZ))) {
                return Number(region.fragment.nodeBaseZ);
            }
            return this.getLayerBaseZForLevel(this.getLayerIndexFromValue(region && region.level, 0));
        }

        getBuildingInteriorVisualRegionPriority(region) {
            if (region && region.kind === "floorFragment") return 2;
            if (region && region.kind === "groundFootprint") return 1;
            return 0;
        }

        getBuildingInteriorVisualRegionAtPoint(renderCache, x, y, movementLevel, wizardWorldZ) {
            const regions = Array.isArray(renderCache && renderCache.interiorRegions) ? renderCache.interiorRegions : [];
            const worldZ = Number(wizardWorldZ);
            if (Number.isFinite(worldZ)) {
                let bestRegion = null;
                let bestLevel = -Infinity;
                let bestPriority = -Infinity;
                for (let i = 0; i < regions.length; i++) {
                    const region = regions[i];
                    if (!this.buildingInteriorRegionContainsFootprintPoint(region, x, y)) continue;
                    const level = this.getLayerIndexFromValue(region.level, -Infinity);
                    const priority = this.getBuildingInteriorVisualRegionPriority(region);
                    if (
                        !Number.isFinite(level) ||
                        level < bestLevel ||
                        (level === bestLevel && priority <= bestPriority)
                    ) continue;
                    const baseZ = this.getBuildingInteriorRegionBaseZ(region);
                    const thresholdZ = baseZ - BUILDING_CUTAWAY_INTERIOR_FLOOR_SWITCH_BELOW_UNITS;
                    if (worldZ + 1e-6 < thresholdZ) continue;
                    bestRegion = region;
                    bestLevel = level;
                    bestPriority = priority;
                }
                if (bestRegion) return bestRegion;
            }
            return this.getBuildingInteriorRegionAtPoint(renderCache, x, y, movementLevel);
        }

        getDoorwayTransitionPresentationWizard(cutawayState) {
            const transition = cutawayState && cutawayState._doorwayPresentationTransition;
            const wizard = transition && transition.presentationWizard;
            return wizard && Number.isFinite(wizard.x) && Number.isFinite(wizard.y) ? wizard : null;
        }

        cloneCutawayStateForDoorwayTransition(state, transition) {
            if (!state || !transition || !transition.presentationWizard) return state;
            const cloneSet = (value) => value instanceof Set ? new Set(value) : value;
            return {
                ...state,
                triggers: Array.isArray(state.triggers)
                    ? state.triggers.map(trigger => trigger && typeof trigger === "object" ? { ...trigger } : trigger)
                    : [],
                hiddenSurfaceIds: cloneSet(state.hiddenSurfaceIds),
                hiddenFragmentIds: cloneSet(state.hiddenFragmentIds),
                _buildingFrameFlagsApplied: false,
                _doorwayPresentationTransition: {
                    doorItem: transition.doorItem || null,
                    buildingId: transition.buildingId || "",
                    startedAtMs: transition.startedAtMs,
                    presentationWizard: transition.presentationWizard
                }
            };
        }

        cloneBuildingDoorwayStablePresentationState(state) {
            if (!state || state._doorwayPresentationTransition) return null;
            return {
                ...state,
                triggers: Array.isArray(state.triggers)
                    ? state.triggers.map(trigger => trigger && typeof trigger === "object" ? { ...trigger } : trigger)
                    : [],
                hiddenSurfaceIds: state.hiddenSurfaceIds instanceof Set ? new Set(state.hiddenSurfaceIds) : state.hiddenSurfaceIds,
                hiddenFragmentIds: state.hiddenFragmentIds instanceof Set ? new Set(state.hiddenFragmentIds) : state.hiddenFragmentIds,
                _buildingFrameFlagsApplied: false
            };
        }

        cutawayStateHasActiveInteriorRegion(state) {
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                if (triggers[i] && triggers[i].activeInteriorRegion) return true;
            }
            return false;
        }

        getActiveBuildingInteriorRenderScope(ctx = null, cutawayState = null) {
            const state = cutawayState || (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            if (triggers.length === 0) return null;
            const scope = {
                active: false,
                buildingIds: new Set(),
                activeFragmentIds: new Set(),
                visibleFragmentIds: new Set(),
                visibleSurfaceIds: new Set(),
                ownerSectionKeys: new Set()
            };
            const addRegion = (region, active = false) => {
                if (!region) return;
                const fragmentId = this.getBuildingInteriorRegionFragmentId(region);
                if (fragmentId) {
                    scope.visibleFragmentIds.add(fragmentId);
                    if (active) scope.activeFragmentIds.add(fragmentId);
                }
                const surfaceId = typeof region.surfaceId === "string" && region.surfaceId.length > 0
                    ? region.surfaceId
                    : (typeof (region.fragment && region.fragment.surfaceId) === "string" ? region.fragment.surfaceId : "");
                if (surfaceId) scope.visibleSurfaceIds.add(surfaceId);
                const sectionKey = typeof (region.fragment && region.fragment.ownerSectionKey) === "string"
                    ? region.fragment.ownerSectionKey
                    : "";
                if (sectionKey) scope.ownerSectionKeys.add(sectionKey);
            };
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const activeRegion = trigger && trigger.activeInteriorRegion;
                if (!trigger || !activeRegion) continue;
                scope.active = true;
                const buildingId = typeof trigger.buildingId === "string" && trigger.buildingId.length > 0
                    ? trigger.buildingId
                    : (typeof (trigger.building && trigger.building.buildingId) === "string" && trigger.building.buildingId.length > 0
                        ? trigger.building.buildingId
                        : (typeof (trigger.building && trigger.building._prototypeBuildingPlacement && trigger.building._prototypeBuildingPlacement.id) === "string"
                            ? trigger.building._prototypeBuildingPlacement.id
                            : ""));
                if (buildingId) scope.buildingIds.add(buildingId);
                addRegion(activeRegion, true);
                const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
                for (let r = 0; r < regions.length; r++) addRegion(regions[r], false);
            }
            return scope.active ? scope : null;
        }

        floorFragmentAllowedByBuildingInteriorScope(fragment, scope = null) {
            if (!scope || !scope.active) return true;
            const fragmentId = typeof (fragment && fragment.fragmentId) === "string" ? fragment.fragmentId : "";
            if (fragmentId && scope.visibleFragmentIds.has(fragmentId)) return true;
            const surfaceId = typeof (fragment && fragment.surfaceId) === "string" ? fragment.surfaceId : "";
            if (surfaceId && scope.visibleSurfaceIds.has(surfaceId)) return true;
            return false;
        }

        rememberBuildingDoorwayStablePresentationState(state) {
            const stable = this.cloneBuildingDoorwayStablePresentationState(state);
            if (stable) this.lastBuildingDoorwayPresentationState = stable;
        }

        getDoorwayTransitionMetricsForDoor(item, wizardX, wizardY, mapRef = null) {
            if (!item || !this.isBuildingCutawayDoorItem(item) || !Number.isFinite(wizardX) || !Number.isFinite(wizardY)) return null;
            const section = this.resolveMountedWallSectionForItem(item);
            if (
                !section ||
                !section.startPoint ||
                !section.endPoint ||
                !Number.isFinite(section.startPoint.x) ||
                !Number.isFinite(section.startPoint.y) ||
                !Number.isFinite(section.endPoint.x) ||
                !Number.isFinite(section.endPoint.y)
            ) {
                return null;
            }
            const sx = Number(section.startPoint.x);
            const sy = Number(section.startPoint.y);
            const dx = mapRef && typeof mapRef.shortestDeltaX === "function"
                ? mapRef.shortestDeltaX(sx, Number(section.endPoint.x))
                : (Number(section.endPoint.x) - sx);
            const dy = mapRef && typeof mapRef.shortestDeltaY === "function"
                ? mapRef.shortestDeltaY(sy, Number(section.endPoint.y))
                : (Number(section.endPoint.y) - sy);
            const len = Math.hypot(dx, dy);
            if (!(len > 1e-6)) return null;
            const ux = dx / len;
            const uy = dy / len;
            const nx = -uy;
            const ny = ux;
            const faceCenters = this.getMountedFaceCentersForItem(item);
            let doorCenterX = Number(item.x);
            let doorCenterY = Number(item.y);
            if (
                faceCenters &&
                faceCenters.front &&
                faceCenters.back &&
                Number.isFinite(faceCenters.front.x) &&
                Number.isFinite(faceCenters.front.y) &&
                Number.isFinite(faceCenters.back.x) &&
                Number.isFinite(faceCenters.back.y)
            ) {
                doorCenterX = (Number(faceCenters.front.x) + Number(faceCenters.back.x)) * 0.5;
                doorCenterY = (Number(faceCenters.front.y) + Number(faceCenters.back.y)) * 0.5;
            }
            if (!Number.isFinite(doorCenterX) || !Number.isFinite(doorCenterY)) {
                doorCenterX = sx + dx * 0.5;
                doorCenterY = sy + dy * 0.5;
            }
            const doorDx = mapRef && typeof mapRef.shortestDeltaX === "function"
                ? mapRef.shortestDeltaX(sx, doorCenterX)
                : (doorCenterX - sx);
            const doorDy = mapRef && typeof mapRef.shortestDeltaY === "function"
                ? mapRef.shortestDeltaY(sy, doorCenterY)
                : (doorCenterY - sy);
            const wizardDx = mapRef && typeof mapRef.shortestDeltaX === "function"
                ? mapRef.shortestDeltaX(sx, wizardX)
                : (wizardX - sx);
            const wizardDy = mapRef && typeof mapRef.shortestDeltaY === "function"
                ? mapRef.shortestDeltaY(sy, wizardY)
                : (wizardY - sy);
            const doorAlong = doorDx * ux + doorDy * uy;
            const wizardAlong = wizardDx * ux + wizardDy * uy;
            const signedDistance = (wizardDx - doorDx) * nx + (wizardDy - doorDy) * ny;
            const halfWidth = Math.max(0.35, Number.isFinite(item.width) ? Math.abs(Number(item.width)) * 0.5 : 0.5);
            const alongDistance = Math.abs(wizardAlong - doorAlong);
            return {
                doorAlong,
                wizardAlong,
                alongDistance,
                signedDistance,
                absDistance: Math.abs(signedDistance),
                halfWidth,
                enterDistance: BUILDING_DOORWAY_TRANSITION_ENTER_DISTANCE,
                exitDistance: BUILDING_DOORWAY_TRANSITION_EXIT_DISTANCE,
                enterSpan: halfWidth + BUILDING_DOORWAY_TRANSITION_SPAN_PADDING,
                exitSpan: halfWidth + BUILDING_DOORWAY_TRANSITION_EXIT_SPAN_PADDING
            };
        }

        findDoorwayTransitionCandidate(state, wizardX, wizardY, wizardLayer, mapRef = null) {
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const activeRegion = trigger && trigger.activeInteriorRegion;
                if (!trigger || !trigger.building || !activeRegion) continue;
                const cache = trigger.renderCache || null;
                const entries = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < entries.length; j++) {
                    const entry = entries[j];
                    const item = entry && entry.item;
                    if (!this.shouldRenderBuildingInteriorDoorItem(item, activeRegion, entry, null, null)) continue;
                    const doorLevel = this.getBuildingInteriorEntryLayer(item, entry, this.getLayerIndexFromValue(activeRegion.level, 0));
                    if (doorLevel !== this.getLayerIndexFromValue(wizardLayer, 0)) continue;
                    const metrics = this.getDoorwayTransitionMetricsForDoor(item, wizardX, wizardY, mapRef);
                    if (!metrics) continue;
                    if (
                        metrics.absDistance <= metrics.enterDistance &&
                        metrics.alongDistance <= metrics.enterSpan
                    ) {
                        return { trigger, activeRegion, doorItem: item, metrics };
                    }
                }
            }
            return null;
        }

        updateBuildingDoorwayPresentationTransition(ctx, state, mapRef, wizardRef, nowMs) {
            if (!state || !wizardRef || !Number.isFinite(state.wizardX) || !Number.isFinite(state.wizardY)) {
                this.buildingDoorwayPresentationTransition = null;
                this.lastBuildingDoorwayPresentationState = null;
                return state;
            }
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const active = this.buildingDoorwayPresentationTransition || null;
            if (active && active.doorItem) {
                const metrics = this.getDoorwayTransitionMetricsForDoor(
                    active.doorItem,
                    Number(state.wizardX),
                    Number(state.wizardY),
                    mapRef
                );
                if (
                    metrics &&
                    metrics.absDistance <= metrics.exitDistance &&
                    metrics.alongDistance <= metrics.exitSpan
                ) {
                    return this.cloneCutawayStateForDoorwayTransition(active.latchedState, active);
                }
                this.buildingDoorwayPresentationTransition = null;
            }

            const candidate = this.findDoorwayTransitionCandidate(
                state,
                Number(state.wizardX),
                Number(state.wizardY),
                this.getLayerIndexFromValue(state.wizardLayer, 0),
                mapRef
            );
            if (!candidate) {
                this.rememberBuildingDoorwayStablePresentationState(state);
                return state;
            }
            const sourceState = state;
            const sourceLayer = this.getLayerIndexFromValue(sourceState && sourceState.wizardLayer, this.getLayerIndexFromValue(state.wizardLayer, 0));
            const presentationWizard = {
                x: Number.isFinite(sourceState && sourceState.wizardX) ? Number(sourceState.wizardX) : Number(state.wizardX),
                y: Number.isFinite(sourceState && sourceState.wizardY) ? Number(sourceState.wizardY) : Number(state.wizardY),
                currentLayer: sourceLayer,
                traversalLayer: sourceLayer,
                currentLayerBaseZ: Number.isFinite(sourceState && sourceState.wizardBaseZ)
                    ? Number(sourceState.wizardBaseZ)
                    : (Number.isFinite(state.wizardBaseZ) ? Number(state.wizardBaseZ) : this.getLayerBaseZForLevel(sourceLayer))
            };
            const transition = {
                doorItem: candidate.doorItem,
                buildingId: typeof candidate.trigger.buildingId === "string" ? candidate.trigger.buildingId : "",
                startedAtMs: currentMs,
                presentationWizard,
                latchedState: null
            };
            transition.latchedState = this.cloneCutawayStateForDoorwayTransition(sourceState, transition);
            this.buildingDoorwayPresentationTransition = transition;
            return transition.latchedState;
        }

        getBuildingInteriorOverlayRegionsForTrigger(trigger) {
            const activeRegion = trigger && trigger.activeInteriorRegion;
            const renderCache = trigger && trigger.renderCache;
            const regions = Array.isArray(renderCache && renderCache.interiorRegions)
                ? renderCache.interiorRegions
                : [];
            if (!activeRegion || regions.length === 0) return [];
            const activeId = typeof activeRegion.id === "string" ? activeRegion.id : "";
            const activeFragmentId = this.getBuildingInteriorRegionFragmentId(activeRegion);
            const activeLevel = this.getLayerIndexFromValue(activeRegion.level, 0);
            const visibleBelow = this.getBuildingInteriorHoleVisibleFragmentIds(trigger, activeRegion);
            const out = [];
            for (let i = 0; i < regions.length; i++) {
                const region = regions[i];
                if (!region || !region.polygon) continue;
                const isActiveRegion = region === activeRegion || (
                    activeId &&
                    typeof region.id === "string" &&
                    region.id === activeId
                );
                const regionFragmentId = this.getBuildingInteriorRegionFragmentId(region);
                const isVisibleThroughActiveHole = !!(
                    !isActiveRegion &&
                    activeFragmentId &&
                    regionFragmentId &&
                    visibleBelow.has(regionFragmentId) &&
                    this.getLayerIndexFromValue(region.level, 0) < activeLevel
                );
                if (isActiveRegion || isVisibleThroughActiveHole) out.push(region);
            }
            out.sort((a, b) => {
                const levelA = this.getLayerIndexFromValue(a && a.level, 0);
                const levelB = this.getLayerIndexFromValue(b && b.level, 0);
                if (levelA !== levelB) return levelA - levelB;
                return String(a && a.id || "").localeCompare(String(b && b.id || ""));
            });
            return out;
        }

        getBuildingInteriorVisibleFloorFragmentIds(ctx = null) {
            const state = (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            const fragmentIds = new Set();
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.activeInteriorRegion) continue;
                const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
                for (let r = 0; r < regions.length; r++) {
                    const fragmentId = this.getBuildingInteriorRegionFragmentId(regions[r]);
                    if (fragmentId) fragmentIds.add(fragmentId);
                }
            }
            return fragmentIds;
        }

        getBuildingInteriorActiveFloorFragmentIds(ctx = null, cutawayState = null) {
            const state = cutawayState || (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            const fragmentIds = new Set();
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.activeInteriorRegion) continue;
                const fragmentId = this.getBuildingInteriorRegionFragmentId(trigger.activeInteriorRegion);
                if (fragmentId) fragmentIds.add(fragmentId);
            }
            return fragmentIds;
        }

        getBuildingInteriorRegionFragmentId(region) {
            if (!region) return "";
            if (typeof region.fragmentId === "string" && region.fragmentId.length > 0) return region.fragmentId;
            if (region.fragment && typeof region.fragment.fragmentId === "string" && region.fragment.fragmentId.length > 0) {
                return region.fragment.fragmentId;
            }
            const id = typeof region.id === "string" ? region.id : "";
            return id.startsWith("fragment:") ? id.slice("fragment:".length) : "";
        }

        getBuildingInteriorHoleVisibleFragmentIds(trigger, activeRegion) {
            const fragmentId = this.getBuildingInteriorRegionFragmentId(activeRegion);
            if (!fragmentId) return new Set();
            const building = trigger && trigger.building;
            const visibleByFragment = building && building.holeVisibleFragments instanceof Map
                ? building.holeVisibleFragments
                : null;
            const visible = visibleByFragment ? visibleByFragment.get(fragmentId) : null;
            return visible instanceof Set ? visible : new Set();
        }

        buildBuildingInteriorRenderPlan(ctx, cutawayState) {
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const planStartMs = diagnosticsEnabled ? performance.now() : 0;
            if (!this._buildingInteriorRenderPlan) {
                this._buildingInteriorRenderPlan = {
                    active: false,
                    items: new Set(),
                    depthBumpedItems: new Set(),
                    depthBumpRegions: [],
                    wallTopFaceOnly: new Map(),
                    doorInteriorSide: new Map()
                };
            }
            const plan = this._buildingInteriorRenderPlan;
            plan.active = false;
            plan.items.clear();
            if (!(plan.depthBumpedItems instanceof Set)) plan.depthBumpedItems = new Set();
            plan.depthBumpedItems.clear();
            if (!Array.isArray(plan.depthBumpRegions)) plan.depthBumpRegions = [];
            plan.depthBumpRegions.length = 0;
            plan.wallTopFaceOnly.clear();
            if (!(plan.doorInteriorSide instanceof Map)) plan.doorInteriorSide = new Map();
            plan.doorInteriorSide.clear();
            const finishPlan = () => {
                if (diagnosticsEnabled) {
                    const previousMs = Number(this.currentFrameMetrics.buildingInteriorPlanMs) || 0;
                    this.setFrameMetric("buildingInteriorPlanMs", previousMs + (performance.now() - planStartMs));
                    this.setFrameMetric("buildingInteriorPlanItems", plan.items instanceof Set ? plan.items.size : 0);
                }
                return plan;
            };
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            if (triggers.length === 0) return finishPlan();
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            const presentationWizardRef = this.getDoorwayTransitionPresentationWizard(cutawayState) || wizardRef;
            const mapRef = (ctx && ctx.map) || global.map || null;
            const dynamicCharacters = this.getBuildingInteriorDynamicCharacterCandidates(ctx, wizardRef);
            const addDepthBumpedItem = (item) => {
                if (item) plan.depthBumpedItems.add(item);
            };
            const addDepthBumpRegion = (region) => {
                if (region && region.polygon) plan.depthBumpRegions.push(region);
            };
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const activeRegion = trigger && trigger.activeInteriorRegion;
                if (!trigger || (!activeRegion && !trigger.building)) continue;
                const renderCache = trigger && trigger.renderCache;
                if (!renderCache) {
                    throw new Error("missing building interior render cache");
                }
                const activeEntries = Array.isArray(renderCache.renderItems) ? renderCache.renderItems : [];
                if (!activeRegion) {
                    for (let j = 0; j < activeEntries.length; j++) {
                        const entry = activeEntries[j];
                        const item = entry && entry.item;
                        if (this.shouldRenderBuildingExteriorGroundDoorItem(item, entry, trigger, ctx, wizardRef)) {
                            plan.items.add(item);
                        }
                    }
                    continue;
                }
                const activeRegionId = typeof activeRegion.id === "string" ? activeRegion.id : "";
                addDepthBumpRegion(activeRegion);
                const prototypeInteriorBitmapPresentableCache = this.getPresentablePrototypeBuildingInteriorBitmapCache(ctx, trigger);
                const prototypeInteriorBitmapReady = !!prototypeInteriorBitmapPresentableCache ||
                    this.isPrototypeBuildingInteriorBitmapReady(ctx, trigger);
                if (prototypeInteriorBitmapReady) {
                    const hasExplicitCoverage = this.prototypeBuildingInteriorBitmapHasExplicitCoverage(prototypeInteriorBitmapPresentableCache);
                    const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
                    for (let regionIndex = 0; regionIndex < regions.length; regionIndex++) {
                        const region = regions[regionIndex];
                        if (!region) continue;
                        const isActiveRegion = region === activeRegion || (
                            activeRegionId &&
                            typeof region.id === "string" &&
                            region.id === activeRegionId
                        );
                        for (let j = 0; j < dynamicCharacters.length; j++) {
                            const character = dynamicCharacters[j];
                            if (this.shouldRenderBuildingInteriorCharacter(character, region, ctx, wizardRef, mapRef)) {
                                plan.items.add(character);
                                if (isActiveRegion) addDepthBumpedItem(character);
                            }
                        }
                        if (hasExplicitCoverage) {
                            const floorRenderItems = this.collectBuildingInteriorFloorRenderItems(ctx, region, mapRef, trigger);
                            for (let j = 0; j < floorRenderItems.length; j++) {
                                const item = floorRenderItems[j];
                                if (!item) continue;
                                if (this.isPrototypeBuildingInteriorBitmapObjectCovered(item, cutawayState, mapRef, ctx)) continue;
                                plan.items.add(item);
                                if (isActiveRegion) addDepthBumpedItem(item);
                            }
                        }
                    }
                    continue;
                }
                for (let j = 0; j < activeEntries.length; j++) {
                    const entry = activeEntries[j];
                    const item = entry && entry.item;
                    if (this.shouldRenderBuildingInteriorDoorItem(item, activeRegion, entry, ctx, wizardRef)) {
                        plan.items.add(item);
                        addDepthBumpedItem(item);
                        const interiorSide = this.getBuildingInteriorMountedDoorSide(
                            item,
                            activeRegion,
                            presentationWizardRef === wizardRef ? ctx : { ...ctx, wizard: presentationWizardRef },
                            mapRef
                        );
                        if (interiorSide) plan.doorInteriorSide.set(item, interiorSide);
                    }
                }
                const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
                for (let regionIndex = 0; regionIndex < regions.length; regionIndex++) {
                    const region = regions[regionIndex];
                    if (!region) continue;
                    const isActiveRegion = region === activeRegion || (
                        activeRegionId &&
                        typeof region.id === "string" &&
                        region.id === activeRegionId
                    );
                    const entries = Array.isArray(region.staticObjects) ? region.staticObjects : [];
                    for (let j = 0; j < entries.length; j++) {
                        const entry = entries[j];
                        const item = entry && entry.item;
                        if (!item || item.gone || item.vanishing) continue;
                        if (this.shouldRenderBuildingInteriorDoorItem(item, region, entry, ctx, wizardRef)) {
                            plan.items.add(item);
                            if (isActiveRegion) addDepthBumpedItem(item);
                            if (isActiveRegion) {
                                const interiorSide = this.getBuildingInteriorMountedDoorSide(
                                    item,
                                    region,
                                    presentationWizardRef === wizardRef ? ctx : { ...ctx, wizard: presentationWizardRef },
                                    mapRef
                                );
                                if (interiorSide) plan.doorInteriorSide.set(item, interiorSide);
                            }
                        } else if (item.type === "wallSection") {
                            if (!this.shouldRenderBuildingInteriorOverlayWall(item, region, trigger, ctx, wizardRef)) continue;
                            plan.items.add(item);
                            if (isActiveRegion) addDepthBumpedItem(item);
                            plan.wallTopFaceOnly.set(
                                item,
                                isActiveRegion ? this.shouldFlattenBuildingInteriorOverlayWall(item, ctx, presentationWizardRef) : false
                            );
                        } else if (this.shouldRenderBuildingInteriorOverlayItem(item, region, entry, ctx, wizardRef)) {
                            if (
                                isActiveRegion &&
                                this.isBuildingInteriorWindowHiddenByCollapsedWall(item, ctx, presentationWizardRef)
                            ) {
                                continue;
                            }
                            plan.items.add(item);
                            if (isActiveRegion) addDepthBumpedItem(item);
                        }
                    }
                    for (let j = 0; j < dynamicCharacters.length; j++) {
                        const character = dynamicCharacters[j];
                        if (this.shouldRenderBuildingInteriorCharacter(character, region, ctx, wizardRef, mapRef)) {
                            plan.items.add(character);
                            if (isActiveRegion) addDepthBumpedItem(character);
                        }
                    }
                    const floorRenderItems = this.collectBuildingInteriorFloorRenderItems(ctx, region, mapRef, trigger);
                    for (let j = 0; j < floorRenderItems.length; j++) {
                        if (floorRenderItems[j]) {
                            plan.items.add(floorRenderItems[j]);
                            if (isActiveRegion) addDepthBumpedItem(floorRenderItems[j]);
                        }
                    }
                }
            }
            plan.active = plan.items.size > 0;
            return finishPlan();
        }

        isPrototypeBuildingInteriorBitmapReady(ctx, trigger) {
            return !!this.getReadyPrototypeBuildingInteriorBitmapCache(ctx, trigger);
        }

        getPresentablePrototypeBuildingInteriorBitmapCache(ctx, trigger) {
            if (!this.isPrototypeBuildingCutawayTrigger(trigger) || !trigger.activeInteriorRegion) return false;
            const mapRef = (ctx && ctx.map) || global.map || null;
            if (!mapRef || typeof mapRef.getPrototypeBuildingInteriorBitmap !== "function") return false;
            const placement = trigger.building && trigger.building._prototypeBuildingPlacement;
            if (!placement || !placement.id) {
                throw new Error(`prototype building interior trigger ${trigger && trigger.buildingId || "(unknown)"} is missing its placement`);
            }
            const floorId = this.getPrototypeBuildingInteriorSourceFloorId(trigger, { required: false });
            if (!floorId || !this.isPrototypeBuildingInteriorBitmapSourceFloorId(floorId)) return false;
            const cache = mapRef.getPrototypeBuildingInteriorBitmap(placement.id, floorId);
            if (!cache || cache.status !== "ready") return false;
            if (!cache.texture) {
                throw new Error(`prototype building interior ${cache.id || placement.id} ready cache is missing its texture`);
            }
            return cache;
        }

        getReadyPrototypeBuildingInteriorBitmapCache(ctx, trigger) {
            const cache = this.getPresentablePrototypeBuildingInteriorBitmapCache(ctx, trigger);
            if (!cache || cache.stale === true) return false;
            return cache;
        }

        prototypeBuildingInteriorBitmapHasExplicitCoverage(cache) {
            return !!(
                cache &&
                (
                    Array.isArray(cache.coveredObjects) ||
                    Array.isArray(cache.coveredObjectEntries) ||
                    cache.coveredObjectSet instanceof Set
                )
            );
        }

        getCompiledBuildingRenderCache(ctx, map, building) {
            if (!building) return null;
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const cacheKey = this.getBuildingRenderCacheKey(ctx, map);
            if (
                building.renderCache &&
                building.renderCache.cacheKey === cacheKey &&
                Array.isArray(building.renderCache.occlusionPolygons)
            ) {
                this.incrementFrameMetric("buildingRenderCacheHits", 1);
                this.recordMoveObjectPerf("rendering.buildingRenderCache.hit", {
                    buildingId: building.buildingId || "",
                    items: Array.isArray(building.renderCache.renderItems) ? building.renderCache.renderItems.length : null
                });
                return building.renderCache;
            }
            this.incrementFrameMetric("buildingRenderCacheMisses", 1);
            const movePerfEnabled = !!(typeof globalThis !== "undefined" && globalThis.__moveObjectPerf);
            const buildStartMs = (diagnosticsEnabled || movePerfEnabled) ? performance.now() : 0;

            const fragments = this.getBuildingFragments(map, building);
            const roofs = this.getRoofsForBuilding(ctx, map, building);
            const occlusionPolygons = this.buildBuildingOcclusionPolygons(ctx, map, building, roofs);
            const groundProjectionPolygons = this.getBuildingGroundProjectionPolygons(map, building);
            const cache = {
                cacheKey,
                buildingId: building.buildingId,
                fragments,
                fragmentGraph: building.fragmentGraph instanceof Map ? building.fragmentGraph : new Map(),
                fragmentIds: building.fragmentIds instanceof Set ? building.fragmentIds : new Set(),
                surfaceIds: building.surfaceIds instanceof Set ? building.surfaceIds : new Set(),
                roofs,
                occlusionPolygons,
                occlusionBounds: this.getCutawayPolygonBounds(occlusionPolygons),
                groundProjectionPolygons,
                interiorRegions: this.buildBuildingInteriorRegions(fragments, groundProjectionPolygons),
                renderItems: [],
                wallSections: [],
                groundProjectionWalls: [],
                wallSectionsByLevel: new Map(),
                maskEntries: null
            };

            const candidates = this.getBuildingRenderObjectCandidates(ctx, map);
            this.incrementFrameMetric("buildingRenderCacheCandidates", candidates.length);
            if (diagnosticsEnabled && isDrawPassBreakdownEnabled()) {
                const candidateTypeCounts = this.createRenderItemTypeMetricCounts();
                for (let i = 0; i < candidates.length; i++) {
                    this.incrementRenderItemTypeMetricCount(candidateTypeCounts, candidates[i]);
                }
                this.setRenderItemTypeFrameMetrics("buildingRenderCacheCandidates", candidateTypeCounts);
            }
            const cachedItems = new Set();
            for (let i = 0; i < candidates.length; i++) {
                const item = candidates[i];
                if (!item || item === global.wizard) continue;
                this.incrementFrameMetric("buildingRenderCacheRefsScanned", 1);
                const refs = this.collectFloorRefsForRenderItem(item, map);
                const level = item.type === "roof"
                    ? this.getLayerIndexForRoof(item, 0)
                    : this.getBuildingInteriorEntryLayer(item, null, 0);
                const matchesBuildingRefs = item.type === "roof"
                    ? this.roofMatchesBuilding(item, map, building)
                    : this.refsMatchBuilding(refs, building);
                const matchesGroundProjectionWall = !!(
                    !matchesBuildingRefs &&
                    item.type === "wallSection" &&
                    level === 0 &&
                    this.renderItemSamplesMatchPolygons(item, groundProjectionPolygons, map)
                );
                const matchesInteriorRegion = !!(
                    !matchesBuildingRefs &&
                    !matchesGroundProjectionWall &&
                    item.type !== "wallSection" &&
                    this.renderItemMatchesBuildingInteriorRegion(cache, item, level, map)
                );
                if (!matchesBuildingRefs && !matchesGroundProjectionWall && !matchesInteriorRegion) continue;
                const entry = { item, level, refs };
                cache.renderItems.push(entry);
                cachedItems.add(item);
                item._buildingRenderCacheId = building.buildingId;
                item._buildingRenderCacheKey = cacheKey;
                item._buildingRenderCacheLevel = level;
                this.populateBuildingInteriorRegionMembership(cache, item, entry, refs, map);
                if (item.type === "wallSection") {
                    cache.wallSections.push(entry);
                    if (!cache.wallSectionsByLevel.has(level)) cache.wallSectionsByLevel.set(level, []);
                    cache.wallSectionsByLevel.get(level).push(entry);
                    if (level === 0) {
                        cache.groundProjectionWalls.push(entry);
                    }
                }
            }
            const manifestEntries = Array.isArray(building.staticObjects) ? building.staticObjects : [];
            for (let i = 0; i < manifestEntries.length; i++) {
                const manifestEntry = manifestEntries[i];
                const item = manifestEntry && manifestEntry.item;
                if (!item || item.gone || item.vanishing || item === global.wizard || cachedItems.has(item)) continue;
                const refs = Array.isArray(manifestEntry.refs) && manifestEntry.refs.length > 0
                    ? manifestEntry.refs
                    : this.collectFloorRefsForRenderItem(item, map);
                const level = Number.isFinite(manifestEntry.level)
                    ? this.getLayerIndexFromValue(manifestEntry.level, 0)
                    : (item.type === "roof"
                        ? this.getLayerIndexForRoof(item, 0)
                        : this.getBuildingInteriorEntryLayer(item, null, 0));
                const entry = { item, level, refs };
                cache.renderItems.push(entry);
                cachedItems.add(item);
                item._buildingRenderCacheId = building.buildingId;
                item._buildingRenderCacheKey = cacheKey;
                item._buildingRenderCacheLevel = level;
                this.populateBuildingInteriorRegionMembership(cache, item, entry, refs, map);
                if (item.type === "wallSection") {
                    cache.wallSections.push(entry);
                    if (!cache.wallSectionsByLevel.has(level)) cache.wallSectionsByLevel.set(level, []);
                    cache.wallSectionsByLevel.get(level).push(entry);
                    if (level === 0) {
                        cache.groundProjectionWalls.push(entry);
                    }
                }
            }
            this.finalizeBuildingInteriorRegions(cache);

            building.renderCache = cache;
            building.occlusionPolygons = occlusionPolygons;
            building.occlusionBounds = cache.occlusionBounds;
            building.groundProjectionPolygons = groundProjectionPolygons;
            this.incrementFrameMetric("buildingRenderCacheItems", cache.renderItems.length);
            this.incrementFrameMetric("buildingRenderCacheWalls", cache.wallSections.length);
            this.incrementFrameMetric("buildingRenderCacheGroundWalls", cache.groundProjectionWalls.length);
            if (diagnosticsEnabled) {
                this.incrementFrameMetric("buildingRenderCacheBuildMs", performance.now() - buildStartMs);
            }
            if (movePerfEnabled) {
                this.recordMoveObjectPerf("rendering.buildingRenderCache.build", {
                    buildingId: building.buildingId || "",
                    candidates: candidates.length,
                    items: cache.renderItems.length,
                    walls: cache.wallSections.length,
                    manifestEntries: manifestEntries.length
                }, performance.now() - buildStartMs);
            }
            return cache;
        }

        floorFragmentOverlapsAnyAbove(map, fragment, fragments) {
            if (!(map && typeof map.doFloorFragmentsOverlapXY === "function") || !fragment) return false;
            const level = this.getLayerIndexFromValue(fragment.level, 0);
            const list = Array.isArray(fragments) ? fragments : [];
            for (let i = 0; i < list.length; i++) {
                const other = list[i];
                if (!other || other === fragment) continue;
                if (this.getLayerIndexFromValue(other.level, 0) <= level) continue;
                if (map.doFloorFragmentsOverlapXY(fragment, other)) return true;
            }
            return false;
        }

        refsMatchBuilding(refs, building) {
            if (!building) return false;
            const fragmentIds = building.fragmentIds instanceof Set ? building.fragmentIds : new Set();
            const surfaceIds = building.surfaceIds instanceof Set ? building.surfaceIds : new Set();
            const list = Array.isArray(refs) ? refs : [];
            for (let i = 0; i < list.length; i++) {
                const ref = list[i];
                if (!ref) continue;
                if (ref.fragmentId && fragmentIds.has(ref.fragmentId)) return true;
                if (ref.surfaceId && surfaceIds.has(ref.surfaceId)) return true;
            }
            return false;
        }

        getRoofBuildingFootprintPolygon(roof) {
            if (!roof) return null;
            const pointSources = [
                roof.interiorHidePolygonPoints,
                roof.shadowBox && roof.shadowBox.points,
                roof.interiorHideHitbox && roof.interiorHideHitbox.points
            ];
            for (let i = 0; i < pointSources.length; i++) {
                const outer = normalizeFloorVisualPointList(pointSources[i]);
                if (outer.length >= 3) return { outer, holes: [] };
            }
            if (Array.isArray(roof.vertices) && roof.vertices.length >= 3) {
                const points = [];
                for (let i = 0; i < roof.vertices.length; i++) {
                    const vertex = roof.vertices[i];
                    const x = Number(roof.x) + (Number(vertex && vertex.x) || 0);
                    const y = Number(roof.y) + (Number(vertex && vertex.y) || 0);
                    if (Number.isFinite(x) && Number.isFinite(y)) points.push({ x, y });
                }
                const hull = typeof convexHull2D === "function" ? convexHull2D(points) : points;
                const outer = normalizeFloorVisualPointList(hull);
                if (outer.length >= 3) return { outer, holes: [] };
            }
            return null;
        }

        roofFootprintOverlapsFloorFragment(roofPolygon, fragment) {
            if (!roofPolygon || !fragment) return false;
            const fragmentPolygon = this.getFloorFragmentInteriorPolygon(fragment);
            if (!fragmentPolygon) return false;
            const intersection = intersectFloorVisualPolygonWithPolygon(
                roofPolygon.outer,
                roofPolygon.holes,
                fragmentPolygon
            );
            if (Array.isArray(intersection) && intersection.length > 0) return true;
            const roofOuter = Array.isArray(roofPolygon.outer) ? roofPolygon.outer : [];
            for (let i = 0; i < roofOuter.length; i++) {
                const point = roofOuter[i];
                if (point && floorVisualPolygonsContainPoint([fragmentPolygon], point.x, point.y)) return true;
            }
            const fragmentOuter = Array.isArray(fragmentPolygon.outer) ? fragmentPolygon.outer : [];
            for (let i = 0; i < fragmentOuter.length; i++) {
                const point = fragmentOuter[i];
                if (point && floorVisualPolygonsContainPoint([roofPolygon], point.x, point.y)) return true;
            }
            return false;
        }

        roofMatchesBuilding(roof, map, building) {
            if (!roof || !building) return false;
            const refs = this.collectFloorRefsForRenderItem(roof, map);
            if (this.refsMatchBuilding(refs, building)) return true;
            const roofLayer = this.getLayerIndexForRoof(roof, 0);
            const minLevel = this.getLayerIndexFromValue(building.minLevel, 0);
            if (Number.isFinite(roofLayer) && roofLayer < minLevel) return false;
            const roofPolygon = this.getRoofBuildingFootprintPolygon(roof);
            if (!roofPolygon) return false;
            const fragments = this.getBuildingFragments(map, building);
            for (let i = 0; i < fragments.length; i++) {
                if (this.roofFootprintOverlapsFloorFragment(roofPolygon, fragments[i])) return true;
            }
            return false;
        }

        getRoofsForBuilding(ctx, map, building) {
            const roofs = this.getRoofsList(ctx);
            if (!Array.isArray(roofs) || roofs.length === 0) return [];
            return roofs.filter(roof => {
                if (!roof || roof.gone || roof.vanishing) return false;
                return this.roofMatchesBuilding(roof, map, building);
            });
        }

        getRoofOcclusionSignature(roof) {
            if (!roof) return "";
            const parts = [
                Number.isFinite(roof.z) ? Number(roof.z) : "",
                Number.isFinite(roof.heightFromGround) ? Number(roof.heightFromGround) : "",
                Number.isFinite(roof.x) ? Number(roof.x) : "",
                Number.isFinite(roof.y) ? Number(roof.y) : "",
                Array.isArray(roof.wallLoopSectionIds) ? roof.wallLoopSectionIds.join(",") : ""
            ];
            const vertices = Array.isArray(roof.vertices) ? roof.vertices : [];
            for (let i = 0; i < vertices.length; i++) {
                const v = vertices[i];
                parts.push(
                    Math.round((Number(v && v.x) || 0) * 1000),
                    Math.round((Number(v && v.y) || 0) * 1000),
                    Math.round((Number(v && v.z) || 0) * 1000)
                );
            }
            const faces = Array.isArray(roof.faces) ? roof.faces : [];
            for (let i = 0; i < faces.length; i++) {
                parts.push(Array.isArray(faces[i]) ? faces[i].join(".") : "");
            }
            return parts.join("|");
        }

        getBuildingRoofSignature(roofs) {
            if (!Array.isArray(roofs) || roofs.length === 0) return "";
            return roofs.map(roof => this.getRoofOcclusionSignature(roof)).sort().join("::");
        }

        collectRoofOcclusionPolygons(roof, layerPointsByLevel) {
            if (!roof || !Array.isArray(roof.vertices) || roof.vertices.length < 3) return [];
            const polygons = [];
            const baseZ = Number.isFinite(roof.z)
                ? Number(roof.z)
                : (Number.isFinite(roof.heightFromGround) ? Number(roof.heightFromGround) : 0);
            const roofLayer = Math.floor((Number(baseZ) || 0) / FLOOR_LAYER_DEFAULT_HEIGHT_UNITS);
            const projectedVertices = roof.vertices.map(vertex => {
                const worldX = Number(roof.x) + (Number(vertex && vertex.x) || 0);
                const worldY = Number(roof.y) + (Number(vertex && vertex.y) || 0);
                const worldZ = baseZ + (Number(vertex && vertex.z) || 0);
                return this.projectWorldPointToCutawayPlane(worldX, worldY, worldZ);
            });
            const validLayerPoints = projectedVertices.filter(Boolean);
            if (validLayerPoints.length >= 3) {
                if (!layerPointsByLevel.has(roofLayer)) layerPointsByLevel.set(roofLayer, []);
                layerPointsByLevel.get(roofLayer).push(...validLayerPoints);
            }
            const faces = Array.isArray(roof.faces) ? roof.faces : [];
            for (let i = 0; i < faces.length; i++) {
                const face = faces[i];
                if (!Array.isArray(face) || face.length < 3) continue;
                const polygon = [];
                for (let j = 0; j < face.length; j++) {
                    const idx = Math.floor(Number(face[j]));
                    const point = Number.isInteger(idx) ? projectedVertices[idx] : null;
                    if (point) polygon.push(point);
                }
                if (polygon.length >= 3) polygons.push(polygon);
            }
            if (polygons.length === 0 && validLayerPoints.length >= 3) {
                polygons.push(validLayerPoints);
            }
            return polygons;
        }

        getBuildingCutawayCompositeProjectionXyRatio(ctx = null) {
            const cameraRef = this.camera || {};
            const candidates = [
                cameraRef.xyratio,
                ctx && ctx.camera && ctx.camera.xyratio,
                typeof global !== "undefined" ? global.xyratio : null
            ];
            for (let i = 0; i < candidates.length; i++) {
                const value = Number(candidates[i]);
                if (Number.isFinite(value) && Math.abs(value) > 1e-6) return Math.abs(value);
            }
            return 1;
        }

        projectRoofVertexToCutawayCompositePlane(roof, vertex, presentationXyRatio = 1) {
            if (!roof || !vertex) return null;
            const ratio = Math.max(1e-6, Math.abs(Number(presentationXyRatio) || 1));
            const baseZ = Number.isFinite(roof.z)
                ? Number(roof.z)
                : (Number.isFinite(roof.heightFromGround) ? Number(roof.heightFromGround) : 0);
            const rotationRadians = Math.atan(1.15547);
            const cosR = Math.cos(rotationRadians);
            const sinR = Math.sin(rotationRadians);
            const localX = Number(vertex.x) || 0;
            const localY = Number(vertex.y) || 0;
            const localZ = Number(vertex.z) || 0;
            const rotatedY = (localY * cosR) - (localZ * sinR);
            return {
                x: (Number(roof.x) || 0) + localX,
                y: (Number(roof.y) || 0) - baseZ + (rotatedY / ratio)
            };
        }

        collectRoofCompositeProjectionPolygons(roof, presentationXyRatio = 1) {
            if (!roof || !Array.isArray(roof.vertices) || roof.vertices.length < 3) return [];
            const projectedVertices = roof.vertices.map(vertex =>
                this.projectRoofVertexToCutawayCompositePlane(roof, vertex, presentationXyRatio)
            );
            const polygons = [];
            const faces = Array.isArray(roof.faces) ? roof.faces : [];
            for (let i = 0; i < faces.length; i++) {
                const face = faces[i];
                if (!Array.isArray(face) || face.length < 3) continue;
                const polygon = [];
                for (let j = 0; j < face.length; j++) {
                    const idx = Math.floor(Number(face[j]));
                    const point = Number.isInteger(idx) ? projectedVertices[idx] : null;
                    if (point) polygon.push(point);
                }
                if (polygon.length >= 3) polygons.push(polygon);
            }
            if (polygons.length > 0) return polygons;
            const fallback = projectedVertices.filter(Boolean);
            return fallback.length >= 3 ? [fallback] : [];
        }

        getCutawayPolygonBounds(polygons) {
            const list = Array.isArray(polygons) ? polygons : [];
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < list.length; i++) {
                const outer = list[i] && Array.isArray(list[i].outer) ? list[i].outer : [];
                for (let p = 0; p < outer.length; p++) {
                    const x = Number(outer[p] && outer[p].x);
                    const y = Number(outer[p] && outer[p].y);
                    if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
                    minX = Math.min(minX, x);
                    minY = Math.min(minY, y);
                    maxX = Math.max(maxX, x);
                    maxY = Math.max(maxY, y);
                }
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) return null;
            return { minX, minY, maxX, maxY };
        }

        isCutawayPointInsideBounds(point, bounds) {
            if (!point || !bounds) return false;
            return point.x >= bounds.minX && point.x <= bounds.maxX && point.y >= bounds.minY && point.y <= bounds.maxY;
        }

        isCutawayPointNearBounds(point, bounds, distance = 0) {
            if (!point || !bounds) return false;
            const d = Math.max(0, Number(distance) || 0);
            return point.x >= bounds.minX - d &&
                point.x <= bounds.maxX + d &&
                point.y >= bounds.minY - d &&
                point.y <= bounds.maxY + d;
        }

        getBuildingCutawayTransitionKey(buildingId) {
            return typeof buildingId === "string" && buildingId.length > 0 ? buildingId : "building";
        }

        getBuildingCutawayTransitionAlpha(transition, nowMs) {
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const configuredMinAlpha = Number(BUILDING_CUTAWAY_GHOST_ALPHA);
            const minAlpha = Number.isFinite(configuredMinAlpha)
                ? Math.max(0, Math.min(1, configuredMinAlpha))
                : 0;
            const durationMs = Math.max(1, Number(BUILDING_CUTAWAY_ENTRY_FADE_MS) || 500);
            const startedAtMs = transition && Number.isFinite(transition.startedAtMs)
                ? Number(transition.startedAtMs)
                : currentMs;
            const startAlpha = transition && Number.isFinite(transition.startAlpha)
                ? Math.max(0, Math.min(1, Number(transition.startAlpha)))
                : 1;
            const targetAlpha = transition && Number.isFinite(transition.targetAlpha)
                ? Math.max(0, Math.min(1, Number(transition.targetAlpha)))
                : minAlpha;
            const progress = Math.max(0, Math.min(1, (currentMs - startedAtMs) / durationMs));
            if (progress <= 0) return startAlpha;
            if (progress >= 1) return targetAlpha;
            return startAlpha + ((targetAlpha - startAlpha) * progress);
        }

        getBuildingCutawayEntryAlpha(buildingId, nowMs) {
            if (!(this.buildingCutawayEntryTransitions instanceof Map)) {
                this.buildingCutawayEntryTransitions = new Map();
            }
            const key = this.getBuildingCutawayTransitionKey(buildingId);
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const configuredMinAlpha = Number(BUILDING_CUTAWAY_GHOST_ALPHA);
            const minAlpha = Number.isFinite(configuredMinAlpha)
                ? Math.max(0, Math.min(1, configuredMinAlpha))
                : 0;
            let transition = this.buildingCutawayEntryTransitions.get(key);
            if (!transition || transition.mode !== "enter" || !Number.isFinite(transition.startedAtMs)) {
                const startAlpha = transition
                    ? this.getBuildingCutawayTransitionAlpha(transition, currentMs)
                    : 1;
                transition = {
                    mode: "enter",
                    startedAtMs: currentMs,
                    startAlpha,
                    targetAlpha: minAlpha,
                    lastExteriorTrigger: transition && transition.lastExteriorTrigger
                        ? transition.lastExteriorTrigger
                        : null
                };
                this.buildingCutawayEntryTransitions.set(key, transition);
            }
            return this.getBuildingCutawayTransitionAlpha(transition, currentMs);
        }

        rememberBuildingCutawayTriggerForExit(trigger) {
            if (!(this.buildingCutawayEntryTransitions instanceof Map)) {
                this.buildingCutawayEntryTransitions = new Map();
            }
            if (!trigger || typeof trigger !== "object") return;
            const key = this.getBuildingCutawayTransitionKey(trigger.buildingId);
            const transition = this.buildingCutawayEntryTransitions.get(key);
            if (!transition) return;
            if (trigger.activeInteriorRegion) {
                transition.lastExteriorTrigger = null;
                return;
            }
            transition.lastExteriorTrigger = {
                ...trigger,
                activeInteriorRegion: null,
                activeInteriorRegionId: "",
                visibleThroughLevel: null,
                visibleThroughPolygons: null
            };
        }

        getBuildingCutawayExitTriggers(activeBuildingIds, nowMs) {
            if (!(this.buildingCutawayEntryTransitions instanceof Map)) return [];
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const triggers = [];
            for (const [key, transition] of this.buildingCutawayEntryTransitions.entries()) {
                if (activeBuildingIds instanceof Set && activeBuildingIds.has(key)) continue;
                if (!transition || !transition.lastExteriorTrigger) {
                    this.buildingCutawayEntryTransitions.delete(key);
                    continue;
                }
                if (transition.mode !== "exit" || !Number.isFinite(transition.startedAtMs)) {
                    const startAlpha = this.getBuildingCutawayTransitionAlpha(transition, currentMs);
                    transition.mode = "exit";
                    transition.startedAtMs = currentMs;
                    transition.startAlpha = startAlpha;
                    transition.targetAlpha = 1;
                }
                const alpha = this.getBuildingCutawayTransitionAlpha(transition, currentMs);
                if (alpha >= 1 - 1e-6) {
                    this.buildingCutawayEntryTransitions.delete(key);
                    continue;
                }
                triggers.push({
                    ...transition.lastExteriorTrigger,
                    alpha,
                    exitingCutaway: true
                });
            }
            return triggers;
        }

        clearBuildingCutawayEntryTransition(buildingId) {
            if (!(this.buildingCutawayEntryTransitions instanceof Map)) return;
            const key = this.getBuildingCutawayTransitionKey(buildingId);
            this.buildingCutawayEntryTransitions.delete(key);
        }

        ensureBuildingInteriorFloorTransitionState() {
            if (!(this.buildingInteriorFloorCutawayLevels instanceof Map)) {
                this.buildingInteriorFloorCutawayLevels = new Map();
            }
            if (!(this.buildingInteriorFloorTransitions instanceof Map)) {
                this.buildingInteriorFloorTransitions = new Map();
            }
        }

        clearBuildingInteriorFloorTransitionState() {
            if (this.buildingInteriorFloorCutawayLevels instanceof Map) this.buildingInteriorFloorCutawayLevels.clear();
            if (this.buildingInteriorFloorTransitions instanceof Map) this.buildingInteriorFloorTransitions.clear();
        }

        getBuildingInteriorFloorTransitionProgress(transition, nowMs) {
            if (!transition || !Number.isFinite(transition.startedAtMs)) return 1;
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            const durationMs = Math.max(1, Number.isFinite(transition.durationMs)
                ? Number(transition.durationMs)
                : BUILDING_CUTAWAY_INTERIOR_FLOOR_FADE_MS);
            return Math.max(0, Math.min(1, (currentMs - Number(transition.startedAtMs)) / durationMs));
        }

        getBuildingInteriorFloorTriggerKey(trigger) {
            if (!trigger || !trigger.building) return "";
            const buildingId = typeof trigger.buildingId === "string" && trigger.buildingId.length > 0
                ? trigger.buildingId
                : (trigger.building && typeof trigger.building.buildingId === "string" ? trigger.building.buildingId : "");
            return this.getBuildingCutawayTransitionKey(buildingId);
        }

        getBuildingInteriorFloorTransitionSourceId(trigger) {
            if (!trigger || !trigger.activeInteriorRegion) return "";
            if (this.isPrototypeBuildingCutawayTrigger(trigger)) {
                const sourceFloorId = this.getPrototypeBuildingInteriorSourceFloorId(trigger, { required: false });
                if (sourceFloorId) return sourceFloorId;
                const region = trigger.activeInteriorRegion;
                return typeof region.id === "string" && region.id.length > 0
                    ? region.id
                    : (typeof trigger.activeInteriorRegionId === "string" ? trigger.activeInteriorRegionId : "");
            }
            const region = trigger.activeInteriorRegion;
            if (typeof region.id === "string" && region.id.length > 0) return region.id;
            if (typeof trigger.activeInteriorRegionId === "string" && trigger.activeInteriorRegionId.length > 0) {
                return trigger.activeInteriorRegionId;
            }
            return "";
        }

        updateBuildingInteriorFloorTransitions(triggers, nowMs) {
            this.ensureBuildingInteriorFloorTransitionState();
            const activeKeys = new Set();
            const list = Array.isArray(triggers) ? triggers : [];
            const currentMs = Number.isFinite(nowMs) ? Number(nowMs) : Date.now();
            for (let i = 0; i < list.length; i++) {
                const trigger = list[i];
                if (!trigger || !trigger.building || !trigger.activeInteriorRegion) continue;
                const key = this.getBuildingInteriorFloorTriggerKey(trigger);
                if (!key) continue;
                const triggerLevel = this.getLayerIndexFromValue(trigger.level, NaN);
                if (!Number.isFinite(triggerLevel)) continue;
                const sourceFloorId = this.getBuildingInteriorFloorTransitionSourceId(trigger);
                activeKeys.add(key);

                const previous = this.buildingInteriorFloorCutawayLevels.get(key);
                const previousLevel = previous && Number.isFinite(previous.triggerLevel)
                    ? Number(previous.triggerLevel)
                    : NaN;
                const previousSourceFloorId = previous && typeof previous.sourceFloorId === "string"
                    ? previous.sourceFloorId
                    : "";
                if (
                    Number.isFinite(previousLevel) &&
                    (previousLevel !== triggerLevel || previousSourceFloorId !== sourceFloorId)
                ) {
                    const activeTransition = this.buildingInteriorFloorTransitions.get(key);
                    if (
                        !activeTransition ||
                        activeTransition.fromTriggerLevel !== previousLevel ||
                        activeTransition.toTriggerLevel !== triggerLevel ||
                        activeTransition.fromSourceFloorId !== previousSourceFloorId ||
                        activeTransition.toSourceFloorId !== sourceFloorId
                    ) {
                        this.buildingInteriorFloorTransitions.set(key, {
                            fromTriggerLevel: previousLevel,
                            toTriggerLevel: triggerLevel,
                            fromSourceFloorId: previousSourceFloorId,
                            toSourceFloorId: sourceFloorId,
                            startedAtMs: currentMs,
                            durationMs: BUILDING_CUTAWAY_INTERIOR_FLOOR_FADE_MS
                        });
                    }
                }

                const transition = this.buildingInteriorFloorTransitions.get(key);
                if (transition) {
                    const progress = this.getBuildingInteriorFloorTransitionProgress(transition, currentMs);
                    if (progress >= 1) {
                        this.buildingInteriorFloorTransitions.delete(key);
                    } else {
                        trigger.interiorFloorTransition = {
                            fromTriggerLevel: transition.fromTriggerLevel,
                            toTriggerLevel: transition.toTriggerLevel,
                            fromSourceFloorId: transition.fromSourceFloorId,
                            toSourceFloorId: transition.toSourceFloorId,
                            startedAtMs: transition.startedAtMs,
                            durationMs: transition.durationMs,
                            progress
                        };
                    }
                }

                this.buildingInteriorFloorCutawayLevels.set(key, { triggerLevel, sourceFloorId });
            }

            for (const key of Array.from(this.buildingInteriorFloorCutawayLevels.keys())) {
                if (activeKeys.has(key)) continue;
                this.buildingInteriorFloorCutawayLevels.delete(key);
                this.buildingInteriorFloorTransitions.delete(key);
            }
        }

        buildBuildingOcclusionPolygons(ctx, map, building, roofs = null) {
            const fragments = this.getBuildingFragments(map, building);
            if (fragments.length === 0) return [];
            const polygons = [];
            const layerPointsByLevel = new Map();
            const addLayerPoints = (level, points) => {
                if (!Array.isArray(points) || points.length < 3) return;
                if (!layerPointsByLevel.has(level)) layerPointsByLevel.set(level, []);
                layerPointsByLevel.get(level).push(...points);
            };
            const addProjectedFloor = (fragment, baseZ, levelForSide = null) => {
                const outer = fragment.outerPolygon;
                const projected = this.projectWorldRingToCutawayPlane(outer, baseZ);
                if (projected.length < 3) return;
                polygons.push(projected);
                if (Number.isFinite(levelForSide)) addLayerPoints(levelForSide, projected);
            };

            let minLevel = Infinity;
            for (let i = 0; i < fragments.length; i++) {
                const level = this.getLayerIndexFromValue(fragments[i].level, 0);
                minLevel = Math.min(minLevel, level);
                const baseZ = Number.isFinite(fragments[i].nodeBaseZ)
                    ? Number(fragments[i].nodeBaseZ)
                    : this.getLayerBaseZForLevel(level);
                addProjectedFloor(fragments[i], baseZ, level);
            }

            for (let i = 0; i < fragments.length; i++) {
                const fragment = fragments[i];
                const level = this.getLayerIndexFromValue(fragment.level, 0);
                if (level === minLevel) addProjectedFloor(fragment, this.getLayerBaseZForLevel(0), 0);
                if (!this.floorFragmentOverlapsAnyAbove(map, fragment, fragments)) {
                    const baseZ = Number.isFinite(fragment.nodeBaseZ)
                        ? Number(fragment.nodeBaseZ) + FLOOR_LAYER_DEFAULT_HEIGHT_UNITS
                        : this.getLayerBaseZForLevel(level + 1);
                    addProjectedFloor(fragment, baseZ, null);
                }
            }

            const buildingRoofs = Array.isArray(roofs) ? roofs : this.getRoofsForBuilding(ctx, map, building);
            for (let i = 0; i < buildingRoofs.length; i++) {
                polygons.push(...this.collectRoofOcclusionPolygons(buildingRoofs[i], layerPointsByLevel));
            }

            const levels = Array.from(layerPointsByLevel.keys()).sort((a, b) => a - b);
            if (levels.length >= 2) {
                const left = [];
                const right = [];
                for (let i = 0; i < levels.length; i++) {
                    const points = layerPointsByLevel.get(levels[i]) || [];
                    if (points.length === 0) continue;
                    let leftPoint = points[0];
                    let rightPoint = points[0];
                    for (let p = 1; p < points.length; p++) {
                        if (points[p].x < leftPoint.x) leftPoint = points[p];
                        if (points[p].x > rightPoint.x) rightPoint = points[p];
                    }
                    left.push(leftPoint);
                    right.push(rightPoint);
                }
                const silhouette = left.concat(right.reverse());
                if (silhouette.length >= 3) polygons.push(silhouette);
            }

            return unionFloorVisualPolygons(polygons);
        }

        getBuildingOcclusionPolygons(ctx, map, building) {
            if (!building) return [];
            const cache = this.getCompiledBuildingRenderCache(ctx, map, building);
            return cache && Array.isArray(cache.occlusionPolygons) ? cache.occlusionPolygons : [];
        }

        getPrototypeBuildingExteriorBitmapForCutaway(ctx, mapRef, placement) {
            if (!placement || typeof placement.id !== "string" || placement.id.length === 0) {
                throw new Error("prototype building cutaway bitmap requires a placement id");
            }
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            if (!map || typeof map.getPrototypeBuildingExteriorBitmap !== "function") return null;
            let cache = map.getPrototypeBuildingExteriorBitmap(placement.id);
            if (cache && cache.status === "error") {
                throw new Error(`prototype building exterior ${placement.id} bitmap failed: ${cache.error || "unknown error"}`);
            }
            if ((!cache || cache.status === "loading") && typeof map.requestPrototypeBuildingExteriorBitmap === "function") {
                const appRef = (ctx && ctx.app) || global.app || null;
                const rendererRef = (ctx && ctx.app && ctx.app.renderer) || (global.app && global.app.renderer) || null;
                if (!appRef || !rendererRef) return null;
                cache = map.requestPrototypeBuildingExteriorBitmap(placement, {
                    app: appRef,
                    renderer: rendererRef,
                    pitch: Math.PI / 4,
                    pixelsPerWorldUnit: 72,
                    paddingPixels: 48,
                    maxDimension: 2048
                });
            }
            if (cache && cache.status === "error") {
                throw new Error(`prototype building exterior ${placement.id} bitmap failed: ${cache.error || "unknown error"}`);
            }
            return cache && cache.status === "ready" ? cache : null;
        }

        getPrototypeBuildingExteriorAlphaMask(ctx, cache) {
            if (!cache || !cache.texture) {
                throw new Error("prototype building exterior alpha mask requires a ready texture");
            }
            const mask = cache.alphaMask || null;
            if (
                !mask ||
                !mask.pixels ||
                typeof mask.pixels.length !== "number" ||
                !(Number(mask.width) > 0) ||
                !(Number(mask.height) > 0)
            ) {
                throw new Error(`prototype building exterior ${cache.id || "(unknown)"} ready cache is missing its alpha mask`);
            }
            return mask;
        }

        isPrototypeBuildingExteriorBitmapPointOpaque(ctx, placement, cache, projectedPoint) {
            if (!placement || !cache || !projectedPoint) return false;
            const transform = placement.transform || {};
            const worldX = Number(transform.x);
            const worldY = Number(transform.y);
            if (!Number.isFinite(worldX) || !Number.isFinite(worldY)) {
                throw new Error(`prototype building exterior ${placement.id || cache.id || "(unknown)"} requires a finite placement transform for cutaway`);
            }
            const worldWidth = cache.bounds && Number.isFinite(cache.bounds.worldWidth)
                ? Math.max(0.01, Number(cache.bounds.worldWidth))
                : Math.max(0.01, Number(cache.width || 1) / Math.max(1e-6, Number(cache.pixelsPerWorldUnit) || 72));
            const worldHeight = cache.bounds && Number.isFinite(cache.bounds.worldHeight)
                ? Math.max(0.01, Number(cache.bounds.worldHeight))
                : Math.max(0.01, Number(cache.height || 1) / (Math.max(1e-6, Number(cache.pixelsPerWorldUnit) || 72) * Math.max(1e-6, Number(cache.xyratio) || 0.66)));
            const anchorX = Number.isFinite(cache.anchorX) ? Number(cache.anchorX) : 0.5;
            const anchorY = Number.isFinite(cache.anchorY) ? Number(cache.anchorY) : 1;
            const left = worldX - anchorX * worldWidth;
            const top = worldY - anchorY * worldHeight;
            const u = (Number(projectedPoint.x) - left) / worldWidth;
            const v = (Number(projectedPoint.y) - top) / worldHeight;
            if (!(u >= 0 && u <= 1 && v >= 0 && v <= 1)) return false;
            const mask = this.getPrototypeBuildingExteriorAlphaMask(ctx, cache);
            const px = Math.max(0, Math.min(mask.width - 1, Math.floor(u * mask.width)));
            const flippedV = 1 - v;
            const py = Math.max(0, Math.min(mask.height - 1, Math.floor(flippedV * mask.height)));
            const alpha = mask.pixels[py * mask.width + px];
            return alpha > PROTOTYPE_BUILDING_EXTERIOR_CUTAWAY_ALPHA_THRESHOLD;
        }

        getWizardShadowCenterCutawayPoint(wizard, resolvedPosition, wizardBaseZ) {
            const x = Number(resolvedPosition && resolvedPosition.x);
            const y = Number(resolvedPosition && resolvedPosition.y);
            if (!Number.isFinite(x) || !Number.isFinite(y)) return null;
            const baseZ = Number.isFinite(wizardBaseZ) ? Number(wizardBaseZ) : 0;
            const support = wizard && wizard.currentMovementSupport && typeof wizard.currentMovementSupport === "object"
                ? wizard.currentMovementSupport
                : null;
            let shadowLocalZ = 0;
            if (wizard && wizard._floorFallState && wizard._floorFallState.active) {
                shadowLocalZ = 0;
            } else if (support && support.type === "stair" && Number.isFinite(Number(support.continuousLocalZ))) {
                shadowLocalZ = Number(support.continuousLocalZ);
            } else if (support && support.type === "stair" && Number.isFinite(Number(support.localZ))) {
                shadowLocalZ = Number(support.localZ);
            } else if (wizard && wizard.isJumping) {
                shadowLocalZ = 0;
            } else {
                const interpolatedZ = Number.isFinite(Number(resolvedPosition && resolvedPosition.z))
                    ? Number(resolvedPosition.z)
                    : (Number.isFinite(Number(wizard && wizard.z)) ? Number(wizard.z) : 0);
                const jumpHeight = Number.isFinite(Number(wizard && wizard.jumpHeight))
                    ? Math.max(0, Number(wizard.jumpHeight))
                    : 0;
                shadowLocalZ = Math.max(0, interpolatedZ - jumpHeight);
            }
            return this.projectWorldPointToCutawayPlane(x, y + 0.18, baseZ + shadowLocalZ);
        }

        getLayerCutawayState(ctx) {
            if (ctx && ctx._renderingLayerCutawayState) return ctx._renderingLayerCutawayState;
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const cutawayStartMs = diagnosticsEnabled ? performance.now() : 0;
            let cutawayHeldDuringBubble = 0;
            let buildingsScanned = 0;
            let buildingBoundsTests = 0;
            let buildingPointTests = 0;
            let buildingTriggers = 0;
            let floorFallbackScanned = 0;
            let bubbleShiftActive = false;
            let cutawayRefreshDuringBubble = false;
            const finish = (state) => {
                if (ctx && typeof ctx === "object") ctx._renderingLayerCutawayState = state;
                if (
                    (!bubbleShiftActive || cutawayRefreshDuringBubble) &&
                    state &&
                    !state._doorwayPresentationTransition &&
                    !this.cutawayStateHasActiveInteriorRegion(state)
                ) {
                    const stableState = this.cloneBuildingDoorwayStablePresentationState(state);
                    if (stableState) this._prototypeBubbleStableCutawayState = stableState;
                }
                if (diagnosticsEnabled) {
                    this.setFrameMetric("layerCutawayStateMs", performance.now() - cutawayStartMs);
                    this.setFrameMetric("layerCutawayHeldDuringBubble", cutawayHeldDuringBubble);
                    this.setFrameMetric("layerCutawayBuildingsScanned", buildingsScanned);
                    this.setFrameMetric("layerCutawayBuildingBoundsTests", buildingBoundsTests);
                    this.setFrameMetric("layerCutawayBuildingPointTests", buildingPointTests);
                    this.setFrameMetric("layerCutawayBuildingTriggers", buildingTriggers);
                    this.setFrameMetric("layerCutawayFloorFallbackScanned", floorFallbackScanned);
                }
                return state;
            };
            const map = ctx && ctx.map ? ctx.map : global.map || null;
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const bubbleShiftSession = (
                map &&
                map._prototypeBubbleShiftSession &&
                map._prototypeBubbleShiftSession.completed !== true
            ) ? map._prototypeBubbleShiftSession : null;
            bubbleShiftActive = !!bubbleShiftSession;
            cutawayRefreshDuringBubble = !!(
                bubbleShiftSession &&
                bubbleShiftSession.phase === "refresh"
            );
            if (!(map && map.floorsById instanceof Map) || !wizard) {
                this.buildingDoorwayPresentationTransition = null;
                this.clearBuildingInteriorFloorTransitionState();
                return finish({ active: false, triggers: [], hiddenFromLevel: Infinity, hiddenSurfaceIds: new Set() });
            }
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const wizardPos = this.resolveInterpolatedItemWorldPosition(wizard, map);
            const x = Number(wizardPos && wizardPos.x);
            const y = Number(wizardPos && wizardPos.y);
            if (!Number.isFinite(x) || !Number.isFinite(y)) {
                this.buildingDoorwayPresentationTransition = null;
                this.clearBuildingInteriorFloorTransitionState();
                return finish({ active: false, triggers: [], hiddenFromLevel: Infinity, hiddenSurfaceIds: new Set() });
            }
            const wizardBaseZ = this.getWizardVisualLayerBaseZ(wizard, wizardLayer);
            const wizardWorldZ = this.getWizardContinuousWorldZ(wizard, wizardBaseZ);
            const nowMs = Number.isFinite(ctx && ctx.renderNowMs)
                ? Number(ctx.renderNowMs)
                : Date.now();

            const canHoldCutawayDuringBubble = !!(
                bubbleShiftActive &&
                !cutawayRefreshDuringBubble &&
                wizardLayer <= 0 &&
                !this.buildingDoorwayPresentationTransition
            );
            if (canHoldCutawayDuringBubble) {
                const stableState = this._prototypeBubbleStableCutawayState || null;
                let heldState = stableState && !this.cutawayStateHasActiveInteriorRegion(stableState)
                    ? this.cloneBuildingDoorwayStablePresentationState(stableState)
                    : null;
                if (!heldState) {
                    heldState = {
                        active: false,
                        wizardLayer,
                        wizardBaseZ: wizardWorldZ,
                        wizardX: x,
                        wizardY: y,
                        triggers: [],
                        hiddenFromLevel: Infinity,
                        hiddenSurfaceIds: new Set(),
                        hiddenFragmentIds: new Set()
                    };
                }
                heldState._heldDuringPrototypeBubbleShift = true;
                cutawayHeldDuringBubble = 1;
                return finish(heldState);
            }

            if (wizardLayer < 0) {
                this.buildingDoorwayPresentationTransition = null;
                this.clearBuildingInteriorFloorTransitionState();
                return finish({
                    active: true,
                    wizardLayer,
                    wizardBaseZ,
                    wizardX: x,
                    wizardY: y,
                    triggers: [],
                    hiddenFromLevel: 0,
                    globalHiddenFromLevel: 0,
                    hiddenSurfaceIds: new Set(),
                    hiddenFragmentIds: new Set()
                });
            }

            const triggers = [];
            const hiddenSurfaceIds = new Set();
            const hiddenFragmentIds = new Set();
            const activeBuildingCutawayIds = new Set();
            let hiddenFromLevel = Infinity;
            if (typeof map.ensureFloorBuildings === "function") {
                const buildings = map.ensureFloorBuildings();
                const wizardProjection = this.projectWorldPointToCutawayPlane(x, y, wizardWorldZ);
                if (wizardProjection && buildings instanceof Map) {
                    for (const building of buildings.values()) {
                        buildingsScanned += 1;
                        if (!building) continue;
                        const buildingMaxLevel = this.getLayerIndexFromValue(building.maxLevel, -Infinity);
                        const buildingTopZ = Number.isFinite(buildingMaxLevel)
                            ? this.getLayerBaseZForLevel(buildingMaxLevel + 1)
                            : Infinity;
                        if (wizardLayer > buildingMaxLevel || wizardWorldZ >= buildingTopZ - 1e-6) {
                            continue;
                        }
                        const renderCache = this.getCompiledBuildingRenderCache(ctx, map, building);
                        if (!renderCache) continue;
                        const occlusionPolygons = renderCache.occlusionPolygons;
                        const groundProjectionPolygons = renderCache.groundProjectionPolygons;
                        buildingBoundsTests += 1;
                        if (!this.isCutawayPointInsideBounds(wizardProjection, renderCache.occlusionBounds)) {
                            continue;
                        }
                        buildingPointTests += 1;
                        if (!floorVisualPolygonsContainPoint(occlusionPolygons, wizardProjection.x, wizardProjection.y)) {
                            continue;
                        }
                        const buildingAlpha = this.getBuildingCutawayEntryAlpha(building.buildingId, nowMs);
                        const activeInteriorRegion = this.getBuildingInteriorVisualRegionAtPoint(renderCache, x, y, wizardLayer, wizardWorldZ);
                        const visibleThroughLevel = activeInteriorRegion
                            ? this.getLayerIndexFromValue(activeInteriorRegion.level, 0)
                            : null;
                        const triggerLevel = activeInteriorRegion
                            ? (activeInteriorRegion.kind === "groundFootprint"
                                ? this.getLayerIndexFromValue(building.minLevel, 0)
                                : this.getLayerIndexFromValue(visibleThroughLevel + 1, building.minLevel))
                            : this.getLayerIndexFromValue(building.minLevel, 0);
                        const trigger = {
                            level: triggerLevel,
                            baseZ: this.getLayerBaseZForLevel(triggerLevel),
                            buildingId: building.buildingId,
                            building,
                            renderCache,
                            distanceToOcclusion: 0,
                            alpha: buildingAlpha,
                            visibleThroughLevel: Number.isFinite(visibleThroughLevel) ? visibleThroughLevel : null,
                            activeInteriorRegion,
                            activeInteriorRegionId: activeInteriorRegion && typeof activeInteriorRegion.id === "string"
                                ? activeInteriorRegion.id
                                : "",
                            visibleThroughPolygons: activeInteriorRegion && activeInteriorRegion.kind === "groundFootprint"
                                ? [activeInteriorRegion.polygon]
                                : null,
                            groundProjectionPolygons,
                            fragmentIds: renderCache.fragmentIds,
                            surfaceIds: renderCache.surfaceIds,
                            occlusionPolygons
                        };
                        triggers.push(trigger);
                        activeBuildingCutawayIds.add(this.getBuildingCutawayTransitionKey(building.buildingId));
                        this.rememberBuildingCutawayTriggerForExit(trigger);
                        buildingTriggers += 1;
                        hiddenFromLevel = Math.min(hiddenFromLevel, trigger.level);
                        for (const surfaceId of trigger.surfaceIds) hiddenSurfaceIds.add(surfaceId);
                        for (const fragmentId of trigger.fragmentIds) hiddenFragmentIds.add(fragmentId);
                    }
                }
            }

            if (typeof map.getPrototypeBuildingCutawayBuildings === "function") {
                const prototypeBuildings = map.getPrototypeBuildingCutawayBuildings();
                const wizardProjection = this.getWizardShadowCenterCutawayPoint(wizard, wizardPos, wizardBaseZ);
                if (wizardProjection && Array.isArray(prototypeBuildings)) {
                    for (let i = 0; i < prototypeBuildings.length; i++) {
                        const building = prototypeBuildings[i];
                        buildingsScanned += 1;
                        if (!building) continue;
                        const placement = building._prototypeBuildingPlacement || null;
                        if (!placement || typeof placement.id !== "string") {
                            throw new Error(`prototype building cutaway record ${building.buildingId || "(unknown)"} is missing its placement`);
                        }
                        const buildingMaxLevel = this.getLayerIndexFromValue(building.maxLevel, -Infinity);
                        const buildingTopZ = Number.isFinite(Number(building.maxTopZ))
                            ? Number(building.maxTopZ)
                            : (Number.isFinite(buildingMaxLevel)
                                ? this.getLayerBaseZForLevel(buildingMaxLevel + 1)
                                : Infinity);
                        if (wizardLayer > buildingMaxLevel || wizardWorldZ >= buildingTopZ - 1e-6) {
                            continue;
                        }
                        const renderCache = this.getCompiledBuildingRenderCache(ctx, map, building);
                        if (!renderCache) continue;
                        const occlusionPolygons = renderCache.occlusionPolygons;
                        const groundProjectionPolygons = renderCache.groundProjectionPolygons;
                        const buildingAlpha = this.getBuildingCutawayEntryAlpha(building.buildingId, nowMs);
                        buildingBoundsTests += 1;
                        if (!this.isCutawayPointInsideBounds(wizardProjection, renderCache.occlusionBounds)) {
                            continue;
                        }
                        buildingPointTests += 1;
                        const exteriorCache = this.getPrototypeBuildingExteriorBitmapForCutaway(ctx, map, placement);
                        if (!exteriorCache) continue;
                        if (!this.isPrototypeBuildingExteriorBitmapPointOpaque(ctx, placement, exteriorCache, wizardProjection)) {
                            continue;
                        }
                        const activeInteriorRegion = this.getBuildingInteriorVisualRegionAtPoint(renderCache, x, y, wizardLayer, wizardWorldZ);
                        const visibleThroughLevel = activeInteriorRegion
                            ? this.getLayerIndexFromValue(activeInteriorRegion.level, 0)
                            : null;
                        const triggerLevel = activeInteriorRegion
                            ? (activeInteriorRegion.kind === "groundFootprint"
                                ? this.getLayerIndexFromValue(building.minLevel, 0)
                                : this.getLayerIndexFromValue(visibleThroughLevel + 1, building.minLevel))
                            : this.getLayerIndexFromValue(building.minLevel, 0);
                        const trigger = {
                            level: triggerLevel,
                            baseZ: this.getLayerBaseZForLevel(triggerLevel),
                            buildingId: building.buildingId,
                            building,
                            renderCache,
                            distanceToOcclusion: 0,
                            alpha: buildingAlpha,
                            visibleThroughLevel: Number.isFinite(visibleThroughLevel) ? visibleThroughLevel : null,
                            activeInteriorRegion,
                            activeInteriorRegionId: activeInteriorRegion && typeof activeInteriorRegion.id === "string"
                                ? activeInteriorRegion.id
                                : "",
                            visibleThroughPolygons: activeInteriorRegion && activeInteriorRegion.kind === "groundFootprint"
                                ? [activeInteriorRegion.polygon]
                                : null,
                            groundProjectionPolygons,
                            fragmentIds: renderCache.fragmentIds,
                            surfaceIds: renderCache.surfaceIds,
                            occlusionPolygons
                        };
                        triggers.push(trigger);
                        activeBuildingCutawayIds.add(this.getBuildingCutawayTransitionKey(building.buildingId));
                        this.rememberBuildingCutawayTriggerForExit(trigger);
                        buildingTriggers += 1;
                        hiddenFromLevel = Math.min(hiddenFromLevel, trigger.level);
                        for (const surfaceId of trigger.surfaceIds) hiddenSurfaceIds.add(surfaceId);
                        for (const fragmentId of trigger.fragmentIds) hiddenFragmentIds.add(fragmentId);
                    }
                }
            }

            const exitTriggers = this.getBuildingCutawayExitTriggers(activeBuildingCutawayIds, nowMs);
            for (const trigger of exitTriggers) {
                triggers.push(trigger);
                buildingTriggers += 1;
                hiddenFromLevel = Math.min(hiddenFromLevel, trigger.level);
                if (trigger.surfaceIds instanceof Set) {
                    for (const surfaceId of trigger.surfaceIds) hiddenSurfaceIds.add(surfaceId);
                }
                if (trigger.fragmentIds instanceof Set) {
                    for (const fragmentId of trigger.fragmentIds) hiddenFragmentIds.add(fragmentId);
                }
            }

            const buildingState = {
                active: triggers.length > 0,
                wizardLayer,
                wizardBaseZ: wizardWorldZ,
                wizardX: x,
                wizardY: y,
                triggers,
                hiddenFromLevel,
                hiddenSurfaceIds,
                hiddenFragmentIds
            };
            this.updateBuildingInteriorFloorTransitions(triggers, nowMs);
            const doorwayState = this.updateBuildingDoorwayPresentationTransition(
                ctx,
                buildingState,
                map,
                wizard,
                nowMs
            );
            if (
                triggers.length > 0 ||
                (doorwayState && doorwayState._doorwayPresentationTransition)
            ) {
                return finish(doorwayState);
            }

            for (const fragment of map.floorsById.values()) {
                floorFallbackScanned += 1;
                if (!fragment) continue;
                const level = this.getLayerIndexFromValue(fragment.level, 0);
                if (level <= wizardLayer) continue;
                const fragmentBaseZ = Number.isFinite(fragment.nodeBaseZ)
                    ? Number(fragment.nodeBaseZ)
                    : this.getLayerBaseZForLevel(level);
                const projectedY = y + (fragmentBaseZ - wizardWorldZ);
                if (!isPointInsideFloorVisibilityFragment(fragment, x, projectedY)) continue;
                const trigger = {
                    level,
                    baseZ: fragmentBaseZ,
                    surfaceId: typeof fragment.surfaceId === "string" ? fragment.surfaceId : "",
                    fragmentId: typeof fragment.fragmentId === "string" ? fragment.fragmentId : "",
                    fragment
                };
                triggers.push(trigger);
                hiddenFromLevel = Math.min(hiddenFromLevel, level);
                if (trigger.surfaceId) hiddenSurfaceIds.add(trigger.surfaceId);
                if (trigger.fragmentId) hiddenFragmentIds.add(trigger.fragmentId);
            }

            return finish({
                active: triggers.length > 0,
                wizardLayer,
                wizardBaseZ: wizardWorldZ,
                wizardX: x,
                wizardY: y,
                triggers,
                hiddenFromLevel,
                hiddenSurfaceIds,
                hiddenFragmentIds
            });
        }

        floorFragmentMatchesCutawayTrigger(fragment, trigger) {
            if (!fragment || !trigger) return false;
            const fragmentId = typeof fragment.fragmentId === "string" ? fragment.fragmentId : "";
            const surfaceId = typeof fragment.surfaceId === "string" ? fragment.surfaceId : "";
            if (fragmentId && trigger.fragmentIds instanceof Set && trigger.fragmentIds.has(fragmentId)) return true;
            if (surfaceId && trigger.surfaceIds instanceof Set && trigger.surfaceIds.has(surfaceId)) return true;
            if (fragment.buildingId && trigger.buildingId && fragment.buildingId === trigger.buildingId) return true;
            const triggerFragmentId = typeof trigger.fragmentId === "string" ? trigger.fragmentId : "";
            const triggerSurfaceId = typeof trigger.surfaceId === "string" ? trigger.surfaceId : "";
            if (fragmentId && triggerFragmentId && fragmentId === triggerFragmentId) return true;
            if (surfaceId && triggerSurfaceId && surfaceId === triggerSurfaceId) return true;
            return false;
        }

        getBuildingInteriorFloorAlphaAtTriggerLevel(fragmentLevel, triggerLevel, cutawayAlpha) {
            if (!Number.isFinite(fragmentLevel) || !Number.isFinite(triggerLevel)) return 1;
            return fragmentLevel >= triggerLevel ? cutawayAlpha : 1;
        }

        getBuildingInteriorFloorTransitionAlpha(fragment, trigger, fallbackAlpha) {
            const transition = trigger && trigger.interiorFloorTransition;
            if (!transition || !fragment || !trigger || !trigger.activeInteriorRegion) return fallbackAlpha;
            const fragmentLevel = this.getLayerIndexFromValue(fragment.level, NaN);
            const fromTriggerLevel = this.getLayerIndexFromValue(transition.fromTriggerLevel, NaN);
            const toTriggerLevel = this.getLayerIndexFromValue(transition.toTriggerLevel, NaN);
            const progress = Number(transition.progress);
            if (
                !Number.isFinite(fragmentLevel) ||
                !Number.isFinite(fromTriggerLevel) ||
                !Number.isFinite(toTriggerLevel) ||
                !Number.isFinite(progress)
            ) {
                return fallbackAlpha;
            }
            const clampedProgress = Math.max(0, Math.min(1, progress));
            const cutawayAlpha = Number.isFinite(trigger.alpha)
                ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                : BUILDING_CUTAWAY_GHOST_ALPHA;
            const fromAlpha = this.getBuildingInteriorFloorAlphaAtTriggerLevel(fragmentLevel, fromTriggerLevel, cutawayAlpha);
            const toAlpha = this.getBuildingInteriorFloorAlphaAtTriggerLevel(fragmentLevel, toTriggerLevel, cutawayAlpha);
            return fromAlpha + ((toAlpha - fromAlpha) * clampedProgress);
        }

        getPrototypeBuildingInteriorBitmapTransitionAlphas(transition) {
            const progress = Math.max(0, Math.min(1, Number(transition && transition.progress)));
            if (!Number.isFinite(progress)) {
                return { toAlpha: 1, fromAlpha: 0 };
            }
            return {
                toAlpha: 1,
                fromAlpha: 1 - progress
            };
        }

        getFloorFragmentCutawayAlpha(fragment, cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active || !fragment) return 1;
            const level = this.getLayerIndexFromValue(fragment.level, 0);
            if (this.isFallRevealHiddenLayer(level)) return 1;
            if (Number.isFinite(state.globalHiddenFromLevel) && level >= state.globalHiddenFromLevel) return 0;
            const triggers = Array.isArray(state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger) continue;
                let triggerLevel = this.getLayerIndexFromValue(trigger.level, Infinity);
                const transition = trigger.interiorFloorTransition;
                if (transition) {
                    const fromTriggerLevel = this.getLayerIndexFromValue(transition.fromTriggerLevel, Infinity);
                    const toTriggerLevel = this.getLayerIndexFromValue(transition.toTriggerLevel, Infinity);
                    triggerLevel = Math.min(triggerLevel, fromTriggerLevel, toTriggerLevel);
                }
                if (level < triggerLevel) continue;
                if (this.floorFragmentMatchesCutawayTrigger(fragment, trigger)) {
                    if (trigger.building) {
                        const alpha = Number.isFinite(trigger.alpha)
                            ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                            : BUILDING_CUTAWAY_GHOST_ALPHA;
                        return this.getBuildingInteriorFloorTransitionAlpha(fragment, trigger, alpha);
                    }
                    return 0;
                }
            }
            return 1;
        }

        isFloorFragmentHiddenByLayerCutaway(fragment, cutawayState = null) {
            return this.getFloorFragmentCutawayAlpha(fragment, cutawayState) <= 0.001;
        }

        getBuildingCutawayAlphaForItem(_item) {
            return 1;
        }

        shouldRenderBuildingInteriorOpeningAtFullAlpha(item, foregroundPlan = null) {
            return !!(
                foregroundPlan &&
                foregroundPlan.items instanceof Set &&
                foregroundPlan.items.has(item) &&
                this.isBuildingCutawayMountedOpeningItem(item)
            );
        }

        getRenderAlphaForObjectItem(item, layerAlpha, cutawayAlpha, foregroundPlan = null) {
            if (this.shouldRenderBuildingInteriorOpeningAtFullAlpha(item, foregroundPlan)) return 1;
            const normalizedLayerAlpha = Number.isFinite(layerAlpha)
                ? Math.max(0, Math.min(1, Number(layerAlpha)))
                : 1;
            const normalizedCutawayAlpha = Number.isFinite(cutawayAlpha)
                ? Math.max(0, Math.min(1, Number(cutawayAlpha)))
                : 1;
            return normalizedLayerAlpha * normalizedCutawayAlpha;
        }

        getBuildingCutawayCompositeAlphaForItem(item) {
            if (
                item &&
                item._cutawayCompositeFrame === this._layerCutawayFrameId &&
                Number.isFinite(item._cutawayCompositeAlpha)
            ) {
                return Math.max(0, Math.min(1, Number(item._cutawayCompositeAlpha)));
            }
            return 1;
        }

        getBuildingCutawayCompositeCaptureAlphaForItem(item) {
            if (!item) return 1;
            if (Number.isFinite(item._renderLayerFadeAlpha)) {
                return Math.max(0, Math.min(1, Number(item._renderLayerFadeAlpha)));
            }
            if (Number.isFinite(item._renderLayerIndex)) {
                return this.getLiveLayerFadeMultiplier(item._renderLayerIndex);
            }
            return 1;
        }

        markBuildingCutawayCompositeCaptureAlpha(displayObj, alpha) {
            if (!displayObj) return;
            displayObj._buildingCutawayCompositeCaptureAlpha = Number.isFinite(alpha)
                ? Math.max(0, Math.min(1, Number(alpha)))
                : 1;
        }

        getBuildingCutawayCompositeAlpha(cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            const triggers = this.getBuildingCutawayCompositeTriggers(state);
            let alpha = 1;
            let found = false;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) continue;
                if (trigger.activeInteriorRegion) return BUILDING_CUTAWAY_INTERIOR_ALPHA;
                const triggerAlpha = Number.isFinite(trigger.alpha)
                    ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                    : BUILDING_CUTAWAY_GHOST_ALPHA;
                alpha = Math.min(alpha, triggerAlpha);
                found = true;
            }
            return found ? alpha : 1;
        }

        isPrototypeBuildingCutawayTrigger(trigger) {
            return !!(
                trigger &&
                trigger.building &&
                trigger.building._prototypeBuildingPlacement
            );
        }

        getBuildingCutawayCompositeTriggers(cutawayState = null) {
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            return triggers.filter(trigger => (
                trigger &&
                trigger.building &&
                !this.isPrototypeBuildingCutawayTrigger(trigger)
            ));
        }

        getFloorFragmentBuildingCutawayTrigger(fragment, cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active || !fragment) return null;
            if (Number.isFinite(state.globalHiddenFromLevel)) return null;
            const level = this.getLayerIndexFromValue(fragment.level, 0);
            const triggers = Array.isArray(state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) continue;
                if (!trigger.activeInteriorRegion && level < this.getLayerIndexFromValue(trigger.level, Infinity)) continue;
                if (this.floorFragmentMatchesCutawayTrigger(fragment, trigger)) return trigger;
            }
            return null;
        }

        isBuildingCutawayDoorItem(item) {
            if (!item) return false;
            const type = typeof item.type === "string" ? item.type.trim().toLowerCase() : "";
            const category = typeof item.category === "string" ? item.category.trim().toLowerCase() : "";
            return type === "door" || category === "doors";
        }

        isBuildingCutawayWindowItem(item) {
            if (!item) return false;
            const type = typeof item.type === "string" ? item.type.trim().toLowerCase() : "";
            const category = typeof item.category === "string" ? item.category.trim().toLowerCase() : "";
            return type === "window" || category === "windows";
        }

        isBuildingCutawayStructuralItem(item) {
            if (!item) return false;
            if (item.type === "wallSection" || item.type === "roof") return true;
            if (this.isBuildingCutawayDoorItem(item) || this.isBuildingCutawayWindowItem(item)) return true;
            return false;
        }

        isBuildingFrameOnlyCutawayState(state) {
            if (!state || !state.active || state._buildingFrameFlagsApplied !== true) return false;
            const triggers = Array.isArray(state.triggers) ? state.triggers : [];
            if (triggers.length === 0) return false;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) return false;
                if (trigger.activeInteriorRegion) return false;
                if (this.isPrototypeBuildingCutawayTrigger(trigger)) return false;
            }
            return true;
        }

        isWorldPointHiddenByLayerCutaway(x, y, level, cutawayState = null, pointBaseZ = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active) return false;
            const itemLevel = this.getLayerIndexFromValue(level, 0);
            if (this.isFallRevealHiddenLayer(itemLevel)) return false;
            if (Number.isFinite(state.globalHiddenFromLevel) && itemLevel >= state.globalHiddenFromLevel) return true;
            this.incrementFrameMetric("layerCutawayWorldPointTests", 1);
            if (this.isBuildingFrameOnlyCutawayState(state)) {
                this.incrementFrameMetric("layerCutawayWorldPointFastPath", 1);
                return false;
            }
            if (!Number.isFinite(x) || !Number.isFinite(y)) return false;
            const sourceBaseZ = Number.isFinite(pointBaseZ)
                ? Number(pointBaseZ)
                : this.getLayerBaseZForLevel(itemLevel);
            const triggers = Array.isArray(state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || itemLevel < this.getLayerIndexFromValue(trigger.level, Infinity)) continue;
                const triggerBaseZ = Number.isFinite(trigger.baseZ)
                    ? Number(trigger.baseZ)
                    : this.getLayerBaseZForLevel(trigger.level);
                if (trigger.fragment && isPointInsideFloorVisibilityFragment(trigger.fragment, x, y + (triggerBaseZ - sourceBaseZ))) return true;
                if (Array.isArray(trigger.occlusionPolygons)) {
                    const projected = this.projectWorldPointToCutawayPlane(x, y, sourceBaseZ);
                    const bounds = trigger.building && trigger.building.occlusionBounds ? trigger.building.occlusionBounds : null;
                    if (
                        projected &&
                        (!bounds || this.isCutawayPointInsideBounds(projected, bounds)) &&
                        floorVisualPolygonsContainPoint(trigger.occlusionPolygons, projected.x, projected.y)
                    ) return true;
                }
            }
            return false;
        }

        collectFloorRefsForRenderItem(item, mapRef = null) {
            const refs = [];
            const seen = new Set();
            const pushRef = (source) => {
                if (!source || typeof source !== "object") return;
                const fragmentId = typeof source.fragmentId === "string" ? source.fragmentId : "";
                const surfaceId = typeof source.surfaceId === "string" ? source.surfaceId : "";
                if (!fragmentId && !surfaceId) return;
                const key = `${surfaceId}|${fragmentId}`;
                if (seen.has(key)) return;
                seen.add(key);
                refs.push({ surfaceId, fragmentId });
            };
            const pushNode = (node) => {
                pushRef(node);
            };
            const pushEndpoint = (endpoint) => {
                pushNode(endpoint);
                if (endpoint && endpoint.nodeA) pushNode(endpoint.nodeA);
                if (endpoint && endpoint.nodeB) pushNode(endpoint.nodeB);
            };

            pushRef(item);
            pushRef(item && item.currentMovementSupport);
            pushRef(item && item._activeFloorFragment);
            pushNode(item && item.node);
            if (Array.isArray(item && item._indexedNodes)) {
                item._indexedNodes.forEach(pushNode);
            }
            if (Array.isArray(item && item.nodes)) {
                item.nodes.forEach(pushNode);
            }
            if (Array.isArray(item && item.wallSections)) {
                item.wallSections.forEach(section => {
                    const sectionRefs = this.collectFloorRefsForRenderItem(section, mapRef);
                    for (let i = 0; i < sectionRefs.length; i++) pushRef(sectionRefs[i]);
                });
            }
            if (Array.isArray(item && item.wallLoopSectionIds)) {
                const wallCtor = global.WallSectionUnit;
                const allSections = wallCtor && wallCtor._allSections instanceof Map ? wallCtor._allSections : null;
                if (allSections) {
                    item.wallLoopSectionIds.forEach(id => {
                        const section = Number.isInteger(id) ? allSections.get(Number(id)) : null;
                        const sectionRefs = this.collectFloorRefsForRenderItem(section, mapRef);
                        for (let i = 0; i < sectionRefs.length; i++) pushRef(sectionRefs[i]);
                    });
                }
            }
            pushEndpoint(item && item.startPoint);
            pushEndpoint(item && item.endPoint);

            const mountedSection = this.resolveMountedWallSectionForItem(item);
            if (mountedSection && mountedSection !== item) {
                const mountedRefs = this.collectFloorRefsForRenderItem(mountedSection, mapRef);
                for (let i = 0; i < mountedRefs.length; i++) pushRef(mountedRefs[i]);
            }

            return refs;
        }

        resolveStraightStairLowerFragment(stair, mapRef = null) {
            if (!stair || stair.stairKind !== "straight") return null;
            const lowerFragmentId = typeof stair.lowerFragmentId === "string" ? stair.lowerFragmentId : "";
            if (!lowerFragmentId) {
                throw new Error(`straight stair ${stair.id || "(unknown)"} missing lower fragment id`);
            }
            if (!(mapRef && mapRef.floorsById instanceof Map)) {
                throw new Error(`straight stair ${stair.id || "(unknown)"} cannot resolve lower fragment without a floor registry`);
            }
            const direct = mapRef.floorsById.get(lowerFragmentId);
            if (direct) return direct;
            for (const fragment of mapRef.floorsById.values()) {
                if (fragment && fragment.fragmentId === lowerFragmentId) return fragment;
            }
            throw new Error(`straight stair ${stair.id || "(unknown)"} lower fragment ${lowerFragmentId} is not loaded`);
        }

        getStraightStairRenderObject(stair, mapRef = null) {
            if (!stair || stair.stairKind !== "straight") return null;
            const stairId = typeof stair.id === "string" && stair.id.length > 0
                ? stair.id
                : (typeof stair.transitionId === "string" ? stair.transitionId : "");
            if (!stairId) throw new Error("straight stair render record missing id");
            const lowerFragmentId = typeof stair.lowerFragmentId === "string" ? stair.lowerFragmentId : "";
            if (!lowerFragmentId) {
                throw new Error(`straight stair ${stairId} missing lower fragment id`);
            }
            const lowerFragment = this.resolveStraightStairLowerFragment(stair, mapRef);
            const lowerLevel = Number.isFinite(stair.lowerLevel) ? Math.round(Number(stair.lowerLevel)) : 0;
            const lowerPoint = stair.lowerPoint || null;
            const higherPoint = stair.higherPoint || null;
            const lowerX = Number(lowerPoint && lowerPoint.x);
            const lowerY = Number(lowerPoint && lowerPoint.y);
            const higherX = Number(higherPoint && higherPoint.x);
            const higherY = Number(higherPoint && higherPoint.y);
            if (!Number.isFinite(lowerX) || !Number.isFinite(lowerY) || !Number.isFinite(higherX) || !Number.isFinite(higherY)) {
                throw new Error(`straight stair ${stairId} has invalid render object endpoints`);
            }
            const mapForDelta = mapRef || (this.camera && this.camera.map) || global.map || null;
            let centerX = lowerX + (higherX - lowerX) * 0.5;
            let centerY = lowerY + (higherY - lowerY) * 0.5;
            if (mapForDelta && typeof mapForDelta.shortestDeltaX === "function") centerX = lowerX + (mapForDelta.shortestDeltaX(lowerX, higherX) * 0.5);
            if (mapForDelta && typeof mapForDelta.shortestDeltaY === "function") centerY = lowerY + (mapForDelta.shortestDeltaY(lowerY, higherY) * 0.5);
            if (mapForDelta && typeof mapForDelta.wrapWorldX === "function") centerX = mapForDelta.wrapWorldX(centerX);
            if (mapForDelta && typeof mapForDelta.wrapWorldY === "function") centerY = mapForDelta.wrapWorldY(centerY);

            if (!(this.stairRenderObjectById instanceof Map)) this.stairRenderObjectById = new Map();
            const cacheKey = `straightStair:${stairId}`;
            let item = this.stairRenderObjectById.get(cacheKey);
            if (!item) {
                item = {
                    id: cacheKey,
                    type: "straightStair",
                    isStairRenderObject: true,
                    gone: false,
                    vanishing: false,
                    visible: true
                };
                this.stairRenderObjectById.set(cacheKey, item);
            }
            item.stairId = stairId;
            item.stair = stair;
            item.map = mapRef || null;
            item.level = lowerLevel;
            item.traversalLayer = lowerLevel;
            item.currentLayer = lowerLevel;
            item.fragmentId = lowerFragmentId;
            item.surfaceId = lowerFragment && typeof lowerFragment.surfaceId === "string" ? lowerFragment.surfaceId : "";
            item.lowerFragmentId = lowerFragmentId;
            item.higherFragmentId = typeof stair.higherFragmentId === "string" ? stair.higherFragmentId : "";
            item.x = centerX;
            item.y = centerY;
            item.lowerPoint = stair.lowerPoint;
            item.higherPoint = stair.higherPoint;
            item.footprint = stair.footprint;
            return item;
        }

        getStairRenderObject(stair, mapRef = null) {
            if (!stair) return null;
            if (stair.renderedByBuildingCutaway === true) return null;
            if (stair.stairKind === "straight") return this.getStraightStairRenderObject(stair, mapRef);
            if (stair.stairKind === "treadPath") return this.getTreadPathStairRenderObject(stair, mapRef);
            const stairId = typeof stair.id === "string" && stair.id.length > 0
                ? stair.id
                : (typeof stair.transitionId === "string" ? stair.transitionId : "(unknown)");
            const kind = typeof stair.stairKind === "string" && stair.stairKind.length > 0
                ? stair.stairKind
                : "(missing)";
            throw new Error(`unsupported stair render kind ${kind} for stair ${stairId}`);
        }

        getTreadPathStairRenderObject(stair, mapRef = null) {
            if (!stair || stair.stairKind !== "treadPath") return null;
            const stairId = typeof stair.id === "string" && stair.id.length > 0
                ? stair.id
                : (typeof stair.transitionId === "string" ? stair.transitionId : "");
            if (!stairId) throw new Error("tread path stair render record missing id");
            const lowerPoint = stair.lowerPoint || null;
            const higherPoint = stair.higherPoint || null;
            const lowerX = Number(lowerPoint && lowerPoint.x);
            const lowerY = Number(lowerPoint && lowerPoint.y);
            const higherX = Number(higherPoint && higherPoint.x);
            const higherY = Number(higherPoint && higherPoint.y);
            if (!Number.isFinite(lowerX) || !Number.isFinite(lowerY) || !Number.isFinite(higherX) || !Number.isFinite(higherY)) {
                throw new Error(`tread path stair ${stairId} has invalid render object endpoints`);
            }
            const lowerLevel = Number.isFinite(stair.lowerLevel) ? Math.round(Number(stair.lowerLevel)) : 0;
            if (!(this.stairRenderObjectById instanceof Map)) this.stairRenderObjectById = new Map();
            const cacheKey = `treadPathStair:${stairId}`;
            let item = this.stairRenderObjectById.get(cacheKey);
            if (!item) {
                item = {
                    id: cacheKey,
                    type: "treadPathStair",
                    isStairRenderObject: true,
                    gone: false,
                    vanishing: false,
                    visible: true
                };
                this.stairRenderObjectById.set(cacheKey, item);
            }
            item.stairId = stairId;
            item.stair = stair;
            item.map = mapRef || null;
            item.level = lowerLevel;
            item.traversalLayer = lowerLevel;
            item.lowerFragmentId = typeof stair.lowerFragmentId === "string" ? stair.lowerFragmentId : "";
            item.higherFragmentId = typeof stair.higherFragmentId === "string" ? stair.higherFragmentId : "";
            item.lowerSurfaceId = typeof stair.lowerSurfaceId === "string" ? stair.lowerSurfaceId : "";
            item.higherSurfaceId = typeof stair.higherSurfaceId === "string" ? stair.higherSurfaceId : "";
            item.x = (lowerX + higherX) * 0.5;
            item.y = (lowerY + higherY) * 0.5;
            item.lowerPoint = stair.lowerPoint;
            item.higherPoint = stair.higherPoint;
            return item;
        }

        collectVisibleStairRenderObjects(ctx, mapRef, seen, out) {
            if (!mapRef || !(mapRef.stairsById instanceof Map) || !seen || !out) return 0;
            let added = 0;
            for (const stair of mapRef.stairsById.values()) {
                if (stair && stair.renderedByBuildingCutaway === true) continue;
                const item = this.getStairRenderObject(stair, mapRef);
                if (!item || seen.has(item)) continue;
                seen.add(item);
                out.push(item);
                added += 1;
            }
            return added;
        }

        shouldRenderBuildingInteriorFloorRenderItem(item, region, ctx, mapRef = null) {
            if (!item || item.gone || item.vanishing || !region) return false;
            if (item === ((ctx && ctx.wizard) || global.wizard || null)) return false;
            if (!this.isScriptVisible(item)) return false;
            const regionLevel = this.getLayerIndexFromValue(region.level, 0);
            const itemLevel = this.getBuildingInteriorEntryLayer(item, null, regionLevel);
            if (itemLevel !== regionLevel) return false;
            const refs = this.collectFloorRefsForRenderItem(item, mapRef || (ctx && ctx.map) || global.map || null);
            if (this.renderItemRefsMatchBuildingInteriorRegion(refs, region)) return true;
            if (this.hasBuildingInteriorFloorRefs(refs) && region.kind === "floorFragment") return false;
            if (region.polygon) {
                return this.renderItemSamplesMatchPolygons(item, [region.polygon], mapRef || (ctx && ctx.map) || global.map || null);
            }
            return false;
        }

        collectBuildingInteriorFloorRenderItems(ctx, region, mapRef = null, trigger = null) {
            const out = [];
            const seen = new Set();
            const add = (item) => {
                if (!item || seen.has(item)) return;
                seen.add(item);
                out.push(item);
            };
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            if (!map) return out;
            const canonicalObjects = this.getCanonicalFloorObjectsForBuildingInteriorRegion(region, map, trigger);
            for (let i = 0; i < canonicalObjects.length; i++) {
                const item = canonicalObjects[i];
                if (this.shouldRenderBuildingInteriorFloorRenderItem(item, region, ctx, map)) add(item);
            }
            if (map.stairsById instanceof Map) {
                for (const stair of map.stairsById.values()) {
                    if (stair && stair.renderedByBuildingCutaway === true) continue;
                    const item = this.getStairRenderObject(stair, map);
                    if (this.shouldRenderBuildingInteriorFloorRenderItem(item, region, ctx, map)) {
                        add(item);
                    }
                }
            }
            return out;
        }

        getCanonicalFloorObjectsForBuildingInteriorRegion(region, mapRef = null, trigger = null) {
            const map = mapRef || global.map || null;
            if (!map || typeof map.getObjectsForFloorMembership !== "function" || !region) return [];
            const membership = this.getBuildingInteriorRegionFloorMembership(region, trigger);
            if (!membership) return [];
            return map.getObjectsForFloorMembership(membership);
        }

        getFloorObjectRenderSignature(item) {
            if (!item || typeof item !== "object") return "";
            const numberOrNull = (value) => Number.isFinite(Number(value)) ? Number(Number(value).toFixed(4)) : null;
            const membership = item._floorMembership && typeof item._floorMembership === "object" ? item._floorMembership : null;
            return JSON.stringify({
                type: typeof item.type === "string" ? item.type : "",
                objectType: typeof item.objectType === "string" ? item.objectType : "",
                category: typeof item.category === "string" ? item.category : "",
                texturePath: typeof item.texturePath === "string" ? item.texturePath : "",
                imagePath: typeof item.imagePath === "string" ? item.imagePath : "",
                imageFileName: typeof item.imageFileName === "string" ? item.imageFileName : "",
                x: numberOrNull(item.x),
                y: numberOrNull(item.y),
                z: numberOrNull(item.z),
                width: numberOrNull(item.width),
                height: numberOrNull(item.height),
                size: numberOrNull(item.size),
                anchorX: numberOrNull(Number.isFinite(item.anchorX) ? item.anchorX : item.placeableAnchorX),
                anchorY: numberOrNull(Number.isFinite(item.anchorY) ? item.anchorY : item.placeableAnchorY),
                billboardAlpha: numberOrNull(item.billboardAlpha),
                billboardTint: numberOrNull(item.billboardTint),
                traversalLayer: numberOrNull(item.traversalLayer),
                level: numberOrNull(item.level),
                rotationAxis: typeof item.rotationAxis === "string" ? item.rotationAxis : "",
                placementRotation: numberOrNull(item.placementRotation),
                isOpen: item.isOpen === true,
                visible: item.visible !== false,
                scriptVisible: this.isScriptVisible(item),
                ownerType: membership && typeof membership.ownerType === "string" ? membership.ownerType : "",
                ownerId: membership && typeof membership.ownerId === "string" ? membership.ownerId : "",
                floorId: membership && typeof membership.floorId === "string" ? membership.floorId : "",
                floorLevel: this.getLayerIndexForObject(item, 0)
            });
        }

        getFloorObjectPickerSignature(item) {
            if (!item || typeof item !== "object") return "";
            const hitbox = item.shadowBox || item.touchBox || null;
            const hitboxSignature = hitbox ? JSON.stringify({
                type: hitbox.constructor && hitbox.constructor.name ? hitbox.constructor.name : "",
                x: Number.isFinite(Number(hitbox.x)) ? Number(Number(hitbox.x).toFixed(4)) : null,
                y: Number.isFinite(Number(hitbox.y)) ? Number(Number(hitbox.y).toFixed(4)) : null,
                radius: Number.isFinite(Number(hitbox.radius)) ? Number(Number(hitbox.radius).toFixed(4)) : null,
                points: Array.isArray(hitbox.points)
                    ? hitbox.points.map(p => ({
                        x: Number.isFinite(Number(p && p.x)) ? Number(Number(p.x).toFixed(4)) : null,
                        y: Number.isFinite(Number(p && p.y)) ? Number(Number(p.y).toFixed(4)) : null
                    }))
                    : null
            }) : "";
            return `${this.getFloorObjectRenderSignature(item)}|picker:${hitboxSignature}`;
        }

        ensurePrototypeBuildingInteriorBitmapCoverageMetadata(ctx, trigger, cache) {
            if (!cache || cache.status !== "ready") return cache;
            if (!cache.texture) {
                throw new Error(`prototype building interior ${cache.id || "(unknown)"} ready cache is missing its texture`);
            }
            if (cache.coveredObjectSet instanceof Set && cache.objectRenderSignatures instanceof Map && cache.objectPickerSignatures instanceof Map) {
                return cache;
            }
            const coveredObjectSet = new Set();
            const objectRenderSignatures = new Map();
            const objectPickerSignatures = new Map();
            const explicitCoveredObjects = Array.isArray(cache.coveredObjects)
                ? cache.coveredObjects
                : (Array.isArray(cache.coveredObjectEntries) ? cache.coveredObjectEntries.map(entry => entry && entry.item) : []);
            for (let i = 0; i < explicitCoveredObjects.length; i++) {
                const item = explicitCoveredObjects[i];
                if (!item || item.gone || item.vanishing) continue;
                coveredObjectSet.add(item);
                objectRenderSignatures.set(item, this.getFloorObjectRenderSignature(item));
                objectPickerSignatures.set(item, this.getFloorObjectPickerSignature(item));
            }
            cache.coveredObjectSet = coveredObjectSet;
            cache.objectRenderSignatures = objectRenderSignatures;
            cache.objectPickerSignatures = objectPickerSignatures;
            return cache;
        }

        isPrototypeBuildingInteriorBitmapObjectCovered(item, cutawayState = null, mapRef = null, ctx = null) {
            if (!item || item.gone || item.vanishing) return false;
            const state = cutawayState || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            if (!map || typeof map.getPrototypeBuildingInteriorBitmap !== "function") return false;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!this.isPrototypeBuildingCutawayTrigger(trigger) || !trigger.activeInteriorRegion) continue;
                const placement = trigger.building && trigger.building._prototypeBuildingPlacement;
                if (!placement || !placement.id) continue;
                const floorId = this.getPrototypeBuildingInteriorSourceFloorId(trigger, { required: false });
                if (!floorId || !this.isPrototypeBuildingInteriorBitmapSourceFloorId(floorId)) continue;
                const cache = map.getPrototypeBuildingInteriorBitmap(placement.id, floorId);
                if (!cache || cache.status !== "ready") continue;
                this.ensurePrototypeBuildingInteriorBitmapCoverageMetadata(ctx, trigger, cache);
                if (!(cache.coveredObjectSet instanceof Set) || !cache.coveredObjectSet.has(item)) continue;
                if (!(cache.objectRenderSignatures instanceof Map) || !(cache.objectPickerSignatures instanceof Map)) {
                    throw new Error(`prototype building interior ${cache.id || placement.id} coverage is missing object signatures`);
                }
                return (
                    cache.objectRenderSignatures.get(item) === this.getFloorObjectRenderSignature(item) &&
                    cache.objectPickerSignatures.get(item) === this.getFloorObjectPickerSignature(item)
                );
            }
            return false;
        }

        renderItemMatchesCutawayTrigger(item, trigger, mapRef = null) {
            if (!item || !trigger) return false;
            const refs = this.collectFloorRefsForRenderItem(item, mapRef);
            for (let i = 0; i < refs.length; i++) {
                const ref = refs[i];
                if (!ref) continue;
                if (ref.fragmentId && trigger.fragmentIds instanceof Set && trigger.fragmentIds.has(ref.fragmentId)) return true;
                if (ref.surfaceId && trigger.surfaceIds instanceof Set && trigger.surfaceIds.has(ref.surfaceId)) return true;
                if (ref.fragmentId && trigger.fragmentId && ref.fragmentId === trigger.fragmentId) return true;
                if (ref.surfaceId && trigger.surfaceId && ref.surfaceId === trigger.surfaceId) return true;
            }
            return false;
        }

        renderItemMatchesCutawayVisibleThroughArea(item, trigger, mapRef = null) {
            const polygons = Array.isArray(trigger && trigger.visibleThroughPolygons)
                ? trigger.visibleThroughPolygons
                : [];
            if (polygons.length === 0) return false;
            return this.renderItemSamplesMatchPolygons(item, polygons, mapRef || global.map || null);
        }

        renderItemMatchesCutawayGroundProjection(item, trigger, mapRef = null) {
            const polygons = Array.isArray(trigger && trigger.groundProjectionPolygons)
                ? trigger.groundProjectionPolygons
                : [];
            if (polygons.length === 0) return false;
            return this.renderItemSamplesMatchPolygons(item, polygons, mapRef || global.map || null);
        }

        renderItemMatchesActiveBuildingInteriorRegion(item, trigger, mapRef = null, refs = null) {
            if (!item || !trigger || !trigger.activeInteriorRegion) return false;
            const refList = Array.isArray(refs) ? refs : this.collectFloorRefsForRenderItem(item, mapRef || global.map || null);
            const hasRefs = this.hasBuildingInteriorFloorRefs(refList);
            const itemMembership = this.getRenderItemFloorMembership(item, mapRef || global.map || null, refList);
            if (!hasRefs && !itemMembership) return false;
            const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
            const visibleRegions = regions.length > 0 ? regions : [trigger.activeInteriorRegion];
            for (let i = 0; i < visibleRegions.length; i++) {
                if (hasRefs && this.renderItemRefsMatchBuildingInteriorRegion(refList, visibleRegions[i])) return true;
                if (itemMembership && this.floorMembershipsMatchForRender(itemMembership, this.getBuildingInteriorRegionFloorMembership(visibleRegions[i], trigger))) return true;
            }
            return false;
        }

        shouldHideRenderItemForExteriorBuildingCutaway(item, trigger, refs = null, mapRef = null) {
            if (!item || !trigger || !trigger.building || trigger.activeInteriorRegion) return false;
            if (item.type === "roof") return false;
            if (this.isBuildingCutawayStructuralItem(item)) return false;
            const refList = Array.isArray(refs) ? refs : this.collectFloorRefsForRenderItem(item, mapRef || global.map || null);
            if (refList.length === 0) return false;
            if (!this.renderItemMatchesCutawayTrigger(item, trigger, mapRef || global.map || null)) return false;
            if (this.shouldRenderBuildingExteriorGroundDoorItem(item, null, trigger, null, null)) return false;
            return true;
        }

        shouldRenderUnreferencedOutdoorItemThroughExteriorBuildingCutaway(item, cutawayState = null) {
            if (!item || this.isBuildingCutawayStructuralItem(item) || item.type === "roof") return false;
            const type = typeof item.type === "string" ? item.type.trim().toLowerCase() : "";
            const category = typeof item.category === "string" ? item.category.trim().toLowerCase() : "";
            if (!(type === "tree" || type === "flower" || category === "trees" || category === "flowers")) {
                return false;
            }
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (trigger && trigger.building && !trigger.activeInteriorRegion) return true;
            }
            return false;
        }

        getCutawayStateWithoutExteriorBuildingTriggers(cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !Array.isArray(state.triggers) || state.triggers.length === 0) return state;
            const triggers = state.triggers.filter(trigger => !(trigger && trigger.building && !trigger.activeInteriorRegion));
            if (triggers.length === state.triggers.length) return state;
            return {
                ...state,
                active: triggers.length > 0 || Number.isFinite(state.globalHiddenFromLevel),
                triggers
            };
        }

        shouldSuppressPrototypeBuildingInteriorLiveRenderItem(item, mapRef = null, cutawayState = null, ctx = null) {
            if (!item) return false;
            if (this.isBuildingCutawayStructuralItem(item)) {
                const state = cutawayState || this.getLayerCutawayState(ctx);
                const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
                const renderMap = mapRef || (ctx && ctx.map) || global.map || null;
                const bitmapCtx = renderMap ? { ...(ctx || {}), map: renderMap } : ctx;
                const refs = this.collectFloorRefsForRenderItem(item, renderMap);
                for (let i = 0; i < triggers.length; i++) {
                    const trigger = triggers[i];
                    if (!trigger || !trigger.activeInteriorRegion || !this.isPrototypeBuildingCutawayTrigger(trigger)) continue;
                    if (!this.getPresentablePrototypeBuildingInteriorBitmapCache(bitmapCtx, trigger)) continue;
                    if (
                        this.renderItemMatchesCutawayTrigger(item, trigger, renderMap) ||
                        this.renderItemMembershipMatchesCutawayTrigger(item, trigger, renderMap, refs)
                    ) {
                        return true;
                    }
                }
                return false;
            }
            return this.isPrototypeBuildingInteriorBitmapObjectCovered(item, cutawayState, mapRef, ctx);
        }

        shouldAllowPrototypeBuildingInteriorLiveRenderItem(item, cutawayState = null, mapRef = null) {
            if (!item) return false;
            const state = cutawayState || this.getLayerCutawayState(null);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            if (triggers.length === 0) return false;
            const refs = this.collectFloorRefsForRenderItem(item, mapRef || global.map || null);
            const membership = this.getRenderItemFloorMembership(item, mapRef || global.map || null, refs);
            if (!this.hasBuildingInteriorFloorRefs(refs) && !membership) return false;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.activeInteriorRegion || !this.isPrototypeBuildingCutawayTrigger(trigger)) continue;
                if (
                    !this.renderItemMatchesCutawayTrigger(item, trigger, mapRef || global.map || null) &&
                    !this.renderItemMembershipMatchesCutawayTrigger(item, trigger, mapRef || global.map || null, refs)
                ) {
                    continue;
                }
                if (this.renderItemMatchesActiveBuildingInteriorRegion(item, trigger, mapRef || global.map || null, refs)) {
                    return true;
                }
            }
            return false;
        }

        shouldRenderPrototypeBuildingInteriorLiveItemAtFullAlpha(item, cutawayState = null, mapRef = null) {
            return !!(
                !this.shouldSuppressPrototypeBuildingInteriorLiveRenderItem(item, mapRef, cutawayState) &&
                this.shouldAllowPrototypeBuildingInteriorLiveRenderItem(item, cutawayState, mapRef)
            );
        }

        renderItemSamplesMatchPolygons(item, polygons, mapRef = null) {
            if (!Array.isArray(polygons) || polygons.length === 0) return false;
            const samples = this.getCutawaySamplePointsForRenderItem(item, mapRef || global.map || null);
            for (let i = 0; i < samples.length; i++) {
                const sample = samples[i];
                if (sample && floorVisualPolygonsContainPoint(polygons, sample.x, sample.y)) return true;
            }
            return false;
        }

        shouldApplyBuildingInteriorActiveFloorDepthBumpToItem(item, itemLayer, plan = null, mapRef = null) {
            if (!item || item.type === "roof" || !plan) return false;
            if (plan.depthBumpedItems instanceof Set && plan.depthBumpedItems.has(item)) return true;
            const regions = Array.isArray(plan.depthBumpRegions) ? plan.depthBumpRegions : [];
            if (regions.length === 0) return false;
            const layer = this.getLayerIndexFromValue(itemLayer, 0);
            for (let i = 0; i < regions.length; i++) {
                const region = regions[i];
                if (!region || !region.polygon) continue;
                if (this.getLayerIndexFromValue(region.level, 0) !== layer) continue;
                if (this.renderItemSamplesMatchPolygons(item, [region.polygon], mapRef || global.map || null)) return true;
            }
            return false;
        }

        getCutawaySamplePointsForRenderItem(item, mapRef = null) {
            const points = [];
            const pushPoint = (x, y) => {
                if (!Number.isFinite(x) || !Number.isFinite(y)) return;
                points.push({ x: Number(x), y: Number(y) });
            };
            if (item && item.type === "wallSection") {
                const sx = Number(item.startPoint && item.startPoint.x);
                const sy = Number(item.startPoint && item.startPoint.y);
                const ex = Number(item.endPoint && item.endPoint.x);
                const ey = Number(item.endPoint && item.endPoint.y);
                pushPoint(sx, sy);
                pushPoint(ex, ey);
                if (Number.isFinite(sx) && Number.isFinite(sy) && Number.isFinite(ex) && Number.isFinite(ey)) {
                    let cx = sx + ((ex - sx) * 0.5);
                    let cy = sy + ((ey - sy) * 0.5);
                    if (mapRef && typeof mapRef.shortestDeltaX === "function") cx = sx + (mapRef.shortestDeltaX(sx, ex) * 0.5);
                    if (mapRef && typeof mapRef.shortestDeltaY === "function") cy = sy + (mapRef.shortestDeltaY(sy, ey) * 0.5);
                    if (mapRef && typeof mapRef.wrapWorldX === "function") cx = mapRef.wrapWorldX(cx);
                    if (mapRef && typeof mapRef.wrapWorldY === "function") cy = mapRef.wrapWorldY(cy);
                    pushPoint(cx, cy);
                }
            }
            if (item && item.type === "beam") {
                pushPoint(item.startX, item.startY);
                pushPoint(item.endX, item.endY);
            }
            if (item && (item.type === "straightStair" || item.type === "treadPathStair")) {
                const stair = item.stair || null;
                const lowerPoint = stair && stair.lowerPoint ? stair.lowerPoint : item.lowerPoint;
                const higherPoint = stair && stair.higherPoint ? stair.higherPoint : item.higherPoint;
                const lx = Number(lowerPoint && lowerPoint.x);
                const ly = Number(lowerPoint && lowerPoint.y);
                const hx = Number(higherPoint && higherPoint.x);
                const hy = Number(higherPoint && higherPoint.y);
                pushPoint(lx, ly);
                pushPoint(hx, hy);
                if (Number.isFinite(lx) && Number.isFinite(ly) && Number.isFinite(hx) && Number.isFinite(hy)) {
                    let cx = lx + ((hx - lx) * 0.5);
                    let cy = ly + ((hy - ly) * 0.5);
                    if (mapRef && typeof mapRef.shortestDeltaX === "function") cx = lx + (mapRef.shortestDeltaX(lx, hx) * 0.5);
                    if (mapRef && typeof mapRef.shortestDeltaY === "function") cy = ly + (mapRef.shortestDeltaY(ly, hy) * 0.5);
                    if (mapRef && typeof mapRef.wrapWorldX === "function") cx = mapRef.wrapWorldX(cx);
                    if (mapRef && typeof mapRef.wrapWorldY === "function") cy = mapRef.wrapWorldY(cy);
                    pushPoint(cx, cy);
                }
            }
            const pos = this.resolveInterpolatedItemWorldPosition(item, mapRef || global.map || null);
            if (pos) pushPoint(Number(pos.x), Number(pos.y));
            return points;
        }

        isRenderItemHiddenByLayerCutaway(item, level, cutawayState = null, mapRef = null) {
            if (!item || item === global.wizard) return false;
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active) return false;
            if (item._cutawayHiddenFrame === this._layerCutawayFrameId) return true;
            const itemLevel = this.getLayerIndexFromValue(level, 0);
            if (Number.isFinite(state.globalHiddenFromLevel) && itemLevel >= state.globalHiddenFromLevel) return true;
            this.incrementFrameMetric("layerCutawayRenderItemTests", 1);
            if (this.isBuildingFrameOnlyCutawayState(state)) {
                this.incrementFrameMetric("layerCutawayRenderItemFastPath", 1);
                return false;
            }
            const refs = this.collectFloorRefsForRenderItem(item, mapRef || global.map || null);
            const triggers = Array.isArray(state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building || !trigger.activeInteriorRegion) continue;
                if (item.type === "roof") continue;
                if (refs.length === 0 || !this.renderItemMatchesCutawayTrigger(item, trigger, mapRef || global.map || null)) continue;
                return !this.renderItemMatchesActiveBuildingInteriorRegion(item, trigger, mapRef || global.map || null, refs);
            }
            for (let i = 0; i < triggers.length; i++) {
                if (this.shouldHideRenderItemForExteriorBuildingCutaway(item, triggers[i], refs, mapRef || global.map || null)) {
                    return true;
                }
            }
            const currentBuildingCacheKey = mapRef
                ? this.getBuildingRenderCacheKey(null, mapRef)
                : null;
            if (
                state._buildingFrameFlagsApplied === true &&
                item._buildingRenderCacheKey &&
                (!currentBuildingCacheKey || item._buildingRenderCacheKey === currentBuildingCacheKey) &&
                item._cutawayHiddenFrame !== this._layerCutawayFrameId
            ) {
                return false;
            }
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || itemLevel < this.getLayerIndexFromValue(trigger.level, Infinity)) continue;
                if (refs.length > 0 && this.renderItemMatchesCutawayTrigger(item, trigger, mapRef || global.map || null)) {
                    if (trigger.building) continue;
                    return true;
                }
            }
            if (refs.length > 0) return false;
            const sourceBaseZ = this.isCharacterRenderItem(item)
                ? 0
                : (item && item.type === "wallSection" && Number.isFinite(item.bottomZ)
                    ? Number(item.bottomZ)
                    : this.getLayerBaseZForLevel(itemLevel));
            const worldPointCutawayState = this.shouldRenderUnreferencedOutdoorItemThroughExteriorBuildingCutaway(item, state)
                ? this.getCutawayStateWithoutExteriorBuildingTriggers(state)
                : state;
            const samples = this.getCutawaySamplePointsForRenderItem(item, mapRef || global.map || null);
            for (let i = 0; i < samples.length; i++) {
                const sample = samples[i];
                if (this.isWorldPointHiddenByLayerCutaway(sample.x, sample.y, itemLevel, worldPointCutawayState, sourceBaseZ)) return true;
            }
            return false;
        }

        beginLayerCutawayFrame() {
            this._layerCutawayFrameId = (Number(this._layerCutawayFrameId) || 0) + 1;
            this.clearLayerCutawayFrameMarkers();
            return this._layerCutawayFrameId;
        }

        applyBuildingCutawayFrameFlags(ctx, cutawayState, mapRef = null, wizardRef = null) {
            const state = cutawayState || this.getLayerCutawayState(ctx);
            if (!state || !state.active || !Array.isArray(state.triggers)) return;
            const frameId = Number(this._layerCutawayFrameId) || this.beginLayerCutawayFrame();
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            const nowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now();
            let ghostItemsFlagged = 0;
            for (let i = 0; i < state.triggers.length; i++) {
                const trigger = state.triggers[i];
                if (!trigger || !trigger.building) continue;
                const cache = trigger.renderCache || this.getCompiledBuildingRenderCache(ctx, map, trigger.building);
                if (!cache) continue;
                const triggerLevel = trigger.activeInteriorRegion
                    ? this.getLayerIndexFromValue(trigger.building && trigger.building.minLevel, 0)
                    : this.getLayerIndexFromValue(trigger.level, Infinity);
                const triggerAlpha = Number.isFinite(trigger.alpha)
                    ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                    : BUILDING_CUTAWAY_GHOST_ALPHA;
                const items = Array.isArray(cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < items.length; j++) {
                    const entry = items[j];
                    const item = entry && entry.item;
                    if (!item || item.gone || item.vanishing) continue;
                    if (!this.isBuildingCutawayStructuralItem(item)) continue;
                    const level = this.getLayerIndexFromValue(entry.level, 0);
                    if (this.shouldRenderBuildingExteriorGroundDoorItem(item, entry, trigger, ctx, wizardRef)) {
                        continue;
                    }
                    if (level >= triggerLevel) {
                        const layerFadeAlpha = this.getLiveLayerFadeMultiplier(level, nowMs);
                        item._renderLayerIndex = level;
                        item._renderLayerFadeAlpha = layerFadeAlpha;
                        item._renderLayerAlpha = layerFadeAlpha;
                        item._cutawayCompositeFrame = frameId;
                        item._cutawayCompositeAlpha = triggerAlpha;
                        ghostItemsFlagged += 1;
                    }
                }

                const shouldCompositeGroundWalls = !Number.isFinite(trigger.visibleThroughLevel) ||
                    this.getLayerIndexFromValue(trigger.visibleThroughLevel, 0) <= 0;
                if (shouldCompositeGroundWalls) {
                    const groundWalls = Array.isArray(cache.groundProjectionWalls) ? cache.groundProjectionWalls : [];
                    for (let j = 0; j < groundWalls.length; j++) {
                        const wall = groundWalls[j] && groundWalls[j].item;
                        if (!wall || wall.gone || wall.vanishing) continue;
                        const layerFadeAlpha = this.getLiveLayerFadeMultiplier(0, nowMs);
                        wall._renderLayerIndex = 0;
                        wall._renderLayerFadeAlpha = layerFadeAlpha;
                        wall._renderLayerAlpha = layerFadeAlpha;
                        wall._cutawayCompositeFrame = frameId;
                        wall._cutawayCompositeAlpha = triggerAlpha;
                        ghostItemsFlagged += 1;
                    }
                }

            }
            this.setFrameMetric("objects3dBuildingGhostItemsFlagged", ghostItemsFlagged);
            this.setFrameMetric("objects3dBuildingHiddenItemsFlagged", ghostItemsFlagged);
            state._buildingFrameFlagsApplied = true;
        }

        prepareLayerCutawayFrame(ctx, mapRef = null, wizardRef = null) {
            const state = this.getLayerCutawayState(ctx);
            const activeDrawFrameId = Number(this._activeDrawFrameId) || 0;
            if (activeDrawFrameId > 0 && this._layerCutawayPreparedDrawFrameId === activeDrawFrameId) {
                return state;
            }
            this.beginLayerCutawayFrame();
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            if (diagnosticsEnabled) {
                this.setFrameMetric("objects3dBuildingGhostItemsFlagged", 0);
                this.setFrameMetric("objects3dBuildingHiddenItemsFlagged", 0);
                this.setFrameMetric("layerCutawayRenderItemTests", 0);
                this.setFrameMetric("layerCutawayRenderItemFastPath", 0);
                this.setFrameMetric("layerCutawayWorldPointTests", 0);
                this.setFrameMetric("layerCutawayWorldPointFastPath", 0);
            }
            const buildingFlagStartMs = diagnosticsEnabled ? performance.now() : 0;
            this.applyBuildingCutawayFrameFlags(ctx, state, mapRef || (ctx && ctx.map) || global.map || null, wizardRef || (ctx && ctx.wizard) || global.wizard || null);
            if (diagnosticsEnabled) {
                this.setFrameMetric("objects3dBuildingFlagMs", performance.now() - buildingFlagStartMs);
            }
            if (activeDrawFrameId > 0) this._layerCutawayPreparedDrawFrameId = activeDrawFrameId;
            return state;
        }

        clearLayerCutawayFrameMarkers() {
            if (!(this._cutawayInteriorOverlayWallSections instanceof Set)) {
                this._cutawayInteriorOverlayWallSections = new Set();
                return;
            }
            this._cutawayInteriorOverlayWallSections.forEach(section => {
                if (section) delete section._cutawayInteriorOverlayFrame;
            });
            this._cutawayInteriorOverlayWallSections.clear();
        }

        getBuildingCutawayGroundMaskPolygons(cutawayState) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active || !Array.isArray(state.triggers)) return [];
            const polygons = [];
            for (let i = 0; i < state.triggers.length; i++) {
                const trigger = state.triggers[i];
                if (!trigger || !trigger.building) continue;
                if (Number.isFinite(trigger.visibleThroughLevel)) continue;
                const maskAlpha = 1 - (Number.isFinite(trigger.alpha)
                    ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                    : BUILDING_CUTAWAY_GHOST_ALPHA);
                if (!(maskAlpha > 0.001)) continue;
                const triggerPolygons = Array.isArray(trigger.groundProjectionPolygons)
                    ? trigger.groundProjectionPolygons
                    : [];
                for (let p = 0; p < triggerPolygons.length; p++) {
                    if (triggerPolygons[p]) polygons.push(triggerPolygons[p]);
                }
            }
            return polygons;
        }

        getBuildingCutawayGroundMaskEntries(cutawayState) {
            const state = cutawayState || this.getLayerCutawayState(null);
            if (!state || !state.active || !Array.isArray(state.triggers)) return [];
            const entries = [];
            for (let i = 0; i < state.triggers.length; i++) {
                const trigger = state.triggers[i];
                if (!trigger || !trigger.building) continue;
                if (Number.isFinite(trigger.visibleThroughLevel)) continue;
                const maskAlpha = 1 - (Number.isFinite(trigger.alpha)
                    ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                    : BUILDING_CUTAWAY_GHOST_ALPHA);
                if (!(maskAlpha > 0.001)) continue;
                const triggerPolygons = Array.isArray(trigger.groundProjectionPolygons)
                    ? trigger.groundProjectionPolygons
                    : [];
                for (let p = 0; p < triggerPolygons.length; p++) {
                    if (!triggerPolygons[p]) continue;
                    entries.push({
                        key: `${trigger.buildingId || "building"}:${p}`,
                        buildingId: trigger.buildingId || "",
                        polygon: triggerPolygons[p],
                        alpha: maskAlpha
                    });
                }
            }
            return entries;
        }

        ensureBuildingCutawayGroundMaskGraphics(container) {
            if (!container || typeof PIXI === "undefined" || !PIXI.Graphics) return null;
            if (!this.buildingCutawayGroundMaskGraphics) {
                this.buildingCutawayGroundMaskGraphics = new PIXI.Graphics();
                this.buildingCutawayGroundMaskGraphics.name = "renderingBuildingCutawayGroundMask";
                this.buildingCutawayGroundMaskGraphics.interactive = false;
                this.buildingCutawayGroundMaskGraphics.visible = false;
            }
            const g = this.buildingCutawayGroundMaskGraphics;
            if (g.parent !== container) {
                container.addChild(g);
            }
            if (typeof container.setChildIndex === "function" && Array.isArray(container.children) && container.children.length > 0) {
                container.setChildIndex(g, 0);
            }
            return g;
        }

        clearBuildingCutawayGroundMask() {
            const g = this.buildingCutawayGroundMaskGraphics;
            if (g) {
                if (typeof g.clear === "function") g.clear();
                g.visible = false;
                if (Object.prototype.hasOwnProperty.call(g, "renderable")) {
                    g.renderable = false;
                }
            }
            if (this.buildingCutawayGroundMaskMeshes instanceof Map) {
                for (const entry of this.buildingCutawayGroundMaskMeshes.values()) {
                    if (entry && entry.mesh) {
                        entry.mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) entry.mesh.renderable = false;
                    }
                }
            }
        }

        drawBuildingCutawayGroundMaskRing(g, points) {
            const ring = normalizeFloorVisualPointList(points);
            if (!g || !this.camera || ring.length < 3) return false;
            let started = false;
            for (let i = 0; i < ring.length; i++) {
                const screen = this.camera.worldToScreen(ring[i].x, ring[i].y, 0);
                if (!screen || !Number.isFinite(screen.x) || !Number.isFinite(screen.y)) return false;
                if (!started) {
                    g.moveTo(screen.x, screen.y);
                    started = true;
                } else {
                    g.lineTo(screen.x, screen.y);
                }
            }
            if (started) g.closePath();
            return started;
        }

        getBuildingCutawayGroundMaskState() {
            if (this.buildingCutawayGroundMaskState) return this.buildingCutawayGroundMaskState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.culling = false;
            this.buildingCutawayGroundMaskState = state;
            return state;
        }

        createBuildingCutawayGroundMaskMesh(entry) {
            if (!entry || !entry.triangulation || typeof PIXI === "undefined" || !PIXI.Geometry || !PIXI.Mesh || !PIXI.Shader) {
                return null;
            }
            const vertexData = new Float32Array(entry.triangulation.vertexCount * 2);
            const geometry = new PIXI.Geometry()
                .addAttribute("aWorldPosition", vertexData, 2)
                .addIndex(entry.triangulation.indices);
            const nearMetric = FLOOR_VISUAL_DEPTH_NEAR_METRIC;
            const farMetric = FLOOR_VISUAL_DEPTH_FAR_METRIC;
            const shader = PIXI.Shader.from(LOS_SHADOW_DEPTH_VS, LOS_SHADOW_DEPTH_FS, {
                uScreenSize: new Float32Array([1, 1]),
                uCameraWorld: new Float32Array([0, 0]),
                uCameraZ: 0,
                uBaseZ: 0,
                uDepthBias: FLOOR_VISUAL_HOLE_DEPTH_BIAS_UNITS,
                uViewScale: 1,
                uXyRatio: 1,
                uDepthRange: new Float32Array([farMetric, 1 / Math.max(1e-6, farMetric - nearMetric)]),
                uTint: new Float32Array([0, 0, 0, 1])
            });
            const mesh = new PIXI.Mesh(geometry, shader);
            mesh.name = "buildingCutawayGroundMaskMesh";
            mesh.interactive = false;
            const state = this.getBuildingCutawayGroundMaskState();
            if (state) mesh.state = state;
            return mesh;
        }

        uploadBuildingCutawayGroundMaskGeometry(entry) {
            if (!entry || !entry.mesh || !entry.triangulation) return false;
            const geometry = entry.mesh.geometry || null;
            const buffer = geometry && typeof geometry.getBuffer === "function"
                ? geometry.getBuffer("aWorldPosition")
                : null;
            if (!buffer || !buffer.data) return false;
            const data = buffer.data;
            const points = entry.triangulation.points;
            if (!Array.isArray(points) || data.length < points.length * 2) return false;
            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                data[i * 2] = Number(pt && pt.x) || 0;
                data[i * 2 + 1] = Number(pt && pt.y) || 0;
            }
            buffer.update();
            entry.uploadedGeometrySignature = entry.signature || "";
            return true;
        }

        updateBuildingCutawayGroundMaskMesh(entry) {
            if (!entry || !entry.mesh) return false;
            const shader = entry.mesh.shader || null;
            const uniforms = shader && shader.uniforms ? shader.uniforms : null;
            if (uniforms) {
                const appRef = (typeof app !== "undefined" && app) ? app : (global.app || null);
                const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width))
                    ? Number(appRef.screen.width)
                    : 1;
                const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height))
                    ? Number(appRef.screen.height)
                    : 1;
                const cam = this.camera || {};
                if (uniforms.uScreenSize) {
                    uniforms.uScreenSize[0] = Math.max(1, screenW);
                    uniforms.uScreenSize[1] = Math.max(1, screenH);
                }
                if (uniforms.uCameraWorld) {
                    uniforms.uCameraWorld[0] = Number(cam.x) || 0;
                    uniforms.uCameraWorld[1] = Number(cam.y) || 0;
                }
                uniforms.uCameraZ = Number(cam.z) || 0;
                uniforms.uBaseZ = 0;
                uniforms.uDepthBias = FLOOR_VISUAL_HOLE_DEPTH_BIAS_UNITS;
                uniforms.uViewScale = Number(cam.viewscale) || 1;
                uniforms.uXyRatio = Number(cam.xyratio) || 1;
                if (uniforms.uDepthRange) {
                    const nearMetric = FLOOR_VISUAL_DEPTH_NEAR_METRIC;
                    const farMetric = FLOOR_VISUAL_DEPTH_FAR_METRIC;
                    uniforms.uDepthRange[0] = farMetric;
                    uniforms.uDepthRange[1] = 1 / Math.max(1e-6, farMetric - nearMetric);
                }
                if (uniforms.uTint) {
                    uniforms.uTint[0] = 0;
                    uniforms.uTint[1] = 0;
                    uniforms.uTint[2] = 0;
                    uniforms.uTint[3] = Number.isFinite(entry.alpha) ? Math.max(0, Math.min(1, Number(entry.alpha))) : 1;
                }
            }
            entry.mesh.alpha = 1;
            entry.mesh.tint = 0xffffff;
            entry.mesh.position.set(0, 0);
            entry.mesh.scale.set(1, 1);
            entry.mesh.visible = true;
            if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) {
                entry.mesh.renderable = true;
            }
            return true;
        }

        renderBuildingCutawayGroundMasks(ctx, cutawayState, container = null) {
            const targetContainer = container || (this.layers && this.layers.objects3d) || null;
            const maskEntries = this.getBuildingCutawayGroundMaskEntries(cutawayState || this.getLayerCutawayState(ctx));
            let maskMeshes = 0;
            let maskVertices = 0;
            let maskTriangles = 0;
            let maskCreated = 0;
            let maskGeometryUploads = 0;
            let maskUniformUpdates = 0;
            let maskAttachOps = 0;
            let maskHiddenMeshes = 0;
            const maskDisabled = !!(
                (typeof globalThis !== "undefined" && globalThis.renderingDisableBuildingCutawayGroundMask === true) ||
                (typeof global !== "undefined" && global.renderingDisableBuildingCutawayGroundMask === true)
            );
            if (maskEntries.length === 0) {
                this.clearBuildingCutawayGroundMask();
                this.setFrameMetric("objects3dBuildingMaskActive", 0);
                this.setFrameMetric("objects3dBuildingMaskEntries", 0);
                this.setFrameMetric("objects3dBuildingMaskMeshes", 0);
                this.setFrameMetric("objects3dBuildingMaskVertices", 0);
                this.setFrameMetric("objects3dBuildingMaskTriangles", 0);
                this.setFrameMetric("objects3dBuildingMaskCreated", 0);
                this.setFrameMetric("objects3dBuildingMaskGeometryUploads", 0);
                this.setFrameMetric("objects3dBuildingMaskUniformUpdates", 0);
                this.setFrameMetric("objects3dBuildingMaskAttachOps", 0);
                this.setFrameMetric("objects3dBuildingMaskHiddenMeshes", 0);
                return [];
            }
            if (maskDisabled) {
                this.clearBuildingCutawayGroundMask();
                this.setFrameMetric("objects3dBuildingMaskActive", 0);
                this.setFrameMetric("objects3dBuildingMaskEntries", maskEntries.length);
                this.setFrameMetric("objects3dBuildingMaskMeshes", 0);
                this.setFrameMetric("objects3dBuildingMaskVertices", 0);
                this.setFrameMetric("objects3dBuildingMaskTriangles", 0);
                this.setFrameMetric("objects3dBuildingMaskCreated", 0);
                this.setFrameMetric("objects3dBuildingMaskGeometryUploads", 0);
                this.setFrameMetric("objects3dBuildingMaskUniformUpdates", 0);
                this.setFrameMetric("objects3dBuildingMaskAttachOps", 0);
                this.setFrameMetric("objects3dBuildingMaskHiddenMeshes", 0);
                return [];
            }
            if (typeof PIXI !== "undefined" && PIXI.Mesh && PIXI.Geometry && PIXI.Shader) {
                if (!(this.buildingCutawayGroundMaskMeshes instanceof Map)) {
                    this.buildingCutawayGroundMaskMeshes = new Map();
                }
                const visibleKeys = new Set();
                const visibleMeshes = [];
                for (let i = 0; i < maskEntries.length; i++) {
                    const source = maskEntries[i];
                    const polygon = source && source.polygon;
                    const outer = normalizeFloorVisualPointList(polygon && polygon.outer);
                    if (outer.length < 3) continue;
                    const holes = Array.isArray(polygon && polygon.holes) ? polygon.holes : [];
                    const signature = buildFloorVisualSignature(outer, holes);
                    const key = source.key || `mask:${i}`;
                    visibleKeys.add(key);
                    let entry = this.buildingCutawayGroundMaskMeshes.get(key);
                    if (!entry || entry.signature !== signature) {
                        if (entry && entry.mesh) {
                            this.destroyCachedPixiMesh(entry.mesh, "building cutaway ground mask mesh");
                        }
                        const triangulation = triangulateFloorVisualPolygon(outer, holes);
                        if (!triangulation) continue;
                        entry = {
                            signature,
                            triangulation,
                            mesh: null,
                            uploadedGeometrySignature: ""
                        };
                        entry.mesh = this.createBuildingCutawayGroundMaskMesh(entry);
                        if (!entry.mesh) continue;
                        if (this.uploadBuildingCutawayGroundMaskGeometry(entry)) {
                            maskGeometryUploads += 1;
                        }
                        maskCreated += 1;
                        this.buildingCutawayGroundMaskMeshes.set(key, entry);
                    }
                    entry.alpha = Number.isFinite(source && source.alpha) ? Math.max(0, Math.min(1, Number(source.alpha))) : 1;
                    if (entry.triangulation) {
                        maskVertices += Number(entry.triangulation.vertexCount) || 0;
                        maskTriangles += entry.triangulation.indices ? Math.floor(entry.triangulation.indices.length / 3) : 0;
                    }
                    if (entry.mesh && entry.mesh.parent !== targetContainer && targetContainer) {
                        targetContainer.addChild(entry.mesh);
                        maskAttachOps += 1;
                        if (typeof targetContainer.setChildIndex === "function" && Array.isArray(targetContainer.children)) {
                            targetContainer.setChildIndex(entry.mesh, Math.min(i, targetContainer.children.length - 1));
                        }
                    }
                    if (this.updateBuildingCutawayGroundMaskMesh(entry)) {
                        maskUniformUpdates += 1;
                    }
                    if (entry.mesh) {
                        entry.mesh.visible = true;
                        if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) entry.mesh.renderable = true;
                        visibleMeshes.push(entry.mesh);
                        maskMeshes += 1;
                    }
                }
                for (const [key, entry] of this.buildingCutawayGroundMaskMeshes.entries()) {
                    if (visibleKeys.has(key)) continue;
                    if (entry && entry.mesh) {
                        entry.mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) entry.mesh.renderable = false;
                        maskHiddenMeshes += 1;
                    }
                }
                this.setFrameMetric("objects3dBuildingMaskActive", visibleMeshes.length > 0 ? 1 : 0);
                this.setFrameMetric("objects3dBuildingMaskEntries", maskEntries.length);
                this.setFrameMetric("objects3dBuildingMaskMeshes", maskMeshes);
                this.setFrameMetric("objects3dBuildingMaskVertices", maskVertices);
                this.setFrameMetric("objects3dBuildingMaskTriangles", maskTriangles);
                this.setFrameMetric("objects3dBuildingMaskCreated", maskCreated);
                this.setFrameMetric("objects3dBuildingMaskGeometryUploads", maskGeometryUploads);
                this.setFrameMetric("objects3dBuildingMaskUniformUpdates", maskUniformUpdates);
                this.setFrameMetric("objects3dBuildingMaskAttachOps", maskAttachOps);
                this.setFrameMetric("objects3dBuildingMaskHiddenMeshes", maskHiddenMeshes);
                return visibleMeshes;
            }
            const g = this.ensureBuildingCutawayGroundMaskGraphics(targetContainer);
            if (!g) return [];
            g.clear();
            let drewAny = false;
            for (let i = 0; i < maskEntries.length; i++) {
                const source = maskEntries[i];
                const polygon = source && source.polygon;
                if (!polygon || !Array.isArray(polygon.outer) || polygon.outer.length < 3) continue;
                const alpha = Number.isFinite(source && source.alpha) ? Math.max(0, Math.min(1, Number(source.alpha))) : 1;
                g.beginFill(0x000000, alpha);
                const drewOuter = this.drawBuildingCutawayGroundMaskRing(g, polygon.outer);
                if (drewOuter) {
                    const holes = Array.isArray(polygon.holes) ? polygon.holes : [];
                    const canDrawHoles = typeof g.beginHole === "function" && typeof g.endHole === "function";
                    if (canDrawHoles) {
                        for (let h = 0; h < holes.length; h++) {
                            g.beginHole();
                            this.drawBuildingCutawayGroundMaskRing(g, holes[h]);
                            g.endHole();
                        }
                    }
                    drewAny = true;
                }
                g.endFill();
            }
            g.visible = drewAny;
            if (Object.prototype.hasOwnProperty.call(g, "renderable")) {
                g.renderable = drewAny;
            }
            this.setFrameMetric("objects3dBuildingMaskActive", drewAny ? 1 : 0);
            this.setFrameMetric("objects3dBuildingMaskEntries", maskEntries.length);
            this.setFrameMetric("objects3dBuildingMaskMeshes", drewAny ? 1 : 0);
            this.setFrameMetric("objects3dBuildingMaskVertices", 0);
            this.setFrameMetric("objects3dBuildingMaskTriangles", 0);
            this.setFrameMetric("objects3dBuildingMaskCreated", 0);
            this.setFrameMetric("objects3dBuildingMaskGeometryUploads", 0);
            this.setFrameMetric("objects3dBuildingMaskUniformUpdates", 0);
            this.setFrameMetric("objects3dBuildingMaskAttachOps", 0);
            this.setFrameMetric("objects3dBuildingMaskHiddenMeshes", 0);
            return drewAny ? [g] : [];
        }

        ensureBuildingCutawayCompositeRenderTexture(ctx, targetWidth = null, targetHeight = null, localBounds = null) {
            if (typeof PIXI === "undefined" || !PIXI.RenderTexture || !PIXI.Sprite || !PIXI.Texture || !PIXI.Rectangle) return null;
            const appRef = (ctx && ctx.app && ctx.app.renderer)
                ? ctx.app
                : ((typeof app !== "undefined" && app && app.renderer) ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            if (!renderer) return null;
            const screen = appRef.screen || renderer.screen || {};
            const width = Math.max(1, Math.round(Number(targetWidth) || Number(screen.width) || Number(renderer.width) || 1));
            const height = Math.max(1, Math.round(Number(targetHeight) || Number(screen.height) || Number(renderer.height) || 1));
            const sizeKey = `${width}x${height}`;
            if (
                !this.buildingCutawayCompositeRenderTexture ||
                !this.buildingCutawayCompositeSize ||
                this.buildingCutawayCompositeSize.key !== sizeKey
            ) {
                const previousTexture = this.buildingCutawayCompositeRenderTexture || null;
                const preservePrevious = !!(
                    previousTexture &&
                    this.buildingCutawayCompositeCache &&
                    this.buildingCutawayCompositeCache.active === true &&
                    this.buildingCutawayCompositeCache.texture === previousTexture
                );
                if (!preservePrevious && previousTexture && typeof previousTexture.destroy === "function") {
                    this.buildingCutawayCompositeRenderTexture.destroy(true);
                } else if (preservePrevious) {
                    this.buildingCutawayCompositeRetiredRenderTexture = previousTexture;
                }
                this.buildingCutawayCompositeRenderTexture = PIXI.RenderTexture.create({
                    width,
                    height,
                    resolution: 1
                });
                this.buildingCutawayCompositeSize = { key: sizeKey, width, height };
            }
            const framebuffer = this.buildingCutawayCompositeRenderTexture &&
                this.buildingCutawayCompositeRenderTexture.baseTexture &&
                this.buildingCutawayCompositeRenderTexture.baseTexture.framebuffer
                ? this.buildingCutawayCompositeRenderTexture.baseTexture.framebuffer
                : null;
            if (framebuffer && typeof framebuffer.enableDepth === "function" && framebuffer.depth !== true) {
                framebuffer.enableDepth();
            }
            if (!this.buildingCutawayCompositeSprite) {
                this.buildingCutawayCompositeSprite = new PIXI.Sprite(this.buildingCutawayCompositeRenderTexture);
                this.buildingCutawayCompositeSprite.name = "buildingCutawayCompositeSprite";
                this.buildingCutawayCompositeSprite.interactive = false;
                if (this.buildingCutawayCompositeSprite.anchor) {
                    this.buildingCutawayCompositeSprite.anchor.set(0.5, 1);
                }
            } else if (this.buildingCutawayCompositeSprite.texture !== this.buildingCutawayCompositeRenderTexture) {
                this.buildingCutawayCompositeSprite.texture = this.buildingCutawayCompositeRenderTexture;
            }
            this.buildingCutawayCompositeSprite.position.set(0, 0);
            this.buildingCutawayCompositeSprite.scale.set(1, 1);
            return {
                app: appRef,
                renderer,
                texture: this.buildingCutawayCompositeRenderTexture,
                sprite: this.buildingCutawayCompositeSprite,
                width,
                height,
                localBounds: localBounds || null
            };
        }

        ensureBuildingCutawayCompositeDataRenderTexture(ctx, targetWidth = null, targetHeight = null) {
            if (typeof PIXI === "undefined" || !PIXI.RenderTexture) return null;
            const appRef = (ctx && ctx.app && ctx.app.renderer)
                ? ctx.app
                : ((typeof app !== "undefined" && app && app.renderer) ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            if (!renderer) return null;
            const width = Math.max(1, Math.round(Number(targetWidth) || 1));
            const height = Math.max(1, Math.round(Number(targetHeight) || 1));
            const sizeKey = `${width}x${height}`;
            if (
                !this.buildingCutawayCompositeDataRenderTexture ||
                !this.buildingCutawayCompositeDataSize ||
                this.buildingCutawayCompositeDataSize.key !== sizeKey
            ) {
                const previousTexture = this.buildingCutawayCompositeDataRenderTexture || null;
                const preservePrevious = !!(
                    previousTexture &&
                    this.buildingCutawayCompositeCache &&
                    this.buildingCutawayCompositeCache.active === true &&
                    this.buildingCutawayCompositeCache.dataTexture === previousTexture
                );
                if (!preservePrevious && previousTexture && typeof previousTexture.destroy === "function") {
                    previousTexture.destroy(true);
                } else if (preservePrevious) {
                    this.buildingCutawayCompositeRetiredDataRenderTexture = previousTexture;
                }
                this.buildingCutawayCompositeDataRenderTexture = PIXI.RenderTexture.create({
                    width,
                    height,
                    resolution: 1
                });
                this.buildingCutawayCompositeDataSize = { key: sizeKey, width, height };
            }
            this.ensureRenderTextureDepthAttachment(
                this.buildingCutawayCompositeDataRenderTexture,
                "building cutaway composite data texture"
            );
            return {
                app: appRef,
                renderer,
                texture: this.buildingCutawayCompositeDataRenderTexture,
                width,
                height
            };
        }

        clearBuildingCutawayCompositeRenderTarget(resources) {
            if (!resources || !resources.renderer || !resources.texture) return false;
            const renderer = resources.renderer;
            const framebuffer = resources.texture.baseTexture && resources.texture.baseTexture.framebuffer
                ? resources.texture.baseTexture.framebuffer
                : null;
            const gl = renderer.gl || null;
            if (!gl || !framebuffer || !renderer.framebuffer || typeof renderer.framebuffer.bind !== "function") return false;
            try {
                renderer.framebuffer.bind(framebuffer);
                gl.clearColor(0, 0, 0, 0);
                gl.clearDepth(1);
                gl.clear(gl.COLOR_BUFFER_BIT | gl.DEPTH_BUFFER_BIT);
                return true;
            } catch (_err) {
                return false;
            }
        }

        ensureRenderTextureDepthAttachment(renderTexture, label = "render texture") {
            const framebuffer = renderTexture && renderTexture.baseTexture && renderTexture.baseTexture.framebuffer
                ? renderTexture.baseTexture.framebuffer
                : (renderTexture && renderTexture.framebuffer ? renderTexture.framebuffer : null);
            if (!framebuffer) {
                throw new Error(`${label} is missing a framebuffer; depth-tested rendering cannot be used.`);
            }
            if (typeof framebuffer.enableDepth === "function") {
                framebuffer.enableDepth();
            } else if (Object.prototype.hasOwnProperty.call(framebuffer, "depth")) {
                framebuffer.depth = true;
            } else {
                throw new Error(`${label} framebuffer does not support a depth attachment.`);
            }
            return framebuffer;
        }

        clearDepthTestedRenderTarget(resources, label = "render target") {
            const cleared = this.clearBuildingCutawayCompositeRenderTarget(resources);
            if (!cleared) {
                throw new Error(`${label} could not be cleared with a depth buffer.`);
            }
            return true;
        }

        ensureLayerTransitionSnapshotResources(ctx = null) {
            if (typeof PIXI === "undefined" || !PIXI.RenderTexture || !PIXI.Sprite || !PIXI.Texture) return null;
            const appRef = (ctx && ctx.app) || (typeof app !== "undefined" ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            if (!renderer) return null;
            const size = this.getRendererScreenSize(ctx);
            const width = size.width;
            const height = size.height;
            const sizeKey = `${width}x${height}`;
            if (
                !this.layerTransitionSnapshotRenderTexture ||
                !this.layerTransitionSnapshotSize ||
                this.layerTransitionSnapshotSize.key !== sizeKey
            ) {
                if (
                    this.layerTransitionSnapshotRenderTexture &&
                    typeof this.layerTransitionSnapshotRenderTexture.destroy === "function"
                ) {
                    this.layerTransitionSnapshotRenderTexture.destroy(true);
                }
                this.layerTransitionSnapshotRenderTexture = PIXI.RenderTexture.create({
                    width,
                    height
                });
                this.layerTransitionSnapshotSize = { key: sizeKey, width, height };
            }
            this.ensureRenderTextureDepthAttachment(
                this.layerTransitionSnapshotRenderTexture,
                "layer transition snapshot texture"
            );
            if (!this.layerTransitionSnapshotSprite) {
                this.layerTransitionSnapshotSprite = new PIXI.Sprite(this.layerTransitionSnapshotRenderTexture);
                this.layerTransitionSnapshotSprite.name = "layerTransitionSnapshotSprite";
                this.layerTransitionSnapshotSprite.interactive = false;
            } else if (this.layerTransitionSnapshotSprite.texture !== this.layerTransitionSnapshotRenderTexture) {
                this.layerTransitionSnapshotSprite.texture = this.layerTransitionSnapshotRenderTexture;
            }
            this.applyLayerTransitionSnapshotSpriteTransform(this.layerTransitionSnapshotSprite, width, height);
            if (PIXI.BLEND_MODES) {
                this.layerTransitionSnapshotSprite.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            return {
                renderer,
                texture: this.layerTransitionSnapshotRenderTexture,
                sprite: this.layerTransitionSnapshotSprite,
                width,
                height
            };
        }

        getLayerTransitionSnapshotCameraState(ctx = null) {
            const cameraRef = this.camera || (ctx && ctx.camera) || null;
            if (!cameraRef) {
                throw new Error("missing layer transition snapshot camera");
            }
            const viewscale = Number(cameraRef.viewscale);
            const xyratio = Number(cameraRef.xyratio);
            const x = Number(cameraRef.x);
            const y = Number(cameraRef.y);
            const z = Number(cameraRef.z);
            if (
                !Number.isFinite(x) ||
                !Number.isFinite(y) ||
                !Number.isFinite(z) ||
                !Number.isFinite(viewscale) ||
                !Number.isFinite(xyratio)
            ) {
                throw new Error("invalid layer transition snapshot camera state");
            }
            return { x, y, z, viewscale, xyratio };
        }

        getLayerTransitionSnapshotScreenOffset(snapshot, ctx = null) {
            if (!snapshot || !snapshot.camera) {
                throw new Error("missing layer transition snapshot camera state");
            }
            const captured = snapshot.camera;
            const current = this.getLayerTransitionSnapshotCameraState(ctx);
            const mapRef = (ctx && ctx.map) || (this.camera && this.camera.map) || global.map || null;
            const dxWorld = mapRef && typeof mapRef.shortestDeltaX === "function"
                ? mapRef.shortestDeltaX(current.x, captured.x)
                : (captured.x - current.x);
            const dyWorld = mapRef && typeof mapRef.shortestDeltaY === "function"
                ? mapRef.shortestDeltaY(current.y, captured.y)
                : (captured.y - current.y);
            const dzWorld = current.z - captured.z;
            return {
                x: dxWorld * current.viewscale,
                y: (dyWorld + dzWorld) * current.viewscale * current.xyratio
            };
        }

        applyLayerTransitionSnapshotSpriteTransform(sprite, width, height, offsetX = 0, offsetY = 0) {
            if (!sprite) {
                throw new Error("missing layer transition snapshot sprite");
            }
            const textureRef = sprite.texture || this.layerTransitionSnapshotRenderTexture || null;
            const textureWidth = Number(textureRef && textureRef.width) || Number(width);
            const textureHeight = Number(textureRef && textureRef.height) || Number(height);
            if (
                !Number.isFinite(textureWidth) ||
                !(textureWidth > 0) ||
                !Number.isFinite(textureHeight) ||
                !(textureHeight > 0)
            ) {
                throw new Error("invalid layer transition snapshot texture size");
            }
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(0, 0);
            }
            sprite.position.set(Number(offsetX) || 0, Number(height) + (Number(offsetY) || 0));
            sprite.scale.set(Number(width) / textureWidth, -(Number(height) / textureHeight));
        }

        setLayerTransitionSnapshotDisplayIndex() {
            const sprite = this.layerTransitionSnapshotSprite;
            const root = this.layers && this.layers.root ? this.layers.root : null;
            if (!sprite || !root || sprite.parent !== root || typeof root.setChildIndex !== "function") return;
            const ui = this.layers && this.layers.ui ? this.layers.ui : null;
            const scriptMessages = this.layers && this.layers.scriptMessages ? this.layers.scriptMessages : null;
            let targetIndex = root.children.length - 1;
            if (scriptMessages && scriptMessages.parent === root) {
                targetIndex = Math.min(targetIndex, Math.max(0, root.getChildIndex(scriptMessages) - 1));
            }
            if (ui && ui.parent === root) {
                targetIndex = Math.min(targetIndex, Math.max(0, root.getChildIndex(ui) - 1));
            }
            if (root.getChildIndex(sprite) !== targetIndex) {
                root.setChildIndex(sprite, targetIndex);
            }
        }

        hidePlayerDisplayObjectsForLayerTransitionSnapshot(ctx, hide) {
            if (typeof hide !== "function") {
                throw new Error("missing layer transition snapshot display-object hider");
            }
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const seen = new Set();
            const hideDisplay = (displayObj) => {
                if (!displayObj || typeof displayObj !== "object") return;
                if (seen.has(displayObj)) return;
                seen.add(displayObj);
                hide(displayObj);
            };
            if (wizard) {
                hideDisplay(wizard.pixiSprite);
                hideDisplay(wizard._renderingDepthMesh);
                hideDisplay(wizard._renderingDisplayObject);
                hideDisplay(wizard.hatGraphics);
                hideDisplay(wizard.shieldGraphics);
                hideDisplay(wizard.shieldDebrisGraphics);
                hideDisplay(wizard.shieldWireframeMesh);
            }
            hideDisplay(this.wizardSprite);
            hideDisplay(this.wizardGhostSprite);
            hideDisplay(this.wizardShadowSprite);
            if (this.wizardShadowProxy) {
                hideDisplay(this.wizardShadowProxy.pixiSprite);
                hideDisplay(this.wizardShadowProxy._renderingDepthMesh);
                hideDisplay(this.wizardShadowProxy._renderingDisplayObject);
            }
        }

        captureLayerTransitionSnapshot(ctx = null, transition = null) {
            if (!transition) return false;
            const resources = this.ensureLayerTransitionSnapshotResources(ctx);
            if (!resources || !resources.renderer || !resources.texture || !resources.sprite) {
                if (typeof PIXI !== "undefined" && ctx && ctx.app) {
                    throw new Error("missing layer transition snapshot render texture");
                }
                return false;
            }
            if (!(this.layers && this.layers.root)) {
                throw new Error("missing layer transition snapshot render root");
            }
            const hidden = [];
            const hiddenSet = new Set();
            const hide = (displayObj) => {
                if (!displayObj || displayObj.visible === false) return;
                if (hiddenSet.has(displayObj)) return;
                hiddenSet.add(displayObj);
                hidden.push({
                    displayObj,
                    visible: displayObj.visible,
                    renderable: displayObj.renderable
                });
                displayObj.visible = false;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = false;
                }
            };
            hide(resources.sprite);
            hide(this.layers.ui);
            hide(this.layers.scriptMessages);
            this.hidePlayerDisplayObjectsForLayerTransitionSnapshot(ctx, hide);
            try {
                const cleared = this.clearBuildingCutawayCompositeRenderTarget(resources);
                if (!cleared) {
                    throw new Error("layer transition snapshot texture could not be cleared");
                }
                resources.renderer.render(this.layers.root, resources.texture, false);
            } catch (err) {
                throw new Error(`layer transition snapshot capture failed: ${err && err.message ? err.message : err}`);
            } finally {
                for (let i = hidden.length - 1; i >= 0; i--) {
                    const entry = hidden[i];
                    if (!entry || !entry.displayObj) continue;
                    entry.displayObj.visible = entry.visible;
                    if (Object.prototype.hasOwnProperty.call(entry.displayObj, "renderable")) {
                        entry.displayObj.renderable = entry.renderable;
                    }
                }
            }
            this.layerTransitionSnapshot = {
                active: true,
                fromLayer: this.getLayerIndexFromValue(transition.fromLayer, 0),
                toLayer: this.getLayerIndexFromValue(transition.toLayer, 0),
                fadingLayer: this.getLayerIndexFromValue(transition.fadingLayer, transition.fromLayer),
                startedAtMs: Number(transition.startedAtMs) || Date.now(),
                durationMs: Math.max(1, Number(transition.durationMs) || 500),
                width: resources.width,
                height: resources.height,
                camera: this.getLayerTransitionSnapshotCameraState(ctx)
            };
            resources.sprite.alpha = 1;
            resources.sprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(resources.sprite, "renderable")) {
                resources.sprite.renderable = true;
            }
            return true;
        }

        hideLayerTransitionSnapshot() {
            const sprite = this.layerTransitionSnapshotSprite;
            if (sprite) {
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }
                if (sprite.parent) sprite.parent.removeChild(sprite);
            }
            this.layerTransitionSnapshot = null;
        }

        renderLayerTransitionSnapshot(ctx = null) {
            const snapshot = this.layerTransitionSnapshot;
            const sprite = this.layerTransitionSnapshotSprite;
            if (!snapshot || snapshot.active !== true || !sprite) {
                this.hideLayerTransitionSnapshot();
                return null;
            }
            const nowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now();
            const alpha = this.getLayerFadeMultiplier(snapshot.fadingLayer, nowMs);
            if (!(alpha > 0.001)) {
                this.hideLayerTransitionSnapshot();
                return null;
            }
            const root = this.layers && this.layers.root ? this.layers.root : null;
            if (!root) {
                throw new Error("missing layer transition snapshot render root");
            }
            if (sprite.parent !== root) {
                root.addChild(sprite);
            }
            this.setLayerTransitionSnapshotDisplayIndex();
            const offset = this.getLayerTransitionSnapshotScreenOffset(snapshot, ctx);
            const width = Number(snapshot.width) ||
                Number(this.layerTransitionSnapshotSize && this.layerTransitionSnapshotSize.width) ||
                Number(sprite.texture && sprite.texture.width);
            const height = Number(snapshot.height) ||
                Number(this.layerTransitionSnapshotSize && this.layerTransitionSnapshotSize.height) ||
                Number(sprite.texture && sprite.texture.height);
            this.applyLayerTransitionSnapshotSpriteTransform(sprite, width, height, offset.x, offset.y);
            sprite.alpha = alpha;
            sprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                sprite.renderable = true;
            }
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                sprite.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            return sprite;
        }

        getBuildingCutawayCompositeDisplayObjects(cutawayState) {
            const frameId = Number(this._layerCutawayFrameId) || 0;
            const out = new Set();
            const add = (displayObj, captureAlpha = 1) => {
                if (!displayObj || !displayObj.parent || displayObj.visible === false) return;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable") && displayObj.renderable === false) return;
                this.markBuildingCutawayCompositeCaptureAlpha(displayObj, captureAlpha);
                out.add(displayObj);
            };
            if (this.floorVisualMeshByKey instanceof Map) {
                for (const entry of this.floorVisualMeshByKey.values()) {
                    const mesh = entry && entry.mesh;
                    if (mesh && mesh._buildingCutawayCompositeFrame === frameId) {
                        add(mesh, mesh._buildingCutawayCompositeCaptureAlpha);
                    }
                }
            }
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) continue;
                const cache = trigger.renderCache || null;
                const items = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < items.length; j++) {
                    const item = items[j] && items[j].item;
                    if (!item || item._cutawayCompositeFrame !== frameId) continue;
                    if (!this.isBuildingCutawayStructuralItem(item)) continue;
                    const captureAlpha = this.getBuildingCutawayCompositeCaptureAlphaForItem(item);
                    const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                    for (let d = 0; d < displayObjects.length; d++) {
                        add(displayObjects[d], captureAlpha);
                    }
                }
            }
            return out;
        }

        getBuildingCutawayCompositeOriginalDisplayObjects(cutawayState) {
            const frameId = Number(this._layerCutawayFrameId) || 0;
            const out = new Set();
            const add = (displayObj) => {
                if (!displayObj || !displayObj.parent || displayObj.visible === false) return;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable") && displayObj.renderable === false) return;
                out.add(displayObj);
            };
            if (this.floorVisualMeshByKey instanceof Map) {
                for (const entry of this.floorVisualMeshByKey.values()) {
                    const mesh = entry && entry.mesh;
                    if (mesh && mesh._buildingCutawayCompositeFrame === frameId) add(mesh);
                }
            }
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) continue;
                const cache = trigger.renderCache || null;
                const items = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < items.length; j++) {
                    const item = items[j] && items[j].item;
                    if (!item || item._cutawayCompositeFrame !== frameId) continue;
                    const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                    for (let d = 0; d < displayObjects.length; d++) add(displayObjects[d]);
                }
                const groundWalls = Array.isArray(cache && cache.groundProjectionWalls) ? cache.groundProjectionWalls : [];
                for (let j = 0; j < groundWalls.length; j++) {
                    const wall = groundWalls[j] && groundWalls[j].item;
                    if (!wall || wall._cutawayCompositeFrame !== frameId) continue;
                    const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(wall);
                    for (let d = 0; d < displayObjects.length; d++) add(displayObjects[d]);
                }
            }
            return out;
        }

        getBuildingCutawayCompositeWallSelectionDiagnostics(cutawayState, displayObjects) {
            const frameId = Number(this._layerCutawayFrameId) || 0;
            const selected = displayObjects instanceof Set ? displayObjects : new Set();
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            let expectedWalls = 0;
            let selectedWalls = 0;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building) continue;
                const cache = trigger.renderCache || null;
                const items = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < items.length; j++) {
                    const item = items[j] && items[j].item;
                    if (!item || item.type !== "wallSection" || item._cutawayCompositeFrame !== frameId) continue;
                    if (item.gone || item.vanishing) continue;
                    expectedWalls += 1;
                    const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                    if (displayObjects.some(displayObject => selected.has(displayObject))) {
                        selectedWalls += 1;
                    }
                }
            }
            return { expectedWalls, selectedWalls };
        }

        isolateBuildingCutawayCompositeDisplayTree(keepSet) {
            const saved = [];
            const saveVisible = (displayObj, visible) => {
                if (!displayObj || displayObj.visible === visible) return;
                saved.push({ displayObj, visible: displayObj.visible, renderable: displayObj.renderable });
                displayObj.visible = visible;
                if (!visible && Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = false;
                }
            };
            const visit = (displayObj) => {
                if (!displayObj) return false;
                if (keepSet.has(displayObj)) return true;
                const children = Array.isArray(displayObj.children) ? displayObj.children : [];
                let hasKeptChild = false;
                for (let i = 0; i < children.length; i++) {
                    if (visit(children[i])) hasKeptChild = true;
                }
                const keep = hasKeptChild;
                if (!keep) saveVisible(displayObj, false);
                return keep;
            };
            const includeLayers = [
                this.layers && this.layers.depthObjects,
                this.layers && this.layers.groundObjects,
                this.layers && this.layers.objects3d,
                this.layers && this.layers.entities
            ].filter(Boolean);
            const includeSet = new Set(includeLayers);
            const rootChildren = this.layers && this.layers.root && Array.isArray(this.layers.root.children)
                ? this.layers.root.children
                : [];
            for (let i = 0; i < rootChildren.length; i++) {
                const child = rootChildren[i];
                if (!child) continue;
                if (includeSet.has(child)) visit(child);
                else saveVisible(child, false);
            }
            return () => {
                for (let i = saved.length - 1; i >= 0; i--) {
                    const entry = saved[i];
                    if (!entry || !entry.displayObj) continue;
                    entry.displayObj.visible = entry.visible;
                    if (Object.prototype.hasOwnProperty.call(entry.displayObj, "renderable")) {
                        entry.displayObj.renderable = entry.renderable;
                    }
                }
            };
        }

        applyBuildingCutawayCompositeLocalCaptureState(displayObjects, bounds, textureWidth, textureHeight) {
            const displaySet = displayObjects instanceof Set ? displayObjects : new Set();
            const savedAlpha = [];
            const savedUniforms = [];
            const root = this.layers && this.layers.root ? this.layers.root : null;
            const rootPosition = root && root.position ? root.position : null;
            const rootX = rootPosition && Number.isFinite(rootPosition.x) ? rootPosition.x : null;
            const rootY = rootPosition && Number.isFinite(rootPosition.y) ? rootPosition.y : null;
            const offsetX = Number.isFinite(bounds && bounds.x) ? Number(bounds.x) : 0;
            const offsetY = Number.isFinite(bounds && bounds.y) ? Number(bounds.y) : 0;
            const width = Math.max(1, Math.round(Number(textureWidth) || Number(bounds && bounds.width) || 1));
            const height = Math.max(1, Math.round(Number(textureHeight) || Number(bounds && bounds.height) || 1));
            const cam = this.camera || {};
            const projectionSpace = !!(bounds && bounds.projectionSpace);
            const presentationXyRatio = this.getBuildingCutawayCompositeProjectionXyRatio();
            const viewScale = projectionSpace
                ? Math.max(1, Number(bounds.pxPerWorld) || BUILDING_CUTAWAY_CACHE_PX_PER_WORLD)
                : Math.max(1e-6, Math.abs(Number(cam.viewscale) || 1));
            const xyRatio = projectionSpace
                ? 1
                : Math.max(1e-6, Math.abs(Number(cam.xyratio) || 1));
            const localCameraX = projectionSpace
                ? (Number(bounds.minX) || 0)
                : ((Number(cam.x) || 0) + offsetX / viewScale);
            const localCameraY = projectionSpace
                ? (Number(bounds.minY) || 0)
                : ((Number(cam.y) || 0) + offsetY / (viewScale * xyRatio));
            const localCameraZ = projectionSpace ? 0 : (Number(cam.z) || 0);
            const rootCaptureAlpha = (displayObj) => {
                if (!displaySet.has(displayObj)) return 1;
                return Number.isFinite(displayObj && displayObj._buildingCutawayCompositeCaptureAlpha)
                    ? Math.max(0, Math.min(1, Number(displayObj._buildingCutawayCompositeCaptureAlpha)))
                    : 1;
            };

            if (!projectionSpace && rootPosition && rootX !== null && rootY !== null) {
                rootPosition.x = rootX - offsetX;
                rootPosition.y = rootY - offsetY;
            }

            const visitSeen = new Set();
            const visit = (displayObj) => {
                if (!displayObj || visitSeen.has(displayObj)) return;
                visitSeen.add(displayObj);
                if (typeof displayObj.alpha === "number") {
                    savedAlpha.push({ displayObj, alpha: displayObj.alpha });
                    displayObj.alpha = rootCaptureAlpha(displayObj);
                }
                const uniforms = displayObj.shader && displayObj.shader.uniforms ? displayObj.shader.uniforms : null;
                if (uniforms) {
                    const saved = { uniforms };
                    let changed = false;
                    if (uniforms.uScreenSize && uniforms.uScreenSize.length >= 2) {
                        saved.uScreenSize = [uniforms.uScreenSize[0], uniforms.uScreenSize[1]];
                        uniforms.uScreenSize[0] = width;
                        uniforms.uScreenSize[1] = height;
                        changed = true;
                    }
                    if (uniforms.uCameraWorld && uniforms.uCameraWorld.length >= 2) {
                        saved.uCameraWorld = [uniforms.uCameraWorld[0], uniforms.uCameraWorld[1]];
                        uniforms.uCameraWorld[0] = localCameraX;
                        uniforms.uCameraWorld[1] = localCameraY;
                        changed = true;
                    }
                    if (Object.prototype.hasOwnProperty.call(uniforms, "uCameraZ")) {
                        saved.uCameraZ = uniforms.uCameraZ;
                        uniforms.uCameraZ = localCameraZ;
                        changed = true;
                    }
                    if (Object.prototype.hasOwnProperty.call(uniforms, "uViewScale")) {
                        saved.uViewScale = uniforms.uViewScale;
                        uniforms.uViewScale = viewScale;
                        changed = true;
                    }
                    if (Object.prototype.hasOwnProperty.call(uniforms, "uXyRatio")) {
                        saved.uXyRatio = uniforms.uXyRatio;
                        uniforms.uXyRatio = xyRatio;
                        changed = true;
                    }
                    if (uniforms.uTint && uniforms.uTint.length >= 4) {
                        saved.uTint = [uniforms.uTint[0], uniforms.uTint[1], uniforms.uTint[2], uniforms.uTint[3]];
                        uniforms.uTint[3] = rootCaptureAlpha(displayObj);
                        changed = true;
                    }
                    if (projectionSpace && Object.prototype.hasOwnProperty.call(uniforms, "uBuildingCutawayProjectionPass")) {
                        saved.uBuildingCutawayProjectionPass = uniforms.uBuildingCutawayProjectionPass;
                        uniforms.uBuildingCutawayProjectionPass = 1;
                        changed = true;
                    }
                    if (projectionSpace && Object.prototype.hasOwnProperty.call(uniforms, "uBuildingCutawayPresentationXyRatio")) {
                        saved.uBuildingCutawayPresentationXyRatio = uniforms.uBuildingCutawayPresentationXyRatio;
                        uniforms.uBuildingCutawayPresentationXyRatio = presentationXyRatio;
                        changed = true;
                    }
                    if (changed) savedUniforms.push(saved);
                }
                const children = Array.isArray(displayObj.children) ? displayObj.children : [];
                for (let i = 0; i < children.length; i++) visit(children[i]);
            };
            displaySet.forEach(displayObj => visit(displayObj));

            return () => {
                for (let i = savedUniforms.length - 1; i >= 0; i--) {
                    const saved = savedUniforms[i];
                    const uniforms = saved && saved.uniforms;
                    if (!uniforms) continue;
                    if (saved.uScreenSize && uniforms.uScreenSize && uniforms.uScreenSize.length >= 2) {
                        uniforms.uScreenSize[0] = saved.uScreenSize[0];
                        uniforms.uScreenSize[1] = saved.uScreenSize[1];
                    }
                    if (saved.uCameraWorld && uniforms.uCameraWorld && uniforms.uCameraWorld.length >= 2) {
                        uniforms.uCameraWorld[0] = saved.uCameraWorld[0];
                        uniforms.uCameraWorld[1] = saved.uCameraWorld[1];
                    }
                    if (Object.prototype.hasOwnProperty.call(saved, "uCameraZ")) uniforms.uCameraZ = saved.uCameraZ;
                    if (Object.prototype.hasOwnProperty.call(saved, "uViewScale")) uniforms.uViewScale = saved.uViewScale;
                    if (Object.prototype.hasOwnProperty.call(saved, "uXyRatio")) uniforms.uXyRatio = saved.uXyRatio;
                    if (saved.uTint && uniforms.uTint && uniforms.uTint.length >= 4) {
                        uniforms.uTint[0] = saved.uTint[0];
                        uniforms.uTint[1] = saved.uTint[1];
                        uniforms.uTint[2] = saved.uTint[2];
                        uniforms.uTint[3] = saved.uTint[3];
                    }
                    if (Object.prototype.hasOwnProperty.call(saved, "uBuildingCutawayProjectionPass")) {
                        uniforms.uBuildingCutawayProjectionPass = saved.uBuildingCutawayProjectionPass;
                    }
                    if (Object.prototype.hasOwnProperty.call(saved, "uBuildingCutawayPresentationXyRatio")) {
                        uniforms.uBuildingCutawayPresentationXyRatio = saved.uBuildingCutawayPresentationXyRatio;
                    }
                }
                for (let i = savedAlpha.length - 1; i >= 0; i--) {
                    const entry = savedAlpha[i];
                    if (entry && entry.displayObj) entry.displayObj.alpha = entry.alpha;
                }
                if (!projectionSpace && rootPosition && rootX !== null && rootY !== null) {
                    rootPosition.x = rootX;
                    rootPosition.y = rootY;
                }
            };
        }

        applyBuildingCutawayCompositeDataPassState(displayObjects) {
            const displaySet = displayObjects instanceof Set ? displayObjects : new Set();
            const savedUniforms = [];
            let dataShaderCount = 0;
            const visitSeen = new Set();
            const visit = (displayObj) => {
                if (!displayObj || visitSeen.has(displayObj)) return;
                visitSeen.add(displayObj);
                const uniforms = displayObj.shader && displayObj.shader.uniforms ? displayObj.shader.uniforms : null;
                if (uniforms && Object.prototype.hasOwnProperty.call(uniforms, "uBuildingCutawayDataPass")) {
                    const saved = {
                        uniforms,
                        uBuildingCutawayDataPass: uniforms.uBuildingCutawayDataPass,
                        uBuildingCutawayDataZRange: uniforms.uBuildingCutawayDataZRange
                            ? [uniforms.uBuildingCutawayDataZRange[0], uniforms.uBuildingCutawayDataZRange[1]]
                            : null
                    };
                    uniforms.uBuildingCutawayDataPass = 1;
                    if (uniforms.uBuildingCutawayDataZRange && uniforms.uBuildingCutawayDataZRange.length >= 2) {
                        uniforms.uBuildingCutawayDataZRange[0] = BUILDING_CUTAWAY_DATA_MIN_Z;
                        uniforms.uBuildingCutawayDataZRange[1] = 1 / Math.max(1e-6, BUILDING_CUTAWAY_DATA_MAX_Z - BUILDING_CUTAWAY_DATA_MIN_Z);
                    }
                    savedUniforms.push(saved);
                    dataShaderCount += 1;
                }
                const children = Array.isArray(displayObj.children) ? displayObj.children : [];
                for (let i = 0; i < children.length; i++) visit(children[i]);
            };
            displaySet.forEach(displayObj => visit(displayObj));
            if (dataShaderCount <= 0) {
                this.failBuildingCutawayComposite("no selected structural display object supports the data pass", {
                    displayObjects: displaySet.size
                });
            }
            return () => {
                for (let i = savedUniforms.length - 1; i >= 0; i--) {
                    const saved = savedUniforms[i];
                    const uniforms = saved && saved.uniforms;
                    if (!uniforms) continue;
                    uniforms.uBuildingCutawayDataPass = saved.uBuildingCutawayDataPass;
                    if (
                        saved.uBuildingCutawayDataZRange &&
                        uniforms.uBuildingCutawayDataZRange &&
                        uniforms.uBuildingCutawayDataZRange.length >= 2
                    ) {
                        uniforms.uBuildingCutawayDataZRange[0] = saved.uBuildingCutawayDataZRange[0];
                        uniforms.uBuildingCutawayDataZRange[1] = saved.uBuildingCutawayDataZRange[1];
                    }
                }
            };
        }

        hideBuildingCutawayCompositeOriginals(displayObjects, preserveDisplayObjects = null) {
            if (!(displayObjects instanceof Set)) return 0;
            const preserve = preserveDisplayObjects instanceof Set ? preserveDisplayObjects : null;
            let hidden = 0;
            displayObjects.forEach(displayObj => {
                if (!displayObj) return;
                if (preserve && preserve.has(displayObj)) return;
                displayObj.visible = false;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = false;
                }
                hidden += 1;
            });
            return hidden;
        }

        getBuildingCutawayCompositeScreenSize(ctx = null) {
            const appRef = (ctx && ctx.app && ctx.app.renderer)
                ? ctx.app
                : ((typeof app !== "undefined" && app && app.renderer) ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            const screen = (appRef && appRef.screen) || (renderer && renderer.screen) || {};
            return {
                width: Math.max(1, Math.round(Number(screen.width) || Number(renderer && renderer.width) || 1)),
                height: Math.max(1, Math.round(Number(screen.height) || Number(renderer && renderer.height) || 1))
            };
        }

        getBuildingCutawayCompositeSignature(ctx = null, cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(ctx);
            const triggers = this.getBuildingCutawayCompositeTriggers(state);
            if (triggers.length === 0) return "";
            const triggerSig = triggers.map(trigger => {
                const buildingId = typeof trigger.buildingId === "string" ? trigger.buildingId : "";
                const cacheKey = trigger.renderCache && typeof trigger.renderCache.cacheKey === "string"
                    ? trigger.renderCache.cacheKey
                    : "";
                return [
                    buildingId,
                    cacheKey
                ].join(":");
            }).sort().join("|");
            const nowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now();
            const fadeSig = Number.isFinite(this._fallRevealHiddenLayer)
                ? [
                    "fall",
                    this.getLayerIndexFromValue(this._fallRevealHiddenLayer, 0),
                    this.getLayerFadeMultiplier(this._fallRevealHiddenLayer, nowMs).toFixed(3)
                ].join(":")
                : "fall:none";
            return [
                `v${BUILDING_CUTAWAY_COMPOSITE_VERSION}`,
                BUILDING_CUTAWAY_CACHE_PX_PER_WORLD,
                this.getBuildingCutawayCompositeProjectionXyRatio(ctx).toFixed(6),
                triggerSig,
                fadeSig
            ].join("|");
        }

        getBuildingCutawayCompositePresentationKey(ctx = null, cutawayState = null) {
            const state = cutawayState || this.getLayerCutawayState(ctx);
            const triggers = this.getBuildingCutawayCompositeTriggers(state);
            if (triggers.length === 0) return "";
            const buildingSig = triggers.map(trigger => (
                typeof trigger.buildingId === "string" && trigger.buildingId.length > 0
                    ? trigger.buildingId
                    : (trigger.building && typeof trigger.building.buildingId === "string" ? trigger.building.buildingId : "")
            )).sort().join("|");
            return [
                `v${BUILDING_CUTAWAY_COMPOSITE_VERSION}`,
                BUILDING_CUTAWAY_CACHE_PX_PER_WORLD,
                this.getBuildingCutawayCompositeProjectionXyRatio(ctx).toFixed(6),
                buildingSig
            ].join("|");
        }

        isBuildingCutawayCompositeCacheUsable(ctx = null, cutawayState = null) {
            const cache = this.buildingCutawayCompositeCache || null;
            if (!cache || !cache.texture || !cache.dataTexture || cache.active !== true) return false;
            const signature = this.getBuildingCutawayCompositeSignature(ctx, cutawayState);
            return !!(signature && cache.signature === signature);
        }

        isBuildingCutawayCompositeCachePresentable(ctx = null, cutawayState = null) {
            const cache = this.buildingCutawayCompositeCache || null;
            if (!cache || !cache.texture || !cache.dataTexture || cache.active !== true) return false;
            const signature = this.getBuildingCutawayCompositeSignature(ctx, cutawayState);
            if (signature && cache.signature === signature) return true;
            const presentationKey = this.getBuildingCutawayCompositePresentationKey(ctx, cutawayState);
            return !!(
                presentationKey &&
                typeof cache.presentationKey === "string" &&
                cache.presentationKey === presentationKey
            );
        }

        isBuildingCutawayCompositeTextureReady(texture) {
            if (!texture) return true;
            if (typeof PIXI !== "undefined" && PIXI.Texture) {
                if (texture === PIXI.Texture.WHITE) return true;
                if (texture === PIXI.Texture.EMPTY) return false;
            }
            const baseTexture = texture.baseTexture || null;
            if (!baseTexture || baseTexture.valid !== true) return false;
            const frame = texture.frame || texture.orig || null;
            const width = Number(frame && frame.width);
            const height = Number(frame && frame.height);
            return width > 0 && height > 0;
        }

        collectBuildingCutawayCompositePendingTextures(displayObjects, maxSamples = 8) {
            const roots = displayObjects instanceof Set ? Array.from(displayObjects) : [];
            const pending = [];
            const seenObjects = new Set();
            const seenBaseTextures = new Set();
            const inspectTexture = (texture, owner, label) => {
                if (!texture || this.isBuildingCutawayCompositeTextureReady(texture)) return;
                const baseTexture = texture.baseTexture || null;
                if (baseTexture && seenBaseTextures.has(baseTexture)) return;
                if (baseTexture) seenBaseTextures.add(baseTexture);
                if (pending.length < maxSamples) {
                    pending.push({
                        texture,
                        baseTexture,
                        label,
                        ownerName: owner && typeof owner.name === "string" ? owner.name : "",
                        ownerType: owner && owner.constructor && owner.constructor.name ? owner.constructor.name : "",
                        baseTextureValid: !!(baseTexture && baseTexture.valid),
                        frameWidth: texture.frame && Number.isFinite(texture.frame.width) ? Number(texture.frame.width) : null,
                        frameHeight: texture.frame && Number.isFinite(texture.frame.height) ? Number(texture.frame.height) : null
                    });
                } else {
                    pending.push({ texture, baseTexture });
                }
            };
            const stack = roots.slice();
            while (stack.length > 0) {
                const current = stack.pop();
                if (!current || seenObjects.has(current)) continue;
                seenObjects.add(current);
                if (Object.prototype.hasOwnProperty.call(current, "texture")) {
                    inspectTexture(current.texture, current, "texture");
                }
                const shaderUniforms = current.shader && current.shader.uniforms ? current.shader.uniforms : null;
                if (shaderUniforms) {
                    for (const key of Object.keys(shaderUniforms)) {
                        const value = shaderUniforms[key];
                        if (value && value.baseTexture) inspectTexture(value, current, `shader.${key}`);
                    }
                }
                const materialTexture = current.material && current.material.texture ? current.material.texture : null;
                if (materialTexture) inspectTexture(materialTexture, current, "material.texture");
                const children = Array.isArray(current.children) ? current.children : null;
                if (children) {
                    for (let i = children.length - 1; i >= 0; i--) stack.push(children[i]);
                }
            }
            return {
                count: seenBaseTextures.size,
                samples: pending.slice(0, maxSamples),
                baseTextures: Array.from(seenBaseTextures)
            };
        }

        watchBuildingCutawayCompositePendingTextures(pendingInfo) {
            const baseTextures = Array.isArray(pendingInfo && pendingInfo.baseTextures) ? pendingInfo.baseTextures : [];
            if (!(this.buildingCutawayCompositePendingBaseTextures instanceof Set)) {
                this.buildingCutawayCompositePendingBaseTextures = new Set();
            }
            for (let i = 0; i < baseTextures.length; i++) {
                const baseTexture = baseTextures[i];
                if (!baseTexture || baseTexture.valid === true || this.buildingCutawayCompositePendingBaseTextures.has(baseTexture)) continue;
                this.buildingCutawayCompositePendingBaseTextures.add(baseTexture);
                let completed = false;
                const onReady = () => {
                    if (completed) return;
                    completed = true;
                    this.buildingCutawayCompositePendingBaseTextures.delete(baseTexture);
                    if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                        globalThis.presentGameFrame();
                    }
                };
                if (typeof baseTexture.once === "function") {
                    baseTexture.once("loaded", onReady);
                    baseTexture.once("update", onReady);
                    baseTexture.once("error", onReady);
                }
            }
        }

        getBuildingCutawayCompositeProjectionBounds(cutawayState = null, paddingPx = 2) {
            const state = cutawayState || this.getLayerCutawayState(null);
            const triggers = Array.isArray(state && state.triggers)
                ? state.triggers.filter(trigger => trigger && trigger.building)
                : [];
            if (triggers.length === 0) return null;
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            const visitPoint = (point) => {
                if (!point) return;
                const x = Number(point.x);
                const y = Number(point.y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) return;
                minX = Math.min(minX, x);
                minY = Math.min(minY, y);
                maxX = Math.max(maxX, x);
                maxY = Math.max(maxY, y);
            };
            const visitRoof = (roof) => {
                const roofPolygons = this.collectRoofCompositeProjectionPolygons(
                    roof,
                    this.getBuildingCutawayCompositeProjectionXyRatio()
                );
                for (let p = 0; p < roofPolygons.length; p++) {
                    const polygon = roofPolygons[p];
                    for (let j = 0; j < polygon.length; j++) visitPoint(polygon[j]);
                }
            };
            for (let i = 0; i < triggers.length; i++) {
                const polygons = Array.isArray(triggers[i] && triggers[i].occlusionPolygons)
                    ? triggers[i].occlusionPolygons
                    : [];
                for (let p = 0; p < polygons.length; p++) {
                    const outer = Array.isArray(polygons[p] && polygons[p].outer)
                        ? polygons[p].outer
                        : (Array.isArray(polygons[p]) ? polygons[p] : []);
                    for (let j = 0; j < outer.length; j++) visitPoint(outer[j]);
                }
                const cache = triggers[i] && triggers[i].renderCache;
                const roofs = Array.isArray(cache && cache.roofs) ? cache.roofs : [];
                for (let r = 0; r < roofs.length; r++) visitRoof(roofs[r]);
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
                return null;
            }
            const pxPerWorld = Math.max(1, Number(BUILDING_CUTAWAY_CACHE_PX_PER_WORLD) || 1);
            const padWorld = Math.max(0, Number(paddingPx) || 0) / pxPerWorld;
            minX -= padWorld;
            minY -= padWorld;
            maxX += padWorld;
            maxY += padWorld;
            const worldWidth = Math.max(1 / pxPerWorld, maxX - minX);
            const worldHeight = Math.max(1 / pxPerWorld, maxY - minY);
            const width = Math.max(1, Math.ceil(worldWidth * pxPerWorld));
            const height = Math.max(1, Math.ceil(worldHeight * pxPerWorld));
            if (width > BUILDING_CUTAWAY_CACHE_MAX_TEXTURE_SIZE || height > BUILDING_CUTAWAY_CACHE_MAX_TEXTURE_SIZE) {
                this.failBuildingCutawayComposite("canonical cache texture too large", {
                    width,
                    height,
                    maxTextureSize: BUILDING_CUTAWAY_CACHE_MAX_TEXTURE_SIZE,
                    pxPerWorld,
                    minX,
                    minY,
                    maxX,
                    maxY
                });
            }
            return {
                x: minX,
                y: minY,
                width,
                height,
                minX,
                minY,
                maxX: minX + (width / pxPerWorld),
                maxY: minY + (height / pxPerWorld),
                worldWidth: width / pxPerWorld,
                worldHeight: height / pxPerWorld,
                pxPerWorld,
                projectionSpace: true
            };
        }

        getBuildingCutawayCompositeAnchor(cutawayState = null, projectionBounds = null) {
            const state = cutawayState || this.getLayerCutawayState(null);
            const triggers = Array.isArray(state && state.triggers)
                ? state.triggers.filter(trigger => trigger && trigger.building)
                : [];
            if (triggers.length === 0) return null;
            const bounds = projectionBounds || this.getBuildingCutawayCompositeProjectionBounds(state);
            if (!bounds) return null;
            let minWorldX = Infinity;
            let maxWorldX = -Infinity;
            let bottomWorldY = -Infinity;
            const visitRing = (ring) => {
                const points = normalizeFloorVisualPointList(ring);
                for (let i = 0; i < points.length; i++) {
                    const wx = Number(points[i].x);
                    const wy = Number(points[i].y);
                    if (!Number.isFinite(wx) || !Number.isFinite(wy)) continue;
                    minWorldX = Math.min(minWorldX, wx);
                    maxWorldX = Math.max(maxWorldX, wx);
                    bottomWorldY = Math.max(bottomWorldY, wy);
                }
            };
            const getFragmentOuter = (fragment) => fragment && fragment.outerPolygon;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const cache = trigger.renderCache || null;
                const fragments = Array.isArray(cache && cache.fragments) ? cache.fragments : [];
                let lowestLevel = Infinity;
                for (let f = 0; f < fragments.length; f++) {
                    const fragment = fragments[f];
                    if (!fragment) continue;
                    const level = this.getLayerIndexFromValue(fragment.level, 0);
                    if (level < lowestLevel) lowestLevel = level;
                }
                let foundLowestFragment = false;
                if (Number.isFinite(lowestLevel)) {
                    for (let f = 0; f < fragments.length; f++) {
                        const fragment = fragments[f];
                        if (!fragment || this.getLayerIndexFromValue(fragment.level, 0) !== lowestLevel) continue;
                        visitRing(this.projectWorldRingToCutawayPlane(
                            getFragmentOuter(fragment),
                            this.getLayerBaseZForLevel(0)
                        ));
                        foundLowestFragment = true;
                    }
                }
                if (foundLowestFragment) continue;
                const polygons = Array.isArray(trigger.groundProjectionPolygons)
                    ? trigger.groundProjectionPolygons
                    : [];
                for (let p = 0; p < polygons.length; p++) {
                    visitRing(polygons[p] && polygons[p].outer);
                }
            }
            if (!Number.isFinite(minWorldX) || !Number.isFinite(maxWorldX) || !Number.isFinite(bottomWorldY)) return null;
            let worldX = (minWorldX + maxWorldX) * 0.5;
            let worldY = bottomWorldY;
            const mapRef = this.camera.map || global.map || null;
            if (mapRef && typeof mapRef.wrapWorldX === "function" && Number.isFinite(worldX)) {
                worldX = mapRef.wrapWorldX(worldX);
            }
            if (mapRef && typeof mapRef.wrapWorldY === "function" && Number.isFinite(worldY)) {
                worldY = mapRef.wrapWorldY(worldY);
            }
            const worldZ = 0;
            const projectedCenterX = worldX;
            const projectedBottomY = worldY - worldZ;
            const centerX = (projectedCenterX - bounds.minX) * bounds.pxPerWorld;
            const bottomY = (projectedBottomY - bounds.minY) * bounds.pxPerWorld;
            return {
                centerX,
                bottomY,
                projectedCenterX,
                projectedBottomY,
                worldX,
                worldY,
                worldZ
            };
        }

        getBuildingCutawayCompositeScreenBounds(cutawayState = null, paddingPx = 2) {
            const state = cutawayState || this.getLayerCutawayState(null);
            const triggers = Array.isArray(state && state.triggers)
                ? state.triggers.filter(trigger => trigger && trigger.building)
                : [];
            if (triggers.length === 0 || !this.camera || typeof this.camera.worldToScreen !== "function") return null;
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            const visitPoint = (point) => {
                if (!point) return;
                const wx = Number(point.x);
                const wy = Number(point.y);
                if (!Number.isFinite(wx) || !Number.isFinite(wy)) return;
                const screen = this.camera.worldToScreen(wx, wy, 0);
                const sx = Number(screen && screen.x);
                const sy = Number(screen && screen.y);
                if (!Number.isFinite(sx) || !Number.isFinite(sy)) return;
                minX = Math.min(minX, sx);
                minY = Math.min(minY, sy);
                maxX = Math.max(maxX, sx);
                maxY = Math.max(maxY, sy);
            };
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const polygons = Array.isArray(trigger && trigger.occlusionPolygons)
                    ? trigger.occlusionPolygons
                    : [];
                for (let p = 0; p < polygons.length; p++) {
                    const polygon = polygons[p];
                    const outer = Array.isArray(polygon && polygon.outer)
                        ? polygon.outer
                        : (Array.isArray(polygon) ? polygon : []);
                    for (let j = 0; j < outer.length; j++) visitPoint(outer[j]);
                }
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
                return null;
            }
            const pad = Math.max(0, Number(paddingPx) || 0);
            const x = Math.floor(minX - pad);
            const y = Math.floor(minY - pad);
            const right = Math.ceil(maxX + pad);
            const bottom = Math.ceil(maxY + pad);
            return {
                x,
                y,
                width: Math.max(1, right - x),
                height: Math.max(1, bottom - y)
            };
        }

        getBuildingCutawayCompositeCaptureBounds(cutawayState = null, anchor = null) {
            return this.getBuildingCutawayCompositeProjectionBounds(cutawayState);
        }

        updateBuildingCutawayCompositeBillboardTexture(resources, anchor, bounds = null) {
            if (!resources || !resources.texture || !anchor || typeof PIXI === "undefined" || !PIXI.Texture || !PIXI.Rectangle) return null;
            const width = Math.max(1, Math.round(Number(resources.width) || 1));
            const height = Math.max(1, Math.round(Number(resources.height) || 1));
            if (resources.localBounds) {
                const oldTexture = this.buildingCutawayCompositeTexture;
                this.buildingCutawayCompositeTexture = resources.texture;
                if (oldTexture && oldTexture !== resources.texture && typeof oldTexture.destroy === "function") {
                    oldTexture.destroy(false);
                }
                const sprite = resources.sprite;
                sprite.texture = resources.texture;
                const anchorX = bounds && bounds.projectionSpace
                    ? Math.max(0, Math.min(1, (Number(anchor.centerX) || 0) / width))
                    : Math.max(0, Math.min(1, ((Number(anchor.centerX) || 0) - (Number(bounds && bounds.x) || 0)) / width));
                const anchorY = bounds && bounds.projectionSpace
                    ? Math.max(0, Math.min(1, (Number(anchor.bottomY) || 0) / height))
                    : Math.max(0, Math.min(1, ((Number(anchor.bottomY) || 0) - (Number(bounds && bounds.y) || 0)) / height));
                if (sprite.anchor && typeof sprite.anchor.set === "function") {
                    sprite.anchor.set(anchorX, anchorY);
                }
                sprite.width = width;
                sprite.height = height;
                return {
                    texture: resources.texture,
                    width,
                    height,
                    anchorX,
                    anchorY,
                    bounds: bounds && bounds.projectionSpace
                        ? {
                            ...bounds,
                            width,
                            height,
                            worldWidth: Number.isFinite(bounds.worldWidth) ? Number(bounds.worldWidth) : width,
                            worldHeight: Number.isFinite(bounds.worldHeight) ? Number(bounds.worldHeight) : height,
                            projectionSpace: true
                        }
                        : {
                            x: Number(bounds && bounds.x) || 0,
                            y: Number(bounds && bounds.y) || 0,
                            width,
                            height
                        }
                };
            }
            const rawX = Number.isFinite(bounds && bounds.x) ? Number(bounds.x) : 0;
            const rawY = Number.isFinite(bounds && bounds.y) ? Number(bounds.y) : 0;
            const rawRight = rawX + (Number.isFinite(bounds && bounds.width) ? Number(bounds.width) : width);
            const rawBottom = rawY + (Number.isFinite(bounds && bounds.height) ? Number(bounds.height) : height);
            const frameX = Math.max(0, Math.min(width - 1, Math.floor(rawX)));
            const frameY = Math.max(0, Math.min(height - 1, Math.floor(rawY)));
            const frameRight = Math.max(frameX + 1, Math.min(width, Math.ceil(rawRight)));
            const frameBottom = Math.max(frameY + 1, Math.min(height, Math.ceil(rawBottom)));
            const frameWidth = Math.max(1, frameRight - frameX);
            const frameHeight = Math.max(1, frameBottom - frameY);
            const frame = new PIXI.Rectangle(frameX, frameY, frameWidth, frameHeight);
            const oldTexture = this.buildingCutawayCompositeTexture;
            this.buildingCutawayCompositeTexture = new PIXI.Texture(resources.texture.baseTexture, frame);
            if (oldTexture && oldTexture !== this.buildingCutawayCompositeTexture && typeof oldTexture.destroy === "function") {
                oldTexture.destroy(false);
            }
            const sprite = resources.sprite;
            sprite.texture = this.buildingCutawayCompositeTexture;
            const anchorX = Math.max(0, Math.min(1, ((Number(anchor.centerX) || 0) - frameX) / frameWidth));
            const anchorY = Math.max(0, Math.min(1, ((Number(anchor.bottomY) || 0) - frameY) / frameHeight));
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(anchorX, anchorY);
            }
            sprite.width = frameWidth;
            sprite.height = frameHeight;
            return {
                texture: this.buildingCutawayCompositeTexture,
                width: frameWidth,
                height: frameHeight,
                anchorX,
                anchorY,
                bounds: {
                    x: frameX,
                    y: frameY,
                    width: frameWidth,
                    height: frameHeight
                }
            };
        }

        ensureBuildingCutawayCompositeProxy(sprite) {
            if (!sprite) return null;
            if (!this.buildingCutawayCompositeProxy) {
                this.buildingCutawayCompositeProxy = {
                    type: "buildingCutawayComposite",
                    category: "",
                    rotationAxis: "billboard",
                    placementRotation: 0,
                    pixiSprite: sprite,
                    x: 0,
                    y: 0,
                    z: 0,
                    width: 1,
                    height: 1,
                    depthBillboardUseVerticalAnchorY: true,
                    gone: false,
                    vanishing: false,
                    visible: true,
                    tint: 0xFFFFFF,
                    _renderLayerBaseZ: 0,
                    _renderDepthBias: 0,
                    _depthBillboardMesh: null,
                    _depthBillboardWorldPositions: null,
                    _depthBillboardLastSignature: "",
                    _depthBillboardLastUvSignature: "",
                    _depthBillboardMeshMode: ""
                };
                const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                    ? global.StaticObject.prototype
                    : null;
                if (staticProto) {
                    if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                        this.buildingCutawayCompositeProxy.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                    }
                    if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                        this.buildingCutawayCompositeProxy.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                    }
                    if (typeof staticProto.updateDepthBillboardMesh === "function") {
                        this.buildingCutawayCompositeProxy.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
                    }
                }
            }
            this.buildingCutawayCompositeProxy.pixiSprite = sprite;
            return this.buildingCutawayCompositeProxy;
        }

        getBuildingCutawayCompositeResourcesForCache(cache) {
            if (!cache || !cache.texture) return null;
            let sprite = this.buildingCutawayCompositeSprite || null;
            if (!sprite) {
                if (typeof PIXI === "undefined" || !PIXI.Sprite) return null;
                sprite = new PIXI.Sprite(cache.texture);
                sprite.name = "buildingCutawayCompositeSprite";
                sprite.interactive = false;
                this.buildingCutawayCompositeSprite = sprite;
            }
            if (sprite.texture !== cache.texture) sprite.texture = cache.texture;
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(
                    Number.isFinite(cache.anchorX) ? cache.anchorX : 0.5,
                    Number.isFinite(cache.anchorY) ? cache.anchorY : 1
                );
            }
            return {
                texture: cache.texture,
                sprite,
                width: Math.max(1, Number(cache.textureWidth) || Number(sprite.width) || 1),
                height: Math.max(1, Number(cache.textureHeight) || Number(sprite.height) || 1),
                localBounds: cache.bounds || null
            };
        }

        canPresentBuildingCutawayCompositeDuringMissingTrigger(cutawayState = null) {
            const cache = this.buildingCutawayCompositeCache || null;
            if (!cache || cache.active !== true || !cache.texture || !cache.dataTexture) return false;
            const graceFrames = Number(this.buildingCutawayCompositeMissingTriggerFrames) || 0;
            if (graceFrames >= 2) return false;
            const state = cutawayState || {};
            const stateLayer = this.getLayerIndexFromValue(state.wizardLayer, NaN);
            const cacheLayer = this.getLayerIndexFromValue(cache.wizardLayer, NaN);
            if (!Number.isFinite(stateLayer) || !Number.isFinite(cacheLayer) || stateLayer !== cacheLayer) return false;
            const dx = Math.abs((Number(state.wizardX) || 0) - (Number(cache.wizardX) || 0));
            const dy = Math.abs((Number(state.wizardY) || 0) - (Number(cache.wizardY) || 0));
            return dx <= 0.001 && dy <= 0.001;
        }

        hideBuildingCutawayCompositeBillboard() {
            if (this.buildingCutawayCompositeSprite) {
                this.buildingCutawayCompositeSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.buildingCutawayCompositeSprite, "renderable")) {
                    this.buildingCutawayCompositeSprite.renderable = false;
                }
            }
            const proxy = this.buildingCutawayCompositeProxy || null;
            const mesh = proxy && proxy._depthBillboardMesh ? proxy._depthBillboardMesh : null;
            if (mesh) {
                mesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                    mesh.renderable = false;
                }
            }
        }

        releaseRetiredBuildingCutawayCompositeRenderTextures(activeCache = null) {
            const release = (fieldName, activeTexture) => {
                const texture = this[fieldName] || null;
                if (!texture || texture === activeTexture) return;
                if (typeof texture.destroy === "function") texture.destroy(true);
                this[fieldName] = null;
            };
            release(
                "buildingCutawayCompositeRetiredRenderTexture",
                activeCache && activeCache.texture ? activeCache.texture : null
            );
            release(
                "buildingCutawayCompositeRetiredDataRenderTexture",
                activeCache && activeCache.dataTexture ? activeCache.dataTexture : null
            );
        }

        failBuildingCutawayComposite(reason, details = null) {
            const detailText = details ? ` ${JSON.stringify(details)}` : "";
            const err = new Error(`[building cutaway composite] ${reason}${detailText}`);
            err.reason = reason;
            err.details = details;
            throw err;
        }

        ensureBuildingCutawayCompositeBillboardState() {
            if (this.buildingCutawayCompositeBillboardState) return this.buildingCutawayCompositeBillboardState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.culling = false;
            this.buildingCutawayCompositeBillboardState = state;
            return state;
        }

        ensurePrototypeBuildingExteriorDepthState() {
            if (this.prototypeBuildingExteriorDepthState) return this.prototypeBuildingExteriorDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = true;
            state.blend = true;
            state.culling = false;
            this.prototypeBuildingExteriorDepthState = state;
            return state;
        }

        ensurePrototypeBuildingExteriorTransparentDepthState() {
            if (this.prototypeBuildingExteriorTransparentDepthState) return this.prototypeBuildingExteriorTransparentDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.culling = false;
            this.prototypeBuildingExteriorTransparentDepthState = state;
            return state;
        }

        renderBuildingCutawayCompositeBillboard(ctx, resources, cache, container = null) {
            if (!resources || !resources.sprite || !cache || !cache.anchor) return null;
            const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                ? global.StaticObject.prototype
                : null;
            if (!staticProto || typeof staticProto.updateDepthBillboardMesh !== "function") return null;
            const sprite = resources.sprite;
            if (cache.texture && sprite.texture !== cache.texture) {
                sprite.texture = cache.texture;
            }
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(
                    Number.isFinite(cache.anchorX) ? cache.anchorX : 0.5,
                    Number.isFinite(cache.anchorY) ? cache.anchorY : 1
                );
            }
            const proxy = this.ensureBuildingCutawayCompositeProxy(sprite);
            if (!proxy || typeof proxy.updateDepthBillboardMesh !== "function") return null;
            const viewScale = Math.max(1e-6, Math.abs(Number(this.camera && this.camera.viewscale) || 1));
            const xyRatio = Math.max(1e-6, Math.abs(Number(this.camera && this.camera.xyratio) || 1));
            const textureWidth = Math.max(1, Number(cache.textureWidth) || Number(sprite.width) || Number(resources.width) || 1);
            const textureHeight = Math.max(1, Number(cache.textureHeight) || Number(sprite.height) || Number(resources.height) || 1);
            const projectedBounds = cache.bounds && cache.bounds.projectionSpace ? cache.bounds : null;
            const worldWidth = projectedBounds && Number.isFinite(projectedBounds.worldWidth)
                ? Math.max(0.01, Number(projectedBounds.worldWidth))
                : (textureWidth / viewScale);
            const worldHeight = projectedBounds && Number.isFinite(projectedBounds.worldHeight)
                ? Math.max(0.01, Number(projectedBounds.worldHeight))
                : (textureHeight / (viewScale * xyRatio));
            const presentationWidthPx = worldWidth * viewScale;
            const presentationHeightPx = worldHeight * viewScale * xyRatio;
            const compositeAlpha = Number.isFinite(cache.alpha)
                ? Math.max(0, Math.min(1, Number(cache.alpha)))
                : BUILDING_CUTAWAY_GHOST_ALPHA;
            sprite.width = presentationWidthPx;
            sprite.height = presentationHeightPx;
            sprite.alpha = compositeAlpha;
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
            const anchor = cache.anchor;
            proxy.x = Number.isFinite(anchor.worldX) ? Number(anchor.worldX) : 0;
            proxy.y = Number.isFinite(anchor.worldY) ? Number(anchor.worldY) : 0;
            proxy.z = Number.isFinite(anchor.worldZ) ? Number(anchor.worldZ) : 0;
            proxy.width = worldWidth;
            proxy.height = worldHeight;
            proxy.map = (ctx && ctx.map) || (this.camera && this.camera.map) || global.map || null;
            proxy._renderLayerBaseZ = 0;
            proxy._renderDepthBias = -0.002;
            const mesh = proxy.updateDepthBillboardMesh(ctx, this.camera, {
                alphaCutoff: 0.01,
                mazeMode: false,
                useVerticalAnchorY: true
            });
            if (!mesh) return null;
            this.flipBuildingCutawayCompositeBillboardUvs(mesh, sprite.texture);
            const billboardState = this.ensureBuildingCutawayCompositeBillboardState();
            if (billboardState) mesh.state = billboardState;
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                mesh.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            if (mesh.shader && mesh.shader.uniforms && mesh.shader.uniforms.uTint) {
                const uniforms = mesh.shader.uniforms;
                const tint = Number.isFinite(sprite.tint) ? Math.max(0, Math.min(0xffffff, Math.floor(sprite.tint))) : 0xffffff;
                const useDataAlpha = !!(cache.dataTexture && cache.activeInterior === true);
                const baseAlpha = useDataAlpha ? 1 : compositeAlpha;
                const rgbAlpha = useDataAlpha ? 1 : compositeAlpha;
                uniforms.uTint[0] = (((tint >> 16) & 0xff) / 255) * rgbAlpha;
                uniforms.uTint[1] = (((tint >> 8) & 0xff) / 255) * rgbAlpha;
                uniforms.uTint[2] = ((tint & 0xff) / 255) * rgbAlpha;
                uniforms.uTint[3] = baseAlpha;
                if (Object.prototype.hasOwnProperty.call(uniforms, "uBuildingCutawayUseDataAlpha")) {
                    uniforms.uBuildingCutawayUseDataAlpha = useDataAlpha ? 1 : 0;
                    uniforms.uBuildingCutawayDataSampler = useDataAlpha ? cache.dataTexture : (PIXI.Texture ? PIXI.Texture.WHITE : null);
                    uniforms.uBuildingCutawayCurrentFloorZ = Number.isFinite(cache.currentFloorZ)
                        ? Number(cache.currentFloorZ)
                        : 0;
                    uniforms.uBuildingCutawayUpperAlpha = compositeAlpha;
                    if (uniforms.uBuildingCutawayDataZRange && uniforms.uBuildingCutawayDataZRange.length >= 2) {
                        uniforms.uBuildingCutawayDataZRange[0] = BUILDING_CUTAWAY_DATA_MIN_Z;
                        uniforms.uBuildingCutawayDataZRange[1] = 1 / Math.max(1e-6, BUILDING_CUTAWAY_DATA_MAX_Z - BUILDING_CUTAWAY_DATA_MIN_Z);
                    }
                }
            }
            const targetContainer = container || (this.layers && (this.layers.objects3d || this.layers.depthObjects || this.layers.root));
            if (targetContainer) {
                if (Object.prototype.hasOwnProperty.call(targetContainer, "sortableChildren")) {
                    targetContainer.sortableChildren = true;
                }
                mesh.zIndex = 2147483647;
                targetContainer.addChild(mesh);
                if (Object.prototype.hasOwnProperty.call(targetContainer, "sortDirty")) {
                    targetContainer.sortDirty = true;
                }
            }
            mesh.visible = true;
            mesh.alpha = 1;
            if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) mesh.renderable = true;
            return mesh;
        }

        flipBuildingCutawayCompositeBillboardUvs(mesh, texture) {
            if (!mesh || !mesh.geometry || !texture || !texture.baseTexture) return false;
            const uvBuffer = mesh.geometry.getBuffer("aUvs");
            if (!uvBuffer) return false;
            const baseTexture = texture.baseTexture;
            const baseW = Number(baseTexture.realWidth || baseTexture.width || 0);
            const baseH = Number(baseTexture.realHeight || baseTexture.height || 0);
            if (!(baseW > 0) || !(baseH > 0)) return false;
            const frame = texture.frame || new PIXI.Rectangle(0, 0, baseW, baseH);
            const u0 = Number(frame.x) / baseW;
            const v0 = Number(frame.y) / baseH;
            const u1 = (Number(frame.x) + Number(frame.width)) / baseW;
            const v1 = (Number(frame.y) + Number(frame.height)) / baseH;
            uvBuffer.data = new Float32Array([
                u0, v0,
                u1, v0,
                u1, v1,
                u0, v1
            ]);
            uvBuffer.update();
            return true;
        }

        getBuildingInteriorCompositePreserveDisplayObjects(plan) {
            const out = new Set();
            if (!plan || !(plan.items instanceof Set)) return out;
            plan.items.forEach(item => {
                const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                for (let i = 0; i < displayObjects.length; i++) {
                    if (displayObjects[i]) out.add(displayObjects[i]);
                }
            });
            return out;
        }

        getBuildingInteriorWallDepthOptions(ctx, wall, topFaceOnly) {
            const camera = this.camera || {};
            const wallLayerAlpha = Number.isFinite(wall && wall._renderLayerAlpha)
                ? Math.max(0, Math.min(1, Number(wall._renderLayerAlpha)))
                : 1;
            return {
                camera: this.camera,
                app: ctx && ctx.app,
                viewscale: Number.isFinite(camera.viewscale) ? Number(camera.viewscale) : 1,
                xyratio: Number.isFinite(camera.xyratio) ? Number(camera.xyratio) : 1,
                worldToScreenFn: (pt) => this.camera.worldToScreen(
                    Number(pt && pt.x) || 0,
                    Number(pt && pt.y) || 0,
                    0
                ),
                mazeMode: false,
                topFaceOnly: !!topFaceOnly,
                bottomFaceOnly: false,
                clipToLosVisibleSpan: false,
                skipMazeRevealAnimation: true,
                player: (ctx && ctx.wizard) || global.wizard || null,
                tint: wall && wall.pixiSprite && Number.isFinite(wall.pixiSprite.tint)
                    ? wall.pixiSprite.tint
                    : 0xFFFFFF,
                alpha: this.getScriptDisplayAlpha(wall) * wallLayerAlpha,
                brightness: Number.isFinite(wall && wall.brightness)
                    ? Number(wall.brightness)
                    : 0
            };
        }

        applyBuildingCutawayCompositeFullWallCaptureGeometry(ctx, foregroundPlan) {
            const wallTopFaceOnly = foregroundPlan && foregroundPlan.wallTopFaceOnly;
            if (!(wallTopFaceOnly instanceof Map) || wallTopFaceOnly.size === 0) return () => {};
            const restored = [];
            wallTopFaceOnly.forEach((topFaceOnly, wall) => {
                if (topFaceOnly !== true) return;
                if (!wall || wall.type !== "wallSection" || typeof wall.getDepthMeshDisplayObject !== "function") return;
                const fullMesh = wall.getDepthMeshDisplayObject(
                    this.getBuildingInteriorWallDepthOptions(ctx, wall, false)
                );
                if (!fullMesh) {
                    this.failBuildingCutawayComposite("active wall full capture geometry unavailable", {
                        wallId: wall.id || wall.wallId || wall.scriptingName || null
                    });
                }
                restored.push(wall);
            });
            return () => {
                for (let i = restored.length - 1; i >= 0; i--) {
                    const wall = restored[i];
                    if (!wall || typeof wall.getDepthMeshDisplayObject !== "function") continue;
                    const restoredMesh = wall.getDepthMeshDisplayObject(
                        this.getBuildingInteriorWallDepthOptions(ctx, wall, true)
                    );
                    if (!restoredMesh) {
                        this.failBuildingCutawayComposite("active wall top-face restore failed", {
                            wallId: wall.id || wall.wallId || wall.scriptingName || null
                        });
                    }
                }
            };
        }

        renderBuildingCutawayComposites(ctx, cutawayState, container = null, maskDisplayObjects = null, foregroundPlan = null) {
            const movePerfEnabled = !!(typeof globalThis !== "undefined" && globalThis.__moveObjectPerf);
            const compositeStartMs = movePerfEnabled ? performance.now() : 0;
            const triggers = this.getBuildingCutawayCompositeTriggers(cutawayState);
            if (triggers.length === 0) {
                const rawTriggers = Array.isArray(cutawayState && cutawayState.triggers)
                    ? cutawayState.triggers.filter(trigger => trigger && trigger.building)
                    : [];
                const onlyExcludedPrototypeTriggers = rawTriggers.length > 0;
                if (
                    !onlyExcludedPrototypeTriggers &&
                    this.canPresentBuildingCutawayCompositeDuringMissingTrigger(cutawayState)
                ) {
                    const staleCache = this.buildingCutawayCompositeCache;
                    const displayObjects = new Set(
                        Array.isArray(staleCache.originalDisplayObjects)
                            ? staleCache.originalDisplayObjects
                            : (Array.isArray(staleCache.displayObjects) ? staleCache.displayObjects : [])
                    );
                    const hiddenOriginals = this.hideBuildingCutawayCompositeOriginals(displayObjects);
                    const staleResources = this.getBuildingCutawayCompositeResourcesForCache(staleCache);
                    if (!staleResources) {
                        this.failBuildingCutawayComposite("missing-trigger stale billboard resources unavailable", {
                            staleSignature: staleCache.signature,
                            hasTexture: !!staleCache.texture,
                            hasSprite: !!this.buildingCutawayCompositeSprite
                        });
                    }
                    const billboardMesh = this.renderBuildingCutawayCompositeBillboard(
                        ctx,
                        staleResources,
                        staleCache,
                        container
                    );
                    if (!billboardMesh) {
                        this.failBuildingCutawayComposite("missing-trigger stale billboard mesh creation failed", {
                            staleSignature: staleCache.signature,
                            hasTexture: !!staleCache.texture
                        });
                    }
                    this.buildingCutawayCompositeMissingTriggerFrames = (Number(this.buildingCutawayCompositeMissingTriggerFrames) || 0) + 1;
                    this.setFrameMetric("objects3dBuildingCompositeActive", 1);
                    this.setFrameMetric("objects3dBuildingCompositeObjects", displayObjects.size);
                    this.setFrameMetric("objects3dBuildingCompositeOriginalsHidden", hiddenOriginals);
                    this.setFrameMetric("objects3dBuildingCompositeCacheHits", 1);
                    this.setFrameMetric("objects3dBuildingCompositeCacheMisses", 1);
                    this.setFrameMetric("objects3dBuildingCompositePendingTextures", 0);
                    if (movePerfEnabled) {
                        this.recordMoveObjectPerf("rendering.cutawayComposite.staleMissingTrigger", {
                            displayObjects: displayObjects.size
                        }, performance.now() - compositeStartMs);
                    }
                    return billboardMesh;
                }
                this.hideBuildingCutawayCompositeBillboard();
                this.buildingCutawayCompositeCache = null;
                this.buildingCutawayCompositeMissingTriggerFrames = 0;
                this.setFrameMetric("objects3dBuildingCompositeActive", 0);
                this.setFrameMetric("objects3dBuildingCompositeObjects", 0);
                this.setFrameMetric("objects3dBuildingCompositeOriginalsHidden", 0);
                this.setFrameMetric("objects3dBuildingCompositeCacheHits", 0);
                this.setFrameMetric("objects3dBuildingCompositeCacheMisses", 0);
                this.setFrameMetric("objects3dBuildingCompositePendingTextures", 0);
                if (movePerfEnabled) {
                    this.recordMoveObjectPerf("rendering.cutawayComposite.none", {
                        rawTriggers: rawTriggers.length
                    }, performance.now() - compositeStartMs);
                }
                return null;
            }
            this.buildingCutawayCompositeMissingTriggerFrames = 0;
            const signature = this.getBuildingCutawayCompositeSignature(ctx, cutawayState);
            if (!signature) {
                this.failBuildingCutawayComposite("missing cache signature", {
                    triggers: triggers.length
                });
            }
            const cacheUsable = !!(
                this.buildingCutawayCompositeCache &&
                this.buildingCutawayCompositeCache.active === true &&
                this.buildingCutawayCompositeCache.signature === signature
            );
            const presentationKey = this.getBuildingCutawayCompositePresentationKey(ctx, cutawayState);
            const staleCachePresentable = !!(
                !cacheUsable &&
                this.buildingCutawayCompositeCache &&
                this.buildingCutawayCompositeCache.active === true &&
                this.buildingCutawayCompositeCache.texture &&
                this.buildingCutawayCompositeCache.dataTexture &&
                presentationKey &&
                this.buildingCutawayCompositeCache.presentationKey === presentationKey
            );
            const displayObjects = this.getBuildingCutawayCompositeDisplayObjects(cutawayState);
            const originalDisplayObjects = this.getBuildingCutawayCompositeOriginalDisplayObjects(cutawayState);
            if (movePerfEnabled) {
                this.recordMoveObjectPerf("rendering.cutawayComposite.frame", {
                    triggers: triggers.length,
                    cacheUsable,
                    staleCachePresentable,
                    displayObjects: displayObjects.size,
                    originalDisplayObjects: originalDisplayObjects.size
                });
            }
            let captureAnchor = cacheUsable
                ? (this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.anchor)
                : this.getBuildingCutawayCompositeAnchor(cutawayState);
            const captureBounds = cacheUsable
                ? (this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.bounds)
                : this.getBuildingCutawayCompositeCaptureBounds(cutawayState, captureAnchor);
            const resourceWidth = cacheUsable
                ? (this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.textureWidth)
                : (captureBounds && captureBounds.width);
            const resourceHeight = cacheUsable
                ? (this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.textureHeight)
                : (captureBounds && captureBounds.height);
            if (!captureAnchor) {
                this.failBuildingCutawayComposite("missing capture anchor", {
                    cacheUsable,
                    signature
                });
            }
            if (!captureBounds) {
                this.failBuildingCutawayComposite("missing capture bounds", {
                    cacheUsable,
                    signature,
                    anchor: captureAnchor
                });
            }
            if (!cacheUsable && displayObjects.size === 0) {
                this.failBuildingCutawayComposite("no display objects selected for capture", {
                    signature,
                    frameId: Number(this._layerCutawayFrameId) || 0,
                    cacheUsable,
                    triggers: triggers.map(trigger => ({
                        buildingId: trigger && trigger.buildingId,
                        activeInteriorRegionId: trigger && trigger.activeInteriorRegionId,
                        level: trigger && trigger.level,
                        visibleThroughLevel: trigger && trigger.visibleThroughLevel,
                        renderItems: trigger && trigger.renderCache && Array.isArray(trigger.renderCache.renderItems)
                            ? trigger.renderCache.renderItems.length
                            : null
                    }))
                });
            }
            const compositeAlpha = this.getBuildingCutawayCompositeAlpha(cutawayState);
            const activeInterior = triggers.some(trigger => !!(trigger && trigger.activeInteriorRegion));
            const currentFloorZ = Number.isFinite(cutawayState && cutawayState.wizardBaseZ)
                ? Number(cutawayState.wizardBaseZ)
                : 0;
            if (!cacheUsable) {
                const wallSelection = this.getBuildingCutawayCompositeWallSelectionDiagnostics(cutawayState, displayObjects);
                if (wallSelection.expectedWalls > 0 && wallSelection.selectedWalls === 0) {
                    this.failBuildingCutawayComposite("no wall display objects selected for capture", {
                        signature,
                        frameId: Number(this._layerCutawayFrameId) || 0,
                        expectedWalls: wallSelection.expectedWalls,
                        selectedWalls: wallSelection.selectedWalls
                    });
                }
                const pendingTextures = this.collectBuildingCutawayCompositePendingTextures(displayObjects);
                if (pendingTextures.count > 0) {
                    this.watchBuildingCutawayCompositePendingTextures(pendingTextures);
                    if (typeof globalThis !== "undefined") {
                        globalThis.renderingLastBuildingCutawayCompositePendingTextures = pendingTextures.samples.map(sample => ({
                            label: sample.label || "",
                            ownerName: sample.ownerName || "",
                            ownerType: sample.ownerType || "",
                            baseTextureValid: !!sample.baseTextureValid,
                            frameWidth: sample.frameWidth,
                            frameHeight: sample.frameHeight
                        }));
                    }
                    if (staleCachePresentable) {
                        const staleCache = this.buildingCutawayCompositeCache;
                        staleCache.alpha = compositeAlpha;
                        staleCache.activeInterior = activeInterior;
                        staleCache.currentFloorZ = currentFloorZ;
                        staleCache.wizardLayer = cutawayState && cutawayState.wizardLayer;
                        staleCache.wizardX = cutawayState && cutawayState.wizardX;
                        staleCache.wizardY = cutawayState && cutawayState.wizardY;
                        staleCache.displayObjects = Array.from(displayObjects);
                        staleCache.originalDisplayObjects = Array.from(originalDisplayObjects);
                        const preserveDisplayObjects = this.getBuildingInteriorCompositePreserveDisplayObjects(foregroundPlan);
                        const hiddenOriginals = this.hideBuildingCutawayCompositeOriginals(originalDisplayObjects, preserveDisplayObjects);
                        const staleResources = this.getBuildingCutawayCompositeResourcesForCache(staleCache);
                        if (!staleResources) {
                            this.failBuildingCutawayComposite("stale billboard resources unavailable", {
                                signature,
                                staleSignature: staleCache.signature,
                                hasTexture: !!staleCache.texture,
                                hasSprite: !!this.buildingCutawayCompositeSprite
                            });
                        }
                        const billboardMesh = this.renderBuildingCutawayCompositeBillboard(
                            ctx,
                            staleResources,
                            staleCache,
                            container
                        );
                        if (!billboardMesh) {
                            this.failBuildingCutawayComposite("stale billboard mesh creation failed", {
                                signature,
                                staleSignature: staleCache.signature,
                                hasTexture: !!staleCache.texture
                            });
                        }
                        this.setFrameMetric("objects3dBuildingCompositeActive", 1);
                        this.setFrameMetric("objects3dBuildingCompositeObjects", displayObjects.size);
                        this.setFrameMetric("objects3dBuildingCompositeOriginalsHidden", hiddenOriginals);
                        this.setFrameMetric("objects3dBuildingCompositeCacheHits", 1);
                        this.setFrameMetric("objects3dBuildingCompositeCacheMisses", 1);
                        this.setFrameMetric("objects3dBuildingCompositePendingTextures", pendingTextures.count);
                        return billboardMesh;
                    }
                    this.hideBuildingCutawayCompositeBillboard();
                    this.buildingCutawayCompositeCache = null;
                    this.setFrameMetric("objects3dBuildingCompositeActive", 0);
                    this.setFrameMetric("objects3dBuildingCompositeObjects", displayObjects.size);
                    this.setFrameMetric("objects3dBuildingCompositeOriginalsHidden", 0);
                    this.setFrameMetric("objects3dBuildingCompositeCacheHits", 0);
                    this.setFrameMetric("objects3dBuildingCompositeCacheMisses", 1);
                    this.setFrameMetric("objects3dBuildingCompositePendingTextures", pendingTextures.count);
                    return null;
                }
            }
            const resources = this.ensureBuildingCutawayCompositeRenderTexture(ctx, resourceWidth, resourceHeight, captureBounds);
            if (!resources) {
                this.failBuildingCutawayComposite("render texture unavailable", {
                    resourceWidth,
                    resourceHeight,
                    hasPixi: typeof PIXI !== "undefined",
                    hasApp: !!((ctx && ctx.app) || (typeof app !== "undefined" && app) || global.app)
                });
            }
            if (!(this.layers && this.layers.root)) {
                this.failBuildingCutawayComposite("missing render root", {
                    hasLayers: !!this.layers
                });
            }
            if (resources.sprite) {
                resources.sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(resources.sprite, "renderable")) resources.sprite.renderable = false;
            }
            let rendered = cacheUsable;
            let dataRendered = cacheUsable && !!(this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.dataTexture);
            if (!cacheUsable) {
                const captureStartMs = movePerfEnabled ? performance.now() : 0;
                const restoreWallCaptureGeometry = this.applyBuildingCutawayCompositeFullWallCaptureGeometry(ctx, foregroundPlan);
                const restoreTree = this.isolateBuildingCutawayCompositeDisplayTree(displayObjects);
                const restoreCaptureState = this.applyBuildingCutawayCompositeLocalCaptureState(
                    displayObjects,
                    captureBounds,
                    resources.width,
                    resources.height
                );
                try {
                    const cleared = this.clearBuildingCutawayCompositeRenderTarget(resources);
                    resources.renderer.render(this.layers.root, resources.texture, !cleared);
                    const dataResources = this.ensureBuildingCutawayCompositeDataRenderTexture(ctx, resources.width, resources.height);
                    if (!dataResources || !dataResources.texture) {
                        this.failBuildingCutawayComposite("data render texture unavailable", {
                            width: resources.width,
                            height: resources.height
                        });
                    }
                    const restoreDataPassState = this.applyBuildingCutawayCompositeDataPassState(displayObjects);
                    try {
                        const dataCleared = this.clearBuildingCutawayCompositeRenderTarget(dataResources);
                        dataResources.renderer.render(this.layers.root, dataResources.texture, !dataCleared);
                        dataRendered = true;
                    } finally {
                        restoreDataPassState();
                    }
                    rendered = true;
                } catch (err) {
                    this.failBuildingCutawayComposite("render-to-texture failed", {
                        message: err && err.message ? err.message : String(err),
                        signature,
                        displayObjects: displayObjects.size,
                        textureWidth: resources.width,
                        textureHeight: resources.height
                    });
                } finally {
                    restoreCaptureState();
                    restoreTree();
                    restoreWallCaptureGeometry();
                    if (movePerfEnabled) {
                        this.recordMoveObjectPerf("rendering.cutawayComposite.capture", {
                            displayObjects: displayObjects.size,
                            resourceWidth: resources.width,
                            resourceHeight: resources.height
                        }, performance.now() - captureStartMs);
                    }
                }
            }
            if (!rendered) {
                this.failBuildingCutawayComposite("render-to-texture did not render", {
                    signature,
                    displayObjects: displayObjects.size
                });
            }
            if (!dataRendered) {
                this.failBuildingCutawayComposite("data render-to-texture did not render", {
                    signature,
                    displayObjects: displayObjects.size
                });
            }
            const preserveDisplayObjects = this.getBuildingInteriorCompositePreserveDisplayObjects(foregroundPlan);
            const hiddenOriginals = this.hideBuildingCutawayCompositeOriginals(originalDisplayObjects, preserveDisplayObjects);
            const sprite = resources.sprite;
            sprite.alpha = compositeAlpha;
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
            if (!cacheUsable) {
                const billboardTexture = this.updateBuildingCutawayCompositeBillboardTexture(resources, captureAnchor, captureBounds);
                if (!billboardTexture || !billboardTexture.texture) {
                    this.failBuildingCutawayComposite("billboard texture update failed", {
                        signature,
                        captureAnchor,
                        captureBounds,
                        resourceWidth: resources.width,
                        resourceHeight: resources.height
                    });
                }
                this.buildingCutawayCompositeCache = {
                    active: true,
                    signature,
                    presentationKey,
                    texture: billboardTexture.texture,
                    dataTexture: this.buildingCutawayCompositeDataRenderTexture,
                    anchor: captureAnchor,
                    bounds: billboardTexture.bounds,
                    anchorX: billboardTexture.anchorX,
                    anchorY: billboardTexture.anchorY,
                    textureWidth: billboardTexture.width,
                    textureHeight: billboardTexture.height,
                    alpha: compositeAlpha,
                    activeInterior,
                    currentFloorZ,
                    wizardLayer: cutawayState && cutawayState.wizardLayer,
                    wizardX: cutawayState && cutawayState.wizardX,
                    wizardY: cutawayState && cutawayState.wizardY,
                    displayObjects: Array.from(displayObjects),
                    originalDisplayObjects: Array.from(originalDisplayObjects)
                };
                this.releaseRetiredBuildingCutawayCompositeRenderTextures(this.buildingCutawayCompositeCache);
            } else if (sprite.anchor && this.buildingCutawayCompositeCache) {
                this.buildingCutawayCompositeCache.alpha = compositeAlpha;
                this.buildingCutawayCompositeCache.activeInterior = activeInterior;
                this.buildingCutawayCompositeCache.currentFloorZ = currentFloorZ;
                this.buildingCutawayCompositeCache.wizardLayer = cutawayState && cutawayState.wizardLayer;
                this.buildingCutawayCompositeCache.wizardX = cutawayState && cutawayState.wizardX;
                this.buildingCutawayCompositeCache.wizardY = cutawayState && cutawayState.wizardY;
                this.buildingCutawayCompositeCache.displayObjects = Array.from(displayObjects);
                this.buildingCutawayCompositeCache.originalDisplayObjects = Array.from(originalDisplayObjects);
                if (this.buildingCutawayCompositeCache.texture) {
                    sprite.texture = this.buildingCutawayCompositeCache.texture;
                }
                sprite.anchor.set(
                    Number.isFinite(this.buildingCutawayCompositeCache.anchorX) ? this.buildingCutawayCompositeCache.anchorX : 0.5,
                    Number.isFinite(this.buildingCutawayCompositeCache.anchorY) ? this.buildingCutawayCompositeCache.anchorY : 1
                );
            }
            const billboardMesh = this.renderBuildingCutawayCompositeBillboard(
                ctx,
                resources,
                this.buildingCutawayCompositeCache,
                container
            );
            if (!billboardMesh) {
                this.failBuildingCutawayComposite("billboard mesh creation failed", {
                    signature,
                    cacheUsable,
                    hasStaticObject: typeof global.StaticObject === "function",
                    hasTexture: !!(this.buildingCutawayCompositeCache && this.buildingCutawayCompositeCache.texture)
                });
            }
            this.setFrameMetric("objects3dBuildingCompositeActive", 1);
            this.setFrameMetric("objects3dBuildingCompositeObjects", displayObjects.size);
            this.setFrameMetric("objects3dBuildingCompositeOriginalsHidden", hiddenOriginals);
            this.setFrameMetric("objects3dBuildingCompositeCacheHits", cacheUsable ? 1 : 0);
            this.setFrameMetric("objects3dBuildingCompositeCacheMisses", cacheUsable ? 0 : 1);
            this.setFrameMetric("objects3dBuildingCompositePendingTextures", 0);
            if (movePerfEnabled) {
                this.recordMoveObjectPerf("rendering.cutawayComposite.total", {
                    cacheUsable,
                    displayObjects: displayObjects.size,
                    hiddenOriginals
                }, performance.now() - compositeStartMs);
            }
            return billboardMesh;
        }

        getBuildingInteriorForegroundState() {
            if (this.buildingInteriorForegroundState) return this.buildingInteriorForegroundState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = true;
            state.blend = true;
            state.culling = false;
            this.buildingInteriorForegroundState = state;
            return state;
        }

        getBuildingInteriorTransparentWindowDepthState() {
            if (this.buildingInteriorTransparentWindowDepthState) return this.buildingInteriorTransparentWindowDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.culling = false;
            this.buildingInteriorTransparentWindowDepthState = state;
            return state;
        }

        isTransparentFrontMountedWindowDepthItem(item, forceMountedWallSide, alpha) {
            if (!item) return false;
            const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
            const type = (typeof item.type === "string") ? item.type.trim().toLowerCase() : "";
            if (category !== "windows" && type !== "window") return false;
            if (forceMountedWallSide !== "front") return false;
            const normalizedAlpha = Number(alpha);
            return Number.isFinite(normalizedAlpha) && normalizedAlpha < 0.999;
        }

        applyMountedWindowDepthState(mesh, item, forceMountedWallSide, alpha) {
            if (!mesh) return false;
            const shouldUseTransparentState = this.isTransparentFrontMountedWindowDepthItem(item, forceMountedWallSide, alpha);
            if (!shouldUseTransparentState) {
                if (mesh._buildingInteriorDefaultDepthState !== undefined) {
                    mesh.state = mesh._buildingInteriorDefaultDepthState;
                }
                return false;
            }
            const state = this.getBuildingInteriorTransparentWindowDepthState();
            if (!state) return false;
            if (mesh._buildingInteriorDefaultDepthState === undefined) {
                mesh._buildingInteriorDefaultDepthState = mesh.state || null;
            }
            mesh.state = state;
            return true;
        }

        getBuildingInteriorView() {
            if (this.buildingInteriorView) return this.buildingInteriorView;
            const InteriorViewCtor = global.RenderingBuildingInteriorView;
            if (typeof InteriorViewCtor !== "function") {
                throw new Error("RenderingBuildingInteriorView is required before Rendering.js.");
            }
            this.buildingInteriorView = new InteriorViewCtor(this);
            return this.buildingInteriorView;
        }

        clearBuildingInteriorForegroundPromotions() {
            const list = Array.isArray(this._buildingInteriorForegroundPromotions)
                ? this._buildingInteriorForegroundPromotions
                : [];
            for (let i = list.length - 1; i >= 0; i--) {
                const entry = list[i];
                const obj = entry && entry.displayObj;
                if (!obj) continue;
                if (obj.destroyed === true || obj._destroyed === true || obj.transform === null) {
                    continue;
                }
                const originalParent = entry.parent || null;
                if (entry.addedToPromotionContainer && !originalParent) {
                    if (obj.parent && typeof obj.parent.removeChild === "function") {
                        obj.parent.removeChild(obj);
                    }
                } else if (originalParent && originalParent.destroyed !== true && obj.parent !== originalParent) {
                    if (obj.parent && typeof obj.parent.removeChild === "function") {
                        obj.parent.removeChild(obj);
                    }
                    if (typeof originalParent.addChildAt === "function" && Number.isFinite(entry.parentIndex)) {
                        const childCount = Array.isArray(originalParent.children) ? originalParent.children.length : 0;
                        const index = Math.max(0, Math.min(childCount, Math.floor(entry.parentIndex)));
                        originalParent.addChildAt(obj, index);
                    } else if (typeof originalParent.addChild === "function") {
                        originalParent.addChild(obj);
                    }
                }
                if (Object.prototype.hasOwnProperty.call(entry, "state")) obj.state = entry.state;
                if (Object.prototype.hasOwnProperty.call(entry, "zIndex")) obj.zIndex = entry.zIndex;
                if (Object.prototype.hasOwnProperty.call(entry, "visible")) obj.visible = entry.visible;
                if (Object.prototype.hasOwnProperty.call(entry, "renderable") && Object.prototype.hasOwnProperty.call(obj, "renderable")) {
                    obj.renderable = entry.renderable;
                }
            }
            this._buildingInteriorForegroundPromotions = [];
        }

        promoteDisplayObjectForBuildingInterior(displayObj, container, currentDisplayObjects = null, promotedSet = null) {
            if (!displayObj || !container) return false;
            const set = promotedSet instanceof Set ? promotedSet : null;
            if (set && set.has(displayObj)) return false;
            if (set) set.add(displayObj);
            if (!Array.isArray(this._buildingInteriorForegroundPromotions)) {
                this._buildingInteriorForegroundPromotions = [];
            }
            const originalParent = displayObj.parent || null;
            const originalParentIndex = originalParent && typeof originalParent.getChildIndex === "function"
                ? originalParent.getChildIndex(displayObj)
                : null;
            this._buildingInteriorForegroundPromotions.push({
                displayObj,
                parent: originalParent,
                parentIndex: originalParentIndex,
                state: displayObj.state,
                zIndex: displayObj.zIndex,
                visible: displayObj.visible,
                renderable: displayObj.renderable,
                addedToPromotionContainer: !originalParent
            });
            const foregroundState = this.getBuildingInteriorForegroundState();
            if (foregroundState && Object.prototype.hasOwnProperty.call(displayObj, "state")) {
                displayObj.state = foregroundState;
            }
            displayObj.zIndex = BUILDING_INTERIOR_FOREGROUND_Z;
            if (originalParent && originalParent !== container && typeof originalParent.removeChild === "function") {
                originalParent.removeChild(displayObj);
            }
            if (displayObj.parent !== container && typeof container.addChild === "function") {
                container.addChild(displayObj);
            }
            const sortContainer = container;
            if (sortContainer && Object.prototype.hasOwnProperty.call(sortContainer, "sortableChildren")) {
                sortContainer.sortableChildren = true;
            }
            if (sortContainer && Object.prototype.hasOwnProperty.call(sortContainer, "sortDirty")) {
                sortContainer.sortDirty = true;
            }
            displayObj.visible = true;
            if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                displayObj.renderable = true;
            }
            if (currentDisplayObjects instanceof Set) currentDisplayObjects.add(displayObj);
            return true;
        }

        isBuildingInteriorPresentationActive(ctx = null) {
            return this.getBuildingInteriorView().isPresentationActive(ctx);
        }

        promoteInteriorPresentationDisplayObject(displayObj, ctx = null, zIndex = BUILDING_INTERIOR_FOREGROUND_Z) {
            return this.getBuildingInteriorView().promoteDisplayObject(displayObj, ctx, zIndex);
        }

        promoteInteriorPresentationForeground(ctx = null) {
            return this.getBuildingInteriorView().promoteForeground(ctx);
        }

        collectBuildingInteriorDisplayObjectsForItem(item) {
            const out = [];
            const push = (displayObj) => {
                if (!displayObj) return;
                if (out.indexOf(displayObj) >= 0) return;
                out.push(displayObj);
            };
            const isWallSection = !!(item && item.type === "wallSection");
            push(item && item._depthDisplayMesh);
            push(item && item._renderingDepthMesh);
            push(item && item._compositeUnderlayMesh);
            push(item && item._renderingDisplayObject);
            push(item && item._flowerBurnFragmentContainer);
            if (!isWallSection && out.length === 0) push(item && item.pixiSprite);
            push(item && item.fireSprite);
            if (!isWallSection) push(item && item._doorBottomFaceDebugGraphics);
            if (item && item.type === "roof") push(item.pixiMesh);
            return out;
        }

        promoteActiveBuildingInteriorRegions(ctx, cutawayState, container, currentDisplayObjects = null, foregroundPlan = null) {
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            if (triggers.length === 0 || !container) {
                this.setFrameMetric("objects3dBuildingInteriorPromoted", 0);
                return 0;
            }
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            const promotedSet = new Set();
            let promoted = 0;
            const promoteItem = (item) => {
                if (!item || item.gone || item.vanishing) return;
                const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                for (let d = 0; d < displayObjects.length; d++) {
                    const displayObj = displayObjects[d];
                    if (!displayObj || displayObj.visible === false || displayObj.renderable === false) continue;
                    if (this.promoteDisplayObjectForBuildingInterior(displayObj, container, currentDisplayObjects, promotedSet)) {
                        promoted += 1;
                    }
                    this.addPickRenderItem(item, displayObj, { forceInclude: true });
                }
            };
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const region = trigger && trigger.activeInteriorRegion;
                if (!region) continue;
                if (this.getPresentablePrototypeBuildingInteriorBitmapCache(ctx, trigger)) continue;
                const regions = this.getBuildingInteriorOverlayRegionsForTrigger(trigger);
                const visibleRegions = regions.length > 0 ? regions : [region];
                for (let regionIndex = 0; regionIndex < visibleRegions.length; regionIndex++) {
                    const visibleRegion = visibleRegions[regionIndex];
                    if (!visibleRegion) continue;
                    const entries = Array.isArray(visibleRegion.staticObjects) ? visibleRegion.staticObjects : [];
                    for (let j = 0; j < entries.length; j++) {
                        const entry = entries[j];
                        const item = entry && entry.item;
                        if (
                            item &&
                            item.type !== "wallSection" &&
                            !this.isBuildingCutawayDoorItem(item) &&
                            !this.isBuildingInteriorOverlaySurfaceItem(item) &&
                            !this.shouldRenderBuildingInteriorOverlayItem(item, visibleRegion, entry, ctx, wizardRef)
                        ) {
                            continue;
                        }
                        promoteItem(item);
                    }
                }
            }
            if (foregroundPlan && foregroundPlan.items instanceof Set) {
                foregroundPlan.items.forEach(item => promoteItem(item));
            }
            this.setFrameMetric("objects3dBuildingInteriorPromoted", promoted);
            return promoted;
        }

        promotePrototypeBuildingInteriorLiveRenderItems(ctx, cutawayState, container, currentDisplayObjects = null, renderItems = [], foregroundPlan = null) {
            const plannedItems = foregroundPlan &&
                foregroundPlan.items instanceof Set
                ? foregroundPlan.items
                : null;
            const fallbackCandidates = Array.isArray(renderItems) ? renderItems : [];
            const candidateCount = plannedItems ? plannedItems.size : fallbackCandidates.length;
            if (!container || candidateCount === 0) {
                this.setFrameMetric("objects3dPrototypeInteriorLivePromoted", 0);
                return 0;
            }
            const mapRef = (ctx && ctx.map) || global.map || null;
            const promotedSet = new Set();
            let promoted = 0;
            const promoteCandidate = (item) => {
                if (!item || item.gone || item.vanishing) return;
                if (!this.shouldRenderPrototypeBuildingInteriorLiveItemAtFullAlpha(item, cutawayState, mapRef)) {
                    return;
                }
                const displayObjects = this.collectBuildingInteriorDisplayObjectsForItem(item);
                for (let d = 0; d < displayObjects.length; d++) {
                    const displayObj = displayObjects[d];
                    if (!displayObj || displayObj.visible === false || displayObj.renderable === false) continue;
                    if (this.promoteDisplayObjectForBuildingInterior(displayObj, container, currentDisplayObjects, promotedSet)) {
                        promoted += 1;
                    }
                    this.addPickRenderItem(item, displayObj, { forceInclude: true });
                }
            };
            if (plannedItems) {
                plannedItems.forEach(item => promoteCandidate(item));
            } else {
                for (let i = 0; i < fallbackCandidates.length; i++) {
                    promoteCandidate(fallbackCandidates[i]);
                }
            }
            this.setFrameMetric("objects3dPrototypeInteriorLivePromoted", promoted);
            return promoted;
        }

        ensureBuildingInteriorOverlayContainer(container = null) {
            const target = container || (this.layers && this.layers.objects3d) || null;
            if (!target || typeof PIXI === "undefined" || !PIXI.Container) return null;
            if (!this.buildingInteriorOverlayContainer) {
                this.buildingInteriorOverlayContainer = new PIXI.Container();
                this.buildingInteriorOverlayContainer.name = "buildingInteriorOverlay";
                this.buildingInteriorOverlayContainer.interactiveChildren = false;
                this.buildingInteriorOverlayContainer.sortableChildren = true;
            }
            const overlay = this.buildingInteriorOverlayContainer;
            overlay.position.set(0, 0);
            overlay.scale.set(1, 1);
            overlay.zIndex = BUILDING_INTERIOR_OVERLAY_Z;
            overlay.visible = true;
            if (Object.prototype.hasOwnProperty.call(overlay, "renderable")) overlay.renderable = true;
            if (Object.prototype.hasOwnProperty.call(target, "sortableChildren")) target.sortableChildren = true;
            if (overlay.parent !== target) target.addChild(overlay);
            if (Object.prototype.hasOwnProperty.call(target, "sortDirty")) target.sortDirty = true;
            return overlay;
        }

        ensureBuildingInteriorDepthClearObject(overlay) {
            if (!overlay || typeof PIXI === "undefined" || !PIXI.Container) return null;
            let clearObject = this.buildingInteriorDepthClearObject;
            if (!clearObject || clearObject.destroyed) {
                clearObject = new PIXI.Container();
                clearObject.name = "buildingInteriorDepthClear";
                clearObject.interactiveChildren = false;
                clearObject.interactive = false;
                clearObject.render = function renderBuildingInteriorDepthClear(renderer) {
                    if (!renderer) return;
                    if (renderer.batch && typeof renderer.batch.flush === "function") {
                        renderer.batch.flush();
                    }
                    const gl = renderer.gl || null;
                    if (!gl || typeof gl.clear !== "function") return;
                    if (typeof gl.depthMask === "function") gl.depthMask(true);
                    if (typeof gl.clearDepth === "function") gl.clearDepth(1);
                    gl.clear(gl.DEPTH_BUFFER_BIT);
                };
                this.buildingInteriorDepthClearObject = clearObject;
            }
            clearObject.zIndex = -2147483648;
            clearObject.visible = true;
            if (Object.prototype.hasOwnProperty.call(clearObject, "renderable")) {
                clearObject.renderable = true;
            }
            if (Object.prototype.hasOwnProperty.call(overlay, "sortableChildren")) overlay.sortableChildren = true;
            if (clearObject.parent !== overlay) overlay.addChild(clearObject);
            if (Object.prototype.hasOwnProperty.call(overlay, "sortDirty")) overlay.sortDirty = true;
            return clearObject;
        }

        collectBuildingInteriorOverlayFloorEntries(ctx, region, trigger) {
            if (!region || !region.polygon || !Array.isArray(region.polygon.outer) || region.polygon.outer.length < 3) return [];
            if (this.isPrototypeBuildingCutawayTrigger(trigger)) return [];
            const regionLevel = this.getLayerIndexFromValue(region.level, 0);
            const floorEntries = this.collectFloorVisualEntries(ctx);
            const out = [];
            for (let i = 0; i < floorEntries.length; i++) {
                const source = floorEntries[i];
                if (!source || typeof source.visibleThroughFragmentId === "string") continue;
                if (this.getLayerIndexFromValue(source.level, 0) !== regionLevel) continue;
                const clipped = intersectFloorVisualPolygonWithPolygon(source.outer, source.holes, region.polygon);
                for (let p = 0; p < clipped.length; p++) {
                    const poly = clipped[p];
                    if (!poly || !Array.isArray(poly.outer) || poly.outer.length < 3) continue;
                    const holes = Array.isArray(poly.holes) ? poly.holes : [];
                    const signature = buildFloorVisualSignature(poly.outer, holes);
                    out.push({
                        key: `${String(trigger && trigger.buildingId || "building")}:${String(region.id || "region")}:floor:${String(source.key || i)}:${p}`,
                        signature,
                        triangulation: null,
                        mesh: null,
                        tint: Number.isFinite(source.tint) ? Number(source.tint) : 0xffffff,
                        alpha: 1,
                        baseZ: Number.isFinite(source.baseZ) ? Number(source.baseZ) : this.getLayerBaseZForLevel(regionLevel),
                        texture: source.texture || null,
                        textureBounds: source.textureBounds || null,
                        textureRepeat: source.textureRepeat || null,
                        texturePath: source.texturePath || "",
                        depthBias: Number.isFinite(source.depthBias) ? Number(source.depthBias) - 0.05 : FLOOR_VISUAL_DEPTH_BIAS_UNITS - 0.05,
                        isHoleOverlay: !!source.isHoleOverlay,
                        zIndex: regionLevel * 100000 + out.length,
                        uploadedGeometrySignature: "",
                        uploadedTextureBoundsSignature: "",
                        uploadedTextureRepeatSignature: "",
                        outer: poly.outer,
                        holes
                    });
                }
            }
            return out;
        }

        renderBuildingInteriorOverlayFloorEntry(source, overlay, activeKeys) {
            if (!source || !overlay || !(activeKeys instanceof Set)) return 0;
            let entry = this.buildingInteriorOverlayFloorMeshes instanceof Map
                ? this.buildingInteriorOverlayFloorMeshes.get(source.key)
                : null;
            const textureBoundsSignature = this.getFloorVisualTextureBoundsSignature(source.textureBounds);
            const textureRepeatSignature = this.getFloorVisualTextureRepeatSignature(source.textureRepeat);
            if (
                !entry ||
                entry.signature !== source.signature ||
                entry.texturePath !== source.texturePath ||
                entry.uploadedTextureBoundsSignature !== textureBoundsSignature ||
                entry.uploadedTextureRepeatSignature !== textureRepeatSignature
            ) {
                if (entry && entry.mesh) {
                    this.destroyCachedPixiMesh(entry.mesh, "building interior overlay floor mesh");
                }
                const triangulation = triangulateFloorVisualPolygon(source.outer, source.holes);
                if (!triangulation) return 0;
                entry = {
                    ...source,
                    triangulation
                };
                entry.mesh = this.createFloorVisualMesh(entry);
                if (!entry.mesh) return 0;
                const foregroundState = this.getBuildingInteriorForegroundState();
                if (foregroundState) entry.mesh.state = foregroundState;
                this.uploadFloorVisualMeshGeometry(entry);
                if (!(this.buildingInteriorOverlayFloorMeshes instanceof Map)) {
                    this.buildingInteriorOverlayFloorMeshes = new Map();
                }
                this.buildingInteriorOverlayFloorMeshes.set(source.key, entry);
            } else {
                entry.tint = source.tint;
                entry.alpha = source.alpha;
                entry.baseZ = source.baseZ;
                entry.depthBias = source.depthBias;
                entry.textureRepeat = source.textureRepeat;
                entry.texturePath = source.texturePath;
                entry.texture = source.texture || null;
                entry.textureBounds = source.textureBounds || null;
                entry.isHoleOverlay = !!source.isHoleOverlay;
            }
            if (entry.mesh.parent !== overlay) overlay.addChild(entry.mesh);
            entry.mesh.zIndex = source.zIndex;
            const foregroundState = this.getBuildingInteriorForegroundState();
            if (foregroundState) entry.mesh.state = foregroundState;
            this.updateFloorVisualMesh(entry);
            entry.mesh.visible = true;
            if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) entry.mesh.renderable = true;
            activeKeys.add(source.key);
            return 1;
        }

        renderBuildingInteriorOverlayFloor(ctx, region, trigger, overlay, activeKeys) {
            const sources = this.collectBuildingInteriorOverlayFloorEntries(ctx, region, trigger);
            let rendered = 0;
            for (let i = 0; i < sources.length; i++) {
                rendered += this.renderBuildingInteriorOverlayFloorEntry(sources[i], overlay, activeKeys);
            }
            return rendered;
        }

        shouldRenderBuildingInteriorOverlayWall(wall, region, trigger, ctx, wizardRef) {
            if (!wall || wall.type !== "wallSection" || wall.gone || wall.vanishing) return false;
            if (this.isBuildingCutawayDoorItem(wall)) return false;
            const wallLayer = this.getLayerIndexForObject(wall, this.getLayerIndexFromValue(region && region.level, 0));
            if (wallLayer !== this.getLayerIndexFromValue(region && region.level, 0)) return false;
            return true;
        }

        shouldFlattenBuildingInteriorOverlayWall(wall, ctx, wizardRef) {
            if (!wall || wall.type !== "wallSection") return false;
            if (typeof wall.isVisibleInMazeModeFacingRule !== "function") return true;
            const facesSame = wall.isVisibleInMazeModeFacingRule({
                worldToScreenFn: (pt) => this.camera.worldToScreen(
                    Number(pt && pt.x) || 0,
                    Number(pt && pt.y) || 0,
                    0
                ),
                viewscale: this.camera.viewscale,
                xyratio: this.camera.xyratio,
                player: wizardRef || (ctx && ctx.wizard) || global.wizard || null
            });
            return !facesSame;
        }

        shouldRenderBuildingInteriorDoorItem(item, region, entry, ctx, wizardRef) {
            if (!item || item.gone || item.vanishing) return false;
            if (!this.isBuildingCutawayDoorItem(item)) return false;
            if (item === wizardRef) return false;
            if (!this.isScriptVisible(item)) return false;
            const regionLevel = this.getLayerIndexFromValue(region && region.level, 0);
            const itemLevel = this.getBuildingInteriorEntryLayer(item, entry, regionLevel);
            return itemLevel === regionLevel;
        }

        shouldRenderBuildingExteriorGroundDoorItem(item, entry, trigger, ctx, wizardRef) {
            if (!item || item.gone || item.vanishing) return false;
            if (!trigger || !trigger.building || trigger.activeInteriorRegion) return false;
            if (trigger.building._prototypeBuildingPlacement) return false;
            if (!this.isBuildingCutawayDoorItem(item)) return false;
            if (item === wizardRef) return false;
            if (!this.isScriptVisible(item)) return false;
            const bottomLevel = this.getLayerIndexFromValue(trigger.building.minLevel, 0);
            const itemLevel = this.getBuildingInteriorEntryLayer(item, entry, bottomLevel);
            return itemLevel === bottomLevel;
        }

        isBuildingInteriorWindowHiddenByCollapsedWall(item, ctx, wizardRef) {
            if (!this.isBuildingCutawayWindowItem(item)) return false;
            const mountedSection = this.resolveMountedWallSectionForItem(item);
            if (!mountedSection || mountedSection.type !== "wallSection") return false;
            return this.shouldFlattenBuildingInteriorOverlayWall(mountedSection, ctx, wizardRef);
        }

        getBuildingInteriorMountedDoorSide(item, region, ctx = null, mapRef = null) {
            if (!item || !this.isBuildingCutawayDoorItem(item) || !region || !region.polygon) return null;
            const faceCenters = this.getMountedFaceCentersForItem(item);
            if (
                !faceCenters ||
                !faceCenters.front ||
                !faceCenters.back ||
                !Number.isFinite(faceCenters.front.x) ||
                !Number.isFinite(faceCenters.front.y) ||
                !Number.isFinite(faceCenters.back.x) ||
                !Number.isFinite(faceCenters.back.y)
            ) {
                return null;
            }
            const polygons = [region.polygon];
            const frontInside = floorVisualPolygonsContainPoint(polygons, faceCenters.front.x, faceCenters.front.y);
            const backInside = floorVisualPolygonsContainPoint(polygons, faceCenters.back.x, faceCenters.back.y);
            if (frontInside && !backInside) return "front";
            if (backInside && !frontInside) return "back";

            const viewer = (ctx && ctx.wizard && Number.isFinite(ctx.wizard.x) && Number.isFinite(ctx.wizard.y))
                ? ctx.wizard
                : null;
            if (!viewer) return null;
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            const frontDx = (map && typeof map.shortestDeltaX === "function")
                ? map.shortestDeltaX(Number(viewer.x), Number(faceCenters.front.x))
                : (Number(faceCenters.front.x) - Number(viewer.x));
            const frontDy = (map && typeof map.shortestDeltaY === "function")
                ? map.shortestDeltaY(Number(viewer.y), Number(faceCenters.front.y))
                : (Number(faceCenters.front.y) - Number(viewer.y));
            const backDx = (map && typeof map.shortestDeltaX === "function")
                ? map.shortestDeltaX(Number(viewer.x), Number(faceCenters.back.x))
                : (Number(faceCenters.back.x) - Number(viewer.x));
            const backDy = (map && typeof map.shortestDeltaY === "function")
                ? map.shortestDeltaY(Number(viewer.y), Number(faceCenters.back.y))
                : (Number(faceCenters.back.y) - Number(viewer.y));
            const frontDist2 = frontDx * frontDx + frontDy * frontDy;
            const backDist2 = backDx * backDx + backDy * backDy;
            if (!Number.isFinite(frontDist2) || !Number.isFinite(backDist2)) return null;
            return frontDist2 <= backDist2 ? "front" : "back";
        }

        isBuildingCutawayMountedOpeningItem(item) {
            return !!(
                this.isWallMountedSpatialItem(item) &&
                (this.isBuildingCutawayDoorItem(item) || this.isBuildingCutawayWindowItem(item))
            );
        }

        getBuildingCutawayMountedExteriorSide(item, cutawayState = null, ctx = null, mapRef = null) {
            if (!item || !this.isBuildingCutawayMountedOpeningItem(item)) return null;
            const state = cutawayState || this.getLayerCutawayState(ctx);
            if (!state || !state.active || !Array.isArray(state.triggers)) return null;
            const faceCenters = this.getMountedFaceCentersForItem(item);
            if (
                !faceCenters ||
                !faceCenters.front ||
                !faceCenters.back ||
                !Number.isFinite(faceCenters.front.x) ||
                !Number.isFinite(faceCenters.front.y) ||
                !Number.isFinite(faceCenters.back.x) ||
                !Number.isFinite(faceCenters.back.y)
            ) {
                return null;
            }
            const map = mapRef || (ctx && ctx.map) || global.map || null;
            let matchedCutawayEntry = false;
            for (let i = 0; i < state.triggers.length; i++) {
                const trigger = state.triggers[i];
                if (!trigger || !trigger.building || trigger.activeInteriorRegion) continue;
                const cache = trigger.renderCache || null;
                const entries = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                let entry = null;
                for (let j = 0; j < entries.length; j++) {
                    if (entries[j] && entries[j].item === item) {
                        entry = entries[j];
                        break;
                    }
                }
                if (!entry) continue;
                matchedCutawayEntry = true;
                const itemLevel = this.getBuildingInteriorEntryLayer(item, entry, 0);
                const regions = Array.isArray(cache && cache.interiorRegions) ? cache.interiorRegions : [];
                const polygons = [];
                for (let r = 0; r < regions.length; r++) {
                    const region = regions[r];
                    if (!region || !region.polygon) continue;
                    if (this.getLayerIndexFromValue(region.level, 0) !== itemLevel) continue;
                    polygons.push(region.polygon);
                }
                if (polygons.length === 0) continue;
                const frontInside = floorVisualPolygonsContainPoint(polygons, faceCenters.front.x, faceCenters.front.y);
                const backInside = floorVisualPolygonsContainPoint(polygons, faceCenters.back.x, faceCenters.back.y);
                if (frontInside && !backInside) return "back";
                if (backInside && !frontInside) return "front";
            }

            if (!matchedCutawayEntry) return null;
            const viewer = (ctx && ctx.wizard && Number.isFinite(ctx.wizard.x) && Number.isFinite(ctx.wizard.y))
                ? ctx.wizard
                : null;
            if (!viewer) return null;
            const frontDx = (map && typeof map.shortestDeltaX === "function")
                ? map.shortestDeltaX(Number(viewer.x), Number(faceCenters.front.x))
                : (Number(faceCenters.front.x) - Number(viewer.x));
            const frontDy = (map && typeof map.shortestDeltaY === "function")
                ? map.shortestDeltaY(Number(viewer.y), Number(faceCenters.front.y))
                : (Number(faceCenters.front.y) - Number(viewer.y));
            const backDx = (map && typeof map.shortestDeltaX === "function")
                ? map.shortestDeltaX(Number(viewer.x), Number(faceCenters.back.x))
                : (Number(faceCenters.back.x) - Number(viewer.x));
            const backDy = (map && typeof map.shortestDeltaY === "function")
                ? map.shortestDeltaY(Number(viewer.y), Number(faceCenters.back.y))
                : (Number(faceCenters.back.y) - Number(viewer.y));
            const frontDist2 = frontDx * frontDx + frontDy * frontDy;
            const backDist2 = backDx * backDx + backDy * backDy;
            if (!Number.isFinite(frontDist2) || !Number.isFinite(backDist2)) return null;
            return frontDist2 <= backDist2 ? "front" : "back";
        }

        shouldRenderBuildingInteriorOverlayItem(item, region, entry, ctx, wizardRef) {
            if (!item || item.gone || item.vanishing || item.type === "wallSection" || item.type === "roof") return false;
            if (this.isBuildingCutawayDoorItem(item) || this.isBuildingInteriorOverlaySurfaceItem(item)) return false;
            if (this.isCharacterRenderItem(item) || item === wizardRef) return false;
            if (!this.isScriptVisible(item)) return false;
            const regionLevel = this.getLayerIndexFromValue(region && region.level, 0);
            const itemLevel = Number.isFinite(entry && entry.level)
                ? this.getLayerIndexFromValue(entry.level, regionLevel)
                : this.getLayerIndexForObject(item, regionLevel);
            if (itemLevel !== regionLevel) return false;
            const refs = Array.isArray(entry && entry.refs)
                ? entry.refs
                : this.collectFloorRefsForRenderItem(item, (ctx && ctx.map) || global.map || null);
            if (this.renderItemRefsMatchBuildingInteriorRegion(refs, region)) return true;
            if (this.hasBuildingInteriorFloorRefs(refs)) return false;
            if (!Array.isArray(refs) || refs.length === 0) return true;
            if (region && region.polygon) {
                return this.renderItemSamplesMatchPolygons(item, [region.polygon], (ctx && ctx.map) || global.map || null);
            }
            return false;
        }

        shouldRenderActiveInteriorItemLiveOverReadyBitmap(item, region, entry, ctx, wizardRef) {
            if (!item || item.gone || item.vanishing) return false;
            const isPlacedStaticObject = !!(
                item.isPlacedObject === true ||
                item.objectType === "placedObject" ||
                item.type === "placedObject" ||
                (item._floorMembership && typeof item._floorMembership === "object")
            );
            if (!isPlacedStaticObject) return false;
            if (item.rotationAxis === "ground") return false;
            if (this.isBuildingInteriorOverlaySurfaceItem(item)) return false;
            return this.shouldRenderBuildingInteriorOverlayItem(item, region, entry, ctx, wizardRef);
        }

        getBuildingInteriorDynamicCharacterCandidates(ctx = null, wizardRef = null) {
            const out = [];
            const seen = new Set();
            const add = (item) => {
                if (!item || seen.has(item)) return;
                if (item === wizardRef) return;
                if (!this.isCharacterRenderItem(item)) return;
                seen.add(item);
                out.push(item);
            };
            const animalsList = Array.isArray(ctx && ctx.animals)
                ? ctx.animals
                : (Array.isArray(global.animals) ? global.animals : []);
            for (let i = 0; i < animalsList.length; i++) add(animalsList[i]);
            const charactersList = Array.isArray(ctx && ctx.characters)
                ? ctx.characters
                : (Array.isArray(global.characters) ? global.characters : []);
            for (let i = 0; i < charactersList.length; i++) add(charactersList[i]);
            return out;
        }

        shouldRenderBuildingInteriorCharacter(item, region, ctx, wizardRef, mapRef = null) {
            if (!item || item === wizardRef || item.gone || item.vanishing) return false;
            if (!this.isCharacterRenderItem(item)) return false;
            if (!this.isScriptVisible(item)) return false;
            const regionLevel = this.getLayerIndexFromValue(region && region.level, 0);
            const itemLevel = this.getLayerIndexForObject(item, regionLevel);
            if (itemLevel !== regionLevel) return false;
            const refs = this.collectFloorRefsForRenderItem(item, mapRef || (ctx && ctx.map) || global.map || null);
            if (this.renderItemRefsMatchBuildingInteriorRegion(refs, region)) return true;
            if (this.hasBuildingInteriorFloorRefs(refs) && region.kind === "floorFragment") return false;
            if (region && region.polygon) {
                return this.renderItemSamplesMatchPolygons(item, [region.polygon], mapRef || (ctx && ctx.map) || global.map || null);
            }
            return false;
        }

        renderActiveBuildingInteriorOverlay(ctx, cutawayState, container, renderItems = []) {
            return this.getBuildingInteriorView().renderActiveOverlay(ctx, cutawayState, container, renderItems);
        }

        hideInactiveBuildingInteriorOverlayMeshes(activeFloorKeys) {
            const floorKeys = activeFloorKeys instanceof Set ? activeFloorKeys : new Set();
            if (this.buildingInteriorOverlayFloorMeshes instanceof Map) {
                for (const [key, entry] of this.buildingInteriorOverlayFloorMeshes.entries()) {
                    if (floorKeys.has(key)) continue;
                    if (entry && entry.mesh) {
                        entry.mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) entry.mesh.renderable = false;
                    }
                }
            }
            if (this.buildingInteriorOverlayContainer) {
                const anyActive = floorKeys.size > 0;
                this.buildingInteriorOverlayContainer.visible = anyActive;
                if (Object.prototype.hasOwnProperty.call(this.buildingInteriorOverlayContainer, "renderable")) {
                    this.buildingInteriorOverlayContainer.renderable = anyActive;
                }
            }
        }

        hideBuildingInteriorOverlayTexture() {
            if (this.buildingInteriorOverlayContainer) {
                this.buildingInteriorOverlayContainer.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.buildingInteriorOverlayContainer, "renderable")) {
                    this.buildingInteriorOverlayContainer.renderable = false;
                }
            }
            if (this.buildingInteriorDepthClearObject) {
                this.buildingInteriorDepthClearObject.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.buildingInteriorDepthClearObject, "renderable")) {
                    this.buildingInteriorDepthClearObject.renderable = false;
                }
            }
        }

        presentBuildingInteriorOverlayTexture(ctx, overlay, container, active) {
            if (!active || !overlay || !container) {
                this.setFrameMetric("buildingInteriorOverlayDepthClearActive", 0);
                this.hideBuildingInteriorOverlayTexture();
                return null;
            }
            this.setFrameMetric("buildingInteriorOverlayTextureWidth", 0);
            this.setFrameMetric("buildingInteriorOverlayTextureHeight", 0);
            this.setFrameMetric("buildingInteriorOverlayTexturePixels", 0);
            this.setFrameMetric("buildingInteriorOverlayDepthClearActive", 1);
            overlay.visible = true;
            if (Object.prototype.hasOwnProperty.call(overlay, "renderable")) overlay.renderable = true;
            this.ensureBuildingInteriorDepthClearObject(overlay);
            if (overlay.parent !== container) container.addChild(overlay);
            if (Object.prototype.hasOwnProperty.call(container, "sortableChildren")) container.sortableChildren = true;
            overlay.zIndex = BUILDING_INTERIOR_OVERLAY_Z;
            if (Object.prototype.hasOwnProperty.call(container, "sortDirty")) container.sortDirty = true;
            return overlay;
        }

        getCurrentFloorDarknessLayer() {
            if (Number.isFinite(this._fallRevealLayer)) {
                return this.getLayerIndexFromValue(this._fallRevealLayer, 0);
            }
            if (Number.isFinite(this._lastRenderedWizardLayer)) {
                return this.getLayerIndexFromValue(this._lastRenderedWizardLayer, 0);
            }
            return this.getLayerIndexFromValue(global && global.wizard ? global.wizard.currentLayer : 0, 0);
        }

        getLayerDarknessMultiplier(level) {
            const layer = this.getLayerIndexFromValue(level, 0);
            const currentLayer = this.getCurrentFloorDarknessLayer();
            return layer < currentLayer ? FLOOR_BELOW_CURRENT_DARKNESS_MULTIPLIER : 1;
        }

        multiplyTint(tint, multiplier) {
            const baseTint = Number.isFinite(tint) ? Math.max(0, Math.min(0xffffff, Math.floor(tint))) : 0xffffff;
            const m = Number.isFinite(multiplier) ? Math.max(0, Math.min(1, Number(multiplier))) : 1;
            const r = Math.round(((baseTint >> 16) & 0xff) * m);
            const g = Math.round(((baseTint >> 8) & 0xff) * m);
            const b = Math.round((baseTint & 0xff) * m);
            return (r << 16) | (g << 8) | b;
        }

        getLayerDarkenedTint(tint, level) {
            return this.multiplyTint(tint, this.getLayerDarknessMultiplier(level));
        }

        applyLayerDarknessToDisplayObject(displayObj, multiplier = 1) {
            if (!displayObj || typeof displayObj !== "object") return;
            const m = Number.isFinite(multiplier) ? Math.max(0, Math.min(1, Number(multiplier))) : 1;
            const tintStateKey = "__renderingLayerDarknessTintState";
            const uniformStateKey = "__renderingLayerDarknessUniformState";
            const applyOne = (obj) => {
                if (!obj || typeof obj !== "object") return;
                if (Number.isFinite(obj.tint)) {
                    const state = obj[tintStateKey];
                    const currentTint = Math.max(0, Math.min(0xffffff, Math.floor(Number(obj.tint))));
                    const hasState = !!(
                        state &&
                        Number.isFinite(state.baseTint) &&
                        Number.isFinite(state.appliedTint)
                    );
                    const baseTint = hasState && currentTint === Math.floor(state.appliedTint)
                        ? Math.max(0, Math.min(0xffffff, Math.floor(Number(state.baseTint))))
                        : currentTint;
                    if (m < 0.999) {
                        const appliedTint = this.multiplyTint(baseTint, m);
                        obj.tint = appliedTint;
                        obj[tintStateKey] = { baseTint, appliedTint };
                    } else {
                        if (hasState && currentTint === Math.floor(state.appliedTint)) {
                            obj.tint = baseTint;
                        }
                        delete obj[tintStateKey];
                    }
                }

                const uniforms = obj.shader && obj.shader.uniforms ? obj.shader.uniforms : null;
                const uTint = uniforms && uniforms.uTint && uniforms.uTint.length >= 3 ? uniforms.uTint : null;
                if (uTint) {
                    const state = obj[uniformStateKey];
                    const current = [Number(uTint[0]) || 0, Number(uTint[1]) || 0, Number(uTint[2]) || 0];
                    const hasState = !!(
                        state &&
                        Array.isArray(state.base) &&
                        Array.isArray(state.applied) &&
                        state.base.length >= 3 &&
                        state.applied.length >= 3
                    );
                    const matchesApplied = hasState &&
                        Math.abs(current[0] - Number(state.applied[0])) < 1e-6 &&
                        Math.abs(current[1] - Number(state.applied[1])) < 1e-6 &&
                        Math.abs(current[2] - Number(state.applied[2])) < 1e-6;
                    const base = matchesApplied
                        ? [Number(state.base[0]) || 0, Number(state.base[1]) || 0, Number(state.base[2]) || 0]
                        : current;
                    if (m < 0.999) {
                        uTint[0] = base[0] * m;
                        uTint[1] = base[1] * m;
                        uTint[2] = base[2] * m;
                        obj[uniformStateKey] = {
                            base,
                            applied: [uTint[0], uTint[1], uTint[2]]
                        };
                    } else {
                        if (matchesApplied) {
                            uTint[0] = base[0];
                            uTint[1] = base[1];
                            uTint[2] = base[2];
                        }
                        delete obj[uniformStateKey];
                    }
                }
            };

            applyOne(displayObj);
            const children = Array.isArray(displayObj.children) ? displayObj.children : [];
            for (let i = 0; i < children.length; i++) {
                this.applyLayerDarknessToDisplayObject(children[i], m);
            }
        }

        applyLayerDarknessForItem(item, level, displayObj = null) {
            const layer = Number.isFinite(level)
                ? this.getLayerIndexFromValue(level, 0)
                : this.getLayerIndexForObject(item, 0);
            this.applyLayerDarknessToDisplayObject(displayObj, this.getLayerDarknessMultiplier(layer));
        }

        init(ctx) {
            if (this.initialized) return;
            const parent = (ctx && ctx.gameContainer) || (ctx && ctx.app && ctx.app.stage) || null;
            if (!parent) return;
            parent.addChild(this.layers.root);
            this.layers.root.zIndex = 10000;
            this.initialized = true;
        }

        getCharacterLayer() {
            return (this.layers && (this.layers.objects3d || this.layers.depthObjects || this.layers.characters || this.layers.entities)) || null;
        }

        isCharacterRenderItem(item) {
            return !!(item && typeof Character !== "undefined" && item instanceof Character);
        }

        getRenderItemOverlayWorldPosition(item) {
            if (!item) return null;
            if (this.isCharacterRenderItem(item) && typeof item.getInterpolatedPosition === "function") {
                const interpolated = item.getInterpolatedPosition();
                if (interpolated) {
                    return {
                        x: Number.isFinite(interpolated.x) ? Number(interpolated.x) : Number(item.x),
                        y: Number.isFinite(interpolated.y) ? Number(interpolated.y) : Number(item.y),
                        z: Number.isFinite(interpolated.z) ? Number(interpolated.z) : Number(item.z)
                    };
                }
            }
            return {
                x: Number.isFinite(item.x) ? Number(item.x) : 0,
                y: Number.isFinite(item.y) ? Number(item.y) : 0,
                z: Number.isFinite(item.z) ? Number(item.z) : 0
            };
        }

        getRenderItemOverlayBaseWorldZ(item) {
            const pos = this.getRenderItemOverlayWorldPosition(item);
            if (!item || !pos) return 0;
            if (this.isCharacterRenderItem(item)) {
                if (Number.isFinite(pos.z)) return Number(pos.z);
                if (Number.isFinite(item.currentLayerBaseZ)) return Number(item.currentLayerBaseZ);
                return 0;
            }
            const category = typeof item.category === "string" ? item.category.trim().toLowerCase() : "";
            const type = typeof item.type === "string" ? item.type.trim().toLowerCase() : "";
            const localZ = Number.isFinite(pos.z) ? Number(pos.z) : 0;
            if (category === "windows" || type === "window") return localZ;
            const layerBaseZ = Number.isFinite(item._renderLayerBaseZ) ? Number(item._renderLayerBaseZ) : 0;
            return layerBaseZ + localZ;
        }

        getRenderItemOverlayScreenPoint(item, zOffset = 0) {
            if (!item || !this.camera || typeof this.camera.worldToScreen !== "function") return null;
            const pos = this.getRenderItemOverlayWorldPosition(item);
            if (!pos || !Number.isFinite(pos.x) || !Number.isFinite(pos.y)) return null;
            const baseZ = this.getRenderItemOverlayBaseWorldZ(item);
            return this.camera.worldToScreen(
                pos.x,
                pos.y,
                baseZ + (Number.isFinite(zOffset) ? Number(zOffset) : 0)
            );
        }

        getWizardShadowTexture() {
            if (this._wizardShadowTexture) return this._wizardShadowTexture;
            const canvas = document.createElement("canvas");
            canvas.width = 128;
            canvas.height = 128;
            const ctx2d = canvas.getContext("2d");
            if (!ctx2d) return PIXI.Texture.WHITE;
            const gradient = ctx2d.createRadialGradient(64, 64, 12, 64, 64, 62);
            gradient.addColorStop(0, "rgba(0,0,0,0.34)");
            gradient.addColorStop(0.6, "rgba(0,0,0,0.18)");
            gradient.addColorStop(1, "rgba(0,0,0,0)");
            ctx2d.fillStyle = gradient;
            ctx2d.beginPath();
            ctx2d.ellipse(64, 64, 62, 62, 0, 0, Math.PI * 2);
            ctx2d.fill();
            this._wizardShadowTexture = PIXI.Texture.from(canvas);
            return this._wizardShadowTexture;
        }

        ensureWizardGhostSprite() {
            if (!this.wizardGhostSprite) {
                this.wizardGhostSprite = new PIXI.Sprite(PIXI.Texture.from("/assets/images/ghost.png"));
                this.wizardGhostSprite.name = "renderingWizardGhost";
                this.wizardGhostSprite.anchor.set(0.5, 1);
                this.wizardGhostSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.wizardGhostSprite, "renderable")) {
                    this.wizardGhostSprite.renderable = false;
                }
            }
            return this.wizardGhostSprite;
        }

        ensureWizardShadowProxy() {
            if (!this.wizardShadowSprite) {
                this.wizardShadowSprite = new PIXI.Sprite(this.getWizardShadowTexture());
                this.wizardShadowSprite.name = "renderingWizardShadowSprite";
                this.wizardShadowSprite.anchor.set(0.5, 0.5);
            }
            if (!this.wizardShadowProxy) {
                this.wizardShadowProxy = {
                    type: "wizardShadow",
                    category: "shadow",
                    rotationAxis: "ground",
                    placementRotation: 0,
                    forceDepthTestGround: true,
                    pixiSprite: this.wizardShadowSprite,
                    x: 0,
                    y: 0,
                    z: 0,
                    width: 0.4,
                    height: 0.4,
                    gone: false,
                    vanishing: false,
                    visible: true,
                    tint: 0xFFFFFF,
                    _depthBillboardMesh: null,
                    _depthBillboardWorldPositions: null,
                    _depthBillboardLastSignature: "",
                    _depthBillboardLastUvSignature: "",
                    _depthBillboardMeshMode: ""
                };
                const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                    ? global.StaticObject.prototype
                    : null;
                if (staticProto) {
                    if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                        this.wizardShadowProxy.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                    }
                    if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                        this.wizardShadowProxy.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                    }
                    if (typeof staticProto.updateDepthBillboardMesh === "function") {
                        this.wizardShadowProxy.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
                    }
                }
            }
            return this.wizardShadowProxy;
        }

        setLegacyLayersVisible(visible) {
            const names = [
                "landLayer",
                "roadLayer",
                "gridLayer",
                "neighborDebugLayer",
                "opaqueMeshLayer",
                "objectLayer",
                "roofLayer",
                "characterLayer",
                "projectileLayer",
                "hitboxLayer"
            ];
            for (let i = 0; i < names.length; i++) {
                const layer = global[names[i]];
                if (layer && typeof layer.visible === "boolean") layer.visible = visible;
            }
        }

        getProjectileTexture(projectile) {
            if (!projectile) return null;
            const frames = Array.isArray(projectile.explosionFrames) ? projectile.explosionFrames : null;
            if (frames && frames.length > 0) {
                const rawFrame = Number.isFinite(projectile.explosionFrame)
                    ? Math.floor(projectile.explosionFrame)
                    : 0;
                const frameIndex = ((rawFrame % frames.length) + frames.length) % frames.length;
                const frameTexture = frames[frameIndex];
                if (frameTexture) return frameTexture;
            }
            const imageSrc = (projectile.image && typeof projectile.image.src === "string" && projectile.image.src.length > 0)
                ? projectile.image.src
                : null;
            if (imageSrc) return PIXI.Texture.from(imageSrc);
            const texturePath = (typeof projectile.texturePath === "string" && projectile.texturePath.length > 0)
                ? projectile.texturePath
                : null;
            if (texturePath) return PIXI.Texture.from(texturePath);
            return null;
        }

        renderProjectileParticles(projectile, container, hiddenByMazeLos) {
            if (!projectile) return null;
            const particles = Array.isArray(projectile.snowParticles) ? projectile.snowParticles : null;
            let graphics = projectile.particleGraphics || null;

            if (!particles || particles.length === 0 || hiddenByMazeLos) {
                if (graphics) {
                    graphics.clear();
                    graphics.visible = false;
                    if (graphics.parent) {
                        graphics.parent.removeChild(graphics);
                    }
                }
                return graphics;
            }

            if (!graphics) {
                graphics = new PIXI.Graphics();
                graphics.name = "projectileParticles";
                projectile.particleGraphics = graphics;
            }
            if (graphics.parent !== container) {
                container.addChild(graphics);
            }

            graphics.clear();
            graphics.visible = true;
            graphics.zIndex = 2;

            for (let i = 0; i < particles.length; i++) {
                const particle = particles[i];
                if (!particle) continue;
                const lifeMs = Math.max(1, Number(particle.lifeMs) || 1);
                const ageMs = Math.max(0, Number(particle.ageMs) || 0);
                const lifeProgress = Math.max(0, Math.min(1, ageMs / lifeMs));
                const alpha = Math.max(0, (Number(particle.alpha) || 0) * (1 - lifeProgress));
                if (alpha <= 0.01) continue;
                const baseSize = Math.max(1, Number(particle.size) || 1);
                const shrink = Math.max(0, Math.min(1, Number(particle.shrink) || 0));
                const radiusPx = Math.max(0.7, baseSize * (1 - (lifeProgress * shrink)));
                const projectileBaseZ = this.getProjectileVisualBaseZ(projectile);
                const screenPoint = this.camera.worldToScreen(
                    Number(particle.x) || 0,
                    Number(particle.y) || 0,
                    projectileBaseZ + Math.max(0, Number(particle.z) || 0)
                );
                graphics.beginFill(Number.isFinite(particle.color) ? Number(particle.color) : 0xeaf7ff, alpha);
                graphics.drawCircle(screenPoint.x, screenPoint.y, radiusPx);
                graphics.endFill();
            }

            return graphics;
        }

        getProjectileVisualProgress(projectile) {
            if (!projectile) return 0;
            if (Number.isFinite(projectile.visualProgress)) {
                return Math.max(0, Math.min(1, Number(projectile.visualProgress)));
            }
            const traveled = Number(projectile.traveledDist);
            const total = Number(projectile.totalDist);
            if (Number.isFinite(traveled) && Number.isFinite(total) && total > 0) {
                return Math.max(0, Math.min(1, traveled / total));
            }
            const ageMs = Number(projectile.ageMs);
            const lifetimeMs = Number(projectile.maxLifetimeMs);
            if (Number.isFinite(ageMs) && Number.isFinite(lifetimeMs) && lifetimeMs > 0) {
                return Math.max(0, Math.min(1, ageMs / lifetimeMs));
            }
            return 0;
        }

        getProjectileVisualBaseZ(projectile) {
            if (!projectile) return 0;
            const startZ = Number(projectile.visualStartZ);
            const targetZ = Number(projectile.visualTargetZ);
            if (Number.isFinite(startZ) && Number.isFinite(targetZ)) {
                const progress = this.getProjectileVisualProgress(projectile);
                return startZ + ((targetZ - startZ) * progress);
            }
            if (Number.isFinite(projectile.visualBaseZ)) return Number(projectile.visualBaseZ);
            const support = projectile.currentMovementSupport && typeof projectile.currentMovementSupport === "object"
                ? projectile.currentMovementSupport
                : null;
            if (support && Number.isFinite(Number(support.baseZ))) return Number(support.baseZ);
            if (Number.isFinite(Number(projectile.currentLayerBaseZ))) return Number(projectile.currentLayerBaseZ);
            if (Number.isFinite(Number(projectile._renderLayerBaseZ))) return Number(projectile._renderLayerBaseZ);
            if (projectile.node && Number.isFinite(Number(projectile.node.baseZ))) return Number(projectile.node.baseZ);
            return 0;
        }

        getLosVisualSetting(key, fallback) {
            const settings = (typeof LOSVisualSettings !== "undefined")
                ? LOSVisualSettings
                : (global.LOSVisualSettings || null);
            if (!settings || typeof settings !== "object") return fallback;
            return Object.prototype.hasOwnProperty.call(settings, key) ? settings[key] : fallback;
        }

        getLosNearRevealRadius() {
            return LOS_NEAR_REVEAL_RADIUS;
        }

        isLosMazeModeEnabled() {
            return !!this.getLosVisualSetting("mazeMode", false);
        }

        isMazeModeOverlayEligible(ctx) {
            if (!this.mazeModeRenderer) return false;
            if (!this.isLosMazeModeEnabled()) return false;
            const wizard = (ctx && ctx.wizard) ? ctx.wizard : null;
            return !this.isOmnivisionActive(wizard);
        }

        applyMazeModeCompositor(ctx) {
            const overlayEligible = this.isMazeModeOverlayEligible(ctx);
            const wasOverlayActive = !!this.mazeModeOverlayActive;
            if (!this.mazeModeRenderer) {
                this.mazeModeOverlayActive = false;
                this.mazeModeJustActivatedFrame = false;
                return false;
            }

            this.mazeModeOverlayActive = !!this.mazeModeRenderer.apply(this, ctx, {
                enabled: overlayEligible
            });
            this.mazeModeJustActivatedFrame = !!(this.mazeModeOverlayActive && !wasOverlayActive);

            const root = this.layers && this.layers.root ? this.layers.root : null;
            if (!root) return this.mazeModeOverlayActive;
            const bringToTop = (node) => {
                if (node && node.parent === root) root.addChild(node);
            };

            if (this.mazeModeOverlayActive) {
                const maskNode = this.mazeModeRenderer.occlusionMaskGraphics || null;
                const backdropNode = this.mazeModeRenderer.blackBackdropGraphics || null;
                if (maskNode && maskNode.parent === root) root.setChildIndex(maskNode, 0);
                if (backdropNode && backdropNode.parent === root) root.setChildIndex(backdropNode, Math.min(1, root.children.length - 1));
                bringToTop(this.layers.ground);
                bringToTop(this.layers.roadsFloor);
                bringToTop(this.layers.groundObjects);
                bringToTop(this.layers.losShadow);
                bringToTop(this.layers.depthObjects);
                bringToTop(this.layers.characters);
                bringToTop(this.layers.objects3d);
                bringToTop(this.layers.entities);
                bringToTop(this.layers.ui);
                bringToTop(this.layers.scriptMessages);
            } else {
                bringToTop(this.layers.ground);
                bringToTop(this.layers.roadsFloor);
                bringToTop(this.layers.groundObjects);
                bringToTop(this.layers.losShadow);
                bringToTop(this.layers.depthObjects);
                bringToTop(this.layers.characters);
                bringToTop(this.layers.objects3d);
                bringToTop(this.layers.entities);
                bringToTop(this.layers.ui);
                bringToTop(this.layers.scriptMessages);
            }

            return this.mazeModeOverlayActive;
        }

        isOmnivisionActive(wizard) {
            if (!wizard) return false;
            const activeAuras = (Array.isArray(wizard.activeAuras))
                ? wizard.activeAuras
                : ((typeof wizard.activeAura === "string") ? [wizard.activeAura] : []);
            return activeAuras.includes("omnivision");
        }

        isInvisibilityActive(wizard) {
            if (!wizard) return false;
            const activeAuras = (Array.isArray(wizard.activeAuras))
                ? wizard.activeAuras
                : ((typeof wizard.activeAura === "string") ? [wizard.activeAura] : []);
            return activeAuras.includes("invisibility");
        }

        getWizardFacingAngleRad(wizard) {
            if (!wizard) return 0;
            if (Number.isFinite(wizard.smoothedFacingAngleDeg)) {
                return Number(wizard.smoothedFacingAngleDeg) * (Math.PI / 180);
            }
            if (Number.isInteger(wizard.lastDirectionRow)) {
                const rowAngleDegByDirectionIndex = [180, -150, -120, -90, -60, -30, 0, 30, 60, 90, 120, 150];
                const rowOffset = Number.isFinite(global.wizardDirectionRowOffset)
                    ? Number(global.wizardDirectionRowOffset)
                    : 0;
                const directionIndex = ((wizard.lastDirectionRow - rowOffset) % 12 + 12) % 12;
                const deg = rowAngleDegByDirectionIndex[directionIndex];
                if (Number.isFinite(deg)) return deg * (Math.PI / 180);
            }
            if (wizard.direction && Number.isFinite(wizard.direction.x) && Number.isFinite(wizard.direction.y)) {
                const mag = Math.hypot(wizard.direction.x, wizard.direction.y);
                if (mag > 1e-6) return Math.atan2(wizard.direction.y, wizard.direction.x);
            }
            return 0;
        }

        resolveInterpolatedItemWorldPosition(item, mapRef) {
            if (!item) return null;
            const interpolated = (typeof item.getInterpolatedPosition === "function")
                ? item.getInterpolatedPosition()
                : null;
            if (interpolated && Number.isFinite(interpolated.x) && Number.isFinite(interpolated.y)) {
                return { x: interpolated.x, y: interpolated.y };
            }
            const alpha = Number.isFinite(global.renderAlpha) ? Math.max(0, Math.min(1, global.renderAlpha)) : 1;
            const x = (Number.isFinite(item.prevX) && Number.isFinite(item.x))
                ? (
                    Number.isFinite(alpha) && mapRef && typeof mapRef.shortestDeltaX === "function"
                        ? (item.prevX + mapRef.shortestDeltaX(item.prevX, item.x) * alpha)
                        : (item.prevX + (item.x - item.prevX) * alpha)
                )
                : item.x;
            const y = (Number.isFinite(item.prevY) && Number.isFinite(item.y))
                ? (
                    Number.isFinite(alpha) && mapRef && typeof mapRef.shortestDeltaY === "function"
                        ? (item.prevY + mapRef.shortestDeltaY(item.prevY, item.y) * alpha)
                        : (item.prevY + (item.y - item.prevY) * alpha)
                )
                : item.y;
            if (!Number.isFinite(x) || !Number.isFinite(y)) return null;
            return { x, y };
        }

        getMountedFaceCentersForItem(item) {
            if (!item) return null;
            const explicitFaceCenters = (
                item.depthBillboardFaceCenters &&
                item.depthBillboardFaceCenters.front &&
                item.depthBillboardFaceCenters.back
            ) ? item.depthBillboardFaceCenters : null;
            if (
                explicitFaceCenters &&
                Number.isFinite(explicitFaceCenters.front.x) &&
                Number.isFinite(explicitFaceCenters.front.y) &&
                Number.isFinite(explicitFaceCenters.back.x) &&
                Number.isFinite(explicitFaceCenters.back.y)
            ) {
                return {
                    front: {
                        x: Number(explicitFaceCenters.front.x),
                        y: Number(explicitFaceCenters.front.y)
                    },
                    back: {
                        x: Number(explicitFaceCenters.back.x),
                        y: Number(explicitFaceCenters.back.y)
                    }
                };
            }
            const getFaceCenters = (typeof global.getMountedWallFaceCentersForObject === "function")
                ? global.getMountedWallFaceCentersForObject
                : null;
            if (!getFaceCenters) return null;
            const resolved = getFaceCenters(item);
            if (
                !resolved ||
                !resolved.front ||
                !resolved.back ||
                !Number.isFinite(resolved.front.x) ||
                !Number.isFinite(resolved.front.y) ||
                !Number.isFinite(resolved.back.x) ||
                !Number.isFinite(resolved.back.y)
            ) {
                return null;
            }
            return {
                front: { x: Number(resolved.front.x), y: Number(resolved.front.y) },
                back: { x: Number(resolved.back.x), y: Number(resolved.back.y) }
            };
        }

        isWallMountedSpatialItem(item) {
            if (!item) return false;
            const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
            const isDoorOrWindow = (
                category === "windows" ||
                category === "doors" ||
                item.type === "window" ||
                item.type === "door"
            );
            if (!isDoorOrWindow || item.rotationAxis !== "spatial") return false;
            if (item._prototypeMountedWallSection && item._prototypeMountedWallSection.type === "wallSection") return true;
            return !!(
                Number.isInteger(item.mountedWallSectionUnitId) ||
                Number.isInteger(item.mountedWallLineGroupId) ||
                Number.isInteger(item.mountedSectionId)
            );
        }

        resolveMountedWallSectionForItem(item) {
            if (!item) return null;
            const activeDrawFrameId = Number(this._activeDrawFrameId) || 0;
            let frameCache = null;
            if (activeDrawFrameId > 0) {
                if (this._mountedWallSectionResolveFrameId !== activeDrawFrameId) {
                    this._mountedWallSectionResolveFrameId = activeDrawFrameId;
                    this._mountedWallSectionResolveCache = new WeakMap();
                }
                frameCache = this._mountedWallSectionResolveCache instanceof WeakMap
                    ? this._mountedWallSectionResolveCache
                    : null;
                if (frameCache && frameCache.has(item)) {
                    return frameCache.get(item);
                }
            }
            let resolved = null;
            if (item._prototypeMountedWallSection && item._prototypeMountedWallSection.type === "wallSection") {
                resolved = item._prototypeMountedWallSection;
                if (frameCache) frameCache.set(item, resolved);
                return resolved;
            }
            const wallCtor = global.WallSectionUnit;
            const allSections = (wallCtor && wallCtor._allSections instanceof Map)
                ? wallCtor._allSections
                : null;
            if (!allSections) {
                if (frameCache) frameCache.set(item, null);
                return null;
            }
            const candidateIds = [
                item.mountedWallSectionUnitId,
                item.mountedSectionId,
                item.mountedWallLineGroupId
            ];
            for (let i = 0; i < candidateIds.length; i++) {
                const id = Number(candidateIds[i]);
                if (!Number.isInteger(id)) continue;
                const section = allSections.get(id) || null;
                if (section && section.type === "wallSection") {
                    resolved = section;
                    break;
                }
            }
            if (frameCache) frameCache.set(item, resolved);
            return resolved;
        }

        hideRenderItemWithMissingMountedSection(item) {
            if (!item) return;
            if (item.pixiSprite) {
                item.pixiSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                    item.pixiSprite.renderable = false;
                }
            }
            if (item.fireSprite) {
                item.fireSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(item.fireSprite, "renderable")) {
                    item.fireSprite.renderable = false;
                }
            }
            if (item._renderingDepthMesh) {
                item._renderingDepthMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(item._renderingDepthMesh, "renderable")) {
                    item._renderingDepthMesh.renderable = false;
                }
            }
            if (item._compositeUnderlayMesh) {
                item._compositeUnderlayMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(item._compositeUnderlayMesh, "renderable")) {
                    item._compositeUnderlayMesh.renderable = false;
                }
            }
            if (item._doorBottomFaceDebugGraphics) {
                item._doorBottomFaceDebugGraphics.visible = false;
                if (Object.prototype.hasOwnProperty.call(item._doorBottomFaceDebugGraphics, "renderable")) {
                    item._doorBottomFaceDebugGraphics.renderable = false;
                }
            }
        }

        getLosVisibilitySamplePointForItem(item, mapRef, observer = null) {
            if (!item) return null;
            if (
                item.isFallenDoorEffect &&
                item._losVisibilitySamplePoint &&
                Number.isFinite(item._losVisibilitySamplePoint.x) &&
                Number.isFinite(item._losVisibilitySamplePoint.y)
            ) {
                return {
                    x: Number(item._losVisibilitySamplePoint.x),
                    y: Number(item._losVisibilitySamplePoint.y)
                };
            }
            const isWallMountedSpatial = this.isWallMountedSpatialItem(item);
            if (isWallMountedSpatial) {
                const faceCenters = this.getMountedFaceCentersForItem(item);
                if (faceCenters) {
                    const refX = (observer && Number.isFinite(observer.x))
                        ? Number(observer.x)
                        : (Number.isFinite(item.x) ? Number(item.x) : 0);
                    const refY = (observer && Number.isFinite(observer.y))
                        ? Number(observer.y)
                        : (Number.isFinite(item.y) ? Number(item.y) : 0);
                    const frontDx = (mapRef && typeof mapRef.shortestDeltaX === "function")
                        ? mapRef.shortestDeltaX(refX, faceCenters.front.x)
                        : (faceCenters.front.x - refX);
                    const frontDy = (mapRef && typeof mapRef.shortestDeltaY === "function")
                        ? mapRef.shortestDeltaY(refY, faceCenters.front.y)
                        : (faceCenters.front.y - refY);
                    const backDx = (mapRef && typeof mapRef.shortestDeltaX === "function")
                        ? mapRef.shortestDeltaX(refX, faceCenters.back.x)
                        : (faceCenters.back.x - refX);
                    const backDy = (mapRef && typeof mapRef.shortestDeltaY === "function")
                        ? mapRef.shortestDeltaY(refY, faceCenters.back.y)
                        : (faceCenters.back.y - refY);
                    const frontDist2 = frontDx * frontDx + frontDy * frontDy;
                    const backDist2 = backDx * backDx + backDy * backDy;
                    const picked = frontDist2 <= backDist2 ? faceCenters.front : faceCenters.back;
                    return { x: picked.x, y: picked.y };
                }
            }
            return this.resolveInterpolatedItemWorldPosition(item, mapRef);
        }

        isWorldPointInLosShadow(worldX, worldY, wizard, mapRef = null) {
            if (!wizard || !Number.isFinite(worldX) || !Number.isFinite(worldY)) return false;
            const state = this.currentLosState;
            if (!state || !state.depth || !Number.isFinite(state.bins) || state.bins < 3) return false;
            const bins = Math.floor(state.bins);
            const depth = state.depth;
            if (!depth || depth.length !== bins) return false;
            const effectiveMap = mapRef || (wizard && wizard.map) || (this.camera && this.camera.map) || global.map || null;
            if (!this.isWorldPointInsideLosShadowLayerFloor(worldX, worldY, wizard, effectiveMap)) return false;
            const dx = (effectiveMap && typeof effectiveMap.shortestDeltaX === "function")
                ? effectiveMap.shortestDeltaX(wizard.x, worldX)
                : (worldX - wizard.x);
            const dy = (effectiveMap && typeof effectiveMap.shortestDeltaY === "function")
                ? effectiveMap.shortestDeltaY(wizard.y, worldY)
                : (worldY - wizard.y);
            const distance = Math.hypot(dx, dy);
            const theta = Math.atan2(dy, dx);
            const twoPi = Math.PI * 2;
            const norm = ((theta + Math.PI) % twoPi + twoPi) % twoPi;
            const binIdx = Math.max(0, Math.min(bins - 1, Math.floor((norm / twoPi) * bins)));
            const losDepth = Number.isFinite(depth[binIdx]) ? Number(depth[binIdx]) : Infinity;
            const losHasForwardFov = !!state.hasForwardFov;
            const losFacingAngle = Number.isFinite(state.facingAngle) ? state.facingAngle : 0;
            const losHalfFovRad = Number.isFinite(state.halfFovRad) ? state.halfFovRad : Math.PI;
            let insideFov = true;
            if (losHasForwardFov) {
                let delta = theta - losFacingAngle;
                while (delta <= -Math.PI) delta += twoPi;
                while (delta > Math.PI) delta -= twoPi;
                insideFov = Math.abs(delta) <= losHalfFovRad;
            }
            const nearReveal = insideFov ? 0 : LOS_NEAR_REVEAL_RADIUS;
            const litDistance = Math.max(nearReveal, losDepth);
            return distance > litDistance;
        }

        isWorldPointInsideLosShadowLayerFloor(worldX, worldY, wizard, mapRef = null) {
            const effectiveMap = mapRef || (wizard && wizard.map) || (this.camera && this.camera.map) || global.map || null;
            if (!effectiveMap || !(effectiveMap.floorsById instanceof Map)) return true;
            const level = this.getWizardVisualLayerIndex(wizard, 0);
            let sawLayerFloor = false;
            for (const fragment of effectiveMap.floorsById.values()) {
                if (!fragment || fragment._floorEditEmpty === true) continue;
                const fragmentLevel = Number.isFinite(fragment.level)
                    ? this.getLayerIndexFromValue(fragment.level, 0)
                    : 0;
                if (fragmentLevel !== level) continue;
                if (!Array.isArray(fragment.outerPolygon) || fragment.outerPolygon.length < 3) continue;
                sawLayerFloor = true;
                if (isPointSupportedByFloorFragment(fragment, worldX, worldY)) return true;
            }
            return level === 0 && !sawLayerFloor;
        }

        isRadialItemHiddenByLos(item, wizard, mapRef = null) {
            if (!item || !wizard) return false;
            const worldPos = this.getLosVisibilitySamplePointForItem(item, mapRef, wizard);
            if (!worldPos) return false;
            if (!this.isWorldPointInLosShadow(worldPos.x, worldPos.y, wizard, mapRef)) return false;

            const state = this.currentLosState;
            if (!state || !state.depth || !Number.isFinite(state.bins) || state.bins < 3) return true;
            const bins = Math.floor(state.bins);
            const depth = state.depth;
            if (!depth || depth.length !== bins) return true;

            const effectiveMap = mapRef || (wizard && wizard.map) || (this.camera && this.camera.map) || global.map || null;
            const dx = (effectiveMap && typeof effectiveMap.shortestDeltaX === "function")
                ? effectiveMap.shortestDeltaX(wizard.x, worldPos.x)
                : (worldPos.x - wizard.x);
            const dy = (effectiveMap && typeof effectiveMap.shortestDeltaY === "function")
                ? effectiveMap.shortestDeltaY(wizard.y, worldPos.y)
                : (worldPos.y - wizard.y);
            const dist = Math.hypot(dx, dy);
            if (dist < 0.01) return false;

            const visR = Math.max(
                Number.isFinite(item.width) ? item.width / 2 : 0,
                Number.isFinite(item.height) ? item.height / 2 : 0,
                Number.isFinite(item.radius) ? item.radius : 0,
                (item.shadowBox && Number.isFinite(item.shadowBox.radius))
                    ? item.shadowBox.radius : 0,
                Number.isFinite(item.visualRadius) ? item.visualRadius : 0
            );
            if (visR <= 0) return true;

            const halfSpan = Math.asin(Math.min(1, visR / dist));
            const centerAngle = Math.atan2(dy, dx);
            const twoPi = Math.PI * 2;
            const a0 = centerAngle - halfSpan;
            const a1 = centerAngle + halfSpan;
            const norm0 = ((a0 + Math.PI) % twoPi + twoPi) % twoPi;
            const norm1 = ((a1 + Math.PI) % twoPi + twoPi) % twoPi;
            const bin0 = Math.max(0, Math.min(bins - 1, Math.floor((norm0 / twoPi) * bins)));
            const bin1 = Math.max(0, Math.min(bins - 1, Math.floor((norm1 / twoPi) * bins)));
            const spanBins = ((bin1 - bin0 + bins) % bins) || 1;

            for (let i = 0; i <= spanBins; i++) {
                const b = (bin0 + i) % bins;
                const d = Number.isFinite(depth[b]) ? depth[b] : Infinity;
                if (d >= dist) return false;
            }
            return true;
        }

        isPlacedObjectEntity(item) {
            return !!(
                item &&
                (item.isPlacedObject || item.objectType === "placedObject" || item.type === "placedObject")
            );
        }

        isPlaceObjectRenderDebugActive(item = null) {
            if (!global || global.debugPlaceObjectRenderTrace !== true) return false;
            if (!item) return true;
            if (!this.isPlacedObjectEntity(item)) return false;
            if (!Number.isFinite(item._placeObjectDebugTraceUntilMs)) return false;
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            return nowMs <= Number(item._placeObjectDebugTraceUntilMs);
        }

        logPlaceObjectRenderDebug(eventName, item = null, payload = {}) {
            if (!this.isPlaceObjectRenderDebugActive(item)) return;
            const logger = (global && typeof global.logPlaceObjectDebug === "function")
                ? global.logPlaceObjectDebug
                : ((global && global.console && typeof global.console.log === "function")
                    ? (name, data) => global.console.log("[PlaceObject]", name, data)
                    : null);
            if (!logger) return;
            logger(`render-${eventName}`, {
                debugId: item && typeof item._placeObjectDebugId === "string" ? item._placeObjectDebugId : "",
                texturePath: item && typeof item.texturePath === "string" ? item.texturePath : "",
                category: item && typeof item.category === "string" ? item.category : "",
                type: item && typeof item.type === "string" ? item.type : "",
                x: item && Number.isFinite(item.x) ? Number(item.x) : null,
                y: item && Number.isFinite(item.y) ? Number(item.y) : null,
                z: item && Number.isFinite(item.z) ? Number(item.z) : null,
                traversalLayer: item && Number.isFinite(item.traversalLayer) ? Number(item.traversalLayer) : null,
                level: item && Number.isFinite(item.level) ? Number(item.level) : null,
                renderLayerIndex: item && Number.isFinite(item._renderLayerIndex) ? Number(item._renderLayerIndex) : null,
                renderLayerAlpha: item && Number.isFinite(item._renderLayerAlpha) ? Number(item._renderLayerAlpha) : null,
                renderLayerBaseZ: item && Number.isFinite(item._renderLayerBaseZ) ? Number(item._renderLayerBaseZ) : null,
                node: item && item.node && typeof global.describePlaceObjectNode === "function"
                    ? global.describePlaceObjectNode(item.node)
                    : null,
                sprite: item && item.pixiSprite ? {
                    visible: item.pixiSprite.visible !== false,
                    renderable: item.pixiSprite.renderable !== false,
                    alpha: Number.isFinite(item.pixiSprite.alpha) ? Number(item.pixiSprite.alpha) : null,
                    parentName: item.pixiSprite.parent && item.pixiSprite.parent.name ? item.pixiSprite.parent.name : "",
                    x: Number.isFinite(item.pixiSprite.x) ? Number(item.pixiSprite.x) : null,
                    y: Number.isFinite(item.pixiSprite.y) ? Number(item.pixiSprite.y) : null,
                    width: Number.isFinite(item.pixiSprite.width) ? Number(item.pixiSprite.width) : null,
                    height: Number.isFinite(item.pixiSprite.height) ? Number(item.pixiSprite.height) : null
                } : null,
                depthMesh: item && item._renderingDepthMesh ? {
                    visible: item._renderingDepthMesh.visible !== false,
                    renderable: item._renderingDepthMesh.renderable !== false,
                    alpha: Number.isFinite(item._renderingDepthMesh.alpha) ? Number(item._renderingDepthMesh.alpha) : null,
                    parentName: item._renderingDepthMesh.parent && item._renderingDepthMesh.parent.name ? item._renderingDepthMesh.parent.name : ""
                } : null,
                ...payload
            });
        }

        forEachWrappedNodeInViewport(mapRef, xPadding, yPadding, callback, cameraOverride = null) {
            if (!mapRef || typeof callback !== "function") return;
            if (typeof mapRef.getVisibleNodesInViewport === "function") {
                const nodes = mapRef.getVisibleNodesInViewport(cameraOverride || this.camera || {}, xPadding, yPadding);
                for (let i = 0; i < nodes.length; i++) {
                    if (nodes[i]) callback(nodes[i]);
                }
                return;
            }
            const camera = cameraOverride || this.camera || {};
            const viewportRef = global.viewport || null;
            const cameraWidth = Number.isFinite(camera.width)
                ? camera.width
                : (viewportRef && Number.isFinite(viewportRef.width) ? viewportRef.width : 0);
            const cameraHeight = Number.isFinite(camera.height)
                ? camera.height
                : (viewportRef && Number.isFinite(viewportRef.height) ? viewportRef.height : 0);
            const padX = Math.max(0, Number.isFinite(xPadding) ? Math.floor(xPadding) : 0);
            const padY = Math.max(0, Number.isFinite(yPadding) ? Math.floor(yPadding) : 0);
            const xScale = 0.866;
            const xStart = Math.floor(camera.x / xScale) - padX;
            const xEnd = Math.ceil((camera.x + cameraWidth) / xScale) + padX;
            const yStart = Math.floor(camera.y) - padY;
            const yEnd = Math.ceil(camera.y + cameraHeight) + padY;
            const xRanges = (typeof global.getWrappedIndexRanges === "function")
                ? global.getWrappedIndexRanges(xStart, xEnd, mapRef.width, mapRef.wrapX)
                : [];
            const yRanges = (typeof global.getWrappedIndexRanges === "function")
                ? global.getWrappedIndexRanges(yStart, yEnd, mapRef.height, mapRef.wrapY)
                : [];
            if (xRanges.length === 0 || yRanges.length === 0) return;

            yRanges.forEach(yRange => {
                for (let y = yRange.start; y <= yRange.end; y++) {
                    xRanges.forEach(xRange => {
                        for (let x = xRange.start; x <= xRange.end; x++) {
                            const node = mapRef.nodes[x] && mapRef.nodes[x][y] ? mapRef.nodes[x][y] : null;
                            if (node) callback(node);
                        }
                    });
                }
            });
        }

        getVisibleNodeRenderKey(node) {
            if (!node) return "";
            if (typeof node.id === "string" && node.id.length > 0) return node.id;
            const xindex = Number.isFinite(node.xindex) ? Number(node.xindex) : 0;
            const yindex = Number.isFinite(node.yindex) ? Number(node.yindex) : 0;
            const layer = Number.isFinite(node.traversalLayer) ? Number(node.traversalLayer) : 0;
            if (
                typeof node._renderingVisibleNodeKey === "string" &&
                node._renderingVisibleNodeKeyX === xindex &&
                node._renderingVisibleNodeKeyY === yindex &&
                node._renderingVisibleNodeKeyLayer === layer
            ) {
                return node._renderingVisibleNodeKey;
            }
            const key = `${xindex},${yindex},${layer}`;
            node._renderingVisibleNodeKey = key;
            node._renderingVisibleNodeKeyX = xindex;
            node._renderingVisibleNodeKeyY = yindex;
            node._renderingVisibleNodeKeyLayer = layer;
            return key;
        }

        resolvePlacedObjectLodTexturePath(item) {
            if (!item || !this.isPlacedObjectEntity(item)) return null;
            const basePath = (typeof item.texturePath === "string" && item.texturePath.length > 0)
                ? item.texturePath
                : null;
            const lodList = Array.isArray(item.lodTextures) ? item.lodTextures : null;
            if (!lodList || lodList.length === 0) return basePath;
            const itemWidthWorld = Math.max(0.01, Number.isFinite(item.width) ? Number(item.width) : 1);
            const itemHeightWorld = Math.max(0.01, Number.isFinite(item.height) ? Number(item.height) : 1);
            const rotationAxis = (typeof item.rotationAxis === "string") ? item.rotationAxis : "visual";
            const yIsoScale = Math.max(0.0001, Math.abs(Number.isFinite(this.camera.xyratio) ? this.camera.xyratio : 0.66));
            const screenWidthPx = itemWidthWorld * this.camera.viewscale;
            const screenHeightPx = (rotationAxis === "spatial")
                ? (itemHeightWorld * this.camera.viewscale)
                : (itemHeightWorld * this.camera.viewscale * yIsoScale);
            const sizeMetric = Math.max(screenWidthPx, screenHeightPx);

            for (let i = 0; i < lodList.length; i++) {
                const entry = lodList[i];
                if (!entry || typeof entry.texturePath !== "string" || entry.texturePath.length === 0) continue;
                const maxSize = Number.isFinite(entry.maxDistance) ? Number(entry.maxDistance) : Infinity;
                if (sizeMetric <= maxSize) return entry.texturePath;
            }
            return basePath || (lodList[lodList.length - 1] && lodList[lodList.length - 1].texturePath) || null;
        }

        resolvePowerupLodTexturePath(item) {
            if (!item) return null;
            const basePath = (typeof item.imagePath === "string" && item.imagePath.length > 0)
                ? item.imagePath
                : null;
            const lodList = Array.isArray(item.lodTextures) ? item.lodTextures : null;
            if (!lodList || lodList.length === 0) return basePath;
            const itemWidthWorld = Math.max(0.01, Number.isFinite(item.width) ? Number(item.width) : 1);
            const itemHeightWorld = Math.max(0.01, Number.isFinite(item.height) ? Number(item.height) : 1);
            const sizeMetric = Math.max(
                itemWidthWorld * this.camera.viewscale,
                itemHeightWorld * this.camera.viewscale
            );

            for (let i = 0; i < lodList.length; i++) {
                const entry = lodList[i];
                if (!entry || typeof entry.texturePath !== "string" || entry.texturePath.length === 0) continue;
                const maxSize = Number.isFinite(entry.maxDistance) ? Number(entry.maxDistance) : Infinity;
                if (sizeMetric <= maxSize) return entry.texturePath;
            }
            return basePath || (lodList[lodList.length - 1] && lodList[lodList.length - 1].texturePath) || null;
        }

        applySpriteTransform(item) {
            if (!item || !item.pixiSprite) return;
            if (item.dead && typeof item.tickDeadFire === "function") {
                item.tickDeadFire();
            }
            if (item.dead && typeof item.tickDeathAnimation === "function") {
                item.tickDeathAnimation();
            }
            if (typeof item._syncFireVisualState === "function") {
                item._syncFireVisualState();
            }
            const interpolatedWorld = (typeof item.getInterpolatedPosition === "function")
                ? item.getInterpolatedPosition()
                : null;
            const mapRef = this.camera.map || global.map || null;
            const alpha = Number.isFinite(global.renderAlpha) ? Math.max(0, Math.min(1, global.renderAlpha)) : 1;
            const fallbackWorldX = (item && Number.isFinite(item.prevX) && Number.isFinite(item.x))
                ? (
                    Number.isFinite(alpha) && mapRef && typeof mapRef.shortestDeltaX === "function"
                        ? (item.prevX + mapRef.shortestDeltaX(item.prevX, item.x) * alpha)
                        : (item.prevX + (item.x - item.prevX) * alpha)
                )
                : item.x;
            const fallbackWorldY = (item && Number.isFinite(item.prevY) && Number.isFinite(item.y))
                ? (
                    Number.isFinite(alpha) && mapRef && typeof mapRef.shortestDeltaY === "function"
                        ? (item.prevY + mapRef.shortestDeltaY(item.prevY, item.y) * alpha)
                        : (item.prevY + (item.y - item.prevY) * alpha)
                )
                : item.y;
            const drawX = (
                interpolatedWorld &&
                Number.isFinite(interpolatedWorld.x) &&
                Number.isFinite(interpolatedWorld.y)
            ) ? interpolatedWorld.x : fallbackWorldX;
            const drawY = (
                interpolatedWorld &&
                Number.isFinite(interpolatedWorld.x) &&
                Number.isFinite(interpolatedWorld.y)
            ) ? interpolatedWorld.y : fallbackWorldY;
            const drawZ = (
                interpolatedWorld &&
                Number.isFinite(interpolatedWorld.z)
            )
                ? interpolatedWorld.z
                : (Number.isFinite(item.z) ? Number(item.z) : 0);
            const layerBaseZ = this.getLayerBaseZForObject(item, 0);
            const coors = this.camera.worldToScreen(drawX, drawY, drawZ + layerBaseZ);
            item.pixiSprite.x = coors.x;
            item.pixiSprite.y = coors.y;

            if (typeof global.ensureSpriteFrames === "function") {
                global.ensureSpriteFrames(item);
            }
            if (item.spriteFrames && item.pixiSprite) {
                const rowIndex = typeof item.getDirectionRow === "function" ? item.getDirectionRow() : 0;
                const safeRow = Math.max(0, Math.min(rowIndex, (item.spriteRows || 1) - 1));
                const safeCol = Math.max(0, Math.min(item.spriteCol || 0, (item.spriteCols || 1) - 1));
                const rowFrames = item.spriteFrames[safeRow] || item.spriteFrames[0];
                const nextTexture = rowFrames && (rowFrames[safeCol] || rowFrames[0]);
                if (nextTexture) item.pixiSprite.texture = nextTexture;
            }

            const spriteTexture = item.pixiSprite.texture || null;
            const nativeTexW = spriteTexture && Number.isFinite(spriteTexture.width) ? Number(spriteTexture.width) : null;
            const nativeTexH = spriteTexture && Number.isFinite(spriteTexture.height) ? Number(spriteTexture.height) : null;
            const frameScale = (typeof item.getSpriteFrameScale === "function")
                ? item.getSpriteFrameScale()
                : null;
            const frameScaleWidth = Math.max(
                0.01,
                frameScale && Number.isFinite(frameScale.width) ? Number(frameScale.width) : 1
            );
            const frameScaleHeight = Math.max(
                0.01,
                frameScale && Number.isFinite(frameScale.height) ? Number(frameScale.height) : 1
            );
            const useNativeLodSize = !!(
                global.debugUseLodNativePixelSize &&
                this.isPlacedObjectEntity(item) &&
                item.rotationAxis !== "spatial" &&
                Number.isFinite(nativeTexW) &&
                Number.isFinite(nativeTexH)
            );

            if (this.isPlacedObjectEntity(item) && item.rotationAxis !== "spatial" && item.pixiSprite instanceof PIXI.Sprite) {
                const lodTexturePath = this.resolvePlacedObjectLodTexturePath(item);
                if (typeof lodTexturePath === "string" && lodTexturePath.length > 0 && lodTexturePath !== item._activeLodTexturePath) {
                    item.pixiSprite.texture = PIXI.Texture.from(lodTexturePath);
                    item._activeLodTexturePath = lodTexturePath;
                }
            }

            let targetWidth = 0;
            let targetHeight = 0;
            if (item.type === "road" || item.type === "roadPath") {
                targetWidth = (item.width || 1) * this.camera.viewscale * 1.1547;
                targetHeight = (item.height || 1) * this.camera.viewscale * this.camera.xyratio;
            } else if (item.rotationAxis === "ground") {
                targetWidth = (item.width || 1) * this.camera.viewscale;
                targetHeight = (item.height || 1) * this.camera.viewscale;
            } else if (useNativeLodSize) {
                targetWidth = nativeTexW;
                targetHeight = nativeTexH;
            } else {
                targetWidth = (item.width || 1) * this.camera.viewscale;
                targetHeight = (item.height || 1) * this.camera.viewscale;
            }
            item.pixiSprite.width = targetWidth * frameScaleWidth;
            item.pixiSprite.height = targetHeight * frameScaleHeight;

            if (item.dead && item.pixiSprite.anchor) {
                // For items with a gradual death fall animation (e.g. Blodia), keep the
                // default foot anchor so the depth billboard bottomZ stays at ground level.
                if (!item._useGradualDeathFall) {
                    // Flip around the sprite midline (y=0.5), not the default foot anchor.
                    item.pixiSprite.anchor.set(0.5, 0.5);
                    item.pixiSprite.y = coors.y - (item.pixiSprite.height * 0.5);
                }
            }

            if (!this.shouldUseDepthBillboard(item) && item.fireSprite) {
                const shouldShowFire = !!(item.isOnFire && this.isScriptVisible(item) && !item.gone && !item.vanishing);
                if (shouldShowFire) {
                    const fireContainer = this.isCharacterRenderItem(item)
                        ? (this.getCharacterLayer() || item.pixiSprite.parent || null)
                        : (this.layers.entities || item.pixiSprite.parent || null);
                    if (fireContainer && item.fireSprite.parent !== fireContainer) {
                        fireContainer.addChild(item.fireSprite);
                    }
                    if (item.fireSprite.anchor) item.fireSprite.anchor.set(0.5, 1);
                    item.fireSprite.x = item.pixiSprite.x;
                    // For trees, flames grow as HP is lost: scale = min(maxHP / hp, 4)
                    const _fireScale = (item.type === 'tree')
                        ? (Number.isFinite(item._renderedFireIntensityScale)
                            ? item._renderedFireIntensityScale
                            : (item.maxHP > 0 && item.hp > 0 ? Math.min(item.maxHP / item.hp, 4) : 1))
                        : 1;
                    item.fireSprite.width = item.pixiSprite.width * 1.6 * _fireScale;
                    item.fireSprite.height = item.pixiSprite.height * 1.2 * _fireScale;
                    if (item.type === "flower") {
                        const hostAnchorY = (item.pixiSprite.anchor && Number.isFinite(item.pixiSprite.anchor.y))
                            ? Number(item.pixiSprite.anchor.y)
                            : 1;
                        const hostTopY = item.pixiSprite.y - (item.pixiSprite.height * hostAnchorY);
                        item.fireSprite.y = hostTopY + (item.pixiSprite.height * 0.5) + (item.fireSprite.height * 0.12);
                    } else {
                        // Place fire bottom at the top of the host sprite, compensating for its anchor.
                        const _sprAnchorY = (item.pixiSprite.anchor && Number.isFinite(item.pixiSprite.anchor.y))
                            ? item.pixiSprite.anchor.y : 1;
                        item.fireSprite.y = item.pixiSprite.y - item.pixiSprite.height * _sprAnchorY;
                    }
                    item.fireSprite.visible = true;
                    if (Object.prototype.hasOwnProperty.call(item.fireSprite, "renderable")) {
                        item.fireSprite.renderable = true;
                    }
                    this.applyLayerDarknessForItem(item, item._renderLayerIndex, item.fireSprite);
                } else {
                    item.fireSprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(item.fireSprite, "renderable")) {
                        item.fireSprite.renderable = false;
                    }
                }
            }

            const visualRotationBase = (item && item.dead)
                ? (Number.isFinite(item.rotation) ? item.rotation : 180)
                : ((item && item.rotationAxis === "none")
                    ? 0
                    : Number.isFinite(item.placementRotation)
                        ? item.placementRotation
                        : item.rotation);
            const visualRotationOffset = (item && typeof item.getAdditionalSpriteRotationDegrees === "function")
                ? Number(item.getAdditionalSpriteRotationDegrees()) || 0
                : 0;
            const visualRotation = (Number(visualRotationBase) || 0) + visualRotationOffset;
            item.pixiSprite.rotation = visualRotation ? (visualRotation * (Math.PI / 180)) : 0;
            const layerAlpha = Number.isFinite(item._renderLayerAlpha)
                ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                : 1;
            item.pixiSprite.alpha = this.getScriptDisplayAlpha(item) * layerAlpha;
            this.applyLayerDarknessForItem(item, item._renderLayerIndex, item.pixiSprite);
        }

        getRoofsList(ctx) {
            const fromCtx = Array.isArray(ctx && ctx.roofs) ? ctx.roofs : null;
            if (fromCtx) return fromCtx;
            if (Array.isArray(global.roofs)) return global.roofs;
            const legacy = global.roof || null;
            return legacy ? [legacy] : [];
        }

        isWorldPointUnderRoof(worldX, worldY, ctx = null) {
            if (!Number.isFinite(worldX) || !Number.isFinite(worldY)) return false;
            const roofList = this.getRoofsList(ctx);
            if (!Array.isArray(roofList) || roofList.length === 0) return false;

            for (let i = 0; i < roofList.length; i++) {
                const roofRef = roofList[i];
                if (!roofRef || roofRef.gone || !roofRef.placed) continue;
                if (!this.isScriptVisible(roofRef)) continue;
                const roofInteriorHitbox = (
                    roofRef.interiorHideHitbox &&
                    typeof roofRef.interiorHideHitbox.containsPoint === "function"
                ) ? roofRef.interiorHideHitbox : roofRef.shadowBox;
                if (!roofInteriorHitbox || typeof roofInteriorHitbox.containsPoint !== "function") continue;
                if (roofInteriorHitbox.containsPoint(worldX, worldY)) {
                    return true;
                }
            }
            return false;
        }

        updateRoofPreview(roof, wizardRef) {
            if (!roof) return;
            if (!roof.pixiMesh) {
                roof.createPixiMesh();
                if (roof.pixiMesh && roof.pixiMesh.parent) {
                    roof.pixiMesh.parent.removeChild(roof.pixiMesh);
                }
            }

            if (!roof.pixiMesh) return;
            roof.currentAlpha = 1;
            const layerAlpha = Number.isFinite(roof._renderLayerAlpha)
                ? Math.max(0, Math.min(1, Number(roof._renderLayerAlpha)))
                : 1;
            roof.pixiMesh.alpha = roof.currentAlpha * layerAlpha;
            roof.pixiMesh.visible = !!roof.placed && (roof.currentAlpha * layerAlpha) > 0.01;
            this.applyLayerDarknessToDisplayObject(
                roof.pixiMesh,
                this.getLayerDarknessMultiplier(roof._renderLayerIndex)
            );

            if (roof.placed) {
                const baseZ = Number.isFinite(roof.z)
                    ? Number(roof.z)
                    : (Number.isFinite(roof.heightFromGround) ? Number(roof.heightFromGround) : 0);
                const usesDepthShader = !!(
                    roof.pixiMesh &&
                    roof.pixiMesh._usesRoofDepthShader &&
                    Array.isArray(roof.pixiMesh._roofDepthUniforms)
                );
                if (usesDepthShader) {
                    const uniformsList = roof.pixiMesh._roofDepthUniforms;
                    const appRef = (typeof app !== "undefined" && app) ? app : (global.app || null);
                    const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width))
                        ? Number(appRef.screen.width)
                        : 1;
                    const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height))
                        ? Number(appRef.screen.height)
                        : 1;
                    const roofCtor = (typeof Roof !== "undefined") ? Roof : null;
                    const nearMetric = Number.isFinite(roofCtor && roofCtor.DEPTH_NEAR_METRIC)
                        ? Number(roofCtor.DEPTH_NEAR_METRIC)
                        : -128;
                    const farMetric = Number.isFinite(roofCtor && roofCtor.DEPTH_FAR_METRIC)
                        ? Number(roofCtor.DEPTH_FAR_METRIC)
                        : 256;
                    const depthSpanInv = 1 / Math.max(1e-6, farMetric - nearMetric);
                    const mapRef = roof.map || this.camera.map || global.map || null;
                    const worldW = (mapRef && Number.isFinite(mapRef.worldWidth) && mapRef.worldWidth > 0)
                        ? Number(mapRef.worldWidth)
                        : 0;
                    const worldH = (mapRef && Number.isFinite(mapRef.worldHeight) && mapRef.worldHeight > 0)
                        ? Number(mapRef.worldHeight)
                        : 0;
                    const wrapX = (mapRef && mapRef.wrapX !== false) ? 1 : 0;
                    const wrapY = (mapRef && mapRef.wrapY !== false) ? 1 : 0;
                    for (let i = 0; i < uniformsList.length; i++) {
                        const u = uniformsList[i];
                        if (!u) continue;
                        u.uScreenSize[0] = Math.max(1, screenW);
                        u.uScreenSize[1] = Math.max(1, screenH);
                        u.uCameraWorld[0] = Number(this.camera.x) || 0;
                        u.uCameraWorld[1] = Number(this.camera.y) || 0;
                        u.uCameraZ = Number(this.camera.z) || 0;
                        u.uViewScale = Number(this.camera.viewscale) || 1;
                        u.uXyRatio = Number(this.camera.xyratio) || 1;
                        u.uDepthRange[0] = farMetric;
                        u.uDepthRange[1] = depthSpanInv;
                        u.uModelOrigin[0] = Number(roof.x) || 0;
                        u.uModelOrigin[1] = Number(roof.y) || 0;
                        u.uModelOrigin[2] = baseZ;
                        u.uWorldSize[0] = worldW;
                        u.uWorldSize[1] = worldH;
                        u.uWrapEnabled[0] = wrapX;
                        u.uWrapEnabled[1] = wrapY;
                        u.uWrapAnchorWorld[0] = Number(roof.x) || 0;
                        u.uWrapAnchorWorld[1] = Number(roof.y) || 0;
                        u.uTint[3] = (Number.isFinite(roof.currentAlpha) ? Number(roof.currentAlpha) : 1) * layerAlpha;
                    }
                    roof.pixiMesh.x = 0;
                    roof.pixiMesh.y = 0;
                    roof.pixiMesh.scale.set(1, 1);
                    roof.pixiMesh.alpha = 1;
                } else {
                    const roofCoords = this.camera.worldToScreen(roof.x, roof.y, 0);
                    const baseYOffsetPx = baseZ * this.camera.viewscale * this.camera.xyratio;
                    roof.pixiMesh.x = roofCoords.x;
                    roof.pixiMesh.y = roofCoords.y - baseYOffsetPx;
                    roof.pixiMesh.scale.set(this.camera.viewscale, this.camera.viewscale);
                }
            }
        }

        _inferWallLoopSectionIds(roof, wallCtor) {
            const poly = roof.interiorHidePolygonPoints;
            if (
                !Array.isArray(poly) || poly.length < 3 ||
                !(wallCtor && wallCtor._allSections instanceof Map)
            ) {
                return [];
            }
            // Wall section endpoints lie at polygon vertices (exact grid coords).
            // Use a generous epsilon to survive save/load float rounding.
            const EPS = 0.5;
            const epsSq = EPS * EPS;
            const isAtVertex = (px, py) => {
                for (let vi = 0; vi < poly.length; vi++) {
                    const v = poly[vi];
                    const dx = px - Number(v.x);
                    const dy = py - Number(v.y);
                    if (dx * dx + dy * dy <= epsSq) return true;
                }
                return false;
            };
            const ids = [];
            wallCtor._allSections.forEach((section) => {
                if (!section || section.gone || section.vanishing) return;
                if (!section.startPoint || !section.endPoint || !Number.isInteger(section.id)) return;
                const sx = Number(section.startPoint.x);
                const sy = Number(section.startPoint.y);
                const ex = Number(section.endPoint.x);
                const ey = Number(section.endPoint.y);
                if (!Number.isFinite(sx) || !Number.isFinite(sy) ||
                    !Number.isFinite(ex) || !Number.isFinite(ey)) return;
                if (isAtVertex(sx, sy) && isAtVertex(ex, ey)) {
                    ids.push(section.id);
                }
            });
            return ids;
        }

        isLosOccluder(item) {
            if (!item || !item.shadowBox) return false;
            if (item.type === "road" || item.type === "roadPath" || item.type === "firewall" || item.type === "roof") return false;
            if (typeof item.hasShadow === "boolean" && !item.hasShadow) return false;
            if (typeof item.castsLosShadows === "boolean" && !item.castsLosShadows) return false;
            const isAnimal = (typeof Animal !== "undefined" && item instanceof Animal);
            if (isAnimal) return false;
            return true;
        }

        getLosObjectId(item) {
            if (!item) return 0;
            if (!Number.isInteger(item._losObjectId)) {
                item._losObjectId = this.nextLosObjectId++;
            }
            return item._losObjectId;
        }

        computeLosCandidateHash(candidates) {
            let xor = 0;
            let sum = 0;
            for (let i = 0; i < candidates.length; i++) {
                const id = this.getLosObjectId(candidates[i]) >>> 0;
                xor = (xor ^ id) >>> 0;
                sum = (sum + ((id * 2654435761) >>> 0)) >>> 0;
            }
            return (xor ^ sum) >>> 0;
        }

        clearLosStateDebug() {
            this.currentLosState = null;
            this.lastLosWizardX = null;
            this.lastLosWizardY = null;
            this.lastLosFacingAngle = null;
            this.lastLosCandidateCount = -1;
            this.lastLosCandidateHash = 0;
            this.lastLosComputeAtMs = 0;
            global.losDebugVisibleObjects = [];
            global.losDebugLastMs = 0;
            global.losDebugBreakdown = {
                buildMs: 0,
                traceMs: 0,
                totalMs: 0,
                recomputed: false,
                candidates: 0
            };
        }

        updateLosState(ctx, visibleNodes, visibleObjectsOverride = null) {
            const wizard = ctx && ctx.wizard ? ctx.wizard : null;
            const losSystem = (typeof LOSSystem !== "undefined") ? LOSSystem : global.LOSSystem;
            if (!wizard || !losSystem || typeof losSystem.computeState !== "function") {
                this.clearLosStateDebug();
                return;
            }

            const omnivisionActive = this.isOmnivisionActive(wizard);
            if (omnivisionActive) {
                this.clearLosStateDebug();
                return;
            }

            const losBuildStartMs = performance.now();
            const visibleObjects = Array.isArray(visibleObjectsOverride)
                ? visibleObjectsOverride
                : this.collectVisibleObjects(visibleNodes, ctx);
            const losNowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : Date.now();
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const wizardLayerBaseZ = this.getWizardVisualLayerBaseZ(wizard, wizardLayer);
            const losCandidates = [];
            for (let i = 0; i < visibleObjects.length; i++) {
                const obj = visibleObjects[i];
                if (!obj || obj === wizard || obj.gone || obj.vanishing) continue;
                const objLayer = this.getLayerIndexForObject(obj, this.isLosMazeModeEnabled() ? 0 : wizardLayer);
                if (objLayer !== wizardLayer) continue;
                this.updateSinkAnimation(obj, losNowMs);
                if (this.isLosOccluder(obj)) losCandidates.push(obj);
            }
            const losBuildMs = performance.now() - losBuildStartMs;

            const candidateCount = losCandidates.length;
            const candidateHash = this.computeLosCandidateHash(losCandidates);
            const facingAngle = this.getWizardFacingAngleRad(wizard);
            const mapRef = ctx && ctx.map ? ctx.map : (wizard.map || null);
            const movedDx = (mapRef && typeof mapRef.shortestDeltaX === "function" && Number.isFinite(this.lastLosWizardX))
                ? mapRef.shortestDeltaX(this.lastLosWizardX, wizard.x)
                : (Number.isFinite(this.lastLosWizardX) ? (wizard.x - this.lastLosWizardX) : Infinity);
            const movedDy = (mapRef && typeof mapRef.shortestDeltaY === "function" && Number.isFinite(this.lastLosWizardY))
                ? mapRef.shortestDeltaY(this.lastLosWizardY, wizard.y)
                : (Number.isFinite(this.lastLosWizardY) ? (wizard.y - this.lastLosWizardY) : Infinity);
            const movedDist = Math.hypot(movedDx, movedDy);
            const facingDelta = Number.isFinite(this.lastLosFacingAngle)
                ? Math.abs(Math.atan2(Math.sin(facingAngle - this.lastLosFacingAngle), Math.cos(facingAngle - this.lastLosFacingAngle)))
                : Infinity;
            const structuralChange = (
                !this.currentLosState ||
                candidateCount !== this.lastLosCandidateCount ||
                candidateHash !== this.lastLosCandidateHash ||
                wizardLayer !== this.lastLosWizardLayer ||
                wizardLayerBaseZ !== this.lastLosWizardBaseZ
            );
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : performance.now();
            const timeSinceLastLosMs = Number.isFinite(this.lastLosComputeAtMs) ? (nowMs - this.lastLosComputeAtMs) : Infinity;
            const shouldRecomputeLos = (
                structuralChange ||
                movedDist > 0.03 ||
                facingDelta > 0.05 ||
                timeSinceLastLosMs >= LOS_THROTTLE_MS
            );

            let losTraceMs = 0;
            if (shouldRecomputeLos) {
                const mazeMode = this.isLosMazeModeEnabled();
                const losForwardFovDegreesRaw = Number(this.getLosVisualSetting("forwardFovDegrees", 200));
                const losForwardFovDegrees = mazeMode
                    ? 360
                    : (
                        Number.isFinite(losForwardFovDegreesRaw)
                            ? Math.max(0, Math.min(360, losForwardFovDegreesRaw))
                            : 200
                    );
                this.currentLosState = losSystem.computeState(wizard, losCandidates, {
                    bins: LOS_BINS,
                    facingAngle,
                    fovDegrees: losForwardFovDegrees,
                    mazeMode
                });
                if (this.currentLosState) {
                    this.currentLosState.viewerLayer = wizardLayer;
                    this.currentLosState.viewerBaseZ = wizardLayerBaseZ;
                }
                losTraceMs = Number.isFinite(this.currentLosState && this.currentLosState.elapsedMs)
                    ? Number(this.currentLosState.elapsedMs)
                    : 0;
                this.lastLosWizardX = wizard.x;
                this.lastLosWizardY = wizard.y;
                this.lastLosFacingAngle = facingAngle;
                this.lastLosWizardLayer = wizardLayer;
                this.lastLosWizardBaseZ = wizardLayerBaseZ;
                this.lastLosCandidateCount = candidateCount;
                this.lastLosCandidateHash = candidateHash;
                this.lastLosComputeAtMs = nowMs;
            }

            global.losDebugVisibleObjects = (this.currentLosState && Array.isArray(this.currentLosState.visibleObjects))
                ? this.currentLosState.visibleObjects
                : [];
            global.losDebugLastMs = losBuildMs + losTraceMs;
            global.losDebugBreakdown = {
                buildMs: losBuildMs,
                traceMs: losTraceMs,
                totalMs: losBuildMs + losTraceMs,
                recomputed: shouldRecomputeLos,
                candidates: candidateCount
            };
            this.setFrameMetric("losCandidates", candidateCount);
            this.setFrameMetric("losBuildMs", losBuildMs);
            this.setFrameMetric("losTraceMs", losTraceMs);
            this.setFrameMetric("losTotalMs", losBuildMs + losTraceMs);
            this.setFrameMetric("losRecomputed", shouldRecomputeLos ? 1 : 0);
            this.setFrameMetric(
                "losVisibleObjects",
                (this.currentLosState && Array.isArray(this.currentLosState.visibleObjects))
                    ? this.currentLosState.visibleObjects.length
                    : 0
            );
        }

        updateWallLosIlluminationTallies(ctx) {
            const wallCtor = global.WallSectionUnit;
            const allSections = (wallCtor && wallCtor._allSections instanceof Map)
                ? wallCtor._allSections
                : null;
            if (!allSections) return;

            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const functionStartMs = diagnosticsEnabled ? performance.now() : 0;
            let resetSections = 0;
            let illuminatedBins = 0;
            let rangedSections = 0;
            let endpointOwnerLookups = 0;
            let endpointOwnersResolved = 0;

            for (const section of allSections.values()) {
                if (!section || typeof section.resetLosIlluminationTally !== "function") continue;
                section.resetLosIlluminationTally();
                resetSections += 1;
            }

            const wizard = ctx && ctx.wizard ? ctx.wizard : null;
            const mazeMode = this.isLosMazeModeEnabled() && !this.isOmnivisionActive(wizard);
            if (!mazeMode) return;

            const state = this.currentLosState;
            if (!wizard || !state || !Array.isArray(state.owner) || !state.depth || !Number.isFinite(state.bins)) return;

            const bins = Math.max(1, Math.floor(state.bins));
            if (bins <= 0) return;
            const minAngle = Number.isFinite(state.minAngle) ? state.minAngle : -Math.PI;
            const twoPi = Math.PI * 2;
            const mapRef = (wizard && wizard.map) || global.map || null;

            const angleToBinIndex = theta => {
                const relative = ((theta - minAngle) % twoPi + twoPi) % twoPi;
                const rawIndex = Math.floor((relative / twoPi) * bins);
                if (rawIndex < 0) return 0;
                if (rawIndex >= bins) return bins - 1;
                return rawIndex;
            };

            const collectEndpointOwners = endpoint => {
                if (!endpoint || !Number.isFinite(endpoint.x) || !Number.isFinite(endpoint.y)) return [];
                endpointOwnerLookups += 1;
                const dx = (mapRef && typeof mapRef.shortestDeltaX === "function")
                    ? mapRef.shortestDeltaX(wizard.x, endpoint.x)
                    : (endpoint.x - wizard.x);
                const dy = (mapRef && typeof mapRef.shortestDeltaY === "function")
                    ? mapRef.shortestDeltaY(wizard.y, endpoint.y)
                    : (endpoint.y - wizard.y);
                if (!Number.isFinite(dx) || !Number.isFinite(dy)) return [];
                const endpointDistance = Math.hypot(dx, dy);
                const theta = Math.atan2(dy, dx);
                const centerBin = angleToBinIndex(theta);
                const endpointSampleRadius = 0.8;
                const angularTolerance = endpointDistance > 1e-6
                    ? Math.atan2(endpointSampleRadius, endpointDistance)
                    : Math.PI;
                const dynamicRadius = Math.ceil((angularTolerance / twoPi) * bins);
                const searchRadiusBins = Math.min(
                    Math.max(2, dynamicRadius),
                    Math.max(2, Math.min(24, Math.floor(bins / 6)))
                );
                const endpointDepthSlack = 0.35;
                const out = [];
                const seen = new Set();
                for (let offset = 0; offset <= searchRadiusBins; offset++) {
                    const candidates = offset === 0
                        ? [centerBin]
                        : [
                            (centerBin - offset + bins) % bins,
                            (centerBin + offset) % bins
                        ];
                    for (let i = 0; i < candidates.length; i++) {
                        const binIndex = candidates[i];
                        const owner = state.owner[binIndex] || null;
                        if (!owner || seen.has(owner)) continue;
                        const hitDist = Number(state.depth[binIndex]);
                        if (!Number.isFinite(hitDist) || hitDist <= 0) continue;
                        if (Number.isFinite(endpointDistance) && hitDist > (endpointDistance + endpointDepthSlack)) continue;
                        seen.add(owner);
                        out.push(owner);
                    }
                }
                endpointOwnersResolved += out.length;
                return out;
            };

            for (let i = 0; i < bins; i++) {
                const owner = state.owner[i];
                if (!owner || owner.type !== "wallSection" || typeof owner.accumulateLosIlluminationT !== "function") continue;
                const hitDist = Number(state.depth[i]);
                if (!Number.isFinite(hitDist) || hitDist <= 0) continue;
                illuminatedBins += 1;

                const theta = minAngle + ((i + 0.5) / bins) * twoPi;
                const hitX = Number(wizard.x) + Math.cos(theta) * hitDist;
                const hitY = Number(wizard.y) + Math.sin(theta) * hitDist;
                const t = (typeof owner._parameterForWorldPointOnSection === "function")
                    ? owner._parameterForWorldPointOnSection({ x: hitX, y: hitY })
                    : null;
                owner.accumulateLosIlluminationT(t);
            }

            for (const section of allSections.values()) {
                if (!section || typeof section.getLosIlluminationRangeT !== "function") continue;
                const range = section.getLosIlluminationRangeT();
                if (!range) continue;
                rangedSections += 1;

                const sectionLength = Number.isFinite(section.length) ? Math.max(0, Number(section.length)) : 0;
                const tMin = Number(range.tMin);
                const tMax = Number(range.tMax);
                const endpointSnapDistance = 1.0;
                const startDistToPlayer = (
                    section.startPoint &&
                    Number.isFinite(section.startPoint.x) &&
                    Number.isFinite(section.startPoint.y)
                )
                    ? Math.hypot(
                        (mapRef && typeof mapRef.shortestDeltaX === "function")
                            ? mapRef.shortestDeltaX(wizard.x, section.startPoint.x)
                            : (Number(section.startPoint.x) - Number(wizard.x)),
                        (mapRef && typeof mapRef.shortestDeltaY === "function")
                            ? mapRef.shortestDeltaY(wizard.y, section.startPoint.y)
                            : (Number(section.startPoint.y) - Number(wizard.y))
                    )
                    : Infinity;
                const endDistToPlayer = (
                    section.endPoint &&
                    Number.isFinite(section.endPoint.x) &&
                    Number.isFinite(section.endPoint.y)
                )
                    ? Math.hypot(
                        (mapRef && typeof mapRef.shortestDeltaX === "function")
                            ? mapRef.shortestDeltaX(wizard.x, section.endPoint.x)
                            : (Number(section.endPoint.x) - Number(wizard.x)),
                        (mapRef && typeof mapRef.shortestDeltaY === "function")
                            ? mapRef.shortestDeltaY(wizard.y, section.endPoint.y)
                            : (Number(section.endPoint.y) - Number(wizard.y))
                    )
                    : Infinity;
                const nearStartEndpointToPlayer = startDistToPlayer <= endDistToPlayer;
                const nearEndEndpointToPlayer = endDistToPlayer < startDistToPlayer;
                const nearStartByDistance = (
                    nearStartEndpointToPlayer &&
                    sectionLength > 0 &&
                    Number.isFinite(tMin) &&
                    (Math.max(0, tMin) * sectionLength) <= endpointSnapDistance
                );
                const nearEndByDistance = (
                    nearEndEndpointToPlayer &&
                    sectionLength > 0 &&
                    Number.isFinite(tMax) &&
                    (Math.max(0, 1 - tMax) * sectionLength) <= endpointSnapDistance
                );

                const ownersAtStart = collectEndpointOwners(section.startPoint);
                const ownersAtEnd = collectEndpointOwners(section.endPoint);
                const snapStartByOwner = (typeof section.isEndpointOwnedBySameWall === "function")
                    ? ownersAtStart.some(owner => section.isEndpointOwnedBySameWall("a", owner))
                    : false;
                const snapEndByOwner = (typeof section.isEndpointOwnedBySameWall === "function")
                    ? ownersAtEnd.some(owner => section.isEndpointOwnedBySameWall("b", owner))
                    : false;
                const snapStart = snapStartByOwner || nearStartByDistance;
                const snapEnd = snapEndByOwner || nearEndByDistance;

                if (typeof section.setLosEndpointSnapEligibility === "function") {
                    section.setLosEndpointSnapEligibility("a", snapStart);
                    section.setLosEndpointSnapEligibility("b", snapEnd);
                }
            }

            this.setFrameMetric("wallLosResetSections", resetSections);
            this.setFrameMetric("wallLosIlluminatedBins", illuminatedBins);
            this.setFrameMetric("wallLosRangedSections", rangedSections);
            this.setFrameMetric("wallLosEndpointLookups", endpointOwnerLookups);
            this.setFrameMetric("wallLosEndpointOwnersResolved", endpointOwnersResolved);
            this.setFrameMetric(
                "wallLosMs",
                diagnosticsEnabled ? (performance.now() - functionStartMs) : 0
            );
        }

        ensureLosShadowGraphics() {
            const layer = this.layers && this.layers.losShadow ? this.layers.losShadow : null;
            if (!layer) return null;
            if (!this.losShadowGraphics) {
                this.losShadowGraphics = new PIXI.Graphics();
                this.losShadowGraphics.name = "renderingLosShadowGraphics";
                this.losShadowGraphics.visible = false;
                this.losShadowGraphics.interactive = false;
                layer.addChild(this.losShadowGraphics);
            } else if (this.losShadowGraphics.parent !== layer) {
                layer.addChild(this.losShadowGraphics);
            }
            const shadowBlurEnabled = !!this.getLosVisualSetting("shadowBlurEnabled", true);
            const shadowBlurStrength = Number(this.getLosVisualSetting("shadowBlurStrength", 12));
            if (shadowBlurEnabled && shadowBlurStrength > 0 && typeof PIXI !== "undefined") {
                if (typeof PIXI.BlurFilter === "function") {
                    if (!this.losShadowGraphics._losBlurFilter || !(this.losShadowGraphics._losBlurFilter instanceof PIXI.BlurFilter)) {
                        this.losShadowGraphics._losBlurFilter = new PIXI.BlurFilter();
                    }
                    this.losShadowGraphics._losBlurFilter.blur = shadowBlurStrength;
                    this.losShadowGraphics.filters = [this.losShadowGraphics._losBlurFilter];
                } else if (PIXI.filters && typeof PIXI.filters.BlurFilter === "function") {
                    if (!this.losShadowGraphics._losBlurFilter || !(this.losShadowGraphics._losBlurFilter instanceof PIXI.filters.BlurFilter)) {
                        this.losShadowGraphics._losBlurFilter = new PIXI.filters.BlurFilter();
                    }
                    this.losShadowGraphics._losBlurFilter.blur = shadowBlurStrength;
                    this.losShadowGraphics.filters = [this.losShadowGraphics._losBlurFilter];
                } else {
                    this.losShadowGraphics.filters = null;
                }
            } else {
                this.losShadowGraphics.filters = null;
            }
            return this.losShadowGraphics;
        }

        getLosShadowDepthState() {
            if (this.losShadowDepthState) return this.losShadowDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.culling = false;
            this.losShadowDepthState = state;
            return state;
        }

        ensureLosShadowDepthMesh() {
            const parent = this.layers && this.layers.depthObjects;
            if (!parent || typeof PIXI === "undefined" || !PIXI.Geometry || !PIXI.Mesh || !PIXI.Shader) return null;
            if (!this.losShadowDepthMesh) {
                const geometry = new PIXI.Geometry()
                    .addAttribute("aWorldPosition", new Float32Array(0), 2)
                    .addIndex(new Uint16Array(0));
                const nearMetric = FLOOR_VISUAL_DEPTH_NEAR_METRIC;
                const farMetric = FLOOR_VISUAL_DEPTH_FAR_METRIC;
                const shader = PIXI.Shader.from(LOS_SHADOW_DEPTH_VS, LOS_SHADOW_DEPTH_FS, {
                    uScreenSize: new Float32Array([1, 1]),
                    uCameraWorld: new Float32Array([0, 0]),
                    uCameraZ: 0,
                    uBaseZ: 0,
                    uDepthBias: LOS_SHADOW_DEPTH_BIAS_UNITS,
                    uViewScale: 1,
                    uXyRatio: 1,
                    uDepthRange: new Float32Array([farMetric, 1 / Math.max(1e-6, farMetric - nearMetric)]),
                    uTint: new Float32Array([0, 0, 0, 0])
                });
                const state = this.getLosShadowDepthState();
                const mesh = new PIXI.Mesh(geometry, shader);
                mesh.name = "renderingLosShadowDepthMesh";
                mesh.interactive = false;
                mesh.visible = false;
                if (state) mesh.state = state;
                this.losShadowDepthMesh = mesh;
            }
            if (this.losShadowDepthMesh.parent !== parent) {
                parent.addChild(this.losShadowDepthMesh);
            }
            return this.losShadowDepthMesh;
        }

        ensureLosShadowDepthMaskGraphics() {
            const parent = this.layers && this.layers.depthObjects;
            if (!parent || typeof PIXI === "undefined" || !PIXI.Graphics) return null;
            if (!this.losShadowDepthMaskGraphics) {
                this.losShadowDepthMaskGraphics = new PIXI.Graphics();
                this.losShadowDepthMaskGraphics.name = "renderingLosShadowDepthFloorMask";
                this.losShadowDepthMaskGraphics.interactive = false;
                this.losShadowDepthMaskGraphics.visible = false;
            }
            if (this.losShadowDepthMaskGraphics.parent !== parent) {
                parent.addChild(this.losShadowDepthMaskGraphics);
            }
            return this.losShadowDepthMaskGraphics;
        }

        clearLosShadowDepthMask() {
            if (this.losShadowDepthMesh) {
                this.losShadowDepthMesh.mask = null;
            }
            if (this.losShadowDepthMaskGraphics) {
                this.losShadowDepthMaskGraphics.clear();
                this.losShadowDepthMaskGraphics.visible = false;
            }
        }

        getLosShadowFloorMaskFragments(ctx, level) {
            const mapRef = ctx && ctx.map ? ctx.map : null;
            if (!mapRef || !(mapRef.floorsById instanceof Map)) return [];
            const targetLevel = this.getLayerIndexFromValue(level, 0);
            const out = [];
            for (const fragment of mapRef.floorsById.values()) {
                if (!fragment) continue;
                const fragmentLevel = Number.isFinite(fragment.level)
                    ? this.getLayerIndexFromValue(fragment.level, 0)
                    : 0;
                if (fragmentLevel !== targetLevel) continue;
                if (fragment._floorEditEmpty === true) continue;
                const outer = normalizeFloorVisualPointList(fragment.outerPolygon);
                if (outer.length < 3) continue;
                const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
                const baseZ = Number.isFinite(fragment.nodeBaseZ)
                    ? Number(fragment.nodeBaseZ)
                    : this.getLayerBaseZForLevel(targetLevel);
                out.push({ outer, holes, baseZ });
            }
            return out;
        }

        drawLosShadowFloorMaskRing(maskGraphics, ring, baseZ) {
            if (!maskGraphics || !Array.isArray(ring) || ring.length < 3 || !this.camera) return false;
            let started = false;
            for (let i = 0; i < ring.length; i++) {
                const point = ring[i];
                if (!point || !Number.isFinite(point.x) || !Number.isFinite(point.y)) continue;
                const screen = this.camera.worldToScreen(Number(point.x), Number(point.y), baseZ);
                if (!screen || !Number.isFinite(screen.x) || !Number.isFinite(screen.y)) continue;
                if (!started) {
                    maskGraphics.moveTo(screen.x, screen.y);
                    started = true;
                } else {
                    maskGraphics.lineTo(screen.x, screen.y);
                }
            }
            if (started) maskGraphics.closePath();
            return started;
        }

        applyLosShadowDepthFloorMask(ctx, depthMesh, level) {
            if (!depthMesh) return false;
            const fragments = this.getLosShadowFloorMaskFragments(ctx, level);
            if (fragments.length === 0) {
                this.clearLosShadowDepthMask();
                return level === 0;
            }
            const maskGraphics = this.ensureLosShadowDepthMaskGraphics();
            if (!maskGraphics) {
                this.clearLosShadowDepthMask();
                return false;
            }
            maskGraphics.clear();
            maskGraphics.visible = true;
            if (Object.prototype.hasOwnProperty.call(maskGraphics, "renderable")) {
                maskGraphics.renderable = true;
            }
            let polygonCount = 0;
            let holeCount = 0;
            for (let i = 0; i < fragments.length; i++) {
                const fragment = fragments[i];
                maskGraphics.beginFill(0xffffff, 1);
                const drewOuter = this.drawLosShadowFloorMaskRing(maskGraphics, fragment.outer, fragment.baseZ);
                if (drewOuter) {
                    const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
                    const canDrawHoles = typeof maskGraphics.beginHole === "function" && typeof maskGraphics.endHole === "function";
                    if (canDrawHoles) {
                        for (let h = 0; h < holes.length; h++) {
                            const hole = normalizeFloorVisualPointList(holes[h]);
                            if (hole.length < 3) continue;
                            maskGraphics.beginHole();
                            if (this.drawLosShadowFloorMaskRing(maskGraphics, hole, fragment.baseZ)) {
                                holeCount += 1;
                            }
                            maskGraphics.endHole();
                        }
                    }
                    polygonCount += 1;
                }
                maskGraphics.endFill();
            }
            if (polygonCount <= 0) {
                this.clearLosShadowDepthMask();
                return false;
            }
            depthMesh.mask = maskGraphics;
            this.setFrameMetric("losShadowFloorMaskPolygons", polygonCount);
            this.setFrameMetric("losShadowFloorMaskHoles", holeCount);
            return true;
        }

        renderLosShadowOverlay(ctx) {
            const graphics = this.ensureLosShadowGraphics();
            if (!graphics) return;
            graphics.clear();
            graphics.visible = false;
            if (graphics.filters) graphics.filters = null;
            const depthMesh = this.ensureLosShadowDepthMesh();
            if (depthMesh) {
                depthMesh.visible = false;
                depthMesh.mask = null;
            }
            this.clearLosShadowDepthMask();
            if (this.mazeModeOverlayActive) {
                return;
            }

            const shadowEnabled = !!this.getLosVisualSetting("shadowEnabled", true);
            const wizard = ctx && ctx.wizard ? ctx.wizard : null;
            const omnivisionActive = this.isOmnivisionActive(wizard);
            const mazeMode = this.isLosMazeModeEnabled() && !omnivisionActive;
            const shadowOpacityRaw = Number(this.getLosVisualSetting("shadowOpacity", 0.4));
            const shadowOpacity = mazeMode
                ? 1
                : (Number.isFinite(shadowOpacityRaw) ? Math.max(0, Math.min(1, shadowOpacityRaw)) : 0.4);
            const state = this.currentLosState;
            if (omnivisionActive || !shadowEnabled || shadowOpacity <= 0 || !wizard || !state || !state.depth || !Number.isFinite(state.bins)) {
                return;
            }

            const bins = Math.max(3, Math.floor(state.bins));
            const depth = state.depth;
            if (!depth || depth.length !== bins) {
                return;
            }

            const minAngle = Number.isFinite(state.minAngle) ? state.minAngle : -Math.PI;
            const twoPi = Math.PI * 2;
            const viewportRef = (ctx && ctx.viewport) || null;
            const viewportW = viewportRef && Number.isFinite(viewportRef.width) ? viewportRef.width : 24;
            const viewportH = viewportRef && Number.isFinite(viewportRef.height) ? viewportRef.height : 24;
            const farDist = Math.max(viewportW, viewportH) * 1.5;
            const angleForBin = idx => minAngle + ((idx + 0.5) / bins) * twoPi;
            const losHasForwardFov = !!state.hasForwardFov;
            const losFacingAngle = Number.isFinite(state.facingAngle) ? state.facingAngle : 0;
            const losHalfFovRad = Number.isFinite(state.halfFovRad) ? state.halfFovRad : Math.PI;
            const isInsideFov = theta => {
                if (!losHasForwardFov) return true;
                let delta = theta - losFacingAngle;
                while (delta <= -Math.PI) delta += twoPi;
                while (delta > Math.PI) delta -= twoPi;
                return Math.abs(delta) <= losHalfFovRad;
            };
            if (!depthMesh || !depthMesh.geometry || !depthMesh.shader || !depthMesh.shader.uniforms) return;
            const shadowColorRaw = Number(this.getLosVisualSetting("shadowColor", 0x777777));
            const shadowColor = Number.isFinite(shadowColorRaw)
                ? Math.max(0, Math.min(0xffffff, Math.floor(shadowColorRaw)))
                : 0x777777;
            const wizardX = Number(wizard.x) || 0;
            const wizardY = Number(wizard.y) || 0;
            const maxVertexCount = (bins + 1) * 2;
            const maxIndexCount = bins * 6;
            if (!this._losShadowPositionScratch || this._losShadowPositionScratch.length < maxVertexCount * 2) {
                this._losShadowPositionScratch = new Float32Array(maxVertexCount * 2);
            }
            if (!this._losShadowIndexScratch || this._losShadowIndexScratch.length < maxIndexCount) {
                this._losShadowIndexScratch = new Uint16Array(maxIndexCount);
            }
            const positions = this._losShadowPositionScratch;
            const indices = this._losShadowIndexScratch;
            const edgeCoordCount = (bins + 1) * 2;
            if (!this._losShadowEdgeNearScratch || this._losShadowEdgeNearScratch.length < edgeCoordCount) {
                this._losShadowEdgeNearScratch = new Float32Array(edgeCoordCount);
            }
            if (!this._losShadowEdgeFarScratch || this._losShadowEdgeFarScratch.length < edgeCoordCount) {
                this._losShadowEdgeFarScratch = new Float32Array(edgeCoordCount);
            }
            const edgeNear = this._losShadowEdgeNearScratch;
            const edgeFar = this._losShadowEdgeFarScratch;
            let vertexCount = 0;
            let indexCount = 0;
            let runActive = false;
            let previousEdge = -1;
            let shadowRuns = 0;

            const computeEdge = (edgeIndex) => {
                const wrapped = ((edgeIndex % bins) + bins) % bins;
                const theta = angleForBin(wrapped);
                const nearReveal = isInsideFov(theta) ? 0 : LOS_NEAR_REVEAL_RADIUS;
                const d = Number.isFinite(depth[wrapped]) ? Math.max(nearReveal, depth[wrapped]) : farDist;
                const cosT = Math.cos(theta);
                const sinT = Math.sin(theta);
                edgeNear[edgeIndex * 2] = wizardX + cosT * d;
                edgeNear[edgeIndex * 2 + 1] = wizardY + sinT * d;
                edgeFar[edgeIndex * 2] = wizardX + cosT * farDist;
                edgeFar[edgeIndex * 2 + 1] = wizardY + sinT * farDist;
                return d;
            };

            const emitEdgeVertices = (edgeIndex) => {
                if (vertexCount + 2 > 65535) return -1;
                const base = vertexCount;
                const nearOffset = edgeIndex * 2;
                const farOffset = edgeIndex * 2;
                let dst = vertexCount * 2;
                positions[dst] = edgeNear[nearOffset];
                positions[dst + 1] = edgeNear[nearOffset + 1];
                positions[dst + 2] = edgeFar[farOffset];
                positions[dst + 3] = edgeFar[farOffset + 1];
                vertexCount += 2;
                return base;
            };

            for (let i = 0; i < bins; i++) {
                const d0 = computeEdge(i);
                const d1 = computeEdge(i + 1);
                const visibleShadow = d0 < farDist || d1 < farDist;
                if (!visibleShadow) {
                    runActive = false;
                    previousEdge = -1;
                    continue;
                }
                if (!runActive) {
                    previousEdge = emitEdgeVertices(i);
                    if (previousEdge < 0) break;
                    runActive = true;
                    shadowRuns += 1;
                }
                const nextEdge = emitEdgeVertices(i + 1);
                if (nextEdge < 0 || indexCount + 6 > indices.length) break;
                indices[indexCount++] = previousEdge;
                indices[indexCount++] = nextEdge;
                indices[indexCount++] = nextEdge + 1;
                indices[indexCount++] = previousEdge;
                indices[indexCount++] = nextEdge + 1;
                indices[indexCount++] = previousEdge + 1;
                previousEdge = nextEdge;
            }
            if (vertexCount < 3 || indexCount < 3) {
                return;
            }
            const geometry = depthMesh.geometry;
            const positionBuffer = geometry.getBuffer("aWorldPosition");
            const indexBuffer = geometry.getIndex();
            if (!positionBuffer || !indexBuffer) return;
            positionBuffer.data = positions.subarray(0, vertexCount * 2);
            indexBuffer.data = indices.subarray(0, indexCount);
            positionBuffer.update();
            indexBuffer.update();

            const appRef = (ctx && ctx.app) || (typeof app !== "undefined" ? app : (global.app || null));
            const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width)) ? Number(appRef.screen.width) : 1;
            const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height)) ? Number(appRef.screen.height) : 1;
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const baseZ = this.getWizardVisualLayerBaseZ(wizard, wizardLayer);
            if (!this.applyLosShadowDepthFloorMask(ctx, depthMesh, wizardLayer)) {
                depthMesh.visible = false;
                return;
            }
            const uniforms = depthMesh.shader.uniforms;
            uniforms.uScreenSize[0] = Math.max(1, screenW);
            uniforms.uScreenSize[1] = Math.max(1, screenH);
            uniforms.uCameraWorld[0] = Number(this.camera.x) || 0;
            uniforms.uCameraWorld[1] = Number(this.camera.y) || 0;
            uniforms.uCameraZ = Number(this.camera.z) || 0;
            uniforms.uBaseZ = baseZ;
            uniforms.uDepthBias = LOS_SHADOW_DEPTH_BIAS_UNITS;
            uniforms.uViewScale = Number(this.camera.viewscale) || 1;
            uniforms.uXyRatio = Number(this.camera.xyratio) || 1;
            uniforms.uDepthRange[0] = FLOOR_VISUAL_DEPTH_FAR_METRIC;
            uniforms.uDepthRange[1] = 1 / Math.max(1e-6, FLOOR_VISUAL_DEPTH_FAR_METRIC - FLOOR_VISUAL_DEPTH_NEAR_METRIC);
            uniforms.uTint[0] = ((shadowColor >> 16) & 0xff) / 255;
            uniforms.uTint[1] = ((shadowColor >> 8) & 0xff) / 255;
            uniforms.uTint[2] = (shadowColor & 0xff) / 255;
            uniforms.uTint[3] = shadowOpacity;
            depthMesh.alpha = 1;
            depthMesh.visible = true;
            if (Object.prototype.hasOwnProperty.call(depthMesh, "renderable")) {
                depthMesh.renderable = true;
            }
            this.setFrameMetric("losShadowDepthVertices", vertexCount);
            this.setFrameMetric("losShadowDepthTriangles", indexCount / 3);
            this.setFrameMetric("losShadowDepthRuns", shadowRuns);
        }

        createRenderItemTypeMetricCounts() {
            return {
                total: 0,
                roads: 0,
                trees: 0,
                flowers: 0,
                walls: 0,
                animals: 0,
                roofs: 0,
                doors: 0,
                windows: 0,
                furniture: 0,
                triggers: 0,
                powerups: 0,
                stairs: 0,
                parked: 0,
                other: 0
            };
        }

        getRenderItemTypeMetricBucket(item) {
            if (!item) return "other";
            if (typeof Animal !== "undefined" && item instanceof Animal) return "animals";
            const rawType = typeof item.type === "string" ? item.type : "";
            const type = rawType.trim().toLowerCase();
            if (type === "road" || type === "roadpath") return "roads";
            if (type === "tree") return "trees";
            if (type === "flower") return "flowers";
            if (type === "wallsection") return "walls";
            if (type === "roof") return "roofs";
            if (type === "door") return "doors";
            if (type === "window") return "windows";
            if (type === "furniture") return "furniture";
            if (type === "triggerarea") return "triggers";
            if (type === "powerup") return "powerups";
            if (type === "straightstair") return "stairs";
            if (
                type === "squirrel" ||
                type === "deer" ||
                type === "bear" ||
                type === "eagleman"
            ) {
                return "animals";
            }
            return "other";
        }

        incrementRenderItemTypeMetricCount(counts, item) {
            if (!counts || !item) return;
            counts.total += 1;
            if (item._prototypeParked === true) counts.parked += 1;
            const bucket = this.getRenderItemTypeMetricBucket(item);
            if (Object.prototype.hasOwnProperty.call(counts, bucket)) {
                counts[bucket] += 1;
            } else {
                counts.other += 1;
            }
        }

        setRenderItemTypeFrameMetrics(prefix, counts) {
            if (!prefix || !counts) return;
            this.setFrameMetric(`${prefix}Total`, counts.total || 0);
            this.setFrameMetric(`${prefix}Roads`, counts.roads || 0);
            this.setFrameMetric(`${prefix}Trees`, counts.trees || 0);
            this.setFrameMetric(`${prefix}Flowers`, counts.flowers || 0);
            this.setFrameMetric(`${prefix}Walls`, counts.walls || 0);
            this.setFrameMetric(`${prefix}Animals`, counts.animals || 0);
            this.setFrameMetric(`${prefix}Roofs`, counts.roofs || 0);
            this.setFrameMetric(`${prefix}Doors`, counts.doors || 0);
            this.setFrameMetric(`${prefix}Windows`, counts.windows || 0);
            this.setFrameMetric(`${prefix}Furniture`, counts.furniture || 0);
            this.setFrameMetric(`${prefix}Triggers`, counts.triggers || 0);
            this.setFrameMetric(`${prefix}Powerups`, counts.powerups || 0);
            this.setFrameMetric(`${prefix}Stairs`, counts.stairs || 0);
            this.setFrameMetric(`${prefix}Parked`, counts.parked || 0);
            this.setFrameMetric(`${prefix}Other`, counts.other || 0);
        }

        collectVisibleObjects(visibleNodes, ctx) {
            const nodes = Array.isArray(visibleNodes) ? visibleNodes : [];
            const mapRef = ctx && ctx.map ? ctx.map : null;
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx);
            const _nowCVO = typeof performance !== "undefined" ? () => performance.now() : () => Date.now();
            const _diagCVO = !!this.currentFrameMetrics;
            const tFloorScan = _diagCVO ? _nowCVO() : 0;
            const floorObjectNodes = this.collectVisibleFloorObjectNodes(ctx, mapRef, nodes);
            if (_diagCVO) this.setFrameMetric("cvoFloorScanMs", _nowCVO() - tFloorScan);
            let objectSourceNodes;
            if (floorObjectNodes.length > 0) {
                if (!this._collectVisibleObjectsSource) this._collectVisibleObjectsSource = [];
                const _combined = this._collectVisibleObjectsSource;
                _combined.length = 0;
                for (let _i = 0; _i < nodes.length; _i++) _combined.push(nodes[_i]);
                for (let _i = 0; _i < floorObjectNodes.length; _i++) _combined.push(floorObjectNodes[_i]);
                objectSourceNodes = _combined;
            } else {
                objectSourceNodes = nodes;
            }
            if (!this._collectVisibleObjectsSeen) this._collectVisibleObjectsSeen = new Set();
            const seen = this._collectVisibleObjectsSeen;
            seen.clear();
            if (!this._collectVisibleObjectsOut) this._collectVisibleObjectsOut = [];
            const out = this._collectVisibleObjectsOut;
            out.length = 0;
            let nodeObjectsRefs = 0;
            let nodeVisibilityRefs = 0;
            let duplicateRefsSkipped = 0;
            let skippedBuildingCutaway = 0;
            let skippedBuildingInteriorScope = 0;
            let skippedRoadRefsForPickerSurface = 0;
            const cutawayFrameId = Number(this._layerCutawayFrameId) || 0;
            const isHiddenByBuildingCutawayFrame = (item) => !!(
                cutawayFrameId > 0 &&
                item &&
                item._cutawayHiddenFrame === cutawayFrameId
            );
            const wallCtor = global.WallSectionUnit;
            const mountedWallCandidates = [];
            const mountedWallCandidateSet = new Set();
            const addMountedWallCandidate = (wall, wallLayer = 0) => {
                if (!wall || wall.gone || wall.vanishing || !Array.isArray(wall.attachedObjects)) return;
                if (mountedWallCandidateSet.has(wall)) return;
                mountedWallCandidateSet.add(wall);
                mountedWallCandidates.push({
                    wall,
                    wallLayer: this.getLayerIndexFromValue(wallLayer, 0)
                });
            };
            const nowVO = _nowCVO;
            const diagnosticsEnabled = _diagCVO;
            const typeDiagnosticsEnabled = diagnosticsEnabled && isDrawPassBreakdownEnabled();
            const tNodeScan = diagnosticsEnabled ? nowVO() : 0;
            for (let n = 0; n < objectSourceNodes.length; n++) {
                const node = objectSourceNodes[n];
                if (!node) continue;
                const nodeLayer = this.getLayerIndexForNode(node);
                for (let listIndex = 0; listIndex < 2; listIndex++) {
                    const list = listIndex === 0 ? node.objects : node.visibilityObjects;
                    if (!Array.isArray(list)) continue;
                    if (listIndex === 0) {
                        nodeObjectsRefs += list.length;
                    } else {
                        nodeVisibilityRefs += list.length;
                    }
                    for (let i = 0; i < list.length; i++) {
                        const obj = list[i];
                        if (!obj || obj.gone || obj.vanishing) continue;
                        if (obj.type === "road") {
                            // Baked road tiles are drawn by the floor renderer and picked through
                            // ScenePicker's per-section road color surface, so keep them out of
                            // the general visible-object/onscreen-object working set.
                            skippedRoadRefsForPickerSurface += 1;
                            continue;
                        }
                        if (interiorScope && interiorScope.active) {
                            skippedBuildingInteriorScope += 1;
                            continue;
                        }
                        if (isHiddenByBuildingCutawayFrame(obj)) {
                            this.logPlaceObjectRenderDebug("collect-skip-building-cutaway", obj, {
                                nodeLayer,
                                source: n >= nodes.length ? "floorNode" : "visibleNode"
                            });
                            skippedBuildingCutaway += 1;
                            continue;
                        }
                        if (
                            mapRef &&
                            mapRef._prototypeTriggerState &&
                            (obj.type === "triggerArea" || obj.isTriggerArea === true)
                        ) {
                            continue;
                        }
                        if (seen.has(obj)) {
                            duplicateRefsSkipped += 1;
                            continue;
                        }
                        const objRenderLayer = Number.isFinite(obj.traversalLayer)
                            ? this.getLayerIndexFromValue(obj.traversalLayer, nodeLayer)
                            : (Number.isFinite(obj.level)
                                ? this.getLayerIndexFromValue(obj.level, nodeLayer)
                                : nodeLayer);
                        if (obj.type === "wallSection") addMountedWallCandidate(obj, objRenderLayer);
                        this.logPlaceObjectRenderDebug(
                            n >= nodes.length ? "collected-from-floor-node" : "collected-from-visible-node",
                            obj,
                            {
                                nodeLayer,
                                nodeId: typeof node.id === "string" ? node.id : "",
                                nodeX: Number.isFinite(node.xindex) ? Number(node.xindex) : null,
                                nodeY: Number.isFinite(node.yindex) ? Number(node.yindex) : null,
                                visibleNodeCount: nodes.length,
                                floorObjectNodeCount: floorObjectNodes.length
                            }
                        );
                        seen.add(obj);
                        out.push(obj);
                    }
                }
            }
            if (diagnosticsEnabled) this.setFrameMetric("cvoNodeScanMs", nowVO() - tNodeScan);
            const tStairs = diagnosticsEnabled ? nowVO() : 0;
            const stairObjectsAdded = this.collectVisibleStairRenderObjects(ctx, mapRef, seen, out);
            if (diagnosticsEnabled) {
                this.setFrameMetric("cvoStairsMs", nowVO() - tStairs);
                this.setFrameMetric("cvoStairsAdded", stairObjectsAdded);
            }
            const tWalls = diagnosticsEnabled ? nowVO() : 0;
            const allWallSections = (wallCtor && wallCtor._allSections instanceof Map)
                ? wallCtor._allSections
                : null;
            let globalWallsConsidered = 0;
            let globalWallsAdded = 0;
            let globalWallsCulled = 0;
            if (allWallSections) {
                const activeSectionKeys = (
                    mapRef &&
                    typeof mapRef.getPrototypeActiveSectionKeys === "function"
                ) ? mapRef.getPrototypeActiveSectionKeys() : null;
                const cam = this.camera || (ctx && ctx.camera) || {};
                const viewportRef = (ctx && ctx.viewport) || {};
                const viewportW = Number.isFinite(viewportRef.width) ? Number(viewportRef.width) : 40;
                const viewportH = Number.isFinite(viewportRef.height) ? Number(viewportRef.height) : 30;
                const camX = Number(cam.x) || 0;
                const camY = Number(cam.y) || 0;
                const wallPadX = Math.max(8, viewportW * 0.25);
                const wallPadY = Math.max(8, viewportH * 0.25);
                const viewMinX = camX - wallPadX;
                const viewMaxX = camX + viewportW + wallPadX;
                const viewMinY = camY - wallPadY;
                const viewMaxY = camY + viewportH + wallPadY;
                const visibleNodeKeys = this._collectVisibleNodesSeenKeys instanceof Set
                    ? this._collectVisibleNodesSeenKeys
                    : null;
                const wallHasVisibleNode = (wall) => {
                    if (!visibleNodeKeys || !wall || !Array.isArray(wall.nodes) || wall.nodes.length === 0) return false;
                    for (let i = 0; i < wall.nodes.length; i++) {
                        const node = wall.nodes[i];
                        if (!node) continue;
                        const key = this.getVisibleNodeRenderKey(node);
                        if (visibleNodeKeys.has(key)) return true;
                    }
                    return false;
                };
                const wallIntersectsViewport = (wall) => {
                    if (!wall) return false;
                    const ownerSectionKey = typeof wall._prototypeOwnerSectionKey === "string"
                        ? wall._prototypeOwnerSectionKey
                        : "";
                    if (
                        ownerSectionKey &&
                        activeSectionKeys instanceof Set &&
                        activeSectionKeys.has(ownerSectionKey)
                    ) {
                        return true;
                    }
                    if (wallHasVisibleNode(wall)) return true;
                    const sxRaw = Number(wall.startPoint && wall.startPoint.x);
                    const syRaw = Number(wall.startPoint && wall.startPoint.y);
                    const exRaw = Number(wall.endPoint && wall.endPoint.x);
                    const eyRaw = Number(wall.endPoint && wall.endPoint.y);
                    if (
                        !Number.isFinite(sxRaw) || !Number.isFinite(syRaw) ||
                        !Number.isFinite(exRaw) || !Number.isFinite(eyRaw)
                    ) {
                        return true;
                    }
                    const sx = (mapRef && typeof mapRef.shortestDeltaX === "function")
                        ? camX + mapRef.shortestDeltaX(camX, sxRaw)
                        : sxRaw;
                    const ex = (mapRef && typeof mapRef.shortestDeltaX === "function")
                        ? camX + mapRef.shortestDeltaX(camX, exRaw)
                        : exRaw;
                    const sy = (mapRef && typeof mapRef.shortestDeltaY === "function")
                        ? camY + mapRef.shortestDeltaY(camY, syRaw)
                        : syRaw;
                    const ey = (mapRef && typeof mapRef.shortestDeltaY === "function")
                        ? camY + mapRef.shortestDeltaY(camY, eyRaw)
                        : eyRaw;
                    const thickness = Number.isFinite(wall.thickness) ? Math.max(0, Number(wall.thickness)) : 0;
                    const minX = Math.min(sx, ex) - thickness - 1;
                    const maxX = Math.max(sx, ex) + thickness + 1;
                    const minY = Math.min(sy, ey) - thickness - 1;
                    const maxY = Math.max(sy, ey) + thickness + 1;
                    return maxX >= viewMinX && minX <= viewMaxX && maxY >= viewMinY && minY <= viewMaxY;
                };
                const getWallTraversalLayer = (wall) => {
                    if (wallCtor && typeof wallCtor.getTraversalLayerForSection === "function") {
                        return wallCtor.getTraversalLayerForSection(wall, 0);
                    }
                    if (Number.isFinite(wall && wall.traversalLayer)) return this.getLayerIndexFromValue(wall.traversalLayer, 0);
                    if (Number.isFinite(wall && wall.level)) return this.getLayerIndexFromValue(wall.level, 0);
                    if (Number.isFinite(wall && wall.bottomZ)) return this.getLayerIndexFromValue(Number(wall.bottomZ) / 3, 0);
                    return 0;
                };
                for (const wall of allWallSections.values()) {
                    if (!wall || wall.gone || wall.vanishing || wall.type !== "wallSection") continue;
                    if (mapRef && wall.map && wall.map !== mapRef) continue;
                    globalWallsConsidered += 1;
                    if (interiorScope && interiorScope.active) {
                        skippedBuildingInteriorScope += 1;
                        continue;
                    }
                    if (!wallIntersectsViewport(wall)) {
                        globalWallsCulled += 1;
                        continue;
                    }
                    const wallLayer = getWallTraversalLayer(wall);
                    if (isHiddenByBuildingCutawayFrame(wall)) {
                        skippedBuildingCutaway += 1;
                        continue;
                    }
                    addMountedWallCandidate(wall, wallLayer);
                    if (seen.has(wall)) continue;
                    seen.add(wall);
                    out.push(wall);
                    globalWallsAdded += 1;
                }
            }
            if (diagnosticsEnabled) {
                this.setFrameMetric("cvoWallsMs", nowVO() - tWalls);
                this.setFrameMetric("visibleGlobalWallsConsidered", globalWallsConsidered);
                this.setFrameMetric("visibleGlobalWallsAdded", globalWallsAdded);
                this.setFrameMetric("visibleGlobalWallsCulled", globalWallsCulled);
            }
            const tMounted = diagnosticsEnabled ? nowVO() : 0;
            let mountedObjectsAdded = 0;
            const visibleWalls = mountedWallCandidates.length > 0
                ? mountedWallCandidates
                : out
                    .filter(obj => (
                        obj &&
                        obj.type === "wallSection" &&
                        Array.isArray(obj.attachedObjects)
                    ))
                    .map(wall => ({
                        wall,
                        wallLayer: this.getLayerIndexForObject(wall, 0)
                    }));
            for (let i = 0; i < visibleWalls.length; i++) {
                const candidate = visibleWalls[i];
                const wall = candidate && candidate.wall;
                const wallLayer = Number.isFinite(candidate && candidate.wallLayer)
                    ? Number(candidate.wallLayer)
                    : this.getLayerIndexForObject(wall, 0);
                if (!wall || !Array.isArray(wall.attachedObjects)) continue;
                for (let j = 0; j < wall.attachedObjects.length; j++) {
                    const entry = wall.attachedObjects[j];
                    const obj = entry && entry.object;
                    if (!obj || obj.gone || obj.vanishing || seen.has(obj)) continue;
                    if (interiorScope && interiorScope.active) {
                        skippedBuildingInteriorScope += 1;
                        continue;
                    }
                    if (isHiddenByBuildingCutawayFrame(obj)) {
                        skippedBuildingCutaway += 1;
                        continue;
                    }
                    if (!Number.isFinite(obj.traversalLayer)) obj.traversalLayer = wallLayer;
                    if (!Number.isFinite(obj.level)) obj.level = wallLayer;
                    seen.add(obj);
                    out.push(obj);
                    mountedObjectsAdded += 1;
                }
            }
            if (diagnosticsEnabled) this.setFrameMetric("cvoMountedMs", nowVO() - tMounted);
            const tAnimals = diagnosticsEnabled ? nowVO() : 0;
            const animalsList = Array.isArray(ctx && ctx.animals)
                ? ctx.animals
                : (Array.isArray(global.animals) ? global.animals : []);
            const animalsPreFilteredVisible = !!(ctx && ctx.animalsPreFilteredVisible);
            let animalsConsidered = 0;
            let animalsAdded = 0;
            let animalsSkippedOffscreen = 0;
            for (let i = 0; i < animalsList.length; i++) {
                const animal = animalsList[i];
                if (!animal || animal.gone || animal.vanishing) continue;
                animalsConsidered += 1;
                if (interiorScope && interiorScope.active) {
                    skippedBuildingInteriorScope += 1;
                    continue;
                }
                if (!animalsPreFilteredVisible && !animal.onScreen) {
                    animalsSkippedOffscreen += 1;
                    continue;
                }
                if (seen.has(animal)) continue;
                seen.add(animal);
                out.push(animal);
                animalsAdded += 1;
            }
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            if (
                mapRef &&
                wizardRef &&
                typeof mapRef.getPrototypeActiveTriggerDisplayObjectsForActor === "function"
            ) {
                const triggerObjects = mapRef.getPrototypeActiveTriggerDisplayObjectsForActor(wizardRef);
                for (let i = 0; i < triggerObjects.length; i++) {
                    const triggerObj = triggerObjects[i];
                    if (!triggerObj || triggerObj.gone || triggerObj.vanishing) continue;
                    if (interiorScope && interiorScope.active) {
                        skippedBuildingInteriorScope += 1;
                        continue;
                    }
                    if (seen.has(triggerObj)) continue;
                    seen.add(triggerObj);
                    out.push(triggerObj);
                }
            }
            const interiorPlan = interiorScope && interiorScope.active && this._buildingInteriorRenderPlan
                ? this._buildingInteriorRenderPlan
                : null;
            if (interiorPlan && interiorPlan.items instanceof Set) {
                interiorPlan.items.forEach(item => {
                    if (!item || item.gone || item.vanishing || seen.has(item)) return;
                    seen.add(item);
                    out.push(item);
                });
            }
            if (diagnosticsEnabled) this.setFrameMetric("cvoAnimalsMs", nowVO() - tAnimals);
            this.setFrameMetric("visibleObjectNodeRefs", nodeObjectsRefs);
            this.setFrameMetric("visibleObjectVisibilityRefs", nodeVisibilityRefs);
            this.setFrameMetric("visibleFloorObjectNodes", floorObjectNodes.length);
            this.setFrameMetric("visibleObjectsSkippedBuildingCutaway", skippedBuildingCutaway);
            this.setFrameMetric("visibleObjectsSkippedBuildingInteriorScope", skippedBuildingInteriorScope);
            this.setFrameMetric("visibleObjectRoadRefsSkippedForPickerSurface", skippedRoadRefsForPickerSurface);
            this.setFrameMetric("visibleGlobalWallsConsidered", globalWallsConsidered);
            this.setFrameMetric("visibleGlobalWallsAdded", globalWallsAdded);
            this.setFrameMetric("visibleMountedObjectsAdded", mountedObjectsAdded);
            this.setFrameMetric("visibleAnimalCandidates", animalsConsidered);
            this.setFrameMetric("visibleAnimalsAdded", animalsAdded);
            this.setFrameMetric("visibleAnimalsSkippedOffscreen", animalsSkippedOffscreen);
            this.setFrameMetric("visibleObjects", out.length);
            if (typeDiagnosticsEnabled) {
                const visibleTypeCounts = this.createRenderItemTypeMetricCounts();
                for (let i = 0; i < out.length; i++) {
                    this.incrementRenderItemTypeMetricCount(visibleTypeCounts, out[i]);
                }
                this.setRenderItemTypeFrameMetrics("visibleObjects", visibleTypeCounts);
            }
            return out;
        }

        getCameraWorldViewportBounds(ctx = null, margin = 0) {
            return this.getCameraScreenSpaceWorldBoundsAtZ(ctx, margin, 0);
        }

        getCameraScreenSpaceWorldBoundsAtZ(ctx = null, margin = 0, worldZ = 0) {
            const cameraRef = (ctx && ctx.camera) || this.camera || {};
            const viewportRef = (ctx && ctx.viewport) || global.viewport || {};
            const x = Number(cameraRef.x);
            const y = Number(cameraRef.y);
            const cameraZ = Number.isFinite(cameraRef.z)
                ? Number(cameraRef.z)
                : (Number.isFinite(viewportRef.z) ? Number(viewportRef.z) : 0);
            const width = Number.isFinite(cameraRef.width)
                ? Number(cameraRef.width)
                : Number(viewportRef.width);
            const height = Number.isFinite(cameraRef.height)
                ? Number(cameraRef.height)
                : Number(viewportRef.height);
            const z = Number.isFinite(Number(worldZ)) ? Number(worldZ) : cameraZ;
            if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(width) || !Number.isFinite(height) || !Number.isFinite(cameraZ)) {
                return null;
            }
            const pad = Number.isFinite(margin) ? Math.max(0, Number(margin)) : 0;
            const projectedMinY = y + (z - cameraZ);
            return {
                minX: x - pad,
                minY: projectedMinY - pad,
                maxX: x + Math.max(0, width) + pad,
                maxY: projectedMinY + Math.max(0, height) + pad,
                cameraZ,
                worldZ: z
            };
        }

        getCameraScreenSpaceIndexBounds(ctx = null, margin = 0, zRange = null) {
            const cameraRef = (ctx && ctx.camera) || this.camera || {};
            const viewportRef = (ctx && ctx.viewport) || global.viewport || {};
            const cameraZ = Number.isFinite(cameraRef.z)
                ? Number(cameraRef.z)
                : (Number.isFinite(viewportRef.z) ? Number(viewportRef.z) : 0);
            const minZ = Number.isFinite(zRange && zRange.minZ) ? Number(zRange.minZ) : cameraZ;
            const maxZ = Number.isFinite(zRange && zRange.maxZ) ? Number(zRange.maxZ) : cameraZ;
            const lowBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, margin, Math.min(minZ, maxZ));
            const highBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, margin, Math.max(minZ, maxZ));
            if (!lowBounds || !highBounds) return null;
            return {
                minX: Math.min(lowBounds.minX, highBounds.minX),
                maxX: Math.max(lowBounds.maxX, highBounds.maxX),
                minY: Math.min(lowBounds.minY, highBounds.minY),
                maxY: Math.max(lowBounds.maxY, highBounds.maxY),
                cameraZ,
                minZ: Math.min(minZ, maxZ),
                maxZ: Math.max(minZ, maxZ)
            };
        }

        getRenderableZRangeForViewportCull(ctx = null, mapRef = null) {
            const cameraRef = (ctx && ctx.camera) || this.camera || {};
            const viewportRef = (ctx && ctx.viewport) || global.viewport || {};
            let minZ = Number.isFinite(cameraRef.z)
                ? Number(cameraRef.z)
                : (Number.isFinite(viewportRef.z) ? Number(viewportRef.z) : 0);
            let maxZ = minZ;
            const includeZ = (z) => {
                const n = Number(z);
                if (!Number.isFinite(n)) return;
                minZ = Math.min(minZ, n);
                maxZ = Math.max(maxZ, n);
            };
            includeZ(0);
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            if (wizardRef) {
                includeZ(this.getWizardVisualLayerBaseZ(wizardRef, this.getWizardVisualLayerIndex(wizardRef, 0)));
                includeZ(this.getWizardContinuousWorldZ(wizardRef, maxZ));
            }
            const map = mapRef || (ctx && ctx.map) || null;
            if (map && map.floorsById instanceof Map) {
                for (const fragment of map.floorsById.values()) {
                    if (fragment && Number.isFinite(Number(fragment.nodeBaseZ))) {
                        includeZ(Number(fragment.nodeBaseZ));
                    }
                }
            }
            return { minZ, maxZ };
        }

        getCachedFloorFragmentOuter(fragment) {
            if (!fragment) return [];
            if (fragment._cachedNormalizedOuterSource !== fragment.outerPolygon || !fragment._cachedNormalizedOuter) {
                fragment._cachedNormalizedOuter = normalizeFloorVisualPointList(fragment.outerPolygon);
                fragment._cachedNormalizedOuterSource = fragment.outerPolygon;
                fragment._cachedNormalizedOuterBounds = null;
            }
            return Array.isArray(fragment._cachedNormalizedOuter) ? fragment._cachedNormalizedOuter : [];
        }

        getCachedFloorFragmentOuterBounds(fragment, outer = null) {
            if (!fragment) return null;
            if (fragment._cachedNormalizedOuterBounds) return fragment._cachedNormalizedOuterBounds;
            const ring = Array.isArray(outer) ? outer : this.getCachedFloorFragmentOuter(fragment);
            if (ring.length === 0) return null;
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < ring.length; i++) {
                const x = Number(ring[i] && ring[i].x);
                const y = Number(ring[i] && ring[i].y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
                minX = Math.min(minX, x);
                minY = Math.min(minY, y);
                maxX = Math.max(maxX, x);
                maxY = Math.max(maxY, y);
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
                return null;
            }
            fragment._cachedNormalizedOuterBounds = { minX, minY, maxX, maxY };
            return fragment._cachedNormalizedOuterBounds;
        }

        floorFragmentBoundsIntersectViewport(bounds, viewportBounds) {
            return !!(
                bounds &&
                viewportBounds &&
                bounds.maxX >= viewportBounds.minX &&
                bounds.minX <= viewportBounds.maxX &&
                bounds.maxY >= viewportBounds.minY &&
                bounds.minY <= viewportBounds.maxY
            );
        }

        getCachedPrototypeSectionWorldBounds(section, state) {
            if (!section) return null;
            if (section._cachedRenderWorldBounds) return section._cachedRenderWorldBounds;
            const geometry = typeof global !== "undefined" ? global.__sectionGeometry : null;
            let points = null;
            if (
                geometry &&
                typeof geometry.getSectionHexagonCorners === "function" &&
                section.centerAxial &&
                state &&
                state.basis
            ) {
                points = geometry.getSectionHexagonCorners(section.centerAxial, state.basis);
            }
            if (!Array.isArray(points) || points.length === 0) {
                throw new Error("missing section geometry for viewport floor section culling");
            }
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < points.length; i++) {
                const x = Number(points[i] && points[i].x);
                const y = Number(points[i] && points[i].y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
                minX = Math.min(minX, x);
                minY = Math.min(minY, y);
                maxX = Math.max(maxX, x);
                maxY = Math.max(maxY, y);
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
                return null;
            }
            section._cachedRenderWorldBounds = { minX, minY, maxX, maxY };
            return section._cachedRenderWorldBounds;
        }

        getPrototypeSectionTerrainClipPolygon(sectionKey, asset, state, mapRef = null) {
            if (mapRef && typeof mapRef.getGroundTerrainSectionClipPolygonPoints === "function") {
                const terrainClip = normalizeFloorVisualPointList(
                    mapRef.getGroundTerrainSectionClipPolygonPoints(sectionKey, asset)
                );
                if (terrainClip.length >= 3) {
                    return { outer: terrainClip, holes: [] };
                }
            }
            const explicitPolygon = normalizeFloorVisualPointList(asset && asset.sectionPolygon);
            if (explicitPolygon.length >= 3) {
                return { outer: explicitPolygon, holes: [] };
            }
            const section = (
                state &&
                state.sectionsByKey instanceof Map &&
                typeof sectionKey === "string"
            ) ? (state.sectionsByKey.get(sectionKey) || null) : null;
            const centerAxial = (
                asset &&
                asset.centerAxial &&
                Number.isFinite(Number(asset.centerAxial.q)) &&
                Number.isFinite(Number(asset.centerAxial.r))
            ) ? asset.centerAxial : (
                section &&
                section.centerAxial &&
                Number.isFinite(Number(section.centerAxial.q)) &&
                Number.isFinite(Number(section.centerAxial.r))
            ) ? section.centerAxial : null;
            const geometry = typeof global !== "undefined" ? global.__sectionGeometry : null;
            const polygon = (
                geometry &&
                typeof geometry.getSectionHexagonCorners === "function" &&
                centerAxial &&
                state &&
                state.basis
            ) ? geometry.getSectionHexagonCorners(centerAxial, state.basis) : null;
            const normalized = normalizeFloorVisualPointList(polygon);
            if (normalized.length >= 3) {
                return { outer: normalized, holes: [] };
            }
            throw new Error(`Cannot render terrain polygons for section ${sectionKey || "(unknown)"} without section polygon geometry.`);
        }

        collectViewportCandidateSectionKeys(ctx, mapRef, viewportBounds) {
            const sectionKeys = new Set();
            const state = mapRef && mapRef._prototypeSectionState ? mapRef._prototypeSectionState : null;
            const activeSectionKeys = mapRef && typeof mapRef.getPrototypeActiveSectionKeys === "function"
                ? mapRef.getPrototypeActiveSectionKeys()
                : (state && state.activeSectionKeys instanceof Set ? state.activeSectionKeys : null);
            if (!state || !(activeSectionKeys instanceof Set) || !(state.sectionsByKey instanceof Map) || !viewportBounds) {
                this.setFrameMetric("floorViewportSectionsConsidered", 0);
                this.setFrameMetric("floorViewportSectionsInFrame", 0);
                return sectionKeys;
            }
            let considered = 0;
            let inFrame = 0;
            activeSectionKeys.forEach((sectionKey) => {
                const section = state.sectionsByKey.get(sectionKey) || null;
                if (!section) return;
                considered += 1;
                const bounds = this.getCachedPrototypeSectionWorldBounds(section, state);
                const sectionViewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(
                    ctx,
                    viewportBounds && Number.isFinite(viewportBounds.margin) ? viewportBounds.margin : 4,
                    viewportBounds && Number.isFinite(viewportBounds.worldZ) ? viewportBounds.worldZ : (viewportBounds && Number.isFinite(viewportBounds.cameraZ) ? viewportBounds.cameraZ : 0)
                ) || viewportBounds;
                if (!this.floorFragmentBoundsIntersectViewport(bounds, sectionViewportBounds)) return;
                inFrame += 1;
                sectionKeys.add(sectionKey);
            });
            this.setFrameMetric("floorViewportSectionsConsidered", considered);
            this.setFrameMetric("floorViewportSectionsInFrame", inFrame);
            return sectionKeys;
        }

        collectViewportFloorSectionKeys(ctx, mapRef, viewportBounds) {
            const sectionKeys = new Set();
            if (!mapRef || !(mapRef.floorsById instanceof Map) || !viewportBounds) {
                this.setFrameMetric("floorViewportSectionsConsidered", 0);
                this.setFrameMetric("floorViewportSectionsInFrame", 0);
                this.setFrameMetric("floorViewportFragmentsScanned", 0);
                this.setFrameMetric("floorViewportFragmentsVisible", 0);
                this.setFrameMetric("floorViewportSectionKeys", 0);
                return sectionKeys;
            }
            const candidateSectionKeys = this.collectViewportCandidateSectionKeys(ctx, mapRef, viewportBounds);
            const selectedLevel = this.getSelectedFloorVisualLevel();
            const isolateLevel = isFloorEditIsolationActive();
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            let scannedFragments = 0;
            let visibleFragments = 0;
            let skippedLevel = 0;
            let skippedCutaway = 0;
            let skippedInvalid = 0;
            let skippedOffscreen = 0;
            const visitFragment = (fragment) => {
                scannedFragments += 1;
                if (!fragment) {
                    skippedInvalid += 1;
                    return;
                }
                const sectionKey = typeof fragment.ownerSectionKey === "string" ? fragment.ownerSectionKey : "";
                if (!sectionKey) {
                    skippedInvalid += 1;
                    return;
                }
                const level = Number.isFinite(fragment.level) ? Math.round(Number(fragment.level)) : 0;
                if (mazeLayerOnly && level !== wizardLayer) {
                    skippedLevel += 1;
                    return;
                }
                if (isolateLevel && level !== selectedLevel) {
                    skippedLevel += 1;
                    return;
                }
                if (!(this.getFloorFragmentCutawayAlpha(fragment, cutawayState) > 0.001)) {
                    skippedCutaway += 1;
                    return;
                }
                const outer = this.getCachedFloorFragmentOuter(fragment);
                if (outer.length < 3) {
                    skippedInvalid += 1;
                    return;
                }
                const fragmentBaseZ = Number.isFinite(Number(fragment.nodeBaseZ))
                    ? Number(fragment.nodeBaseZ)
                    : this.getLayerBaseZForLevel(level);
                const fragmentViewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, 4, fragmentBaseZ);
                const bounds = this.getCachedFloorFragmentOuterBounds(fragment, outer);
                if (!this.floorFragmentBoundsIntersectViewport(bounds, fragmentViewportBounds)) {
                    skippedOffscreen += 1;
                    return;
                }
                visibleFragments += 1;
                sectionKeys.add(sectionKey);
            };
            if (candidateSectionKeys.size > 0 && mapRef.floorFragmentsBySectionKey instanceof Map) {
                candidateSectionKeys.forEach((sectionKey) => {
                    const fragmentIds = mapRef.floorFragmentsBySectionKey.get(sectionKey);
                    if (!(fragmentIds instanceof Set)) return;
                    fragmentIds.forEach((fragmentId) => {
                        visitFragment(mapRef.floorsById.get(fragmentId) || null);
                    });
                });
            } else if (!(mapRef.floorFragmentsBySectionKey instanceof Map)) {
                for (const fragment of mapRef.floorsById.values()) {
                    visitFragment(fragment);
                }
            }
            this.setFrameMetric("floorViewportFragmentsScanned", scannedFragments);
            this.setFrameMetric("floorViewportFragmentsVisible", visibleFragments);
            this.setFrameMetric("floorViewportFragmentsSkippedLevel", skippedLevel);
            this.setFrameMetric("floorViewportFragmentsSkippedCutaway", skippedCutaway);
            this.setFrameMetric("floorViewportFragmentsSkippedInvalid", skippedInvalid);
            this.setFrameMetric("floorViewportFragmentsSkippedOffscreen", skippedOffscreen);
            this.setFrameMetric("floorViewportSectionKeys", sectionKeys.size);
            return sectionKeys;
        }

        collectVisibleFloorObjectNodes(ctx, mapRef, visibleNodes = []) {
            const map = mapRef || (ctx && ctx.map) || null;
            if (!map || !(map.floorNodesById instanceof Map)) return [];
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx);
            if (interiorScope && interiorScope.active) {
                if (!this._collectFloorObjectNodesOut) this._collectFloorObjectNodesOut = [];
                this._collectFloorObjectNodesOut.length = 0;
                this.setFrameMetric("floorObjectNodeInteriorScopeActive", 1);
                this.setFrameMetric("floorObjectNodeCandidates", 0);
                this.setFrameMetric("floorObjectNodeSectionsScanned", 0);
                this.setFrameMetric("floorObjectNodeYRowsScanned", 0);
                this.setFrameMetric("floorObjectNodeCandidatesScanned", 0);
                this.setFrameMetric("floorObjectNodesSkippedStale", 0);
                this.setFrameMetric("floorObjectNodesSkippedOffscreen", 0);
                this.setFrameMetric("floorObjectNodesSkippedDuplicate", 0);
                return this._collectFloorObjectNodesOut;
            }
            this.setFrameMetric("floorObjectNodeInteriorScopeActive", 0);
            const cameraRef = (ctx && ctx.camera) || this.camera || {};
            const viewportRef = (ctx && ctx.viewport) || {};
            const xScale = 0.866;
            const padX = 4;
            const padY = 4;
            const zRange = this.getRenderableZRangeForViewportCull(ctx, map);
            const indexBounds = this.getCameraScreenSpaceIndexBounds(ctx, 4, zRange);
            const minX = indexBounds
                ? Math.floor(indexBounds.minX / xScale) - padX
                : Math.floor((Number(cameraRef.x) || 0) / xScale) - padX;
            const maxX = indexBounds
                ? Math.ceil(indexBounds.maxX / xScale) + padX
                : Math.ceil(((Number(cameraRef.x) || 0) + (Number(viewportRef.width) || 0)) / xScale) + padX;
            const minY = indexBounds
                ? Math.floor(indexBounds.minY) - padY
                : Math.floor(Number(cameraRef.y) || 0) - padY;
            const maxY = indexBounds
                ? Math.ceil(indexBounds.maxY) + padY
                : Math.ceil((Number(cameraRef.y) || 0) + (Number(viewportRef.height) || 0)) + padY;
            const viewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(
                ctx,
                4,
                Number.isFinite(cameraRef.z) ? Number(cameraRef.z) : (Number.isFinite(viewportRef.z) ? Number(viewportRef.z) : 0)
            );
            const viewportSectionKeys = this.collectViewportCandidateSectionKeys(ctx, map, viewportBounds);
            const useViewportSectionFilter = !!(map.floorsById instanceof Map && viewportBounds);
            const activeSectionKeys = (
                !useViewportSectionFilter &&
                map &&
                typeof map.getPrototypeActiveSectionKeys === "function"
            ) ? map.getPrototypeActiveSectionKeys() : null;
            this.setFrameMetric("floorViewportSectionFilterActive", useViewportSectionFilter ? 1 : 0);
            const hasLiveEntries = (list) => {
                if (!Array.isArray(list) || list.length === 0) return false;
                for (let i = 0; i < list.length; i++) {
                    const obj = list[i];
                    if (!obj || obj.gone || obj.vanishing || obj._prototypeParked === true) continue;
                    if (obj.type === "road" || obj.type === "roadPath") continue;
                    return true;
                }
                return false;
            };
            const shouldCacheFloorObjectNode = (node) => {
                if (!node) return false;
                const hasIndexedEntries = (list) => {
                    if (!Array.isArray(list) || list.length === 0) return false;
                    for (let i = 0; i < list.length; i++) {
                        const obj = list[i];
                        if (!obj || obj.gone || obj.vanishing || obj._prototypeParked === true) continue;
                        if (obj.type === "road" || obj.type === "roadPath") continue;
                        return true;
                    }
                    return false;
                };
                return hasIndexedEntries(node.objects) || hasIndexedEntries(node.visibilityObjects);
            };
            const shouldScanFloorObjectNode = (node) => {
                if (!node) return false;
                const ownerSectionKey = typeof node.ownerSectionKey === "string" && node.ownerSectionKey.length > 0
                    ? node.ownerSectionKey
                    : (typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "");
                const activitySectionKey = typeof node._prototypeSectionKey === "string" && node._prototypeSectionKey.length > 0
                    ? node._prototypeSectionKey
                    : ownerSectionKey;
                if (
                    ownerSectionKey &&
                    useViewportSectionFilter &&
                    !viewportSectionKeys.has(ownerSectionKey)
                ) {
                    return false;
                }
                if (
                    !useViewportSectionFilter &&
                    activitySectionKey &&
                    activeSectionKeys instanceof Set &&
                    activeSectionKeys.size > 0 &&
                    !activeSectionKeys.has(activitySectionKey)
                ) {
                    return false;
                }
                return hasLiveEntries(node.objects) || hasLiveEntries(node.visibilityObjects);
            };

            const mapSizeNow = map.floorNodesById.size;
            const cacheVersionNow = Number(map._floorObjectNodeCacheVersion) || 0;
            let rebuiltFloorObjectNodeCache = 0;
            let floorObjectNodeIndexBuildMs = 0;
            let floorObjectNodeIndexRows = 0;
            let floorObjectNodeIndexReady = 0;
            let floorObjectNodeIndex = (map && typeof map.getFloorObjectNodeSpatialIndex === "function")
                ? map.getFloorObjectNodeSpatialIndex()
                : null;
            const bubbleShiftActive = !!(
                map &&
                map._prototypeBubbleShiftSession &&
                map._prototypeBubbleShiftSession.completed !== true
            );
            const indexIsCurrent = (index) => !!(
                index &&
                index.version === cacheVersionNow &&
                index.mapSize === mapSizeNow &&
                index.bySectionY instanceof Map &&
                index.allByY instanceof Map
            );
            if (!indexIsCurrent(floorObjectNodeIndex)) {
                if (!bubbleShiftActive && map && typeof map.ensureFloorObjectNodeSpatialIndex === "function") {
                    const buildStartMs = performance.now();
                    floorObjectNodeIndex = map.ensureFloorObjectNodeSpatialIndex();
                    floorObjectNodeIndexBuildMs = performance.now() - buildStartMs;
                    rebuiltFloorObjectNodeCache = 1;
                } else {
                    // Non-GameMap test fixtures can still exercise this renderer path.
                    const bySectionY = new Map();
                    const allByY = new Map();
                    let totalNodes = 0;
                    const getRow = (rowsByY, yKey) => {
                        let row = rowsByY.get(yKey);
                        if (!Array.isArray(row)) {
                            row = [];
                            rowsByY.set(yKey, row);
                        }
                        return row;
                    };
                    const rowsToSort = [];
                    const addNode = (node) => {
                        if (!shouldCacheFloorObjectNode(node)) return;
                        const yKey = Number.isFinite(node.yindex) ? Math.round(Number(node.yindex)) : null;
                        if (!Number.isFinite(yKey)) return;
                        const sectionKey = typeof node.ownerSectionKey === "string" && node.ownerSectionKey.length > 0
                            ? node.ownerSectionKey
                            : (typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "");
                        let sectionRows = bySectionY.get(sectionKey);
                        if (!(sectionRows instanceof Map)) {
                            sectionRows = new Map();
                            bySectionY.set(sectionKey, sectionRows);
                        }
                        getRow(sectionRows, yKey).push(node);
                        getRow(allByY, yKey).push(node);
                        totalNodes += 1;
                    };
                    for (const nodeArr of map.floorNodesById.values()) {
                        if (!Array.isArray(nodeArr)) continue;
                        for (let i = 0; i < nodeArr.length; i++) {
                            addNode(nodeArr[i]);
                        }
                    }
                    for (const sectionRows of bySectionY.values()) {
                        for (const row of sectionRows.values()) {
                            if (Array.isArray(row) && row.length > 1) rowsToSort.push(row);
                        }
                    }
                    for (const row of allByY.values()) {
                        if (Array.isArray(row) && row.length > 1) rowsToSort.push(row);
                    }
                    for (let r = 0; r < rowsToSort.length; r++) {
                        rowsToSort[r].sort((left, right) => (Number(left && left.xindex) || 0) - (Number(right && right.xindex) || 0));
                    }
                    floorObjectNodeIndex = { version: cacheVersionNow, mapSize: mapSizeNow, bySectionY, allByY, totalNodes, rowsBuilt: rowsToSort.length };
                    rebuiltFloorObjectNodeCache = 1;
                }
            }
            floorObjectNodeIndexReady = indexIsCurrent(floorObjectNodeIndex) ? 1 : 0;
            floorObjectNodeIndexRows = floorObjectNodeIndex && Number.isFinite(floorObjectNodeIndex.rowsBuilt)
                ? Number(floorObjectNodeIndex.rowsBuilt)
                : 0;
            this.setFrameMetric("floorObjectNodeCandidates", floorObjectNodeIndex && Number.isFinite(floorObjectNodeIndex.totalNodes) ? Number(floorObjectNodeIndex.totalNodes) : 0);
            this.setFrameMetric("floorObjectNodeCacheVersion", cacheVersionNow);
            this.setFrameMetric("floorObjectNodeCacheRebuilt", rebuiltFloorObjectNodeCache);
            this.setFrameMetric("floorObjectNodeIndexBuildMs", floorObjectNodeIndexBuildMs);
            this.setFrameMetric("floorObjectNodeIndexRows", floorObjectNodeIndexRows);
            this.setFrameMetric("floorObjectNodeIndexReady", floorObjectNodeIndexReady);
            this.setFrameMetric("floorObjectNodeIndexStaleDuringBubbleShift", (!floorObjectNodeIndexReady && bubbleShiftActive) ? 1 : 0);
            const floorNodeCandidateBucketsBySectionY = floorObjectNodeIndex && floorObjectNodeIndex.bySectionY instanceof Map
                ? floorObjectNodeIndex.bySectionY
                : null;
            const floorNodeCandidateBucketsByY = floorObjectNodeIndex && floorObjectNodeIndex.allByY instanceof Map
                ? floorObjectNodeIndex.allByY
                : null;

            // Reuse the seenNodeKeys set already built by collectVisibleNodes (same nodes, same keys).
            // Falls back to building from the visibleNodes array if _collectVisibleNodesSeenKeys is absent.
            let seenNodeKeys;
            if (this._collectVisibleNodesSeenKeys instanceof Set) {
                seenNodeKeys = this._collectVisibleNodesSeenKeys;
            } else {
                if (!this._collectFloorObjectNodesSeenKeys) this._collectFloorObjectNodesSeenKeys = new Set();
                seenNodeKeys = this._collectFloorObjectNodesSeenKeys;
                seenNodeKeys.clear();
                if (Array.isArray(visibleNodes)) {
                    for (let i = 0; i < visibleNodes.length; i++) {
                        const node = visibleNodes[i];
                        if (!node) continue;
                        const key = this.getVisibleNodeRenderKey(node);
                        seenNodeKeys.add(key);
                    }
                }
            }
            if (!this._collectFloorObjectNodesOut) this._collectFloorObjectNodesOut = [];
            const out = this._collectFloorObjectNodesOut;
            out.length = 0;
            let skippedStale = 0;
            let skippedOffscreen = 0;
            let skippedDuplicate = 0;
            let sectionsScanned = 0;
            let candidatesScanned = 0;
            let yRowsScanned = 0;
            const lowerBoundFloorNodeX = (row, targetX) => {
                let lo = 0;
                let hi = Array.isArray(row) ? row.length : 0;
                while (lo < hi) {
                    const mid = (lo + hi) >> 1;
                    const x = Number(row[mid] && row[mid].xindex);
                    if (Number.isFinite(x) && x < targetX) lo = mid + 1;
                    else hi = mid;
                }
                return lo;
            };
            const upperBoundFloorNodeX = (row, targetX) => {
                let lo = 0;
                let hi = Array.isArray(row) ? row.length : 0;
                while (lo < hi) {
                    const mid = (lo + hi) >> 1;
                    const x = Number(row[mid] && row[mid].xindex);
                    if (Number.isFinite(x) && x <= targetX) lo = mid + 1;
                    else hi = mid;
                }
                return lo;
            };
            const visitRowRange = (row) => {
                if (!Array.isArray(row) || row.length === 0) return;
                yRowsScanned += 1;
                const start = lowerBoundFloorNodeX(row, minX);
                const end = upperBoundFloorNodeX(row, maxX);
                for (let i = start; i < end; i++) {
                    candidatesScanned += 1;
                    visitCandidateNode(row[i]);
                }
            };
            const visitCandidateNode = (node) => {
                if (!node) return;
                // Re-check in case objects were removed since the cache was built
                if (!shouldScanFloorObjectNode(node)) {
                    skippedStale += 1;
                    return;
                }
                const debugObjects = Array.isArray(node.objects) ? node.objects : [];
                for (let j = 0; j < debugObjects.length; j++) {
                    this.logPlaceObjectRenderDebug("floor-node-candidate", debugObjects[j], {
                        nodeId: typeof node.id === "string" ? node.id : "",
                        nodeX: Number.isFinite(node.xindex) ? Number(node.xindex) : null,
                        nodeY: Number.isFinite(node.yindex) ? Number(node.yindex) : null,
                        minX,
                        maxX,
                        minY,
                        maxY
                    });
                }
                const xi = Number(node.xindex);
                const yi = Number(node.yindex);
                if (!Number.isFinite(xi) || !Number.isFinite(yi)) return;
                if (xi < minX || xi > maxX || yi < minY || yi > maxY) {
                    skippedOffscreen += 1;
                    for (let j = 0; j < debugObjects.length; j++) {
                        this.logPlaceObjectRenderDebug("floor-node-candidate-offscreen", debugObjects[j], {
                            nodeId: typeof node.id === "string" ? node.id : "",
                            nodeX: xi,
                            nodeY: yi,
                            minX,
                            maxX,
                            minY,
                            maxY
                        });
                    }
                    return;
                }
                const nodeBaseZ = this.getLayerBaseZForNode(node);
                const nodeViewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, 4, nodeBaseZ);
                if (
                    nodeViewportBounds &&
                    (
                        Number(node.x) < nodeViewportBounds.minX ||
                        Number(node.x) > nodeViewportBounds.maxX ||
                        Number(node.y) < nodeViewportBounds.minY ||
                        Number(node.y) > nodeViewportBounds.maxY
                    )
                ) {
                    skippedOffscreen += 1;
                    return;
                }
                const key = this.getVisibleNodeRenderKey(node);
                if (seenNodeKeys.has(key)) {
                    skippedDuplicate += 1;
                    return;
                }
                seenNodeKeys.add(key);
                out.push(node);
            };
            if (floorNodeCandidateBucketsBySectionY && useViewportSectionFilter) {
                viewportSectionKeys.forEach((sectionKey) => {
                    const rowsByY = floorNodeCandidateBucketsBySectionY.get(sectionKey);
                    if (!(rowsByY instanceof Map) || rowsByY.size === 0) return;
                    sectionsScanned += 1;
                    for (let y = minY; y <= maxY; y++) visitRowRange(rowsByY.get(y));
                });
            } else {
                sectionsScanned = floorNodeCandidateBucketsBySectionY ? floorNodeCandidateBucketsBySectionY.size : 0;
                if (floorNodeCandidateBucketsByY) {
                    for (let y = minY; y <= maxY; y++) visitRowRange(floorNodeCandidateBucketsByY.get(y));
                }
            }
            this.setFrameMetric("floorObjectNodeSectionsScanned", sectionsScanned);
            this.setFrameMetric("floorObjectNodeYRowsScanned", yRowsScanned);
            this.setFrameMetric("floorObjectNodeCandidatesScanned", candidatesScanned);
            this.setFrameMetric("floorObjectNodesSkippedStale", skippedStale);
            this.setFrameMetric("floorObjectNodesSkippedOffscreen", skippedOffscreen);
            this.setFrameMetric("floorObjectNodesSkippedDuplicate", skippedDuplicate);
            return out;
        }

        collectVisibleNodes(ctx, xPadding = 0, yPadding = 0) {
            const map = ctx.map;
            if (!map || !Array.isArray(map.nodes)) return [];
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx);
            if (interiorScope && interiorScope.active) {
                if (!this._collectVisibleNodesOut) this._collectVisibleNodesOut = [];
                const nodes = this._collectVisibleNodesOut;
                nodes.length = 0;
                if (!this._collectVisibleNodesSeenKeys) this._collectVisibleNodesSeenKeys = new Set();
                this._collectVisibleNodesSeenKeys.clear();
                this.setFrameMetric("visibleNodes", 0);
                this.setFrameMetric("visibleNodesWrapped", 0);
                this.setFrameMetric("visibleNodesFallback", 0);
                this.setFrameMetric("visibleNodeFilterSkipped", 0);
                this.setFrameMetric("visibleNodeFallbackUsed", 0);
                this.setFrameMetric("visibleNodesSkippedBuildingInteriorScope", 1);
                this.setFrameMetric("visibleFloorNodes", 0);
                return nodes;
            }
            this.setFrameMetric("visibleNodesSkippedBuildingInteriorScope", 0);
            const prototypeState = map._prototypeSectionState || null;
            this.setFrameMetric(
                "visibleLoadedNodes",
                prototypeState && Array.isArray(prototypeState.loadedNodes) ? prototypeState.loadedNodes.length : 0
            );
            this.setFrameMetric(
                "visibleNodeCoordIndexSize",
                prototypeState && prototypeState.loadedNodesByCoordKey instanceof Map
                    ? prototypeState.loadedNodesByCoordKey.size
                    : 0
            );
            if (!this._collectVisibleNodesOut) this._collectVisibleNodesOut = [];
            const nodes = this._collectVisibleNodesOut;
            nodes.length = 0;
            if (!this._collectVisibleNodesSeenKeys) this._collectVisibleNodesSeenKeys = new Set();
            const seenNodeKeys = this._collectVisibleNodesSeenKeys;
            seenNodeKeys.clear();
            const shouldRenderNode = (typeof map.shouldRenderNode === "function")
                ? map.shouldRenderNode.bind(map)
                : null;
            const selectedFloorLevel = Number.isFinite(global.selectedFloorEditLevel)
                ? Math.round(Number(global.selectedFloorEditLevel))
                : 0;
            const isolateFloorLevel = isFloorEditIsolationActive();
            // Floor editor surfaces are polygon meshes now; these tile-like
            // runtime floor nodes are only needed by traversal/pathfinding.
            const collectSyntheticFloorNodes = false;
            const shouldRenderFloorLevel = (node) => {
                if (!node) return false;
                const level = Number.isFinite(node.traversalLayer)
                    ? Number(node.traversalLayer)
                    : (Number.isFinite(node.level) ? Number(node.level) : 0);
                if (!isolateFloorLevel) return true;
                if (Math.round(level) !== selectedFloorLevel) return false;
                const tileKey = `${node.xindex},${node.yindex}`;
                if (selectedFloorLevel !== 0) {
                    const hiddenByLevel = map._floorEditHiddenTileKeysByLevel;
                    if (hiddenByLevel instanceof Map) {
                        const hiddenKeys = hiddenByLevel.get(selectedFloorLevel);
                        if (hiddenKeys instanceof Set && hiddenKeys.has(tileKey)) {
                            return false;
                        }
                    }
                    const sectionKey = typeof node._prototypeSectionKey === "string"
                        ? node._prototypeSectionKey
                        : "";
                    const state = map._prototypeSectionState || null;
                    const asset = sectionKey && state && state.sectionAssetsByKey instanceof Map
                        ? state.sectionAssetsByKey.get(sectionKey)
                        : null;
                    if (asset && Array.isArray(asset.floorHoles)) {
                        for (let i = 0; i < asset.floorHoles.length; i++) {
                            const hole = asset.floorHoles[i];
                            if (!hole || Math.round(Number(hole.level) || 0) !== selectedFloorLevel) continue;
                            if (!(hole._tileCoordKeySet instanceof Set)) {
                                hole._tileCoordKeySet = new Set(Array.isArray(hole.tileCoordKeys) ? hole.tileCoordKeys : []);
                            }
                            if (hole._tileCoordKeySet.has(tileKey)) return false;
                        }
                    }
                }
                return true;
            };
            let skippedByRenderFilter = 0;
            let wrappedNodes = 0;
            let fallbackNodes = 0;
            // On non-wrapping maps getVisibleNodesInViewport never returns duplicates,
            // so the has() guard is pure overhead. Keep it only when wrapping is active.
            const _mapWraps = !!(map.wrapX || map.wrapY);
            const addVisibleNode = _mapWraps
                ? (node) => {
                    if (!node) return false;
                    const key = this.getVisibleNodeRenderKey(node);
                    if (seenNodeKeys.has(key)) return false;
                    seenNodeKeys.add(key);
                    nodes.push(node);
                    return true;
                }
                : (node) => {
                    if (!node) return false;
                    const key = this.getVisibleNodeRenderKey(node);
                    seenNodeKeys.add(key);
                    nodes.push(node);
                    return true;
                };

            const processViewportNode = (node) => {
                if (shouldRenderNode && !shouldRenderNode(node)) {
                    skippedByRenderFilter += 1;
                    return;
                }
                if (!shouldRenderFloorLevel(node)) {
                    skippedByRenderFilter += 1;
                    return;
                }
                if (addVisibleNode(node)) {
                    wrappedNodes += 1;
                }
            };
            if (typeof map.getVisibleNodesInViewport === "function") {
                const viewportNodes = map.getVisibleNodesInViewport(ctx.camera || this.camera || {}, xPadding, yPadding);
                if (!Array.isArray(viewportNodes)) {
                    throw new Error("prototype visible node query must return an array");
                }
                for (let i = 0; i < viewportNodes.length; i++) {
                    const node = viewportNodes[i];
                    if (!node) continue;
                    if (shouldRenderNode && !shouldRenderNode(node)) {
                        skippedByRenderFilter += 1;
                        continue;
                    }
                    if (!shouldRenderFloorLevel(node)) {
                        skippedByRenderFilter += 1;
                        continue;
                    }
                    if (addVisibleNode(node)) {
                        wrappedNodes += 1;
                    }
                }
            } else {
                this.forEachWrappedNodeInViewport(
                    map,
                    xPadding,
                    yPadding,
                    processViewportNode,
                    ctx.camera
                );
            }
            const visibleNodeBucketStats = (map && typeof map.getVisibleNodeBucketIndexStats === "function")
                ? map.getVisibleNodeBucketIndexStats()
                : null;
            this.setFrameMetric(
                "visibleNodeBucketCandidates",
                visibleNodeBucketStats && Number.isFinite(visibleNodeBucketStats.candidates)
                    ? Number(visibleNodeBucketStats.candidates)
                    : 0
            );
            this.setFrameMetric(
                "visibleNodeBucketCount",
                visibleNodeBucketStats && Number.isFinite(visibleNodeBucketStats.bucketCount)
                    ? Number(visibleNodeBucketStats.bucketCount)
                    : 0
            );
            if (isolateFloorLevel && collectSyntheticFloorNodes) {
                this.collectVisibleFloorNodes(ctx, selectedFloorLevel, shouldRenderNode, shouldRenderFloorLevel, addVisibleNode);
            } else {
                this.setFrameMetric("visibleFloorNodes", 0);
            }
            if (nodes.length > 0 || (isolateFloorLevel && selectedFloorLevel !== 0)) {
                this.setFrameMetric("visibleNodes", nodes.length);
                this.setFrameMetric("visibleNodesWrapped", wrappedNodes);
                this.setFrameMetric("visibleNodesFallback", 0);
                this.setFrameMetric("visibleNodeFilterSkipped", skippedByRenderFilter);
                this.setFrameMetric("visibleNodeFallbackUsed", 0);
                return nodes;
            }

            const cam = this.camera;
            const padX = Math.max(0, Number.isFinite(xPadding) ? Math.floor(xPadding) : 0);
            const padY = Math.max(0, Number.isFinite(yPadding) ? Math.floor(yPadding) : 0);
            const minX = Math.max(0, Math.floor(cam.x / 0.866) - padX);
            const maxX = Math.min(map.width - 1, Math.ceil((cam.x + ctx.viewport.width) / 0.866) + padX);
            const minY = Math.max(0, Math.floor(cam.y) - padY);
            const maxY = Math.min(map.height - 1, Math.ceil(cam.y + ctx.viewport.height) + padY);
            for (let x = minX; x <= maxX; x++) {
                const col = map.nodes[x];
                if (!Array.isArray(col)) continue;
                for (let y = minY; y <= maxY; y++) {
                    const node = col[y];
                    if (shouldRenderNode && !shouldRenderNode(node)) {
                        skippedByRenderFilter += 1;
                        continue;
                    }
                    if (!shouldRenderFloorLevel(node)) {
                        skippedByRenderFilter += 1;
                        continue;
                    }
                    if (addVisibleNode(node)) {
                        fallbackNodes += 1;
                    }
                }
            }
            if (isolateFloorLevel && collectSyntheticFloorNodes) {
                this.collectVisibleFloorNodes(ctx, selectedFloorLevel, shouldRenderNode, shouldRenderFloorLevel, addVisibleNode);
            } else {
                this.setFrameMetric("visibleFloorNodes", 0);
            }
            this.setFrameMetric("visibleNodes", nodes.length);
            this.setFrameMetric("visibleNodesWrapped", wrappedNodes);
            this.setFrameMetric("visibleNodesFallback", fallbackNodes);
            this.setFrameMetric("visibleNodeFilterSkipped", skippedByRenderFilter);
            this.setFrameMetric("visibleNodeFallbackUsed", fallbackNodes > 0 ? 1 : 0);
            return nodes;
        }

        collectVisibleFloorNodes(ctx, selectedFloorLevel, shouldRenderNode, shouldRenderFloorLevel, addVisibleNode) {
            const map = ctx && ctx.map;
            if (!map || !(map.floorNodesById instanceof Map)) return 0;
            const cam = this.camera;
            const padX = 4;
            const padY = 4;
            const xScale = 0.866;
            const zRange = this.getRenderableZRangeForViewportCull(ctx, map);
            const indexBounds = this.getCameraScreenSpaceIndexBounds(ctx, 4, zRange);
            const minX = indexBounds
                ? Math.floor(indexBounds.minX / xScale) - padX
                : Math.floor((Number(cam.x) || 0) / xScale) - padX;
            const maxX = indexBounds
                ? Math.ceil(indexBounds.maxX / xScale) + padX
                : Math.ceil(((Number(cam.x) || 0) + (Number(ctx.viewport && ctx.viewport.width) || 0)) / xScale) + padX;
            const minY = indexBounds
                ? Math.floor(indexBounds.minY) - padY
                : Math.floor(Number(cam.y) || 0) - padY;
            const maxY = indexBounds
                ? Math.ceil(indexBounds.maxY) + padY
                : Math.ceil((Number(cam.y) || 0) + (Number(ctx.viewport && ctx.viewport.height) || 0)) + padY;
            let added = 0;
            for (const floorNodes of map.floorNodesById.values()) {
                if (!Array.isArray(floorNodes) || floorNodes.length === 0) continue;
                for (let i = 0; i < floorNodes.length; i++) {
                    const node = floorNodes[i];
                    if (!node) continue;
                    if (!shouldRenderFloorLevel(node)) continue;
                    if (shouldRenderNode && !shouldRenderNode(node)) continue;
                    const xi = Number(node.xindex);
                    const yi = Number(node.yindex);
                    if (!Number.isFinite(xi) || !Number.isFinite(yi)) continue;
                    if (xi < minX || xi > maxX || yi < minY || yi > maxY) continue;
                    const nodeBaseZ = this.getLayerBaseZForNode(node);
                    const nodeViewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, 4, nodeBaseZ);
                    if (
                        nodeViewportBounds &&
                        (
                            Number(node.x) < nodeViewportBounds.minX ||
                            Number(node.x) > nodeViewportBounds.maxX ||
                            Number(node.y) < nodeViewportBounds.minY ||
                            Number(node.y) > nodeViewportBounds.maxY
                        )
                    ) {
                        continue;
                    }
                    if (addVisibleNode(node)) added += 1;
                }
            }
            if (Number.isFinite(selectedFloorLevel)) {
                this.setFrameMetric("visibleFloorNodes", added);
            }
            return added;
        }

        syncOnscreenObjectsCache(ctx, visibleNodes, visibleObjectsOverride = null) {
            const cache = (typeof global.onscreenObjects !== "undefined") ? global.onscreenObjects : null;
            if (!cache || typeof cache.clear !== "function" || typeof cache.add !== "function") return;
            cache.clear();

            const visibleObjects = Array.isArray(visibleObjectsOverride)
                ? visibleObjectsOverride
                : this.collectVisibleObjects(visibleNodes, ctx);
            let cacheObjectsAdded = 0;
            for (let i = 0; i < visibleObjects.length; i++) {
                const obj = visibleObjects[i];
                if (!obj || obj.gone || obj.vanishing) continue;
                cache.add(obj);
                cacheObjectsAdded += 1;
            }

            const roofList = this.getRoofsList(ctx);
            let cacheRoofsAdded = 0;
            for (let i = 0; i < roofList.length; i++) {
                const roofRef = roofList[i];
                if (!roofRef || roofRef.gone || !roofRef.placed || !roofRef.pixiMesh || !roofRef.pixiMesh.visible) continue;
                cache.add(roofRef);
                cacheRoofsAdded += 1;
            }
            this.setFrameMetric("onscreenCacheObjects", cacheObjectsAdded);
            this.setFrameMetric("onscreenCacheRoofs", cacheRoofsAdded);
        }

        shouldUseDepthBillboard(item) {
            if (!item || item.gone || item.vanishing) return false;
            if (item._flowerBurnFragmentContainer && Array.isArray(item._flowerBurnFragments) && item._flowerBurnFragments.length > 0) {
                return false;
            }
            if (item.type === "road" || item.type === "roadPath" || item.type === "roof" || item.type === "wallSection") {
                return false;
            }
            if (item.type === "triggerArea" || item.isTriggerArea === true) {
                return false;
            }
            const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
            const isSpatialDoorOrWindow = !!(
                item.rotationAxis === "spatial" &&
                (category === "windows" || category === "doors" || item.type === "window" || item.type === "door")
            );
            const isWallMountedSpatial = this.isWallMountedSpatialItem(item);
            if (item.rotationAxis === "spatial" && !isSpatialDoorOrWindow) return false;
            if (typeof item.updateDepthBillboardMesh !== "function") return false;
            const sprite = item.pixiSprite;
            if (!sprite && !(typeof item.texturePath === "string" && item.texturePath.length > 0)) return false;
            return true;
        }

        applyScriptBrightness(item, displayObj = null) {
            if (!item) return;
            const scriptingApi = (typeof global.Scripting !== "undefined" && global.Scripting)
                ? global.Scripting
                : ((typeof Scripting !== "undefined" && Scripting) ? Scripting : null);
            if (!scriptingApi || typeof scriptingApi.applyTargetBrightness !== "function") return;
            scriptingApi.applyTargetBrightness(item, displayObj);
        }

        applyFrozenTint(item, displayObj = null) {
            if (!item) return;
            const nowMs = Date.now();
            const clamp01 = value => Math.max(0, Math.min(1, Number(value) || 0));
            const fullyFrozenBaseTint = 0x2222ff;
            const freezeBrightnessFilterKey = "__freezeBrightnessFilter";
            const freezeTemperatureRecoverRateDegreesPerSecond = 1;
            const frozenUntilMs = Number(item._freezeTintUntilMs);
            const degreesBelowBaseline = (typeof item.getDegreesBelowBaseline === "function")
                ? Math.max(0, Number(item.getDegreesBelowBaseline()) || 0)
                : 0;
            const baselineTemperature = (typeof item.getTemperatureBaseline === "function")
                ? Number(item.getTemperatureBaseline())
                : Number(item.baselineTemperature);
            const freezeThreshold = (typeof item.getFreezeTemperatureThreshold === "function")
                ? Number(item.getFreezeTemperatureThreshold())
                : -20;
            const fullFreezeDegrees = Math.max(
                1,
                Number.isFinite(baselineTemperature) && Number.isFinite(freezeThreshold)
                    ? Math.abs(baselineTemperature - freezeThreshold)
                    : 20
            );
            const isDead = item.dead === true;
            const isTemperatureFrozen = typeof item.isTemperatureFrozen === "function" && item.isTemperatureFrozen();
            const hasFreezeFlash = Number.isFinite(frozenUntilMs) && frozenUntilMs > nowMs;
            const deadFreezeFadeCompleted = isDead && item._freezeDeathFadeCompleted === true;
            const liveTemperatureFreezeProgress = clamp01(degreesBelowBaseline / fullFreezeDegrees);
            const liveFreezeProgress = deadFreezeFadeCompleted
                ? 0
                : Math.max(liveTemperatureFreezeProgress, hasFreezeFlash ? 0.35 : 0);
            const hadFrozenVisualState = !!(
                !deadFreezeFadeCompleted && (
                    liveFreezeProgress > 1e-6 ||
                    Number.isFinite(item._freezeOriginalTint) ||
                    Number.isFinite(item._freezeDeathFadeStartedAtMs) ||
                    Number.isFinite(item._freezeDeathFadeInitialDegreesBelow)
                )
            );
            if (isDead && hadFrozenVisualState && !Number.isFinite(item._freezeDeathFadeStartedAtMs)) {
                item._freezeDeathFadeStartedAtMs = nowMs;
                item._freezeDeathFadeInitialDegreesBelow = Math.max(0, liveFreezeProgress * fullFreezeDegrees);
                item._freezeDeathFadeCompleted = false;
            } else if (!isDead && Number.isFinite(item._freezeDeathFadeStartedAtMs)) {
                item._freezeDeathFadeStartedAtMs = null;
                item._freezeDeathFadeInitialDegreesBelow = null;
                item._freezeDeathFadeCompleted = null;
            }
            const deathFadeStartMs = Number(item._freezeDeathFadeStartedAtMs);
            const deathFadeInitialDegreesBelow = Number(item._freezeDeathFadeInitialDegreesBelow);
            const corpseDegreesBelowBaseline = (isDead && Number.isFinite(deathFadeStartMs))
                ? Math.max(
                    0,
                    (Number.isFinite(deathFadeInitialDegreesBelow)
                        ? deathFadeInitialDegreesBelow
                        : (liveFreezeProgress * fullFreezeDegrees)) -
                    (((nowMs - deathFadeStartMs) / 1000) * freezeTemperatureRecoverRateDegreesPerSecond)
                )
                : null;
            const freezeProgress = isDead
                ? clamp01((Number.isFinite(corpseDegreesBelowBaseline) ? corpseDegreesBelowBaseline : 0) / fullFreezeDegrees)
                : liveFreezeProgress;
            const shouldRenderFrozen = hadFrozenVisualState && freezeProgress > 1e-6;
            const targetTint = Number.isFinite(item._freezeTintColor)
                ? Math.max(0, Math.min(0xFFFFFF, Math.floor(Number(item._freezeTintColor))))
                : 0x9fd8ff;
            const baseTint = Number.isFinite(item.tint)
                ? Math.max(0, Math.min(0xFFFFFF, Math.floor(Number(item.tint))))
                : (Number.isFinite(item._freezeOriginalTint) ? Number(item._freezeOriginalTint) : 0xFFFFFF);
            const displayObjects = new Set();
            if (displayObj && typeof displayObj === "object") displayObjects.add(displayObj);
            if (item.pixiSprite && typeof item.pixiSprite === "object") displayObjects.add(item.pixiSprite);
            if (item._renderingDepthMesh && typeof item._renderingDepthMesh === "object") displayObjects.add(item._renderingDepthMesh);
            if (item._renderingDisplayObject && typeof item._renderingDisplayObject === "object") displayObjects.add(item._renderingDisplayObject);
            if (item._compositeUnderlayMesh && typeof item._compositeUnderlayMesh === "object") displayObjects.add(item._compositeUnderlayMesh);

            if (shouldRenderFrozen) {
                if (!Number.isFinite(item._freezeOriginalTint)) {
                    const currentTint = Number.isFinite(item.tint)
                        ? Number(item.tint)
                        : (item.pixiSprite && Number.isFinite(item.pixiSprite.tint) ? Number(item.pixiSprite.tint) : 0xFFFFFF);
                    item._freezeOriginalTint = Math.max(0, Math.min(0xFFFFFF, Math.floor(currentTint)));
                }
                const blendColor = (fromColor, toColor, factor) => {
                    const t = Math.max(0, Math.min(1, Number(factor) || 0));
                    const fromR = (fromColor >> 16) & 0xFF;
                    const fromG = (fromColor >> 8) & 0xFF;
                    const fromB = fromColor & 0xFF;
                    const toR = (toColor >> 16) & 0xFF;
                    const toG = (toColor >> 8) & 0xFF;
                    const toB = toColor & 0xFF;
                    const r = Math.round(fromR + ((toR - fromR) * t));
                    const g = Math.round(fromG + ((toG - fromG) * t));
                    const b = Math.round(fromB + ((toB - fromB) * t));
                    return (r << 16) | (g << 8) | b;
                };
                const applyFreezeBrightness = (obj, brightnessPercent = null) => {
                    if (!obj || typeof obj !== "object") return;
                    if (
                        obj.shader &&
                        obj.shader.uniforms &&
                        Object.prototype.hasOwnProperty.call(obj.shader.uniforms, "uBrightness")
                    ) {
                        obj.shader.uniforms.uBrightness = Number.isFinite(brightnessPercent)
                            ? Math.max(-1, Math.min(1, Number(brightnessPercent) / 100))
                            : 0;
                        return;
                    }
                    const pixiScope = (typeof PIXI !== "undefined" && PIXI)
                        ? PIXI
                        : ((typeof globalThis !== "undefined" && globalThis.PIXI) ? globalThis.PIXI : null);
                    const ColorMatrixFilterCtor = pixiScope && pixiScope.filters && pixiScope.filters.ColorMatrixFilter;
                    const SpriteCtor = pixiScope && pixiScope.Sprite;
                    const currentFilters = Array.isArray(obj.filters) ? obj.filters.filter(Boolean) : [];
                    const existingFilter = obj[freezeBrightnessFilterKey];
                    const retainedFilters = currentFilters.filter(filter => filter !== existingFilter);
                    const isSprite = (typeof SpriteCtor === "function") && (obj instanceof SpriteCtor);
                    if (!isSprite || typeof ColorMatrixFilterCtor !== "function") {
                        obj[freezeBrightnessFilterKey] = null;
                        obj.filters = retainedFilters.length > 0 ? retainedFilters : null;
                        return;
                    }
                    if (!(Number.isFinite(brightnessPercent) && brightnessPercent > 1e-6)) {
                        obj[freezeBrightnessFilterKey] = null;
                        obj.filters = retainedFilters.length > 0 ? retainedFilters : null;
                        return;
                    }
                    const normalized = clamp01(Number(brightnessPercent) / 100);
                    const whiteMix = 0.55 * normalized;
                    const scale = 1 - whiteMix;
                    const filter = (existingFilter instanceof ColorMatrixFilterCtor)
                        ? existingFilter
                        : new ColorMatrixFilterCtor();
                    filter.matrix = [
                        scale, 0, 0, 0, whiteMix,
                        0, scale, 0, 0, whiteMix,
                        0, 0, scale, 0, whiteMix,
                        0, 0, 0, 1, 0
                    ];
                    obj[freezeBrightnessFilterKey] = filter;
                    retainedFilters.push(filter);
                    obj.filters = retainedFilters;
                };
                const freezeFadeScale = (isDead && liveFreezeProgress > 1e-6)
                    ? clamp01(freezeProgress / liveFreezeProgress)
                    : 1;
                const impactFlashBlend = (hasFreezeFlash ? 1 : 0) * freezeFadeScale;
                const coldTintBlend = Math.max(
                    (!isDead && isTemperatureFrozen ? 1 : 0),
                    clamp01(0.2 + (freezeProgress * 0.8))
                );
                const intermediateTint = blendColor(baseTint, targetTint, Math.max(impactFlashBlend, coldTintBlend));
                const appliedTint = blendColor(intermediateTint, fullyFrozenBaseTint, freezeProgress);
                const appliedBrightness = 35 * freezeProgress;
                displayObjects.forEach(obj => {
                    if (Number.isFinite(obj.tint)) obj.tint = appliedTint;
                    applyFreezeBrightness(obj, appliedBrightness);
                });
                return;
            }

            if (
                Number.isFinite(item._freezeOriginalTint) ||
                Number.isFinite(frozenUntilMs) ||
                Number.isFinite(item._freezeDeathFadeStartedAtMs) ||
                item._freezeDeathFadeCompleted === true
            ) {
                displayObjects.forEach(obj => {
                    if (Number.isFinite(obj.tint)) obj.tint = baseTint;
                    if (
                        obj &&
                        typeof obj === "object" &&
                        obj.shader &&
                        obj.shader.uniforms &&
                        Object.prototype.hasOwnProperty.call(obj.shader.uniforms, "uBrightness")
                    ) {
                        obj.shader.uniforms.uBrightness = 0;
                    }
                    if (obj && typeof obj === "object" && Object.prototype.hasOwnProperty.call(obj, freezeBrightnessFilterKey)) {
                        const currentFilters = Array.isArray(obj.filters) ? obj.filters.filter(Boolean) : [];
                        const existingFilter = obj[freezeBrightnessFilterKey];
                        const retainedFilters = currentFilters.filter(filter => filter !== existingFilter);
                        obj[freezeBrightnessFilterKey] = null;
                        obj.filters = retainedFilters.length > 0 ? retainedFilters : null;
                    }
                });
                item._freezeTintUntilMs = 0;
                item._freezeTintColor = null;
                item._freezeOriginalTint = null;
                item._freezeDeathFadeStartedAtMs = null;
                item._freezeDeathFadeInitialDegreesBelow = null;
                item._freezeDeathFadeCompleted = isDead ? true : null;
            }
        }

        isDebugModeEnabled() {
            return !!(
                (typeof debugMode !== "undefined" && debugMode) ||
                global.debugMode
            );
        }

        shouldRevealScriptHiddenInDebug(item) {
            return !!(item && item.visible === false && this.isDebugModeEnabled());
        }

        isScriptVisible(item) {
            if (!(item && item.visible === false)) return true;
            return this.shouldRevealScriptHiddenInDebug(item);
        }

        getScriptDisplayAlpha(item) {
            if (this.shouldRevealScriptHiddenInDebug(item)) {
                return 0.35;
            }
            return 1;
        }

        isForceVisible(item) {
            if (!item) return false;
            if (item.forceVisible === true || item._forceVisible === true) return true;
            if (item.forceVisible === 1 || item._forceVisible === 1) return true;
            if (typeof item.forceVisible === "string" && item.forceVisible.trim().toLowerCase() === "true") return true;
            if (typeof item._forceVisible === "string" && item._forceVisible.trim().toLowerCase() === "true") return true;
            return false;
        }

        updateSinkAnimation(item, nowMs = null) {
            if (!item || typeof item !== "object") return 0;
            const sinkState = (item._scriptSinkState && typeof item._scriptSinkState === "object")
                ? item._scriptSinkState
                : null;
            if (!sinkState) return 0;
            const baseProperty = (typeof sinkState.baseProperty === "string" && sinkState.baseProperty.length > 0)
                ? sinkState.baseProperty
                : (item.type === "wallSection" ? "bottomZ" : "z");
            const startBase = Number.isFinite(sinkState.startBase) ? Number(sinkState.startBase) : 0;
            const targetBase = Number.isFinite(sinkState.targetBase) ? Number(sinkState.targetBase) : startBase;
            const durationMs = Number.isFinite(sinkState.durationMs) ? Math.max(0, Number(sinkState.durationMs)) : 0;
            const candidateNowMs = Number.isFinite(nowMs) ? Number(nowMs) : NaN;
            const currentMs = (Number.isFinite(candidateNowMs) && candidateNowMs > 1e12)
                ? candidateNowMs
                : Date.now();
            const pausedUntilMs = Number(item._scriptPausedUntilMs);
            const frozenUntilMs = Number(item._scriptFrozenUntilMs);
            const blockedUntilMs = Math.max(
                Number.isFinite(pausedUntilMs) ? pausedUntilMs : 0,
                frozenUntilMs > 0 ? frozenUntilMs : 0
            );
            const wasBlocked = blockedUntilMs > currentMs;
            const lastUpdateMs = Number.isFinite(sinkState.lastUpdateMs) ? Number(sinkState.lastUpdateMs) : currentMs;
            if (!wasBlocked) {
                const deltaMs = Math.max(0, currentMs - lastUpdateMs);
                sinkState.elapsedMs = Math.max(0, Number(sinkState.elapsedMs) || 0) + deltaMs;
            }
            sinkState.lastUpdateMs = currentMs;
            const animationProgress = durationMs > 0
                ? Math.max(0, Math.min(1, (Number(sinkState.elapsedMs) || 0) / durationMs))
                : 1;
            const startProgress = Number.isFinite(sinkState.startProgress)
                ? Math.max(0, Math.min(1, Number(sinkState.startProgress)))
                : 0;
            const targetProgress = Number.isFinite(sinkState.targetProgress)
                ? Math.max(0, Math.min(1, Number(sinkState.targetProgress)))
                : 1;
            const progress = startProgress + ((targetProgress - startProgress) * animationProgress);
            const nextBase = startBase + (targetBase - startBase) * animationProgress;
            const prevBase = Number.isFinite(item[baseProperty]) ? Number(item[baseProperty]) : startBase;
            const heightProperty = (typeof sinkState.heightProperty === "string" && sinkState.heightProperty.length > 0)
                ? sinkState.heightProperty
                : "";
            const startHeight = Number.isFinite(sinkState.startHeight) ? Math.max(0, Number(sinkState.startHeight)) : NaN;
            const targetHeight = Number.isFinite(sinkState.targetHeight) ? Math.max(0, Number(sinkState.targetHeight)) : NaN;
            const prevHeight = (heightProperty && Number.isFinite(item[heightProperty]))
                ? Math.max(0, Number(item[heightProperty]))
                : NaN;
            item[baseProperty] = nextBase;
            if (baseProperty === "z") {
                if (Number.isFinite(item.prevZ) || Object.prototype.hasOwnProperty.call(item, "prevZ")) {
                    item.prevZ = nextBase;
                }
                if (Number.isFinite(item.heightFromGround) || item.type === "roof") {
                    item.heightFromGround = nextBase;
                }
            }
            const nextHeight = (heightProperty && Number.isFinite(startHeight) && Number.isFinite(targetHeight))
                ? Math.max(0, startHeight + ((targetHeight - startHeight) * animationProgress))
                : NaN;
            if (heightProperty && Number.isFinite(startHeight)) {
                item[heightProperty] = nextHeight;
            }
            if (
                (baseProperty === "bottomZ" && Math.abs(nextBase - prevBase) > 1e-6) ||
                (heightProperty && Number.isFinite(prevHeight) && Number.isFinite(nextHeight) && Math.abs(nextHeight - prevHeight) > 1e-6)
            ) {
                if (Object.prototype.hasOwnProperty.call(item, "mesh3d")) {
                    item.mesh3d = null;
                }
                if (Object.prototype.hasOwnProperty.call(item, "_depthGeometryCache")) {
                    item._depthGeometryCache = null;
                }
            }
            sinkState.progress = progress;
            sinkState.currentBase = nextBase;
            const prevNonBlocking = !!sinkState.nonBlocking;
            const prevLosTransparent = !!sinkState.losTransparent;
            if (typeof globalThis !== "undefined" && typeof globalThis.syncTargetSinkInteractionState === "function") {
                globalThis.syncTargetSinkInteractionState(item);
            }
            if (
                (prevNonBlocking !== !!sinkState.nonBlocking || prevLosTransparent !== !!sinkState.losTransparent) &&
                typeof globalThis !== "undefined" &&
                typeof globalThis.refreshTargetSinkBlocking === "function"
            ) {
                globalThis.refreshTargetSinkBlocking(item);
            }
            sinkState.active = animationProgress < 1;
            if (animationProgress >= 1 && sinkState.nonBlocking === false) {
                if (typeof globalThis !== "undefined" && typeof globalThis.restoreTargetSinkBlockingState === "function") {
                    globalThis.restoreTargetSinkBlockingState(item, sinkState);
                }
                item._scriptSinkState = null;
            }
            return progress;
        }

        clearSinkClip(item, displayObj = null) {
            const obj = displayObj || (item && item._renderingDisplayObject) || null;
            if (obj && obj.mask && item && item._scriptSinkMaskGraphics && obj.mask === item._scriptSinkMaskGraphics) {
                obj.mask = null;
            }
            if (item && item._scriptSinkMaskGraphics) {
                item._scriptSinkMaskGraphics.clear();
                item._scriptSinkMaskGraphics.visible = false;
                if (Object.prototype.hasOwnProperty.call(item._scriptSinkMaskGraphics, "renderable")) {
                    item._scriptSinkMaskGraphics.renderable = false;
                }
            }
        }

        applySinkClip(item, displayObj = null) {
            if (!item || !displayObj) return true;
            const sinkState = (item._scriptSinkState && typeof item._scriptSinkState === "object")
                ? item._scriptSinkState
                : null;
            const progress = sinkState && Number.isFinite(sinkState.progress)
                ? Math.max(0, Math.min(1, Number(sinkState.progress)))
                : 0;
            if (progress <= 1e-4) {
                this.clearSinkClip(item, displayObj);
                return true;
            }
            const visibleRatio = Math.max(0, 1 - progress);
            if (visibleRatio <= 1e-4) {
                this.clearSinkClip(item, displayObj);
                displayObj.visible = false;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = false;
                }
                return false;
            }
            displayObj.visible = true;
            if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                displayObj.renderable = true;
            }
            if (!displayObj.parent || typeof displayObj.getBounds !== "function" || typeof PIXI === "undefined") {
                return true;
            }
            if (displayObj instanceof PIXI.Mesh) {
                this.clearSinkClip(item, displayObj);
                return true;
            }
            if (!item._scriptSinkMaskGraphics) {
                item._scriptSinkMaskGraphics = new PIXI.Graphics();
                item._scriptSinkMaskGraphics.name = "renderingSinkMask";
                item._scriptSinkMaskGraphics.interactive = false;
            }
            const maskGraphics = item._scriptSinkMaskGraphics;
            if (maskGraphics.parent !== displayObj.parent) {
                displayObj.parent.addChild(maskGraphics);
            }
            const bounds = displayObj.getBounds();
            if (!bounds || !Number.isFinite(bounds.x) || !Number.isFinite(bounds.y) ||
                !Number.isFinite(bounds.width) || !Number.isFinite(bounds.height) ||
                bounds.width <= 0 || bounds.height <= 0) {
                this.clearSinkClip(item, displayObj);
                return true;
            }
            const groundPoint = (
                this.camera &&
                Number.isFinite(item.x) &&
                Number.isFinite(item.y) &&
                typeof this.camera.worldToScreen === "function"
            )
                ? this.camera.worldToScreen(Number(item.x), Number(item.y), 0)
                : null;
            const clipBottom = (groundPoint && Number.isFinite(groundPoint.y))
                ? Math.min(bounds.y + bounds.height, Number(groundPoint.y))
                : (bounds.y + Math.max(0.5, bounds.height * visibleRatio));
            const visibleHeight = clipBottom - bounds.y;
            if (!(visibleHeight > 0.5)) {
                this.clearSinkClip(item, displayObj);
                displayObj.visible = false;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = false;
                }
                return false;
            }
            maskGraphics.clear();
            maskGraphics.beginFill(0xffffff, 1);
            maskGraphics.drawRect(bounds.x, bounds.y, bounds.width, visibleHeight);
            maskGraphics.endFill();
            maskGraphics.visible = true;
            if (Object.prototype.hasOwnProperty.call(maskGraphics, "renderable")) {
                maskGraphics.renderable = true;
            }
            displayObj.mask = maskGraphics;
            return true;
        }

        shouldShowTriggerAreaPickerPolygon() {
            const activeSpell = this.getActiveToolSpellName();
            return activeSpell === "editscript";
        }

        isWallBottomFaceOutlineDebugEnabled() {
            const wallCtor = global.WallSectionUnit || null;
            return !!(wallCtor && wallCtor._showBottomFaceOnlyDebug);
        }

        renderDoorBottomFaceDebugOutline(item, container) {
            if (!item || !item.pixiSprite || !container || typeof PIXI === "undefined") return false;
            if (!item._doorBottomFaceDebugGraphics) {
                item._doorBottomFaceDebugGraphics = new PIXI.Graphics();
                item._doorBottomFaceDebugGraphics.name = "renderingDoorBottomFaceDebugOutline";
                item._doorBottomFaceDebugGraphics.visible = false;
                item._doorBottomFaceDebugGraphics.interactive = false;
            }
            this.applySpriteTransform(item);
            const sprite = item.pixiSprite;
            const g = item._doorBottomFaceDebugGraphics;
            if (g.parent !== container) {
                container.addChild(g);
            }
            const width = Number(sprite.width) || 0;
            const height = Number(sprite.height) || 0;
            const x = Number(sprite.x);
            const y = Number(sprite.y);
            if (!(width > 0) || !(height > 0) || !Number.isFinite(x) || !Number.isFinite(y)) {
                g.visible = false;
                return false;
            }
            const anchorX = (sprite.anchor && Number.isFinite(sprite.anchor.x)) ? Number(sprite.anchor.x) : 0.5;
            const anchorY = (sprite.anchor && Number.isFinite(sprite.anchor.y)) ? Number(sprite.anchor.y) : 1;
            g.clear();
            g.position.set(x, y);
            g.rotation = Number(sprite.rotation) || 0;
            g.alpha = this.getScriptDisplayAlpha(item);
            g.lineStyle(2, 0x33cc66, 1);
            g.drawRect(-anchorX * width, -anchorY * height, width, height);
            g.visible = true;
            if (Object.prototype.hasOwnProperty.call(g, "renderable")) {
                g.renderable = true;
            }
            return true;
        }

        getActiveToolSpellName(wizardOverride = null) {
            const wizard = wizardOverride || global.wizard || null;
            if (!wizard) return "";
            if (typeof wizard.currentSpell === "string" && wizard.currentSpell.length > 0) {
                return wizard.currentSpell;
            }
            if (typeof wizard.selectedSpellName === "string" && wizard.selectedSpellName.length > 0) {
                return wizard.selectedSpellName;
            }
            return "";
        }

        shouldShowTriggerAreaToolOutlines(wizardOverride = null) {
            if (this.isDebugModeEnabled()) return true;
            const activeSpell = this.getActiveToolSpellName(wizardOverride);
            if (global.renderingShowPickerScreen) {
                return activeSpell === "editscript";
            }
            return activeSpell === "editscript" || activeSpell === "triggerarea";
        }

        shouldShowTriggerAreaVertexMarkersForTool(wizardOverride = null) {
            const activeSpell = this.getActiveToolSpellName(wizardOverride);
            return activeSpell === "triggerarea";
        }

        isTriggerAreaHighlighted(item) {
            if (!item) return false;
            const pickerApi = (typeof global.renderingScenePicker !== "undefined")
                ? global.renderingScenePicker
                : null;
            if (!pickerApi || typeof pickerApi.getHoveredObject !== "function") return false;
            try {
                return pickerApi.getHoveredObject() === item;
            } catch (_err) {
                return false;
            }
        }

        getTriggerAreaOutlineClipRect() {
            const appRef = (typeof app !== "undefined" && app)
                ? app
                : (global.app || null);
            const screenWidth = Math.max(
                1,
                Number(appRef && appRef.renderer && appRef.renderer.width) ||
                Number(appRef && appRef.screen && appRef.screen.width) ||
                Number(window && window.innerWidth) ||
                1
            );
            const screenHeight = Math.max(
                1,
                Number(appRef && appRef.renderer && appRef.renderer.height) ||
                Number(appRef && appRef.screen && appRef.screen.height) ||
                Number(window && window.innerHeight) ||
                1
            );
            const insetX = Math.max(0, (Number(this.camera && this.camera.viewscale) || 1) * 0.5);
            const insetY = Math.max(0, (Number(this.camera && this.camera.viewscale) || 1) * (Number(this.camera && this.camera.xyratio) || 1) * 0.5);
            const rect = {
                left: insetX,
                top: insetY,
                right: screenWidth - insetX,
                bottom: screenHeight - insetY
            };
            if (!(rect.right > rect.left) || !(rect.bottom > rect.top)) return null;
            return rect;
        }

        clipPolygonAgainstBoundary(points, isInside, intersect) {
            const input = Array.isArray(points) ? points : [];
            if (input.length === 0) return [];
            const output = [];
            let previous = input[input.length - 1];
            let previousInside = !!isInside(previous);
            for (let i = 0; i < input.length; i++) {
                const current = input[i];
                const currentInside = !!isInside(current);
                if (currentInside) {
                    if (!previousInside) {
                        const entry = intersect(previous, current);
                        if (entry) output.push(entry);
                    }
                    output.push(current);
                } else if (previousInside) {
                    const exit = intersect(previous, current);
                    if (exit) output.push(exit);
                }
                previous = current;
                previousInside = currentInside;
            }
            return output;
        }

        clipTriggerAreaScreenPolygon(points, rect) {
            let clipped = Array.isArray(points)
                ? points
                    .filter((pt) => pt && Number.isFinite(pt.x) && Number.isFinite(pt.y))
                    .map((pt) => ({ x: Number(pt.x), y: Number(pt.y) }))
                : [];
            if (!rect || clipped.length < 3) return clipped;
            const intersectVertical = (boundaryX) => (a, b) => {
                const dx = Number(b.x) - Number(a.x);
                if (Math.abs(dx) <= 1e-7) {
                    return { x: boundaryX, y: Number(a.y) };
                }
                const t = (boundaryX - Number(a.x)) / dx;
                return {
                    x: boundaryX,
                    y: Number(a.y) + (Number(b.y) - Number(a.y)) * t
                };
            };
            const intersectHorizontal = (boundaryY) => (a, b) => {
                const dy = Number(b.y) - Number(a.y);
                if (Math.abs(dy) <= 1e-7) {
                    return { x: Number(a.x), y: boundaryY };
                }
                const t = (boundaryY - Number(a.y)) / dy;
                return {
                    x: Number(a.x) + (Number(b.x) - Number(a.x)) * t,
                    y: boundaryY
                };
            };
            clipped = this.clipPolygonAgainstBoundary(clipped, (pt) => Number(pt.x) >= rect.left, intersectVertical(rect.left));
            clipped = this.clipPolygonAgainstBoundary(clipped, (pt) => Number(pt.x) <= rect.right, intersectVertical(rect.right));
            clipped = this.clipPolygonAgainstBoundary(clipped, (pt) => Number(pt.y) >= rect.top, intersectHorizontal(rect.top));
            clipped = this.clipPolygonAgainstBoundary(clipped, (pt) => Number(pt.y) <= rect.bottom, intersectHorizontal(rect.bottom));
            return clipped;
        }

        renderTriggerAreaOmnivisionOutline(item, container, omnivisionActive, wizardOverride = null) {
            if (!item) return;
            if (!item._triggerOutlineGraphics) {
                item._triggerOutlineGraphics = new PIXI.Graphics();
                item._triggerOutlineGraphics.name = "renderingTriggerAreaOutline";
                item._triggerOutlineGraphics.visible = false;
                item._triggerOutlineGraphics.interactive = false;
            }
            const g = item._triggerOutlineGraphics;
            if (g.parent !== container) {
                container.addChild(g);
            }
            g.clear();
            const points = (item.shadowBox && Array.isArray(item.shadowBox.points))
                ? item.shadowBox.points
                : null;
            if (!this.shouldShowTriggerAreaToolOutlines(wizardOverride) || !points || points.length < 3) {
                g.visible = false;
                return;
            }
            const screenPoints = [];
            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                if (!pt || !Number.isFinite(pt.x) || !Number.isFinite(pt.y)) continue;
                const sp = this.camera.worldToScreen(Number(pt.x), Number(pt.y), 0);
                if (!sp || !Number.isFinite(sp.x) || !Number.isFinite(sp.y)) continue;
                screenPoints.push(sp);
            }
            if (screenPoints.length < 3) {
                g.visible = false;
                return;
            }

            const clippedPoints = this.clipTriggerAreaScreenPolygon(
                screenPoints,
                this.getTriggerAreaOutlineClipRect()
            );
            if (!Array.isArray(clippedPoints) || clippedPoints.length < 2) {
                g.visible = false;
                return;
            }

            const dashLengthPx = 10;
            const gapLengthPx = 6;
            const outlineColor = this.isTriggerAreaHighlighted(item) ? 0x66c2ff : 0xffffff;
            g.lineStyle(3, outlineColor, 1);
            let drewAny = false;
            for (let i = 0; i < clippedPoints.length; i++) {
                const a = clippedPoints[i];
                const b = clippedPoints[(i + 1) % clippedPoints.length];
                const dx = Number(b.x) - Number(a.x);
                const dy = Number(b.y) - Number(a.y);
                const len = Math.hypot(dx, dy);
                if (!(len > 0)) continue;
                const ux = dx / len;
                const uy = dy / len;
                let dist = 0;
                while (dist < len) {
                    const dashStart = dist;
                    const dashEnd = Math.min(len, dist + dashLengthPx);
                    g.moveTo(
                        Number(a.x) + ux * dashStart,
                        Number(a.y) + uy * dashStart
                    );
                    g.lineTo(
                        Number(a.x) + ux * dashEnd,
                        Number(a.y) + uy * dashEnd
                    );
                    drewAny = true;
                    dist += dashLengthPx + gapLengthPx;
                }
            }
            g.visible = drewAny;
        }

        renderTriggerAreaVertexMarkers(item, container, wizardOverride = null, ctx = null) {
            if (!item) return;
            if (!item._triggerVertexGraphics) {
                item._triggerVertexGraphics = new PIXI.Graphics();
                item._triggerVertexGraphics.name = "renderingTriggerAreaVertices";
                item._triggerVertexGraphics.visible = false;
                item._triggerVertexGraphics.interactive = false;
            }
            const g = item._triggerVertexGraphics;
            if (g.parent !== container) {
                container.addChild(g);
            }
            g.clear();
            const points = (item.shadowBox && Array.isArray(item.shadowBox.points))
                ? item.shadowBox.points
                : null;
            if (!this.shouldShowTriggerAreaVertexMarkersForTool(wizardOverride) || !points || points.length < 3) {
                g.visible = false;
                return;
            }

            const wizard = global.wizard || null;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const selection = (
                wizard &&
                spellSystemRef &&
                typeof spellSystemRef.getTriggerAreaVertexSelection === "function"
            )
                ? spellSystemRef.getTriggerAreaVertexSelection(wizard)
                : null;

            g.lineStyle(2, 0xffffff, 1);
            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                if (!pt || !Number.isFinite(pt.x) || !Number.isFinite(pt.y)) continue;
                const sp = this.camera.worldToScreen(Number(pt.x), Number(pt.y), 0);
                if (!sp || !Number.isFinite(sp.x) || !Number.isFinite(sp.y)) continue;
                const isSelected = !!(selection && selection.area === item && selection.vertexIndex === i);
                g.drawCircle(sp.x, sp.y, isSelected ? 10 : 6);
            }
            g.visible = true;
            this.promoteInteriorPresentationDisplayObject(g, ctx);
        }

        renderDepthBillboardObjects(ctx, renderItems, foregroundPlan = null, cutawayState = null) {
            const container = this.layers.objects3d || this.layers.depthObjects;
            const characterContainer = this.getCharacterLayer() || container;
            const groundContainer = this.layers.groundObjects;
            if (!container) { if (!this._depthRenderedItems) this._depthRenderedItems = new Set(); this._depthRenderedItems.clear(); return this._depthRenderedItems; }
            if (!this._depthRenderedItems) this._depthRenderedItems = new Set();
            const depthRenderedItems = this._depthRenderedItems;
            depthRenderedItems.clear();
            if (!this._depthBillboardMeshSets) this._depthBillboardMeshSets = [new Set(), new Set()];
            this._depthBillboardMeshIdx = ((this._depthBillboardMeshIdx || 0) + 1) % 2;
            const currentMeshes = this._depthBillboardMeshSets[this._depthBillboardMeshIdx];
            currentMeshes.clear();
            if (!this._depthBillboardItemSets) this._depthBillboardItemSets = [new Set(), new Set()];
            this._depthBillboardItemIdx = ((this._depthBillboardItemIdx || 0) + 1) % 2;
            const currentItems = this._depthBillboardItemSets[this._depthBillboardItemIdx];
            currentItems.clear();
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            const mazeModeForDepth = this.isLosMazeModeEnabled() && !this.isOmnivisionActive(wizardRef);
            let depthCandidates = 0;
            let depthMissingMountedSection = 0;
            let depthHiddenByScript = 0;
            let depthDoorBottomOutlineOnly = 0;

            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!this.shouldUseDepthBillboard(item)) continue;
                depthCandidates += 1;
                if (this.isWallMountedSpatialItem(item)) {
                    const _mountedSection = this.resolveMountedWallSectionForItem(item);
                    // Section streaming can temporarily leave a mounted door/window alive
                    // while its backing wall section is unloaded. In that state, falling
                    // back to the single-plane depth billboard uses raw sprite pixel
                    // dimensions and renders at the wrong size, so keep it hidden until
                    // the mounted wall is present again.
                    if (!_mountedSection) {
                        this.hideRenderItemWithMissingMountedSection(item);
                        depthMissingMountedSection += 1;
                        depthRenderedItems.add(item);
                        continue;
                    }
                }
                if (!this.isScriptVisible(item)) {
                    depthHiddenByScript += 1;
                    if (item.pixiSprite) {
                        item.pixiSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = false;
                        }
                    }
                    if (item.fireSprite) {
                        item.fireSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.fireSprite, "renderable")) {
                            item.fireSprite.renderable = false;
                        }
                    }
                    if (item._compositeUnderlayMesh) {
                        item._compositeUnderlayMesh.visible = false;
                    }
                    continue;
                }
                if (typeof item.updateSpriteAnimation === "function") {
                    item.updateSpriteAnimation();
                }
                const sprite = item.pixiSprite;
                const disableMazeDepthVariant = this.isWallMountedSpatialItem(item);
                const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
                const isMountedDoor = !!(
                    disableMazeDepthVariant &&
                    (category === "doors" || item.type === "door")
                );
                const isMountedOpening = !!(
                    disableMazeDepthVariant &&
                    (
                        category === "doors" ||
                        category === "windows" ||
                        item.type === "door" ||
                        item.type === "window"
                    )
                );
                const showDoorBottomDebugOutline = !!(
                    isMountedDoor &&
                    this.isWallBottomFaceOutlineDebugEnabled()
                );
                let forceMountedWallSide = null;
                let drawOnlyMountedWallSide = false;
                if (isMountedDoor) {
                    const interiorDoorSide = (
                        foregroundPlan &&
                        foregroundPlan.doorInteriorSide instanceof Map
                    ) ? foregroundPlan.doorInteriorSide.get(item) : null;
                    if (interiorDoorSide === "front" || interiorDoorSide === "back") {
                        forceMountedWallSide = interiorDoorSide;
                        drawOnlyMountedWallSide = true;
                    }
                }
                if (!drawOnlyMountedWallSide && isMountedOpening) {
                    const exteriorOpeningSide = this.getBuildingCutawayMountedExteriorSide(
                        item,
                        cutawayState,
                        ctx,
                        (ctx && ctx.map) || global.map || null
                    );
                    if (exteriorOpeningSide === "front" || exteriorOpeningSide === "back") {
                        forceMountedWallSide = exteriorOpeningSide;
                        drawOnlyMountedWallSide = true;
                    }
                }
                if (isMountedDoor) {
                    const mountedSection = this.resolveMountedWallSectionForItem(item);
                    const isBottomFaceOnly = !!(
                        mazeModeForDepth &&
                        mountedSection &&
                        typeof mountedSection.isBottomOnlyVisibleInMazeMode === "function" &&
                        mountedSection.isBottomOnlyVisibleInMazeMode({ player: wizardRef, camera: this.camera })
                    );
                    if (!drawOnlyMountedWallSide && isBottomFaceOnly) {
                        forceMountedWallSide = "center";
                    }
                }
                if (showDoorBottomDebugOutline) {
                    depthDoorBottomOutlineOnly += 1;
                    const itemContainer = this.isCharacterRenderItem(item) ? characterContainer : container;
                    const targetContainer = (item.rotationAxis === "ground" && groundContainer)
                        ? groundContainer
                        : itemContainer;
                    const outlineVisible = this.renderDoorBottomFaceDebugOutline(item, targetContainer);
                    if (item._renderingDepthMesh) {
                        if (item._renderingDepthMesh.parent) {
                            item._renderingDepthMesh.parent.removeChild(item._renderingDepthMesh);
                        }
                        item._renderingDepthMesh.visible = false;
                        item._renderingDepthMesh = null;
                    }
                    if (item._compositeUnderlayMesh) {
                        item._compositeUnderlayMesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item._compositeUnderlayMesh, "renderable")) {
                            item._compositeUnderlayMesh.renderable = false;
                        }
                    }
                    if (sprite) {
                        sprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                            sprite.renderable = false;
                        }
                    }
                    if (item.fireSprite) {
                        item.fireSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.fireSprite, "renderable")) {
                            item.fireSprite.renderable = false;
                        }
                    }
                    currentItems.add(item);
                    depthRenderedItems.add(item);
                    if (outlineVisible && item._doorBottomFaceDebugGraphics) {
                        currentMeshes.add(item._doorBottomFaceDebugGraphics);
                        this.addPickRenderItem(item, item._doorBottomFaceDebugGraphics, { forceInclude: true });
                    }
                    continue;
                }
                const mesh = item.updateDepthBillboardMesh(ctx, this.camera, {
                    alphaCutoff: TREE_ALPHA_CUTOFF,
                    mazeMode: disableMazeDepthVariant ? false : mazeModeForDepth,
                    player: wizardRef,
                    forceMountedWallSide,
                    drawOnlyMountedWallSide
                });
                if (!mesh) {
                    this.logPlaceObjectRenderDebug("depth-billboard-no-mesh", item, {
                        rotationAxis: typeof item.rotationAxis === "string" ? item.rotationAxis : "",
                        hasSprite: !!item.pixiSprite,
                        hasTexturePath: typeof item.texturePath === "string" && item.texturePath.length > 0
                    });
                    continue;
                }
                item._renderingDepthMesh = mesh;

                const itemContainer = this.isCharacterRenderItem(item) ? characterContainer : container;

                const groundItemLayer = Number.isFinite(item._renderLayerIndex)
                    ? this.getLayerIndexFromValue(item._renderLayerIndex, 0)
                    : this.getLayerIndexForObject(item, 0);
                const targetContainer = (item.rotationAxis === "ground" && groundContainer && groundItemLayer === 0)
                    ? groundContainer
                    : itemContainer;

                // Add composite underlay mesh BEFORE the main mesh so it renders behind
                const underlayMesh = item._compositeUnderlayMesh;
                if (underlayMesh && !underlayMesh.destroyed && item._compositeUnderlayShouldRender) {
                    if (underlayMesh.parent !== targetContainer) {
                        targetContainer.addChild(underlayMesh);
                    }
                    const layerAlpha = Number.isFinite(item._renderLayerAlpha)
                        ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                        : 1;
                    underlayMesh.visible = true;
                    underlayMesh.alpha = this.getScriptDisplayAlpha(item) * layerAlpha;
                    if (Object.prototype.hasOwnProperty.call(underlayMesh, "renderable")) {
                        underlayMesh.renderable = true;
                    }
                    this.applyLayerDarknessForItem(item, item._renderLayerIndex, underlayMesh);
                    if (this.applySinkClip(item, underlayMesh)) {
                        currentMeshes.add(underlayMesh);
                    }
                } else if (underlayMesh && !underlayMesh.destroyed) {
                    underlayMesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(underlayMesh, "renderable")) {
                        underlayMesh.renderable = false;
                    }
                }

                if (mesh.parent !== targetContainer) {
                    targetContainer.addChild(mesh);
                }
                const layerAlpha = Number.isFinite(item._renderLayerAlpha)
                    ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                    : 1;
                mesh.visible = true;
                mesh.alpha = this.getScriptDisplayAlpha(item) * layerAlpha;
                this.applyMountedWindowDepthState(mesh, item, forceMountedWallSide, mesh.alpha);
                if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                    mesh.renderable = true;
                }
                this.applyScriptBrightness(item, mesh);
                this.applyFrozenTint(item, mesh);
                this.applyLayerDarknessForItem(item, item._renderLayerIndex, mesh);
                currentItems.add(item);
                depthRenderedItems.add(item);
                const meshVisibleAfterSinkClip = this.applySinkClip(item, mesh);
                if (meshVisibleAfterSinkClip) {
                    currentMeshes.add(mesh);
                }
                this.logPlaceObjectRenderDebug("depth-billboard", item, {
                    rotationAxis: typeof item.rotationAxis === "string" ? item.rotationAxis : "",
                    targetContainerName: targetContainer && targetContainer.name ? targetContainer.name : "",
                    meshVisibleAfterSinkClip
                });
                // Use the same depth billboard mesh for picker hits so picker-screen
                // occlusion matches the regular depth-rendered scene (trees included).
                if (meshVisibleAfterSinkClip) {
                    this.addPickRenderItem(item, mesh, { forceInclude: true });
                }
                if (item._doorBottomFaceDebugGraphics) {
                    item._doorBottomFaceDebugGraphics.visible = false;
                    if (Object.prototype.hasOwnProperty.call(item._doorBottomFaceDebugGraphics, "renderable")) {
                        item._doorBottomFaceDebugGraphics.renderable = false;
                    }
                }

                // Hide legacy sprite when depth mesh is active.
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }

                // Position fire sprite overlay if present
                if (item.fireSprite) {
                    const fireSprite = item.fireSprite;
                    const fireContainer = targetContainer || itemContainer;
                    if (fireSprite.parent !== fireContainer) {
                        fireContainer.addChild(fireSprite);
                    }
                    if (fireContainer && mesh && typeof fireContainer.getChildIndex === "function" && typeof fireContainer.setChildIndex === "function") {
                        const meshIndex = fireContainer.getChildIndex(mesh);
                        const maxIndex = Math.max(0, fireContainer.children.length - 1);
                        const desiredIndex = Math.min(meshIndex + 1, maxIndex);
                        if (fireContainer.getChildIndex(fireSprite) !== desiredIndex) {
                            fireContainer.setChildIndex(fireSprite, desiredIndex);
                        }
                    }

                    let fp = null;
                    if (item.type === "tree") {
                        fireSprite.anchor.set(0.5, 1);
                        const worldPositions = item._depthBillboardWorldPositions;
                        if (worldPositions && worldPositions.length >= 12) {
                            // Use a point 1/3 down from the crown (TR/TL midpoint toward BL/BR midpoint).
                            // worldPositions layout: BL[0-2], BR[3-5], TR[6-8], TL[9-11]
                            const crownX = (worldPositions[6] + worldPositions[9]) / 2;
                            const crownY = (worldPositions[7] + worldPositions[10]) / 2;
                            const crownZ = (worldPositions[8] + worldPositions[11]) / 2;
                            const baseX = (worldPositions[0] + worldPositions[3]) / 2;
                            const baseY = (worldPositions[1] + worldPositions[4]) / 2;
                            const baseZ = (worldPositions[2] + worldPositions[5]) / 2;
                            const t = 1 / 3; // fraction down from crown
                            const tx = crownX + (baseX - crownX) * t;
                            const ty = crownY + (baseY - crownY) * t;
                            const tz = crownZ + (baseZ - crownZ) * t;
                            fp = this.camera.worldToScreen(tx, ty, tz);
                        } else {
                            const treeWidth = Number.isFinite(item.width) ? item.width : 4;
                            const treeHeight = Number.isFinite(item.height) ? item.height : 4;
                            const anchorX = (item.pixiSprite && item.pixiSprite.anchor && Number.isFinite(item.pixiSprite.anchor.x))
                                ? Number(item.pixiSprite.anchor.x)
                                : 0.5;
                            const anchorY = (item.pixiSprite && item.pixiSprite.anchor && Number.isFinite(item.pixiSprite.anchor.y))
                                ? Number(item.pixiSprite.anchor.y)
                                : 1;
                            const topWorldX = item.x + (0.5 - anchorX) * treeWidth;
                            const topWorldZ = anchorY * treeHeight * 0.75; // 25% down from top
                            const treeBaseZ = this.getRenderItemOverlayBaseWorldZ(item);
                            fp = this.camera.worldToScreen(topWorldX, item.y, treeBaseZ + topWorldZ);
                        }
                    } else {
                        const itemHeight = Number.isFinite(item.height) ? item.height : 0;
                        const isDeadAnimal = !!(
                            item &&
                            item.dead &&
                            typeof Animal !== "undefined" &&
                            item instanceof Animal
                        );
                        if (fireSprite.anchor) {
                            fireSprite.anchor.set(0.5, isDeadAnimal ? 0.5 : 1);
                        }
                        if (item.isFallenDoorEffect && typeof item.getFallenDoorWorldPointFromLocalAnchor === "function") {
                            const anchorWorld = item.getFallenDoorWorldPointFromLocalAnchor();
                            if (anchorWorld) {
                                fp = this.camera.worldToScreen(anchorWorld.x, anchorWorld.y, anchorWorld.z);
                            }
                        }
                        if (!fp && isDeadAnimal && item._useGradualDeathFall) {
                            // Keep corpse fire centered on the billboard while it falls by
                            // rotating the upright midpoint around the corpse foot pivot.
                            const centerScreen = this.getRenderItemOverlayScreenPoint(item, itemHeight * 0.5);
                            const pivotScreen = this.getRenderItemOverlayScreenPoint(item, 0);
                            const rotRad = (Number.isFinite(item.rotation) ? item.rotation : 0) * (Math.PI / 180);
                            const cosR = Math.cos(rotRad);
                            const sinR = Math.sin(rotRad);
                            if (centerScreen && pivotScreen) {
                                const dx = centerScreen.x - pivotScreen.x;
                                const dy = centerScreen.y - pivotScreen.y;
                                fp = {
                                    x: pivotScreen.x + dx * cosR - dy * sinR,
                                    y: pivotScreen.y + dx * sinR + dy * cosR
                                };
                            }
                        } else if (!fp && isDeadAnimal) {
                            fp = this.getRenderItemOverlayScreenPoint(item, itemHeight * 0.5);
                        } else if (!fp) {
                            // Keep flower flames in the bloom canopy, not at stem/base level.
                            const fireBaseHeightRatio = (item.type === "flower") ? 0.68 : 0.75;
                            fp = this.getRenderItemOverlayScreenPoint(item, itemHeight * fireBaseHeightRatio);
                        }
                    }
                    if (!fp || !Number.isFinite(fp.x) || !Number.isFinite(fp.y)) {
                        fireSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(fireSprite, "renderable")) {
                            fireSprite.renderable = false;
                        }
                        continue;
                    }
                    fireSprite.x = fp.x;
                    fireSprite.y = fp.y;

                    const _fireScale = (item.type === 'tree')
                        ? (Number.isFinite(item._renderedFireIntensityScale)
                            ? item._renderedFireIntensityScale
                            : (item.maxHP > 0 && item.hp > 0 ? Math.min(item.maxHP / item.hp, 4) : 1))
                        : 1;
                    const fireScale = Number.isFinite(item.fireScale) ? item.fireScale : 1;
                    const treeWidth = Number.isFinite(item.width) ? item.width : 4;
                    const treeHeight = Number.isFinite(item.height) ? item.height : 4;
                    const vs = this.camera.viewscale;
                    // Gradual death-fall fire: apply animated scale and alpha.
                    const deathFireMul = (item._useGradualDeathFall && Number.isFinite(item._deathFireScale))
                        ? Math.max(0, item._deathFireScale) : 1;
                    const deathFireAlpha = (item._useGradualDeathFall && Number.isFinite(item._deathFireAlpha))
                        ? Math.max(0, Math.min(1, item._deathFireAlpha)) : 1;
                    fireSprite.width = treeWidth * vs * fireScale * _fireScale * 0.8 * deathFireMul;
                    fireSprite.height = treeHeight * vs * fireScale * _fireScale * 0.6 * deathFireMul;
                    fireSprite.alpha = deathFireAlpha;
                    fireSprite.visible = true;
                    fireSprite.renderable = true;
                    this.applyLayerDarknessForItem(item, item._renderLayerIndex, fireSprite);
                }
            }

            for (const mesh of this._depthBillboardMeshSets[1 - this._depthBillboardMeshIdx]) {
                if (!currentMeshes.has(mesh) && mesh) {
                    mesh.visible = false;
                }
            }
            for (const item of this._depthBillboardItemSets[1 - this._depthBillboardItemIdx]) {
                if (currentItems.has(item)) continue;
                if (item && item.pixiSprite) {
                    // When a flower transitions to fragment-crumble mode, its fragment
                    // container takes over rendering. Keep the underlying pixiSprite
                    // hidden so it does not ghost on top of the fragments.
                    const hasActiveFragments = !!(item._flowerBurnFragmentContainer &&
                        Array.isArray(item._flowerBurnFragments) &&
                        item._flowerBurnFragments.length > 0);
                    if (hasActiveFragments) {
                        item.pixiSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = false;
                        }
                    } else {
                        const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
                        const isSpatialDoorOrWindow = !!(
                            item.rotationAxis === "spatial" &&
                            (category === "doors" || category === "windows" || item.type === "door" || item.type === "window")
                        );
                        const shouldShowSprite = isSpatialDoorOrWindow ? false : this.isScriptVisible(item);
                        item.pixiSprite.visible = shouldShowSprite;
                        item.pixiSprite.alpha = shouldShowSprite ? this.getScriptDisplayAlpha(item) : 1;
                        if (shouldShowSprite) {
                            this.applyLayerDarknessForItem(item, item._renderLayerIndex, item.pixiSprite);
                        }
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = shouldShowSprite;
                        }
                    }
                }
                if (item && item._renderingDepthMesh) {
                    item._renderingDepthMesh = null;
                }
                if (item && item._compositeUnderlayMesh) {
                    item._compositeUnderlayMesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(item._compositeUnderlayMesh, "renderable")) {
                        item._compositeUnderlayMesh.renderable = false;
                    }
                }
                if (item && item._doorBottomFaceDebugGraphics) {
                    item._doorBottomFaceDebugGraphics.visible = false;
                    if (Object.prototype.hasOwnProperty.call(item._doorBottomFaceDebugGraphics, "renderable")) {
                        item._doorBottomFaceDebugGraphics.renderable = false;
                    }
                }
                // Hide fire sprite when item leaves depth billboard rendering
                if (item && item.fireSprite) {
                    item.fireSprite.visible = false;
                }
            }
            this.activeDepthBillboardMeshes = currentMeshes;  // keep for external compatibility
            this.activeDepthBillboardItems = currentItems;   // keep for external compatibility

            this.setFrameMetric("depthCandidates", depthCandidates);
            this.setFrameMetric("depthMissingMountedSection", depthMissingMountedSection);
            this.setFrameMetric("depthHiddenByScript", depthHiddenByScript);
            this.setFrameMetric("depthDoorBottomOutlineOnly", depthDoorBottomOutlineOnly);

            return depthRenderedItems;
        }

        renderGroundObjects(ctx, renderItems, alreadyRenderedItems) {
            const container = this.layers.groundObjects;
            if (!container) { if (!this._groundRenderedItems) this._groundRenderedItems = new Set(); this._groundRenderedItems.clear(); return this._groundRenderedItems; }
            if (!container.sortableChildren) container.sortableChildren = true;
            if (!this._groundRenderedItems) this._groundRenderedItems = new Set();
            const groundRenderedItems = this._groundRenderedItems;
            groundRenderedItems.clear();
            if (!this._groundObjectSpriteSets) this._groundObjectSpriteSets = [new Set(), new Set()];
            this._groundObjectSpriteIdx = ((this._groundObjectSpriteIdx || 0) + 1) % 2;
            const currentSprites = this._groundObjectSpriteSets[this._groundObjectSpriteIdx];
            currentSprites.clear();

            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item || item.gone || item.vanishing) continue;
                if (item.type === "roadPath") {
                    if (item.pixiSprite) {
                        item.pixiSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = false;
                        }
                    }
                    continue;
                }
                if (item.rotationAxis !== "ground") continue;
                if (item.type === "triggerArea" || item.isTriggerArea === true) continue;
                if (alreadyRenderedItems && alreadyRenderedItems.has(item)) {
                    this.logPlaceObjectRenderDebug("ground-sprite-skip-already-depth-rendered", item);
                    continue;
                }
                if (!this.isScriptVisible(item)) continue;
                const sprite = item.pixiSprite;
                if (!sprite) {
                    this.logPlaceObjectRenderDebug("ground-sprite-skip-no-sprite", item);
                    continue;
                }

                // Fallback sprite path for ground items not handled by depth billboard
                if (sprite.parent !== container) {
                    container.addChild(sprite);
                }
                // Ensure last-placed rug always renders on top
                sprite.zIndex = Number.isFinite(item._groundLayerOrder) ? item._groundLayerOrder : 0;
                sprite.visible = true;
                const layerAlpha = Number.isFinite(item._renderLayerAlpha)
                    ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                    : 1;
                sprite.alpha = this.getScriptDisplayAlpha(item) * layerAlpha;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = true;
                }
                this.applyScriptBrightness(item, sprite);
                this.applyFrozenTint(item, sprite);
                this.applyLayerDarknessForItem(item, item._renderLayerIndex, sprite);
                groundRenderedItems.add(item);
                const spriteVisibleAfterSinkClip = this.applySinkClip(item, sprite);
                this.logPlaceObjectRenderDebug("ground-sprite", item, {
                    spriteVisibleAfterSinkClip,
                    containerName: container && container.name ? container.name : ""
                });
                if (spriteVisibleAfterSinkClip) {
                    currentSprites.add(sprite);
                    this.addPickRenderItem(item, sprite);
                }
            }

            for (const sprite of this._groundObjectSpriteSets[1 - this._groundObjectSpriteIdx]) {
                if (!currentSprites.has(sprite) && sprite) {
                    sprite.visible = false;
                }
            }
            this._activeGroundObjectSprites = currentSprites;

            this.setFrameMetric("groundObjectSpritesRendered", currentSprites.size);

            return groundRenderedItems;
        }

        getGroundTileZIndex(node, mapRef) {
            const mapWidth = Number.isFinite(mapRef && mapRef.width) ? Math.max(1, Math.floor(mapRef.width)) : 1;
            const y = Number.isFinite(node && node.yindex) ? Math.floor(node.yindex) : 0;
            const x = Number.isFinite(node && node.xindex) ? Math.floor(node.xindex) : 0;
            return y * mapWidth + x;
        }

        ensureGroundTileContainer() {
            const layer = this.layers && this.layers.ground;
            if (!layer) return null;
            if (!this.groundTileContainer) {
                this.groundTileContainer = new PIXI.Container();
                this.groundTileContainer.name = "renderingGroundTiles";
                layer.addChild(this.groundTileContainer);
            } else if (this.groundTileContainer.parent !== layer) {
                layer.addChild(this.groundTileContainer);
            }
            return this.groundTileContainer;
        }

        beginGroundTileProfiling(nowMs) {
            const profiler = this.groundTileProfiler;
            if (!profiler || profiler.printed || !isGroundTileProfilingEnabled()) return null;
            const currentNow = Number.isFinite(nowMs) ? Number(nowMs) : performance.now();
            if (!Number.isFinite(profiler.startMs)) {
                profiler.startMs = currentNow;
                profiler.deadlineMs = currentNow + 10000;
            }
            return profiler;
        }

        maybePrintGroundTileProfile(nowMs) {
            const profiler = this.groundTileProfiler;
            if (!profiler || profiler.printed || !Number.isFinite(profiler.deadlineMs)) return;
            const currentNow = Number.isFinite(nowMs) ? Number(nowMs) : performance.now();
            if (currentNow < profiler.deadlineMs) return;
            const frameCount = Math.max(1, Number(profiler.frameCount) || 1);
            const totals = profiler.totals || {};
            const counts = profiler.counts || {};
            console.log("[ground tile profile 10s]", {
                durationMs: Number((currentNow - profiler.startMs).toFixed(2)),
                frameCount,
                avg: {
                    totalMs: Number((Number(totals.totalMs || 0) / frameCount).toFixed(3)),
                    activeKeyBuildMs: Number((Number(totals.activeKeyBuildMs || 0) / frameCount).toFixed(3)),
                    visibleSetMs: Number((Number(totals.visibleSetMs || 0) / frameCount).toFixed(3)),
                    createSpriteMs: Number((Number(totals.createSpriteMs || 0) / frameCount).toFixed(3)),
                    parentAttachMs: Number((Number(totals.parentAttachMs || 0) / frameCount).toFixed(3)),
                    textureResolveMs: Number((Number(totals.textureResolveMs || 0) / frameCount).toFixed(3)),
                    positionSizeMs: Number((Number(totals.positionSizeMs || 0) / frameCount).toFixed(3)),
                    cleanupMs: Number((Number(totals.cleanupMs || 0) / frameCount).toFixed(3))
                },
                counts: {
                    visibleNodesPerFrame: Number((Number(counts.visibleNodes || 0) / frameCount).toFixed(2)),
                    createdSpritesPerFrame: Number((Number(counts.createdSprites || 0) / frameCount).toFixed(2)),
                    attachedSpritesPerFrame: Number((Number(counts.attachedSprites || 0) / frameCount).toFixed(2)),
                    cleanedSpritesPerFrame: Number((Number(counts.cleanedSprites || 0) / frameCount).toFixed(2)),
                    evictedSpritesPerFrame: Number((Number(counts.evictedSprites || 0) / frameCount).toFixed(2)),
                    reusedSpritesPerFrame: Number((Number(counts.reusedSprites || 0) / frameCount).toFixed(2)),
                    skippedForLevel0ChunksPerFrame: Number((Number(counts.skippedForLevel0Chunks || 0) / frameCount).toFixed(2))
                }
            });
            profiler.printed = true;
        }

        acquireGroundTileSprite() {
            let sprite = Array.isArray(this.groundSpritePool) && this.groundSpritePool.length > 0
                ? this.groundSpritePool.pop()
                : null;
            if (!sprite) {
                sprite = new PIXI.Sprite(PIXI.Texture.WHITE);
                sprite.name = "renderingGroundTile";
                sprite.anchor.set(0.5, 0.5);
            }
            sprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                sprite.renderable = true;
            }
            sprite.alpha = 1;
            return sprite;
        }

        releaseGroundTileSprite(sprite) {
            if (!sprite) return false;
            if (sprite.parent) {
                sprite.parent.removeChild(sprite);
            }
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                sprite.renderable = false;
            }
            sprite.alpha = 1;
            sprite.texture = PIXI.Texture.WHITE;
            if (Array.isArray(this.groundSpritePool) && this.groundSpritePool.length < GROUND_TILE_POOL_LIMIT) {
                this.groundSpritePool.push(sprite);
                return false;
            }
            if (typeof sprite.destroy === "function") {
                sprite.destroy({ children: false, texture: false, baseTexture: false });
            }
            return true;
        }

        trimGroundTileSpriteCache(maxEvictions = GROUND_TILE_TRIM_CHUNK_SIZE) {
            if (!(this.groundSpriteByNodeKey instanceof Map) || this.groundSpriteByNodeKey.size <= GROUND_TILE_CACHE_LIMIT) {
                return 0;
            }
            const visibleKeys = this.groundVisibleNodeKeys instanceof Set
                ? this.groundVisibleNodeKeys
                : new Set();
            const evictTarget = Math.max(
                0,
                Math.min(
                    Math.floor(Number(maxEvictions) || 0),
                    this.groundSpriteByNodeKey.size - GROUND_TILE_CACHE_LIMIT
                )
            );
            if (evictTarget <= 0) return 0;

            let evictedSprites = 0;
            for (const [key, sprite] of this.groundSpriteByNodeKey.entries()) {
                if (visibleKeys.has(key)) continue;
                this.groundSpriteByNodeKey.delete(key);
                this.groundVisibleNodeKeys.delete(key);
                this.releaseGroundTileSprite(sprite);
                evictedSprites += 1;
                if (evictedSprites >= evictTarget || this.groundSpriteByNodeKey.size <= GROUND_TILE_CACHE_LIMIT) {
                    break;
                }
            }
            return evictedSprites;
        }

        renderGroundTiles(ctx, visibleNodes) {
            const map = ctx.map;
            const layer = this.layers.ground;
            if (!map || !Array.isArray(map.nodes) || !layer) return;
            const frameStartMs = performance.now();
            const profiler = this.beginGroundTileProfiling(
                ctx && Number.isFinite(ctx.renderNowMs) ? Number(ctx.renderNowMs) : frameStartMs
            );

            const cam = this.camera;
            const tileWorldW = (Number.isFinite(map.hexWidth) ? map.hexWidth : (1 / 0.866))
                * GROUND_TILE_OVERLAP_SCALE;
            const tileWorldH = (Number.isFinite(map.hexHeight) ? map.hexHeight : 1)
                * GROUND_TILE_OVERLAP_SCALE;
            const nodes = Array.isArray(visibleNodes) ? visibleNodes : [];
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs))
                ? Number(ctx.renderNowMs)
                : ((typeof performance !== "undefined" && performance && typeof performance.now === "function")
                    ? performance.now()
                    : Date.now());
            if (!this._groundVisibleNodeKeysSets) this._groundVisibleNodeKeysSets = [new Set(), new Set()];
            this._groundVisibleNodeKeysIdx = ((this._groundVisibleNodeKeysIdx || 0) + 1) % 2;
            const visibleNodeKeys = this._groundVisibleNodeKeysSets[this._groundVisibleNodeKeysIdx];
            visibleNodeKeys.clear();
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx, cutawayState);
            if (interiorScope && interiorScope.active) {
                const previouslyVisibleNodeKeys = this._groundVisibleNodeKeysSets[1 - this._groundVisibleNodeKeysIdx];
                let cleanedSprites = 0;
                for (const key of previouslyVisibleNodeKeys) {
                    const sprite = this.groundSpriteByNodeKey.get(key);
                    if (sprite) {
                        sprite.visible = false;
                        cleanedSprites += 1;
                    }
                }
                this.groundVisibleNodeKeys = visibleNodeKeys;
                if (profiler) {
                    profiler.frameCount += 1;
                    profiler.totals.totalMs += (performance.now() - frameStartMs);
                    profiler.totals.cleanupMs += 0;
                    profiler.counts.visibleNodes += 0;
                    profiler.counts.cleanedSprites += cleanedSprites;
                    this.maybePrintGroundTileProfile(
                        ctx && Number.isFinite(ctx.renderNowMs) ? Number(ctx.renderNowMs) : performance.now()
                    );
                }
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.groundTilesSkippedForBuildingInteriorScope = 1;
                    this.currentFrameMetrics.groundTileSpritesVisible = 0;
                }
                return;
            }
            const bakedLevel0SectionKeys = this.getBakedLevel0SectionKeys(ctx);
            const activeKeyBuildStartMs = profiler ? performance.now() : 0;
            const activePrototypeNodeKeys = (typeof map.getLoadedPrototypeNodeKeySet === "function")
                ? map.getLoadedPrototypeNodeKeySet()
                : null;
            const activeKeyBuildMs = profiler ? (performance.now() - activeKeyBuildStartMs) : 0;
            const usePrototypeContainerTransform = !!activePrototypeNodeKeys;
            const container = usePrototypeContainerTransform
                ? this.ensureGroundTileContainer()
                : layer;
            if (!container) return;

            if (usePrototypeContainerTransform) {
                container.position.set(
                    -(Number(cam.x) || 0) * (Number(cam.viewscale) || 1),
                    (-(Number(cam.y) || 0) + (Number(cam.z) || 0)) * (Number(cam.viewscale) || 1) * (Number(cam.xyratio) || 1)
                );
                container.scale.set(
                    Number(cam.viewscale) || 1,
                    (Number(cam.viewscale) || 1) * (Number(cam.xyratio) || 1)
                );
            } else if (this.groundTileContainer) {
                this.groundTileContainer.position.set(0, 0);
                this.groundTileContainer.scale.set(1, 1);
            }

            let createSpriteMs = 0;
            let parentAttachMs = 0;
            let textureResolveMs = 0;
            let positionSizeMs = 0;
            let createdSprites = 0;
            let attachedSprites = 0;
            let reusedSprites = 0;
            let skippedForLevel0Chunks = 0;
            if (!this._level0ChunkReadyCache) this._level0ChunkReadyCache = new Map();
            const level0ChunkReadyCache = FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED ? (this._level0ChunkReadyCache.clear(), this._level0ChunkReadyCache) : null;
            if (!this._level0SectionAssetCache) this._level0SectionAssetCache = new Map();
            const level0SectionAssetCache = FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED ? (this._level0SectionAssetCache.clear(), this._level0SectionAssetCache) : null;
            const _groundCovVer = Number(this.level0GroundCoverageVersion) || 0;
            if (!this._nonzeroFloorFragmentsByLevel) this._nonzeroFloorFragmentsByLevel = new Map();
            const nonzeroFloorFragmentsByLevel = this._nonzeroFloorFragmentsByLevel;
            nonzeroFloorFragmentsByLevel.clear();
            if (map.floorsById instanceof Map) {
                for (const fragment of map.floorsById.values()) {
                    if (!fragment || fragment._floorEditEmpty === true) continue;
                    const fragmentLevel = Number.isFinite(fragment.level)
                        ? this.getLayerIndexFromValue(fragment.level, 0)
                        : 0;
                    if (fragmentLevel === 0) continue;
                    if (!Array.isArray(fragment.outerPolygon) || fragment.outerPolygon.length < 3) continue;
                    if (!nonzeroFloorFragmentsByLevel.has(fragmentLevel)) {
                        nonzeroFloorFragmentsByLevel.set(fragmentLevel, []);
                    }
                    nonzeroFloorFragmentsByLevel.get(fragmentLevel).push(fragment);
                }
            }
            if (!this._nonzeroFloorCoverageByNodeKey) this._nonzeroFloorCoverageByNodeKey = new Map();
            const nonzeroFloorCoverageByNodeKey = this._nonzeroFloorCoverageByNodeKey;
            nonzeroFloorCoverageByNodeKey.clear();
            const visibleSetStartMs = profiler ? performance.now() : 0;
            for (let i = 0; i < nodes.length; i++) {
                const node = nodes[i];
                if (!node) continue;
                const nodeLayer = this.getLayerIndexForNode(node);
                if (nodeLayer !== 0) {
                    const nodeKey = `${node.xindex},${node.yindex},${nodeLayer}`;
                    let coveredByFloorFragment = nonzeroFloorCoverageByNodeKey.get(nodeKey);
                    if (coveredByFloorFragment === undefined) {
                        coveredByFloorFragment = false;
                        const levelFragments = nonzeroFloorFragmentsByLevel.get(nodeLayer) || [];
                        const nodeX = Number(node.x);
                        const nodeY = Number(node.y);
                        if (Number.isFinite(nodeX) && Number.isFinite(nodeY)) {
                            for (let f = 0; f < levelFragments.length; f++) {
                                if (isPointSupportedByFloorFragment(levelFragments[f], nodeX, nodeY)) {
                                    coveredByFloorFragment = true;
                                    break;
                                }
                            }
                        }
                        nonzeroFloorCoverageByNodeKey.set(nodeKey, coveredByFloorFragment);
                    }
                    if (coveredByFloorFragment) continue;
                }
                if (mazeLayerOnly && nodeLayer !== wizardLayer) continue;
                if (this.isWorldPointHiddenByLayerCutaway(Number(node.x), Number(node.y), nodeLayer, cutawayState)) continue;
                const sectionKey = typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "";
                if (sectionKey && bakedLevel0SectionKeys.has(sectionKey)) continue;
                {
                    let coveredByChunk;
                    if (node._groundCovVer === _groundCovVer) {
                        coveredByChunk = node._groundCov;
                    } else {
                        coveredByChunk = this.isGroundNodeCoveredByReadyLevel0Chunk(
                            ctx, node, tileWorldW, tileWorldH,
                            level0ChunkReadyCache, level0SectionAssetCache
                        );
                        node._groundCov = coveredByChunk;
                        node._groundCovVer = _groundCovVer;
                    }
                    if (coveredByChunk) {
                        skippedForLevel0Chunks += 1;
                        continue;
                    }
                }
                const key = `${node.xindex},${node.yindex}`;
                visibleNodeKeys.add(key);
                let sprite = this.groundSpriteByNodeKey.get(key);
                if (!sprite) {
                    const createStartMs = profiler ? performance.now() : 0;
                    sprite = this.acquireGroundTileSprite();
                    this.groundSpriteByNodeKey.set(key, sprite);
                    if (profiler) createSpriteMs += (performance.now() - createStartMs);
                    createdSprites += 1;
                } else {
                    this.groundSpriteByNodeKey.delete(key);
                    this.groundSpriteByNodeKey.set(key, sprite);
                    reusedSprites += 1;
                }
                if (sprite.parent !== container) {
                    const attachStartMs = profiler ? performance.now() : 0;
                    container.addChild(sprite);
                    if (profiler) parentAttachMs += (performance.now() - attachStartMs);
                    attachedSprites += 1;
                }

                const textureStartMs = profiler ? performance.now() : 0;
                if (typeof map.getGroundTextureForNode !== "function") {
                    throw new Error("ground tile rendering requires map.getGroundTextureForNode");
                }
                const texture = map.getGroundTextureForNode(node);
                if (sprite.texture !== texture) {
                    sprite.texture = texture;
                }
                if (profiler) textureResolveMs += (performance.now() - textureStartMs);

                const positionStartMs = profiler ? performance.now() : 0;
                const nodeBaseZ = this.getLayerBaseZForNode(node);
                if (usePrototypeContainerTransform) {
                    sprite.x = Number(node.x) || 0;
                    sprite.y = (Number(node.y) || 0) - nodeBaseZ;
                    sprite.width = tileWorldW;
                    sprite.height = tileWorldH;
                } else {
                    const center = cam.worldToScreen(node.x, node.y, nodeBaseZ);
                    sprite.x = center.x;
                    sprite.y = center.y;
                    sprite.width = tileWorldW * cam.viewscale;
                    sprite.height = tileWorldH * cam.viewscale * cam.xyratio;
                }
                sprite.alpha = this.getLiveLayerFadeMultiplier(nodeLayer, nowMs);
                this.applyLayerDarknessToDisplayObject(sprite, this.getLayerDarknessMultiplier(nodeLayer));
                sprite.visible = true;
                if (profiler) positionSizeMs += (performance.now() - positionStartMs);
            }
            const visibleSetMs = profiler ? (performance.now() - visibleSetStartMs) : 0;

            const cleanupStartMs = profiler ? performance.now() : 0;
            let cleanedSprites = 0;
            let evictedSprites = 0;
            const previouslyVisibleNodeKeys = this._groundVisibleNodeKeysSets[1 - this._groundVisibleNodeKeysIdx];
            for (const key of previouslyVisibleNodeKeys) {
                if (visibleNodeKeys.has(key)) continue;
                const sprite = this.groundSpriteByNodeKey.get(key);
                if (sprite) {
                    sprite.visible = false;
                    cleanedSprites += 1;
                }
            }
            this.groundVisibleNodeKeys = visibleNodeKeys;
            evictedSprites = this.trimGroundTileSpriteCache(GROUND_TILE_TRIM_CHUNK_SIZE);
            cleanedSprites += evictedSprites;
            const cleanupMs = profiler ? (performance.now() - cleanupStartMs) : 0;

            if (profiler) {
                profiler.frameCount += 1;
                profiler.totals.totalMs += (performance.now() - frameStartMs);
                profiler.totals.activeKeyBuildMs += activeKeyBuildMs;
                profiler.totals.visibleSetMs += visibleSetMs;
                profiler.totals.createSpriteMs += createSpriteMs;
                profiler.totals.parentAttachMs += parentAttachMs;
                profiler.totals.textureResolveMs += textureResolveMs;
                profiler.totals.positionSizeMs += positionSizeMs;
                profiler.totals.cleanupMs += cleanupMs;
                profiler.counts.visibleNodes += nodes.length;
                profiler.counts.createdSprites += createdSprites;
                profiler.counts.attachedSprites += attachedSprites;
                profiler.counts.cleanedSprites += cleanedSprites;
                profiler.counts.evictedSprites += evictedSprites;
                profiler.counts.reusedSprites += reusedSprites;
                profiler.counts.skippedForLevel0Chunks = (Number(profiler.counts.skippedForLevel0Chunks) || 0) + skippedForLevel0Chunks;
                this.maybePrintGroundTileProfile(
                    ctx && Number.isFinite(ctx.renderNowMs) ? Number(ctx.renderNowMs) : performance.now()
                );
            }
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.groundTilesSkippedForLevel0Chunks = skippedForLevel0Chunks;
                this.currentFrameMetrics.groundTileSpritesVisible = visibleNodeKeys.size;
            }
        }

        ensureFloorVisualContainer() {
            const parent = this.layers && this.layers.depthObjects;
            if (!parent || typeof PIXI === "undefined") return null;
            if (!this.floorVisualContainer) {
                this.floorVisualContainer = new PIXI.Container();
                this.floorVisualContainer.name = "renderingFloorVisualPolygons";
                this.floorVisualContainer.interactiveChildren = false;
                this.floorVisualContainer.sortableChildren = true;
            }
            if (this.floorVisualContainer.parent !== parent) {
                if (typeof parent.addChildAt === "function") {
                    parent.addChildAt(this.floorVisualContainer, 0);
                } else {
                    parent.addChild(this.floorVisualContainer);
                }
            }
            return this.floorVisualContainer;
        }

        updateFloorVisualContainerTransform(container) {
            if (!container) return;
            container.position.set(0, 0);
            container.scale.set(1, 1);
        }

        ensureRoadSpriteDepthContainer() {
            const parent = this.layers && this.layers.depthObjects;
            const container = this.layers && this.layers.roadsFloor;
            if (!parent || !container) return container || null;
            if (typeof PIXI === "undefined") return container;
            const floorContainer = this.ensureFloorVisualContainer();
            if (container.parent !== parent) {
                parent.addChild(container);
            }
            if (
                floorContainer &&
                floorContainer.parent === parent &&
                container.parent === parent &&
                typeof parent.getChildIndex === "function" &&
                typeof parent.setChildIndex === "function"
            ) {
                const floorIndex = parent.getChildIndex(floorContainer);
                let desiredIndex = Math.min(floorIndex + 1, parent.children.length - 1);
                if (this.roadPathDepthContainer && this.roadPathDepthContainer.parent === parent) {
                    desiredIndex = Math.min(desiredIndex, parent.getChildIndex(this.roadPathDepthContainer));
                }
                if (parent.getChildIndex(container) !== desiredIndex) {
                    parent.setChildIndex(container, desiredIndex);
                }
            }
            container.position.set(0, 0);
            container.scale.set(1, 1);
            container.visible = true;
            if (Object.prototype.hasOwnProperty.call(container, "renderable")) {
                container.renderable = true;
            }
            return container;
        }

        ensureRoadPathDepthContainer() {
            const parent = this.layers && this.layers.depthObjects;
            if (!parent || typeof PIXI === "undefined") return null;
            const floorContainer = this.ensureFloorVisualContainer();
            if (!this.roadPathDepthContainer) {
                this.roadPathDepthContainer = new PIXI.Container();
                this.roadPathDepthContainer.name = "renderingRoadPathDepthMeshes";
                this.roadPathDepthContainer.interactiveChildren = false;
            }
            if (this.roadPathDepthContainer.parent !== parent) {
                parent.addChild(this.roadPathDepthContainer);
            }
            if (
                floorContainer &&
                floorContainer.parent === parent &&
                this.roadPathDepthContainer.parent === parent &&
                typeof parent.getChildIndex === "function" &&
                typeof parent.setChildIndex === "function"
            ) {
                const floorIndex = parent.getChildIndex(floorContainer);
                const desiredIndex = Math.min(floorIndex + 1, parent.children.length - 1);
                if (parent.getChildIndex(this.roadPathDepthContainer) !== desiredIndex) {
                    parent.setChildIndex(this.roadPathDepthContainer, desiredIndex);
                }
            }
            this.roadPathDepthContainer.position.set(0, 0);
            this.roadPathDepthContainer.scale.set(1, 1);
            this.roadPathDepthContainer.visible = true;
            if (Object.prototype.hasOwnProperty.call(this.roadPathDepthContainer, "renderable")) {
                this.roadPathDepthContainer.renderable = true;
            }
            return this.roadPathDepthContainer;
        }

        getSelectedFloorVisualLevel() {
            return Number.isFinite(global.selectedFloorEditLevel)
                ? Math.round(Number(global.selectedFloorEditLevel))
                : 0;
        }

        hasEditedLevel0FloorAsset(asset) {
            if (FLOOR_LEVEL0_FORCE_BAKED_SURFACE && asset && Array.isArray(asset.tileCoordKeys) && asset.tileCoordKeys.length > 0) {
                return true;
            }
            if (!asset || !Array.isArray(asset.floors)) return false;
            for (let i = 0; i < asset.floors.length; i++) {
                const floor = asset.floors[i];
                if (!floor || Math.round(Number(floor.level) || 0) !== 0) continue;
                if (floor._prototypeSynthesizedGround === true) continue;
                if (floor._floorEditEmpty === true) return true;
                if (Array.isArray(floor.outerPolygon) && floor.outerPolygon.length >= 3) return true;
            }
            return false;
        }

        getLevel0GroundSurfaceChunkWorldSize() {
            const pxPerWorld = Math.max(1, Number(FLOOR_LEVEL0_CHUNK_TEXTURE_PX_PER_WORLD) || 32);
            return Math.max(0.001, FLOOR_LEVEL0_CHUNK_TEXTURE_SIZE / pxPerWorld);
        }

        getLevel0GroundSurfaceChunkCoord(value) {
            const chunkWorldSize = this.getLevel0GroundSurfaceChunkWorldSize();
            return Math.floor((Number(value) || 0) / chunkWorldSize);
        }

        getLevel0GroundSurfaceChunkKey(sectionKey, chunkX, chunkY) {
            return `${sectionKey || ""}:${Math.floor(Number(chunkX) || 0)},${Math.floor(Number(chunkY) || 0)}`;
        }

        getLevel0GroundSurfaceChunkBounds(chunkX, chunkY, map) {
            const chunkWorldSize = this.getLevel0GroundSurfaceChunkWorldSize();
            const minX = Math.floor(Number(chunkX) || 0) * chunkWorldSize;
            const minY = Math.floor(Number(chunkY) || 0) * chunkWorldSize;
            return {
                minX,
                minY,
                maxX: minX + chunkWorldSize,
                maxY: minY + chunkWorldSize,
                width: chunkWorldSize,
                height: chunkWorldSize,
                tileWorldW: ((Number.isFinite(map && map.hexWidth) ? map.hexWidth : (1 / 0.866)) * GROUND_TILE_OVERLAP_SCALE),
                tileWorldH: ((Number.isFinite(map && map.hexHeight) ? map.hexHeight : 1) * GROUND_TILE_OVERLAP_SCALE),
                chunkX: Math.floor(Number(chunkX) || 0),
                chunkY: Math.floor(Number(chunkY) || 0)
            };
        }

        getLevel0GroundSurfaceChunkCoordsForBounds(bounds) {
            if (!bounds) return [];
            const minX = Number(bounds.minX);
            const minY = Number(bounds.minY);
            const maxX = Number(bounds.maxX);
            const maxY = Number(bounds.maxY);
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) return [];
            const startX = this.getLevel0GroundSurfaceChunkCoord(Math.min(minX, maxX));
            const endX = this.getLevel0GroundSurfaceChunkCoord(Math.max(minX, maxX) - 1e-7);
            const startY = this.getLevel0GroundSurfaceChunkCoord(Math.min(minY, maxY));
            const endY = this.getLevel0GroundSurfaceChunkCoord(Math.max(minY, maxY) - 1e-7);
            const out = [];
            for (let chunkX = startX; chunkX <= endX; chunkX++) {
                for (let chunkY = startY; chunkY <= endY; chunkY++) {
                    out.push({ chunkX, chunkY });
                }
            }
            return out;
        }

        getLevel0GroundSurfaceAssetTileCoordSignature(asset) {
            const tileCoordKeys = Array.isArray(asset && asset.tileCoordKeys) ? asset.tileCoordKeys : [];
            const version = Number(asset && asset._level0SurfaceVersion) || 0;
            const cached = asset && asset._level0GroundSurfaceTileCoordSignatureCache;
            if (
                cached &&
                cached.source === tileCoordKeys &&
                cached.version === version &&
                cached.length === tileCoordKeys.length &&
                typeof cached.signature === "string"
            ) {
                return cached.signature;
            }
            const signature = `${tileCoordKeys.length}:${tileCoordKeys.join("|")}`;
            if (asset) {
                asset._level0GroundSurfaceTileCoordSignatureCache = {
                    source: tileCoordKeys,
                    version,
                    length: tileCoordKeys.length,
                    signature
                };
            }
            return signature;
        }

        getLevel0GroundSurfaceChunkSignature(asset, chunkX, chunkY) {
            const normalizedChunkX = Math.floor(Number(chunkX) || 0);
            const normalizedChunkY = Math.floor(Number(chunkY) || 0);
            const tileCoordSignature = this.getLevel0GroundSurfaceAssetTileCoordSignature(asset);
            const terrainPolygonSignature = this.getLevel0TerrainPolygonsSignature(asset);
            return [
                normalizedChunkX,
                normalizedChunkY,
                Number(asset && asset._level0SurfaceVersion) || 0,
                Number(asset && asset._level0RoadSurfaceModelVersion) || 0,
                Number(asset && asset._level0RoadSurfaceVersion) || 0,
                Number(asset && asset._level0GroundSurfaceVersion) || 0,
                Number(asset && asset._level0SurfaceTextureReadyVersion) || 0,
                FLOOR_LEVEL0_BAKE_ROAD_PATHS ? 1 : 0,
                FLOOR_LEVEL0_TERRAIN_BOUNDARY_MODEL_VERSION,
                terrainPolygonSignature,
                tileCoordSignature
            ].join(":");
        }

        getLevel0GroundSurfaceChunkTexture(ctx, sectionKey, asset, chunkX, chunkY) {
            const map = ctx && ctx.map;
            if (!map || !asset || typeof document === "undefined" || typeof PIXI === "undefined") return null;
            if (!(this.level0GroundSurfaceChunkCache instanceof Map)) {
                this.level0GroundSurfaceChunkCache = new Map();
            }
            const cacheKey = this.getLevel0GroundSurfaceChunkKey(sectionKey, chunkX, chunkY);
            const signature = this.getLevel0GroundSurfaceChunkSignature(asset, chunkX, chunkY);
            let cache = this.level0GroundSurfaceChunkCache.get(cacheKey);
            if (cache && cache.signature === signature && cache.ready === true && cache.texture && cache.bounds) {
                cache.lastUsedTick = ++this.level0GroundSurfaceChunkTick;
                this.touchRenderCacheEntry(this.level0GroundSurfaceChunkCache, cacheKey, cache);
                return cache;
            }
            const buildLimit = Math.max(0, Math.floor(Number(FLOOR_LEVEL0_CHUNK_BUILDS_PER_FRAME) || 0));
            const buildsThisFrame = Math.max(0, Number(this.level0GroundSurfaceChunkBuildsThisFrame) || 0);
            if (buildsThisFrame >= buildLimit) {
                if (cache && cache.texture && cache.bounds) {
                    const wasReady = cache.ready === true;
                    cache.ready = false;
                    cache.pending = true;
                    cache.targetSignature = signature;
                    cache.lastUsedTick = ++this.level0GroundSurfaceChunkTick;
                    this.touchRenderCacheEntry(this.level0GroundSurfaceChunkCache, cacheKey, cache);
                    if (wasReady) this.bumpLevel0GroundCoverageVersion();
                    return cache;
                }
                return null;
            }
            this.level0GroundSurfaceChunkBuildsThisFrame = buildsThisFrame + 1;

            const bounds = this.getLevel0GroundSurfaceChunkBounds(chunkX, chunkY, map);
            const canvas = document.createElement("canvas");
            canvas.width = FLOOR_LEVEL0_CHUNK_TEXTURE_SIZE;
            canvas.height = FLOOR_LEVEL0_CHUNK_TEXTURE_SIZE;
            const ctx2d = canvas.getContext("2d");
            if (!ctx2d) return cache && cache.texture ? cache : null;
            ctx2d.clearRect(0, 0, canvas.width, canvas.height);
            const scale = FLOOR_LEVEL0_CHUNK_TEXTURE_SIZE / Math.max(0.001, Number(bounds.width) || 1);
            const candidateNodes = this.getLevel0PatchCandidateNodes(map, sectionKey, bounds, bounds);
            const groundBakeNodes = this.expandLevel0GroundBakeNodes(candidateNodes);
            const terrainBake = this.drawLevel0GroundTerrainToCanvas(ctx2d, map, groundBakeNodes, bounds, scale, sectionKey, asset);
            let pendingTexture = !!(terrainBake && terrainBake.pending);
            let bakedGroundTiles = terrainBake && Number.isFinite(terrainBake.baked) ? terrainBake.baked : 0;
            const roadBake = this.addRoadsToLevel0GroundSurfaceCanvas(ctx2d, groundBakeNodes, bounds, scale, sectionKey, asset);
            pendingTexture = pendingTexture || !!(roadBake && roadBake.pending);
            if (pendingTexture) {
                if (cache && cache.texture) {
                    const wasReady = cache.ready === true;
                    cache.ready = false;
                    cache.targetSignature = signature;
                    cache.pending = true;
                    cache.lastUsedTick = ++this.level0GroundSurfaceChunkTick;
                    this.touchRenderCacheEntry(this.level0GroundSurfaceChunkCache, cacheKey, cache);
                    if (wasReady) this.bumpLevel0GroundCoverageVersion();
                    return cache;
                }
                return null;
            }

            const texture = PIXI.Texture.from(canvas);
            if (!texture) return cache && cache.texture ? cache : null;
            if (cache && cache.texture && cache.texture !== texture && typeof cache.texture.destroy === "function") {
                cache.texture.destroy(true);
            }
            cache = {
                key: cacheKey,
                sectionKey,
                chunkX: Math.floor(Number(chunkX) || 0),
                chunkY: Math.floor(Number(chunkY) || 0),
                signature,
                ready: true,
                pending: false,
                texture,
                canvas,
                ctx2d,
                bounds,
                scale,
                bakedGroundTiles,
                bakedRoads: roadBake && Number.isFinite(roadBake.baked) ? roadBake.baked : 0
            };
            cache.lastUsedTick = ++this.level0GroundSurfaceChunkTick;
            this.level0GroundSurfaceChunkCache.set(cacheKey, cache);
            this.bumpLevel0GroundCoverageVersion();
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorLevel0ChunkBuilds = (this.currentFrameMetrics.floorLevel0ChunkBuilds || 0) + 1;
                this.currentFrameMetrics.floorLevel0ChunkGroundTiles = (this.currentFrameMetrics.floorLevel0ChunkGroundTiles || 0) + bakedGroundTiles;
                this.currentFrameMetrics.floorLevel0ChunkRoads = (this.currentFrameMetrics.floorLevel0ChunkRoads || 0) + cache.bakedRoads;
            }
            return cache;
        }

        touchRenderCacheEntry(cacheMap, key, value) {
            if (!(cacheMap instanceof Map) || !cacheMap.has(key)) return;
            cacheMap.delete(key);
            cacheMap.set(key, value);
        }

        trimRenderCacheIncremental(cacheMap, limit, maxRemove = 48, onRemove = null) {
            if (!(cacheMap instanceof Map)) return 0;
            const safeLimit = Math.max(0, Math.floor(Number(limit) || 0));
            if (cacheMap.size <= safeLimit) return 0;
            const removeBudget = Math.max(1, Math.floor(Number(maxRemove) || 1));
            let removed = 0;
            const removeCount = Math.min(removeBudget, Math.max(0, cacheMap.size - safeLimit));
            for (const [key, cache] of cacheMap) {
                if (typeof onRemove === "function") {
                    onRemove(cache, key);
                }
                cacheMap.delete(key);
                removed += 1;
                if (removed >= removeCount || cacheMap.size <= safeLimit) break;
            }
            return removed;
        }

        trimLevel0GroundSurfaceChunkCache(limit = FLOOR_LEVEL0_CHUNK_CACHE_LIMIT) {
            if (!(this.level0GroundSurfaceChunkCache instanceof Map)) return 0;
            const removed = this.trimRenderCacheIncremental(
                this.level0GroundSurfaceChunkCache,
                limit,
                32,
                (cache) => {
                    if (cache && cache.texture && typeof cache.texture.destroy === "function") {
                        cache.texture.destroy(true);
                    }
                }
            );
            if (removed > 0) this.bumpLevel0GroundCoverageVersion();
            return removed;
        }

        trimLevel0AnimatedWaterChunkEntryCache(limit = FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4) {
            if (!(this.level0AnimatedWaterChunkEntryCache instanceof Map)) return 0;
            return this.trimRenderCacheIncremental(this.level0AnimatedWaterChunkEntryCache, limit, 64);
        }

        bumpLevel0GroundCoverageVersion() {
            this.level0GroundCoverageVersion = (Number(this.level0GroundCoverageVersion) || 0) + 1;
            return this.level0GroundCoverageVersion;
        }

        resetLevel0GroundSurfaceCaches(reason = "") {
            const destroyTexture = (cache) => {
                if (cache && cache.texture && typeof cache.texture.destroy === "function") {
                    cache.texture.destroy(true);
                }
            };
            if (this.level0GroundSurfaceCache instanceof Map) {
                for (const cache of this.level0GroundSurfaceCache.values()) destroyTexture(cache);
                this.level0GroundSurfaceCache.clear();
            } else {
                this.level0GroundSurfaceCache = new Map();
            }
            if (this.level0GroundSurfaceChunkCache instanceof Map) {
                for (const cache of this.level0GroundSurfaceChunkCache.values()) destroyTexture(cache);
                this.level0GroundSurfaceChunkCache.clear();
            } else {
                this.level0GroundSurfaceChunkCache = new Map();
            }
            if (this.level0GroundSurfaceBakeNodeCache instanceof Map) {
                this.level0GroundSurfaceBakeNodeCache.clear();
            } else {
                this.level0GroundSurfaceBakeNodeCache = new Map();
            }
            if (this.level0AnimatedWaterChunkEntryCache instanceof Map) {
                this.level0AnimatedWaterChunkEntryCache.clear();
            } else {
                this.level0AnimatedWaterChunkEntryCache = new Map();
            }
            this.level0AnimatedWaterChunkTick = 0;
            if (this.level0TerrainPolygonEntryCache instanceof Map) {
                this.level0TerrainPolygonEntryCache.clear();
            } else {
                this.level0TerrainPolygonEntryCache = new Map();
            }
            this.level0TerrainPolygonEntryTick = 0;
            if (this._level0ChunkReadyCache instanceof Map) this._level0ChunkReadyCache.clear();
            if (this._level0SectionAssetCache instanceof Map) this._level0SectionAssetCache.clear();
            this.bakedLevel0SectionKeys = new Set();
            this.bakedLevel0SectionSignature = "";
            this.level0GroundSurfaceChunkBuildsThisFrame = 0;
            this.bumpLevel0GroundCoverageVersion();
            return {
                ok: true,
                reason: typeof reason === "string" ? reason : "",
                coverageVersion: this.level0GroundCoverageVersion
            };
        }

        trimFloorVisualChunkClipCache(limit = FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4) {
            if (!(this.floorVisualChunkClipCache instanceof Map)) return 0;
            return this.trimRenderCacheIncremental(this.floorVisualChunkClipCache, limit, 128);
        }

        trimLevel0TerrainPolygonEntryCache(limit = FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4) {
            if (!(this.level0TerrainPolygonEntryCache instanceof Map)) return 0;
            return this.trimRenderCacheIncremental(this.level0TerrainPolygonEntryCache, limit, 64);
        }

        getCachedFloorVisualClippedPolygonsForChunk(fragmentId, renderOuter, holes, chunkBounds, chunkX, chunkY, shapeSignature) {
            const normalizedHoles = Array.isArray(holes) ? holes : [];
            if (!(this.floorVisualChunkClipCache instanceof Map)) {
                this.floorVisualChunkClipCache = new Map();
            }
            const signature = typeof shapeSignature === "string" && shapeSignature.length > 0
                ? shapeSignature
                : buildFloorVisualSignature(renderOuter, normalizedHoles);
            const cacheKey = `${fragmentId || ""}:chunk:${Math.floor(Number(chunkX) || 0)},${Math.floor(Number(chunkY) || 0)}:${signature}`;
            const cached = this.floorVisualChunkClipCache.get(cacheKey);
            if (cached && Array.isArray(cached.polygons)) {
                cached.lastUsedTick = ++this.floorVisualChunkClipTick;
                this.touchRenderCacheEntry(this.floorVisualChunkClipCache, cacheKey, cached);
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorVisualChunkClipCacheHits = (this.currentFrameMetrics.floorVisualChunkClipCacheHits || 0) + 1;
                }
                return cached.polygons;
            }
            const clippedPolygons = collectFloorVisualClippedPolygonsForRect(renderOuter, normalizedHoles, chunkBounds);
            if (clippedPolygons === null) return null;
            this.floorVisualChunkClipCache.set(cacheKey, {
                polygons: Array.isArray(clippedPolygons) ? clippedPolygons : [],
                lastUsedTick: ++this.floorVisualChunkClipTick
            });
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorVisualChunkClipCacheMisses = (this.currentFrameMetrics.floorVisualChunkClipCacheMisses || 0) + 1;
            }
            return clippedPolygons;
        }

        collectLevel0AnimatedWaterFloorVisualEntries(ctx, fragmentId, sectionKey, asset, chunkBounds, coord, baseZ, alpha) {
            const map = ctx && ctx.map;
            if (!map || !asset || !chunkBounds || !coord) return [];
            if (!(this.level0AnimatedWaterChunkEntryCache instanceof Map)) {
                this.level0AnimatedWaterChunkEntryCache = new Map();
            }
            const chunkX = Math.floor(Number(coord.chunkX) || 0);
            const chunkY = Math.floor(Number(coord.chunkY) || 0);
            const chunkKey = `${Math.floor(Number(coord.chunkX) || 0)},${Math.floor(Number(coord.chunkY) || 0)}`;
            const cacheKey = `${sectionKey || ""}:${chunkX},${chunkY}`;
            const signature = `${this.getLevel0GroundSurfaceChunkSignature(asset, chunkX, chunkY)}:animatedWater:v${FLOOR_LEVEL0_TERRAIN_BOUNDARY_MODEL_VERSION}`;
            let cached = this.level0AnimatedWaterChunkEntryCache.get(cacheKey);
            if (!cached || cached.signature !== signature || !Array.isArray(cached.templates)) {
                const assetHasTerrainPolygons = !!(asset && Array.isArray(asset.terrainPolygons));
                const assetGroups = this.getLevel0TerrainPolygonGroupsFromAsset(map, asset);
                const groups = assetHasTerrainPolygons
                    ? assetGroups
                    : this.collectLevel0TerrainGroups(
                        map,
                        this.expandLevel0GroundBakeNodes(this.getLevel0PatchCandidateNodes(map, sectionKey, chunkBounds, chunkBounds))
                    );
                const templates = [];
                const materialPath = this.getLevel0TerrainMaterialPathForType(map, "water");
                const textureRepeat = this.getLevel0TerrainMaterialRepeat(map, "water");
                for (let i = 0; i < groups.length; i++) {
                    const group = groups[i];
                    if (!group || group.type !== "water") continue;
                    const loops = Array.isArray(group.loops) ? group.loops : this.buildLevel0TerrainRegionLoops(map, group);
                    if (loops.length === 0) continue;
                    const polygons = this.getLevel0TerrainRegionPolygonsFromLoops(loops);
                    for (let p = 0; p < polygons.length; p++) {
                        const polygon = polygons[p];
                        const clipped = collectFloorVisualClippedPolygonsForRect(
                            polygon.outer,
                            Array.isArray(polygon.holes) ? polygon.holes : [],
                            chunkBounds
                        );
                        if (!Array.isArray(clipped) || clipped.length === 0) continue;
                        for (let c = 0; c < clipped.length; c++) {
                            const piece = clipped[c];
                            if (!piece || !Array.isArray(piece.outer) || piece.outer.length < 3) continue;
                            templates.push({
                                keySuffix: `water:${chunkKey}:${i}:${p}:${c}`,
                                outer: piece.outer,
                                holes: Array.isArray(piece.holes) ? piece.holes : [],
                                textureRepeat,
                                texturePath: materialPath
                            });
                        }
                    }
                }
                cached = {
                    signature,
                    templates,
                    lastUsedTick: 0
                };
                this.level0AnimatedWaterChunkEntryCache.set(cacheKey, cached);
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorAnimatedWaterChunkCacheMisses = (this.currentFrameMetrics.floorAnimatedWaterChunkCacheMisses || 0) + 1;
                }
            } else if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorAnimatedWaterChunkCacheHits = (this.currentFrameMetrics.floorAnimatedWaterChunkCacheHits || 0) + 1;
            }
            cached.lastUsedTick = ++this.level0AnimatedWaterChunkTick;
            this.touchRenderCacheEntry(this.level0AnimatedWaterChunkEntryCache, cacheKey, cached);
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : Date.now();
            const out = [];
            const templates = cached.templates;
            for (let i = 0; i < templates.length; i++) {
                const template = templates[i];
                out.push({
                    key: `fragment:${fragmentId}:${template.keySuffix}`,
                    level: 0,
                    baseZ,
                    outer: template.outer,
                    holes: template.holes,
                    texture: null,
                    textureBounds: null,
                    textureRepeat: template.textureRepeat,
                    texturePath: template.texturePath,
                    tint: this.getLayerDarkenedTint(0xffffff, 0),
                    alpha,
                    depthBias: FLOOR_VISUAL_DEPTH_BIAS_UNITS + 0.002,
                    isHoleOverlay: false,
                    isAnimatedWater: true,
                    animationNowMs: nowMs
                });
            }
            return out;
        }

        collectLevel0TerrainPolygonFloorVisualEntries(ctx, fragmentId, sectionKey, asset, baseZ, alpha, activeInteriorDepthBump = 0, clipPolygon = null) {
            const map = ctx && ctx.map;
            if (!map || !asset || !Array.isArray(asset.terrainPolygons) || asset.terrainPolygons.length === 0) return [];
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : Date.now();
            const clipOuter = normalizeFloorVisualPointList(clipPolygon && clipPolygon.outer);
            const clipHoles = Array.isArray(clipPolygon && clipPolygon.holes) ? clipPolygon.holes : [];
            // Experiment: render terrain directly from section asset polygons, without
            // the additional floor/section render-time clip.
            const hasClipPolygon = false;
            const sectionBoundaryRings = clipOuter.length >= 3
                ? [clipOuter].concat(clipHoles.map(hole => normalizeFloorVisualPointList(hole)).filter(hole => hole.length >= 3))
                : [];
            const edgeFadeClipRings = hasClipPolygon
                ? sectionBoundaryRings
                : [];
            const edgeFadeSkipRings = sectionBoundaryRings;
            const edgeFadeClipSignature = buildFloorVisualClipBoundarySignature(edgeFadeClipRings);
            const edgeFadeSkipSignature = buildFloorVisualClipBoundarySignature(edgeFadeSkipRings);
            const terrainAlphaBlendWidth = FLOOR_LEVEL0_TERRAIN_ALPHA_BLEND_WIDTH_UNITS;
            const cacheKey = [
                "terrainEntries",
                sectionKey || "",
                fragmentId || "",
                this.getLevel0TerrainPolygonsSignature(asset),
                `clip:${edgeFadeClipSignature}`,
                `skip:${edgeFadeSkipSignature}`,
                `fade:${terrainAlphaBlendWidth}`,
                `bump:${Number.isFinite(activeInteriorDepthBump) ? activeInteriorDepthBump : 0}`,
                `model:${FLOOR_LEVEL0_TERRAIN_BOUNDARY_MODEL_VERSION}`
            ].join("|");
            if (!(this.level0TerrainPolygonEntryCache instanceof Map)) {
                this.level0TerrainPolygonEntryCache = new Map();
            }
            let cached = this.level0TerrainPolygonEntryCache.get(cacheKey);
            if (cached && Array.isArray(cached.templates)) {
                cached.lastUsedTick = ++this.level0TerrainPolygonEntryTick;
                this.touchRenderCacheEntry(this.level0TerrainPolygonEntryCache, cacheKey, cached);
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorTerrainPolygonEntryCacheHits = (this.currentFrameMetrics.floorTerrainPolygonEntryCacheHits || 0) + 1;
                }
                const out = [];
                for (let i = 0; i < cached.templates.length; i++) {
                    const entry = this.cloneCachedLevel0TerrainPolygonEntry(cached.templates[i], baseZ, alpha, nowMs);
                    if (entry) out.push(entry);
                }
                return out;
            }
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorTerrainPolygonEntryCacheMisses = (this.currentFrameMetrics.floorTerrainPolygonEntryCacheMisses || 0) + 1;
            }
            const groups = this.getLevel0TerrainPolygonGroupsFromAsset(map, asset);
            this.sortLevel0TerrainGroupsForDraw(groups);
            const templates = [];
            for (let i = 0; i < groups.length; i++) {
                const group = groups[i];
                if (!group) continue;
                const loops = Array.isArray(group.loops) ? group.loops : [];
                if (loops.length === 0) continue;
                const polygons = this.getLevel0TerrainRegionPolygonsFromLoops(loops);
                const texturePath = typeof group.materialPath === "string" && group.materialPath.length > 0
                    ? group.materialPath
                    : this.getLevel0TerrainMaterialPathForType(map, group.type);
                const textureRepeat = this.getLevel0TerrainMaterialRepeat(map, group.type);
                const terrainDrawOrder = this.getLevel0TerrainDrawOrder(group.type);
                const terrainDepthBias = FLOOR_VISUAL_DEPTH_BIAS_UNITS +
                    0.002 +
                    this.getLevel0TerrainDepthBiasOffset(group.type) +
                    activeInteriorDepthBump;
                for (let p = 0; p < polygons.length; p++) {
                    const polygon = polygons[p];
                    if (!polygon || !Array.isArray(polygon.outer) || polygon.outer.length < 3) continue;
                    const clippedPolygons = hasClipPolygon
                        ? intersectFloorVisualPolygonWithPolygonStrict(
                            polygon.outer,
                            Array.isArray(polygon.holes) ? polygon.holes : [],
                            { outer: clipOuter, holes: clipHoles },
                            `terrain ${group.type || "unknown"} section ${sectionKey || "(none)"}`
                        )
                        : [polygon];
                    for (let c = 0; c < clippedPolygons.length; c++) {
                        const piece = clippedPolygons[c];
                        if (!piece || !Array.isArray(piece.outer) || piece.outer.length < 3) continue;
                        templates.push({
                            key: `fragment:${fragmentId}:terrain:${sectionKey || ""}:${group.type}:${i}:${p}:${c}`,
                            level: 0,
                            baseZ: 0,
                            outer: piece.outer,
                            holes: Array.isArray(piece.holes) ? piece.holes : [],
                            texture: null,
                            textureBounds: null,
                            textureRepeat,
                            texturePath,
                            tint: this.getLayerDarkenedTint(0xffffff, 0),
                            alpha: 1,
                            depthBias: terrainDepthBias,
                            isHoleOverlay: false,
                            isAnimatedWater: group.type === "water",
                            isTerrainPolygon: true,
                            terrainType: group.type,
                            floorVisualSortOrder: 100 + terrainDrawOrder,
                            edgeFadeWidth: terrainAlphaBlendWidth,
                            edgeFadeClipRings,
                            edgeFadeClipSignature,
                            edgeFadeSkipRings,
                            skipClipBoundaryFade: edgeFadeSkipRings.length > 0,
                            edgeFadeDiagnosticContext: {
                                terrainType: group.type,
                                sectionKey: sectionKey || "",
                                groupIndex: i,
                                polygonIndex: p,
                                clippedPieceIndex: c
                            },
                            animationNowMs: 0
                        });
                    }
                }
            }
            this.sortLevel0TerrainFloorVisualEntriesForDraw(templates);
            cached = {
                templates,
                lastUsedTick: ++this.level0TerrainPolygonEntryTick
            };
            this.level0TerrainPolygonEntryCache.set(cacheKey, cached);
            const out = [];
            for (let i = 0; i < templates.length; i++) {
                const entry = this.cloneCachedLevel0TerrainPolygonEntry(templates[i], baseZ, alpha, nowMs);
                if (entry) out.push(entry);
            }
            return out;
        }

        collectLevel0SectionTerrainPolygonFloorVisualEntries(ctx, viewportSectionKeys, nowMs) {
            if (!FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED) return [];
            const map = ctx && ctx.map;
            const state = map && map._prototypeSectionState;
            if (!map || !state || !(state.sectionAssetsByKey instanceof Map)) return [];
            const selectedLevel = this.getSelectedFloorVisualLevel();
            if (isFloorEditIsolationActive() && selectedLevel !== 0) return [];
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            if (mazeLayerOnly && wizardLayer !== 0) return [];
            const sourceSectionKeys = viewportSectionKeys instanceof Set && viewportSectionKeys.size > 0
                ? viewportSectionKeys
                : (
                    typeof map.getPrototypeActiveSectionKeys === "function"
                        ? map.getPrototypeActiveSectionKeys()
                        : (state.activeSectionKeys instanceof Set ? state.activeSectionKeys : null)
                );
            const out = [];
            if (!(sourceSectionKeys instanceof Set) || sourceSectionKeys.size === 0) return out;
            const baseZ = this.getLayerBaseZForLevel(0);
            const alpha = this.getLiveLayerFadeMultiplier(0, nowMs);
            sourceSectionKeys.forEach((sectionKey) => {
                const asset = state.sectionAssetsByKey.get(sectionKey);
                if (!asset || !Array.isArray(asset.terrainPolygons) || asset.terrainPolygons.length === 0) return;
                const clipPolygon = this.getPrototypeSectionTerrainClipPolygon(sectionKey, asset, state, map);
                const terrainEntries = this.collectLevel0TerrainPolygonFloorVisualEntries(
                    ctx,
                    `section:${sectionKey}`,
                    sectionKey,
                    asset,
                    baseZ,
                    alpha,
                    0,
                    clipPolygon
                );
                for (let i = 0; i < terrainEntries.length; i++) {
                    if (terrainEntries[i]) out.push(terrainEntries[i]);
                }
            });
            return out;
        }

        collectLevel0ChunkFloorVisualEntries(ctx, fragmentId, fragment, asset, outer, holes, baseZ, alpha) {
            if (!FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED) return null;
            const map = ctx && ctx.map;
            const sectionKey = typeof fragment.ownerSectionKey === "string" ? fragment.ownerSectionKey : "";
            if (!map || !sectionKey || !asset) return [];
            const metrics = this.currentFrameMetrics || null;
            const timeEnabled = !!(metrics && typeof performance !== "undefined" && performance && typeof performance.now === "function");
            const now = timeEnabled ? performance.now.bind(performance) : null;
            const renderOuter = expandFloorVisualPolygonFromCentroid(outer, FLOOR_LEVEL0_SEAM_BLEED_UNITS);
            const shapeSignature = buildFloorVisualSignature(renderOuter, holes);
            const polygonBounds = getFloorVisualPointBounds(renderOuter);
            if (!polygonBounds) return [];
            const chunkCoords = this.getLevel0GroundSurfaceChunkCoordsForBounds(polygonBounds);
            const out = [];
            const cam = this.camera || null;
            const viewportRef = ctx && ctx.viewport ? ctx.viewport : null;
            const chunkWorldSize = this.getLevel0GroundSurfaceChunkWorldSize();
            const viewBounds = (
                cam &&
                Number.isFinite(cam.x) &&
                Number.isFinite(cam.y) &&
                Number.isFinite(viewportRef && viewportRef.width) &&
                Number.isFinite(viewportRef && viewportRef.height)
            ) ? {
                minX: Number(cam.x) - chunkWorldSize,
                minY: Number(cam.y) - chunkWorldSize,
                maxX: Number(cam.x) + Math.max(0, Number(viewportRef.width)) + chunkWorldSize,
                maxY: Number(cam.y) + Math.max(0, Number(viewportRef.height)) + chunkWorldSize
            } : null;
            for (let i = 0; i < chunkCoords.length; i++) {
                const coord = chunkCoords[i];
                const chunkBounds = this.getLevel0GroundSurfaceChunkBounds(coord.chunkX, coord.chunkY, map);
                if (metrics) metrics.floorLevel0ChunksConsidered = (metrics.floorLevel0ChunksConsidered || 0) + 1;
                if (
                    viewBounds &&
                    (
                        chunkBounds.maxX < viewBounds.minX ||
                        chunkBounds.minX > viewBounds.maxX ||
                        chunkBounds.maxY < viewBounds.minY ||
                        chunkBounds.minY > viewBounds.maxY
                    )
                ) {
                    continue;
                }
                if (metrics) metrics.floorLevel0ChunksInView = (metrics.floorLevel0ChunksInView || 0) + 1;
                const clipStartMs = now ? now() : 0;
                const clippedPolygons = this.getCachedFloorVisualClippedPolygonsForChunk(
                    fragmentId,
                    renderOuter,
                    holes,
                    chunkBounds,
                    coord.chunkX,
                    coord.chunkY,
                    shapeSignature
                );
                if (metrics && now) metrics.floorLevel0ChunkClipMs = (metrics.floorLevel0ChunkClipMs || 0) + (now() - clipStartMs);
                if (clippedPolygons === null) return null;
                if (!Array.isArray(clippedPolygons) || clippedPolygons.length === 0) continue;
                const textureStartMs = now ? now() : 0;
                const chunk = this.getLevel0GroundSurfaceChunkTexture(ctx, sectionKey, asset, coord.chunkX, coord.chunkY);
                if (metrics && now) metrics.floorLevel0ChunkTextureMs = (metrics.floorLevel0ChunkTextureMs || 0) + (now() - textureStartMs);
                if (!chunk || !chunk.texture || !chunk.bounds) continue;
                for (let p = 0; p < clippedPolygons.length; p++) {
                    const clipped = clippedPolygons[p];
                    if (!clipped || !Array.isArray(clipped.outer) || clipped.outer.length < 3) continue;
                    const polygonKeySuffix = (
                        p === 0 &&
                        clippedPolygons.length === 1 &&
                        (!Array.isArray(holes) || holes.length === 0)
                    ) ? "" : `:poly:${p}`;
                    out.push({
                        key: `fragment:${fragmentId}:chunk:${coord.chunkX},${coord.chunkY}${polygonKeySuffix}`,
                        level: 0,
                        baseZ,
                        outer: clipped.outer,
                        holes: Array.isArray(clipped.holes) ? clipped.holes : [],
                        texture: chunk.texture,
                        textureBounds: chunk.bounds,
                        textureRepeat: null,
                        texturePath: `level0chunk:${sectionKey}:${coord.chunkX},${coord.chunkY}`,
                        tint: 0xffffff,
                        alpha,
                        depthBias: FLOOR_VISUAL_DEPTH_BIAS_UNITS - 0.005,
                        isHoleOverlay: false
                    });
                }
                const waterStartMs = now ? now() : 0;
                const waterEntries = this.collectLevel0AnimatedWaterFloorVisualEntries(
                    ctx,
                    fragmentId,
                    sectionKey,
                    asset,
                    chunkBounds,
                    coord,
                    baseZ,
                    alpha
                );
                if (metrics && now) metrics.floorLevel0ChunkWaterMs = (metrics.floorLevel0ChunkWaterMs || 0) + (now() - waterStartMs);
                for (let w = 0; w < waterEntries.length; w++) out.push(waterEntries[w]);
            }
            if (metrics) metrics.floorLevel0ChunkEntries = (metrics.floorLevel0ChunkEntries || 0) + out.length;
            return out;
        }

        isRoadCoveredByReadyLevel0Chunks(ctx, road) {
            if (!FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED || !road) return false;
            const map = ctx && ctx.map;
            const node = typeof road.getNode === "function" ? road.getNode() : road.node;
            const sectionKey = node && typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "";
            const state = map && map._prototypeSectionState;
            const asset = sectionKey && state && state.sectionAssetsByKey instanceof Map
                ? state.sectionAssetsByKey.get(sectionKey)
                : null;
            if (!asset) return false;
            const worldX = Number.isFinite(road.x) ? Number(road.x) : (Number.isFinite(node && node.x) ? Number(node.x) : 0);
            const worldY = Number.isFinite(road.y) ? Number(road.y) : (Number.isFinite(node && node.y) ? Number(node.y) : 0);
            const width = Math.max(0.001, Number(road.width) || 1) * 1.1547;
            const height = Math.max(0.001, Number(road.height) || 1);
            const bounds = {
                minX: worldX - width * 0.5,
                minY: worldY - height * 0.5,
                maxX: worldX + width * 0.5,
                maxY: worldY + height * 0.5
            };
            const coords = this.getLevel0GroundSurfaceChunkCoordsForBounds(bounds);
            if (coords.length === 0) return false;
            for (let i = 0; i < coords.length; i++) {
                const coord = coords[i];
                const key = this.getLevel0GroundSurfaceChunkKey(sectionKey, coord.chunkX, coord.chunkY);
                const cache = this.level0GroundSurfaceChunkCache instanceof Map
                    ? this.level0GroundSurfaceChunkCache.get(key)
                    : null;
                const signature = this.getLevel0GroundSurfaceChunkSignature(asset, coord.chunkX, coord.chunkY);
                if (!cache || cache.ready !== true || cache.signature !== signature || !cache.texture || !cache.bounds) {
                    return false;
                }
            }
            return true;
        }

        getRoadPathVisualBounds(roadPath) {
            const geometry = roadPath && roadPath.generatedGeometry;
            const points = geometry && Array.isArray(geometry.outline)
                ? geometry.outline
                : (Array.isArray(roadPath && roadPath.outlinePolygon) ? roadPath.outlinePolygon : null);
            if (!Array.isArray(points) || points.length < 3) {
                throw new Error("Cannot resolve road path bake bounds without outline geometry.");
            }
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                const x = Number(pt && pt.x);
                const y = Number(pt && pt.y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) {
                    throw new Error("Cannot resolve road path bake bounds with non-finite outline point.");
                }
                minX = Math.min(minX, x);
                minY = Math.min(minY, y);
                maxX = Math.max(maxX, x);
                maxY = Math.max(maxY, y);
            }
            return { minX, minY, maxX, maxY };
        }

        getRoadPathEndpointPair(roadPath) {
            const geometry = roadPath && roadPath.generatedGeometry;
            const points = geometry && Array.isArray(geometry.points)
                ? geometry.points
                : (Array.isArray(roadPath && roadPath.pathPoints) ? roadPath.pathPoints : null);
            if (!Array.isArray(points) || points.length < 2) {
                throw new Error("Cannot resolve road path endpoint continuity without path points.");
            }
            const normalizeEndpoint = (point, label) => {
                const x = Number(point && point.x);
                const y = Number(point && point.y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) {
                    throw new Error(`Cannot resolve road path endpoint continuity with non-finite ${label} point.`);
                }
                return { x, y };
            };
            return {
                start: normalizeEndpoint(points[0], "start"),
                end: normalizeEndpoint(points[points.length - 1], "end")
            };
        }

        getRoadPathContinuityTextureKey(roadPath) {
            const RoadClass = (typeof global !== "undefined" && global && global.Road)
                ? global.Road
                : ((typeof Road !== "undefined" && Road) ? Road : null);
            const defaultTexturePath = RoadClass && typeof RoadClass._defaultFillTexturePath === "string"
                ? RoadClass._defaultFillTexturePath
                : "/assets/images/flooring/dirt.jpg";
            const rawTexturePath = typeof (roadPath && roadPath.fillTexturePath) === "string" && roadPath.fillTexturePath.length > 0
                ? roadPath.fillTexturePath
                : defaultTexturePath;
            if (RoadClass && typeof RoadClass._normalizeFillTexturePath === "function") {
                return RoadClass._normalizeFillTexturePath(rawTexturePath);
            }
            return rawTexturePath;
        }

        getRoadPathContinuityMaterialKey(roadPath) {
            const fallbackNode = typeof (roadPath && roadPath.getNode) === "function" ? roadPath.getNode() : (roadPath && roadPath.node);
            const roadLayer = this.getLayerIndexForObject(roadPath, this.getLayerIndexForNode(fallbackNode || roadPath));
            return `${roadLayer}:${this.getRoadPathContinuityTextureKey(roadPath)}`;
        }

        buildRoadPathEndpointContinuity(roadPaths) {
            const out = new Map();
            const buckets = new Map();
            const snapBuckets = new Map();
            const eps = 1e-4;
            const epsSq = eps * eps;
            const bucketCoord = (value) => Math.floor(Number(value) / eps);
            const bucketKey = (materialKey, bx, by) => `${materialKey}:${bx}:${by}`;
            const snapBucketKey = (materialKey, snapId) => `${materialKey}:${snapId}`;
            const markConnected = (a, b) => {
                if (a.roadPath === b.roadPath || a.materialKey !== b.materialKey) return;
                const dx = a.point.x - b.point.x;
                const dy = a.point.y - b.point.y;
                if ((dx * dx) + (dy * dy) > epsSq) return;
                const aContinuity = out.get(a.roadPath);
                const bContinuity = out.get(b.roadPath);
                if (aContinuity) aContinuity[a.end === "start" ? "startConnected" : "endConnected"] = true;
                if (bContinuity) bContinuity[b.end === "start" ? "startConnected" : "endConnected"] = true;
            };
            const markSnapConnected = (a, b) => {
                if (a.roadPath === b.roadPath || a.materialKey !== b.materialKey || !a.snapId || a.snapId !== b.snapId) return;
                const aContinuity = out.get(a.roadPath);
                const bContinuity = out.get(b.roadPath);
                if (aContinuity) aContinuity[a.end === "start" ? "startConnected" : "endConnected"] = true;
                if (bContinuity) bContinuity[b.end === "start" ? "startConnected" : "endConnected"] = true;
            };
            const addEndpoint = (endpoint) => {
                const bx = bucketCoord(endpoint.point.x);
                const by = bucketCoord(endpoint.point.y);
                for (let dx = -1; dx <= 1; dx++) {
                    for (let dy = -1; dy <= 1; dy++) {
                        const existing = buckets.get(bucketKey(endpoint.materialKey, bx + dx, by + dy));
                        if (!Array.isArray(existing)) continue;
                        for (let i = 0; i < existing.length; i++) markConnected(endpoint, existing[i]);
                    }
                }
                const key = bucketKey(endpoint.materialKey, bx, by);
                let bucket = buckets.get(key);
                if (!bucket) {
                    bucket = [];
                    buckets.set(key, bucket);
                }
                bucket.push(endpoint);
                if (endpoint.snapId) {
                    const snapKey = snapBucketKey(endpoint.materialKey, endpoint.snapId);
                    const existing = snapBuckets.get(snapKey);
                    if (Array.isArray(existing)) {
                        for (let i = 0; i < existing.length; i++) markSnapConnected(endpoint, existing[i]);
                        existing.push(endpoint);
                    } else {
                        snapBuckets.set(snapKey, [endpoint]);
                    }
                }
            };
            const iterable = roadPaths && typeof roadPaths[Symbol.iterator] === "function" ? roadPaths : [];
            for (const roadPath of iterable) {
                if (!roadPath || roadPath.gone || roadPath.type !== "roadPath") continue;
                if (!out.has(roadPath)) out.set(roadPath, { startConnected: false, endConnected: false });
                const pair = this.getRoadPathEndpointPair(roadPath);
                const materialKey = this.getRoadPathContinuityMaterialKey(roadPath);
                const endpointSnapIds = roadPath.endpointSnapIds && typeof roadPath.endpointSnapIds === "object"
                    ? roadPath.endpointSnapIds
                    : {};
                const startSnapId = typeof endpointSnapIds.start === "string" ? endpointSnapIds.start.trim() : "";
                const endSnapId = typeof endpointSnapIds.end === "string" ? endpointSnapIds.end.trim() : "";
                addEndpoint({ roadPath, end: "start", point: pair.start, materialKey, snapId: startSnapId });
                addEndpoint({ roadPath, end: "end", point: pair.end, materialKey, snapId: endSnapId });
            }
            return out;
        }

        collectRoadPathContinuityObjectsFromNodes(nodes) {
            const out = new Set();
            const sourceNodes = Array.isArray(nodes) ? nodes : [];
            for (let i = 0; i < sourceNodes.length; i++) {
                const node = sourceNodes[i];
                if (!node || !Array.isArray(node.objects)) continue;
                for (let j = 0; j < node.objects.length; j++) {
                    const obj = node.objects[j];
                    if (!obj || obj.gone || obj.type !== "roadPath") continue;
                    out.add(obj);
                }
            }
            return out;
        }

        getRoadPathRenderViewportBounds(ctx, margin = 8) {
            const cameraRef = (ctx && ctx.camera) || this.camera || {};
            const cameraZ = Number.isFinite(cameraRef.z)
                ? Number(cameraRef.z)
                : (Number.isFinite(ctx && ctx.viewport && ctx.viewport.z) ? Number(ctx.viewport.z) : 0);
            const projected = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, margin, cameraZ);
            if (projected) return projected;
            const viewportRef = (ctx && ctx.viewport) || {};
            const width = Math.max(0, Number(viewportRef.width) || 0);
            const height = Math.max(0, Number(viewportRef.height) || 0);
            const x = Number(cameraRef.x) || 0;
            const y = Number(cameraRef.y) || 0;
            return {
                minX: x - margin,
                minY: y - margin,
                maxX: x + width + margin,
                maxY: y + height + margin
            };
        }

        expandWorldBounds(bounds, margin = 0) {
            if (!bounds) return null;
            const amount = Math.max(0, Number(margin) || 0);
            return {
                minX: Number(bounds.minX) - amount,
                minY: Number(bounds.minY) - amount,
                maxX: Number(bounds.maxX) + amount,
                maxY: Number(bounds.maxY) + amount
            };
        }

        collectRoadPathRenderObjects(ctx, bakedLevel0SectionKeys = null) {
            const visibleRoadPathObjects = new Set();
            const continuityRoadPathObjects = new Set();
            const bakedRoadPathObjects = new Set();
            const map = ctx && ctx.map;
            if (!map) {
                return {
                    visibleRoadPathObjects,
                    continuityRoadPathObjects,
                    bakedRoadPathObjects,
                    considered: 0,
                    skippedOffscreen: 0,
                    skippedForLevel0Bake: 0
                };
            }
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            const viewportBounds = this.getRoadPathRenderViewportBounds(ctx, 8);
            const continuityBounds = this.expandWorldBounds(viewportBounds, 16);
            const seen = new Set();
            let considered = 0;
            let skippedOffscreen = 0;
            let skippedForLevel0Bake = 0;
            const addCandidate = (roadPath) => {
                if (!roadPath || seen.has(roadPath)) return;
                seen.add(roadPath);
                if (roadPath.gone || roadPath.vanishing || roadPath._prototypeParked === true || roadPath.type !== "roadPath") return;
                considered += 1;
                if (!this.isScriptVisible(roadPath)) return;
                const fallbackNode = typeof roadPath.getNode === "function" ? roadPath.getNode() : roadPath.node;
                const roadLayer = this.getLayerIndexForObject(roadPath, this.getLayerIndexForNode(fallbackNode || roadPath));
                if (mazeLayerOnly && roadLayer !== wizardLayer && !this.isFallRevealHiddenLayer(roadLayer)) return;
                if (this.isRenderItemHiddenByLayerCutaway(roadPath, roadLayer, cutawayState, map)) return;
                const bounds = this.getRoadPathVisualBounds(roadPath);
                const inContinuityRange = !continuityBounds || this.floorFragmentBoundsIntersectViewport(bounds, continuityBounds);
                if (inContinuityRange) continuityRoadPathObjects.add(roadPath);
                if (viewportBounds && !this.floorFragmentBoundsIntersectViewport(bounds, viewportBounds)) {
                    skippedOffscreen += 1;
                    return;
                }
                if (this.isRoadPathBakedIntoLevel0Surface(ctx, roadPath, bakedLevel0SectionKeys)) {
                    bakedRoadPathObjects.add(roadPath);
                    skippedForLevel0Bake += 1;
                    return;
                }
                visibleRoadPathObjects.add(roadPath);
            };
            const objectState = map._prototypeObjectState || null;
            if (objectState && objectState.activeRuntimeObjectsByRecordId instanceof Map) {
                for (const roadPath of objectState.activeRuntimeObjectsByRecordId.values()) addCandidate(roadPath);
            }
            if (Array.isArray(map.objects)) {
                for (let i = 0; i < map.objects.length; i++) addCandidate(map.objects[i]);
            }
            return {
                visibleRoadPathObjects,
                continuityRoadPathObjects,
                bakedRoadPathObjects,
                considered,
                skippedOffscreen,
                skippedForLevel0Bake
            };
        }

        isRoadPathCoveredByReadyLevel0Chunks(ctx, roadPath) {
            if (!FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED || !roadPath) return false;
            const map = ctx && ctx.map;
            const fallbackNode = typeof roadPath.getNode === "function" ? roadPath.getNode() : roadPath.node;
            const roadLayer = this.getLayerIndexForObject(roadPath, this.getLayerIndexForNode(fallbackNode || roadPath));
            if (roadLayer !== 0) return false;
            const sectionKey = fallbackNode && typeof fallbackNode._prototypeSectionKey === "string" ? fallbackNode._prototypeSectionKey : "";
            const state = map && map._prototypeSectionState;
            const asset = sectionKey && state && state.sectionAssetsByKey instanceof Map
                ? state.sectionAssetsByKey.get(sectionKey)
                : null;
            if (!asset) return false;
            const bounds = this.getRoadPathVisualBounds(roadPath);
            return this.isLevel0GroundSurfaceChunkReadyForBounds(sectionKey, asset, bounds, null, map);
        }

        getLevel0GroundSurfaceAssetTileCoordSet(asset) {
            const tileCoordKeys = Array.isArray(asset && asset.tileCoordKeys) ? asset.tileCoordKeys : [];
            const signature = `${tileCoordKeys.length}:${Number(asset && asset._level0SurfaceVersion) || 0}`;
            if (asset && asset._level0GroundSurfaceTileCoordSet instanceof Set && asset._level0GroundSurfaceTileCoordSetSignature === signature) {
                return asset._level0GroundSurfaceTileCoordSet;
            }
            const set = new Set(tileCoordKeys);
            if (asset) {
                asset._level0GroundSurfaceTileCoordSet = set;
                asset._level0GroundSurfaceTileCoordSetSignature = signature;
            }
            return set;
        }

        isLevel0GroundSurfaceChunkReadyForBounds(sectionKey, asset, bounds, readyCache = null, map = null) {
            if (!FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED || !sectionKey || !asset || !bounds) return false;
            const coords = this.getLevel0GroundSurfaceChunkCoordsForBounds(bounds);
            if (coords.length === 0) return false;
            for (let i = 0; i < coords.length; i++) {
                const coord = coords[i];
                const cacheKey = this.getLevel0GroundSurfaceChunkKey(sectionKey, coord.chunkX, coord.chunkY);
                if (readyCache instanceof Map && readyCache.has(cacheKey)) {
                    if (readyCache.get(cacheKey) !== true) return false;
                    continue;
                }
                const cache = this.level0GroundSurfaceChunkCache instanceof Map
                    ? this.level0GroundSurfaceChunkCache.get(cacheKey)
                    : null;
                const signature = this.getLevel0GroundSurfaceChunkSignature(asset, coord.chunkX, coord.chunkY);
                const ready = !!(
                    cache &&
                    cache.ready === true &&
                    cache.signature === signature &&
                    cache.texture &&
                    cache.bounds
                );
                if (readyCache instanceof Map) readyCache.set(cacheKey, ready);
                if (!ready) return false;
            }
            return true;
        }

        isGroundNodeCoveredByReadyLevel0Chunk(ctx, node, tileWorldW, tileWorldH, readyCache = null, assetCache = null) {
            if (!node) return false;
            const nodeLayer = this.getLayerIndexForNode(node);
            if (nodeLayer !== 0) return false;
            const map = ctx && ctx.map;
            const sectionKey = typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "";
            if (!map || !sectionKey) return false;
            let asset = null;
            let editedLevel0 = false;
            if (assetCache instanceof Map && assetCache.has(sectionKey)) {
                const cached = assetCache.get(sectionKey);
                asset = cached && cached.asset ? cached.asset : null;
                editedLevel0 = !!(cached && cached.editedLevel0);
            } else {
                const state = map._prototypeSectionState || null;
                asset = state && state.sectionAssetsByKey instanceof Map
                    ? state.sectionAssetsByKey.get(sectionKey)
                    : null;
                editedLevel0 = !!(asset && this.hasEditedLevel0FloorAsset(asset));
                if (assetCache instanceof Map) {
                    assetCache.set(sectionKey, {
                        asset: asset || null,
                        editedLevel0
                    });
                }
            }
            if (!asset || !editedLevel0) return false;
            const tileKey = `${node.xindex},${node.yindex}`;
            const tileCoordSet = this.getLevel0GroundSurfaceAssetTileCoordSet(asset);
            if (tileCoordSet instanceof Set && !tileCoordSet.has(tileKey)) return false;
            if (FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED) return true;
            if (!FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED) return false;
            const x = Number(node.x);
            const y = Number(node.y);
            if (!Number.isFinite(x) || !Number.isFinite(y)) return false;
            const width = Math.max(0.001, Number(tileWorldW) || 1);
            const height = Math.max(0.001, Number(tileWorldH) || 1);
            return this.isLevel0GroundSurfaceChunkReadyForBounds(
                sectionKey,
                asset,
                {
                    minX: x - width * 0.5,
                    minY: y - height * 0.5,
                    maxX: x + width * 0.5,
                    maxY: y + height * 0.5
                },
                readyCache,
                map
            );
        }

        getBakedLevel0SectionKeys(ctx) {
            if (!FLOOR_LEVEL0_BAKED_SURFACE_ENABLED) return new Set();
            const map = ctx && ctx.map;
            const state = map && map._prototypeSectionState;
            if (!state || !(state.sectionAssetsByKey instanceof Map)) return new Set();
            if (FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED) {
                let removedChunkedSectionCaches = 0;
                if (this.level0GroundSurfaceCache instanceof Map) {
                    for (const [sectionKey, cache] of this.level0GroundSurfaceCache.entries()) {
                        if (cache && cache.texture && typeof cache.texture.destroy === "function") {
                            cache.texture.destroy(true);
                        }
                        this.level0GroundSurfaceCache.delete(sectionKey);
                        removedChunkedSectionCaches += 1;
                        if (this.level0GroundSurfaceBakeNodeCache instanceof Map) {
                            this.level0GroundSurfaceBakeNodeCache.delete(sectionKey);
                        }
                    }
                }
                if (removedChunkedSectionCaches > 0) this.bumpLevel0GroundCoverageVersion();
                this.bakedLevel0SectionKeys = new Set();
                this.bakedLevel0SectionSignature = `chunked:${state.sectionAssetsByKey.size}`;
                return this.bakedLevel0SectionKeys;
            }
            const assetCount = state.sectionAssetsByKey.size;
            let editVersion = 0;
            for (const [, asset] of state.sectionAssetsByKey.entries()) {
                editVersion += Number(asset && asset._level0SurfaceVersion) || 0;
                editVersion += Number(asset && asset._level0RoadSurfaceVersion) || 0;
                editVersion += Number(asset && asset._level0GroundSurfaceVersion) || 0;
                editVersion += Number(asset && asset._level0SurfaceTextureReadyVersion) || 0;
            }
            const signature = `${assetCount}:${editVersion}`;
            if (signature === this.bakedLevel0SectionSignature && this.bakedLevel0SectionKeys instanceof Set) {
                return this.bakedLevel0SectionKeys;
            }
            const out = new Set();
            for (const [sectionKey, asset] of state.sectionAssetsByKey.entries()) {
                const cache = this.level0GroundSurfaceCache instanceof Map
                    ? this.level0GroundSurfaceCache.get(sectionKey)
                    : null;
                const nodes = state.nodesBySectionKey instanceof Map
                    ? (state.nodesBySectionKey.get(sectionKey) || [])
                    : [];
                const expectedSignature = cache
                    ? this.getLevel0GroundSurfaceSignature(asset, nodes)
                    : "";
                if (
                    this.hasEditedLevel0FloorAsset(asset) &&
                    (
                        FLOOR_LEVEL0_FORCE_BAKED_SURFACE ||
                        (
                            cache &&
                            cache.ready === true &&
                            cache.signature === expectedSignature &&
                            cache.texture &&
                            cache.bounds
                        )
                    )
                ) {
                    out.add(sectionKey);
                }
            }
            if (this.level0GroundSurfaceCache instanceof Map) {
                for (const sectionKey of this.level0GroundSurfaceCache.keys()) {
                    if (!out.has(sectionKey)) {
                        const cache = this.level0GroundSurfaceCache.get(sectionKey);
                        if (cache && cache.texture && typeof cache.texture.destroy === "function") {
                            cache.texture.destroy(true);
                        }
                        this.level0GroundSurfaceCache.delete(sectionKey);
                        if (this.level0GroundSurfaceBakeNodeCache instanceof Map) {
                            this.level0GroundSurfaceBakeNodeCache.delete(sectionKey);
                        }
                    }
                }
            }
            this.bakedLevel0SectionKeys = out;
            this.bakedLevel0SectionSignature = signature;
            return out;
        }

        getLevel0GroundSurfaceSignature(asset, nodes) {
            const tileCoordKeys = Array.isArray(asset && asset.tileCoordKeys) ? asset.tileCoordKeys : [];
            return [
                Number(asset && asset._level0SurfaceVersion) || 0,
                Number(asset && asset._level0RoadSurfaceModelVersion) || 0,
                Number(asset && asset._level0RoadSurfaceVersion) || 0,
                Number(asset && asset._level0GroundSurfaceVersion) || 0,
                Number(asset && asset._level0SurfaceTextureReadyVersion) || 0,
                FLOOR_LEVEL0_BAKE_ROAD_PATHS ? 1 : 0,
                this.getLevel0TerrainPolygonsSignature(asset),
                tileCoordKeys.length,
                Array.isArray(nodes) ? nodes.length : 0
            ].join(":");
        }

        getLevel0TerrainPolygonsSignature(asset) {
            const polygons = Array.isArray(asset && asset.terrainPolygons) ? asset.terrainPolygons : [];
            if (polygons.length === 0) return "terrain:0";
            const parts = [`terrain:${polygons.length}`];
            for (let i = 0; i < polygons.length; i++) {
                const polygon = polygons[i] || {};
                const points = Array.isArray(polygon.points) ? polygon.points : [];
                const holes = Array.isArray(polygon.holes) ? polygon.holes : [];
                parts.push(String(polygon.type || ""));
                parts.push(String(points.length));
                for (let p = 0; p < points.length; p++) {
                    parts.push(`${Math.round(Number(points[p] && points[p].x) * 1000)},${Math.round(Number(points[p] && points[p].y) * 1000)}`);
                }
                parts.push(`holes:${holes.length}`);
                for (let h = 0; h < holes.length; h++) {
                    const hole = Array.isArray(holes[h]) ? holes[h] : [];
                    parts.push(String(hole.length));
                    for (let p = 0; p < hole.length; p++) {
                        parts.push(`${Math.round(Number(hole[p] && hole[p].x) * 1000)},${Math.round(Number(hole[p] && hole[p].y) * 1000)}`);
                    }
                }
            }
            return parts.join("|");
        }

        getRoadSectionKey(road) {
            if (!road) return "";
            const node = typeof road.getNode === "function" ? road.getNode() : road.node;
            return node && typeof node._prototypeSectionKey === "string" ? node._prototypeSectionKey : "";
        }

        isRoadBakedIntoLevel0Surface(ctx, road, bakedSectionKeys = null) {
            if (FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED) return false;
            if (FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED) {
                return this.isRoadCoveredByReadyLevel0Chunks(ctx, road);
            }
            const sectionKey = this.getRoadSectionKey(road);
            if (!sectionKey) return false;
            const sectionKeys = bakedSectionKeys instanceof Set
                ? bakedSectionKeys
                : this.getBakedLevel0SectionKeys(ctx);
            return sectionKeys instanceof Set && sectionKeys.has(sectionKey);
        }

        isRoadPathBakedIntoLevel0Surface(ctx, roadPath, bakedSectionKeys = null) {
            if (FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED) return false;
            if (!FLOOR_LEVEL0_BAKE_ROAD_PATHS) return false;
            if (!roadPath || roadPath.type !== "roadPath") return false;
            const fallbackNode = typeof roadPath.getNode === "function" ? roadPath.getNode() : roadPath.node;
            const roadLayer = this.getLayerIndexForObject(roadPath, this.getLayerIndexForNode(fallbackNode || roadPath));
            if (roadLayer !== 0) return false;
            if (FLOOR_LEVEL0_CHUNKED_SURFACE_ENABLED) {
                return this.isRoadPathCoveredByReadyLevel0Chunks(ctx, roadPath);
            }
            const sectionKey = this.getRoadSectionKey(roadPath);
            if (!sectionKey) return false;
            const sectionKeys = bakedSectionKeys instanceof Set
                ? bakedSectionKeys
                : this.getBakedLevel0SectionKeys(ctx);
            return sectionKeys instanceof Set && sectionKeys.has(sectionKey);
        }

        markLevel0GroundSurfacePendingTexture(sectionKey, baseTexture, asset = null) {
            if (!sectionKey || !baseTexture || typeof baseTexture.once !== "function") return;
            const key = `${sectionKey}:${baseTexture.uid || baseTexture.cacheId || ""}`;
            if (this.level0GroundSurfacePendingLoads instanceof Set && this.level0GroundSurfacePendingLoads.has(key)) return;
            if (!(this.level0GroundSurfacePendingLoads instanceof Set)) {
                this.level0GroundSurfacePendingLoads = new Set();
            }
            this.level0GroundSurfacePendingLoads.add(key);
            baseTexture.once("loaded", () => {
                this.level0GroundSurfacePendingLoads.delete(key);
                if (asset) {
                    asset._level0SurfaceTextureReadyVersion = (Number(asset._level0SurfaceTextureReadyVersion) || 0) + 1;
                }
                this.bakedLevel0SectionSignature = "";
                if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                    globalThis.presentGameFrame();
                }
            });
        }

        getLevel0BakeImageSource(texture) {
            if (!isRenderablePixiTexture(texture)) return null;
            const baseTexture = texture.baseTexture || null;
            const resource = baseTexture && baseTexture.resource ? baseTexture.resource : null;
            const source = resource && resource.source ? resource.source : null;
            if (!source) return null;
            const width = Number(source.naturalWidth || source.videoWidth || source.width) || 0;
            const height = Number(source.naturalHeight || source.videoHeight || source.height) || 0;
            if (width <= 0 || height <= 0) return null;
            return source;
        }

        getLevel0TerrainNodeKey(node) {
            return `${Math.floor(Number(node && node.xindex) || 0)},${Math.floor(Number(node && node.yindex) || 0)}`;
        }

        getLevel0TerrainPointKey(point) {
            const q = 1000000;
            return `${Math.round(Number(point && point.x) * q)},${Math.round(Number(point && point.y) * q)}`;
        }

        getLevel0TerrainHexCorners(map, node) {
            const x = Number(node && node.x);
            const y = Number(node && node.y);
            if (!Number.isFinite(x) || !Number.isFinite(y)) {
                throw new Error("Cannot build terrain region polygon without finite node coordinates.");
            }
            const w = Number.isFinite(map && map.hexWidth) ? Number(map.hexWidth) : (1 / 0.866);
            const h = Number.isFinite(map && map.hexHeight) ? Number(map.hexHeight) : 1;
            return [
                { x: x - w * 0.5, y },
                { x: x - w * 0.25, y: y - h * 0.5 },
                { x: x + w * 0.25, y: y - h * 0.5 },
                { x: x + w * 0.5, y },
                { x: x + w * 0.25, y: y + h * 0.5 },
                { x: x - w * 0.25, y: y + h * 0.5 }
            ];
        }

        getLevel0TerrainNodeAtCoord(map, xindex, yindex) {
            if (!map) return null;
            if (typeof map.getGroundTerrainNodeByCoord === "function") {
                return map.getGroundTerrainNodeByCoord(xindex, yindex) || null;
            }
            if (typeof map.getGroundNodeForCoord === "function") {
                return map.getGroundNodeForCoord(xindex, yindex) || null;
            }
            if (typeof map.getNode === "function") {
                return map.getNode(xindex, yindex, 0) || null;
            }
            return null;
        }

        getLevel0TerrainTouchingNodesForVertex(map, point) {
            const px = Number(point && point.x);
            const py = Number(point && point.y);
            if (!Number.isFinite(px) || !Number.isFinite(py)) {
                throw new Error("terrain vertex membership requires a finite point");
            }
            const approxX = Math.round(px / 0.866);
            const approxY = Math.round(py);
            const out = [];
            const seen = new Set();
            const targetKey = this.getLevel0TerrainPointKey(point);
            for (let xi = approxX - 3; xi <= approxX + 3; xi++) {
                for (let yi = approxY - 3; yi <= approxY + 3; yi++) {
                    const node = this.getLevel0TerrainNodeAtCoord(map, xi, yi);
                    if (!node || node._prototypeVoid === true) continue;
                    const nodeKey = this.getLevel0TerrainNodeKey(node);
                    if (seen.has(nodeKey)) continue;
                    const corners = this.getLevel0TerrainHexCorners(map, node);
                    let touches = false;
                    for (let c = 0; c < corners.length; c++) {
                        if (this.getLevel0TerrainPointKey(corners[c]) === targetKey) {
                            touches = true;
                            break;
                        }
                    }
                    if (!touches) continue;
                    seen.add(nodeKey);
                    out.push(node);
                }
            }
            return out;
        }

        getLevel0TerrainNonGroupTouchCountForVertex(map, point, nodeKeys) {
            if (!(nodeKeys instanceof Set)) {
                throw new Error("terrain vertex membership requires a terrain group node set");
            }
            const touching = this.getLevel0TerrainTouchingNodesForVertex(map, point);
            if (touching.length !== 3) {
                throw new Error(`terrain vertex membership expected 3 touching hexes, found ${touching.length}`);
            }
            let groupCount = 0;
            for (let i = 0; i < touching.length; i++) {
                if (nodeKeys.has(this.getLevel0TerrainNodeKey(touching[i]))) groupCount += 1;
            }
            return 3 - groupCount;
        }

        getLevel0TerrainNeighborSlotKey(map, node, direction) {
            if (!node) return "";
            const neighbor = node.neighbors && node.neighbors[direction];
            if (neighbor) return this.getLevel0TerrainNodeKey(neighbor);
            const offset = node.neighborOffsets && node.neighborOffsets[direction];
            if (offset && Number.isFinite(offset.x) && Number.isFinite(offset.y)) {
                const xindex = Number(node.xindex) + Number(offset.x);
                const yindex = Number(node.yindex) + Number(offset.y);
                const resolved = this.getLevel0TerrainNodeAtCoord(map, xindex, yindex);
                if (resolved) return this.getLevel0TerrainNodeKey(resolved);
                return `${Math.floor(xindex)},${Math.floor(yindex)}`;
            }
            return `missing:${this.getLevel0TerrainNodeKey(node)}:${direction}`;
        }

        buildLevel0TerrainVertexSlotMap(map, group) {
            const nodes = Array.isArray(group && group.nodes) ? group.nodes : [];
            const dirs = [1, 3, 5, 7, 9, 11];
            const out = new Map();
            for (let n = 0; n < nodes.length; n++) {
                const node = nodes[n];
                if (!node) continue;
                const nodeKey = this.getLevel0TerrainNodeKey(node);
                const corners = this.getLevel0TerrainHexCorners(map, node);
                for (let c = 0; c < corners.length; c++) {
                    const pointKey = this.getLevel0TerrainPointKey(corners[c]);
                    let slots = out.get(pointKey);
                    if (!slots) {
                        slots = new Set();
                        out.set(pointKey, slots);
                    }
                    slots.add(nodeKey);
                    slots.add(this.getLevel0TerrainNeighborSlotKey(map, node, dirs[(c + 5) % 6]));
                    slots.add(this.getLevel0TerrainNeighborSlotKey(map, node, dirs[c]));
                }
            }
            return out;
        }

        getLevel0TerrainNonGroupTouchCountFromSlots(point, nodeKeys, vertexSlotsByPointKey) {
            if (!(nodeKeys instanceof Set)) {
                throw new Error("terrain vertex membership requires a terrain group node set");
            }
            if (!(vertexSlotsByPointKey instanceof Map)) {
                throw new Error("terrain vertex membership requires a vertex slot map");
            }
            const pointKey = this.getLevel0TerrainPointKey(point);
            const slots = vertexSlotsByPointKey.get(pointKey);
            if (!(slots instanceof Set)) {
                throw new Error("terrain vertex membership could not resolve touching hex slots for boundary vertex");
            }
            if (slots.size !== 3) {
                throw new Error(`terrain vertex membership expected 3 touching hex slots, found ${slots.size}`);
            }
            let groupCount = 0;
            for (const slotKey of slots) {
                if (nodeKeys.has(slotKey)) groupCount += 1;
            }
            return 3 - groupCount;
        }

        getLevel0TerrainTypeForNode(map, node) {
            if (map && typeof map.getGroundTerrainTypeForNode === "function") {
                return map.getGroundTerrainTypeForNode(node);
            }
            if (map && typeof map.getGroundTerrainDef === "function") {
                const def = map.getGroundTerrainDef(Number.isFinite(node && node.groundTextureId) ? Math.floor(Number(node.groundTextureId)) : 0);
                if (def && typeof def.name === "string" && def.name.length > 0) return def.name;
            }
            throw new Error("level 0 terrain region baking requires map ground terrain type accessors");
        }

        getLevel0TerrainTexturePathForNode(map, node) {
            if (map && typeof map.getGroundTexturePathForNode === "function") {
                return map.getGroundTexturePathForNode(node);
            }
            throw new Error("level 0 terrain region baking requires map.getGroundTexturePathForNode");
        }

        getLevel0TerrainMaterialPathForType(map, typeName) {
            if (map && typeof map.getGroundPolygonMaterialPathForType === "function") {
                return map.getGroundPolygonMaterialPathForType(typeName);
            }
            throw new Error("level 0 terrain region baking requires map.getGroundPolygonMaterialPathForType");
        }

        getLevel0TerrainMaterialScaleForType(map, typeName) {
            if (map && typeof map.getGroundPolygonMaterialScaleForType === "function") {
                return map.getGroundPolygonMaterialScaleForType(typeName);
            }
            throw new Error("level 0 terrain region baking requires map.getGroundPolygonMaterialScaleForType");
        }

        getLevel0TerrainEdgeFadePx(scale) {
            const RoadClass = typeof global.Road !== "undefined" ? global.Road : null;
            if (
                !RoadClass ||
                !Number.isFinite(RoadClass._edgeFadePx) ||
                !Number.isFinite(RoadClass._pixelsPerWorldUnit) ||
                !(RoadClass._pixelsPerWorldUnit > 0)
            ) {
                throw new Error("Cannot bake ground terrain alpha fade without road edge fade metrics.");
            }
            return Math.max(0.0001, Number(RoadClass._edgeFadePx) / Number(RoadClass._pixelsPerWorldUnit)) * scale;
        }

        collectLevel0TerrainGroups(map, nodes) {
            if (!map || !Array.isArray(nodes)) return [];
            const seedRecordsByKey = new Map();
            for (let i = 0; i < nodes.length; i++) {
                const node = nodes[i];
                if (!node || node._prototypeVoid === true) continue;
                const type = this.getLevel0TerrainTypeForNode(map, node);
                if (type === "grass") continue;
                const key = this.getLevel0TerrainNodeKey(node);
                if (seedRecordsByKey.has(key)) continue;
                seedRecordsByKey.set(key, {
                    key,
                    node,
                    type,
                    materialPath: this.getLevel0TerrainMaterialPathForType(map, type),
                    materialScale: this.getLevel0TerrainMaterialScaleForType(map, type)
                });
            }
            const dirs = [1, 3, 5, 7, 9, 11];
            const groups = [];
            const visited = new Set();
            for (const record of seedRecordsByKey.values()) {
                if (visited.has(record.key)) continue;
                visited.add(record.key);
                const group = {
                    type: record.type,
                    materialPath: record.materialPath,
                    materialScale: record.materialScale,
                    nodes: [],
                    nodeKeys: new Set()
                };
                const queue = [record];
                for (let q = 0; q < queue.length; q++) {
                    const current = queue[q];
                    group.nodes.push(current.node);
                    group.nodeKeys.add(current.key);
                    for (let d = 0; d < dirs.length; d++) {
                        const neighbor = current.node.neighbors && current.node.neighbors[dirs[d]];
                        if (!neighbor || neighbor._prototypeVoid === true) continue;
                        const neighborKey = this.getLevel0TerrainNodeKey(neighbor);
                        if (visited.has(neighborKey)) continue;
                        const neighborType = this.getLevel0TerrainTypeForNode(map, neighbor);
                        if (neighborType !== group.type) continue;
                        const neighborRecord = {
                            key: neighborKey,
                            node: neighbor,
                            type: neighborType,
                            materialPath: group.materialPath,
                            materialScale: group.materialScale
                        };
                        visited.add(neighborKey);
                        queue.push(neighborRecord);
                    }
                }
                groups.push(group);
            }
            return groups;
        }

        getLevel0TerrainLoopArea(points) {
            if (!Array.isArray(points) || points.length < 3) return 0;
            let area = 0;
            for (let i = 0; i < points.length; i++) {
                const a = points[i];
                const b = points[(i + 1) % points.length];
                area += Number(a.x) * Number(b.y) - Number(a.y) * Number(b.x);
            }
            return area * 0.5;
        }

        pointInLevel0TerrainLoop(px, py, points) {
            let inside = false;
            for (let i = 0, j = points.length - 1; i < points.length; j = i++) {
                const xi = points[i].x;
                const yi = points[i].y;
                const xj = points[j].x;
                const yj = points[j].y;
                const intersects = ((yi > py) !== (yj > py)) &&
                    (px < ((xj - xi) * (py - yi)) / ((yj - yi) || 1e-7) + xi);
                if (intersects) inside = !inside;
            }
            return inside;
        }

        pointInLevel0TerrainRegion(px, py, loops) {
            let inside = false;
            for (let i = 0; i < loops.length; i++) {
                const points = loops[i] && Array.isArray(loops[i].points) ? loops[i].points : loops[i];
                if (Array.isArray(points) && points.length >= 3 && this.pointInLevel0TerrainLoop(px, py, points)) {
                    inside = !inside;
                }
            }
            return inside;
        }

        simplifyLevel0TerrainLoop(points) {
            if (!Array.isArray(points)) return [];
            const out = [];
            for (let i = 0; i < points.length; i++) {
                const point = points[i];
                const prev = out[out.length - 1];
                if (prev && Math.abs(prev.x - point.x) < 1e-7 && Math.abs(prev.y - point.y) < 1e-7) continue;
                out.push({ x: point.x, y: point.y });
            }
            if (out.length > 1) {
                const first = out[0];
                const last = out[out.length - 1];
                if (Math.abs(first.x - last.x) < 1e-7 && Math.abs(first.y - last.y) < 1e-7) out.pop();
            }
            let changed = true;
            while (changed && out.length > 3) {
                changed = false;
                for (let i = 0; i < out.length; i++) {
                    const prev = out[(i + out.length - 1) % out.length];
                    const cur = out[i];
                    const next = out[(i + 1) % out.length];
                    const ax = cur.x - prev.x;
                    const ay = cur.y - prev.y;
                    const bx = next.x - cur.x;
                    const by = next.y - cur.y;
                    if (Math.abs(ax * by - ay * bx) < 1e-7) {
                        out.splice(i, 1);
                        changed = true;
                        break;
                    }
                }
            }
            return out;
        }

        buildLevel0TerrainRegionLoops(map, group) {
            const nodes = Array.isArray(group && group.nodes) ? group.nodes : [];
            const nodeKeys = group && group.nodeKeys instanceof Set ? group.nodeKeys : new Set();
            if (nodes.length === 0) return [];
            const api = getFloorVisualPolygonClippingApi();
            if (!api || typeof api.union !== "function") {
                throw new Error("terrain region boundary extraction requires polygon clipping union");
            }
            const geometries = [];
            for (let i = 0; i < nodes.length; i++) {
                const node = nodes[i];
                const corners = this.getLevel0TerrainHexCorners(map, node);
                const geometry = floorVisualClipMultiPolygonFromRings(corners, []);
                if (Array.isArray(geometry) && geometry.length > 0) geometries.push(geometry);
            }
            if (geometries.length === 0) return [];
            let unionGeometry = null;
            try {
                unionGeometry = api.union(...geometries);
            } catch (err) {
                throw new Error(`terrain region boundary union failed: ${err && err.message ? err.message : err}`);
            }
            const polygons = floorVisualClipGeometryToPolygons(unionGeometry);
            if (!Array.isArray(polygons) || polygons.length === 0) return [];
            const rawLoops = [];
            for (let i = 0; i < polygons.length; i++) {
                const polygon = polygons[i];
                const outer = this.simplifyLevel0TerrainLoop(polygon && polygon.outer);
                if (outer.length < 3) {
                    throw new Error("terrain region union produced an invalid outer boundary");
                }
                rawLoops.push({
                    points: outer,
                    isHole: false,
                    area: this.getLevel0TerrainLoopArea(outer)
                });
                const holes = Array.isArray(polygon && polygon.holes) ? polygon.holes : [];
                for (let h = 0; h < holes.length; h++) {
                    const hole = this.simplifyLevel0TerrainLoop(holes[h]);
                    if (hole.length < 3) {
                        throw new Error("terrain region union produced an invalid hole boundary");
                    }
                    rawLoops.push({
                        points: hole,
                        isHole: true,
                        area: this.getLevel0TerrainLoopArea(hole)
                    });
                }
            }
            const out = [];
            const vertexSlotsByPointKey = this.buildLevel0TerrainVertexSlotMap(map, group);
            for (let i = 0; i < rawLoops.length; i++) {
                const raw = rawLoops[i];
                const requiredNonGroupCount = raw.isHole ? 1 : 2;
                const pointRecords = [];
                for (let p = 0; p < raw.points.length; p++) {
                    const point = raw.points[p];
                    const nonGroupCount = this.getLevel0TerrainNonGroupTouchCountFromSlots(point, nodeKeys, vertexSlotsByPointKey);
                    pointRecords.push({
                        point,
                        nonGroupCount,
                        baseKeep: nonGroupCount === requiredNonGroupCount
                    });
                }
                const forcedPointsByRunStart = new Map();
                const isForcedCandidate = (record) => !!(
                    record &&
                    record.baseKeep === false &&
                    record.nonGroupCount === 1
                );
                const addForcedRunPoint = (runStart, runLength) => {
                    if (runLength < 3) return;
                    const count = pointRecords.length;
                    let xSum = 0;
                    let ySum = 0;
                    for (let r = 0; r < runLength; r++) {
                        const point = pointRecords[(runStart + r) % count].point;
                        xSum += Number(point.x);
                        ySum += Number(point.y);
                    }
                    forcedPointsByRunStart.set(runStart, {
                        x: xSum / runLength,
                        y: ySum / runLength
                    });
                };
                if (pointRecords.length >= 3) {
                    const count = pointRecords.length;
                    const startIndex = pointRecords.findIndex(record => !isForcedCandidate(record));
                    if (startIndex >= 0) {
                        let runStart = -1;
                        let runLength = 0;
                        for (let step = 1; step <= count; step++) {
                            const index = (startIndex + step) % count;
                            if (isForcedCandidate(pointRecords[index])) {
                                if (runStart < 0) runStart = index;
                                runLength += 1;
                            } else if (runStart >= 0) {
                                if (runLength >= 3) {
                                    addForcedRunPoint(runStart, runLength);
                                }
                                runStart = -1;
                                runLength = 0;
                            }
                        }
                    }
                }
                const kept = [];
                for (let p = 0; p < pointRecords.length; p++) {
                    if (forcedPointsByRunStart.has(p)) {
                        kept.push(forcedPointsByRunStart.get(p));
                    }
                    if (pointRecords[p].baseKeep) {
                        kept.push(pointRecords[p].point);
                    }
                }
                const simplified = this.simplifyLevel0TerrainLoop(kept);
                if (simplified.length < 3) {
                    throw new Error(`terrain region smoothing produced fewer than three vertices for ${raw.isHole ? "hole" : "outer"} loop`);
                }
                out.push({
                    points: simplified,
                    isHole: raw.isHole,
                    area: this.getLevel0TerrainLoopArea(simplified)
                });
            }
            return out;
        }

        getLevel0TerrainLoopsBounds(loops) {
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < loops.length; i++) {
                const points = loops[i] && Array.isArray(loops[i].points) ? loops[i].points : [];
                for (let p = 0; p < points.length; p++) {
                    minX = Math.min(minX, Number(points[p].x));
                    minY = Math.min(minY, Number(points[p].y));
                    maxX = Math.max(maxX, Number(points[p].x));
                    maxY = Math.max(maxY, Number(points[p].y));
                }
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) return null;
            return { minX, minY, maxX, maxY };
        }

        getLevel0OpaqueTerrainMaterialSource(texturePath, source) {
            if (typeof texturePath !== "string" || texturePath.length === 0) {
                throw new Error("terrain material opacity bake requires a texture path");
            }
            if (!source) {
                throw new Error("terrain material opacity bake requires an image source");
            }
            const sourceWidth = Number(source.naturalWidth || source.videoWidth || source.width) || 0;
            const sourceHeight = Number(source.naturalHeight || source.videoHeight || source.height) || 0;
            if (!(sourceWidth > 0) || !(sourceHeight > 0)) {
                throw new Error("terrain material opacity bake requires positive source dimensions");
            }
            const cropWidth = Math.max(1, Math.floor(sourceWidth * 0.5));
            const cropHeight = Math.max(1, Math.floor(sourceHeight * 0.5));
            const cropX = Math.max(0, Math.floor((sourceWidth - cropWidth) * 0.5));
            const cropY = Math.max(0, Math.floor((sourceHeight - cropHeight) * 0.5));
            if (!this.level0TerrainOpaqueMaterialSourceByPath) {
                this.level0TerrainOpaqueMaterialSourceByPath = new Map();
            }
            const cacheKey = `${texturePath}:${sourceWidth}x${sourceHeight}:center:${cropX},${cropY},${cropWidth},${cropHeight}`;
            const cached = this.level0TerrainOpaqueMaterialSourceByPath.get(cacheKey);
            if (cached && cached.source) return cached;
            if (typeof document === "undefined" || !document || typeof document.createElement !== "function") {
                throw new Error("terrain material opacity bake requires canvas creation support");
            }
            const scratch = document.createElement("canvas");
            scratch.width = cropWidth;
            scratch.height = cropHeight;
            const scratchCtx = scratch.getContext("2d", { willReadFrequently: true });
            if (!scratchCtx) {
                throw new Error("terrain material opacity bake requires a readable canvas context");
            }
            scratchCtx.clearRect(0, 0, cropWidth, cropHeight);
            scratchCtx.drawImage(source, cropX, cropY, cropWidth, cropHeight, 0, 0, cropWidth, cropHeight);
            let imageData = null;
            try {
                imageData = scratchCtx.getImageData(0, 0, cropWidth, cropHeight);
            } catch (err) {
                throw new Error(`terrain material opacity bake could not read source pixels: ${err && err.message ? err.message : err}`);
            }
            const data = imageData.data;
            let rSum = 0;
            let gSum = 0;
            let bSum = 0;
            let weightSum = 0;
            for (let i = 0; i < data.length; i += 4) {
                const a = data[i + 3];
                if (a <= 0) continue;
                rSum += data[i] * a;
                gSum += data[i + 1] * a;
                bSum += data[i + 2] * a;
                weightSum += a;
            }
            const fillR = weightSum > 0 ? Math.round(rSum / weightSum) : 255;
            const fillG = weightSum > 0 ? Math.round(gSum / weightSum) : 255;
            const fillB = weightSum > 0 ? Math.round(bSum / weightSum) : 255;
            const canvas = document.createElement("canvas");
            canvas.width = cropWidth;
            canvas.height = cropHeight;
            const ctx = canvas.getContext("2d");
            if (!ctx) {
                throw new Error("terrain material opacity bake requires a material canvas context");
            }
            ctx.fillStyle = `rgb(${fillR},${fillG},${fillB})`;
            ctx.fillRect(0, 0, cropWidth, cropHeight);
            ctx.drawImage(source, cropX, cropY, cropWidth, cropHeight, 0, 0, cropWidth, cropHeight);
            const entry = { source: canvas, width: cropWidth, height: cropHeight };
            this.level0TerrainOpaqueMaterialSourceByPath.set(cacheKey, entry);
            return entry;
        }

        appendLevel0TerrainLoopsPath(ctx2d, loops, offsetX = 0, offsetY = 0) {
            ctx2d.beginPath();
            for (let i = 0; i < loops.length; i++) {
                const points = loops[i] && Array.isArray(loops[i].points) ? loops[i].points : [];
                if (points.length < 3) {
                    throw new Error("Cannot draw terrain region loop with fewer than three vertices.");
                }
                for (let p = 0; p < points.length; p++) {
                    const x = Number(points[p].x) - offsetX;
                    const y = Number(points[p].y) - offsetY;
                    if (p === 0) ctx2d.moveTo(x, y);
                    else ctx2d.lineTo(x, y);
                }
                ctx2d.closePath();
            }
        }

        distanceToLevel0TerrainLoops(px, py, loops) {
            if (!Array.isArray(loops) || loops.length === 0) return Infinity;
            let bestSq = Infinity;
            for (let i = 0; i < loops.length; i++) {
                const points = loops[i] && Array.isArray(loops[i].points) ? loops[i].points : [];
                if (points.length < 3) continue;
                for (let p = 0; p < points.length; p++) {
                    const a = points[p];
                    const b = points[(p + 1) % points.length];
                    const closest = closestFloorVisualSegmentPoint2D(px, py, a.x, a.y, b.x, b.y);
                    if (closest && Number.isFinite(closest.distanceSq)) {
                        bestSq = Math.min(bestSq, closest.distanceSq);
                    }
                }
            }
            return Number.isFinite(bestSq) ? Math.sqrt(Math.max(0, bestSq)) : Infinity;
        }

        applyLevel0TerrainDistanceFadeToCanvas(terrainCtx, loops, fadePx) {
            const canvas = terrainCtx && terrainCtx.canvas;
            const width = Math.max(0, Math.floor(Number(canvas && canvas.width) || 0));
            const height = Math.max(0, Math.floor(Number(canvas && canvas.height) || 0));
            if (!(width > 0) || !(height > 0)) {
                throw new Error("Cannot apply terrain distance fade without a terrain canvas.");
            }
            const safeFadePx = Math.max(0.0001, Number(fadePx) || 0);
            let imageData = null;
            try {
                imageData = terrainCtx.getImageData(0, 0, width, height);
            } catch (err) {
                throw new Error(`Cannot apply terrain distance fade because canvas pixels are unreadable: ${err && err.message ? err.message : err}`);
            }
            const data = imageData.data;
            for (let y = 0; y < height; y++) {
                const py = y + 0.5;
                for (let x = 0; x < width; x++) {
                    const idx = ((y * width) + x) * 4;
                    const alpha = data[idx + 3];
                    if (alpha <= 0) continue;
                    const px = x + 0.5;
                    const inside = this.pointInLevel0TerrainRegion(px, py, loops);
                    const distance = this.distanceToLevel0TerrainLoops(px, py, loops);
                    if (!Number.isFinite(distance)) {
                        data[idx + 3] = 0;
                        continue;
                    }
                    const signedDistance = inside ? distance : -distance;
                    const fadeAlpha = Math.max(0, Math.min(1, (signedDistance + safeFadePx) / (safeFadePx * 2)));
                    data[idx + 3] = Math.round(alpha * fadeAlpha);
                }
            }
            terrainCtx.putImageData(imageData, 0, 0);
        }

        applyLevel0TerrainHardMaskToCanvas(terrainCtx, loops, expandPx = 0) {
            const canvas = terrainCtx && terrainCtx.canvas;
            const width = Math.max(0, Math.floor(Number(canvas && canvas.width) || 0));
            const height = Math.max(0, Math.floor(Number(canvas && canvas.height) || 0));
            if (!(width > 0) || !(height > 0)) {
                throw new Error("Cannot apply terrain hard mask without a terrain canvas.");
            }
            const safeExpandPx = Math.max(0, Number(expandPx) || 0);
            if (typeof document === "undefined" || !document || typeof document.createElement !== "function") {
                throw new Error("Cannot apply terrain hard mask without canvas creation support.");
            }
            const maskCanvas = document.createElement("canvas");
            maskCanvas.width = width;
            maskCanvas.height = height;
            const maskCtx = maskCanvas.getContext("2d");
            if (!maskCtx) {
                throw new Error("Cannot apply terrain hard mask without a mask canvas context.");
            }
            maskCtx.fillStyle = "#fff";
            this.appendLevel0TerrainLoopsPath(maskCtx, loops);
            maskCtx.fill("evenodd");
            if (safeExpandPx > 0) {
                maskCtx.strokeStyle = "#fff";
                maskCtx.lineCap = "round";
                maskCtx.lineJoin = "round";
                maskCtx.lineWidth = safeExpandPx * 2;
                maskCtx.stroke();
            }
            terrainCtx.save();
            terrainCtx.globalCompositeOperation = "destination-in";
            terrainCtx.drawImage(maskCanvas, 0, 0);
            terrainCtx.restore();
        }

        usesLevel0TerrainHardEdge(typeName) {
            return typeName === "water";
        }

        getLevel0TerrainDrawOrder(typeName) {
            const type = typeof typeName === "string" ? typeName.trim().toLowerCase() : "";
            if (type === "grass") return 0;
            if (type === "mud") return 1;
            if (type === "mowedgrass") return 2;
            if (type === "desert" || type === "sand") return 3;
            if (type === "water") return 4;
            throw new Error(`unknown level 0 terrain draw order for "${typeName}"`);
        }

        getLevel0TerrainDepthBiasOffset(typeName) {
            return this.getLevel0TerrainDrawOrder(typeName) * FLOOR_LEVEL0_TERRAIN_DEPTH_BIAS_STEP_UNITS;
        }

        sortLevel0TerrainGroupsForDraw(groups) {
            if (!Array.isArray(groups)) return groups;
            groups.sort((a, b) => {
                const orderA = this.getLevel0TerrainDrawOrder(a && a.type);
                const orderB = this.getLevel0TerrainDrawOrder(b && b.type);
                if (orderA !== orderB) return orderA - orderB;
                return String(a && a.type || "").localeCompare(String(b && b.type || ""));
            });
            return groups;
        }

        sortLevel0TerrainFloorVisualEntriesForDraw(entries) {
            if (!Array.isArray(entries)) return entries;
            entries.sort((a, b) => {
                const orderA = this.getLevel0TerrainDrawOrder(a && a.terrainType);
                const orderB = this.getLevel0TerrainDrawOrder(b && b.terrainType);
                if (orderA !== orderB) return orderA - orderB;
                return String(a && a.key || "").localeCompare(String(b && b.key || ""));
            });
            return entries;
        }

        cloneCachedLevel0TerrainPolygonEntry(template, baseZ, alpha, nowMs) {
            if (!template) return null;
            return {
                ...template,
                baseZ,
                alpha,
                animationNowMs: nowMs
            };
        }

        getLevel0TerrainMaterialRepeat(map, typeName) {
            const terrainScale = this.getLevel0TerrainMaterialScaleForType(map, typeName);
            const tileWorldW = Math.max(
                0.0001,
                (Number.isFinite(map && map.hexWidth) ? Number(map.hexWidth) : (1 / 0.866)) *
                    GROUND_TILE_OVERLAP_SCALE *
                    terrainScale
            );
            const tileWorldH = Math.max(
                0.0001,
                (Number.isFinite(map && map.hexHeight) ? Number(map.hexHeight) : 1) *
                    GROUND_TILE_OVERLAP_SCALE *
                    terrainScale
            );
            return {
                x: 1 / tileWorldW,
                y: 1 / tileWorldH
            };
        }

        getLevel0TerrainPolygonGroupsFromAsset(map, asset) {
            const polygons = Array.isArray(asset && asset.terrainPolygons) ? asset.terrainPolygons : [];
            if (polygons.length === 0) return [];
            return polygons.map((polygon, index) => {
                const type = polygon && typeof polygon.type === "string" && polygon.type.length > 0
                    ? polygon.type
                    : "";
                if (!type) {
                    throw new Error(`terrain polygon ${index} is missing terrain type`);
                }
                const points = this.simplifyLevel0TerrainLoop(polygon.points);
                if (points.length < 3) {
                    throw new Error(`terrain polygon ${index} for ${type} has fewer than three points`);
                }
                const loops = [{
                    points,
                    isHole: false,
                    area: this.getLevel0TerrainLoopArea(points)
                }];
                const holes = Array.isArray(polygon.holes) ? polygon.holes : [];
                for (let h = 0; h < holes.length; h++) {
                    const holePoints = this.simplifyLevel0TerrainLoop(holes[h]);
                    if (holePoints.length < 3) {
                        throw new Error(`terrain polygon ${index} for ${type} has a hole ${h} with fewer than three points`);
                    }
                    loops.push({
                        points: holePoints,
                        isHole: true,
                        area: this.getLevel0TerrainLoopArea(holePoints)
                    });
                }
                return {
                    type,
                    materialPath: this.getLevel0TerrainMaterialPathForType(map, type),
                    materialScale: this.getLevel0TerrainMaterialScaleForType(map, type),
                    loops
                };
            });
        }

        getLevel0TerrainRegionPolygonsFromLoops(loops) {
            const outers = [];
            const holes = [];
            for (let i = 0; i < loops.length; i++) {
                const loop = loops[i];
                const points = this.simplifyLevel0TerrainLoop(loop && loop.points);
                if (points.length < 3) continue;
                if (loop && loop.isHole) {
                    holes.push(points);
                } else {
                    outers.push({ outer: points, holes: [] });
                }
            }
            for (let h = 0; h < holes.length; h++) {
                const hole = holes[h];
                const probe = hole[0];
                let owner = null;
                for (let o = 0; o < outers.length; o++) {
                    if (pointInFloorVisualPolygon2D(probe.x, probe.y, outers[o].outer, { epsilon: 0.0001 })) {
                        owner = outers[o];
                        break;
                    }
                }
                if (!owner) {
                    throw new Error("terrain region hole could not be assigned to an outer water polygon");
                }
                owner.holes.push(hole);
            }
            return outers;
        }

        drawLevel0TerrainRegionToCanvas(ctx2d, map, group, loops, bounds, scale, sectionKey = "", asset = null) {
            if (!ctx2d || !map || !group || !Array.isArray(loops) || loops.length === 0 || !bounds || !Number.isFinite(scale)) {
                return { baked: 0, pending: false };
            }
            const materialPath = typeof group.materialPath === "string" && group.materialPath.length > 0 ? group.materialPath : "";
            if (!materialPath) {
                throw new Error(`Cannot bake ${group.type || "terrain"} region without a polygon material path.`);
            }
            const texture = PIXI.Texture.from(materialPath);
            const baseTexture = texture && texture.baseTexture ? texture.baseTexture : null;
            if (baseTexture && baseTexture.valid !== true) {
                this.markLevel0GroundSurfacePendingTexture(sectionKey, baseTexture, asset);
                return { baked: 0, pending: true };
            }
            const source = this.getLevel0BakeImageSource(texture);
            if (!source) return { baked: 0, pending: true };
            const materialSource = source;
            const sourceWidth = Number(source.naturalWidth || source.videoWidth || source.width) || 0;
            const sourceHeight = Number(source.naturalHeight || source.videoHeight || source.height) || 0;
            if (!(sourceWidth > 0) || !(sourceHeight > 0)) return { baked: 0, pending: true };
            const materialScale = Number(group.materialScale);
            if (!Number.isFinite(materialScale) || !(materialScale > 0)) {
                throw new Error(`Cannot bake ${group.type || "terrain"} region with an invalid polygon material scale.`);
            }
            const loopBounds = this.getLevel0TerrainLoopsBounds(loops);
            if (!loopBounds) return { baked: 0, pending: false };
            const targetCanvas = ctx2d.canvas || null;
            const targetWidth = Math.max(1, Math.floor(Number(targetCanvas && targetCanvas.width) || 0));
            const targetHeight = Math.max(1, Math.floor(Number(targetCanvas && targetCanvas.height) || 0));
            if (!(targetWidth > 0) || !(targetHeight > 0)) {
                throw new Error("Cannot bake terrain region without a target canvas.");
            }
            if (typeof document === "undefined" || !document || typeof document.createElement !== "function") {
                throw new Error("Cannot bake terrain region alpha fade without canvas creation support.");
            }
            const hardEdge = this.usesLevel0TerrainHardEdge(group.type);
            const terrainFadePx = this.getLevel0TerrainEdgeFadePx(scale);
            const hardMaskExpandPx = hardEdge ? terrainFadePx : 0;
            const fadePx = hardEdge ? 0 : terrainFadePx;
            const edgePadPx = hardEdge
                ? Math.max(2, Math.ceil(hardMaskExpandPx))
                : Math.max(2, Math.ceil(fadePx * 2));
            const clipX = Math.max(0, Math.floor((loopBounds.minX - bounds.minX) * scale - edgePadPx));
            const clipY = Math.max(0, Math.floor((loopBounds.minY - bounds.minY) * scale - edgePadPx));
            const clipMaxX = Math.min(targetWidth, Math.ceil((loopBounds.maxX - bounds.minX) * scale + edgePadPx));
            const clipMaxY = Math.min(targetHeight, Math.ceil((loopBounds.maxY - bounds.minY) * scale + edgePadPx));
            const clipW = Math.max(0, clipMaxX - clipX);
            const clipH = Math.max(0, clipMaxY - clipY);
            if (!(clipW > 0) || !(clipH > 0)) return { baked: 0, pending: false };

            const localLoops = loops.map(loop => ({
                ...loop,
                points: loop.points.map(point => ({
                    x: (Number(point.x) - bounds.minX) * scale - clipX,
                    y: (Number(point.y) - bounds.minY) * scale - clipY
                }))
            }));
            const terrainCanvas = document.createElement("canvas");
            terrainCanvas.width = clipW;
            terrainCanvas.height = clipH;
            const terrainCtx = terrainCanvas.getContext("2d");
            if (!terrainCtx) {
                throw new Error("Cannot bake terrain region without a temporary canvas context.");
            }
            terrainCtx.save();
            const tileWorldW = Math.max(0.0001, (Number(bounds.tileWorldW) || 1) * materialScale);
            const tileWorldH = Math.max(0.0001, (Number(bounds.tileWorldH) || 1) * materialScale);
            const tilePxW = Math.max(1, tileWorldW * scale);
            const tilePxH = Math.max(1, tileWorldH * scale);
            const tileDrawPxW = tilePxW + 1;
            const tileDrawPxH = tilePxH + 1;
            const drawMinWorldX = Number(bounds.minX) + clipX / scale;
            const drawMinWorldY = Number(bounds.minY) + clipY / scale;
            const drawMaxWorldX = Number(bounds.minX) + (clipX + clipW) / scale;
            const drawMaxWorldY = Number(bounds.minY) + (clipY + clipH) / scale;
            const startWorldX = Math.floor(drawMinWorldX / tileWorldW) * tileWorldW;
            const endWorldX = Math.ceil(drawMaxWorldX / tileWorldW) * tileWorldW;
            const startWorldY = Math.floor(drawMinWorldY / tileWorldH) * tileWorldH;
            const endWorldY = Math.ceil(drawMaxWorldY / tileWorldH) * tileWorldH;
            for (let wx = startWorldX; wx <= endWorldX; wx += tileWorldW) {
                const dx = (wx - bounds.minX) * scale - clipX;
                for (let wy = startWorldY; wy <= endWorldY; wy += tileWorldH) {
                    const dy = (wy - bounds.minY) * scale - clipY;
                    terrainCtx.drawImage(materialSource, 0, 0, sourceWidth, sourceHeight, dx, dy, tileDrawPxW, tileDrawPxH);
                }
            }
            if (hardEdge) {
                this.applyLevel0TerrainHardMaskToCanvas(terrainCtx, localLoops, hardMaskExpandPx);
            } else {
                this.applyLevel0TerrainDistanceFadeToCanvas(terrainCtx, localLoops, fadePx);
            }
            terrainCtx.restore();

            ctx2d.save();
            ctx2d.globalAlpha = 1;
            ctx2d.globalCompositeOperation = "source-over";
            ctx2d.drawImage(terrainCanvas, clipX, clipY);
            ctx2d.restore();
            return { baked: 1, pending: false };
        }

        drawLevel0GroundTerrainToCanvas(ctx2d, map, nodes, bounds, scale, sectionKey = "", asset = null) {
            // Legacy baked terrain helper. Current painted terrain polygons are
            // drawn live by collectLevel0TerrainPolygonFloorVisualEntries().
            if (!ctx2d || !map || !Array.isArray(nodes) || !bounds || !Number.isFinite(scale)) {
                return { baked: 0, pending: false };
            }
            if (typeof map.getGroundTerrainTextureIdForType !== "function") {
                throw new Error("level 0 ground terrain baking requires map.getGroundTerrainTextureIdForType");
            }
            let baked = 0;
            let pending = false;
            for (let i = 0; i < nodes.length; i++) {
                const node = nodes[i];
                if (!node) continue;
                const grassId = map.getGroundTerrainTextureIdForType("grass", node.xindex, node.yindex);
                if (this.drawLevel0GroundTileToCanvas(ctx2d, map, node, bounds, scale, sectionKey, asset, {
                    textureIdOverride: grassId
                })) {
                    baked += 1;
                } else {
                    pending = true;
                }
            }
            const assetHasTerrainPolygons = !!(asset && Array.isArray(asset.terrainPolygons));
            const assetGroups = this.getLevel0TerrainPolygonGroupsFromAsset(map, asset);
            const groups = assetHasTerrainPolygons
                ? assetGroups
                : this.collectLevel0TerrainGroups(map, nodes);
            this.sortLevel0TerrainGroupsForDraw(groups);
            for (let i = 0; i < groups.length; i++) {
                if (groups[i] && groups[i].type === "water") continue;
                const loops = Array.isArray(groups[i].loops) ? groups[i].loops : this.buildLevel0TerrainRegionLoops(map, groups[i]);
                if (loops.length === 0) continue;
                const regionBake = this.drawLevel0TerrainRegionToCanvas(ctx2d, map, groups[i], loops, bounds, scale, sectionKey, asset);
                baked += regionBake && Number.isFinite(regionBake.baked) ? regionBake.baked : 0;
                pending = pending || !!(regionBake && regionBake.pending);
            }
            return { baked, pending };
        }

        drawLevel0GroundTileToCanvas(ctx2d, map, node, bounds, scale, sectionKey = "", asset = null, options = null) {
            if (!ctx2d || !map || !node || !bounds || !Number.isFinite(scale)) return false;
            if (typeof map.getGroundTextureForNode !== "function") {
                throw new Error("level 0 ground baking requires map.getGroundTextureForNode");
            }
            const textureIdOverride = options && Number.isFinite(options.textureIdOverride)
                ? Math.floor(Number(options.textureIdOverride))
                : null;
            if (textureIdOverride !== null && typeof map.getGroundTextureForTextureId !== "function") {
                throw new Error("level 0 ground baking with texture override requires map.getGroundTextureForTextureId");
            }
            const texture = textureIdOverride !== null
                ? map.getGroundTextureForTextureId(textureIdOverride, node.xindex, node.yindex)
                : map.getGroundTextureForNode(node);
            if (!isRenderablePixiTexture(texture)) {
                const baseTexture = texture && texture.baseTexture ? texture.baseTexture : null;
                if (baseTexture && baseTexture.valid !== true) {
                    this.markLevel0GroundSurfacePendingTexture(sectionKey, baseTexture, asset);
                }
                return false;
            }
            const source = this.getLevel0BakeImageSource(texture);
            if (!source) return false;
            const width = bounds.tileWorldW * scale;
            const height = bounds.tileWorldH * scale;
            const x = (Number(node.x) - bounds.minX) * scale - width * 0.5;
            const y = (Number(node.y) - bounds.minY) * scale - height * 0.5;
            ctx2d.drawImage(source, x, y, width, height);
            return true;
        }

        drawRoadToLevel0GroundSurfaceCanvas(ctx2d, road, node, bounds, scale, sectionKey = "", asset = null, options = null) {
            if (!ctx2d || !road || road.gone || road.type !== "road" || !bounds || !Number.isFinite(scale)) {
                return { baked: 0, pending: false };
            }
            const RoadClass = typeof global.Road !== "undefined" ? global.Road : null;
            const fillTexture = RoadClass && typeof RoadClass._getFillTexture === "function"
                ? RoadClass._getFillTexture(road.fillTexturePath)
                : null;
            const fillBase = fillTexture && fillTexture.baseTexture ? fillTexture.baseTexture : null;
            if (fillBase && fillBase.valid !== true) {
                this.markLevel0GroundSurfacePendingTexture(sectionKey, fillBase, asset);
                return { baked: 0, pending: true };
            }
            const worldX = Number.isFinite(road.x) ? road.x : (Number.isFinite(node && node.x) ? node.x : 0);
            const worldY = Number.isFinite(road.y) ? road.y : (Number.isFinite(node && node.y) ? node.y : 0);
            const refreshTexture = !options || options.refreshTexture !== false;
            if (refreshTexture && typeof road.updateTexture === "function") {
                road.updateTexture(null, road.fillTexturePath);
            }
            const texture = road.pixiSprite && isRenderablePixiTexture(road.pixiSprite.texture)
                ? road.pixiSprite.texture
                : null;
            const source = this.getLevel0BakeImageSource(texture);
            if (!source) return { baked: 0, pending: true };
            const width = (Number(road.width) || 1) * 1.1547 * scale;
            const height = (Number(road.height) || 1) * scale;
            const x = (worldX - bounds.minX) * scale - width * 0.5;
            const y = (worldY - bounds.minY) * scale - height * 0.5;
            const previousAlpha = ctx2d.globalAlpha;
            ctx2d.globalAlpha = Number.isFinite(road.alpha) ? road.alpha : 1;
            ctx2d.drawImage(source, x, y, width, height);
            ctx2d.globalAlpha = previousAlpha;
            return { baked: 1, pending: false };
        }

        drawRoadPathToLevel0GroundSurfaceCanvas(ctx2d, roadPath, node, bounds, scale, sectionKey = "", asset = null, options = null) {
            if (!ctx2d || !roadPath || roadPath.gone || roadPath.type !== "roadPath" || !bounds || !Number.isFinite(scale)) {
                return { baked: 0, pending: false };
            }
            const fallbackNode = node || (typeof roadPath.getNode === "function" ? roadPath.getNode() : roadPath.node);
            const roadLayer = this.getLayerIndexForObject(roadPath, this.getLayerIndexForNode(fallbackNode || roadPath));
            if (roadLayer !== 0) return { baked: 0, pending: false };
            const geometry = roadPath.generatedGeometry || null;
            const outline = geometry && Array.isArray(geometry.outline) ? geometry.outline : roadPath.outlinePolygon;
            if (!Array.isArray(outline) || outline.length < 3) {
                throw new Error("Cannot bake road path without outline geometry.");
            }
            const RoadClass = typeof global.Road !== "undefined" ? global.Road : null;
            if (
                !RoadClass ||
                typeof RoadClass._getFillTexture !== "function" ||
                !Number.isFinite(RoadClass._edgeFadePx) ||
                !Number.isFinite(RoadClass._pixelsPerWorldUnit) ||
                !(RoadClass._pixelsPerWorldUnit > 0)
            ) {
                throw new Error("Cannot bake road path without road texture and edge fade metrics.");
            }
            const fillTexturePath = (typeof roadPath.fillTexturePath === "string" && roadPath.fillTexturePath.length > 0)
                ? roadPath.fillTexturePath
                : RoadClass._defaultFillTexturePath;
            const fillTexture = RoadClass._getFillTexture(fillTexturePath);
            const fillBase = fillTexture && fillTexture.baseTexture ? fillTexture.baseTexture : null;
            if (fillBase && fillBase.valid !== true) {
                this.markLevel0GroundSurfacePendingTexture(sectionKey, fillBase, asset);
                return { baked: 0, pending: true };
            }
            const source = this.getLevel0BakeImageSource(fillTexture);
            if (!source) return { baked: 0, pending: true };
            const textureRepeat = this.getFloorVisualTextureRepeat(fillTexturePath);
            const repeatX = textureRepeat && Number.isFinite(textureRepeat.x) ? Math.max(0.0001, Number(textureRepeat.x)) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const repeatY = textureRepeat && Number.isFinite(textureRepeat.y) ? Math.max(0.0001, Number(textureRepeat.y)) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const tileWorldW = 1 / repeatX;
            const tileWorldH = 1 / repeatY;
            const tilePxW = Math.max(1, tileWorldW * scale);
            const tilePxH = Math.max(1, tileWorldH * scale);
            const fadePx = Math.max(0.0001, Number(RoadClass._edgeFadePx) / Number(RoadClass._pixelsPerWorldUnit)) * scale;
            const canvasPoints = outline.map((pt, index) => {
                const x = Number(pt && pt.x);
                const y = Number(pt && pt.y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) {
                    throw new Error(`Cannot bake road path with non-finite outline point ${index}.`);
                }
                return {
                    x: (x - bounds.minX) * scale,
                    y: (y - bounds.minY) * scale,
                    worldX: x,
                    worldY: y
                };
            });
            const pathBounds = this.getRoadPathVisualBounds(roadPath);
            const drawMinX = Math.max(Number(bounds.minX), pathBounds.minX);
            const drawMinY = Math.max(Number(bounds.minY), pathBounds.minY);
            const drawMaxX = Math.min(Number(bounds.maxX), pathBounds.maxX);
            const drawMaxY = Math.min(Number(bounds.maxY), pathBounds.maxY);
            if (!(drawMaxX > drawMinX) || !(drawMaxY > drawMinY)) {
                return { baked: 0, pending: false };
            }
            const sourceWidth = Number(source.naturalWidth || source.videoWidth || source.width) || 0;
            const sourceHeight = Number(source.naturalHeight || source.videoHeight || source.height) || 0;
            if (!(sourceWidth > 0) || !(sourceHeight > 0)) return { baked: 0, pending: true };
            const pointInPolygon = (px, py, points) => {
                let inside = false;
                for (let i = 0, j = points.length - 1; i < points.length; j = i++) {
                    const xi = points[i].x;
                    const yi = points[i].y;
                    const xj = points[j].x;
                    const yj = points[j].y;
                    const intersects = ((yi > py) !== (yj > py)) &&
                        (px < ((xj - xi) * (py - yi)) / ((yj - yi) || 1e-7) + xi);
                    if (intersects) inside = !inside;
                }
                return inside;
            };
            const targetCanvas = ctx2d.canvas || null;
            const targetWidth = Math.max(1, Math.floor(Number(targetCanvas && targetCanvas.width) || 0));
            const targetHeight = Math.max(1, Math.floor(Number(targetCanvas && targetCanvas.height) || 0));
            if (!(targetWidth > 0) || !(targetHeight > 0)) {
                throw new Error("Cannot bake road path without a target canvas.");
            }
            if (typeof document === "undefined" || !document || typeof document.createElement !== "function") {
                throw new Error("Cannot bake road path alpha fade without canvas creation support.");
            }
            const fadePadPx = Math.max(2, Math.ceil(fadePx * 2));
            const clipX = Math.max(0, Math.floor((drawMinX - bounds.minX) * scale - fadePadPx));
            const clipY = Math.max(0, Math.floor((drawMinY - bounds.minY) * scale - fadePadPx));
            const clipMaxX = Math.min(targetWidth, Math.ceil((drawMaxX - bounds.minX) * scale + fadePadPx));
            const clipMaxY = Math.min(targetHeight, Math.ceil((drawMaxY - bounds.minY) * scale + fadePadPx));
            const clipW = Math.max(0, clipMaxX - clipX);
            const clipH = Math.max(0, clipMaxY - clipY);
            if (!(clipW > 0) || !(clipH > 0)) return { baked: 0, pending: false };
            const roadCanvas = document.createElement("canvas");
            roadCanvas.width = clipW;
            roadCanvas.height = clipH;
            const roadCtx = roadCanvas.getContext("2d");
            if (!roadCtx) {
                throw new Error("Cannot bake road path without a temporary road canvas context.");
            }
            const localPoints = canvasPoints.map(pt => ({
                x: pt.x - clipX,
                y: pt.y - clipY
            }));
            const pathPointCount = geometry && Array.isArray(geometry.points)
                ? geometry.points.length
                : (Array.isArray(roadPath.pathPoints) ? roadPath.pathPoints.length : 0);
            if (!(pathPointCount >= 2) || localPoints.length !== pathPointCount * 2) {
                throw new Error("Cannot bake road path endpoint continuity without matching outline and path point geometry.");
            }
            const endpointContinuity = options && options.endpointContinuity ? options.endpointContinuity : null;
            const suppressStartCapFade = !!(endpointContinuity && endpointContinuity.startConnected);
            const suppressEndCapFade = !!(endpointContinuity && endpointContinuity.endConnected);
            const endCapEdgeIndex = pathPointCount - 1;
            const startCapEdgeIndex = localPoints.length - 1;
            roadCtx.save();
            roadCtx.beginPath();
            for (let i = 0; i < localPoints.length; i++) {
                const pt = localPoints[i];
                if (i === 0) roadCtx.moveTo(pt.x, pt.y);
                else roadCtx.lineTo(pt.x, pt.y);
            }
            roadCtx.closePath();
            roadCtx.clip();
            roadCtx.globalAlpha = Number.isFinite(roadPath.alpha) ? Math.max(0, Math.min(1, Number(roadPath.alpha))) : 1;
            const startWorldX = Math.floor(drawMinX / tileWorldW) * tileWorldW;
            const endWorldX = Math.ceil(drawMaxX / tileWorldW) * tileWorldW;
            const startWorldY = Math.floor(drawMinY / tileWorldH) * tileWorldH;
            const endWorldY = Math.ceil(drawMaxY / tileWorldH) * tileWorldH;
            for (let wx = startWorldX; wx <= endWorldX; wx += tileWorldW) {
                const dx = (wx - bounds.minX) * scale - clipX;
                for (let wy = startWorldY; wy <= endWorldY; wy += tileWorldH) {
                    const dy = (wy - bounds.minY) * scale - clipY;
                    roadCtx.drawImage(source, 0, 0, sourceWidth, sourceHeight, dx, dy, tilePxW, tilePxH);
                }
            }
            roadCtx.globalAlpha = 1;
            roadCtx.globalCompositeOperation = "destination-out";
            const centroid = localPoints.reduce((acc, pt) => {
                acc.x += pt.x;
                acc.y += pt.y;
                return acc;
            }, { x: 0, y: 0 });
            centroid.x /= localPoints.length;
            centroid.y /= localPoints.length;
            for (let i = 0; i < localPoints.length; i++) {
                if (
                    (suppressEndCapFade && i === endCapEdgeIndex) ||
                    (suppressStartCapFade && i === startCapEdgeIndex)
                ) {
                    continue;
                }
                const p0 = localPoints[i];
                const p1 = localPoints[(i + 1) % localPoints.length];
                const ex = p1.x - p0.x;
                const ey = p1.y - p0.y;
                const edgeLen = Math.hypot(ex, ey);
                if (!(edgeLen > 0.0001)) continue;
                const tx = ex / edgeLen;
                const ty = ey / edgeLen;
                let nx = -ey / edgeLen;
                let ny = ex / edgeLen;
                const mx = (p0.x + p1.x) * 0.5;
                const my = (p0.y + p1.y) * 0.5;
                const probe = Math.max(2, fadePx * 0.35);
                const scoreA = pointInPolygon(mx + nx * probe, my + ny * probe, localPoints) ? 1 : 0;
                const scoreB = pointInPolygon(mx - nx * probe, my - ny * probe, localPoints) ? 1 : 0;
                if (scoreB > scoreA) {
                    nx = -nx;
                    ny = -ny;
                } else if (scoreA === scoreB && ((centroid.x - mx) * nx + (centroid.y - my) * ny) < 0) {
                    nx = -nx;
                    ny = -ny;
                }
                const ext = fadePx * 1.5;
                const a0 = { x: p0.x - tx * ext, y: p0.y - ty * ext };
                const a1 = { x: p1.x + tx * ext, y: p1.y + ty * ext };
                const b0 = { x: a0.x + nx * fadePx, y: a0.y + ny * fadePx };
                const b1 = { x: a1.x + nx * fadePx, y: a1.y + ny * fadePx };
                const grad = roadCtx.createLinearGradient(mx, my, mx + nx * fadePx, my + ny * fadePx);
                grad.addColorStop(0, "rgba(0,0,0,1)");
                grad.addColorStop(1, "rgba(0,0,0,0)");
                roadCtx.fillStyle = grad;
                roadCtx.beginPath();
                roadCtx.moveTo(a0.x, a0.y);
                roadCtx.lineTo(a1.x, a1.y);
                roadCtx.lineTo(b1.x, b1.y);
                roadCtx.lineTo(b0.x, b0.y);
                roadCtx.closePath();
                roadCtx.fill();
            }
            roadCtx.restore();

            ctx2d.save();
            ctx2d.globalAlpha = 1;
            ctx2d.globalCompositeOperation = "source-over";
            ctx2d.drawImage(roadCanvas, clipX, clipY);
            ctx2d.restore();
            return { baked: 1, pending: false };
        }

        addRoadsToLevel0GroundSurfaceCanvas(ctx2d, nodes, bounds, scale, sectionKey = "", asset = null) {
            if (!ctx2d || !Array.isArray(nodes) || !bounds || typeof PIXI === "undefined") return { baked: 0, pending: false };
            const seenRoads = new Set();
            const seenRoadPaths = new Set();
            const roadPathContinuity = FLOOR_LEVEL0_BAKE_ROAD_PATHS
                ? this.buildRoadPathEndpointContinuity(this.collectRoadPathContinuityObjectsFromNodes(nodes))
                : null;
            let baked = 0;
            let pending = false;
            for (let i = 0; i < nodes.length; i++) {
                const node = nodes[i];
                if (!node || !Array.isArray(node.objects)) continue;
                for (let j = 0; j < node.objects.length; j++) {
                    const road = node.objects[j];
                    if (!road || road.gone) continue;
                    if (road.type === "road") {
                        if (seenRoads.has(road)) continue;
                        seenRoads.add(road);
                        const roadBake = this.drawRoadToLevel0GroundSurfaceCanvas(ctx2d, road, node, bounds, scale, sectionKey, asset, {
                            refreshTexture: true
                        });
                        baked += roadBake && Number.isFinite(roadBake.baked) ? roadBake.baked : 0;
                        pending = pending || !!(roadBake && roadBake.pending);
                        continue;
                    }
                    if (!FLOOR_LEVEL0_BAKE_ROAD_PATHS || road.type !== "roadPath" || seenRoadPaths.has(road)) continue;
                    seenRoadPaths.add(road);
                    const roadBake = this.drawRoadPathToLevel0GroundSurfaceCanvas(ctx2d, road, node, bounds, scale, sectionKey, asset, {
                        endpointContinuity: roadPathContinuity.get(road) || null
                    });
                    baked += roadBake && Number.isFinite(roadBake.baked) ? roadBake.baked : 0;
                    pending = pending || !!(roadBake && roadBake.pending);
                }
            }
            return { baked, pending };
        }

        getLevel0GroundSurfaceBounds(asset, map) {
            const tileCoordKeys = Array.isArray(asset && asset.tileCoordKeys) ? asset.tileCoordKeys : [];
            const tileWorldW = (Number.isFinite(map && map.hexWidth) ? map.hexWidth : (1 / 0.866))
                * GROUND_TILE_OVERLAP_SCALE;
            const tileWorldH = (Number.isFinite(map && map.hexHeight) ? map.hexHeight : 1)
                * GROUND_TILE_OVERLAP_SCALE;
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            for (let i = 0; i < tileCoordKeys.length; i++) {
                const [xRaw, yRaw] = String(tileCoordKeys[i]).split(",");
                const x = Number(xRaw);
                const y = Number(yRaw);
                if (!Number.isFinite(x) || !Number.isFinite(y)) continue;
                const wx = x * 0.866;
                const wy = y + (x % 2 === 0 ? 0.5 : 0);
                minX = Math.min(minX, wx - tileWorldW * 0.5);
                maxX = Math.max(maxX, wx + tileWorldW * 0.5);
                minY = Math.min(minY, wy - tileWorldH * 0.5);
                maxY = Math.max(maxY, wy + tileWorldH * 0.5);
            }
            if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
                return null;
            }
            return {
                minX,
                minY,
                maxX,
                maxY,
                width: Math.max(0.001, maxX - minX),
                height: Math.max(0.001, maxY - minY),
                tileWorldW,
                tileWorldH
            };
        }

        expandLevel0GroundBakeNodes(nodes) {
            const seed = Array.isArray(nodes) ? nodes : [];
            const out = [];
            const seen = new Set();
            const pushNode = (node) => {
                if (!node) return;
                const key = `${Number(node.xindex)},${Number(node.yindex)}`;
                if (seen.has(key)) return;
                seen.add(key);
                out.push(node);
            };
            for (let i = 0; i < seed.length; i++) {
                const node = seed[i];
                pushNode(node);
                const neighbors = Array.isArray(node && node.neighbors) ? node.neighbors : null;
                if (!neighbors) continue;
                for (let n = 0; n < neighbors.length; n++) {
                    const neighbor = neighbors[n];
                    if (!neighbor || neighbor._prototypeVoid === true) continue;
                    pushNode(neighbor);
                }
            }
            return out;
        }

        getLevel0GroundSurfaceBakeNodes(map, sectionKey) {
            const state = map && map._prototypeSectionState;
            if (!state || !(state.nodesBySectionKey instanceof Map)) return [];
            const sectionNodes = state.nodesBySectionKey.get(sectionKey) || [];
            if (!(this.level0GroundSurfaceBakeNodeCache instanceof Map)) {
                this.level0GroundSurfaceBakeNodeCache = new Map();
            }
            const loadedNodeCount = Array.isArray(state.loadedNodes) ? state.loadedNodes.length : 0;
            const graphSectionCount = state.nodesBySectionKey.size;
            const activeCenterKey = typeof state.activeCenterKey === "string" ? state.activeCenterKey : "";
            const signature = `${sectionNodes.length}:${loadedNodeCount}:${graphSectionCount}:${activeCenterKey}`;
            const cached = this.level0GroundSurfaceBakeNodeCache.get(sectionKey);
            if (cached && cached.signature === signature && Array.isArray(cached.nodes)) {
                return cached.nodes;
            }
            const nodes = this.expandLevel0GroundBakeNodes(sectionNodes);
            this.level0GroundSurfaceBakeNodeCache.set(sectionKey, { signature, nodes });
            return nodes;
        }

        getLevel0GroundSurfacePatchRects(asset, bounds) {
            const rects = Array.isArray(asset && asset._level0RoadSurfacePatchRects)
                ? asset._level0RoadSurfacePatchRects
                : [];
            if (!bounds || rects.length === 0) return [];
            const out = [];
            for (let i = 0; i < rects.length; i++) {
                const rect = rects[i];
                const minX = Math.max(bounds.minX, Number(rect && rect.minX));
                const minY = Math.max(bounds.minY, Number(rect && rect.minY));
                const maxX = Math.min(bounds.maxX, Number(rect && rect.maxX));
                const maxY = Math.min(bounds.maxY, Number(rect && rect.maxY));
                if (
                    Number.isFinite(minX) &&
                    Number.isFinite(minY) &&
                    Number.isFinite(maxX) &&
                    Number.isFinite(maxY) &&
                    maxX > minX &&
                    maxY > minY
                ) {
                    out.push({ minX, minY, maxX, maxY });
                }
            }
            return out;
        }

        rectsCoverLargeLevel0SurfaceArea(rects, bounds) {
            if (!Array.isArray(rects) || rects.length === 0 || !bounds) return false;
            const surfaceArea = Math.max(0.001, Number(bounds.width) * Number(bounds.height));
            let area = 0;
            for (let i = 0; i < rects.length; i++) {
                const rect = rects[i];
                area += Math.max(0, Number(rect.maxX) - Number(rect.minX)) *
                    Math.max(0, Number(rect.maxY) - Number(rect.minY));
            }
            return (area / surfaceArea) > 0.35;
        }

        getLevel0PatchCandidateNodes(map, sectionKey, rect, bounds) {
            if (!map || !rect || !bounds) return [];
            const state = map._prototypeSectionState || null;
            const sectionNodes = state && state.nodesBySectionKey instanceof Map
                ? (state.nodesBySectionKey.get(sectionKey) || [])
                : [];
            const out = [];
            const seen = new Set();
            const pushCandidateNode = (node) => {
                if (!node || node._prototypeVoid === true) return;
                const key = `${Number(node.xindex)},${Number(node.yindex)}`;
                if (seen.has(key)) return;
                seen.add(key);
                out.push(node);
            };
            const padX = (Number(bounds.tileWorldW) || 1.2) * 3;
            const padY = (Number(bounds.tileWorldH) || 1) * 3;
            const minX = Number(rect.minX) - padX;
            const maxX = Number(rect.maxX) + padX;
            const minY = Number(rect.minY) - padY;
            const maxY = Number(rect.maxY) + padY;
            const nodeOverlapsCandidateBounds = (node) => {
                const x = Number(node && node.x);
                const y = Number(node && node.y);
                if (!Number.isFinite(x) || !Number.isFinite(y)) return false;
                return x >= minX && x <= maxX && y >= minY && y <= maxY;
            };
            const xScale = 0.866;
            const padXi = Math.ceil((Number(bounds.tileWorldW) || 1.2) / xScale) + 2;
            const padYi = Math.ceil(Number(bounds.tileWorldH) || 1) + 2;
            const minXi = Math.floor(Number(rect.minX) / xScale) - padXi;
            const maxXi = Math.ceil(Number(rect.maxX) / xScale) + padXi;
            const minYi = Math.floor(Number(rect.minY)) - padYi;
            const maxYi = Math.ceil(Number(rect.maxY)) + padYi;
            if (state && state.allNodesByCoordKey instanceof Map && state.allNodesByCoordKey.size > 0) {
                for (let xi = minXi; xi <= maxXi; xi++) {
                    for (let yi = minYi; yi <= maxYi; yi++) {
                        const node = state.allNodesByCoordKey.get(`${xi},${yi}`) || null;
                        if (!node || !nodeOverlapsCandidateBounds(node)) continue;
                        pushCandidateNode(node);
                    }
                }
                return out;
            }
            if (Array.isArray(sectionNodes) && sectionNodes.length > 0) {
                for (let i = 0; i < sectionNodes.length; i++) {
                    const node = sectionNodes[i];
                    if (!nodeOverlapsCandidateBounds(node)) continue;
                    pushCandidateNode(node);
                }
                return out;
            }
            if (typeof map.getNode !== "function") return [];
            for (let xi = minXi; xi <= maxXi; xi++) {
                for (let yi = minYi; yi <= maxYi; yi++) {
                    const node = map.getNode(xi, yi);
                    if (!node || !nodeOverlapsCandidateBounds(node)) continue;
                    pushCandidateNode(node);
                }
            }
            return out;
        }

        patchLevel0GroundSurfaceTexture(ctx, sectionKey, asset, cache, signature) {
            const map = ctx && ctx.map;
            const bounds = cache && cache.bounds;
            const canvas = cache && cache.canvas;
            const ctx2d = cache && cache.ctx2d;
            const scale = Number(cache && cache.scale);
            const rects = this.getLevel0GroundSurfacePatchRects(asset, bounds);
            if (!map || !bounds || !canvas || !ctx2d || !Number.isFinite(scale) || rects.length === 0) return null;
            const startMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            let bakedGroundTiles = 0;
            let bakedRoads = 0;
            let pendingTexture = false;
            let patchedPixels = 0;
            let candidateNodeTotal = 0;
            let textureMisses = 0;
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            for (let r = 0; r < rects.length; r++) {
                const rect = rects[r];
                const x = Math.floor((rect.minX - bounds.minX) * scale);
                const y = Math.floor((rect.minY - bounds.minY) * scale);
                const w = Math.max(1, Math.ceil((rect.maxX - rect.minX) * scale));
                const h = Math.max(1, Math.ceil((rect.maxY - rect.minY) * scale));
                let previousPatchCanvas = null;
                let previousPatchCtx = null;
                if (typeof document !== "undefined" && document && typeof document.createElement === "function") {
                    try {
                        previousPatchCanvas = document.createElement("canvas");
                        previousPatchCanvas.width = w;
                        previousPatchCanvas.height = h;
                        previousPatchCtx = previousPatchCanvas.getContext("2d");
                        if (previousPatchCtx) {
                            previousPatchCtx.drawImage(canvas, x, y, w, h, 0, 0, w, h);
                        }
                    } catch (_err) {
                        previousPatchCanvas = null;
                        previousPatchCtx = null;
                    }
                }
                if (!previousPatchCanvas || !previousPatchCtx) return null;
                patchedPixels += w * h;
                const candidateNodes = this.getLevel0PatchCandidateNodes(map, sectionKey, rect, bounds);
                const groundCandidateNodes = this.expandLevel0GroundBakeNodes(candidateNodes);
                candidateNodeTotal += candidateNodes.length;
                let rectPendingTexture = false;
                ctx2d.save();
                ctx2d.beginPath();
                ctx2d.rect(x, y, w, h);
                ctx2d.clip();
                ctx2d.clearRect(x, y, w, h);
                const seenRoads = new Set();
                const seenRoadPaths = new Set();
                const roadPathContinuity = FLOOR_LEVEL0_BAKE_ROAD_PATHS
                    ? this.buildRoadPathEndpointContinuity(this.collectRoadPathContinuityObjectsFromNodes(groundCandidateNodes))
                    : null;
                const terrainBake = this.drawLevel0GroundTerrainToCanvas(ctx2d, map, groundCandidateNodes, bounds, scale, sectionKey, asset);
                bakedGroundTiles += terrainBake && Number.isFinite(terrainBake.baked) ? terrainBake.baked : 0;
                if (terrainBake && terrainBake.pending) {
                    pendingTexture = true;
                    rectPendingTexture = true;
                    textureMisses += 1;
                }
                // Include one-ring neighbors so seam-crossing roads are baked on both sides.
                for (let i = 0; i < groundCandidateNodes.length; i++) {
                    const node = groundCandidateNodes[i];
                    if (!node || !Array.isArray(node.objects)) continue;
                    for (let j = 0; j < node.objects.length; j++) {
                        const road = node.objects[j];
                        if (!road || road.gone) continue;
                        let roadBake = null;
                        if (road.type === "road") {
                            if (seenRoads.has(road)) continue;
                            seenRoads.add(road);
                            roadBake = this.drawRoadToLevel0GroundSurfaceCanvas(ctx2d, road, node, bounds, scale, sectionKey, asset, {
                                refreshTexture: false
                            });
                        } else if (FLOOR_LEVEL0_BAKE_ROAD_PATHS && road.type === "roadPath") {
                            if (seenRoadPaths.has(road)) continue;
                            seenRoadPaths.add(road);
                            roadBake = this.drawRoadPathToLevel0GroundSurfaceCanvas(ctx2d, road, node, bounds, scale, sectionKey, asset, {
                                endpointContinuity: roadPathContinuity.get(road) || null
                            });
                        } else {
                            continue;
                        }
                        bakedRoads += roadBake && Number.isFinite(roadBake.baked) ? roadBake.baked : 0;
                        if (roadBake && roadBake.pending) {
                            pendingTexture = true;
                            rectPendingTexture = true;
                        }
                    }
                }
                ctx2d.restore();
                if (rectPendingTexture) {
                    ctx2d.drawImage(previousPatchCanvas, x, y);
                }
            }
            if (cache.texture && cache.texture.baseTexture && typeof cache.texture.baseTexture.update === "function") {
                cache.texture.baseTexture.update();
            } else if (cache.texture && typeof cache.texture.update === "function") {
                cache.texture.update();
            }
            if (pendingTexture) {
                const previousPendingSignature = cache._level0PatchPendingSignature;
                const previousPendingCount = previousPendingSignature === signature
                    ? Math.max(0, Number(cache._level0PatchPendingCount) || 0)
                    : 0;
                const nextPendingCount = Math.min(previousPendingCount + 1, 6);
                cache._level0PatchPendingSignature = signature;
                cache._level0PatchPendingCount = nextPendingCount;
                cache._level0PatchRetryAtMs = nowMs + Math.min(1000, 80 * Math.pow(2, nextPendingCount - 1));
                this.bakedLevel0SectionSignature = "";
                return cache;
            }
            cache.signature = signature;
            cache.ready = true;
            cache._level0PatchPendingSignature = "";
            cache._level0PatchPendingCount = 0;
            cache._level0PatchRetryAtMs = 0;
            asset._level0RoadSurfacePatchRects = [];
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorLevel0BakePatchRects = (this.currentFrameMetrics.floorLevel0BakePatchRects || 0) + rects.length;
                this.currentFrameMetrics.floorLevel0BakeGroundTiles = (this.currentFrameMetrics.floorLevel0BakeGroundTiles || 0) + bakedGroundTiles;
                this.currentFrameMetrics.floorLevel0BakeRoads = (this.currentFrameMetrics.floorLevel0BakeRoads || 0) + bakedRoads;
                this.currentFrameMetrics.floorLevel0BakePixels = (this.currentFrameMetrics.floorLevel0BakePixels || 0) + patchedPixels;
            }
            if (global.renderingDiagnostics && global.renderingDiagnostics.level0RoadPatchBakeLogging === true) {
                const elapsedMs = ((typeof performance !== "undefined" && performance && typeof performance.now === "function")
                    ? performance.now()
                    : Date.now()) - startMs;
                console.log("[level0 road patch bake]", {
                    sectionKey,
                    rects: rects.length,
                    candidateNodes: candidateNodeTotal,
                    groundTiles: bakedGroundTiles,
                    roads: bakedRoads,
                    textureMisses,
                    pendingTexture,
                    pixels: patchedPixels,
                    ms: Math.round(elapsedMs * 100) / 100
                });
            }
            this.bakedLevel0SectionSignature = "";
            return cache;
        }

        getLevel0GroundSurfaceTexture(ctx, sectionKey, asset) {
            const map = ctx && ctx.map;
            const state = map && map._prototypeSectionState;
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorLevel0BakeRequests = (this.currentFrameMetrics.floorLevel0BakeRequests || 0) + 1;
            }
            if (!state || !(state.nodesBySectionKey instanceof Map) || typeof document === "undefined") return null;
            const nodes = state.nodesBySectionKey.get(sectionKey) || [];
            const groundBakeNodes = this.getLevel0GroundSurfaceBakeNodes(map, sectionKey);
            const signature = this.getLevel0GroundSurfaceSignature(asset, groundBakeNodes);
            let cache = this.level0GroundSurfaceCache.get(sectionKey);
            if (cache && cache.signature === signature && cache.texture && cache.bounds) {
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorLevel0BakeHits = (this.currentFrameMetrics.floorLevel0BakeHits || 0) + 1;
                }
                return cache;
            }
            if (
                cache &&
                cache.texture &&
                cache.bounds &&
                cache._level0PatchPendingSignature === signature &&
                Number(cache._level0PatchRetryAtMs) > (
                    (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                        ? performance.now()
                        : Date.now()
                )
            ) {
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorLevel0BakePending = (this.currentFrameMetrics.floorLevel0BakePending || 0) + 1;
                }
                return cache;
            }
            const patchedCache = cache && cache.texture && cache.bounds
                ? this.patchLevel0GroundSurfaceTexture(ctx, sectionKey, asset, cache, signature)
                : null;
            if (patchedCache) {
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorLevel0BakeHits = (this.currentFrameMetrics.floorLevel0BakeHits || 0) + 1;
                }
                return patchedCache;
            }
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorLevel0BakeMisses = (this.currentFrameMetrics.floorLevel0BakeMisses || 0) + 1;
            }
            const bounds = this.getLevel0GroundSurfaceBounds(asset, map);
            if (!bounds) return null;

            const baseScale = FLOOR_LEVEL0_SURFACE_TEXTURE_PX_PER_WORLD;
            const scale = Math.min(
                baseScale,
                FLOOR_LEVEL0_SURFACE_TEXTURE_MAX_SIZE / Math.max(bounds.width, bounds.height)
            );
            const widthPx = Math.max(1, Math.ceil(bounds.width * scale));
            const heightPx = Math.max(1, Math.ceil(bounds.height * scale));
            const canvas = document.createElement("canvas");
            canvas.width = widthPx;
            canvas.height = heightPx;
            const ctx2d = canvas.getContext("2d");
            if (!ctx2d) return null;
            ctx2d.clearRect(0, 0, widthPx, heightPx);
            const terrainBake = this.drawLevel0GroundTerrainToCanvas(ctx2d, map, groundBakeNodes, bounds, scale, sectionKey, asset);
            let pendingTexture = !!(terrainBake && terrainBake.pending);
            let bakedGroundTiles = terrainBake && Number.isFinite(terrainBake.baked) ? terrainBake.baked : 0;
            const roadBake = this.addRoadsToLevel0GroundSurfaceCanvas(ctx2d, groundBakeNodes, bounds, scale, sectionKey, asset);
            pendingTexture = pendingTexture || !!(roadBake && roadBake.pending);
            if (pendingTexture) {
                if (this.currentFrameMetrics) {
                    this.currentFrameMetrics.floorLevel0BakePending = (this.currentFrameMetrics.floorLevel0BakePending || 0) + 1;
                }
                return null;
            }
            const renderTexture = PIXI.Texture.from(canvas);
            if (!renderTexture) return null;
            const previousCache = cache;
            cache = {
                signature,
                texture: renderTexture,
                bounds,
                scale,
                widthPx,
                heightPx,
                canvas,
                ctx2d,
                bakedGroundTiles,
                bakedRoads: roadBake && Number.isFinite(roadBake.baked) ? roadBake.baked : 0,
                ready: true
            };
            if (this.currentFrameMetrics) {
                this.currentFrameMetrics.floorLevel0BakeGroundTiles = (this.currentFrameMetrics.floorLevel0BakeGroundTiles || 0) + bakedGroundTiles;
                this.currentFrameMetrics.floorLevel0BakeRoads = (this.currentFrameMetrics.floorLevel0BakeRoads || 0) + cache.bakedRoads;
                this.currentFrameMetrics.floorLevel0BakePixels = (this.currentFrameMetrics.floorLevel0BakePixels || 0) + (widthPx * heightPx);
            }
            this.level0GroundSurfaceCache.set(sectionKey, cache);
            asset._level0RoadSurfacePatchRects = [];
            this.bakedLevel0SectionSignature = "";
            if (
                previousCache &&
                previousCache.texture &&
                previousCache.texture !== renderTexture &&
                typeof previousCache.texture.destroy === "function"
            ) {
                previousCache.texture.destroy(true);
            }
            return cache;
        }

        getFloorVisualTexture(entry) {
            if (entry && entry.texture) return entry.texture;
            const texturePath = entry && typeof entry.texturePath === "string" ? entry.texturePath : "";
            if (texturePath && !texturePath.startsWith("level0:")) {
                if (texturePath === FLOOR_VISUAL_CAVE_TEXTURE_PATH && this.floorVisualCaveTexture) {
                    return this.floorVisualCaveTexture;
                }
                if (!this.floorVisualTextureByPath) this.floorVisualTextureByPath = new Map();
                let texture = this.floorVisualTextureByPath.get(texturePath);
                if (!texture) {
                    texture = PIXI.Texture.from(texturePath);
                    const baseTexture = texture.baseTexture || null;
                    if (baseTexture && PIXI.WRAP_MODES) {
                        baseTexture.wrapMode = PIXI.WRAP_MODES.REPEAT;
                    }
                    this.floorVisualTextureByPath.set(texturePath, texture);
                    if (texturePath === FLOOR_VISUAL_CAVE_TEXTURE_PATH) {
                        this.floorVisualCaveTexture = texture;
                    }
                }
                return texture;
            }
            return PIXI.Texture.WHITE;
        }

        normalizeFloorVisualTextureConfigPath(texturePath) {
            if (typeof texturePath !== "string" || texturePath.length === 0) return "";
            const raw = texturePath.split("?")[0].split("#")[0];
            if (raw.startsWith("/")) return raw;
            try {
                if (typeof window !== "undefined" && window.location && window.location.origin) {
                    return new URL(raw, window.location.origin).pathname || raw;
                }
            } catch (_) {}
            return raw;
        }

        buildFloorVisualTextureConfigMaps(doc) {
            const cfg = {
                byPath: new Map(),
                byFile: new Map(),
                defaultRepeatX: FLOOR_VISUAL_TEXTURE_WORLD_SCALE,
                defaultRepeatY: FLOOR_VISUAL_TEXTURE_WORLD_SCALE
            };
            const defaults = doc && typeof doc.defaults === "object" && doc.defaults ? doc.defaults : {};
            const defaultRepeat = Number.isFinite(defaults.repeatsPerMapUnit)
                ? Math.max(0.0001, Number(defaults.repeatsPerMapUnit))
                : null;
            cfg.defaultRepeatX = Number.isFinite(defaults.repeatsPerMapUnitX)
                ? Math.max(0.0001, Number(defaults.repeatsPerMapUnitX))
                : (defaultRepeat || FLOOR_VISUAL_TEXTURE_WORLD_SCALE);
            cfg.defaultRepeatY = Number.isFinite(defaults.repeatsPerMapUnitY)
                ? Math.max(0.0001, Number(defaults.repeatsPerMapUnitY))
                : (defaultRepeat || FLOOR_VISUAL_TEXTURE_WORLD_SCALE);
            const items = doc && Array.isArray(doc.items) ? doc.items : [];
            for (let i = 0; i < items.length; i++) {
                const item = items[i];
                if (!item || typeof item !== "object") continue;
                const texturePath = this.normalizeFloorVisualTextureConfigPath(item.texturePath);
                const fallbackRepeat = Number.isFinite(item.repeatsPerMapUnit)
                    ? Math.max(0.0001, Number(item.repeatsPerMapUnit))
                    : null;
                const repeatsPerMapUnitX = Number.isFinite(item.repeatsPerMapUnitX)
                    ? Math.max(0.0001, Number(item.repeatsPerMapUnitX))
                    : (fallbackRepeat || cfg.defaultRepeatX);
                const repeatsPerMapUnitY = Number.isFinite(item.repeatsPerMapUnitY)
                    ? Math.max(0.0001, Number(item.repeatsPerMapUnitY))
                    : (fallbackRepeat || cfg.defaultRepeatY);
                const normalizedEntry = { texturePath, repeatsPerMapUnitX, repeatsPerMapUnitY };
                if (texturePath) cfg.byPath.set(texturePath, normalizedEntry);
                const file = typeof item.file === "string" && item.file.length > 0
                    ? item.file.toLowerCase()
                    : "";
                if (file) cfg.byFile.set(file, normalizedEntry);
                const textureFile = texturePath ? (texturePath.split("/").pop() || "").toLowerCase() : "";
                if (textureFile) cfg.byFile.set(textureFile, normalizedEntry);
                const lodTextures = Array.isArray(item.lodTextures) ? item.lodTextures : [];
                for (let j = 0; j < lodTextures.length; j++) {
                    const lodPath = this.normalizeFloorVisualTextureConfigPath(lodTextures[j] && lodTextures[j].texturePath);
                    if (!lodPath) continue;
                    cfg.byPath.set(lodPath, normalizedEntry);
                    const lodFile = (lodPath.split("/").pop() || "").toLowerCase();
                    if (lodFile) cfg.byFile.set(lodFile, normalizedEntry);
                }
            }
            return cfg;
        }

        ensureFloorVisualTextureConfigLoaded() {
            if (this.floorVisualTextureConfigCache) return Promise.resolve(this.floorVisualTextureConfigCache);
            if (this.floorVisualTextureConfigPromise) return this.floorVisualTextureConfigPromise;
            if (typeof fetch !== "function") {
                this.floorVisualTextureConfigCache = this.buildFloorVisualTextureConfigMaps(null);
                return Promise.resolve(this.floorVisualTextureConfigCache);
            }
            const applyAndReturn = (doc) => {
                this.floorVisualTextureConfigCache = this.buildFloorVisualTextureConfigMaps(doc);
                return this.floorVisualTextureConfigCache;
            };
            this.floorVisualTextureConfigPromise = fetch(FLOORING_TEXTURE_CONFIG_URL, { cache: "no-cache" })
                .then(resp => (resp && resp.ok) ? resp.json() : null)
                .then(doc => {
                    const cfg = applyAndReturn(doc);
                    if (typeof presentGameFrame === "function") presentGameFrame();
                    return cfg;
                })
                .catch(() => applyAndReturn(null))
                .finally(() => {
                    this.floorVisualTextureConfigPromise = null;
                });
            return this.floorVisualTextureConfigPromise;
        }

        getFloorVisualTextureConfigEntry(texturePath) {
            const normalized = this.normalizeFloorVisualTextureConfigPath(texturePath);
            const filename = (normalized.split("/").pop() || "").toLowerCase();
            const RoadClass = (typeof Road !== "undefined" && Road)
                ? Road
                : ((global && global.Road) ? global.Road : null);
            if (RoadClass && typeof RoadClass._getFlooringTextureConfigEntry === "function") {
                const entry = RoadClass._getFlooringTextureConfigEntry(normalized);
                if (entry) return entry;
            } else if (!this.floorVisualTextureConfigCache) {
                void this.ensureFloorVisualTextureConfigLoaded();
            }
            const cache = this.floorVisualTextureConfigCache || null;
            const byPath = cache && cache.byPath ? cache.byPath : null;
            const byFile = cache && cache.byFile ? cache.byFile : null;
            return (byPath && byPath.get(normalized)) || (byFile && byFile.get(filename)) || null;
        }

        getFloorVisualTextureRepeat(texturePath) {
            const cache = this.floorVisualTextureConfigCache || null;
            const entry = this.getFloorVisualTextureConfigEntry(texturePath);
            return {
                x: entry && Number.isFinite(entry.repeatsPerMapUnitX)
                    ? Math.max(0.0001, Number(entry.repeatsPerMapUnitX))
                    : (cache && Number.isFinite(cache.defaultRepeatX) ? cache.defaultRepeatX : FLOOR_VISUAL_TEXTURE_WORLD_SCALE),
                y: entry && Number.isFinite(entry.repeatsPerMapUnitY)
                    ? Math.max(0.0001, Number(entry.repeatsPerMapUnitY))
                    : (cache && Number.isFinite(cache.defaultRepeatY) ? cache.defaultRepeatY : FLOOR_VISUAL_TEXTURE_WORLD_SCALE)
            };
        }

        getFloorVisualTextureRepeatSignature(textureRepeat) {
            if (!textureRepeat) return "";
            const x = Number.isFinite(textureRepeat.x) ? Number(textureRepeat.x) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const y = Number.isFinite(textureRepeat.y) ? Number(textureRepeat.y) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            return `${x.toFixed(6)}:${y.toFixed(6)}`;
        }

        getFloorVisualDepthState() {
            if (this.floorVisualDepthState) return this.floorVisualDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = true;
            state.blend = true;
            state.culling = false;
            this.floorVisualDepthState = state;
            return state;
        }

        getRoadPathDepthState() {
            if (this.roadPathDepthState) return this.roadPathDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) {
                throw new Error("road path alpha fade requires PIXI.State");
            }
            if (!PIXI.BLEND_MODES || !Object.prototype.hasOwnProperty.call(PIXI.BLEND_MODES, "NORMAL")) {
                throw new Error("road path alpha fade requires PIXI.BLEND_MODES.NORMAL");
            }
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = false;
            state.blend = true;
            state.blendMode = PIXI.BLEND_MODES.NORMAL;
            state.culling = false;
            this.roadPathDepthState = state;
            return state;
        }

        getStairDepthState() {
            if (this.stairDepthState) return this.stairDepthState;
            if (typeof PIXI === "undefined" || !PIXI.State) return null;
            const state = new PIXI.State();
            state.depthTest = true;
            state.depthMask = true;
            state.blend = true;
            state.culling = false;
            this.stairDepthState = state;
            return state;
        }

        buildStraightStairMeshGeometry(stair) {
            if (!stair || stair.stairKind !== "straight") return null;
            const lowerPoint = stair.lowerPoint || null;
            const higherPoint = stair.higherPoint || null;
            const lowerX = Number(lowerPoint && lowerPoint.x);
            const lowerY = Number(lowerPoint && lowerPoint.y);
            const higherX = Number(higherPoint && higherPoint.x);
            const higherY = Number(higherPoint && higherPoint.y);
            const lowerZ = Number(stair.lowerZ);
            const higherZ = Number(stair.higherZ);
            if (
                !Number.isFinite(lowerX) ||
                !Number.isFinite(lowerY) ||
                !Number.isFinite(higherX) ||
                !Number.isFinite(higherY) ||
                !Number.isFinite(lowerZ) ||
                !Number.isFinite(higherZ)
            ) {
                throw new Error(`straight stair ${stair.id || "(unknown)"} has invalid render endpoints`);
            }
            const mapRef = this.camera && this.camera.map ? this.camera.map : (global.map || null);
            const dx = mapRef && typeof mapRef.shortestDeltaX === "function"
                ? mapRef.shortestDeltaX(lowerX, higherX)
                : (higherX - lowerX);
            const dy = mapRef && typeof mapRef.shortestDeltaY === "function"
                ? mapRef.shortestDeltaY(lowerY, higherY)
                : (higherY - lowerY);
            const length = Math.hypot(dx, dy);
            if (!(length > 1e-6)) {
                throw new Error(`straight stair ${stair.id || "(unknown)"} has zero render length`);
            }
            const width = Number.isFinite(stair.width) ? Math.max(0.05, Number(stair.width)) : 1.2;
            const stepCount = Number.isFinite(stair.stepCount)
                ? Math.max(1, Math.round(Number(stair.stepCount)))
                : Math.max(2, Math.ceil(length / 0.55), Math.ceil(Math.abs(higherZ - lowerZ) / 0.35));
            const ux = dx / length;
            const uy = dy / length;
            const px = -uy * width * 0.5;
            const py = ux * width * 0.5;
            const dz = higherZ - lowerZ;
            const texturePath = typeof stair.texturePath === "string" ? stair.texturePath : "";
            const repeat = this.getFloorVisualTextureRepeat(texturePath || FLOOR_VISUAL_CAVE_TEXTURE_PATH);
            const repeatX = repeat && Number.isFinite(repeat.x) ? Number(repeat.x) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const repeatY = repeat && Number.isFinite(repeat.y) ? Number(repeat.y) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const positions = [];
            const uvs = [];
            const shades = [];
            const indices = [];
            const faceTypes = [];

            const edgePoint = (t, side, z) => ({
                x: lowerX + dx * t + px * side,
                y: lowerY + dy * t + py * side,
                z
            });
            const pushFace = (verts, uvList, shade, faceType) => {
                const base = positions.length / 3;
                if (base + 4 > 65535) {
                    throw new Error(`straight stair ${stair.id || "(unknown)"} render mesh exceeds 65535 vertices`);
                }
                for (let i = 0; i < 4; i++) {
                    const v = verts[i];
                    positions.push(Number(v.x), Number(v.y), Number(v.z));
                    const uv = uvList[i] || { u: 0, v: 0 };
                    uvs.push(Number(uv.u) || 0, Number(uv.v) || 0);
                    shades.push(Number.isFinite(shade) ? Number(shade) : 1);
                }
                indices.push(base, base + 1, base + 2, base, base + 2, base + 3);
                faceTypes.push(faceType);
            };

            for (let i = 0; i < stepCount; i++) {
                const t0 = i / stepCount;
                const t1 = (i + 1) / stepCount;
                const z0 = lowerZ + dz * t0;
                const z1 = lowerZ + dz * t1;
                const run0 = t0 * length;
                const run1 = t1 * length;
                const rise = Math.abs(z1 - z0);

                pushFace(
                    [
                        edgePoint(t0, 1, z1),
                        edgePoint(t1, 1, z1),
                        edgePoint(t1, -1, z1),
                        edgePoint(t0, -1, z1)
                    ],
                    [
                        { u: run0 * repeatX, v: 0 },
                        { u: run1 * repeatX, v: 0 },
                        { u: run1 * repeatX, v: width * repeatY },
                        { u: run0 * repeatX, v: width * repeatY }
                    ],
                    1,
                    "tread"
                );

                pushFace(
                    [
                        edgePoint(t0, -1, z0),
                        edgePoint(t0, 1, z0),
                        edgePoint(t0, 1, z1),
                        edgePoint(t0, -1, z1)
                    ],
                    [
                        { u: 0, v: rise * repeatY },
                        { u: width * repeatX, v: rise * repeatY },
                        { u: width * repeatX, v: 0 },
                        { u: 0, v: 0 }
                    ],
                    0.74,
                    "riser"
                );

                pushFace(
                    [
                        edgePoint(t0, 1, z0),
                        edgePoint(t1, 1, z0),
                        edgePoint(t1, 1, z1),
                        edgePoint(t0, 1, z1)
                    ],
                    [
                        { u: run0 * repeatX, v: rise * repeatY },
                        { u: run1 * repeatX, v: rise * repeatY },
                        { u: run1 * repeatX, v: 0 },
                        { u: run0 * repeatX, v: 0 }
                    ],
                    0.62,
                    "side"
                );

                pushFace(
                    [
                        edgePoint(t1, -1, z0),
                        edgePoint(t0, -1, z0),
                        edgePoint(t0, -1, z1),
                        edgePoint(t1, -1, z1)
                    ],
                    [
                        { u: run1 * repeatX, v: rise * repeatY },
                        { u: run0 * repeatX, v: rise * repeatY },
                        { u: run0 * repeatX, v: 0 },
                        { u: run1 * repeatX, v: 0 }
                    ],
                    0.66,
                    "side"
                );
            }

            return {
                positions: new Float32Array(positions),
                uvs: new Float32Array(uvs),
                shades: new Float32Array(shades),
                indices: new Uint16Array(indices),
                vertexCount: positions.length / 3,
                triangleCount: indices.length / 3,
                faceTypes,
                texturePath: texturePath || FLOOR_VISUAL_CAVE_TEXTURE_PATH,
                signature: [
                    stair.id || "",
                    lowerX.toFixed(4),
                    lowerY.toFixed(4),
                    higherX.toFixed(4),
                    higherY.toFixed(4),
                    lowerZ.toFixed(4),
                    higherZ.toFixed(4),
                    width.toFixed(4),
                    stepCount,
                    texturePath,
                    repeatX.toFixed(6),
                    repeatY.toFixed(6)
                ].join("|")
            };
        }

        buildTreadPathStairMeshGeometry(stair) {
            if (!stair || stair.stairKind !== "treadPath") return null;
            const traversal = (typeof globalThis !== "undefined" && globalThis.StairTraversal)
                ? globalThis.StairTraversal
                : null;
            if (
                !traversal ||
                typeof traversal.createTreadPathFrame !== "function" ||
                typeof traversal.pointFromPathLocal !== "function" ||
                typeof traversal.treadPathSteppedBaseZ !== "function"
            ) {
                throw new Error("tread path stair rendering requires StairTraversal runtime");
            }
            const stairId = typeof stair.id === "string" && stair.id.length > 0 ? stair.id : "(unknown)";
            const lowerZ = Number(stair.lowerZ);
            const higherZ = Number(stair.higherZ);
            if (!Number.isFinite(lowerZ) || !Number.isFinite(higherZ)) {
                throw new Error(`tread path stair ${stairId} has invalid render z endpoints`);
            }
            const frame = stair.traversalFrame || traversal.createTreadPathFrame(stair);
            const stepCount = Number.isFinite(Number(stair.stepCount))
                ? Math.max(1, Math.round(Number(stair.stepCount)))
                : Math.max(1, Array.isArray(stair.treads) ? stair.treads.length - 1 : 1);
            const texturePath = typeof stair.texturePath === "string" ? stair.texturePath : "";
            const repeat = this.getFloorVisualTextureRepeat(texturePath || FLOOR_VISUAL_CAVE_TEXTURE_PATH);
            const repeatX = repeat && Number.isFinite(repeat.x) ? Number(repeat.x) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const repeatY = repeat && Number.isFinite(repeat.y) ? Number(repeat.y) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
            const positions = [];
            const uvs = [];
            const shades = [];
            const indices = [];
            const faceTypes = [];
            const pushFace = (verts, uvList, shade, faceType) => {
                const base = positions.length / 3;
                if (base + 4 > 65535) {
                    throw new Error(`tread path stair ${stairId} render mesh exceeds 65535 vertices`);
                }
                for (let i = 0; i < 4; i++) {
                    const v = verts[i];
                    if (!Number.isFinite(v.x) || !Number.isFinite(v.y) || !Number.isFinite(v.z)) {
                        throw new Error(`tread path stair ${stairId} render mesh has a non-finite vertex`);
                    }
                    positions.push(Number(v.x), Number(v.y), Number(v.z));
                    const uv = uvList[i] || { u: 0, v: 0 };
                    uvs.push(Number(uv.u) || 0, Number(uv.v) || 0);
                    shades.push(Number.isFinite(shade) ? Number(shade) : 1);
                }
                indices.push(base, base + 1, base + 2, base, base + 2, base + 3);
                faceTypes.push(faceType);
            };

            for (let i = 0; i < stepCount; i++) {
                const u0 = i / stepCount;
                const u1 = (i + 1) / stepCount;
                const left0 = traversal.pointFromPathLocal(frame, u0, 0);
                const right0 = traversal.pointFromPathLocal(frame, u0, 1);
                const right1 = traversal.pointFromPathLocal(frame, u1, 1);
                const left1 = traversal.pointFromPathLocal(frame, u1, 0);
                const treadZ = traversal.treadPathSteppedBaseZ(lowerZ, higherZ, stepCount, i);
                const previousZ = i === 0
                    ? lowerZ
                    : traversal.treadPathSteppedBaseZ(lowerZ, higherZ, stepCount, i - 1);
                const run0 = Math.max(0, frame.pathLength * u0);
                const run1 = Math.max(0, frame.pathLength * u1);
                const width0 = Math.hypot(right0.x - left0.x, right0.y - left0.y);
                const width1 = Math.hypot(right1.x - left1.x, right1.y - left1.y);
                const width = Math.max(width0, width1, 0.001);
                const rise = Math.abs(treadZ - previousZ);

                pushFace(
                    [
                        { ...left0, z: treadZ },
                        { ...left1, z: treadZ },
                        { ...right1, z: treadZ },
                        { ...right0, z: treadZ }
                    ],
                    [
                        { u: run0 * repeatX, v: 0 },
                        { u: run1 * repeatX, v: 0 },
                        { u: run1 * repeatX, v: width * repeatY },
                        { u: run0 * repeatX, v: width * repeatY }
                    ],
                    1,
                    "tread"
                );

                if (rise > 0.000001) {
                    pushFace(
                        [
                            { ...right0, z: previousZ },
                            { ...left0, z: previousZ },
                            { ...left0, z: treadZ },
                            { ...right0, z: treadZ }
                        ],
                        [
                            { u: 0, v: rise * repeatY },
                            { u: width * repeatX, v: rise * repeatY },
                            { u: width * repeatX, v: 0 },
                            { u: 0, v: 0 }
                        ],
                        0.74,
                        "riser"
                    );
                }
            }

            const lastLeft = traversal.pointFromPathLocal(frame, 1, 0);
            const lastRight = traversal.pointFromPathLocal(frame, 1, 1);
            const finalTreadZ = traversal.treadPathSteppedBaseZ(lowerZ, higherZ, stepCount, stepCount - 1);
            const finalRise = Math.abs(higherZ - finalTreadZ);
            if (finalRise > 0.000001) {
                const finalWidth = Math.max(
                    Math.hypot(lastRight.x - lastLeft.x, lastRight.y - lastLeft.y),
                    0.001
                );
                pushFace(
                    [
                        { ...lastLeft, z: finalTreadZ },
                        { ...lastRight, z: finalTreadZ },
                        { ...lastRight, z: higherZ },
                        { ...lastLeft, z: higherZ }
                    ],
                    [
                        { u: 0, v: finalRise * repeatY },
                        { u: finalWidth * repeatX, v: finalRise * repeatY },
                        { u: finalWidth * repeatX, v: 0 },
                        { u: 0, v: 0 }
                    ],
                    0.74,
                    "riser"
                );
            }

            const treadSignature = Array.isArray(stair.treads)
                ? stair.treads.map(tread => [
                    Number(tread && tread.left && tread.left.x).toFixed(4),
                    Number(tread && tread.left && tread.left.y).toFixed(4),
                    Number(tread && tread.right && tread.right.x).toFixed(4),
                    Number(tread && tread.right && tread.right.y).toFixed(4)
                ].join(",")).join(";")
                : "";
            return {
                positions: new Float32Array(positions),
                uvs: new Float32Array(uvs),
                shades: new Float32Array(shades),
                indices: new Uint16Array(indices),
                vertexCount: positions.length / 3,
                triangleCount: indices.length / 3,
                faceTypes,
                texturePath: texturePath || FLOOR_VISUAL_CAVE_TEXTURE_PATH,
                signature: [
                    stair.id || "",
                    "treadPath",
                    lowerZ.toFixed(4),
                    higherZ.toFixed(4),
                    stepCount,
                    texturePath,
                    repeatX.toFixed(6),
                    repeatY.toFixed(6),
                    treadSignature
                ].join("|")
            };
        }

        createStraightStairMesh(entry) {
            if (!entry || !entry.geometry || typeof PIXI === "undefined" || !PIXI.Geometry || !PIXI.Mesh || !PIXI.Shader) {
                return null;
            }
            const geometry = new PIXI.Geometry()
                .addAttribute("aWorldPosition", entry.geometry.positions, 3)
                .addAttribute("aUvs", entry.geometry.uvs, 2)
                .addAttribute("aShade", entry.geometry.shades, 1)
                .addIndex(entry.geometry.indices);
            const shader = PIXI.Shader.from(STAIR_DEPTH_VS, STAIR_DEPTH_FS, {
                uScreenSize: new Float32Array([1, 1]),
                uCameraWorld: new Float32Array([0, 0]),
                uCameraZ: 0,
                uDepthBias: FLOOR_VISUAL_DEPTH_BIAS_UNITS + 0.018,
                uViewScale: 1,
                uXyRatio: 1,
                uDepthRange: new Float32Array([
                    FLOOR_VISUAL_DEPTH_FAR_METRIC,
                    1 / Math.max(1e-6, FLOOR_VISUAL_DEPTH_FAR_METRIC - FLOOR_VISUAL_DEPTH_NEAR_METRIC)
                ]),
                uTint: new Float32Array([1, 1, 1, 1]),
                uAlphaCutoff: 0.001,
                uSampler: this.getFloorVisualTexture({ texturePath: entry.geometry.texturePath })
            });
            const state = this.getStairDepthState();
            const mesh = new PIXI.Mesh(geometry, shader, state || undefined, PIXI.DRAW_MODES ? PIXI.DRAW_MODES.TRIANGLES : undefined);
            mesh.name = "straightStairDepthMesh";
            mesh.interactive = false;
            mesh.visible = false;
            return mesh;
        }

        updateStraightStairMesh(entry, ctx) {
            if (!entry || !entry.mesh || !entry.mesh.shader || !entry.mesh.shader.uniforms) return false;
            const u = entry.mesh.shader.uniforms;
            const appRef = (ctx && ctx.app) || (typeof app !== "undefined" && app) || global.app || null;
            const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width))
                ? Number(appRef.screen.width)
                : 1;
            const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height))
                ? Number(appRef.screen.height)
                : 1;
            const cam = this.camera || {};
            if (u.uScreenSize) {
                u.uScreenSize[0] = Math.max(1, screenW);
                u.uScreenSize[1] = Math.max(1, screenH);
            }
            if (u.uCameraWorld) {
                u.uCameraWorld[0] = Number(cam.x) || 0;
                u.uCameraWorld[1] = Number(cam.y) || 0;
            }
            u.uCameraZ = Number(cam.z) || 0;
            u.uDepthBias = FLOOR_VISUAL_DEPTH_BIAS_UNITS + 0.018;
            u.uViewScale = Number(cam.viewscale) || 1;
            u.uXyRatio = Number(cam.xyratio) || 1;
            if (u.uDepthRange) {
                u.uDepthRange[0] = FLOOR_VISUAL_DEPTH_FAR_METRIC;
                u.uDepthRange[1] = 1 / Math.max(1e-6, FLOOR_VISUAL_DEPTH_FAR_METRIC - FLOOR_VISUAL_DEPTH_NEAR_METRIC);
            }
            if (u.uTint) {
                const tint = Number.isFinite(entry.tint) ? Math.max(0, Math.min(0xffffff, Math.floor(entry.tint))) : 0xffffff;
                u.uTint[0] = ((tint >> 16) & 0xff) / 255;
                u.uTint[1] = ((tint >> 8) & 0xff) / 255;
                u.uTint[2] = (tint & 0xff) / 255;
                u.uTint[3] = Number.isFinite(entry.alpha) ? Math.max(0, Math.min(1, Number(entry.alpha))) : 1;
            }
            u.uSampler = this.getFloorVisualTexture({ texturePath: entry.geometry.texturePath });
            entry.mesh.alpha = 1;
            entry.mesh.tint = 0xffffff;
            entry.mesh.position.set(0, 0);
            entry.mesh.scale.set(1, 1);
            entry.mesh.visible = true;
            if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) {
                entry.mesh.renderable = true;
            }
            return true;
        }

        getStraightStairRenderDisplayObject(item, ctx) {
            if (!item || item.type !== "straightStair") return null;
            const mapRef = (ctx && ctx.map) || item.map || (this.camera && this.camera.map) || global.map || null;
            const stair = item.stair || (
                mapRef && mapRef.stairsById instanceof Map && typeof item.stairId === "string"
                    ? mapRef.stairsById.get(item.stairId)
                    : null
            );
            if (!stair || stair.stairKind !== "straight") {
                throw new Error(`straight stair render object ${item.stairId || "(unknown)"} missing runtime stair record`);
            }
            const stairId = typeof stair.id === "string" && stair.id.length > 0
                ? stair.id
                : (typeof stair.transitionId === "string" ? stair.transitionId : "");
            if (!stairId) throw new Error("straight stair render record missing id");
            const geometry = this.buildStraightStairMeshGeometry(stair);
            if (!geometry) return null;
            if (!(this.stairMeshById instanceof Map)) this.stairMeshById = new Map();
            const cacheKey = `stair:${stairId}`;
            let entry = this.stairMeshById.get(cacheKey);
            if (!entry || entry.signature !== geometry.signature) {
                if (entry && entry.mesh) {
                    this.destroyCachedPixiMesh(entry.mesh, "straight stair mesh");
                }
                entry = {
                    key: cacheKey,
                    signature: geometry.signature,
                    geometry,
                    mesh: null,
                    tint: 0xffffff,
                    alpha: 1
                };
                entry.mesh = this.createStraightStairMesh(entry);
                if (!entry.mesh) return null;
                this.stairMeshById.set(cacheKey, entry);
            } else {
                entry.geometry = geometry;
            }
            const itemLayer = Number.isFinite(item._renderLayerIndex)
                ? item._renderLayerIndex
                : this.getLayerIndexForObject(item, 0);
            entry.tint = this.getLayerDarkenedTint(0xffffff, itemLayer);
            entry.alpha = Number.isFinite(item._renderLayerAlpha)
                ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                : 1;
            if (!this.updateStraightStairMesh(entry, ctx)) return null;
            item._renderingDisplayObject = entry.mesh;
            item._renderingDepthMesh = entry.mesh;
            return entry.mesh;
        }

        getTreadPathStairRenderDisplayObject(item, ctx) {
            if (!item || item.type !== "treadPathStair") return null;
            const mapRef = (ctx && ctx.map) || item.map || (this.camera && this.camera.map) || global.map || null;
            const stair = item.stair || (
                mapRef && mapRef.stairsById instanceof Map && typeof item.stairId === "string"
                    ? mapRef.stairsById.get(item.stairId)
                    : null
            );
            if (!stair || stair.stairKind !== "treadPath") {
                throw new Error(`tread path stair render object ${item.stairId || "(unknown)"} missing runtime stair record`);
            }
            const stairId = typeof stair.id === "string" && stair.id.length > 0
                ? stair.id
                : (typeof stair.transitionId === "string" ? stair.transitionId : "");
            if (!stairId) throw new Error("tread path stair render record missing id");
            const geometry = this.buildTreadPathStairMeshGeometry(stair);
            if (!geometry) return null;
            if (!(this.stairMeshById instanceof Map)) this.stairMeshById = new Map();
            const cacheKey = `treadPathStair:${stairId}`;
            let entry = this.stairMeshById.get(cacheKey);
            if (!entry || entry.signature !== geometry.signature) {
                if (entry && entry.mesh) {
                    this.destroyCachedPixiMesh(entry.mesh, "tread path stair mesh");
                }
                entry = {
                    key: cacheKey,
                    signature: geometry.signature,
                    geometry,
                    mesh: null,
                    tint: 0xffffff,
                    alpha: 1
                };
                entry.mesh = this.createStraightStairMesh(entry);
                if (!entry.mesh) return null;
                entry.mesh.name = "treadPathStairDepthMesh";
                this.stairMeshById.set(cacheKey, entry);
            } else {
                entry.geometry = geometry;
            }
            const itemLayer = Number.isFinite(item._renderLayerIndex)
                ? item._renderLayerIndex
                : this.getLayerIndexForObject(item, 0);
            entry.tint = this.getLayerDarkenedTint(0xffffff, itemLayer);
            entry.alpha = Number.isFinite(item._renderLayerAlpha)
                ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                : 1;
            if (!this.updateStraightStairMesh(entry, ctx)) return null;
            item._renderingDisplayObject = entry.mesh;
            item._renderingDepthMesh = entry.mesh;
            return entry.mesh;
        }

        isFloorHoleCoveredByStraightStair(level, hole, mapRef) {
            const holeRing = normalizeFloorVisualPointList(hole);
            if (holeRing.length < 3 || !mapRef || !(mapRef.stairsById instanceof Map)) return false;
            const targetLevel = Number.isFinite(level) ? Math.round(Number(level)) : 0;
            for (const stair of mapRef.stairsById.values()) {
                if (!stair || stair.stairKind !== "straight") continue;
                const higherLevel = Number.isFinite(stair.higherLevel) ? Math.round(Number(stair.higherLevel)) : 0;
                if (higherLevel !== targetLevel) continue;
                const footprint = normalizeFloorVisualPointList(stair.footprint);
                if (footprint.length < 3) continue;
                let holeInsideFootprint = true;
                for (let i = 0; i < holeRing.length; i++) {
                    if (!pointInFloorVisualPolygon2D(holeRing[i].x, holeRing[i].y, footprint, { epsilon: 0.02 })) {
                        holeInsideFootprint = false;
                        break;
                    }
                }
                if (!holeInsideFootprint) continue;
                for (let i = 0; i < footprint.length; i++) {
                    if (pointInFloorVisualPolygon2D(footprint[i].x, footprint[i].y, holeRing, { epsilon: 0.02 })) {
                        return true;
                    }
                }
            }
            return false;
        }

        createFloorVisualMesh(entry) {
            if (!entry || !entry.triangulation || typeof PIXI === "undefined" || !PIXI.Geometry || !PIXI.Mesh || !PIXI.Shader) {
                return null;
            }
            const vertexData = new Float32Array(entry.triangulation.vertexCount * 2);
            const uvData = new Float32Array(entry.triangulation.vertexCount * 2);
            const edgeAlphaData = new Float32Array(entry.triangulation.vertexCount);
            edgeAlphaData.fill(1);
            const geometry = new PIXI.Geometry()
                .addAttribute("aVertexPosition", vertexData, 2)
                .addAttribute("aUvs", uvData, 2)
                .addAttribute("aEdgeAlpha", edgeAlphaData, 1)
                .addIndex(entry.triangulation.indices);
            const nearMetric = FLOOR_VISUAL_DEPTH_NEAR_METRIC;
            const farMetric = FLOOR_VISUAL_DEPTH_FAR_METRIC;
            const shader = PIXI.Shader.from(FLOOR_VISUAL_DEPTH_VS, entry.isAnimatedWater ? FLOOR_VISUAL_WATER_DEPTH_FS : FLOOR_VISUAL_DEPTH_FS, {
                uScreenSize: new Float32Array([1, 1]),
                uCameraWorld: new Float32Array([0, 0]),
                uCameraZ: 0,
                uBaseZ: 0,
                uDepthBias: Number.isFinite(entry.depthBias) ? Number(entry.depthBias) : FLOOR_VISUAL_DEPTH_BIAS_UNITS,
                uViewScale: 1,
                uXyRatio: 1,
                uDepthRange: new Float32Array([farMetric, 1 / Math.max(1e-6, farMetric - nearMetric)]),
                uTint: new Float32Array([1, 1, 1, 1]),
                uPhaseOffset0: new Float32Array([0, 0]),
                uPhaseOffset1: new Float32Array([0, 0]),
                uPhaseOffset2: new Float32Array([0, 0]),
                uPhaseWeights: new Float32Array([1, 0, 0]),
                uSpatialPhase: 0,
                uSpatialFrequency: 2.25,
                uSpatialStrength: 0.42,
                uAlphaCutoff: 0.001,
                uBuildingCutawayDataPass: 0,
                uBuildingCutawayDataZRange: new Float32Array([
                    BUILDING_CUTAWAY_DATA_MIN_Z,
                    1 / Math.max(1e-6, BUILDING_CUTAWAY_DATA_MAX_Z - BUILDING_CUTAWAY_DATA_MIN_Z)
                ]),
                uSampler: this.getFloorVisualTexture(entry)
            });
            const mesh = new PIXI.Mesh(geometry, shader);
            mesh.name = entry.isAnimatedWater ? "floorAnimatedWaterMesh" : (entry.isHoleOverlay ? "floorHoleVisualMesh" : "floorSurfaceVisualMesh");
            mesh.interactive = false;
            mesh.alpha = entry.alpha;
            mesh.tint = entry.tint;
            const state = this.getFloorVisualDepthState();
            if (state) mesh.state = state;
            return mesh;
        }

        sanitizePixiGeometryBeforeDestroy(geometry) {
            if (!geometry || !Array.isArray(geometry.buffers)) return;
            const vaoObjects = geometry.glVertexArrayObjects && typeof geometry.glVertexArrayObjects === "object"
                ? geometry.glVertexArrayObjects
                : null;
            const vaoKeys = vaoObjects ? Object.keys(vaoObjects) : [];
            for (let k = 0; k < vaoKeys.length; k++) {
                const contextUid = vaoKeys[k];
                let missingBuffer = false;
                for (let i = 0; i < geometry.buffers.length; i++) {
                    const buffer = geometry.buffers[i];
                    const glBuffers = buffer && buffer._glBuffers && typeof buffer._glBuffers === "object"
                        ? buffer._glBuffers
                        : null;
                    if (!glBuffers || !glBuffers[contextUid]) {
                        missingBuffer = true;
                        break;
                    }
                }
                if (missingBuffer) {
                    delete vaoObjects[contextUid];
                }
            }
            for (let i = 0; i < geometry.buffers.length; i++) {
                const buffer = geometry.buffers[i];
                const glBuffers = buffer && buffer._glBuffers && typeof buffer._glBuffers === "object"
                    ? buffer._glBuffers
                    : null;
                if (!glBuffers) continue;
                const keys = Object.keys(glBuffers);
                for (let k = 0; k < keys.length; k++) {
                    if (!glBuffers[keys[k]]) delete glBuffers[keys[k]];
                }
            }
        }

        recordStalePixiMeshDestroy(label, err = null) {
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            this._stalePixiMeshDestroyWarningCount = (Number(this._stalePixiMeshDestroyWarningCount) || 0) + 1;
            if (
                Number.isFinite(this._lastStalePixiMeshDestroyWarningAtMs) &&
                (nowMs - this._lastStalePixiMeshDestroyWarningAtMs) < 5000
            ) {
                return;
            }
            this._lastStalePixiMeshDestroyWarningAtMs = nowMs;
            const count = Number(this._stalePixiMeshDestroyWarningCount) || 0;
            this._stalePixiMeshDestroyWarningCount = 0;
            console.warn("[rendering] detached stale Pixi mesh during destroy", {
                label,
                suppressedCount: Math.max(0, count - 1),
                error: err && err.message ? String(err.message) : null
            });
        }

        destroyCachedPixiMesh(mesh, label = "cached Pixi mesh") {
            if (!mesh) return false;
            if (mesh.parent && typeof mesh.parent.removeChild === "function") {
                mesh.parent.removeChild(mesh);
            }
            mesh.visible = false;
            if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                mesh.renderable = false;
            }
            if (typeof mesh.destroy !== "function") return false;
            if (!mesh.geometry || !Number.isFinite(mesh.geometry.refCount)) {
                mesh.geometry = null;
                mesh.shader = null;
                mesh.state = null;
                this.recordStalePixiMeshDestroy(label);
                return false;
            }
            this.sanitizePixiGeometryBeforeDestroy(mesh.geometry);
            try {
                mesh.destroy({ children: false, texture: false, baseTexture: false });
                return true;
            } catch (_err) {
                mesh.geometry = null;
                mesh.shader = null;
                mesh.state = null;
                this.recordStalePixiMeshDestroy(label, _err);
                return false;
            }
        }

        getFloorVisualTextureBoundsSignature(bounds) {
            if (!bounds) return "";
            return [
                Number(bounds.minX) || 0,
                Number(bounds.minY) || 0,
                Number(bounds.width) || 0,
                Number(bounds.height) || 0
            ].map(value => Math.round(value * 1000)).join(",");
        }

        uploadFloorVisualMeshGeometry(entry) {
            if (!entry || !entry.mesh || !entry.triangulation) return false;
            const geometry = entry.mesh.geometry || null;
            const buffer = geometry && typeof geometry.getBuffer === "function"
                ? geometry.getBuffer("aVertexPosition")
                : null;
            if (!buffer || !buffer.data) return false;
            const data = buffer.data;
            const points = entry.triangulation.points;
            if (!Array.isArray(points) || data.length < points.length * 2) return false;
            const uvBuffer = geometry && typeof geometry.getBuffer === "function"
                ? geometry.getBuffer("aUvs")
                : null;
            const uvData = uvBuffer && uvBuffer.data ? uvBuffer.data : null;
            const edgeAlphaBuffer = geometry && typeof geometry.getBuffer === "function"
                ? geometry.getBuffer("aEdgeAlpha")
                : null;
            const edgeAlphaData = edgeAlphaBuffer && edgeAlphaBuffer.data ? edgeAlphaBuffer.data : null;
            const sourceEdgeAlpha = entry.triangulation && entry.triangulation.edgeAlpha ? entry.triangulation.edgeAlpha : null;
            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                data[i * 2] = pt.x;
                data[i * 2 + 1] = pt.y;
                if (uvData && uvData.length >= (i * 2 + 2)) {
                    if (entry.textureBounds) {
                        uvData[i * 2] = (pt.x - entry.textureBounds.minX) / entry.textureBounds.width;
                        uvData[i * 2 + 1] = (pt.y - entry.textureBounds.minY) / entry.textureBounds.height;
                    } else {
                        const repeat = entry.textureRepeat || this.getFloorVisualTextureRepeat(entry.texturePath);
                        const repeatX = repeat && Number.isFinite(repeat.x) ? Number(repeat.x) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
                        const repeatY = repeat && Number.isFinite(repeat.y) ? Number(repeat.y) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
                        uvData[i * 2] = pt.x * repeatX;
                        uvData[i * 2 + 1] = pt.y * repeatY;
                    }
                }
                if (edgeAlphaData && edgeAlphaData.length > i) {
                    edgeAlphaData[i] = sourceEdgeAlpha && sourceEdgeAlpha.length > i
                        ? Math.max(0, Math.min(1, Number(sourceEdgeAlpha[i])))
                        : 1;
                }
            }
            buffer.update();
            if (uvBuffer) uvBuffer.update();
            if (edgeAlphaBuffer) edgeAlphaBuffer.update();
            entry.uploadedGeometrySignature = entry.signature || "";
            entry.uploadedTextureBoundsSignature = this.getFloorVisualTextureBoundsSignature(entry.textureBounds);
            entry.uploadedTextureRepeatSignature = this.getFloorVisualTextureRepeatSignature(entry.textureRepeat);
            return true;
        }

        updateFloorVisualMesh(entry) {
            if (!entry || !entry.mesh) return false;
            const texture = this.getFloorVisualTexture(entry);
            const shader = entry.mesh.shader || null;
            const uniforms = shader && shader.uniforms ? shader.uniforms : null;
            if (uniforms) {
                uniforms.uSampler = texture;
                const appRef = (typeof app !== "undefined" && app) ? app : (global.app || null);
                const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width))
                    ? Number(appRef.screen.width)
                    : 1;
                const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height))
                    ? Number(appRef.screen.height)
                    : 1;
                const cam = this.camera || {};
                if (uniforms.uScreenSize) {
                    uniforms.uScreenSize[0] = Math.max(1, screenW);
                    uniforms.uScreenSize[1] = Math.max(1, screenH);
                }
                if (uniforms.uCameraWorld) {
                    uniforms.uCameraWorld[0] = Number(cam.x) || 0;
                    uniforms.uCameraWorld[1] = Number(cam.y) || 0;
                }
                uniforms.uCameraZ = Number(cam.z) || 0;
                uniforms.uBaseZ = Number.isFinite(entry.baseZ) ? Number(entry.baseZ) : 0;
                uniforms.uDepthBias = Number.isFinite(entry.depthBias) ? Number(entry.depthBias) : FLOOR_VISUAL_DEPTH_BIAS_UNITS;
                uniforms.uViewScale = Number(cam.viewscale) || 1;
                uniforms.uXyRatio = Number(cam.xyratio) || 1;
                if (uniforms.uDepthRange) {
                    const nearMetric = FLOOR_VISUAL_DEPTH_NEAR_METRIC;
                    const farMetric = FLOOR_VISUAL_DEPTH_FAR_METRIC;
                    uniforms.uDepthRange[0] = farMetric;
                    uniforms.uDepthRange[1] = 1 / Math.max(1e-6, farMetric - nearMetric);
                }
                if (uniforms.uTint) {
                    const tint = Number.isFinite(entry.tint) ? Math.max(0, Math.min(0xffffff, Math.floor(entry.tint))) : 0xffffff;
                    uniforms.uTint[0] = ((tint >> 16) & 0xff) / 255;
                    uniforms.uTint[1] = ((tint >> 8) & 0xff) / 255;
                    uniforms.uTint[2] = (tint & 0xff) / 255;
                    uniforms.uTint[3] = Math.max(0, Math.min(1, Number.isFinite(entry.alpha) ? Number(entry.alpha) : 1));
                }
                if (entry.isAnimatedWater) {
                    const nowMs = Number.isFinite(entry.animationNowMs)
                        ? Number(entry.animationNowMs)
                        : ((typeof performance !== "undefined" && performance && typeof performance.now === "function") ? performance.now() : Date.now());
                    const t = nowMs * 0.001;
                    const cycleSeconds = 2.4;
                    const phase = ((t / cycleSeconds) % 1 + 1) % 1;
                    const wave = phase * Math.PI * 2;
                    const w0 = 0.5 + 0.5 * Math.sin(wave);
                    const w1 = 0.5 + 0.5 * Math.sin(wave + Math.PI * 2 / 3);
                    const w2 = 0.5 + 0.5 * Math.sin(wave + Math.PI * 4 / 3);
                    const zeroHold = 0.02;
                    const zeroFadeWidth = 0.12;
                    const smoothZeroGate = (value) => {
                        const t0 = Math.max(0, Math.min(1, (value - zeroHold) / zeroFadeWidth));
                        return t0 * t0 * (3 - 2 * t0);
                    };
                    const z0 = w0 * smoothZeroGate(w0);
                    const z1 = w1 * smoothZeroGate(w1);
                    const z2 = w2 * smoothZeroGate(w2);
                    const wSum = Math.max(0.0001, z0 + z1 + z2);
                    const phaseWeights = [z0 / wSum, z1 / wSum, z2 / wSum];
                    if (uniforms.uPhaseWeights) {
                        uniforms.uPhaseWeights[0] = phaseWeights[0];
                        uniforms.uPhaseWeights[1] = phaseWeights[1];
                        uniforms.uPhaseWeights[2] = phaseWeights[2];
                    }
                    uniforms.uSpatialPhase = wave;
                    uniforms.uSpatialFrequency = 2.25;
                    uniforms.uSpatialStrength = 0.42;
                    if (!this._animatedWaterDriftState) {
                        this._animatedWaterDriftState = {
                            timeSeconds: t,
                            offsets: [
                                [0, 0],
                                [0, 0],
                                [0, 0]
                            ],
                            previousOpacities: [1, 1, 1]
                        };
                    }
                    const driftState = this._animatedWaterDriftState;
                    if (!Array.isArray(driftState.offsets) || driftState.offsets.length !== 3) {
                        driftState.offsets = [
                            [0, 0],
                            [0, 0],
                            [0, 0]
                        ];
                    }
                    if (!Array.isArray(driftState.previousOpacities) || driftState.previousOpacities.length !== 3) {
                        driftState.previousOpacities = [1, 1, 1];
                    }
                    const previousTime = Number.isFinite(driftState.timeSeconds) ? Number(driftState.timeSeconds) : t;
                    const driftDt = Math.max(0, Math.min(0.25, t - previousTime));
                    driftState.timeSeconds = t;
                    const baseDriftX = 0.014;
                    const baseDriftY = 0.010;
                    const maxFadeDriftMultiplier = 2;
                    const minFadeDriftDenominator = 1 / maxFadeDriftMultiplier;
                    const invisibleOpacityThreshold = 0.0005;
                    const rotateUvDrift = (offset, cos, sin) => ([
                        offset[0] * cos + offset[1] * sin,
                        -offset[0] * sin + offset[1] * cos
                    ]);
                    for (let i = 0; i < 3; i++) {
                        const opacity = Math.max(0, Math.min(1, Number(phaseWeights[i]) || 0));
                        const speedMultiplier = 1 / Math.max(
                            minFadeDriftDenominator,
                            minFadeDriftDenominator + (1 - minFadeDriftDenominator) * opacity
                        );
                        const offset = Array.isArray(driftState.offsets[i]) ? driftState.offsets[i] : [0, 0];
                        offset[0] = Number(offset[0]) || 0;
                        offset[1] = Number(offset[1]) || 0;
                        offset[0] += baseDriftX * speedMultiplier * driftDt;
                        offset[1] += baseDriftY * speedMultiplier * driftDt;
                        const previousOpacity = Number.isFinite(driftState.previousOpacities[i])
                            ? Math.max(0, Math.min(1, Number(driftState.previousOpacities[i])))
                            : 1;
                        if (opacity <= invisibleOpacityThreshold && previousOpacity > invisibleOpacityThreshold) {
                            const randomAngle = Math.random() * Math.PI * 2;
                            const randomDistance = 0.15 + Math.random() * 0.85;
                            offset[0] += Math.cos(randomAngle) * randomDistance;
                            offset[1] += Math.sin(randomAngle) * randomDistance;
                        }
                        driftState.previousOpacities[i] = opacity;
                        driftState.offsets[i] = offset;
                    }
                    const drift0 = driftState.offsets[0];
                    const drift1 = rotateUvDrift(driftState.offsets[1], 0.7986355, 0.6018150);
                    const drift2 = rotateUvDrift(driftState.offsets[2], 0.2756374, 0.9612617);
                    if (uniforms.uPhaseOffset0) {
                        uniforms.uPhaseOffset0[0] = drift0[0];
                        uniforms.uPhaseOffset0[1] = drift0[1];
                    }
                    if (uniforms.uPhaseOffset1) {
                        uniforms.uPhaseOffset1[0] = 0.37 + drift1[0];
                        uniforms.uPhaseOffset1[1] = 0.19 + drift1[1];
                    }
                    if (uniforms.uPhaseOffset2) {
                        uniforms.uPhaseOffset2[0] = 0.71 + drift2[0];
                        uniforms.uPhaseOffset2[1] = 0.43 + drift2[1];
                    }
                }
            }
            entry.mesh.alpha = 1;
            entry.mesh.tint = 0xffffff;
            entry.mesh.position.set(0, 0);
            entry.mesh.scale.set(1, 1);
            entry.mesh.visible = true;
            if (Object.prototype.hasOwnProperty.call(entry.mesh, "renderable")) {
                entry.mesh.renderable = true;
            }
            return true;
        }

        collectFloorVisualEntries(ctx) {
            const map = ctx && ctx.map;
            const selectedLevel = this.getSelectedFloorVisualLevel();
            const isolateLevel = isFloorEditIsolationActive();
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : Date.now();
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx, cutawayState);
            const activeInteriorFloorFragmentIds = this.getBuildingInteriorActiveFloorFragmentIds(ctx, cutawayState);
            const entries = [];
            const metrics = this.currentFrameMetrics || null;
            let scannedFragments = 0;
            let skippedInteriorScope = 0;
            let skippedLevelIsolation = 0;
            let skippedUneditedLevel0 = 0;
            let skippedNoSurface = 0;
            let skippedInvalidOuter = 0;
            let level0Entries = 0;
            let nonzeroEntries = 0;
            let level0Sections = new Set();
            if (!map) return entries;
            if (map.floorsById instanceof Map) {
                const fragmentEntries = (interiorScope && interiorScope.active && interiorScope.visibleFragmentIds.size > 0)
                    ? Array.from(interiorScope.visibleFragmentIds, fragmentId => [fragmentId, map.floorsById.get(fragmentId) || null])
                    : Array.from(map.floorsById.entries());
                if (interiorScope && interiorScope.active) {
                    skippedInteriorScope = Math.max(0, map.floorsById.size - fragmentEntries.length);
                }
                for (const [fragmentId, fragment] of fragmentEntries) {
                    scannedFragments += 1;
                    if (!fragment) continue;
                    if (fragment.renderedByBuildingCutaway === true) {
                        skippedLevelIsolation += 1;
                        continue;
                    }
                    if (!this.floorFragmentAllowedByBuildingInteriorScope(fragment, interiorScope)) {
                        skippedInteriorScope += 1;
                        continue;
                    }
                    const level = Number.isFinite(fragment.level) ? Math.round(Number(fragment.level)) : 0;
                    if (mazeLayerOnly && level !== wizardLayer) {
                        skippedLevelIsolation += 1;
                        continue;
                    }
                    if (isolateLevel && level !== selectedLevel) {
                        skippedLevelIsolation += 1;
                        continue;
                    }
                    const cutawayAlpha = this.getFloorFragmentCutawayAlpha(fragment, cutawayState);
                    if (!(cutawayAlpha > 0.001)) {
                        skippedLevelIsolation += 1;
                        continue;
                    }
                    const buildingCutawayTrigger = this.getFloorFragmentBuildingCutawayTrigger(fragment, cutawayState);
                    const buildingCutawayCompositeFrame = buildingCutawayTrigger
                        ? (Number(this._layerCutawayFrameId) || 0)
                        : 0;
                    const buildingCutawayCompositeAlpha = buildingCutawayTrigger && Number.isFinite(buildingCutawayTrigger.alpha)
                        ? Math.max(0, Math.min(1, Number(buildingCutawayTrigger.alpha)))
                        : BUILDING_CUTAWAY_GHOST_ALPHA;
                    const floorCutawayAlpha = buildingCutawayTrigger ? 1 : cutawayAlpha;
                    const activeInteriorDepthBump = activeInteriorFloorFragmentIds.has(fragmentId)
                        ? BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS
                        : 0;
                    const sectionKey = typeof fragment.ownerSectionKey === "string" ? fragment.ownerSectionKey : "";
                    const state = map._prototypeSectionState || null;
                    const asset = sectionKey && state && state.sectionAssetsByKey instanceof Map
                        ? state.sectionAssetsByKey.get(sectionKey)
                        : null;
                    if (fragment._cachedNormalizedOuterSource !== fragment.outerPolygon || !fragment._cachedNormalizedOuter) {
                        fragment._cachedNormalizedOuter = normalizeFloorVisualPointList(fragment.outerPolygon);
                        fragment._cachedNormalizedOuterSource = fragment.outerPolygon;
                    }
                    const outer = fragment._cachedNormalizedOuter;
                    if (outer.length < 3) {
                        skippedInvalidOuter += 1;
                        continue;
                    }
                    const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
                    let level0Surface = null;
                    if (level === 0) {
                        if (!FLOOR_LEVEL0_BAKED_SURFACE_ENABLED) continue;
                        if (!this.hasEditedLevel0FloorAsset(asset)) {
                            skippedUneditedLevel0 += 1;
                            continue;
                        }
                        const baseZ = Number.isFinite(fragment.nodeBaseZ)
                            ? Number(fragment.nodeBaseZ)
                            : this.getLayerBaseZForLevel(level);
                            const fadeMultiplier = this.getLiveLayerFadeMultiplier(level, nowMs) * floorCutawayAlpha;
                            if (FLOOR_LEVEL0_POLYGON_MATERIAL_ENABLED) {
                                const renderOuter = outer;
                                const texturePath = this.getLevel0TerrainMaterialPathForType(map, "grass") || FLOOR_LEVEL0_GRASS_MATERIAL_PATH;
                                entries.push({
                                key: `fragment:${fragmentId}:level0-material`,
                                level,
                                baseZ,
                                outer: renderOuter,
                                holes,
                                texture: null,
                                textureBounds: null,
                                textureRepeat: this.getLevel0TerrainMaterialRepeat(map, "grass"),
                                texturePath,
                                tint: 0xffffff,
                                alpha: fadeMultiplier,
                                depthBias: FLOOR_VISUAL_DEPTH_BIAS_UNITS - 0.005 + activeInteriorDepthBump,
                                isHoleOverlay: false
                            });
                            if (buildingCutawayCompositeFrame > 0) {
                                entries[entries.length - 1].buildingCutawayCompositeFrame = buildingCutawayCompositeFrame;
                                entries[entries.length - 1].buildingCutawayCompositeAlpha = buildingCutawayCompositeAlpha;
                            }
                            if (sectionKey) level0Sections.add(sectionKey);
                            level0Entries += 1;
                            continue;
                        }
                        const chunkEntries = this.collectLevel0ChunkFloorVisualEntries(
                            ctx,
                            fragmentId,
                            fragment,
                            asset,
                            outer,
                            holes,
                            baseZ,
                            fadeMultiplier
                        );
                        if (Array.isArray(chunkEntries)) {
                            if (buildingCutawayCompositeFrame > 0) {
                                for (let ce = 0; ce < chunkEntries.length; ce++) {
                                    if (chunkEntries[ce]) {
                                        chunkEntries[ce].buildingCutawayCompositeFrame = buildingCutawayCompositeFrame;
                                        chunkEntries[ce].buildingCutawayCompositeAlpha = buildingCutawayCompositeAlpha;
                                        if (activeInteriorDepthBump > 0) {
                                            const baseDepthBias = Number.isFinite(chunkEntries[ce].depthBias)
                                                ? Number(chunkEntries[ce].depthBias)
                                                : FLOOR_VISUAL_DEPTH_BIAS_UNITS;
                                            chunkEntries[ce].depthBias = baseDepthBias + activeInteriorDepthBump;
                                        }
                                    }
                                }
                            } else if (activeInteriorDepthBump > 0) {
                                for (let ce = 0; ce < chunkEntries.length; ce++) {
                                    if (!chunkEntries[ce]) continue;
                                    const baseDepthBias = Number.isFinite(chunkEntries[ce].depthBias)
                                        ? Number(chunkEntries[ce].depthBias)
                                        : FLOOR_VISUAL_DEPTH_BIAS_UNITS;
                                    chunkEntries[ce].depthBias = baseDepthBias + activeInteriorDepthBump;
                                }
                            }
                            if (chunkEntries.length === 0) {
                                skippedNoSurface += 1;
                            } else {
                                for (let ce = 0; ce < chunkEntries.length; ce++) entries.push(chunkEntries[ce]);
                                if (sectionKey) level0Sections.add(sectionKey);
                                level0Entries += chunkEntries.length;
                            }
                            continue;
                        }
                        level0Surface = this.getLevel0GroundSurfaceTexture(ctx, sectionKey, asset);
                        if (!level0Surface || !level0Surface.texture || !level0Surface.bounds) {
                            skippedNoSurface += 1;
                            continue;
                        }
                        if (sectionKey) level0Sections.add(sectionKey);
                    }
                    if (level === 0) level0Entries += 1;
                    else nonzeroEntries += 1;
                    const baseZ = Number.isFinite(fragment.nodeBaseZ)
                        ? Number(fragment.nodeBaseZ)
                        : this.getLayerBaseZForLevel(level);
                    const fadeMultiplier = this.getLiveLayerFadeMultiplier(level, nowMs) * floorCutawayAlpha;
                    const paintedTexturePath = (
                        level !== 0 &&
                        typeof fragment.texturePath === "string" &&
                        fragment.texturePath.length > 0
                    ) ? fragment.texturePath : "";
                    const visualTexturePath = paintedTexturePath ||
                        (level !== 0 ? FLOOR_VISUAL_CAVE_TEXTURE_PATH : `level0:${fragment.ownerSectionKey || ""}`);
                    const hasTexturedFill = visualTexturePath.length > 0;
                    const defaultAlpha = level === 0 ? 1 : (isolateLevel && level === selectedLevel ? 0.9 : 0.86);
                    const textureRepeat = visualTexturePath && !visualTexturePath.startsWith("level0:")
                        ? this.getFloorVisualTextureRepeat(visualTexturePath)
                        : null;
                    const renderOuter = level === 0
                        ? expandFloorVisualPolygonFromCentroid(outer, FLOOR_LEVEL0_SEAM_BLEED_UNITS)
                        : outer;
                    entries.push({
                        key: `fragment:${fragmentId}`,
                        level,
                        baseZ,
                        outer: renderOuter,
                        holes,
                        texture: level0Surface ? level0Surface.texture : null,
                        textureBounds: level0Surface ? level0Surface.bounds : null,
                        textureRepeat,
                        texturePath: visualTexturePath,
                        tint: this.getLayerDarkenedTint(
                            hasTexturedFill ? 0xffffff : FLOOR_VISUAL_UPPER_FILL,
                            level
                        ),
                        alpha: ((paintedTexturePath || level !== 0) ? 1 : defaultAlpha) * fadeMultiplier,
                        depthBias: (level === 0 ? (FLOOR_VISUAL_DEPTH_BIAS_UNITS - 0.005) : FLOOR_VISUAL_DEPTH_BIAS_UNITS) + activeInteriorDepthBump,
                        isHoleOverlay: false
                    });
                    if (buildingCutawayCompositeFrame > 0) {
                        entries[entries.length - 1].buildingCutawayCompositeFrame = buildingCutawayCompositeFrame;
                        entries[entries.length - 1].buildingCutawayCompositeAlpha = buildingCutawayCompositeAlpha;
                    }
                    const normalizedHoles = Array.isArray(holes) ? holes : [];
                    for (let h = 0; h < normalizedHoles.length; h++) {
                        const holeOuter = normalizeFloorVisualPointList(normalizedHoles[h]);
                        if (holeOuter.length < 3) continue;
                        if (this.isFloorHoleCoveredByStraightStair(level, holeOuter, map)) continue;
                        entries.push({
                            key: `fragment:${fragmentId}:hole:${h}`,
                            level,
                            baseZ,
                            outer: holeOuter,
                            holes: [],
                            texture: null,
                            textureBounds: null,
                            textureRepeat: null,
                            texturePath: "",
                            tint: this.getLayerDarkenedTint(FLOOR_VISUAL_HOLE_FILL, level),
                            alpha: fadeMultiplier,
                            depthBias: FLOOR_VISUAL_HOLE_DEPTH_BIAS_UNITS + activeInteriorDepthBump,
                            isHoleOverlay: true
                        });
                        if (buildingCutawayCompositeFrame > 0) {
                            entries[entries.length - 1].buildingCutawayCompositeFrame = buildingCutawayCompositeFrame;
                            entries[entries.length - 1].buildingCutawayCompositeAlpha = buildingCutawayCompositeAlpha;
                        }
                    }
                    if (normalizedHoles.length > 0 && typeof fragment.buildingId === "string") {
                        const buildings = map.ensureFloorBuildings();
                        const bldg = buildings instanceof Map ? buildings.get(fragment.buildingId) : null;
                        const visibleBelow = bldg && bldg.holeVisibleFragments instanceof Map
                            ? bldg.holeVisibleFragments.get(fragmentId)
                            : null;
                        if (visibleBelow instanceof Set) {
                            for (const belowId of visibleBelow) {
                                const belowFrag = map.floorsById.get(belowId);
                                if (!belowFrag) continue;
                                const belowLevel = Number.isFinite(belowFrag.level) ? Math.round(Number(belowFrag.level)) : 0;
                                const belowBaseZ = Number.isFinite(belowFrag.nodeBaseZ)
                                    ? Number(belowFrag.nodeBaseZ)
                                    : this.getLayerBaseZForLevel(belowLevel);
                                if (belowFrag._cachedNormalizedOuterSource !== belowFrag.outerPolygon || !belowFrag._cachedNormalizedOuter) {
                                    belowFrag._cachedNormalizedOuter = normalizeFloorVisualPointList(belowFrag.outerPolygon);
                                    belowFrag._cachedNormalizedOuterSource = belowFrag.outerPolygon;
                                }
                                const belowOuter = belowFrag._cachedNormalizedOuter;
                                if (belowOuter.length < 3) continue;
                                const belowHoles = Array.isArray(belowFrag.holes) ? belowFrag.holes : [];
                                const belowTexturePath = typeof belowFrag.texturePath === "string" && belowFrag.texturePath.length > 0
                                    ? belowFrag.texturePath
                                    : FLOOR_VISUAL_CAVE_TEXTURE_PATH;
                                const belowTextureRepeat = this.getFloorVisualTextureRepeat(belowTexturePath);
                                const belowFadeMultiplier = this.getLiveLayerFadeMultiplier(belowLevel, nowMs) * floorCutawayAlpha;
                                entries.push({
                                    key: `fragment:${belowId}:through:${fragmentId}`,
                                    level: belowLevel,
                                    baseZ: belowBaseZ,
                                    outer: belowOuter,
                                    holes: belowHoles,
                                    texture: null,
                                    textureBounds: null,
                                    textureRepeat: belowTextureRepeat,
                                    texturePath: belowTexturePath,
                                    tint: this.getLayerDarkenedTint(0xffffff, belowLevel),
                                    alpha: belowFadeMultiplier,
                                    depthBias: FLOOR_VISUAL_DEPTH_BIAS_UNITS,
                                    isHoleOverlay: false,
                                    visibleThroughFragmentId: fragmentId
                                });
                                if (buildingCutawayCompositeFrame > 0) {
                                    entries[entries.length - 1].buildingCutawayCompositeFrame = buildingCutawayCompositeFrame;
                                    entries[entries.length - 1].buildingCutawayCompositeAlpha = buildingCutawayCompositeAlpha;
                                }
                            }
                        }
                    }
                }
            }
            const viewportBounds = this.getCameraScreenSpaceWorldBoundsAtZ(ctx, 4, this.getLayerBaseZForLevel(0));
            const viewportSectionKeys = this.collectViewportFloorSectionKeys(ctx, map, viewportBounds);
            const sectionTerrainEntries = this.collectLevel0SectionTerrainPolygonFloorVisualEntries(
                ctx,
                viewportSectionKeys,
                nowMs
            );
            for (let i = 0; i < sectionTerrainEntries.length; i++) {
                const entry = sectionTerrainEntries[i];
                if (!entry) continue;
                entries.push(entry);
                if (typeof entry.key === "string") {
                    const match = /^fragment:section:([^:]+):terrain:/.exec(entry.key);
                    if (match && match[1]) level0Sections.add(match[1]);
                }
            }
            level0Entries += sectionTerrainEntries.length;
            if (metrics) {
                metrics.floorFragmentsScanned = scannedFragments;
                metrics.floorFragmentsSkippedInteriorScope = skippedInteriorScope;
                metrics.floorFragmentsSkippedLevelIsolation = skippedLevelIsolation;
                metrics.floorFragmentsSkippedUneditedLevel0 = skippedUneditedLevel0;
                metrics.floorFragmentsSkippedNoSurface = skippedNoSurface;
                metrics.floorFragmentsSkippedInvalidOuter = skippedInvalidOuter;
                metrics.floorLevel0Entries = level0Entries;
                metrics.floorNonzeroEntries = nonzeroEntries;
                metrics.floorLevel0Sections = level0Sections.size;
                metrics.floorEntriesCollected = entries.length;
            }
            entries.sort((a, b) => {
                const levelA = Number.isFinite(a && a.level) ? Number(a.level) : 0;
                const levelB = Number.isFinite(b && b.level) ? Number(b.level) : 0;
                if (levelA !== levelB) return levelA - levelB;
                const sortA = Number.isFinite(a && a.floorVisualSortOrder) ? Number(a.floorVisualSortOrder) : 0;
                const sortB = Number.isFinite(b && b.floorVisualSortOrder) ? Number(b.floorVisualSortOrder) : 0;
                if (sortA !== sortB) return sortA - sortB;
                return String(a && a.key).localeCompare(String(b && b.key));
            });
            return entries;
        }

        renderFloorVisualPolygons(ctx) {
            const container = this.ensureFloorVisualContainer();
            if (!container) return;
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const _pnow = diagnosticsEnabled ? performance.now.bind(performance) : null;
            this.level0GroundSurfaceChunkBuildsThisFrame = 0;
            this.updateFloorVisualContainerTransform(container);
            const collectStartMs = _pnow ? _pnow() : 0;
            const entries = this.collectFloorVisualEntries(ctx);
            const collectMs = _pnow ? (_pnow() - collectStartMs) : 0;
            const visibleKeys = new Set();
            let rendered = 0;
            let meshesCreated = 0;
            let geometryUploads = 0;
            let visibleVertices = 0;
            let visibleTriangles = 0;
            const previousVisibleKeys = this.floorVisualVisibleKeys || new Set();
            const entryLoopStartMs = _pnow ? _pnow() : 0;
            for (let i = 0; i < entries.length; i++) {
                const source = entries[i];
                // source.outer is already normalized by collectFloorVisualEntries
                const outer = source.outer;
                if (outer.length < 3) continue;
                const cacheKey = source.key;
                let entry = this.floorVisualMeshByKey.get(cacheKey);
                const signature = [
                    buildFloorVisualSignature(outer, source.holes),
                    `edgeFadeClip:${source.edgeFadeClipSignature || ""}`,
                    `edgeFadeSkip:${buildFloorVisualClipBoundarySignature(source.edgeFadeSkipRings)}`,
                    `skipClipBoundaryFade:${source.skipClipBoundaryFade === true ? 1 : 0}`
                ].join(":");
                source._grassBladeMaskSignature = [
                    typeof source.key === "string" ? source.key : "",
                    Number.isFinite(source.baseZ) ? Number(source.baseZ).toFixed(4) : "NaN",
                    signature
                ].join("|");
                if (
                    !entry ||
                    entry.signature !== signature ||
                    entry.isHoleOverlay !== source.isHoleOverlay ||
                    entry.isAnimatedWater !== (source.isAnimatedWater === true) ||
                    entry.texturePath !== (source.texturePath || "")
                ) {
                    if (entry && entry.mesh) {
                        this.destroyCachedPixiMesh(entry.mesh, "floor visual mesh");
                    }
                    const triangulation = Number.isFinite(source.edgeFadeWidth) && Number(source.edgeFadeWidth) > 0
                        ? triangulateFloorVisualTerrainBlendPolygon(outer, source.holes, Number(source.edgeFadeWidth), {
                            edgeFadeClipRings: source.edgeFadeClipRings,
                            edgeFadeSkipRings: source.edgeFadeSkipRings,
                            skipClipBoundaryFade: source.skipClipBoundaryFade === true,
                            edgeFadeDiagnosticContext: {
                                ...(source.edgeFadeDiagnosticContext || {}),
                                entryKey: cacheKey,
                                terrainType: source.terrainType || (source.edgeFadeDiagnosticContext && source.edgeFadeDiagnosticContext.terrainType) || ""
                            }
                        })
                        : triangulateFloorVisualPolygon(outer, source.holes);
                    if (!triangulation) continue;
                    entry = {
                        signature,
                        triangulation,
                        mesh: null,
                        tint: source.tint,
                        alpha: source.alpha,
                        baseZ: Number.isFinite(source.baseZ) ? Number(source.baseZ) : this.getLayerBaseZForLevel(source.level),
                        texture: source.texture || null,
                        textureBounds: source.textureBounds || null,
                        textureRepeat: source.textureRepeat || null,
                        texturePath: source.texturePath || "",
                        depthBias: Number.isFinite(source.depthBias) ? Number(source.depthBias) : FLOOR_VISUAL_DEPTH_BIAS_UNITS,
                        isHoleOverlay: source.isHoleOverlay,
                        isAnimatedWater: source.isAnimatedWater === true,
                        animationNowMs: Number(source.animationNowMs) || 0,
                        zIndex: 0,
                        uploadedGeometrySignature: "",
                        uploadedTextureBoundsSignature: "",
                        uploadedTextureRepeatSignature: ""
                    };
                    entry.mesh = this.createFloorVisualMesh(entry);
                    if (!entry.mesh) continue;
                    this.uploadFloorVisualMeshGeometry(entry);
                    geometryUploads += 1;
                    container.addChild(entry.mesh);
                    this.floorVisualMeshByKey.set(cacheKey, entry);
                    meshesCreated += 1;
                }
                const assignStartMs = 0; // removed per-entry timing (was ~300 performance.now() calls/frame)
                entry.signature = signature;
                entry.tint = source.tint;
                entry.alpha = source.alpha;
                entry.baseZ = Number.isFinite(source.baseZ) ? Number(source.baseZ) : this.getLayerBaseZForLevel(source.level);
                entry.isHoleOverlay = source.isHoleOverlay;
                entry.texture = source.texture || null;
                entry.textureBounds = source.textureBounds || null;
                entry.textureRepeat = source.textureRepeat || null;
                entry.texturePath = source.texturePath || "";
                entry.depthBias = Number.isFinite(source.depthBias) ? Number(source.depthBias) : FLOOR_VISUAL_DEPTH_BIAS_UNITS;
                entry.isAnimatedWater = source.isAnimatedWater === true;
                entry.animationNowMs = Number(source.animationNowMs) || 0;
                entry.buildingCutawayCompositeFrame = Number(source.buildingCutawayCompositeFrame) || 0;
                entry.buildingCutawayCompositeAlpha = Number.isFinite(source.buildingCutawayCompositeAlpha)
                    ? Math.max(0, Math.min(1, Number(source.buildingCutawayCompositeAlpha)))
                    : 1;
                entry.zIndex = (Number.isFinite(source.level) ? Number(source.level) : 0) * 100000 + i;
                if (entry.mesh && entry.mesh.parent !== container) {
                    container.addChild(entry.mesh);
                }
                if (entry.mesh) {
                    entry.mesh._buildingCutawayCompositeFrame = entry.buildingCutawayCompositeFrame;
                    entry.mesh._buildingCutawayCompositeAlpha = entry.buildingCutawayCompositeAlpha;
                    const compositeAlpha = Number.isFinite(entry.buildingCutawayCompositeAlpha)
                        ? Math.max(0, Math.min(1, Number(entry.buildingCutawayCompositeAlpha)))
                        : 1;
                    const entryAlpha = Number.isFinite(entry.alpha)
                        ? Math.max(0, Math.min(1, Number(entry.alpha)))
                        : 1;
                    entry.mesh._buildingCutawayCompositeCaptureAlpha = compositeAlpha > 1e-6
                        ? Math.max(0, Math.min(1, entryAlpha / compositeAlpha))
                        : entryAlpha;
                }
                if (entry.mesh && entry.mesh.zIndex !== entry.zIndex) {
                    entry.mesh.zIndex = entry.zIndex;
                }
                if (entry.mesh) entry.mesh.y = 0;
                if (
                    entry.uploadedGeometrySignature !== signature ||
                    entry.uploadedTextureBoundsSignature !== this.getFloorVisualTextureBoundsSignature(entry.textureBounds) ||
                    entry.uploadedTextureRepeatSignature !== this.getFloorVisualTextureRepeatSignature(entry.textureRepeat)
                ) {
                    this.uploadFloorVisualMeshGeometry(entry);
                    geometryUploads += 1;
                }
                if (this.updateFloorVisualMesh(entry)) {
                    visibleKeys.add(cacheKey);
                    rendered += 1;
                    if (entry.triangulation) {
                        visibleVertices += Number(entry.triangulation.vertexCount) || 0;
                        visibleTriangles += entry.triangulation.indices ? Math.floor(entry.triangulation.indices.length / 3) : 0;
                    }
                }
            }
            const entryLoopMs = _pnow ? (_pnow() - entryLoopStartMs) : 0;
            const cachedMeshes = this.floorVisualMeshByKey.size;
            const hideStartMs = _pnow ? _pnow() : 0;
            for (const key of previousVisibleKeys) {
                if (!visibleKeys.has(key)) {
                    const entry = this.floorVisualMeshByKey.get(key);
                    if (entry && entry.mesh) entry.mesh.visible = false;
                }
            }
            const hideMs = _pnow ? (_pnow() - hideStartMs) : 0;
            this.floorVisualVisibleKeys = visibleKeys;
            const trimStartMs = _pnow ? _pnow() : 0;
            const trimmedLevel0Chunks = this.trimLevel0GroundSurfaceChunkCache(
                Math.max(FLOOR_LEVEL0_CHUNK_CACHE_LIMIT, visibleKeys.size)
            );
            const trimmedAnimatedWaterChunks = this.trimLevel0AnimatedWaterChunkEntryCache(
                Math.max(FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4, visibleKeys.size * 2)
            );
            const trimmedChunkClips = this.trimFloorVisualChunkClipCache(
                Math.max(FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4, visibleKeys.size * 2)
            );
            const trimmedTerrainEntries = this.trimLevel0TerrainPolygonEntryCache(
                Math.max(FLOOR_LEVEL0_CHUNK_CACHE_LIMIT * 4, visibleKeys.size * 2)
            );
            const trimMs = _pnow ? (_pnow() - trimStartMs) : 0;
            this.renderGrassDepthEffect(ctx, entries);
            this.renderTerrainPolygonDiagnosticOverlay(ctx, entries);
            this.setFrameMetric("floorVisualPolygons", rendered);
            this.setFrameMetric("floorVisualMeshesCreated", meshesCreated);
            this.setFrameMetric("floorVisualGeometryUploads", geometryUploads);
            this.setFrameMetric("floorVisualCollectMs", collectMs);
            this.setFrameMetric("floorVisualMeshUpdateMs", entryLoopMs);
            this.setFrameMetric("floorVisualHideMs", hideMs);
            this.setFrameMetric("floorVisualTrimMs", trimMs);
            this.setFrameMetric("floorVisualVertices", visibleVertices);
            this.setFrameMetric("floorVisualTriangles", visibleTriangles);
            this.setFrameMetric("floorVisualMeshCacheSize", cachedMeshes);
            this.setFrameMetric(
                "floorVisualChunkClipCacheSize",
                this.floorVisualChunkClipCache instanceof Map ? this.floorVisualChunkClipCache.size : 0
            );
            this.setFrameMetric("floorVisualChunkClipsTrimmed", trimmedChunkClips);
            this.setFrameMetric(
                "floorTerrainPolygonEntryCacheSize",
                this.level0TerrainPolygonEntryCache instanceof Map ? this.level0TerrainPolygonEntryCache.size : 0
            );
            this.setFrameMetric("floorTerrainPolygonEntriesTrimmed", trimmedTerrainEntries);
            this.setFrameMetric(
                "floorAnimatedWaterChunkCacheSize",
                this.level0AnimatedWaterChunkEntryCache instanceof Map ? this.level0AnimatedWaterChunkEntryCache.size : 0
            );
            this.setFrameMetric("floorAnimatedWaterChunksTrimmed", trimmedAnimatedWaterChunks);
            this.setFrameMetric(
                "floorLevel0ChunkCacheSize",
                this.level0GroundSurfaceChunkCache instanceof Map ? this.level0GroundSurfaceChunkCache.size : 0
            );
            this.setFrameMetric("floorLevel0ChunksTrimmed", trimmedLevel0Chunks);
        }

        clearTerrainPolygonDiagnosticOverlay() {
            const debugRenderer = global.RenderingTerrainPaintDebugRenderer;
            if (debugRenderer && typeof debugRenderer.clear === "function") {
                debugRenderer.clear(this);
                return;
            }
            if (!this.terrainPolygonDiagnosticGraphics) return;
            this.terrainPolygonDiagnosticGraphics.clear();
            this.terrainPolygonDiagnosticGraphics.visible = false;
        }

        renderTerrainPolygonDiagnosticOverlay(ctx, entries) {
            const debugRenderer = global.RenderingTerrainPaintDebugRenderer;
            if (
                !debugRenderer ||
                typeof debugRenderer.render !== "function" ||
                typeof debugRenderer.isEnabled !== "function" ||
                !debugRenderer.isEnabled()
            ) {
                this.clearTerrainPolygonDiagnosticOverlay();
                return;
            }
            debugRenderer.render(this, ctx, entries);
        }

        renderGrassDepthEffect(ctx, entries) {
            const api = global.RenderingGrassBlades;
            const requested = global.renderingGrassBladesEnabled === true || global.renderingGrassDepthEnabled === true;
            if (requested && (!api || typeof api.Renderer !== "function")) {
                throw new Error("grass blade effect is enabled but RenderingGrassBlades module is missing");
            }
            const enabled = !!(api && typeof api.isEnabled === "function" && api.isEnabled());
            if (!enabled) {
                if (this.grassDepthRenderer && typeof this.grassDepthRenderer.hide === "function") {
                    this.grassDepthRenderer.hide();
                }
                this.setFrameMetric("grassDepthRendered", 0);
                this.setFrameMetric("grassDepthRoots", 0);
                this.setFrameMetric("grassDepthMs", 0);
                this.setFrameMetric("grassDepthMaskMs", 0);
                this.setFrameMetric("grassDepthMaskRebuilt", 0);
                this.setFrameMetric("grassDepthLosShadow", 0);
                this.setFrameMetric("grassDepthWizardShadow", 0);
                this.setFrameMetric("grassDepthLayers", 0);
                this.setFrameMetric("grassDepthChunks", 0);
                this.setFrameMetric("grassDepthChunksBuilt", 0);
                this.setFrameMetric("grassDepthChunksPending", 0);
                this.setFrameMetric("grassDepthBlades", 0);
                this.setFrameMetric("grassDepthMaskEntries", 0);
                this.setFrameMetric("grassDepthMaskPlanMs", 0);
                this.setFrameMetric("grassDepthChunkEntryRefs", 0);
                this.setFrameMetric("grassDepthMaskIndexRebuilt", 0);
                this.setFrameMetric("grassDepthMaskIndexedChunks", 0);
                return false;
            }
            if (!this.grassDepthRenderer) {
                this.grassDepthRenderer = new api.Renderer();
            }
            const perfNow = this.currentFrameMetrics &&
                typeof performance !== "undefined" &&
                performance &&
                typeof performance.now === "function"
                ? performance.now.bind(performance)
                : null;
            const startMs = perfNow ? perfNow() : 0;
            const result = this.grassDepthRenderer.render(this, ctx, entries);
            const elapsedMs = perfNow ? (perfNow() - startMs) : 0;
            this.setFrameMetric("grassDepthRendered", Number(result && result.rendered) || 0);
            this.setFrameMetric("grassDepthRoots", Number(result && result.roots) || 0);
            this.setFrameMetric("grassDepthMs", elapsedMs);
            this.setFrameMetric("grassDepthMaskMs", Number(result && result.maskMs) || 0);
            this.setFrameMetric("grassDepthMaskRebuilt", Number(result && result.maskRebuilt) || 0);
            this.setFrameMetric("grassDepthLosShadow", Number(result && result.losShadow) || 0);
            this.setFrameMetric("grassDepthWizardShadow", Number(result && result.wizardShadow) || 0);
            this.setFrameMetric("grassDepthLayers", Number(result && result.layers) || 0);
            this.setFrameMetric("grassDepthChunks", Number(result && result.chunks) || 0);
            this.setFrameMetric("grassDepthChunksBuilt", Number(result && result.chunksBuilt) || 0);
            this.setFrameMetric("grassDepthChunksPending", Number(result && result.chunksPending) || 0);
            this.setFrameMetric("grassDepthBlades", Number(result && result.blades) || 0);
            this.setFrameMetric("grassDepthMaskEntries", Number(result && result.maskEntries) || 0);
            this.setFrameMetric("grassDepthMaskPlanMs", Number(result && result.maskPlanMs) || 0);
            this.setFrameMetric("grassDepthChunkEntryRefs", Number(result && result.chunkEntryRefs) || 0);
            this.setFrameMetric("grassDepthMaskIndexRebuilt", Number(result && result.maskIndexRebuilt) || 0);
            this.setFrameMetric("grassDepthMaskIndexedChunks", Number(result && result.maskIndexedChunks) || 0);
            return !!(result && result.rendered);
        }

        renderRoadsAndFloors(ctx, visibleNodes) {
            const map = ctx.map;
            const cam = this.camera;
            const container = this.ensureRoadSpriteDepthContainer();
            if (!map || !Array.isArray(map.nodes) || !container) return;
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const functionStartMs = diagnosticsEnabled ? performance.now() : 0;
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs))
                ? Number(ctx.renderNowMs)
                : ((typeof performance !== "undefined" && performance && typeof performance.now === "function")
                    ? performance.now()
                    : Date.now());
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mazeLayerOnly = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            const hiddenSpriteGraceMs = 500;
            const maxHiddenSprites = 96;
            let roadSpritesCreated = 0;
            let roadSpritesAttached = 0;
            let roadTextureRefreshes = 0;
            let roadTextureAssignments = 0;
            let roadHiddenSprites = 0;
            let roadDestroyedSprites = 0;
            let roadEvictedSprites = 0;

            const destroyRoadSprite = (road, sprite) => {
                if (isRoadTextureLifecycleDiagnosticsEnabled()) {
                    console.warn("[road render sprite destroy]", {
                        roadId: Number.isInteger(road && road._prototypeRecordId) ? Number(road._prototypeRecordId) : null,
                        roadGone: !!(road && road.gone),
                        roadTextureCacheKey: (road && typeof road._roadTextureCacheKey === "string") ? road._roadTextureCacheKey : "",
                        sprite: summarizePixiDisplayObject(sprite)
                    });
                }
                if (sprite) {
                    if (sprite.destroyed !== true) {
                        syncRoadRenderSpriteTextureRetention(sprite, null);
                    }
                    if (sprite.parent) {
                        sprite.parent.removeChild(sprite);
                    }
                    if (sprite.destroyed !== true && typeof sprite.destroy === "function") {
                        // Pixi's destroy() crashes if _texture is null; restore a safe sentinel first.
                        if (!sprite._texture) { sprite._texture = PIXI.Texture.EMPTY; }
                        sprite.destroy({ children: false, texture: false, baseTexture: false });
                    }
                }
                this.roadSpriteByObject.delete(road);
                roadDestroyedSprites += 1;
            };
            const destroyRoadPathMesh = (roadPath, mesh) => {
                if (mesh) {
                    if (mesh.parent) {
                        mesh.parent.removeChild(mesh);
                    }
                    if (roadPath && roadPath._renderingDisplayObject === mesh) {
                        roadPath._renderingDisplayObject = null;
                    }
                    if (
                        mesh.destroyed !== true &&
                        typeof mesh.destroy === "function" &&
                        mesh.geometry &&
                        Number.isFinite(mesh.geometry.refCount)
                    ) {
                        try {
                            mesh.destroy({ children: false, texture: false, baseTexture: false });
                        } catch (error) {
                            const message = error && error.message ? String(error.message) : "";
                            if (!message.includes("refCount")) throw error;
                            mesh.geometry = null;
                            mesh.shader = null;
                            mesh.state = null;
                        }
                    } else {
                        mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                            mesh.renderable = false;
                        }
                    }
                }
                if (this.roadPathMeshByObject instanceof Map) {
                    this.roadPathMeshByObject.delete(roadPath);
                }
            };
            const getRoadPathFillTexturePath = (roadPath) => {
                const RoadClass = (typeof Road !== "undefined" && Road)
                    ? Road
                    : ((global && global.Road) ? global.Road : null);
                const defaultTexturePath = RoadClass && typeof RoadClass._defaultFillTexturePath === "string"
                    ? RoadClass._defaultFillTexturePath
                    : "/assets/images/flooring/dirt.jpg";
                const normalizeTexturePath = (texturePath) => {
                    if (typeof texturePath !== "string" || texturePath.length === 0) return "";
                    if (RoadClass && typeof RoadClass._normalizeFillTexturePath === "function") {
                        return RoadClass._normalizeFillTexturePath(texturePath);
                    }
                    return texturePath;
                };
                const defaultNormalized = normalizeTexturePath(defaultTexturePath) || defaultTexturePath;
                const selectedTexturePath = wizard && typeof wizard.selectedFlooringTexture === "string" && wizard.selectedFlooringTexture.length > 0
                    ? normalizeTexturePath(wizard.selectedFlooringTexture)
                    : "";
                const objectTexturePath = roadPath && typeof roadPath.fillTexturePath === "string" && roadPath.fillTexturePath.length > 0
                    ? normalizeTexturePath(roadPath.fillTexturePath)
                    : "";
                if (
                    selectedTexturePath &&
                    selectedTexturePath !== defaultNormalized &&
                    (!objectTexturePath || objectTexturePath === defaultNormalized)
                ) {
                    return selectedTexturePath;
                }
                return objectTexturePath || selectedTexturePath || defaultNormalized;
            };
            const getRoadPathFillTexture = (texturePath) => {
                const texture = this.getFloorVisualTexture({ texturePath });
                const baseTexture = texture && texture.baseTexture ? texture.baseTexture : null;
                if (baseTexture) {
                    if (PIXI.WRAP_MODES) baseTexture.wrapMode = PIXI.WRAP_MODES.REPEAT;
                    if (PIXI.SCALE_MODES) baseTexture.scaleMode = PIXI.SCALE_MODES.LINEAR;
                    if (
                        baseTexture &&
                        baseTexture.valid !== true &&
                        typeof baseTexture.once === "function" &&
                        baseTexture._roadPathTextureRefreshPending !== true
                    ) {
                        baseTexture._roadPathTextureRefreshPending = true;
                        baseTexture.once("loaded", () => {
                            baseTexture._roadPathTextureRefreshPending = false;
                            if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                                globalThis.presentGameFrame();
                            }
                        });
                    }
                }
                return texture || PIXI.Texture.WHITE;
            };
            const getRoadPathTextureRepeat = (texturePath) => {
                const repeat = this.getFloorVisualTextureRepeat(texturePath);
                return {
                    x: repeat && Number.isFinite(repeat.x) ? Number(repeat.x) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE,
                    y: repeat && Number.isFinite(repeat.y) ? Number(repeat.y) : FLOOR_VISUAL_TEXTURE_WORLD_SCALE
                };
            };
            const resolveRoadPathFillTexturePathForView = (roadPath, texturePath) => {
                const RoadClass = (typeof Road !== "undefined" && Road)
                    ? Road
                    : ((global && global.Road) ? global.Road : null);
                if (!RoadClass || typeof RoadClass.resolveFillTexturePathForSize !== "function") {
                    return texturePath;
                }
                const roadWidth = Number.isFinite(roadPath && roadPath.roadWidth)
                    ? Math.max(0.001, Number(roadPath.roadWidth))
                    : (Number.isFinite(roadPath && roadPath.width) ? Math.max(0.001, Number(roadPath.width)) : 1);
                const roadScreenWidth = roadWidth * (Number(cam.viewscale) || 1) * 1.1547;
                const roadScreenHeight = roadWidth * (Number(cam.viewscale) || 1) * (Number(cam.xyratio) || 1);
                const lodMetric = typeof RoadClass.getFillTextureLodMetric === "function"
                    ? RoadClass.getFillTextureLodMetric(texturePath, roadScreenWidth, roadScreenHeight)
                    : Math.max(roadScreenWidth, roadScreenHeight);
                return RoadClass.resolveFillTexturePathForSize(texturePath, lodMetric);
            };
            const getRoadPathEdgeFadeWorldUnits = () => {
                const RoadClass = (typeof Road !== "undefined" && Road)
                    ? Road
                    : ((global && global.Road) ? global.Road : null);
                if (
                    !RoadClass ||
                    !Number.isFinite(RoadClass._edgeFadePx) ||
                    !Number.isFinite(RoadClass._pixelsPerWorldUnit) ||
                    !(RoadClass._pixelsPerWorldUnit > 0)
                ) {
                    throw new Error("Cannot render road path alpha fade without road edge fade metrics.");
                }
                return Math.max(0.0001, Number(RoadClass._edgeFadePx) / Number(RoadClass._pixelsPerWorldUnit));
            };
            const buildRoadPathMeshData = (roadPath, segments, textureRepeat, endpointContinuity = null) => {
                const repeatX = textureRepeat && Number.isFinite(textureRepeat.x)
                    ? Number(textureRepeat.x)
                    : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
                const repeatY = textureRepeat && Number.isFinite(textureRepeat.y)
                    ? Number(textureRepeat.y)
                    : FLOOR_VISUAL_TEXTURE_WORLD_SCALE;
                const edgeFadeWorldUnits = getRoadPathEdgeFadeWorldUnits();
                const maxUvSpanPerQuad = 0.5;
                const emitted = [];
                let pathDistance = 0;
                const suppressStartFade = !!(endpointContinuity && endpointContinuity.startConnected);
                const suppressEndFade = !!(endpointContinuity && endpointContinuity.endConnected);
                const lerpPoint = (from, to, t) => ({
                    x: Number(from.x) + (Number(to.x) - Number(from.x)) * t,
                    y: Number(from.y) + (Number(to.y) - Number(from.y)) * t
                });
                let totalPathLength = 0;
                for (let s = 0; s < segments.length; s++) {
                    const segment = segments[s];
                    const polygon = segment && Array.isArray(segment.polygon) ? segment.polygon : null;
                    if (!polygon || polygon.length !== 4) {
                        throw new Error(`Cannot render road path segment ${s} without four-point polygon geometry.`);
                    }
                    const leftStart = polygon[0];
                    const leftEnd = polygon[1];
                    const rightEnd = polygon[2];
                    const rightStart = polygon[3];
                    const startPoint = segment && segment.startPoint ? segment.startPoint : {
                        x: (Number(leftStart.x) + Number(rightStart.x)) * 0.5,
                        y: (Number(leftStart.y) + Number(rightStart.y)) * 0.5
                    };
                    const endPoint = segment && segment.endPoint ? segment.endPoint : {
                        x: (Number(leftEnd.x) + Number(rightEnd.x)) * 0.5,
                        y: (Number(leftEnd.y) + Number(rightEnd.y)) * 0.5
                    };
                    const length = Math.hypot(
                        Number(endPoint.x) - Number(startPoint.x),
                        Number(endPoint.y) - Number(startPoint.y)
                    );
                    if (!(length > 0)) {
                        throw new Error(`Cannot render road path segment ${s} with zero visual length.`);
                    }
                    totalPathLength += length;
                }
                if (!(totalPathLength > 0)) {
                    throw new Error("Cannot render road path with zero total visual length.");
                }
                const addSortedUniqueCut = (cuts, value) => {
                    if (!Number.isFinite(value)) {
                        throw new Error("Cannot render road path mesh with non-finite fade cut.");
                    }
                    const clamped = Math.max(0, Math.min(1, Number(value)));
                    for (let i = 0; i < cuts.length; i++) {
                        if (Math.abs(cuts[i] - clamped) < 1e-5) return;
                    }
                    cuts.push(clamped);
                    cuts.sort((a, b) => a - b);
                };
                const computeRoadPathEdgeAlpha = (widthAtPoint, crossT, distanceAlongPath) => {
                    if (!Number.isFinite(widthAtPoint) || !(widthAtPoint > 0)) {
                        throw new Error("Cannot render road path alpha fade with invalid segment width.");
                    }
                    if (!Number.isFinite(distanceAlongPath)) {
                        throw new Error("Cannot render road path alpha fade with non-finite path distance.");
                    }
                    const lateralDistance = Math.min(crossT, 1 - crossT) * widthAtPoint;
                    let endDistance = Infinity;
                    if (!suppressStartFade) endDistance = Math.min(endDistance, distanceAlongPath);
                    if (!suppressEndFade) endDistance = Math.min(endDistance, totalPathLength - distanceAlongPath);
                    return Math.max(0, Math.min(1, Math.min(lateralDistance, endDistance) / edgeFadeWorldUnits));
                };
                const emitVertex = (point, u, v, edgeAlpha) => {
                    if (!point || !Number.isFinite(point.x) || !Number.isFinite(point.y)) {
                        throw new Error("Cannot render road path mesh with non-finite point.");
                    }
                    if (!Number.isFinite(u) || !Number.isFinite(v)) {
                        throw new Error("Cannot render road path mesh with non-finite texture coordinates.");
                    }
                    if (!Number.isFinite(edgeAlpha)) {
                        throw new Error("Cannot render road path mesh with non-finite edge alpha.");
                    }
                    emitted.push({ x: Number(point.x), y: Number(point.y), u, v, edgeAlpha: Math.max(0, Math.min(1, Number(edgeAlpha))) });
                };
                const emitQuad = (v00, v10, v11, v01) => {
                    emitVertex(v00.point, v00.u, v00.v, v00.edgeAlpha);
                    emitVertex(v10.point, v10.u, v10.v, v10.edgeAlpha);
                    emitVertex(v11.point, v11.u, v11.v, v11.edgeAlpha);
                    emitVertex(v00.point, v00.u, v00.v, v00.edgeAlpha);
                    emitVertex(v11.point, v11.u, v11.v, v11.edgeAlpha);
                    emitVertex(v01.point, v01.u, v01.v, v01.edgeAlpha);
                };
                for (let s = 0; s < segments.length; s++) {
                    const segment = segments[s];
                    const polygon = segment && Array.isArray(segment.polygon) ? segment.polygon : null;
                    if (!polygon || polygon.length !== 4) {
                        throw new Error(`Cannot render road path segment ${s} without four-point polygon geometry.`);
                    }
                    const leftStart = polygon[0];
                    const leftEnd = polygon[1];
                    const rightEnd = polygon[2];
                    const rightStart = polygon[3];
                    const startPoint = segment && segment.startPoint ? segment.startPoint : {
                        x: (Number(leftStart.x) + Number(rightStart.x)) * 0.5,
                        y: (Number(leftStart.y) + Number(rightStart.y)) * 0.5
                    };
                    const endPoint = segment && segment.endPoint ? segment.endPoint : {
                        x: (Number(leftEnd.x) + Number(rightEnd.x)) * 0.5,
                        y: (Number(leftEnd.y) + Number(rightEnd.y)) * 0.5
                    };
                    const length = Math.hypot(
                        Number(endPoint.x) - Number(startPoint.x),
                        Number(endPoint.y) - Number(startPoint.y)
                    );
                    if (!(length > 0)) {
                        throw new Error(`Cannot render road path segment ${s} with zero visual length.`);
                    }
                    const startWidth = Math.hypot(
                        Number(rightStart.x) - Number(leftStart.x),
                        Number(rightStart.y) - Number(leftStart.y)
                    );
                    const endWidth = Math.hypot(
                        Number(rightEnd.x) - Number(leftEnd.x),
                        Number(rightEnd.y) - Number(leftEnd.y)
                    );
                    const width = Math.max(0.001, startWidth, endWidth);
                    const uvSpan = Math.max(length * repeatX, width * repeatY);
                    const subdivisions = Math.max(1, Math.ceil(uvSpan / maxUvSpanPerQuad));
                    if (subdivisions > 4096) {
                        throw new Error(`Cannot render road path segment ${s}; visual tessellation would require ${subdivisions} subdivisions.`);
                    }
                    const lengthCuts = [];
                    for (let i = 0; i <= subdivisions; i++) {
                        addSortedUniqueCut(lengthCuts, i / subdivisions);
                    }
                    addSortedUniqueCut(lengthCuts, edgeFadeWorldUnits / length);
                    addSortedUniqueCut(lengthCuts, 1 - (edgeFadeWorldUnits / length));
                    addSortedUniqueCut(lengthCuts, 0.5);
                    const widthCuts = [0, 1];
                    addSortedUniqueCut(widthCuts, edgeFadeWorldUnits / width);
                    addSortedUniqueCut(widthCuts, 1 - (edgeFadeWorldUnits / width));
                    addSortedUniqueCut(widthCuts, 0.5);
                    const makeVertex = (t, crossT) => {
                        const left = lerpPoint(leftStart, leftEnd, t);
                        const right = lerpPoint(rightStart, rightEnd, t);
                        const widthAtPoint = Math.hypot(right.x - left.x, right.y - left.y);
                        const point = {
                            x: left.x + (right.x - left.x) * crossT,
                            y: left.y + (right.y - left.y) * crossT
                        };
                        const distanceAlongPath = pathDistance + length * t;
                        return {
                            point,
                            u: distanceAlongPath * repeatX,
                            v: crossT * width * repeatY,
                            edgeAlpha: computeRoadPathEdgeAlpha(widthAtPoint, crossT, distanceAlongPath)
                        };
                    };
                    for (let i = 0; i < lengthCuts.length - 1; i++) {
                        const t0 = lengthCuts[i];
                        const t1 = lengthCuts[i + 1];
                        if (!(t1 - t0 > 1e-6)) continue;
                        for (let j = 0; j < widthCuts.length - 1; j++) {
                            const c0 = widthCuts[j];
                            const c1 = widthCuts[j + 1];
                            if (!(c1 - c0 > 1e-6)) continue;
                            emitQuad(
                                makeVertex(t0, c0),
                                makeVertex(t1, c0),
                                makeVertex(t1, c1),
                                makeVertex(t0, c1)
                            );
                        }
                    }
                    pathDistance += length;
                }
                const vertexCount = emitted.length;
                if (vertexCount <= 0) {
                    throw new Error("Cannot render road path without generated visual mesh vertices.");
                }
                const vertexData = new Float32Array(vertexCount * 2);
                const worldPositionData = new Float32Array(vertexCount * 3);
                const uvData = new Float32Array(vertexCount * 2);
                const edgeAlphaData = new Float32Array(vertexCount);
                const indexData = vertexCount > 65535
                    ? new Uint32Array(vertexCount)
                    : new Uint16Array(vertexCount);
                let minUx = Infinity;
                let minUy = Infinity;
                for (let i = 0; i < emitted.length; i++) {
                    minUx = Math.min(minUx, emitted[i].u);
                    minUy = Math.min(minUy, emitted[i].v);
                }
                const uvOffsetX = Number.isFinite(minUx) ? Math.floor(minUx) : 0;
                const uvOffsetY = Number.isFinite(minUy) ? Math.floor(minUy) : 0;
                for (let i = 0; i < emitted.length; i++) {
                    const point = emitted[i];
                    vertexData[i * 2] = point.x;
                    vertexData[i * 2 + 1] = point.y;
                    worldPositionData[i * 3] = point.x;
                    worldPositionData[i * 3 + 1] = point.y;
                    worldPositionData[i * 3 + 2] = 0;
                    uvData[i * 2] = point.u - uvOffsetX;
                    uvData[i * 2 + 1] = point.v - uvOffsetY;
                    edgeAlphaData[i] = point.edgeAlpha;
                    indexData[i] = i;
                }
                return { vertexData, worldPositionData, uvData, edgeAlphaData, indexData, vertexCount };
            };
            const createRoadPathMesh = (meshData) => {
                if (
                    typeof PIXI === "undefined" ||
                    !PIXI.Geometry ||
                    !PIXI.Mesh ||
                    !PIXI.Shader ||
                    !PIXI.Texture
                ) {
                    throw new Error("Cannot render road path polygon mesh because Pixi mesh support is unavailable.");
                }
                const geometry = new PIXI.Geometry()
                    .addAttribute("aVertexPosition", meshData.vertexData, 2)
                    .addAttribute("aWorldPosition", meshData.worldPositionData, 3)
                    .addAttribute("aUvs", meshData.uvData, 2)
                    .addAttribute("aEdgeAlpha", meshData.edgeAlphaData, 1)
                    .addIndex(meshData.indexData);
                const shader = PIXI.Shader.from(ROAD_PATH_DEPTH_VS, ROAD_PATH_DEPTH_FS, {
                    uScreenSize: new Float32Array([1, 1]),
                    uCameraWorld: new Float32Array([0, 0]),
                    uCameraZ: 0,
                    uBaseZ: 0,
                    uDepthBias: ROAD_PATH_DEPTH_BIAS_UNITS,
                    uViewScale: 1,
                    uXyRatio: 1,
                    uDepthRange: new Float32Array([
                        FLOOR_VISUAL_DEPTH_FAR_METRIC,
                        1 / Math.max(1e-6, FLOOR_VISUAL_DEPTH_FAR_METRIC - FLOOR_VISUAL_DEPTH_NEAR_METRIC)
                    ]),
                    uTint: new Float32Array([1, 1, 1, 1]),
                    uAlphaCutoff: 0.001,
                    uBuildingCutawayDataPass: 0,
                    uBuildingCutawayDataZRange: new Float32Array([
                        BUILDING_CUTAWAY_DATA_MIN_Z,
                        1 / Math.max(1e-6, BUILDING_CUTAWAY_DATA_MAX_Z - BUILDING_CUTAWAY_DATA_MIN_Z)
                    ]),
                    uSampler: PIXI.Texture.WHITE
                });
                const state = this.getRoadPathDepthState();
                const mesh = new PIXI.Mesh(
                    geometry,
                    shader,
                    state || undefined,
                    PIXI.DRAW_MODES ? PIXI.DRAW_MODES.TRIANGLES : undefined
                );
                mesh.name = "renderingRoadPathPolygon";
                mesh.interactive = false;
                mesh.blendMode = PIXI.BLEND_MODES.NORMAL;
                mesh.tint = 0x9b8162;
                mesh._roadPathVertexCount = meshData.vertexCount;
                return mesh;
            };
            const updateRoadPathMeshGeometry = (mesh, meshData) => {
                const geometry = mesh && mesh.geometry ? mesh.geometry : null;
                const positionBuffer = geometry && typeof geometry.getBuffer === "function"
                    ? geometry.getBuffer("aVertexPosition")
                    : null;
                const uvBuffer = geometry && typeof geometry.getBuffer === "function"
                    ? geometry.getBuffer("aUvs")
                    : null;
                const edgeAlphaBuffer = geometry && typeof geometry.getBuffer === "function"
                    ? geometry.getBuffer("aEdgeAlpha")
                    : null;
                const worldPositionBuffer = geometry && typeof geometry.getBuffer === "function"
                    ? geometry.getBuffer("aWorldPosition")
                    : null;
                if (!positionBuffer || !positionBuffer.data || positionBuffer.data.length !== meshData.vertexData.length) {
                    return false;
                }
                positionBuffer.data.set(meshData.vertexData);
                positionBuffer.update();
                if (worldPositionBuffer && worldPositionBuffer.data && worldPositionBuffer.data.length === meshData.worldPositionData.length) {
                    worldPositionBuffer.data.set(meshData.worldPositionData);
                    worldPositionBuffer.update();
                }
                if (uvBuffer && uvBuffer.data && uvBuffer.data.length === meshData.uvData.length) {
                    uvBuffer.data.set(meshData.uvData);
                    uvBuffer.update();
                } else {
                    return false;
                }
                if (edgeAlphaBuffer && edgeAlphaBuffer.data && edgeAlphaBuffer.data.length === meshData.edgeAlphaData.length) {
                    edgeAlphaBuffer.data.set(meshData.edgeAlphaData);
                    edgeAlphaBuffer.update();
                } else {
                    return false;
                }
                mesh._roadPathVertexCount = meshData.vertexCount;
                return true;
            };
            const updateRoadPathMeshUniforms = (mesh, tint, alpha, baseZ, texture) => {
                const uniforms = mesh && mesh.shader && mesh.shader.uniforms ? mesh.shader.uniforms : null;
                if (!uniforms) return false;
                const appRef = (ctx && ctx.app) || (typeof app !== "undefined" && app) || global.app || null;
                const screenW = (appRef && appRef.screen && Number.isFinite(appRef.screen.width))
                    ? Number(appRef.screen.width)
                    : 1;
                const screenH = (appRef && appRef.screen && Number.isFinite(appRef.screen.height))
                    ? Number(appRef.screen.height)
                    : 1;
                if (uniforms.uScreenSize) {
                    uniforms.uScreenSize[0] = Math.max(1, screenW);
                    uniforms.uScreenSize[1] = Math.max(1, screenH);
                }
                if (uniforms.uCameraWorld) {
                    uniforms.uCameraWorld[0] = Number(cam.x) || 0;
                    uniforms.uCameraWorld[1] = Number(cam.y) || 0;
                }
                uniforms.uCameraZ = Number(cam.z) || 0;
                uniforms.uBaseZ = Number.isFinite(baseZ) ? Number(baseZ) : 0;
                uniforms.uDepthBias = ROAD_PATH_DEPTH_BIAS_UNITS;
                uniforms.uViewScale = Number(cam.viewscale) || 1;
                uniforms.uXyRatio = Number(cam.xyratio) || 1;
                if (uniforms.uDepthRange) {
                    uniforms.uDepthRange[0] = FLOOR_VISUAL_DEPTH_FAR_METRIC;
                    uniforms.uDepthRange[1] = 1 / Math.max(1e-6, FLOOR_VISUAL_DEPTH_FAR_METRIC - FLOOR_VISUAL_DEPTH_NEAR_METRIC);
                }
                if (uniforms.uTint) {
                    const color = Number.isFinite(tint) ? Math.max(0, Math.min(0xffffff, Math.floor(tint))) : 0xffffff;
                    uniforms.uTint[0] = ((color >> 16) & 0xff) / 255;
                    uniforms.uTint[1] = ((color >> 8) & 0xff) / 255;
                    uniforms.uTint[2] = (color & 0xff) / 255;
                    uniforms.uTint[3] = Number.isFinite(alpha) ? Math.max(0, Math.min(1, Number(alpha))) : 1;
                }
                uniforms.uAlphaCutoff = 0.001;
                uniforms.uBuildingCutawayDataPass = 0;
                uniforms.uSampler = texture || PIXI.Texture.WHITE;
                return true;
            };

            const nodes = Array.isArray(visibleNodes) ? visibleNodes : [];
            const bakedLevel0SectionKeys = this.getBakedLevel0SectionKeys(ctx);
            const roadPathRenderObjects = this.collectRoadPathRenderObjects(ctx, bakedLevel0SectionKeys);
            const visibleRoadObjects = new Set();
            const visibleRoadPathObjects = roadPathRenderObjects.visibleRoadPathObjects;
            const continuityRoadPathObjects = roadPathRenderObjects.continuityRoadPathObjects;
            const bakedRoadPathObjects = roadPathRenderObjects.bakedRoadPathObjects;
            let roadsSkippedForLevel0Bake = Number(roadPathRenderObjects.skippedForLevel0Bake) || 0;
            for (let n = 0; n < nodes.length; n++) {
                const node = nodes[n];
                if (!node || !Array.isArray(node.objects)) continue;
                for (let i = 0; i < node.objects.length; i++) {
                    const obj = node.objects[i];
                    if (!obj || obj.gone) continue;
                    if (obj.type !== "road") continue;
                    if (!this.isScriptVisible(obj)) continue;
                    const roadLayer = this.getLayerIndexForNode(obj && obj.node ? obj.node : node);
                    if (mazeLayerOnly && roadLayer !== wizardLayer && !this.isFallRevealHiddenLayer(roadLayer)) continue;
                    if (this.isRenderItemHiddenByLayerCutaway(obj, roadLayer, cutawayState, map)) continue;
                    if (this.isRoadBakedIntoLevel0Surface(ctx, obj, bakedLevel0SectionKeys)) {
                        const bakedSprite = this.roadSpriteByObject.get(obj);
                        if (bakedSprite) {
                            bakedSprite.visible = false;
                            if (Object.prototype.hasOwnProperty.call(bakedSprite, "renderable")) {
                                bakedSprite.renderable = false;
                            }
                        }
                        if (obj._renderingDisplayObject === bakedSprite) {
                            obj._renderingDisplayObject = null;
                        }
                        roadsSkippedForLevel0Bake += 1;
                        continue;
                    }
                    visibleRoadObjects.add(obj);
                }
            }
            const roadPathEndpointContinuity = this.buildRoadPathEndpointContinuity(continuityRoadPathObjects);

            if (!(this.roadPathMeshByObject instanceof Map)) {
                this.roadPathMeshByObject = new Map();
            }
            for (const roadPath of bakedRoadPathObjects) {
                const bakedMesh = this.roadPathMeshByObject.get(roadPath);
                if (bakedMesh) {
                    bakedMesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(bakedMesh, "renderable")) {
                        bakedMesh.renderable = false;
                    }
                }
                if (roadPath && roadPath._renderingDisplayObject === bakedMesh) {
                    roadPath._renderingDisplayObject = null;
                }
                const pickProxy = this.getGroundHitboxPickProxyForItem(roadPath, 1);
                if (pickProxy) {
                    this.addPickRenderItem(roadPath, pickProxy, { forceInclude: true });
                }
            }
            const roadPathContainer = visibleRoadPathObjects.size > 0
                ? this.ensureRoadPathDepthContainer()
                : null;
            if (visibleRoadPathObjects.size > 0 && !roadPathContainer) {
                throw new Error("Cannot render road path alpha fade without a depth-layer road path container.");
            }
            for (const roadPath of visibleRoadPathObjects) {
                const geometry = roadPath && roadPath.generatedGeometry;
                const segments = geometry && Array.isArray(geometry.segments) ? geometry.segments : null;
                if (!segments || segments.length === 0) {
                    throw new Error("Cannot render road path without generated segment geometry.");
                }
                const roadPathRenderZ = Number.isFinite(roadPath.renderZ)
                    ? Number(roadPath.renderZ)
                    : 0.001;
                const roadLayer = this.getLayerIndexForNode(roadPath && roadPath.node ? roadPath.node : roadPath);
                const roadBaseZ = this.getLayerBaseZForNode(roadPath && roadPath.node ? roadPath.node : roadPath);
                const fillTexturePath = resolveRoadPathFillTexturePathForView(
                    roadPath,
                    getRoadPathFillTexturePath(roadPath)
                );
                const fillTexture = getRoadPathFillTexture(fillTexturePath);
                const textureRepeat = getRoadPathTextureRepeat(fillTexturePath);
                const meshData = buildRoadPathMeshData(
                    roadPath,
                    segments,
                    textureRepeat,
                    roadPathEndpointContinuity.get(roadPath) || null
                );
                let mesh = this.roadPathMeshByObject.get(roadPath);
                const textureRepeatSignature = this.getFloorVisualTextureRepeatSignature(textureRepeat);
                if (
                    mesh &&
                    (
                        Number(mesh._roadPathVertexCount) !== meshData.vertexCount ||
                        mesh._roadPathTextureRepeatSignature !== textureRepeatSignature
                    )
                ) {
                    destroyRoadPathMesh(roadPath, mesh);
                    mesh = null;
                }
                if (!mesh) {
                    mesh = createRoadPathMesh(meshData);
                    this.roadPathMeshByObject.set(roadPath, mesh);
                } else if (!updateRoadPathMeshGeometry(mesh, meshData)) {
                    destroyRoadPathMesh(roadPath, mesh);
                    mesh = createRoadPathMesh(meshData);
                    this.roadPathMeshByObject.set(roadPath, mesh);
                }
                if (mesh.parent !== roadPathContainer) {
                    roadPathContainer.addChild(mesh);
                }
                const alpha = (Number.isFinite(roadPath.alpha) ? roadPath.alpha : 1) * this.getLiveLayerFadeMultiplier(
                    roadLayer,
                    nowMs
                );
                mesh._roadPathTextureRepeatSignature = textureRepeatSignature;
                mesh._roadPathFillTexturePath = fillTexturePath;
                updateRoadPathMeshUniforms(mesh, 0xffffff, alpha, roadBaseZ + roadPathRenderZ, fillTexture);
                mesh.tint = 0xffffff;
                mesh.alpha = 1;
                mesh.position.set(0, 0);
                mesh.scale.set(1, 1);
                mesh.visible = true;
                if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                    mesh.renderable = true;
                }
                mesh._lastVisibleAtMs = nowMs;
                roadPath._renderingDisplayObject = mesh;
                this.applyLayerDarknessToDisplayObject(mesh, this.getLayerDarknessMultiplier(roadLayer));
                this.addPickRenderItem(roadPath, mesh);
            }
            for (const [roadPath, mesh] of this.roadPathMeshByObject.entries()) {
                if (!roadPath || roadPath.gone) {
                    destroyRoadPathMesh(roadPath, mesh);
                    continue;
                }
                if (!visibleRoadPathObjects.has(roadPath) && mesh) {
                    const lastVisibleAtMs = Number.isFinite(mesh._lastVisibleAtMs)
                        ? Number(mesh._lastVisibleAtMs)
                        : 0;
                    if (lastVisibleAtMs > 0 && (nowMs - lastVisibleAtMs) > hiddenSpriteGraceMs) {
                        destroyRoadPathMesh(roadPath, mesh);
                        continue;
                    }
                    mesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                        mesh.renderable = false;
                    }
                }
            }

            const roadObjects = Array.from(visibleRoadObjects);
            for (let i = 0; i < roadObjects.length; i++) {
                const road = roadObjects[i];
                let sprite = this.roadSpriteByObject.get(road);
                if (!sprite) {
                    sprite = new PIXI.Sprite(PIXI.Texture.WHITE);
                    sprite.name = "renderingRoad";
                    sprite.anchor.set(0.5, 0.5);
                    this.roadSpriteByObject.set(road, sprite);
                    roadSpritesCreated += 1;
                }
                if (sprite.parent !== container) {
                    container.addChild(sprite);
                    roadSpritesAttached += 1;
                }

                const worldX = Number.isFinite(road.x) ? road.x : (road.node && Number.isFinite(road.node.x) ? road.node.x : 0);
                const worldY = Number.isFinite(road.y) ? road.y : (road.node && Number.isFinite(road.node.y) ? road.node.y : 0);
                const roadLayer = this.getLayerIndexForNode(road && road.node ? road.node : road);
                const roadBaseZ = Number.isFinite(road && road.z)
                    ? Number(road.z)
                    : this.getLayerBaseZForNode(road && road.node ? road.node : road);
                const roadScreenWidth = (Number(road.width) || 1) * cam.viewscale * 1.1547;
                const roadScreenHeight = (Number(road.height) || 1) * cam.viewscale * cam.xyratio;
                if (typeof global.Road !== "undefined" && typeof global.Road.resolveFillTexturePathForSize === "function") {
                    const lodMetric = typeof global.Road.getFillTextureLodMetric === "function"
                        ? global.Road.getFillTextureLodMetric(road.fillTexturePath, roadScreenWidth, roadScreenHeight)
                        : Math.max(roadScreenWidth, roadScreenHeight);
                    const resolvedFillTexturePath = global.Road.resolveFillTexturePathForSize(road.fillTexturePath, lodMetric);
                    if (resolvedFillTexturePath !== road._resolvedRenderFillTexturePath && typeof road.updateTexture === "function") {
                        road.updateTexture(null, resolvedFillTexturePath);
                    }
                }

                const sourceTexture = (
                    road &&
                    road.pixiSprite &&
                    isRenderablePixiTexture(road.pixiSprite.texture)
                ) ? road.pixiSprite.texture : null;
                if (!sourceTexture && typeof road.updateTexture === "function") {
                    road.updateTexture();
                    roadTextureRefreshes += 1;
                }
                const refreshedSourceTexture = (
                    road &&
                    road.pixiSprite &&
                    isRenderablePixiTexture(road.pixiSprite.texture)
                ) ? road.pixiSprite.texture : null;
                if (refreshedSourceTexture && refreshedSourceTexture !== sprite.texture) {
                    sprite.texture = refreshedSourceTexture;
                    syncRoadRenderSpriteTextureRetention(sprite, road);
                    roadTextureAssignments += 1;
                } else if (refreshedSourceTexture) {
                    syncRoadRenderSpriteTextureRetention(sprite, road);
                } else if (!isRenderablePixiTexture(sprite.texture)) {
                    syncRoadRenderSpriteTextureRetention(sprite, null);
                    sprite.texture = PIXI.Texture.WHITE;
                    roadTextureAssignments += 1;
                }

                const p = cam.worldToScreen(worldX, worldY, roadBaseZ);
                sprite.x = p.x;
                sprite.y = p.y;
                if (!isRenderablePixiTexture(sprite.texture)) {
                    syncRoadRenderSpriteTextureRetention(sprite, null);
                    sprite.texture = PIXI.Texture.WHITE;
                }
                sprite.width = roadScreenWidth;
                sprite.height = roadScreenHeight;
                sprite.alpha = (Number.isFinite(road.alpha) ? road.alpha : 1) * this.getLiveLayerFadeMultiplier(roadLayer, nowMs);
                sprite.visible = true;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = true;
                }
                sprite._lastVisibleAtMs = nowMs;
                this.applyScriptBrightness(road, sprite);
                this.applyLayerDarknessToDisplayObject(sprite, this.getLayerDarknessMultiplier(roadLayer));
                road._renderingDisplayObject = sprite;
                this.addPickRenderItem(road, sprite);
            }

            const hiddenEntries = [];
            for (const [road, sprite] of this.roadSpriteByObject.entries()) {
                if (!road || road.gone) {
                    destroyRoadSprite(road, sprite);
                    continue;
                }
                if (!visibleRoadObjects.has(road) && sprite) {
                    const lastVisibleAtMs = Number.isFinite(sprite._lastVisibleAtMs)
                        ? Number(sprite._lastVisibleAtMs)
                        : 0;
                    if (lastVisibleAtMs > 0 && (nowMs - lastVisibleAtMs) > hiddenSpriteGraceMs) {
                        destroyRoadSprite(road, sprite);
                        continue;
                    }
                    if (!isRenderablePixiTexture(sprite.texture)) {
                        syncRoadRenderSpriteTextureRetention(sprite, null);
                        sprite.texture = PIXI.Texture.WHITE;
                    }
                    sprite.visible = false;
                    roadHiddenSprites += 1;
                    hiddenEntries.push({ road, sprite, lastVisibleAtMs });
                }
            }

            if (hiddenEntries.length > maxHiddenSprites) {
                hiddenEntries.sort((a, b) => a.lastVisibleAtMs - b.lastVisibleAtMs);
                const evictCount = hiddenEntries.length - maxHiddenSprites;
                for (let i = 0; i < evictCount; i++) {
                    const entry = hiddenEntries[i];
                    destroyRoadSprite(entry.road, entry.sprite);
                    roadEvictedSprites += 1;
                }
            }

            this.setFrameMetric("roadsVisible", roadObjects.length);
            this.setFrameMetric("roadsCached", this.roadSpriteByObject instanceof Map ? this.roadSpriteByObject.size : 0);
            this.setFrameMetric("roadsCreated", roadSpritesCreated);
            this.setFrameMetric("roadsAttached", roadSpritesAttached);
            this.setFrameMetric("roadsTextureRefreshes", roadTextureRefreshes);
            this.setFrameMetric("roadsTextureAssignments", roadTextureAssignments);
            this.setFrameMetric("roadsHidden", roadHiddenSprites);
            this.setFrameMetric("roadsDestroyed", roadDestroyedSprites);
            this.setFrameMetric("roadsEvicted", roadEvictedSprites);
            this.setFrameMetric("roadsSkippedForLevel0Bake", roadsSkippedForLevel0Bake);
            this.setFrameMetric("roadPathsConsidered", Number(roadPathRenderObjects.considered) || 0);
            this.setFrameMetric("roadPathsVisible", visibleRoadPathObjects.size);
            this.setFrameMetric("roadPathsContinuity", continuityRoadPathObjects.size);
            this.setFrameMetric("roadPathsSkippedOffscreen", Number(roadPathRenderObjects.skippedOffscreen) || 0);
            this.setFrameMetric(
                "roadsMs",
                diagnosticsEnabled ? (performance.now() - functionStartMs) : 0
            );
        }

        renderHexGridOverlay(ctx) {
            const showPickerScreen = getShowPickerScreenFlag();
            const layer = showPickerScreen ? this.layers.ui : this.layers.depthObjects;
            if (!layer) return;
            const appRef = (ctx && ctx.app) || global.app || null;
            if (!appRef || !appRef.renderer) return;
            const wallCtor = global.WallSectionUnit || null;
            const directionalBlockingDebugEnabled = !!(
                wallCtor &&
                wallCtor._showDirectionalBlockingDebug
            );

            if (showPickerScreen) {
                if (!this.hexGridPickerBackdrop) {
                    this.hexGridPickerBackdrop = new PIXI.Sprite(PIXI.Texture.WHITE);
                    this.hexGridPickerBackdrop.name = "renderingHexGridPickerBackdrop";
                    this.hexGridPickerBackdrop.interactive = false;
                    this.hexGridPickerBackdrop.tint = 0x000000;
                    this.hexGridPickerBackdrop.alpha = 1;
                }
                if (this.hexGridPickerBackdrop.parent !== layer) {
                    layer.addChild(this.hexGridPickerBackdrop);
                }
                this.hexGridPickerBackdrop.position.set(0, 0);
                this.hexGridPickerBackdrop.width = Math.max(1, Number(appRef.renderer.width) || Number(window.innerWidth) || 1);
                this.hexGridPickerBackdrop.height = Math.max(1, Number(appRef.renderer.height) || Number(window.innerHeight) || 1);
                this.hexGridPickerBackdrop.visible = true;
                if (layer.getChildIndex(this.hexGridPickerBackdrop) !== 0) {
                    layer.setChildIndex(this.hexGridPickerBackdrop, 0);
                }
            } else if (this.hexGridPickerBackdrop) {
                this.hexGridPickerBackdrop.visible = false;
            }

            if (this.getActiveBuildingInteriorRenderScope(ctx)) {
                if (this.hexGridContainer) this.hexGridContainer.visible = false;
                return;
            }

            const gridEnabled = !!(
                (typeof showHexGrid !== "undefined" && showHexGrid)
            );
            if (!gridEnabled && !directionalBlockingDebugEnabled) {
                if (this.hexGridContainer) this.hexGridContainer.visible = false;
                return;
            }
            const cam = this.camera;

            if (!this.hexGridContainer) {
                this.hexGridContainer = new PIXI.Container();
                this.hexGridContainer.name = "renderingHexGridContainer";
                this.hexGridContainer.interactiveChildren = false;
                this.hexGridContainer.zIndex = Number.MIN_SAFE_INTEGER;
                layer.addChild(this.hexGridContainer);
            } else if (this.hexGridContainer.parent !== layer) {
                layer.addChild(this.hexGridContainer);
            }
            this.hexGridContainer.visible = true;

            if (!this.hexGridDirectionalBlockingGraphics) {
                this.hexGridDirectionalBlockingGraphics = new PIXI.Graphics();
                this.hexGridDirectionalBlockingGraphics.name = "renderingHexGridDirectionalBlockingDebug";
                this.hexGridDirectionalBlockingGraphics.interactive = false;
                this.hexGridContainer.addChild(this.hexGridDirectionalBlockingGraphics);
            } else if (this.hexGridDirectionalBlockingGraphics.parent !== this.hexGridContainer) {
                this.hexGridContainer.addChild(this.hexGridDirectionalBlockingGraphics);
            }

            const vs = cam.viewscale;
            const vsy = cam.viewscale * cam.xyratio;
            if (vs <= 0 || vsy <= 0) return;

            // Hex geometry in screen pixels.
            // Nodes: x = xIndex*0.866, y = yIndex + (xIndex%2===0 ? 0.5 : 0)
            // Even columns are shifted DOWN by 0.5 world units.
            const colStep = 0.866 * vs;       // horizontal distance between adjacent columns
            const vy = vsy;                    // vertical distance between adjacent rows
            const hexPxW = vs / 0.866;         // full hex bounding width
            const halfW = hexPxW / 2;
            const halfH = vy / 2;
            const quarterW = hexPxW / 4;

            // In the texture, hex[col][row] center is at:
            //   tx = col*colStep + cx0
            //   ty = row*vy + cy0 - (col%2===1 ? halfH : 0)
            // cy0=vy ensures odd-col row-0 hexes don't clip above y=0.
            const cx0 = halfW;
            const cy0 = vy;

            const TILE_COLS = 16;  // must be even so parity is preserved when tiling
            const TILE_ROWS = 16;
            // Texture bounding box:
            //   width  = rightmost hex right-edge  = (TILE_COLS-1)*colStep + hexPxW
            //   height = lowest hex bottom-edge    = TILE_ROWS*vy + halfH  (even-col last row)
            const tileTexW = (TILE_COLS - 1) * colStep + hexPxW;
            const tileTexH = TILE_ROWS * vy + halfH;

            // Rebuild texture only when zoom or aspect ratio changes meaningfully.
            let rebuildTexture = !this.hexGridTexture;
            if (!rebuildTexture && Math.abs(vs - this.hexGridLastViewscale) > 1e-3) rebuildTexture = true;
            if (!rebuildTexture && Math.abs(cam.xyratio - this.hexGridLastXyratio) > 1e-3) rebuildTexture = true;

            if (rebuildTexture) {
                const gfx = new PIXI.Graphics();
                gfx.lineStyle(1, 0xffffff, 0.35);
                for (let col = 0; col < TILE_COLS; col++) {
                    const tx = col * colStep + cx0;
                    for (let row = 0; row < TILE_ROWS; row++) {
                        const ty = row * vy + cy0 - (col % 2 === 1 ? halfH : 0);
                        gfx.moveTo(tx - halfW, ty);
                        gfx.lineTo(tx - quarterW, ty - halfH);
                        gfx.lineTo(tx + quarterW, ty - halfH);
                        gfx.lineTo(tx + halfW, ty);
                        gfx.lineTo(tx + quarterW, ty + halfH);
                        gfx.lineTo(tx - quarterW, ty + halfH);
                        gfx.closePath();
                    }
                }
                const pxW = Math.ceil(tileTexW);
                const pxH = Math.ceil(tileTexH);
                const tex = appRef.renderer.generateTexture(gfx, {
                    region: new PIXI.Rectangle(0, 0, pxW, pxH),
                    resolution: 1
                });
                gfx.destroy(true);
                if (this.hexGridTexture && this.hexGridTexture !== tex) {
                    this.hexGridTexture.destroy(true);
                }
                this.hexGridTexture = tex;
                this.hexGridLastViewscale = vs;
                this.hexGridLastXyratio = cam.xyratio;
            }

            // Anchor: even column just offscreen to the top-left.
            // xIndex must be even so the texture parity (even-col first) is always correct.
            const xIndexRaw = Math.floor(cam.x / 0.866) - 1;
            const xIndex = xIndexRaw % 2 === 0 ? xIndexRaw : xIndexRaw - 1;
            const yIndex = Math.floor(cam.y) - 1;
            // Even column: world y = yIndex + 0.5
            const anchorWorldX = xIndex * 0.866;
            const anchorWorldY = yIndex + 0.5;
            const anchorScreen = cam.worldToScreen(anchorWorldX, anchorWorldY);

            // Top-left of first tile sprite in screen space
            const startScreenX = anchorScreen.x - cx0;
            const startScreenY = anchorScreen.y - cy0;

            // Step between successive tile copies (exactly 16 columns / 16 rows)
            const stepX = TILE_COLS * colStep;   // 16 * 0.866 * vs
            const stepY = TILE_ROWS * vy;         // 16 * vy

            const screenW = Math.max(1, appRef.renderer.width || window.innerWidth || 800);
            const screenH = Math.max(1, appRef.renderer.height || window.innerHeight || 600);

            const colsNeeded = Math.ceil((screenW - startScreenX) / stepX) + 1;
            const rowsNeeded = Math.ceil((screenH - startScreenY) / stepY) + 1;

            let idx = 0;
            if (gridEnabled) {
                for (let r = 0; r < rowsNeeded; r++) {
                    for (let c = 0; c < colsNeeded; c++) {
                        let spr = this.hexGridSprites[idx];
                        if (!spr) {
                            spr = new PIXI.Sprite(this.hexGridTexture);
                            spr.name = "renderingHexGridTile";
                            spr.anchor.set(0, 0);
                            spr.interactive = false;
                            this.hexGridContainer.addChild(spr);
                            this.hexGridSprites[idx] = spr;
                        }
                        if (spr.texture !== this.hexGridTexture) spr.texture = this.hexGridTexture;
                        spr.x = startScreenX + c * stepX;
                        spr.y = startScreenY + r * stepY;
                        spr.visible = true;
                        idx++;
                    }
                }
            }
            for (; idx < this.hexGridSprites.length; idx++) {
                if (this.hexGridSprites[idx]) this.hexGridSprites[idx].visible = false;
            }

            const directionalGfx = this.hexGridDirectionalBlockingGraphics;
            if (directionalGfx) {
                directionalGfx.clear();
                directionalGfx.visible = directionalBlockingDebugEnabled;
                if (directionalBlockingDebugEnabled && wallCtor && wallCtor._allSections instanceof Map) {
                    const drawnMarkers = new Map();
                    const worldMarginX = Math.max(2, (Number(ctx && ctx.viewport && ctx.viewport.width) || 20) * 0.15);
                    const worldMarginY = Math.max(2, (Number(ctx && ctx.viewport && ctx.viewport.height) || 20) * 0.15);
                    const minWorldX = Number(cam.x) - worldMarginX;
                    const maxWorldX = Number(cam.x) + Number(ctx && ctx.viewport && ctx.viewport.width || 20) + worldMarginX;
                    const minWorldY = Number(cam.y) - worldMarginY;
                    const maxWorldY = Number(cam.y) + Number(ctx && ctx.viewport && ctx.viewport.height || 20) + worldMarginY;
                    for (const section of wallCtor._allSections.values()) {
                        if (!section || section.gone) continue;
                        const sectionCenter = section.center || null;
                        if (
                            sectionCenter &&
                            Number.isFinite(sectionCenter.x) &&
                            Number.isFinite(sectionCenter.y) &&
                            (
                                Number(sectionCenter.x) < minWorldX ||
                                Number(sectionCenter.x) > maxWorldX ||
                                Number(sectionCenter.y) < minWorldY ||
                                Number(sectionCenter.y) > maxWorldY
                            )
                        ) {
                            continue;
                        }
                        const debugData = section._directionalBlockingDebug;
                        if (!debugData) continue;
                        const blockedLinks = Array.isArray(section.blockedLinks) ? section.blockedLinks : [];

                        for (let i = 0; i < blockedLinks.length; i++) {
                            const link = blockedLinks[i];
                            const sourceNode = link && link.node;
                            const dir = Number(link && link.direction);
                            if (!sourceNode || !Array.isArray(sourceNode.neighbors) || !Number.isInteger(dir)) continue;
                            const destinationNode = sourceNode.neighbors[dir];
                            if (!destinationNode) continue;
                            const incomingDir = ((dir + 6) % 12 + 12) % 12;
                            const markerKey = `${Number(destinationNode.xindex)},${Number(destinationNode.yindex)}|${incomingDir}`;
                            const color = (typeof wallCtor._getDirectionalBlockingDebugColor === "function")
                                ? wallCtor._getDirectionalBlockingDebugColor(link.blocker)
                                : 0xff0000;
                            const existingColor = drawnMarkers.get(markerKey);
                            if (existingColor === 0x3399ff || existingColor === color) continue;
                            drawnMarkers.set(markerKey, {
                                fromNode: sourceNode,
                                destinationNode,
                                incomingDir,
                                color
                            });
                        }
                    }

                    for (const marker of drawnMarkers.values()) {
                        if (!marker || !marker.fromNode || !marker.destinationNode) continue;
                        const sourceWorldX = Number(marker.fromNode.x) || 0;
                        const sourceWorldY = Number(marker.fromNode.y) || 0;
                        const destinationWorldX = Number(marker.destinationNode.x) || 0;
                        const destinationWorldY = Number(marker.destinationNode.y) || 0;
                        const dxWorld = sourceWorldX - destinationWorldX;
                        const dyWorld = sourceWorldY - destinationWorldY;
                        const worldLen = Math.hypot(dxWorld, dyWorld);
                        if (!(worldLen > 1e-4)) continue;

                        const ux = dxWorld / worldLen;
                        const uy = dyWorld / worldLen;
                        const px = -uy;
                        const py = ux;
                        const incomingDir = Number.isInteger(marker.incomingDir) ? marker.incomingDir : 0;
                        const halfMarkerWorldLength = (incomingDir % 2 === 1) ? 0.28 : 0.22;
                        const markerCenterWorldX = destinationWorldX + ux * worldLen * 0.56;
                        const markerCenterWorldY = destinationWorldY + uy * worldLen * 0.56;
                        const startScreen = cam.worldToScreen(
                            markerCenterWorldX - px * halfMarkerWorldLength,
                            markerCenterWorldY - py * halfMarkerWorldLength,
                            0
                        );
                        const endScreen = cam.worldToScreen(
                            markerCenterWorldX + px * halfMarkerWorldLength,
                            markerCenterWorldY + py * halfMarkerWorldLength,
                            0
                        );

                        directionalGfx.lineStyle(3, marker.color, 0.95);
                        directionalGfx.moveTo(startScreen.x, startScreen.y);
                        directionalGfx.lineTo(endScreen.x, endScreen.y);
                    }
                }
            }

            if (directionalGfx && directionalGfx.parent === this.hexGridContainer) {
                this.hexGridContainer.setChildIndex(directionalGfx, this.hexGridContainer.children.length - 1);
            }

            if (this.hexGridContainer.parent === layer) {
                if (showPickerScreen) {
                    const targetIdx = (this.hexGridPickerBackdrop && this.hexGridPickerBackdrop.parent === layer) ? 1 : 0;
                    if (layer.getChildIndex(this.hexGridContainer) !== targetIdx) {
                        layer.setChildIndex(this.hexGridContainer, targetIdx);
                    }
                } else {
                    let gridIdx = layer.children.length - 1;
                    if (this.floorVisualContainer && this.floorVisualContainer.parent === layer) {
                        gridIdx = Math.min(layer.children.length - 1, layer.getChildIndex(this.floorVisualContainer) + 1);
                    }
                    if (layer.getChildIndex(this.hexGridContainer) !== gridIdx) {
                        layer.setChildIndex(this.hexGridContainer, gridIdx);
                    }
                    if (this.losShadowDepthMesh && this.losShadowDepthMesh.parent === layer) {
                        const shadowIdx = Math.min(layer.children.length - 1, layer.getChildIndex(this.hexGridContainer) + 1);
                        if (layer.getChildIndex(this.losShadowDepthMesh) !== shadowIdx) {
                            layer.setChildIndex(this.losShadowDepthMesh, shadowIdx);
                        }
                    }
                }
            }
        }

        renderObjects3D(ctx, visibleNodes, visibleObjectsOverride = null) {
            const container = this.layers.objects3d;
            if (!container) return;
            this.clearBuildingInteriorForegroundPromotions();
            const diagnosticsEnabled = !!this.currentFrameMetrics;
            const nowIfEnabled = () => diagnosticsEnabled ? performance.now() : 0;
            const showPickerScreen = getShowPickerScreenFlag();
            const wizard = (ctx && ctx.wizard) ? ctx.wizard : null;
            const mapRef = (ctx && ctx.map) ? ctx.map : (this.camera && this.camera.map) || global.map || null;
            const omnivisionActive = this.isOmnivisionActive(wizard);
            const mazeMode = this.isLosMazeModeEnabled() && !omnivisionActive;
            const useMazeLosClipping = mazeMode;
            const renderNowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : performance.now();
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const buildLosSetsStartMs = nowIfEnabled();
            const losVisibleObjectSet = (
                useMazeLosClipping &&
                this.currentLosState &&
                Array.isArray(this.currentLosState.visibleObjects)
            ) ? new Set(this.currentLosState.visibleObjects) : null;
            const visibleWallIdSet = (() => {
                if (!useMazeLosClipping || !losVisibleObjectSet) return null;
                const out = new Set();
                for (const obj of losVisibleObjectSet) {
                    if (!obj || obj.type !== "wallSection" || !Number.isInteger(obj.id)) continue;
                    out.add(Number(obj.id));
                }
                return out;
            })();
            const losVisibleWalls = (() => {
                if (!losVisibleObjectSet) return [];
                const out = [];
                for (const obj of losVisibleObjectSet) {
                    if (!obj || obj.type !== "wallSection") continue;
                    if (this.getLayerIndexForObject(obj, wizardLayer) !== wizardLayer) continue;
                    out.push(obj);
                }
                return out;
            })();
            const buildLosSetsMs = diagnosticsEnabled ? (performance.now() - buildLosSetsStartMs) : 0;
            this.setFrameMetric("objects3dLosBuildMs", buildLosSetsMs);
            this.setFrameMetric("objects3dLosVisibleSetSize", losVisibleObjectSet ? losVisibleObjectSet.size : 0);
            this.setFrameMetric("objects3dLosVisibleWalls", losVisibleWalls.length);
            const sharesVisibleCollinearWallLine = (item) => {
                if (!item || !useMazeLosClipping || !losVisibleObjectSet || losVisibleWalls.length === 0) {
                    return false;
                }
                if (item.type === "wallSection") {
                    if (losVisibleObjectSet.has(item)) return true;
                    for (let i = 0; i < losVisibleWalls.length; i++) {
                        const visibleWall = losVisibleWalls[i];
                        if (!visibleWall || visibleWall === item) continue;
                        const hasMazeGuard = (
                            typeof item.canShareMazeCollinearVisibilityWith === "function" ||
                            typeof visibleWall.canShareMazeCollinearVisibilityWith === "function"
                        );
                        if (
                            typeof item.canShareMazeCollinearVisibilityWith === "function" &&
                            item.canShareMazeCollinearVisibilityWith(visibleWall, wizard)
                        ) {
                            return true;
                        }
                        if (
                            typeof visibleWall.canShareMazeCollinearVisibilityWith === "function" &&
                            visibleWall.canShareMazeCollinearVisibilityWith(item, wizard)
                        ) {
                            return true;
                        }
                        if (hasMazeGuard) continue;
                        if (
                            typeof item._isSameWallLineForVisibility === "function" &&
                            item._isSameWallLineForVisibility(visibleWall)
                        ) {
                            return true;
                        }
                        if (
                            typeof visibleWall._isSameWallLineForVisibility === "function" &&
                            visibleWall._isSameWallLineForVisibility(item)
                        ) {
                            return true;
                        }
                    }
                    return false;
                }
                if (!this.isWallMountedSpatialItem(item)) return false;
                const mountedSection = this.resolveMountedWallSectionForItem(item);
                if (!mountedSection || mountedSection.type !== "wallSection") return false;
                for (let i = 0; i < losVisibleWalls.length; i++) {
                    const visibleWall = losVisibleWalls[i];
                    if (!visibleWall || typeof visibleWall.isEndpointOwnedBySameWall !== "function") continue;
                    const hasMazeGuard = (
                        typeof mountedSection.canShareMazeCollinearVisibilityWith === "function" ||
                        typeof visibleWall.canShareMazeCollinearVisibilityWith === "function"
                    );
                    if (visibleWall === mountedSection) return true;
                    if (
                        typeof mountedSection.canShareMazeCollinearVisibilityWith === "function" &&
                        mountedSection.canShareMazeCollinearVisibilityWith(visibleWall, wizard)
                    ) {
                        return true;
                    }
                    if (
                            typeof visibleWall.canShareMazeCollinearVisibilityWith === "function" &&
                            visibleWall.canShareMazeCollinearVisibilityWith(mountedSection, wizard)
                    ) {
                        return true;
                    }
                    if (hasMazeGuard) continue;
                    if (
                        visibleWall.isEndpointOwnedBySameWall("a", item) ||
                        visibleWall.isEndpointOwnedBySameWall("b", item)
                    ) {
                        return true;
                    }
                }
                return false;
            };
            const isWallDirectlyVisibleByMazeLos = (item) => {
                if (!useMazeLosClipping || !item || item.type !== "wallSection") return false;
                return !!(
                    (losVisibleObjectSet && losVisibleObjectSet.has(item)) ||
                    sharesVisibleCollinearWallLine(item)
                );
            };
            const isWallVisibleByMazeSample = (item) => {
                if (!useMazeLosClipping || !wizard || !item || item.type !== "wallSection") return false;
                if (isWallDirectlyVisibleByMazeLos(item)) return true;
                const samplePos = this.getLosVisibilitySamplePointForItem(item, mapRef, wizard);
                if (!samplePos) return false;
                return !this.isWorldPointInLosShadow(samplePos.x, samplePos.y, wizard, mapRef);
            };
            const animalsList = Array.isArray(ctx && ctx.animals)
                ? ctx.animals
                : (Array.isArray(global.animals) ? global.animals : null);
            let animalSet = null;
            if (Array.isArray(animalsList)) {
                if (!this._renderObjects3DAnimalSet) this._renderObjects3DAnimalSet = new Set();
                const _as = this._renderObjects3DAnimalSet;
                _as.clear();
                for (let _i = 0; _i < animalsList.length; _i++) _as.add(animalsList[_i]);
                animalSet = _as;
            }
            const LOS_ANIMAL_EDGE_SAMPLES = 8;
            const isAnimalHiddenByLos = (item) => {
                if (!animalSet || !animalSet.has(item)) return false;
                if (!useMazeLosClipping && this.isForceVisible(item)) return false;
                return this.isRadialItemHiddenByLos(item, wizard, mapRef);
            };
            const isItemHiddenByMazeLos = (item) => {
                if (!useMazeLosClipping || !wizard || !item) return false;
                if (item.type === "wallSection") {
                    if (item.castsLosShadows === false) return false;
                    return !isWallVisibleByMazeSample(item);
                }
                if (this.isWallMountedSpatialItem(item) && losVisibleObjectSet) {
                    const category = (typeof item.category === "string") ? item.category.trim().toLowerCase() : "";
                    const isDoor = item.type === "door" || category === "doors";
                    if (isDoor) {
                        const mountedSection = this.resolveMountedWallSectionForItem(item);
                        if (mountedSection && mountedSection.type === "wallSection" && !losVisibleObjectSet.has(mountedSection)) {
                            return true;
                        }
                    }
                }
                if (sharesVisibleCollinearWallLine(item)) return false;
                if (losVisibleObjectSet && this.isLosOccluder(item)) {
                    return !losVisibleObjectSet.has(item);
                }
                const samplePos = this.getLosVisibilitySamplePointForItem(item, mapRef, wizard);
                if (!samplePos) return false;
                return this.isWorldPointInLosShadow(samplePos.x, samplePos.y, wizard, mapRef);
            };

            const baseVisibleObjects = Array.isArray(visibleObjectsOverride)
                ? visibleObjectsOverride
                : this.collectVisibleObjects(visibleNodes, ctx);
            let animalLosHiddenCount = 0;
            let itemMazeHiddenCount = 0;
            let wallMazeHiddenCount = 0;
            let itemCutawayHiddenCount = 0;
            let missingMountedSectionFilteredCount = 0;
            const cutawayState = this.prepareLayerCutawayFrame(ctx, mapRef, wizard);
            const buildingInteriorRenderPlan = this.buildBuildingInteriorRenderPlan(ctx, cutawayState);
            this.prepareBuildingInteriorPickerFrame(ctx, cutawayState, buildingInteriorRenderPlan);
            const visibleObjects = (() => {
                if (!buildingInteriorRenderPlan.active || !(buildingInteriorRenderPlan.items instanceof Set)) {
                    return baseVisibleObjects;
                }
                const out = Array.isArray(baseVisibleObjects) ? baseVisibleObjects.slice() : [];
                const seen = new Set(out);
                buildingInteriorRenderPlan.items.forEach(item => {
                    if (!item || seen.has(item)) return;
                    seen.add(item);
                    out.push(item);
                });
                return out;
            })();
            const buildingCompositeCacheUsable = this.isBuildingCutawayCompositeCacheUsable(ctx, cutawayState);
            const filterStartMs = nowIfEnabled();
            const mapItems = visibleObjects.filter(item => {
                if (!item) return false;
                if (!this.isScriptVisible(item)) {
                    this.logPlaceObjectRenderDebug("filter-drop-script-hidden", item);
                    return false;
                }
                if (item.type === "road" || item === wizard) {
                    this.logPlaceObjectRenderDebug("filter-drop-road-or-wizard", item);
                    return false;
                }
                if (this.isWallMountedSpatialItem(item) && !this.resolveMountedWallSectionForItem(item)) {
                    missingMountedSectionFilteredCount += 1;
                    this.hideRenderItemWithMissingMountedSection(item);
                    this.logPlaceObjectRenderDebug("filter-drop-missing-mounted-section", item, {
                        mountedWallSectionUnitId: Number.isInteger(item.mountedWallSectionUnitId) ? Number(item.mountedWallSectionUnitId) : null,
                        mountedSectionId: Number.isInteger(item.mountedSectionId) ? Number(item.mountedSectionId) : null,
                        mountedWallLineGroupId: Number.isInteger(item.mountedWallLineGroupId) ? Number(item.mountedWallLineGroupId) : null
                    });
                    return false;
                }
                const mountedWallLayer = this.getMountedWallLayerIndexForItem(item, wizardLayer);
                const itemLayer = Number.isFinite(mountedWallLayer)
                    ? Number(mountedWallLayer)
                    : this.getLayerIndexForObject(item, useMazeLosClipping ? 0 : wizardLayer);
                const itemOnFallRevealLayer = this.isFallRevealHiddenLayer(itemLayer);
                if (
                    buildingCompositeCacheUsable &&
                    item._cutawayCompositeFrame === this._layerCutawayFrameId &&
                    !buildingInteriorRenderPlan.items.has(item)
                ) {
                    this.logPlaceObjectRenderDebug("filter-drop-building-composite", item, {
                        itemLayer,
                        cutawayFrameId: this._layerCutawayFrameId
                    });
                    return false;
                }
                if (
                    item._cutawayInteriorOverlayFrame === this._layerCutawayFrameId &&
                    !buildingInteriorRenderPlan.items.has(item)
                ) {
                    this.logPlaceObjectRenderDebug("filter-drop-interior-overlay", item, {
                        itemLayer,
                        cutawayFrameId: this._layerCutawayFrameId
                    });
                    return false;
                }
                if (useMazeLosClipping && itemLayer !== wizardLayer && !itemOnFallRevealLayer) {
                    itemMazeHiddenCount += 1;
                    if (item.type === "wallSection") {
                        wallMazeHiddenCount += 1;
                    }
                    this.logPlaceObjectRenderDebug("filter-drop-maze-layer", item, {
                        itemLayer,
                        wizardLayer
                    });
                    return false;
                }
                const planHasItem = buildingInteriorRenderPlan.items.has(item);
                const prototypeInteriorLiveFullAlpha = !planHasItem &&
                    this.shouldRenderPrototypeBuildingInteriorLiveItemAtFullAlpha(item, cutawayState, mapRef);
                if (
                    !planHasItem &&
                    this.shouldSuppressPrototypeBuildingInteriorLiveRenderItem(item, mapRef, cutawayState, ctx) &&
                    !prototypeInteriorLiveFullAlpha
                ) {
                    this.logPlaceObjectRenderDebug("filter-drop-prototype-building-interior-live", item, {
                        itemLayer
                    });
                    return false;
                }
                const layerAlpha = prototypeInteriorLiveFullAlpha
                    ? 1
                    : this.getLiveLayerFadeMultiplier(itemLayer, renderNowMs);
                const cutawayAlpha = this.getBuildingCutawayAlphaForItem(item);
                const renderAlpha = this.getRenderAlphaForObjectItem(item, layerAlpha, cutawayAlpha, buildingInteriorRenderPlan);
                item._renderLayerIndex = itemLayer;
                item._renderLayerFadeAlpha = layerAlpha;
                item._renderLayerAlpha = renderAlpha;
                item._renderLayerBaseZ = this.getLayerBaseZForObject(item, itemLayer);
                const baseRenderDepthBias = Number.isFinite(item.renderDepthOffset)
                    ? Number(item.renderDepthOffset)
                    : 0;
                item._renderDepthBias = baseRenderDepthBias + (
                    this.shouldApplyBuildingInteriorActiveFloorDepthBumpToItem(item, itemLayer, buildingInteriorRenderPlan, mapRef)
                        ? BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS
                        : 0
                );
                if (
                    !buildingInteriorRenderPlan.items.has(item) &&
                    this.isRenderItemHiddenByLayerCutaway(item, itemLayer, cutawayState, mapRef)
                ) {
                    itemCutawayHiddenCount += 1;
                    this.logPlaceObjectRenderDebug("filter-drop-layer-cutaway", item, {
                        itemLayer,
                        layerAlpha,
                        cutawayAlpha,
                        renderAlpha
                    });
                    return false;
                }
                if (!(renderAlpha > 0.001)) {
                    this.logPlaceObjectRenderDebug("filter-drop-alpha", item, {
                        itemLayer,
                        layerAlpha,
                        cutawayAlpha,
                        renderAlpha
                    });
                    return false;
                }
                if (!itemOnFallRevealLayer && isAnimalHiddenByLos(item)) {
                    animalLosHiddenCount += 1;
                    this.logPlaceObjectRenderDebug("filter-drop-animal-los", item, { itemLayer });
                    return false;
                }
                const forceBuildingInteriorDoorVisible = !!(
                    buildingInteriorRenderPlan.items.has(item) &&
                    this.isBuildingCutawayDoorItem(item)
                );
                if (!itemOnFallRevealLayer && !forceBuildingInteriorDoorVisible && isItemHiddenByMazeLos(item)) {
                    itemMazeHiddenCount += 1;
                    if (item.type === "wallSection") {
                        wallMazeHiddenCount += 1;
                    }
                    this.logPlaceObjectRenderDebug("filter-drop-maze-los", item, { itemLayer });
                    return false;
                }
                this.logPlaceObjectRenderDebug("filter-keep", item, {
                    itemLayer,
                    layerAlpha,
                    cutawayAlpha,
                    renderAlpha,
                    visibleObjectsCount: visibleObjects.length
                });
                return true;
            });
            const filterMs = diagnosticsEnabled ? (performance.now() - filterStartMs) : 0;
            const inMazeModeActivationRevealBypassWindow = !!(
                useMazeLosClipping &&
                Number.isFinite(this.mazeModeActivatedAtMs) &&
                (renderNowMs - Number(this.mazeModeActivatedAtMs)) <= MAZE_MODE_ACTIVATION_SKIP_REVEAL_MS
            );
            const skipMazeRevealAnimationForActivation = !!(
                useMazeLosClipping &&
                (
                    this.mazeModeSuppressRevealAnimation ||
                    inMazeModeActivationRevealBypassWindow
                )
            );
            if (!this._renderObjects3DRoofItems) this._renderObjects3DRoofItems = [];
            const roofItems = this._renderObjects3DRoofItems;
            roofItems.length = 0;
            const _allRoofsList = this.getRoofsList(ctx);
            for (let _ri = 0; _ri < _allRoofsList.length; _ri++) {
                const _r = _allRoofsList[_ri];
                if (_r && !_r.gone && this.isScriptVisible(_r)) roofItems.push(_r);
            }
            if (!this._renderObjects3DFilteredRoofItems) this._renderObjects3DFilteredRoofItems = [];
            const filteredRoofItems = this._renderObjects3DFilteredRoofItems;
            filteredRoofItems.length = 0;
            for (let i = 0; i < roofItems.length; i++) {
                const roofRef = roofItems[i];
                const roofLayer = this.getLayerIndexForRoof(roofRef, 0);
                const roofOnFallRevealLayer = this.isFallRevealHiddenLayer(roofLayer);
                if (useMazeLosClipping && roofLayer !== wizardLayer && !roofOnFallRevealLayer) continue;
                if (
                    buildingCompositeCacheUsable &&
                    roofRef._cutawayCompositeFrame === this._layerCutawayFrameId &&
                    !buildingInteriorRenderPlan.items.has(roofRef)
                ) continue;
                const roofLayerAlpha = this.getLiveLayerFadeMultiplier(roofLayer, renderNowMs);
                const roofCutawayAlpha = this.getBuildingCutawayAlphaForItem(roofRef);
                const roofRenderAlpha = roofLayerAlpha * roofCutawayAlpha;
                roofRef._renderLayerIndex = roofLayer;
                roofRef._renderLayerFadeAlpha = roofLayerAlpha;
                roofRef._renderLayerAlpha = roofRenderAlpha;
                if (this.isRenderItemHiddenByLayerCutaway(roofRef, roofLayer, cutawayState, mapRef)) continue;
                if (!roofOnFallRevealLayer && isItemHiddenByMazeLos(roofRef)) continue;
                if (roofRenderAlpha > 0.001) {
                    filteredRoofItems.push(roofRef);
                }
            }
            if (!this._renderObjects3DVisibleRoofSet) this._renderObjects3DVisibleRoofSet = new Set();
            const visibleRoofSet = this._renderObjects3DVisibleRoofSet;
            visibleRoofSet.clear();
            for (let _vri = 0; _vri < filteredRoofItems.length; _vri++) visibleRoofSet.add(filteredRoofItems[_vri]);
            for (let i = 0; i < roofItems.length; i++) {
                const roofRef = roofItems[i];
                if (visibleRoofSet.has(roofRef)) continue;
                if (roofRef && roofRef.pixiMesh) {
                    roofRef.pixiMesh.visible = false;
                    roofRef.pixiMesh.alpha = 0;
                }
            }
            if (!this._renderObjects3DRenderItems) this._renderObjects3DRenderItems = [];
            const renderItems = this._renderObjects3DRenderItems;
            renderItems.length = 0;
            for (let _mi = 0; _mi < mapItems.length; _mi++) renderItems.push(mapItems[_mi]);
            for (let _rri = 0; _rri < filteredRoofItems.length; _rri++) renderItems.push(filteredRoofItems[_rri]);
            this.setFrameMetric("objects3dFilterMs", filterMs);
            this.setFrameMetric("objects3dAnimalLosHidden", animalLosHiddenCount);
            this.setFrameMetric("objects3dCutawayHidden", itemCutawayHiddenCount);
            this.setFrameMetric("objects3dMazeHidden", itemMazeHiddenCount);
            this.setFrameMetric("objects3dMazeHiddenWalls", wallMazeHiddenCount);
            this.setFrameMetric("objects3dMissingMountedSectionFiltered", missingMountedSectionFilteredCount);
            this.setFrameMetric("objects3dMapItems", mapItems.length);
            this.setFrameMetric("objects3dRoofItems", filteredRoofItems.length);
            this.setFrameMetric("objects3dRenderItems", renderItems.length);
            if (diagnosticsEnabled && isDrawPassBreakdownEnabled()) {
                const mapItemTypeCounts = this.createRenderItemTypeMetricCounts();
                const renderItemTypeCounts = this.createRenderItemTypeMetricCounts();
                for (let i = 0; i < mapItems.length; i++) {
                    this.incrementRenderItemTypeMetricCount(mapItemTypeCounts, mapItems[i]);
                }
                for (let i = 0; i < renderItems.length; i++) {
                    this.incrementRenderItemTypeMetricCount(renderItemTypeCounts, renderItems[i]);
                }
                this.setRenderItemTypeFrameMetrics("objects3dMapItems", mapItemTypeCounts);
                this.setRenderItemTypeFrameMetrics("objects3dRenderItems", renderItemTypeCounts);
            }
            for (let i = 0; i < renderItems.length; i++) {
                this.updateSinkAnimation(renderItems[i], renderNowMs);
            }

            const transformStartMs = nowIfEnabled();
            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item) continue;
                if (item.type === "roof") {
                    this.updateRoofPreview(item, ctx.wizard || global.wizard || null);
                    continue;
                }
                if (item.type === "straightStair" || item.type === "treadPathStair") {
                    continue;
                }
                const isWallMountedSpatial = this.isWallMountedSpatialItem(item);
                if (!isWallMountedSpatial) {
                    if (item.skipTransform && typeof item.draw === "function") {
                        item.draw();
                    } else {
                        this.applySpriteTransform(item);
                    }
                }
            }
            const transformMs = diagnosticsEnabled ? (performance.now() - transformStartMs) : 0;
            this.setFrameMetric("objects3dTransformMs", transformMs);
            const depthStartMs = nowIfEnabled();
            const depthBillboardRenderedItems = this.renderDepthBillboardObjects(ctx, renderItems, buildingInteriorRenderPlan, cutawayState);
            const depthMs = diagnosticsEnabled ? (performance.now() - depthStartMs) : 0;
            this.setFrameMetric("objects3dDepthMs", depthMs);
            this.setFrameMetric("objects3dDepthRendered", depthBillboardRenderedItems.size);
            const groundStartMs = nowIfEnabled();
            const groundObjectsRenderedItems = this.renderGroundObjects(ctx, renderItems, depthBillboardRenderedItems);
            const groundMs = diagnosticsEnabled ? (performance.now() - groundStartMs) : 0;
            this.setFrameMetric("objects3dGroundMs", groundMs);
            this.setFrameMetric("objects3dGroundRendered", groundObjectsRenderedItems.size);
            if (!this._currentDisplayObjectSets) this._currentDisplayObjectSets = [new Set(), new Set()];
            this._currentDisplayObjectIdx = ((this._currentDisplayObjectIdx || 0) + 1) % 2;
            const currentDisplayObjects = this._currentDisplayObjectSets[this._currentDisplayObjectIdx];
            currentDisplayObjects.clear();
            const buildingMaskStartMs = nowIfEnabled();
            const buildingCutawayGroundMasks = this.renderBuildingCutawayGroundMasks(ctx, cutawayState, container);
            const buildingMaskMs = diagnosticsEnabled ? (performance.now() - buildingMaskStartMs) : 0;
            this.setFrameMetric("objects3dBuildingMaskMs", buildingMaskMs);
            if (Array.isArray(buildingCutawayGroundMasks)) {
                for (let i = 0; i < buildingCutawayGroundMasks.length; i++) {
                    if (buildingCutawayGroundMasks[i]) currentDisplayObjects.add(buildingCutawayGroundMasks[i]);
                }
            }
            const displayStartMs = nowIfEnabled();

            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item) continue;
                if (depthBillboardRenderedItems.has(item)) continue;
                if (groundObjectsRenderedItems.has(item)) continue;
                if (item.type === "roadPath") {
                    if (item.pixiSprite) {
                        item.pixiSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = false;
                        }
                    }
                    continue;
                }
                if (item.type === "triggerArea") {
                    const triggerOverlayContainer = (!showPickerScreen && this.layers && this.layers.ui)
                        ? this.layers.ui
                        : container;
                    this.renderTriggerAreaOmnivisionOutline(item, triggerOverlayContainer, omnivisionActive, wizard);
                    this.renderTriggerAreaVertexMarkers(item, triggerOverlayContainer, wizard, ctx);
                    if (item._triggerOutlineGraphics) {
                        currentDisplayObjects.add(item._triggerOutlineGraphics);
                    }
                    if (item._triggerVertexGraphics) {
                        currentDisplayObjects.add(item._triggerVertexGraphics);
                    }
                    if (item.pixiSprite) {
                        this.applySpriteTransform(item);
                        item.pixiSprite.visible = false;
                        if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                            item.pixiSprite.renderable = false;
                        }
                        if (this.shouldShowTriggerAreaPickerPolygon()) {
                            this.addPickRenderItem(item, item.pixiSprite, { forceInclude: true });
                        }
                    }
                    continue;
                }
                const isStairItem = item.type === "straightStair" || item.type === "treadPathStair";
                let displayObj = isStairItem
                    ? (item.type === "treadPathStair"
                        ? this.getTreadPathStairRenderDisplayObject(item, ctx)
                        : this.getStraightStairRenderDisplayObject(item, ctx))
                    : ((item.type === "roof")
                        ? (item.pixiMesh || null)
                        : (item._flowerBurnFragmentContainer || item.pixiSprite || null));
                const wallCtor = global.WallSectionUnit || null;
                const wallBottomOutlineOnly = !!(
                    item.type === "wallSection" &&
                    wallCtor &&
                    wallCtor._showBottomFaceOnlyDebug
                );
                if (
                    item.type === "wallSection" &&
                    typeof item.getDepthMeshDisplayObject === "function" &&
                    !wallBottomOutlineOnly
                ) {
                    const interiorTopFaceOnly = buildingInteriorRenderPlan.wallTopFaceOnly.get(item) === true;
                    const clipWallToLosVisibleSpan = !!(
                        useMazeLosClipping &&
                        isWallDirectlyVisibleByMazeLos(item)
                    );
                    const wallLayerAlpha = Number.isFinite(item._renderLayerAlpha)
                        ? Math.max(0, Math.min(1, Number(item._renderLayerAlpha)))
                        : 1;
                    const depthDisplay = item.getDepthMeshDisplayObject({
                        camera: this.camera,
                        app: ctx.app,
                        viewscale: this.camera.viewscale,
                        xyratio: this.camera.xyratio,
                        worldToScreenFn: (pt) => this.camera.worldToScreen(Number(pt && pt.x) || 0, Number(pt && pt.y) || 0, 0),
                        // Use regular wall geometry; LOS visibility still filters which walls render.
                        mazeMode: false,
                        topFaceOnly: interiorTopFaceOnly,
                        bottomFaceOnly: !!(item.type === "wallSection" && wallCtor && wallCtor._showBottomFaceOnlyDebug),
                        clipToLosVisibleSpan: clipWallToLosVisibleSpan,
                        skipMazeRevealAnimation: !!(
                            this.mazeModeJustActivatedFrame ||
                            skipMazeRevealAnimationForActivation
                        ),
                        visibleWallIdSet,
                        nowMs: renderNowMs,
                        player: wizard,
                        tint: item.pixiSprite && Number.isFinite(item.pixiSprite.tint)
                            ? item.pixiSprite.tint
                            : 0xFFFFFF,
                        alpha: this.getScriptDisplayAlpha(item) * wallLayerAlpha,
                        brightness: Number.isFinite(item.brightness)
                            ? Number(item.brightness)
                            : 0
                    });
                    if (depthDisplay) {
                        displayObj = depthDisplay;
                    } else if (useMazeLosClipping) {
                        displayObj = null;
                    }
                }
                if (
                    (item.type === "beam" || item.type === "column") &&
                    typeof item.getDepthMeshDisplayObject === "function"
                ) {
                    const depthDisplay = item.getDepthMeshDisplayObject({
                        camera: this.camera,
                        app: ctx.app,
                        viewscale: this.camera.viewscale,
                        xyratio: this.camera.xyratio,
                        tint: 0xFFFFFF,
                        alpha: this.getScriptDisplayAlpha ? this.getScriptDisplayAlpha(item) : 1,
                        brightness: Number.isFinite(item.brightness) ? Number(item.brightness) : 0
                    });
                    if (depthDisplay) displayObj = depthDisplay;
                }
                if (wallBottomOutlineOnly && item._depthDisplayMesh) {
                    if (item._depthDisplayMesh.parent) {
                        item._depthDisplayMesh.parent.removeChild(item._depthDisplayMesh);
                    }
                    item._depthDisplayMesh.visible = false;
                }
                if (!displayObj) continue;
                const isRoofItem = item.type === "roof";
                if (isRoofItem && !displayObj.visible) {
                    if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                        displayObj.renderable = false;
                    }
                    item._renderingDisplayObject = displayObj;
                    continue;
                }
                if (displayObj.parent !== container) {
                    container.addChild(displayObj);
                }
                if (!isRoofItem) {
                    displayObj.visible = true;
                }
                item._renderingDisplayObject = displayObj;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = true;
                }
                if (isStairItem) {
                    this.logPlaceObjectRenderDebug("display-stair-object", item, {
                        displayObjectName: displayObj && displayObj.name ? displayObj.name : "",
                        containerName: container && container.name ? container.name : ""
                    });
                    currentDisplayObjects.add(displayObj);
                    this.addPickRenderItem(item, displayObj);
                    continue;
                }
                this.applyScriptBrightness(item, displayObj);
                this.applyFrozenTint(item, displayObj);
                this.applyLayerDarknessForItem(item, item._renderLayerIndex, displayObj);
                const displayVisibleAfterSinkClip = this.applySinkClip(item, displayObj);
                this.logPlaceObjectRenderDebug("display-object", item, {
                    displayVisibleAfterSinkClip,
                    displayObjectName: displayObj && displayObj.name ? displayObj.name : "",
                    containerName: container && container.name ? container.name : ""
                });
                if (displayVisibleAfterSinkClip) {
                    currentDisplayObjects.add(displayObj);
                }
                if (
                    item.type === "roof" &&
                    typeof PIXI !== "undefined" &&
                    displayObj instanceof PIXI.Container
                ) {
                    const roofChildren = Array.isArray(displayObj.children) ? displayObj.children : [];
                    for (let c = 0; c < roofChildren.length; c++) {
                        const child = roofChildren[c];
                        if (!child) continue;
                        if (!(child instanceof PIXI.Mesh) && !(child instanceof PIXI.Sprite)) continue;
                        if (displayVisibleAfterSinkClip) {
                            this.addPickRenderItem(item, child);
                        }
                    }
                } else if (displayVisibleAfterSinkClip) {
                    this.addPickRenderItem(item, displayObj);
                }
            }

            const buildingCompositeStartMs = nowIfEnabled();
            const buildingCutawayComposite = this.renderBuildingCutawayComposites(
                ctx,
                cutawayState,
                container,
                buildingCutawayGroundMasks,
                buildingInteriorRenderPlan
            );
            const buildingCompositeMs = diagnosticsEnabled ? (performance.now() - buildingCompositeStartMs) : 0;
            this.setFrameMetric("objects3dBuildingCompositeMs", buildingCompositeMs);
            if (buildingCutawayComposite) currentDisplayObjects.add(buildingCutawayComposite);
            this.renderActiveBuildingInteriorOverlay(ctx, cutawayState, container, renderItems);
            this.promoteActiveBuildingInteriorRegions(ctx, cutawayState, container, currentDisplayObjects, buildingInteriorRenderPlan);
            const prototypeInteriorMeshes = this.renderPrototypeBuildingInteriors(ctx);
            if (prototypeInteriorMeshes && typeof prototypeInteriorMeshes.forEach === "function") {
                prototypeInteriorMeshes.forEach(mesh => {
                    if (mesh) currentDisplayObjects.add(mesh);
                });
            }
            this.promotePrototypeBuildingInteriorLiveRenderItems(
                ctx,
                cutawayState,
                container,
                currentDisplayObjects,
                renderItems,
                buildingInteriorRenderPlan
            );
            this.renderPrototypeBuildingExteriorGroundDoors(
                ctx,
                cutawayState,
                buildingInteriorRenderPlan,
                container,
                currentDisplayObjects
            );

            // Ensure wall-mounted depth billboards (windows/doors) win picker hits over wall sections.
            // Their visible pixels should be targetable even when coplanar with section meshes.
            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item || !depthBillboardRenderedItems.has(item)) continue;
                const isWallMountedSpatial = this.isWallMountedSpatialItem(item);
                if (!isWallMountedSpatial) continue;
                const mesh = item._renderingDepthMesh;
                if (!mesh || !mesh.parent || !mesh.visible) continue;
                this.addPickRenderItem(item, mesh, { forceInclude: true });
            }

            for (const obj of this._currentDisplayObjectSets[1 - this._currentDisplayObjectIdx]) {
                if (!currentDisplayObjects.has(obj) && obj) {
                    obj.visible = false;
                }
            }
            this.activeObjectDisplayObjects = currentDisplayObjects;

            if (!this._visibleAnimalItemSets) this._visibleAnimalItemSets = [new Set(), new Set()];
            this._visibleAnimalItemIdx = ((this._visibleAnimalItemIdx || 0) + 1) % 2;
            const visibleAnimalItems = this._visibleAnimalItemSets[this._visibleAnimalItemIdx];
            visibleAnimalItems.clear();
            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item) continue;
                if (!(typeof Animal !== "undefined" && item instanceof Animal)) continue;
                visibleAnimalItems.add(item);
                if (typeof item.updateHealthBarOverlay === "function") {
                    item.updateHealthBarOverlay(this.camera, this.layers.entities || this.getCharacterLayer());
                }
            }
            for (const animal of this._visibleAnimalItemSets[1 - this._visibleAnimalItemIdx]) {
                if (visibleAnimalItems.has(animal)) continue;
                if (animal && typeof animal.hideHealthBarOverlay === "function") {
                    animal.hideHealthBarOverlay();
                }
            }
            this.activeAnimalHealthBarItems = visibleAnimalItems;

            if (!this._visibleTreeItemSets) this._visibleTreeItemSets = [new Set(), new Set()];
            this._visibleTreeItemIdx = ((this._visibleTreeItemIdx || 0) + 1) % 2;
            const visibleTreeItems = this._visibleTreeItemSets[this._visibleTreeItemIdx];
            visibleTreeItems.clear();
            for (let i = 0; i < renderItems.length; i++) {
                const item = renderItems[i];
                if (!item || item.type !== "tree") continue;
                if (typeof item.updateHealthBarOverlay !== "function") continue;
                visibleTreeItems.add(item);
                item.updateHealthBarOverlay(this.camera, this.layers.entities || this.getCharacterLayer());
            }
            for (const tree of this._visibleTreeItemSets[1 - this._visibleTreeItemIdx]) {
                if (visibleTreeItems.has(tree)) continue;
                if (tree && typeof tree.hideHealthBarOverlay === "function") {
                    tree.hideHealthBarOverlay();
                }
            }
            this.activeTreeHealthBarItems = visibleTreeItems;

            const displayMs = diagnosticsEnabled ? (performance.now() - displayStartMs) : 0;
            this.setFrameMetric("objects3dDisplayMs", displayMs);
            this.setFrameMetric("objects3dDisplayObjects", currentDisplayObjects.size);
            this.setFrameMetric("objects3dVisibleAnimals", visibleAnimalItems.size);
            this.setFrameMetric("objects3dVisibleTrees", visibleTreeItems.size);
        }

        renderWizard(ctx) {
            const depthContainer = this.getCharacterLayer();
            const overlayContainer = (this.layers && (this.layers.entities || this.layers.characters || this.layers.depthObjects)) || null;
            const wizard = ctx.wizard;
            if (!wizard || !Number.isFinite(wizard.x) || !Number.isFinite(wizard.y)) return;
            if (!depthContainer || !overlayContainer) return;
            const keepWizardShieldInCharacterLayer = (displayObj) => {
                if (!displayObj || !depthContainer) return;
                if (displayObj.parent !== depthContainer) {
                    depthContainer.addChild(displayObj);
                }
                if (
                    displayObj.visible !== false &&
                    depthContainer.children &&
                    depthContainer.children[depthContainer.children.length - 1] !== displayObj &&
                    typeof depthContainer.setChildIndex === "function"
                ) {
                    depthContainer.setChildIndex(displayObj, depthContainer.children.length - 1);
                }
            };

            const createWizardSprite = (texture) => {
                const sprite = new PIXI.Sprite(texture || PIXI.Texture.WHITE);
                sprite.name = "renderingWizard";
                sprite.anchor.set(0.5, 0.75);
                return sprite;
            };
            const ensureWizardSprite = (forceRecreate = false) => {
                const current = this.wizardSprite;
                const transform = current && current.transform ? current.transform : null;
                const anchor = current && current._anchor ? current._anchor : null;
                const usable = !!(
                    current &&
                    current.destroyed !== true &&
                    transform &&
                    transform.scale &&
                    anchor
                );
                if (!forceRecreate && usable) {
                    return current;
                }
                if (current && current.parent) {
                    current.parent.removeChild(current);
                }
                if (current && typeof current.destroy === "function") {
                    try {
                        current.destroy({ children: false, texture: false, baseTexture: false });
                    } catch (_destroyErr) {
                        // Ignore stale Pixi teardown failures and rebuild the sprite.
                    }
                }
                const initialTexture = (Array.isArray(ctx.wizardFrames) && ctx.wizardFrames[0])
                    ? ctx.wizardFrames[0]
                    : PIXI.Texture.WHITE;
                this.wizardSprite = createWizardSprite(initialTexture);
                return this.wizardSprite;
            };

            let wizardSprite = ensureWizardSprite();
            wizard.pixiSprite = wizardSprite;
            if (wizardSprite.parent) {
                wizardSprite.parent.removeChild(wizardSprite);
            }

            const frameIndex = getWizardBodyFrameIndex(wizard, ctx);

            if (Array.isArray(ctx.wizardFrames) && ctx.wizardFrames[frameIndex]) {
                try {
                    wizardSprite.texture = ctx.wizardFrames[frameIndex];
                } catch (_textureErr) {
                    wizardSprite = ensureWizardSprite(true);
                    wizard.pixiSprite = wizardSprite;
                    try {
                        wizardSprite.texture = ctx.wizardFrames[frameIndex] || PIXI.Texture.WHITE;
                    } catch (_retryErr) {
                        wizardSprite.texture = PIXI.Texture.WHITE;
                    }
                }
            }
            const wizardBodySourceTexture = wizardSprite.texture || PIXI.Texture.WHITE;

            const invisibilityActive = this.isInvisibilityActive(wizard);
            const wizardAlpha = invisibilityActive ? 0.45 : 1;
            wizardSprite.alpha = wizardAlpha;

            const alpha = Number.isFinite(ctx.renderAlpha)
                ? Math.max(0, Math.min(1, ctx.renderAlpha))
                : 1;
            const renderPos = (wizard && typeof wizard.getInterpolatedPosition === "function")
                ? wizard.getInterpolatedPosition(alpha)
                : {
                    x: Number.isFinite(wizard.x) ? wizard.x : 0,
                    y: Number.isFinite(wizard.y) ? wizard.y : 0,
                    z: Number.isFinite(wizard.z) ? wizard.z : 0
                };
            const support = wizard && wizard.currentMovementSupport && typeof wizard.currentMovementSupport === "object"
                ? wizard.currentMovementSupport
                : null;
            const wizardLayer = this.getLayerIndexFromValue(
                support && Number.isFinite(support.layer)
                    ? support.layer
                    : wizard.currentLayer,
                0
            );
            const wizardLayerBaseZ = support && Number.isFinite(support.baseZ)
                ? Number(support.baseZ)
                : (Number.isFinite(wizard.currentLayerBaseZ)
                    ? Number(wizard.currentLayerBaseZ)
                    : this.getLayerBaseZForLevel(wizardLayer));
            const mapRef = (ctx && ctx.map) || (this.camera && this.camera.map) || global.map || null;
            const cutawayState = this.getLayerCutawayState(ctx);
            const buildingInteriorRenderPlan = this.buildBuildingInteriorRenderPlan(ctx, cutawayState);
            const wizardOnActiveInteriorStair = !!(
                support &&
                support.type === "stair" &&
                this.cutawayStateHasActiveInteriorRegion(cutawayState)
            );
            const wizardActiveFloorDepthBump = (
                wizardOnActiveInteriorStair ||
                this.shouldApplyBuildingInteriorActiveFloorDepthBumpToItem(
                    wizard,
                    wizardLayer,
                    buildingInteriorRenderPlan,
                    mapRef
                )
            ) ? BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS : 0;
            const wizardBodyDepthBump = wizardActiveFloorDepthBump > 0
                ? wizardActiveFloorDepthBump + BUILDING_INTERIOR_WIZARD_BODY_DEPTH_LIFT_UNITS
                : WIZARD_BODY_GROUND_DEPTH_BIAS_UNITS;
            const pGround = this.camera.worldToScreen(renderPos.x, renderPos.y, wizardLayerBaseZ);
            const interpolatedJumpHeight = Number.isFinite(renderPos.z) ? renderPos.z : 0;
            let stairSurfaceLocalZ = NaN;
            let wizardVisualLocalZ = interpolatedJumpHeight;
            if (support && support.type === "stair") {
                stairSurfaceLocalZ = this.getWizardStairRenderSurfaceLocalZ(support, mapRef, interpolatedJumpHeight);
                if (Number.isFinite(stairSurfaceLocalZ)) {
                    const continuousLocalZ = Number.isFinite(Number(support.continuousLocalZ))
                        ? Number(support.continuousLocalZ)
                        : (Number.isFinite(Number(support.localZ)) ? Number(support.localZ) : stairSurfaceLocalZ);
                    const jumpLift = Math.max(0, interpolatedJumpHeight - continuousLocalZ);
                    wizardVisualLocalZ = stairSurfaceLocalZ + jumpLift;
                }
            }
            const jumpHeight = Number.isFinite(wizard.jumpHeight) ? Math.max(0, Number(wizard.jumpHeight)) : 0;
            let shadowLocalZ = 0;
            if (wizard._floorFallState && wizard._floorFallState.active) {
                shadowLocalZ = 0;
            } else if (support && support.type === "stair" && Number.isFinite(stairSurfaceLocalZ)) {
                shadowLocalZ = stairSurfaceLocalZ;
            } else if (support && support.type === "stair" && Number.isFinite(Number(support.continuousLocalZ))) {
                shadowLocalZ = Number(support.continuousLocalZ);
            } else if (support && support.type === "stair" && Number.isFinite(Number(support.localZ))) {
                shadowLocalZ = Number(support.localZ);
            } else if (wizard.isJumping) {
                shadowLocalZ = 0;
            } else {
                shadowLocalZ = Math.max(0, interpolatedJumpHeight - jumpHeight);
            }
            const jumpOffsetPx = wizardVisualLocalZ * this.camera.viewscale * this.camera.xyratio;
            const wizardCenterY = pGround.y - jumpOffsetPx - (this.camera.viewscale * 0.25);
            const renderNowMs = Number.isFinite(ctx.renderNowMs)
                ? Number(ctx.renderNowMs)
                : ((typeof performance !== "undefined" && performance && typeof performance.now === "function")
                    ? performance.now()
                    : Date.now());
            const deathAnimationActive = !!(
                wizard &&
                typeof wizard.isAdventureDeathAnimationActive === "function" &&
                wizard.isAdventureDeathAnimationActive(renderNowMs)
            );
            const deathAnimationProgress = deathAnimationActive && typeof wizard.getAdventureDeathAnimationProgress === "function"
                ? wizard.getAdventureDeathAnimationProgress(renderNowMs)
                : 0;
            const drowningDeathActive = deathAnimationActive && wizard && wizard._adventureDeathCause === "drowning";
            const ghostSprite = this.ensureWizardGhostSprite();
            const wizardBodyFullWorldHeight = 1 / Math.max(1e-6, Math.abs(Number(this.camera.xyratio) || 1));
            const wizardAirborneForWater = wizard.isJumping === true || wizardVisualLocalZ > 0.05;
            const wizardWaterImmersion = (deathAnimationActive && !drowningDeathActive) || wizardAirborneForWater
                ? null
                : this.getWizardWaterImmersionState(mapRef, renderPos, wizardLayer, wizard);
            const wizardWaterRenderState = this.getWizardWaterBodyRenderState(
                wizardWaterImmersion,
                wizardBodyFullWorldHeight
            );
            const wizardBodyVisibleRatio = deathAnimationActive ? 1 : wizardWaterRenderState.visibleRatio;
            const wizardWaterLowerBodyScreenPx = WIZARD_BODY_VISUAL_LOWER_UNITS * this.camera.viewscale * this.camera.xyratio;
            wizardSprite.texture = deathAnimationActive
                ? wizardBodySourceTexture
                : this.getWizardWaterCroppedTexture(wizardBodySourceTexture, wizardBodyVisibleRatio);
            wizardSprite.anchor.set(0.5, deathAnimationActive ? 0.5 : 0.75);

            wizardSprite.width = this.camera.viewscale;
            wizardSprite.height = this.camera.viewscale * wizardBodyVisibleRatio;
            wizardSprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(wizardSprite, "renderable")) {
                wizardSprite.renderable = false;
            }
            if (ghostSprite) {
                ghostSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(ghostSprite, "renderable")) {
                    ghostSprite.renderable = false;
                }
            }

            if (deathAnimationActive) {
                if (wizard._renderingDepthMesh) {
                    wizard._renderingDepthMesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(wizard._renderingDepthMesh, "renderable")) {
                        wizard._renderingDepthMesh.renderable = false;
                    }
                }
                const shadowProxy = this.ensureWizardShadowProxy();
                if (shadowProxy && shadowProxy._renderingDepthMesh) {
                    shadowProxy._renderingDepthMesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(shadowProxy._renderingDepthMesh, "renderable")) {
                        shadowProxy._renderingDepthMesh.renderable = false;
                    }
                }
                if (wizardSprite.parent !== overlayContainer) {
                    overlayContainer.addChild(wizardSprite);
                }
                wizardSprite.anchor.set(0.5, 0.5);
                wizardSprite.x = pGround.x;
                wizardSprite.y = wizardCenterY;
                wizardSprite.rotation = drowningDeathActive ? 0 : Math.PI / 2;
                wizardSprite.alpha = wizardAlpha;
                wizardSprite.visible = !drowningDeathActive;
                if (Object.prototype.hasOwnProperty.call(wizardSprite, "renderable")) {
                    wizardSprite.renderable = !drowningDeathActive;
                }

                if (ghostSprite) {
                    if (ghostSprite.parent !== overlayContainer) {
                        overlayContainer.addChild(ghostSprite);
                    }
                    const riseDistance = this.camera.viewscale * 0.55;
                    ghostSprite.x = pGround.x;
                    ghostSprite.y = wizardCenterY - (this.camera.viewscale * 0.05) - (riseDistance * deathAnimationProgress);
                    ghostSprite.width = this.camera.viewscale * 0.8;
                    ghostSprite.height = this.camera.viewscale * 0.95;
                    ghostSprite.alpha = Math.max(0, Math.min(1, deathAnimationProgress / 0.2));
                    ghostSprite.visible = true;
                    if (Object.prototype.hasOwnProperty.call(ghostSprite, "renderable")) {
                        ghostSprite.renderable = true;
                    }
                }
            }

            const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                ? global.StaticObject.prototype
                : null;
            let wizardDepthMesh = null;
            wizard._renderLayerBaseZ = wizardLayerBaseZ;
            wizard._renderDepthBias = wizardBodyDepthBump;
            wizard._renderDepthFlattenZ = true;
            if (!deathAnimationActive && staticProto && typeof staticProto.updateDepthBillboardMesh === "function") {
                if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                    wizard.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                }
                if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                    wizard.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                }
                const savedX = wizard.x;
                const savedY = wizard.y;
                const savedZ = wizard.z;
                wizard.x = renderPos.x;
                wizard.y = renderPos.y;
                wizard.z = wizardVisualLocalZ - WIZARD_BODY_VISUAL_LOWER_UNITS;
                wizardDepthMesh = staticProto.updateDepthBillboardMesh.call(wizard, ctx, this.camera, {
                    alphaCutoff: TREE_ALPHA_CUTOFF,
                    mazeMode: false,
                    player: wizard
                });
                wizard.x = savedX;
                wizard.y = savedY;
                wizard.z = savedZ;
            }
            if (wizardDepthMesh) {
                wizard._renderingDepthMesh = wizardDepthMesh;
                if (wizardDepthMesh.parent !== depthContainer) {
                    depthContainer.addChild(wizardDepthMesh);
                }
                wizardDepthMesh.zIndex = wizardActiveFloorDepthBump > 0 ? BUILDING_INTERIOR_WIZARD_BODY_Z : 0;
                wizardDepthMesh.alpha = wizardAlpha;
                wizardDepthMesh.visible = true;
                if (Object.prototype.hasOwnProperty.call(wizardDepthMesh, "renderable")) {
                    wizardDepthMesh.renderable = true;
                }
                if (Object.prototype.hasOwnProperty.call(depthContainer, "sortableChildren")) {
                    depthContainer.sortableChildren = true;
                }
                if (Object.prototype.hasOwnProperty.call(depthContainer, "sortDirty")) {
                    depthContainer.sortDirty = true;
                }
                this.addPickRenderItem(wizard, wizardDepthMesh, { forceInclude: true });
            } else if (wizard._renderingDepthMesh) {
                wizard._renderingDepthMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(wizard._renderingDepthMesh, "renderable")) {
                    wizard._renderingDepthMesh.renderable = false;
                }
            }

            const shadowProxy = this.ensureWizardShadowProxy();
            if (
                !deathAnimationActive &&
                !wizardWaterRenderState.inWater &&
                shadowProxy &&
                typeof shadowProxy.updateDepthBillboardMesh === "function"
            ) {
                shadowProxy._renderLayerBaseZ = wizardLayerBaseZ;
                shadowProxy._renderDepthBias = WIZARD_SHADOW_DEPTH_BIAS_UNITS + wizardActiveFloorDepthBump;
                shadowProxy.map = wizard.map || global.map || null;
                shadowProxy.x = renderPos.x;
                shadowProxy.y = renderPos.y + 0.18;
                shadowProxy.z = shadowLocalZ;
                shadowProxy.visible = true;
                shadowProxy.gone = false;
                shadowProxy.vanishing = false;
                shadowProxy.width = 0.44;
                shadowProxy.height = 0.44;
                shadowProxy.pixiSprite.width = 0.44 * this.camera.viewscale;
                shadowProxy.pixiSprite.height = 0.44 * this.camera.viewscale;
                shadowProxy.pixiSprite.alpha = invisibilityActive ? 0.25 : 1;
                shadowProxy.pixiSprite.tint = 0xFFFFFF;
                const shadowMesh = shadowProxy.updateDepthBillboardMesh(ctx, this.camera, {
                    alphaCutoff: 0.01,
                    mazeMode: false,
                    player: wizard
                });
                if (shadowMesh) {
                    shadowProxy._renderingDepthMesh = shadowMesh;
                    if (shadowMesh.parent !== depthContainer) {
                        depthContainer.addChild(shadowMesh);
                    }
                    shadowMesh.zIndex = wizardActiveFloorDepthBump > 0 ? BUILDING_INTERIOR_WIZARD_SHADOW_Z : 0;
                    shadowMesh.alpha = invisibilityActive ? 0.25 : 1;
                    shadowMesh.visible = true;
                    if (Object.prototype.hasOwnProperty.call(shadowMesh, "renderable")) {
                        shadowMesh.renderable = true;
                    }
                    if (Object.prototype.hasOwnProperty.call(depthContainer, "sortableChildren")) {
                        depthContainer.sortableChildren = true;
                    }
                    if (Object.prototype.hasOwnProperty.call(depthContainer, "sortDirty")) {
                        depthContainer.sortDirty = true;
                    }
                }
            } else if (shadowProxy && shadowProxy._renderingDepthMesh) {
                shadowProxy._renderingDepthMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(shadowProxy._renderingDepthMesh, "renderable")) {
                    shadowProxy._renderingDepthMesh.renderable = false;
                }
            }

            const hat = wizard.hatGraphics;
            if (wizard && typeof wizard.drawShield === "function") {
                if (deathAnimationActive) {
                    if (wizard.shieldGraphics) {
                        wizard.shieldGraphics.visible = false;
                    }
                    if (wizard.shieldWireframeMesh) {
                        wizard.shieldWireframeMesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(wizard.shieldWireframeMesh, "renderable")) {
                            wizard.shieldWireframeMesh.renderable = false;
                        }
                    }
                } else {
                    wizard.drawShield(interpolatedJumpHeight, renderPos);
                    keepWizardShieldInCharacterLayer(wizard.shieldGraphics);
                    keepWizardShieldInCharacterLayer(wizard.shieldDebrisGraphics);
                    keepWizardShieldInCharacterLayer(wizard.shieldWireframeMesh);
                }
            }
            if (hat && typeof hat === "object") {
                if (hat.parent !== overlayContainer) {
                    overlayContainer.addChild(hat);
                }
                const hatLiftPx = WIZARD_HAT_LIFT_UNITS * this.camera.viewscale * this.camera.xyratio;
                let hatAlpha = wizardAlpha;
                let drowningHatWaterlineY = NaN;
                if (deathAnimationActive) {
                    if (drowningDeathActive) {
                        const hatYOffset = (Number.isFinite(wizard.hatRenderYOffsetUnits) ? wizard.hatRenderYOffsetUnits : 0)
                            * this.camera.viewscale * this.camera.xyratio;
                        const baseHatY = pGround.y - jumpOffsetPx - hatYOffset - hatLiftPx;
                        const hatRenderScale = Number.isFinite(wizard.hatRenderScale) ? Math.max(0.05, wizard.hatRenderScale) : 1;
                        const hatLocalVisibleHeightPx = this.camera.viewscale * hatRenderScale * 0.5;
                        let drownedHatStartY = baseHatY;
                        if (wizardWaterRenderState.inWater) {
                            let renderedBodyTopY = NaN;
                            const bodyWorldPositions = wizard._depthBillboardWorldPositions;
                            if (bodyWorldPositions && bodyWorldPositions.length >= 12) {
                                for (let i = 2; i < bodyWorldPositions.length; i += 3) {
                                    const sx = Number(bodyWorldPositions[i - 2]);
                                    const sy = Number(bodyWorldPositions[i - 1]);
                                    const sz = Number(bodyWorldPositions[i]);
                                    if (!Number.isFinite(sx) || !Number.isFinite(sy) || !Number.isFinite(sz)) continue;
                                    const projected = this.camera.worldToScreen(sx, sy, sz + wizardLayerBaseZ);
                                    if (!projected || !Number.isFinite(projected.y)) continue;
                                    renderedBodyTopY = Number.isFinite(renderedBodyTopY)
                                        ? Math.min(renderedBodyTopY, projected.y)
                                        : projected.y;
                                }
                            }
                            if (!Number.isFinite(renderedBodyTopY)) {
                                renderedBodyTopY = pGround.y - jumpOffsetPx + wizardWaterLowerBodyScreenPx
                                    - (this.camera.viewscale * wizardWaterRenderState.visibleRatio);
                            }
                            const dryBodyTopY = pGround.y - jumpOffsetPx + wizardWaterLowerBodyScreenPx
                                - this.camera.viewscale;
                            drownedHatStartY = renderedBodyTopY + (baseHatY - dryBodyTopY);
                        }
                        drowningHatWaterlineY = drownedHatStartY - hatLocalVisibleHeightPx;
                        const deathDurationMs = Number.isFinite(wizard._adventureDeathAnimationDurationMs)
                            ? Math.max(1, Number(wizard._adventureDeathAnimationDurationMs))
                            : 4000;
                        const sinkProgressLinear = Math.max(0, Math.min(1, deathAnimationProgress * deathDurationMs / 1500));
                        const sinkProgress = sinkProgressLinear * sinkProgressLinear;
                        const sinkDistance = hatLocalVisibleHeightPx + 2;
                        hat.x = pGround.x;
                        hat.y = drownedHatStartY + sinkDistance * sinkProgress;
                        hat.rotation = 0;
                    } else {
                        const hatYOffset = (Number.isFinite(wizard.hatRenderYOffsetUnits) ? wizard.hatRenderYOffsetUnits : 0)
                            * this.camera.viewscale * this.camera.xyratio;
                        const bodyCenterToHatOriginY = (this.camera.viewscale * 0.25) - hatYOffset;
                        const deathRotation = Math.PI / 2;
                        const cosTheta = Math.cos(deathRotation);
                        const sinTheta = Math.sin(deathRotation);
                        const rotatedHatOffsetX = 0 * cosTheta - bodyCenterToHatOriginY * sinTheta;
                        const rotatedHatOffsetY = 0 * sinTheta + bodyCenterToHatOriginY * cosTheta;
                        hat.x = pGround.x + rotatedHatOffsetX;
                        hat.y = wizardCenterY + rotatedHatOffsetY - hatLiftPx;
                        hat.rotation = deathRotation;
                    }
                } else {
                    hat.x = pGround.x;
                    const hatYOffset = (Number.isFinite(wizard.hatRenderYOffsetUnits) ? wizard.hatRenderYOffsetUnits : 0)
                        * this.camera.viewscale * this.camera.xyratio;
                    const baseHatY = pGround.y - jumpOffsetPx - hatYOffset - hatLiftPx;
                    if (wizardWaterRenderState.inWater) {
                        let renderedBodyTopY = NaN;
                        const bodyWorldPositions = wizard._depthBillboardWorldPositions;
                        if (bodyWorldPositions && bodyWorldPositions.length >= 12) {
                            for (let i = 2; i < bodyWorldPositions.length; i += 3) {
                                const sx = Number(bodyWorldPositions[i - 2]);
                                const sy = Number(bodyWorldPositions[i - 1]);
                                const sz = Number(bodyWorldPositions[i]);
                                if (!Number.isFinite(sx) || !Number.isFinite(sy) || !Number.isFinite(sz)) continue;
                                const projected = this.camera.worldToScreen(sx, sy, sz + wizardLayerBaseZ);
                                if (!projected || !Number.isFinite(projected.y)) continue;
                                renderedBodyTopY = Number.isFinite(renderedBodyTopY)
                                    ? Math.min(renderedBodyTopY, projected.y)
                                    : projected.y;
                            }
                        }
                        if (!Number.isFinite(renderedBodyTopY)) {
                            renderedBodyTopY = pGround.y - jumpOffsetPx + wizardWaterLowerBodyScreenPx
                                - (this.camera.viewscale * wizardBodyVisibleRatio);
                        }
                        const dryBodyTopY = pGround.y - jumpOffsetPx + wizardWaterLowerBodyScreenPx
                            - this.camera.viewscale;
                        hat.y = renderedBodyTopY + (baseHatY - dryBodyTopY);
                    } else {
                        hat.y = baseHatY;
                    }
                    hat.rotation = 0;
                }
                if (hat.scale && typeof hat.scale.set === "function") {
                    const hatRes = Number.isFinite(wizard.hatResolution) ? Math.max(1, wizard.hatResolution) : 1;
                    const hatRenderScale = Number.isFinite(wizard.hatRenderScale) ? Math.max(0.05, wizard.hatRenderScale) : 1;
                    const s = (this.camera.viewscale / hatRes) * hatRenderScale;
                    hat.scale.set(s, s);
                }
                hat.alpha = hatAlpha;
                hat.visible = true;
                if (hat.parent && hat.parent.children[hat.parent.children.length - 1] !== hat) {
                    hat.parent.setChildIndex(hat, hat.parent.children.length - 1);
                }
                this.promoteInteriorPresentationDisplayObject(hat, ctx, BUILDING_INTERIOR_WIZARD_HAT_Z);
                if (drowningDeathActive) {
                    if (!Number.isFinite(drowningHatWaterlineY)) {
                        throw new Error("wizard drowning hat mask requires a finite waterline");
                    }
                    const maskGraphics = this.ensureWizardDrowningHatMaskGraphics(hat.parent);
                    const screenSize = this.getRendererScreenSize(ctx);
                    const pad = Math.max(screenSize.width, screenSize.height, this.camera.viewscale * 4, 1);
                    maskGraphics.clear();
                    maskGraphics.beginFill(0xffffff, 1);
                    maskGraphics.drawRect(-pad, -pad, screenSize.width + (pad * 2), drowningHatWaterlineY + pad);
                    maskGraphics.endFill();
                    maskGraphics.visible = true;
                    if (Object.prototype.hasOwnProperty.call(maskGraphics, "renderable")) {
                        maskGraphics.renderable = true;
                    }
                    hat.mask = maskGraphics;
                    if (hat.parent && hat.parent.children[hat.parent.children.length - 1] !== hat) {
                        hat.parent.setChildIndex(hat, hat.parent.children.length - 1);
                    }
                } else {
                    this.clearWizardDrowningHatMask(hat);
                }
            } else {
                this.clearWizardDrowningHatMask();
            }
        }

        applyPowerupPremultipliedDisplayAlpha(powerup, depthMesh, displayAlpha) {
            if (!powerup || !depthMesh) return;
            const alpha = Math.max(0, Math.min(1, Number.isFinite(displayAlpha) ? Number(displayAlpha) : 1));
            const sprite = powerup.pixiSprite || null;
            const baseAlpha = Math.max(0, Math.min(1, Number.isFinite(sprite && sprite.alpha)
                ? Number(sprite.alpha)
                : (Number.isFinite(powerup.billboardAlpha) ? Number(powerup.billboardAlpha) : 1)));
            const uniforms = depthMesh.shader && depthMesh.shader.uniforms ? depthMesh.shader.uniforms : null;
            const uTint = uniforms && uniforms.uTint && uniforms.uTint.length >= 4 ? uniforms.uTint : null;
            if (!uTint) {
                if (alpha < 0.999 || baseAlpha < 0.999) {
                    throw new Error(`powerup ${powerup.id || powerup.scriptingName || powerup.imageFileName || "(unknown)"} fade requires depth billboard uTint uniform`);
                }
                return;
            }
            const finalAlpha = baseAlpha * alpha;
            uTint[0] *= finalAlpha;
            uTint[1] *= finalAlpha;
            uTint[2] *= finalAlpha;
            uTint[3] = finalAlpha;
        }

        isPowerupBillboardTextureReady(texture) {
            if (!texture) return false;
            if (typeof PIXI !== "undefined" && PIXI.Texture) {
                if (texture === PIXI.Texture.WHITE || texture === PIXI.Texture.EMPTY) return false;
            }
            const baseTexture = texture.baseTexture || null;
            if (!baseTexture || baseTexture.valid !== true) return false;
            const frame = texture.frame || texture.orig || null;
            const width = Number(frame && frame.width);
            const height = Number(frame && frame.height);
            return width > 0 && height > 0;
        }

        watchPowerupPendingTexture(texture) {
            const baseTexture = texture && texture.baseTexture ? texture.baseTexture : null;
            if (!baseTexture || baseTexture.valid === true || typeof baseTexture.once !== "function") return;
            if (!(this._powerupPendingBaseTextures instanceof Set)) {
                this._powerupPendingBaseTextures = new Set();
            }
            if (this._powerupPendingBaseTextures.has(baseTexture)) return;
            this._powerupPendingBaseTextures.add(baseTexture);
            let completed = false;
            const onReady = () => {
                if (completed) return;
                completed = true;
                this._powerupPendingBaseTextures.delete(baseTexture);
                if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                    globalThis.presentGameFrame();
                }
            };
            baseTexture.once("loaded", onReady);
            baseTexture.once("error", onReady);
        }

        hidePowerupDisplayObjects(powerup, sprite = null) {
            const spriteRef = sprite || (powerup && powerup.pixiSprite) || null;
            if (spriteRef) {
                spriteRef.visible = false;
                if (Object.prototype.hasOwnProperty.call(spriteRef, "renderable")) {
                    spriteRef.renderable = false;
                }
            }
            const depthMesh = powerup && powerup._renderingDepthMesh ? powerup._renderingDepthMesh : null;
            if (depthMesh) {
                depthMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(depthMesh, "renderable")) {
                    depthMesh.renderable = false;
                }
            }
            if (powerup) {
                powerup._renderingDepthMesh = null;
                powerup._renderingDisplayObject = null;
            }
        }

        renderPowerups(ctx) {
            const depthContainer = this.layers.depthObjects;
            if (!depthContainer) return;
            const maskedContainer = this.layers.groundObjects || depthContainer;
            const wizard = (ctx && ctx.wizard) ? ctx.wizard : null;
            const mapRef = (ctx && ctx.map) ? ctx.map : (this.camera && this.camera.map) || global.map || null;
            const mazeLosActive = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const renderNowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now();
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);
            const buildingInteriorRenderPlan = this.buildBuildingInteriorRenderPlan(ctx, cutawayState);

            const list = Array.isArray(ctx && ctx.powerups)
                ? ctx.powerups
                : (Array.isArray(global.powerups) ? global.powerups : []);
            if (!this._powerupDisplayObjectSets) this._powerupDisplayObjectSets = [new Set(), new Set()];
            this._powerupDisplayObjectIdx = ((this._powerupDisplayObjectIdx || 0) + 1) % 2;
            const currentDisplayObjects = this._powerupDisplayObjectSets[this._powerupDisplayObjectIdx];
            currentDisplayObjects.clear();

            for (let i = 0; i < list.length; i++) {
                const powerup = list[i];
                if (!powerup || powerup.gone || powerup.collected) continue;
                if (!Number.isFinite(powerup.x) || !Number.isFinite(powerup.y)) continue;
                this.updateSinkAnimation(powerup, renderNowMs);
                if (typeof powerup.ensureSprite === "function") {
                    powerup.ensureSprite();
                }
                const sprite = powerup.pixiSprite;
                if (!sprite) continue;
                if (typeof powerup.updateSpriteAnimation === "function") {
                    powerup.updateSpriteAnimation();
                }
                const basePowerupAlpha = Math.max(0, Math.min(1, Number.isFinite(powerup.billboardAlpha)
                    ? Number(powerup.billboardAlpha)
                    : (Number.isFinite(sprite.alpha) ? Number(sprite.alpha) : 1)));
                sprite.alpha = basePowerupAlpha;
                const powerupLayer = this.getLayerIndexForObject(powerup, 0);
                powerup._renderLayerIndex = powerupLayer;
                const powerupOnFallRevealLayer = this.isFallRevealHiddenLayer(powerupLayer);
                const powerupLayerAlpha = this.getLiveLayerFadeMultiplier(powerupLayer, renderNowMs);
                const activeLayerTransition = this._layerFadeTransition || null;
                const powerupLayerIsUpwardTransitionDestination = !!(
                    activeLayerTransition &&
                    powerupLayer === this.getLayerIndexFromValue(activeLayerTransition.toLayer, powerupLayer) &&
                    powerupLayer === wizardLayer &&
                    this.getLayerIndexFromValue(activeLayerTransition.toLayer, powerupLayer) >
                        this.getLayerIndexFromValue(activeLayerTransition.fromLayer, powerupLayer)
                );
                if (
                    (mazeLosActive && powerupLayer !== wizardLayer && !powerupOnFallRevealLayer) ||
                    (!(powerupLayerAlpha > 0.001) && !powerupLayerIsUpwardTransitionDestination) ||
                    this.isRenderItemHiddenByLayerCutaway(powerup, powerupLayer, cutawayState, mapRef)
                ) {
                    this.hidePowerupDisplayObjects(powerup, sprite);
                    continue;
                }

                if (
                    !powerupOnFallRevealLayer &&
                    wizard &&
                    (mazeLosActive || !this.isForceVisible(powerup)) &&
                    this.isRadialItemHiddenByLos(powerup, wizard, mapRef)
                ) {
                    this.hidePowerupDisplayObjects(powerup, sprite);
                    continue;
                }

                const point = this.camera.worldToScreen(
                    powerup.x,
                    powerup.y,
                    (() => {
                        const support = powerup.currentMovementSupport && typeof powerup.currentMovementSupport === "object"
                            ? powerup.currentMovementSupport
                            : null;
                        const floorBaseZ = Number.isFinite(support && support.baseZ)
                            ? Number(support.baseZ)
                            : (Number.isFinite(powerup.currentLayerBaseZ)
                                ? Number(powerup.currentLayerBaseZ)
                                : (Number.isFinite(powerup._floorBaseZ)
                                    ? Number(powerup._floorBaseZ)
                                    : this.getLayerBaseZForLevel(powerupLayer)));
                        powerup._renderLayerBaseZ = floorBaseZ;
                        return floorBaseZ + (Number.isFinite(powerup.z) ? Number(powerup.z) : 0);
                    })()
                );
                const basePowerupDepthBias = Number.isFinite(powerup.renderDepthOffset)
                    ? Number(powerup.renderDepthOffset)
                    : 0;
                powerup._renderDepthBias = basePowerupDepthBias + (
                    this.shouldApplyBuildingInteriorActiveFloorDepthBumpToItem(powerup, powerupLayer, buildingInteriorRenderPlan, mapRef)
                        ? BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS
                        : 0
                );
                const w = Number.isFinite(powerup.width) ? Math.max(0.01, Number(powerup.width)) : 0.8;
                const h = Number.isFinite(powerup.height) ? Math.max(0.01, Number(powerup.height)) : 0.8;
                const lodTexturePath = this.resolvePowerupLodTexturePath(powerup);
                if (typeof lodTexturePath === "string" && lodTexturePath.length > 0 && lodTexturePath !== powerup._activeLodTexturePath) {
                    sprite.texture = PIXI.Texture.from(lodTexturePath);
                    powerup._activeLodTexturePath = lodTexturePath;
                }
                if (!this.isPowerupBillboardTextureReady(sprite.texture)) {
                    this.watchPowerupPendingTexture(sprite.texture);
                    this.hidePowerupDisplayObjects(powerup, sprite);
                    continue;
                }
                sprite.x = point.x;
                sprite.y = point.y;
                sprite.width = w * this.camera.viewscale;
                sprite.height = h * this.camera.viewscale;

                let depthMesh = null;
                if (typeof powerup.updateDepthBillboardMesh === "function") {
                    depthMesh = powerup.updateDepthBillboardMesh(ctx, this.camera, { alphaCutoff: TREE_ALPHA_CUTOFF });
                }

                if (!depthMesh) {
                    this.hidePowerupDisplayObjects(powerup, sprite);
                    continue;
                }

                const targetContainer = mazeLosActive
                    ? maskedContainer
                    : depthContainer;
                if (depthMesh.parent !== targetContainer) {
                    targetContainer.addChild(depthMesh);
                }
                depthMesh.alpha = 1;
                depthMesh.visible = true;
                if (Object.prototype.hasOwnProperty.call(depthMesh, "renderable")) {
                    depthMesh.renderable = true;
                }
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }

                powerup._renderingDepthMesh = depthMesh;
                powerup._renderingDisplayObject = depthMesh;
                this.applyLayerDarknessForItem(powerup, powerupLayer, depthMesh);
                this.applyPowerupPremultipliedDisplayAlpha(powerup, depthMesh, 1);
                if (this.applySinkClip(powerup, depthMesh)) {
                    currentDisplayObjects.add(depthMesh);
                    this.addPickRenderItem(powerup, depthMesh, { forceInclude: true });
                }
            }

            for (const sprite of this._powerupDisplayObjectSets[1 - this._powerupDisplayObjectIdx]) {
                if (!currentDisplayObjects.has(sprite) && sprite) {
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                        sprite.renderable = false;
                    }
                }
            }
            this.activePowerupDisplayObjects = currentDisplayObjects;
        }

        renderProjectiles(ctx) {
            const container = this.layers.entities;
            if (!container) return;
            const wizard = (ctx && ctx.wizard) ? ctx.wizard : null;
            const mapRef = (ctx && ctx.map) ? ctx.map : (this.camera && this.camera.map) || global.map || null;
            const mazeLosActive = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );
            const wizardLayer = this.getWizardVisualLayerIndex(wizard, 0);
            const cutawayState = this.getLayerCutawayState(ctx);

            const list = Array.isArray(ctx && ctx.projectiles)
                ? ctx.projectiles
                : (
                    (typeof projectiles !== "undefined" && Array.isArray(projectiles))
                        ? projectiles
                        : (Array.isArray(global.projectiles) ? global.projectiles : [])
                );
            const currentDisplayObjects = new Set();

            for (let i = 0; i < list.length; i++) {
                const projectile = list[i];
                if (!projectile || projectile.gone) continue;

                const texture = this.getProjectileTexture(projectile);
                let sprite = projectile.pixiSprite || null;
                if (!sprite) {
                    sprite = new PIXI.Sprite(texture || PIXI.Texture.WHITE);
                    sprite.anchor.set(0.5, 0.5);
                    projectile.pixiSprite = sprite;
                } else if (texture && sprite.texture !== texture) {
                    sprite.texture = texture;
                }

                if (sprite.parent !== container) {
                    container.addChild(sprite);
                }

                const worldX = Number.isFinite(projectile.x) ? Number(projectile.x) : 0;
                const worldY = Number.isFinite(projectile.y) ? Number(projectile.y) : 0;
                const localZ = Number.isFinite(projectile.z) ? Number(projectile.z) : 0;
                const baseZ = projectile.zIsWorld === true ? 0 : this.getProjectileVisualBaseZ(projectile);
                const worldZ = projectile.zIsWorld === true ? localZ : (baseZ + localZ);
                const projectileLayer = this.getLayerIndexFromValue(
                    Number.isFinite(projectile.currentLayer)
                        ? projectile.currentLayer
                        : Math.floor((Number(baseZ) || 0) / FLOOR_LAYER_DEFAULT_HEIGHT_UNITS),
                    0
                );
                projectile._renderLayerIndex = projectileLayer;
                const projectileOnFallRevealLayer = this.isFallRevealHiddenLayer(projectileLayer);
                const projectileLayerAlpha = this.getLiveLayerFadeMultiplier(projectileLayer, Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now());
                const hiddenByMazeLos = !!(
                    !projectileOnFallRevealLayer &&
                    mazeLosActive &&
                    (
                        projectileLayer !== wizardLayer ||
                        this.isWorldPointInLosShadow(worldX, worldY, wizard, mapRef)
                    )
                );
                const hiddenByCutaway = this.isWorldPointHiddenByLayerCutaway(worldX, worldY, projectileLayer, cutawayState);
                const visible = projectile.visible !== false && projectileLayerAlpha > 0.001 && !hiddenByMazeLos && !hiddenByCutaway;
                const spriteVisible = visible && !projectile.hideProjectileSprite;
                sprite.visible = spriteVisible;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = spriteVisible;
                }
                sprite.alpha = projectileLayerAlpha;

                if (spriteVisible) {
                    const p = this.camera.worldToScreen(worldX, worldY, worldZ);
                    sprite.x = p.x;
                    sprite.y = p.y;

                    const zoomFactor = Math.max(
                        0.01,
                        Number.isFinite(global.viewportZoomFactor)
                            ? Number(global.viewportZoomFactor)
                            : 1
                    );
                    const apparentSize = Number.isFinite(projectile.apparentSize)
                        ? Number(projectile.apparentSize)
                        : NaN;
                    const fallbackSize = (
                        Number.isFinite(projectile.size)
                            ? Number(projectile.size)
                            : 0.35
                    ) * this.camera.viewscale;
                    const sizePx = Math.max(
                        1,
                        Number.isFinite(apparentSize) && apparentSize > 0
                            ? (apparentSize * zoomFactor)
                            : fallbackSize
                    );
                    sprite.width = sizePx;
                    sprite.height = sizePx;

                    if ((projectile.type === "arrow" || projectile.rotateSpriteToMovement) && projectile.movement) {
                        const moveX = Number(projectile.movement.x) || 0;
                        const moveY = Number(projectile.movement.y) || 0;
                        if (Math.hypot(moveX, moveY) > 1e-6) {
                            const rotationOffset = Number.isFinite(projectile.spriteRotationOffset)
                                ? Number(projectile.spriteRotationOffset)
                                : Math.PI * 0.5;
                            sprite.rotation = Math.atan2(moveY, moveX) + rotationOffset;
                        }
                    } else {
                        sprite.rotation = Number(projectile.spriteRotation) || 0;
                    }
                    this.applyLayerDarknessForItem(projectile, projectileLayer, sprite);
                }

                currentDisplayObjects.add(sprite);
                const particleGraphics = this.renderProjectileParticles(projectile, container, hiddenByMazeLos || hiddenByCutaway);
                if (particleGraphics) {
                    currentDisplayObjects.add(particleGraphics);
                }
            }

            for (const sprite of this.activeProjectileDisplayObjects) {
                if (!currentDisplayObjects.has(sprite) && sprite) {
                    if (typeof sprite.clear === "function") {
                        sprite.clear();
                    }
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                        sprite.renderable = false;
                    }
                    if (sprite.parent) {
                        sprite.parent.removeChild(sprite);
                    }
                }
            }
            this.activeProjectileDisplayObjects = currentDisplayObjects;
        }

        renderCreatureTracePaths(ctx) {
            const container = this.layers.entities;
            if (!container) return;
            const creatureList = Array.isArray(ctx && ctx.animals)
                ? ctx.animals
                : ((typeof animals !== "undefined" && Array.isArray(animals)) ? animals : (Array.isArray(global.animals) ? global.animals : []));
            const mapRef = (ctx && ctx.map) ? ctx.map : (this.camera && this.camera.map) || global.map || null;
            const renderNowMs = Number.isFinite(ctx && ctx.renderNowMs) ? Number(ctx.renderNowMs) : Date.now();
            const isGamePaused = !!((typeof paused !== "undefined" && paused) || global.paused);
            const CameraCtor = global.RenderingCamera;

            for (let i = 0; i < creatureList.length; i++) {
                const creature = creatureList[i];
                if (!creature) continue;

                const traceState = (typeof creature.updateTracePathLifetime === "function")
                    ? creature.updateTracePathLifetime(renderNowMs, isGamePaused)
                    : null;
                const traceLog = Array.isArray(creature.nodeVisitLog) ? creature.nodeVisitLog : [];
                const shouldRender = !!(
                    traceState &&
                    traceLog.length >= 2 &&
                    !creature.gone
                );

                let graphics = creature._tracePathGraphics || null;
                if (!shouldRender) {
                    if (graphics) {
                        graphics.clear();
                        graphics.visible = false;
                    }
                    continue;
                }

                if (!graphics) {
                    graphics = new PIXI.Graphics();
                    graphics.name = "creatureTracePath";
                    creature._tracePathGraphics = graphics;
                }
                if (graphics.parent !== container) {
                    container.addChild(graphics);
                }

                graphics.clear();
                graphics.visible = true;
                graphics.zIndex = -10;
                graphics.alpha = 1;
                graphics.lineStyle(Math.max(2, this.camera.viewscale * 0.16), 0xb04cff, 0.9);

                const firstEntry = traceLog[0];
                if (!firstEntry) {
                    graphics.visible = false;
                    continue;
                }

                let continuousPoint = (CameraCtor && typeof CameraCtor.alignWorldPointToReference === "function")
                    ? CameraCtor.alignWorldPointToReference(
                        mapRef,
                        Number(this.camera && this.camera.x) || 0,
                        Number(this.camera && this.camera.y) || 0,
                        Number(firstEntry.x),
                        Number(firstEntry.y)
                    )
                    : { x: Number(firstEntry.x), y: Number(firstEntry.y) };
                let screenPoint = this.camera.worldToScreen(continuousPoint.x, continuousPoint.y, 0);
                graphics.moveTo(screenPoint.x, screenPoint.y);

                for (let j = 1; j < traceLog.length; j++) {
                    const entry = traceLog[j];
                    if (!entry || !Number.isFinite(entry.x) || !Number.isFinite(entry.y)) continue;
                    continuousPoint = (CameraCtor && typeof CameraCtor.alignWorldPointToReference === "function")
                        ? CameraCtor.alignWorldPointToReference(
                            mapRef,
                            continuousPoint.x,
                            continuousPoint.y,
                            Number(entry.x),
                            Number(entry.y)
                        )
                        : { x: Number(entry.x), y: Number(entry.y) };
                    screenPoint = this.camera.worldToScreen(continuousPoint.x, continuousPoint.y, 0);
                    graphics.lineTo(screenPoint.x, screenPoint.y);
                }
            }
        }

        renderScriptMessages(ctx) {
            const container = this.layers.scriptMessages;
            if (!container) return;
            const mapRef = (ctx && ctx.map) ? ctx.map : (this.camera && this.camera.map) || global.map || null;
            const wizard = (ctx && ctx.wizard) ? ctx.wizard : null;
            const wizardUnderRoof = !!(
                wizard &&
                Number.isFinite(wizard.x) &&
                Number.isFinite(wizard.y) &&
                this.isWorldPointUnderRoof(Number(wizard.x), Number(wizard.y), ctx)
            );
            const mazeLosActive = !!(
                this.isLosMazeModeEnabled() &&
                !this.isOmnivisionActive(wizard) &&
                wizard
            );

            // Collect objects with messages from the global registry AND from game objects
            const objectsWithMessages = [];
            const seen = new Set();

            // Primary source: global registry (set by this.message handler)
            const globalTargets = (global._scriptMessageTargets instanceof Set)
                ? global._scriptMessageTargets
                : null;
            if (globalTargets) {
                for (const item of globalTargets) {
                    if (!item || item.gone) {
                        globalTargets.delete(item);
                        continue;
                    }
                    if (!Array.isArray(item._scriptMessages) || item._scriptMessages.length === 0) {
                        globalTargets.delete(item);
                        continue;
                    }
                    if (item.visible === false) continue;
                    seen.add(item);
                    objectsWithMessages.push(item);
                }
            }

            // Fallback: scan game objects only when the registry isn't available.
            // When globalTargets exists, it's the authoritative source — the scan is O(all objects)
            // and adds nothing since seen already contains every registered item.
            if (!globalTargets && mapRef && typeof mapRef.getGameObjects === "function") {
                const allObjects = mapRef.getGameObjects({ refresh: false }) || [];
                for (let i = 0; i < allObjects.length; i++) {
                    const item = allObjects[i];
                    if (!item || item.gone || seen.has(item)) continue;
                    if (!Array.isArray(item._scriptMessages) || item._scriptMessages.length === 0) continue;
                    if (item.visible === false) continue;
                    objectsWithMessages.push(item);
                }
            }

            const activeKeys = new Set();
            const wizardLayerIndex = wizard ? this.getWizardVisualLayerIndex(wizard, 0) : null;
            const wizardFloorMembership = wizard ? this.getRenderItemFloorMembership(wizard, mapRef) : null;
            const parseScriptMessageColor = (value, fallback = 0xFFFFFF) => {
                if (Number.isFinite(value)) {
                    return Math.max(0, Math.min(0xFFFFFF, Math.floor(Number(value))));
                }
                if (typeof value === "string") {
                    const text = value.trim().toLowerCase();
                    if (/^#?[0-9a-f]{6}$/.test(text)) {
                        return parseInt(text.replace(/^#/, ""), 16);
                    }
                    if (/^0x[0-9a-f]{6}$/.test(text)) {
                        return parseInt(text, 16);
                    }
                }
                return fallback;
            };
            const normalizeScriptMessageFontSize = (value, fallback = 14) => {
                const parsed = Number(value);
                if (!Number.isFinite(parsed)) return fallback;
                return Math.max(8, Math.min(96, parsed));
            };

            for (let i = 0; i < objectsWithMessages.length; i++) {
                const item = objectsWithMessages[i];
                const messages = item._scriptMessages;

                const worldPos = this.resolveInterpolatedItemWorldPosition(item, mapRef);
                if (!worldPos) continue;
                const itemLayerIndex = this.getLayerIndexForObject(
                    item,
                    Number.isFinite(wizardLayerIndex) ? wizardLayerIndex : 0
                );
                if (wizard && itemLayerIndex !== wizardLayerIndex) {
                    continue;
                }
                const itemFloorMembership = this.getRenderItemFloorMembership(item, mapRef);
                if (
                    wizard &&
                    itemFloorMembership &&
                    wizardFloorMembership &&
                    !this.floorMembershipsMatchForRender(itemFloorMembership, wizardFloorMembership)
                ) {
                    continue;
                }
                const itemLayerBaseZ = this.getLayerBaseZForObject(item, itemLayerIndex);
                if (
                    mazeLosActive &&
                    this.isWorldPointInLosShadow(worldPos.x, worldPos.y, wizard, mapRef)
                ) {
                    continue;
                }
                if (this.isWorldPointUnderRoof(worldPos.x, worldPos.y, ctx) && !wizardUnderRoof) {
                    continue;
                }

                // Stable key per object (lazy-assigned, survives array reordering)
                if (!item._scriptMessageRenderingId) {
                    item._scriptMessageRenderingId = "msgobj:" + (this._nextScriptMessageObjId = (this._nextScriptMessageObjId || 0) + 1);
                }

                for (let m = 0; m < messages.length; m++) {
                    const msg = messages[m];
                    if (!msg || typeof msg.text !== "string" || !msg.text.length) continue;
                    const key = item._scriptMessageRenderingId + ":" + m;
                    activeKeys.add(key);
                    const fillColor = parseScriptMessageColor(msg.color, 0xFFFFFF);
                    const fontSize = normalizeScriptMessageFontSize(msg.fontsize, 14);
                    const styleSignature = `${fillColor}:${fontSize}`;

                    let entry = this.scriptMessageTextObjects.get(key);
                    if (!entry) {
                        const textObj = new PIXI.Text(msg.text, {
                            fontFamily: "Arial, Helvetica, sans-serif",
                            fontSize,
                            fontWeight: "bold",
                            fill: fillColor,
                            stroke: 0x000000,
                            strokeThickness: 3,
                            align: "center",
                            wordWrap: true,
                            wordWrapWidth: 200
                        });
                        textObj.anchor.set(0.5, 0.5);
                        textObj.name = "scriptMsg_" + key;
                        container.addChild(textObj);
                        entry = { textObj, lastText: msg.text, lastStyleSignature: styleSignature };
                        this.scriptMessageTextObjects.set(key, entry);
                    } else if (entry.lastText !== msg.text) {
                        entry.textObj.text = msg.text;
                        entry.lastText = msg.text;
                    }
                    if (entry.lastStyleSignature !== styleSignature) {
                        entry.textObj.style.fill = fillColor;
                        entry.textObj.style.fontSize = fontSize;
                        entry.lastStyleSignature = styleSignature;
                    }

                    const offsetX = Number.isFinite(msg.x) ? msg.x : 0;
                    const offsetY = Number.isFinite(msg.y) ? msg.y : 0;

                    // Compute the visual center of the object, accounting for its
                    // actual anchor, dimensions, and rotation axis so the message
                    // is centred on its appearance rather than its anchor point.
                    let visCenterX = worldPos.x;
                    let visCenterY = worldPos.y;
                    let visCenterZ = Number.isFinite(item.z) ? Number(item.z) : 0;

                    const itemW = Number.isFinite(item.width) ? item.width : 0;
                    const itemH = Number.isFinite(item.height) ? item.height : 0;
                    const rotAxis = (typeof item.rotationAxis === "string") ? item.rotationAxis : "visual";

                    if (rotAxis === "ground") {
                        // Flat on the ground — visual center is just the position, no Z offset.
                    } else if (item._depthBillboardWorldPositions && item._depthBillboardWorldPositions.length >= 12) {
                        // Use the actual billboard quad corner positions for accuracy.
                        const wp = item._depthBillboardWorldPositions;
                        const count = Math.min(wp.length / 3, rotAxis === "spatial" && wp.length >= 24 ? 8 : 4);
                        let sx = 0, sy = 0, sz = 0;
                        for (let v = 0; v < count; v++) {
                            sx += wp[v * 3];
                            sy += wp[v * 3 + 1];
                            sz += wp[v * 3 + 2];
                        }
                        visCenterX = sx / count;
                        visCenterY = sy / count;
                        visCenterZ = sz / count;
                    } else if (itemW > 0 || itemH > 0) {
                        // Fallback: derive visual center from anchor + dimensions.
                        const sprAnchor = item.pixiSprite && item.pixiSprite.anchor;
                        const ax = Number.isFinite(item.placeableAnchorX)
                            ? Number(item.placeableAnchorX)
                            : (sprAnchor && Number.isFinite(sprAnchor.x) ? Number(sprAnchor.x) : 0.5);
                        const ay = Number.isFinite(item.placeableAnchorY)
                            ? Number(item.placeableAnchorY)
                            : (sprAnchor && Number.isFinite(sprAnchor.y) ? Number(sprAnchor.y) : 1);
                        const xyR = Math.max(0.0001, this.camera.xyratio || 0.66);
                        const worldHeightZ = (rotAxis === "spatial")
                            ? (itemH / xyR)
                            : itemH;
                        visCenterX += (0.5 - ax) * itemW;
                        visCenterZ += (ay - 0.5) * worldHeightZ;
                    }

                    const screenPos = this.camera.worldToScreen(
                        visCenterX + offsetX,
                        visCenterY + offsetY,
                        itemLayerBaseZ + visCenterZ
                    );
                    entry.textObj.x = screenPos.x;
                    entry.textObj.y = screenPos.y;
                    entry.textObj.visible = true;
                }
            }

            // Hide/remove stale text objects
            for (const [key, entry] of this.scriptMessageTextObjects.entries()) {
                if (!activeKeys.has(key)) {
                    entry.textObj.visible = false;
                    if (entry.textObj.parent) {
                        entry.textObj.parent.removeChild(entry.textObj);
                    }
                    entry.textObj.destroy();
                    this.scriptMessageTextObjects.delete(key);
                }
            }
        }

        startWizardThoughtBubble(wizardRef, text, duration) {
            if (!wizardRef || typeof wizardRef !== "object") {
                throw new Error("wizard.thoughtBubble requires a wizard object");
            }
            const bubbleText = String(text === undefined || text === null ? "" : text);
            if (!bubbleText.trim().length) {
                throw new Error("wizard.thoughtBubble text cannot be empty");
            }
            const durationSeconds = Number(duration);
            if (!Number.isFinite(durationSeconds)) {
                throw new Error("wizard.thoughtBubble duration must be a finite number of seconds");
            }
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            this.destroyWizardThoughtBubbleTexture();
            this.wizardThoughtBubble = {
                wizard: wizardRef,
                text: bubbleText,
                startedAtMs: null,
                requestedAtMs: nowMs,
                durationMs: Math.max(0, durationSeconds * 1000),
                fadeMs: 500,
                baseViewscale: null,
                pivotX: 0,
                pivotY: 0,
                smallRadiusPx: 0,
                smallDiameterPx: 0,
                cloudTextureReady: false,
                cloudTextureError: null
            };
            this.scheduleWizardThoughtBubbleRefresh();
            if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                globalThis.presentGameFrame();
            }
            return true;
        }

        scheduleWizardThoughtBubbleRefresh() {
            if (this.wizardThoughtBubbleRefreshHandle !== null) return;
            if (typeof requestAnimationFrame !== "function") return;
            this.wizardThoughtBubbleRefreshHandle = requestAnimationFrame(() => {
                this.wizardThoughtBubbleRefreshHandle = null;
                if (!this.wizardThoughtBubble) return;
                if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                    globalThis.presentGameFrame();
                }
                if (this.wizardThoughtBubble) {
                    this.scheduleWizardThoughtBubbleRefresh();
                }
            });
        }

        destroyWizardThoughtBubbleTexture() {
            if (this.wizardThoughtBubbleTexture) {
                this.wizardThoughtBubbleTexture.destroy(true);
                this.wizardThoughtBubbleTexture = null;
            }
            if (this.wizardThoughtBubbleSprite) {
                this.wizardThoughtBubbleSprite.texture = (typeof PIXI !== "undefined" && PIXI.Texture) ? PIXI.Texture.EMPTY : null;
                this.wizardThoughtBubbleSprite.visible = false;
            }
        }

        clearWizardThoughtBubble() {
            this.wizardThoughtBubble = null;
            if (this.wizardThoughtBubbleRefreshHandle !== null && typeof cancelAnimationFrame === "function") {
                cancelAnimationFrame(this.wizardThoughtBubbleRefreshHandle);
            }
            this.wizardThoughtBubbleRefreshHandle = null;
            this.destroyWizardThoughtBubbleTexture();
            if (this.wizardThoughtBubbleSprite && this.wizardThoughtBubbleSprite.parent) {
                this.wizardThoughtBubbleSprite.parent.removeChild(this.wizardThoughtBubbleSprite);
            }
        }

        getWizardThoughtBubbleAlpha(nowMs) {
            const bubble = this.wizardThoughtBubble;
            if (!bubble) return 0;
            if (!Number.isFinite(bubble.startedAtMs)) return 1;
            const elapsedMs = Math.max(0, nowMs - bubble.startedAtMs);
            if (elapsedMs <= bubble.durationMs) return 1;
            if (bubble.fadeMs <= 0) return 0;
            const fadeElapsedMs = elapsedMs - bubble.durationMs;
            const fadeProgress = Math.max(0, Math.min(1, fadeElapsedMs / bubble.fadeMs));
            return 1 - fadeProgress;
        }

        getWizardThoughtBubbleCloudTexture(bubble) {
            if (typeof PIXI === "undefined" || !PIXI.Texture) {
                throw new Error("wizard.thoughtBubble requires PIXI.Texture");
            }
            const texture = PIXI.Texture.from("/assets/images/cloud.png");
            const baseTexture = texture && texture.baseTexture ? texture.baseTexture : null;
            if (!baseTexture) {
                throw new Error("wizard.thoughtBubble missing cloud base texture");
            }
            if (baseTexture.valid === true) {
                bubble.cloudTextureReady = true;
                return texture;
            }
            if (bubble.cloudTextureError) {
                throw new Error(`wizard.thoughtBubble failed to load /assets/images/cloud.png: ${bubble.cloudTextureError}`);
            }
            if (!this.wizardThoughtBubblePendingBaseTextures.has(baseTexture)) {
                if (typeof baseTexture.once !== "function") {
                    throw new Error("wizard.thoughtBubble cloud texture cannot report load completion");
                }
                this.wizardThoughtBubblePendingBaseTextures.add(baseTexture);
                baseTexture.once("loaded", () => {
                    this.wizardThoughtBubblePendingBaseTextures.delete(baseTexture);
                    if (this.wizardThoughtBubble) {
                        this.wizardThoughtBubble.cloudTextureReady = true;
                    }
                    if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                        globalThis.presentGameFrame();
                    }
                });
                baseTexture.once("error", (error) => {
                    this.wizardThoughtBubblePendingBaseTextures.delete(baseTexture);
                    if (this.wizardThoughtBubble) {
                        this.wizardThoughtBubble.cloudTextureError = error && error.message ? error.message : "texture error";
                    }
                    if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                        globalThis.presentGameFrame();
                    }
                });
            }
            return null;
        }

        ensureWizardThoughtBubbleTexture(ctx, bubble) {
            if (this.wizardThoughtBubbleTexture) return true;
            if (typeof PIXI === "undefined" || !PIXI.Container || !PIXI.Graphics || !PIXI.Sprite || !PIXI.Text || !PIXI.Rectangle) {
                throw new Error("wizard.thoughtBubble requires Pixi display classes");
            }
            const appRef = (ctx && ctx.app) || global.app || null;
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            if (!renderer || typeof renderer.generateTexture !== "function") {
                throw new Error("wizard.thoughtBubble requires a Pixi renderer");
            }
            const cloudTexture = this.getWizardThoughtBubbleCloudTexture(bubble);
            if (!cloudTexture) {
                return false;
            }

            const baseViewscale = Math.max(1, Number(this.camera && this.camera.viewscale) || 40);
            bubble.baseViewscale = baseViewscale;
            const smallDiameter = Math.max(18, Math.min(52, baseViewscale * 0.42));
            const r1 = smallDiameter * 0.5;
            const r2 = smallDiameter * 0.75;
            const r3 = smallDiameter;
            const outlineWidth = Math.max(2, Math.round(smallDiameter * 0.09));
            const fontSize = Math.max(16, Math.min(30, Math.round(smallDiameter * 0.78)));
            const textLength = bubble.text.replace(/\s+/g, " ").trim().length;
            const cloudWidth = Math.max(230, Math.min(560, 210 + textLength * fontSize * 0.22));
            const cloudHeight = cloudWidth * (772 / 1173);
            const padding = Math.max(10, outlineWidth * 3);
            const baseCloudX = padding;
            const cloudX = baseCloudX + (cloudWidth * 0.3);
            const cloudY = padding;
            const circleShiftX = r1 * 0.5;
            const c3x = baseCloudX + cloudWidth * 0.52 + circleShiftX;
            const c3y = cloudY + cloudHeight + smallDiameter * 0.5 + r3;
            const c2x = c3x - smallDiameter * 1.5;
            const c2y = c3y + smallDiameter * 1.25;
            const c1x = c2x - smallDiameter * 1.08;
            const c1y = c2y + smallDiameter * 1.0;
            const chainDx = c3x - c1x;
            const chainDy = c3y - c1y;
            const chainLength = Math.max(1e-6, Math.hypot(chainDx, chainDy));
            const chainUnitX = chainDx / chainLength;
            const chainUnitY = chainDy / chainLength;
            const spreadC1x = c1x - (chainUnitX * smallDiameter);
            const spreadC1y = c1y - (chainUnitY * smallDiameter);
            const spreadC3x = c3x + (chainUnitX * r3 * 2 / 3);
            const spreadC3y = c3y + (chainUnitY * r3 * 2 / 3);
            const strokePad = Math.max(1, outlineWidth);
            const relC2x = c2x - spreadC1x;
            const relC2y = c2y - spreadC1y;
            const relC3x = spreadC3x - spreadC1x;
            const relC3y = spreadC3y - spreadC1y;
            const relCloudX = Math.max(cloudX - spreadC1x - r1, -r1);
            const relCloudY = cloudY - spreadC1y + r1;
            const originX = r1 + strokePad;
            const relMinY = Math.min(
                -r1 - strokePad,
                relC2y - r2 - strokePad,
                relC3y - r3 - strokePad,
                relCloudY
            );
            const relMaxY = Math.max(
                r1 + strokePad,
                relC2y + r2 + strokePad,
                relC3y + r3 + strokePad,
                relCloudY + cloudHeight
            );
            const originY = -relMinY;
            const drawC1x = originX;
            const drawC1y = originY;
            const drawC2x = originX + relC2x;
            const drawC2y = originY + relC2y;
            const drawC3x = originX + relC3x;
            const drawC3y = originY + relC3y;
            const drawCloudX = originX + relCloudX;
            const drawCloudY = originY + relCloudY;
            const width = Math.ceil(Math.max(
                drawC1x + r1 + strokePad,
                drawC2x + r2 + strokePad,
                drawC3x + r3 + strokePad,
                drawCloudX + cloudWidth
            ));
            const height = Math.ceil(relMaxY - relMinY);

            const textureContainer = new PIXI.Container();
            const circles = new PIXI.Graphics();
            circles.lineStyle(outlineWidth, 0x000000, 1);
            circles.beginFill(0xffffff, 1);
            circles.drawCircle(drawC1x, drawC1y, r1);
            circles.drawCircle(drawC2x, drawC2y, r2);
            circles.drawCircle(drawC3x, drawC3y, r3);
            circles.endFill();
            textureContainer.addChild(circles);

            const cloud = new PIXI.Sprite(cloudTexture);
            cloud.x = drawCloudX;
            cloud.y = drawCloudY;
            cloud.width = cloudWidth;
            cloud.height = cloudHeight;
            textureContainer.addChild(cloud);

            const textObj = new PIXI.Text(bubble.text, {
                fontFamily: "Arial, Helvetica, sans-serif",
                fontSize,
                fontWeight: "bold",
                fill: 0x000000,
                align: "center",
                wordWrap: true,
                wordWrapWidth: cloudWidth * 0.62,
                lineJoin: "round"
            });
            textObj.anchor.set(0.5, 0.5);
            textObj.x = drawCloudX + cloudWidth * 0.5;
            textObj.y = drawCloudY + cloudHeight * 0.48;
            textureContainer.addChild(textObj);

            const texture = renderer.generateTexture(textureContainer, {
                region: new PIXI.Rectangle(0, 0, width, height),
                resolution: 1
            });
            textureContainer.destroy({ children: true });
            if (!texture) {
                throw new Error("wizard.thoughtBubble failed to generate render texture");
            }
            this.wizardThoughtBubbleTexture = texture;
            bubble.pivotX = 0;
            bubble.pivotY = height;
            bubble.smallRadiusPx = r1;
            bubble.smallDiameterPx = smallDiameter;
            return true;
        }

        renderWizardThoughtBubble(ctx) {
            const bubble = this.wizardThoughtBubble;
            if (!bubble) {
                this.clearWizardThoughtBubble();
                return;
            }
            const nowMs = Number.isFinite(ctx && ctx.renderNowMs)
                ? Number(ctx.renderNowMs)
                : ((typeof performance !== "undefined" && performance && typeof performance.now === "function")
                    ? performance.now()
                    : Date.now());
            const alpha = this.getWizardThoughtBubbleAlpha(nowMs);
            if (!(alpha > 0)) {
                this.clearWizardThoughtBubble();
                return;
            }
            const wizard = bubble.wizard || (ctx && ctx.wizard) || global.wizard || null;
            if (!wizard || wizard.gone || !Number.isFinite(wizard.x) || !Number.isFinite(wizard.y)) {
                this.clearWizardThoughtBubble();
                return;
            }
            if (!this.ensureWizardThoughtBubbleTexture(ctx, bubble)) {
                this.scheduleWizardThoughtBubbleRefresh();
                return;
            }
            if (!Number.isFinite(bubble.startedAtMs)) {
                bubble.startedAtMs = nowMs;
            }
            const container = this.layers && this.layers.scriptMessages ? this.layers.scriptMessages : null;
            if (!container) {
                throw new Error("wizard.thoughtBubble requires the scriptMessages render layer");
            }
            let sprite = this.wizardThoughtBubbleSprite;
            if (!sprite) {
                sprite = new PIXI.Sprite(this.wizardThoughtBubbleTexture);
                sprite.name = "wizardThoughtBubble";
                sprite.interactive = false;
                sprite.interactiveChildren = false;
                this.wizardThoughtBubbleSprite = sprite;
            } else if (sprite.texture !== this.wizardThoughtBubbleTexture) {
                sprite.texture = this.wizardThoughtBubbleTexture;
            }
            if (sprite.parent !== container) {
                container.addChild(sprite);
            }

            const renderAlphaValue = Number.isFinite(ctx && ctx.renderAlpha)
                ? Math.max(0, Math.min(1, Number(ctx.renderAlpha)))
                : 1;
            const renderPos = (wizard && typeof wizard.getInterpolatedPosition === "function")
                ? wizard.getInterpolatedPosition(renderAlphaValue)
                : {
                    x: Number(wizard.x),
                    y: Number(wizard.y),
                    z: Number.isFinite(wizard.z) ? Number(wizard.z) : 0
                };
            const support = wizard && wizard.currentMovementSupport && typeof wizard.currentMovementSupport === "object"
                ? wizard.currentMovementSupport
                : null;
            const wizardLayer = this.getLayerIndexFromValue(
                support && Number.isFinite(support.layer) ? support.layer : wizard.currentLayer,
                0
            );
            const wizardLayerBaseZ = support && Number.isFinite(support.baseZ)
                ? Number(support.baseZ)
                : (Number.isFinite(wizard.currentLayerBaseZ)
                    ? Number(wizard.currentLayerBaseZ)
                    : this.getLayerBaseZForLevel(wizardLayer));
            const pGround = this.camera.worldToScreen(renderPos.x, renderPos.y, wizardLayerBaseZ);
            const viewScale = Math.max(1, Number(this.camera && this.camera.viewscale) || Number(bubble.baseViewscale) || 40);
            const xyRatio = Math.max(0.0001, Number(this.camera && this.camera.xyratio) || 0.66);
            const localZ = Number.isFinite(renderPos.z) ? Number(renderPos.z) : 0;
            const jumpOffsetPx = localZ * viewScale * xyRatio;
            const wizardCenterY = pGround.y - jumpOffsetPx - (viewScale * 0.25);
            const scale = Number.isFinite(bubble.baseViewscale) && bubble.baseViewscale > 0
                ? (viewScale / bubble.baseViewscale)
                : 1;
            const smallDiameter = Math.max(1, Number(bubble.smallDiameterPx) || (bubble.smallRadiusPx * 2) || 1) * scale;
            const wizardHeadX = pGround.x;
            const wizardHeadY = wizardCenterY - (viewScale * 0.45);
            const bubbleCornerX = wizardHeadX + (smallDiameter * 0.15);
            const bubbleCornerY = wizardHeadY - (smallDiameter * 0.1) - (smallDiameter * 0.5);

            sprite.pivot.set(bubble.pivotX, bubble.pivotY);
            sprite.position.set(bubbleCornerX, bubbleCornerY);
            sprite.scale.set(scale, scale);
            sprite.alpha = alpha;
            sprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                sprite.renderable = true;
            }
            this.scheduleWizardThoughtBubbleRefresh();
        }

        parseScriptScreenColor(value) {
            if (Number.isFinite(value)) {
                const numeric = Math.floor(Number(value));
                if (numeric < 0 || numeric > 0xFFFFFF) {
                    throw new Error(`screenColor color number out of range: ${value}`);
                }
                return numeric;
            }
            const text = String(value === undefined || value === null ? "" : value).trim();
            if (/^#?[0-9a-fA-F]{6}$/.test(text)) {
                return parseInt(text.replace(/^#/, ""), 16);
            }
            if (/^0x[0-9a-fA-F]{6}$/.test(text)) {
                return parseInt(text.slice(2), 16);
            }
            throw new Error(`invalid screenColor color '${text}'`);
        }

        startScriptScreenColorOverlay(color, opacity, duration, fade) {
            const parsedColor = this.parseScriptScreenColor(color);
            const parsedOpacity = Number(opacity);
            const parsedDuration = Number(duration);
            const parsedFade = Number(fade);
            if (!Number.isFinite(parsedOpacity)) {
                throw new Error("screenColor opacity must be a finite number");
            }
            if (!Number.isFinite(parsedDuration)) {
                throw new Error("screenColor duration must be a finite number of seconds");
            }
            if (!Number.isFinite(parsedFade)) {
                throw new Error("screenColor fade must be a finite number of seconds");
            }
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            this.scriptScreenColorOverlay = {
                color: parsedColor,
                opacity: Math.max(0, Math.min(1, parsedOpacity)),
                startedAtMs: nowMs,
                durationMs: Math.max(0, parsedDuration * 1000),
                fadeMs: Math.max(0, parsedFade * 1000)
            };
            this.scheduleScriptScreenColorOverlayRefresh();
            if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                globalThis.presentGameFrame();
            }
            return true;
        }

        scheduleScriptScreenColorOverlayRefresh() {
            if (this.scriptScreenColorOverlayRefreshHandle !== null) return;
            if (typeof requestAnimationFrame !== "function") return;
            this.scriptScreenColorOverlayRefreshHandle = requestAnimationFrame(() => {
                this.scriptScreenColorOverlayRefreshHandle = null;
                if (!this.scriptScreenColorOverlay) return;
                if (typeof globalThis !== "undefined" && typeof globalThis.presentGameFrame === "function") {
                    globalThis.presentGameFrame();
                }
                if (this.scriptScreenColorOverlay) {
                    this.scheduleScriptScreenColorOverlayRefresh();
                }
            });
        }

        clearScriptScreenColorOverlay() {
            this.scriptScreenColorOverlay = null;
            if (this.scriptScreenColorOverlayRefreshHandle !== null && typeof cancelAnimationFrame === "function") {
                cancelAnimationFrame(this.scriptScreenColorOverlayRefreshHandle);
            }
            this.scriptScreenColorOverlayRefreshHandle = null;
            const gfx = this.scriptScreenColorOverlayGraphics;
            if (gfx) {
                gfx.visible = false;
                if (gfx.parent) gfx.parent.removeChild(gfx);
                if (typeof gfx.clear === "function") gfx.clear();
            }
        }

        getScriptScreenColorOverlayOpacity(nowMs) {
            const overlay = this.scriptScreenColorOverlay;
            if (!overlay) return 0;
            const elapsedMs = Math.max(0, nowMs - overlay.startedAtMs);
            const opacity = Math.max(0, Math.min(1, Number(overlay.opacity) || 0));
            if (elapsedMs <= overlay.durationMs) {
                return opacity;
            }
            if (overlay.fadeMs <= 0) {
                return 0;
            }
            const fadeElapsedMs = elapsedMs - overlay.durationMs;
            const fadeProgress = Math.max(0, Math.min(1, fadeElapsedMs / overlay.fadeMs));
            return opacity * (1 - fadeProgress);
        }

        renderScriptScreenColorOverlay(ctx) {
            if (!this.scriptScreenColorOverlay) {
                this.clearScriptScreenColorOverlay();
                return;
            }
            if (typeof PIXI === "undefined" || !PIXI.Graphics) {
                throw new Error("screenColor overlay requires PIXI.Graphics");
            }
            const appRef = (ctx && ctx.app) || global.app || null;
            const stage = appRef && appRef.stage ? appRef.stage : null;
            if (!stage) {
                throw new Error("screenColor overlay requires the Pixi app stage");
            }
            const nowMs = (typeof performance !== "undefined" && performance && typeof performance.now === "function")
                ? performance.now()
                : Date.now();
            const alpha = this.getScriptScreenColorOverlayOpacity(nowMs);
            if (!(alpha > 0)) {
                this.clearScriptScreenColorOverlay();
                return;
            }
            let gfx = this.scriptScreenColorOverlayGraphics;
            if (!gfx) {
                gfx = new PIXI.Graphics();
                gfx.name = "scriptScreenColorOverlay";
                gfx.interactive = false;
                gfx.interactiveChildren = false;
                this.scriptScreenColorOverlayGraphics = gfx;
            }
            const screen = appRef.screen || {};
            const renderer = appRef.renderer || {};
            const width = Math.max(1, Number(screen.width) || Number(renderer.width) || Number(global.innerWidth) || 1);
            const height = Math.max(1, Number(screen.height) || Number(renderer.height) || Number(global.innerHeight) || 1);
            gfx.clear();
            gfx.beginFill(this.scriptScreenColorOverlay.color, 1);
            gfx.drawRect(0, 0, width, height);
            gfx.endFill();
            gfx.alpha = alpha;
            gfx.visible = true;
            gfx.position.set(0, 0);
            gfx.scale.set(1, 1);
            if (gfx.parent !== stage) {
                stage.addChild(gfx);
            }
            const cursorLayerRef = global.cursorLayer || null;
            if (
                cursorLayerRef &&
                cursorLayerRef.parent === stage &&
                typeof stage.getChildIndex === "function" &&
                typeof stage.setChildIndex === "function"
            ) {
                const gfxIndex = stage.getChildIndex(gfx);
                const cursorIndex = stage.getChildIndex(cursorLayerRef);
                const targetIndex = gfxIndex < cursorIndex
                    ? Math.max(0, cursorIndex - 1)
                    : Math.max(0, cursorIndex);
                if (gfxIndex !== targetIndex) {
                    stage.setChildIndex(gfx, targetIndex);
                }
            } else if (typeof stage.setChildIndex === "function" && Array.isArray(stage.children)) {
                stage.setChildIndex(gfx, Math.max(0, stage.children.length - 1));
            }
            this.scheduleScriptScreenColorOverlayRefresh();
        }

        getMousePosRef(ctx) {
            if (ctx && ctx.mousePos) return ctx.mousePos;
            if (typeof mousePos !== "undefined") return mousePos;
            return global.mousePos || null;
        }

        resolveScreenPointOnLayerPlane(screenX, screenY, baseZ, mapRef = null, wizardRef = null) {
            if (
                !this.camera ||
                !Number.isFinite(screenX) ||
                !Number.isFinite(screenY) ||
                !Number.isFinite(this.camera.x) ||
                !Number.isFinite(this.camera.y)
            ) {
                return null;
            }
            const vs = Number.isFinite(this.camera.viewscale) && this.camera.viewscale
                ? Number(this.camera.viewscale)
                : 1;
            const xyr = Number.isFinite(this.camera.xyratio) && this.camera.xyratio
                ? Number(this.camera.xyratio)
                : 1;
            const cameraZ = Number.isFinite(this.camera.z) ? Number(this.camera.z) : 0;
            let worldX = (Number(screenX) / vs) + Number(this.camera.x);
            let worldY = (Number(screenY) / (vs * xyr)) + Number(this.camera.y) + (Number(baseZ) - cameraZ);
            if (mapRef && typeof mapRef.wrapWorldX === "function" && Number.isFinite(worldX)) {
                worldX = mapRef.wrapWorldX(worldX);
            }
            if (mapRef && typeof mapRef.wrapWorldY === "function" && Number.isFinite(worldY)) {
                worldY = mapRef.wrapWorldY(worldY);
            }
            if (
                wizardRef &&
                mapRef &&
                typeof mapRef.shortestDeltaX === "function" &&
                typeof mapRef.shortestDeltaY === "function" &&
                Number.isFinite(wizardRef.x) &&
                Number.isFinite(wizardRef.y) &&
                Number.isFinite(worldX) &&
                Number.isFinite(worldY)
            ) {
                worldX = Number(wizardRef.x) + mapRef.shortestDeltaX(Number(wizardRef.x), worldX);
                worldY = Number(wizardRef.y) + mapRef.shortestDeltaY(Number(wizardRef.y), worldY);
            }
            return { x: worldX, y: worldY };
        }

        ensurePlaceObjectPreviewItem(mapRef = null) {
            if (!this.placeObjectPreviewItem) {
                this.placeObjectPreviewItem = {
                    type: "placedObjectPreview",
                    map: mapRef || global.map || null,
                    gone: false,
                    vanishing: false,
                    isPlacedObject: true,
                    objectType: "placedObject",
                    pixiSprite: this.placeObjectPreviewSprite
                };
            }
            const previewItem = this.placeObjectPreviewItem;
            previewItem.map = mapRef || previewItem.map || global.map || null;
            previewItem.gone = false;
            previewItem.vanishing = false;
            previewItem.isPlacedObject = true;
            previewItem.objectType = "placedObject";
            previewItem.pixiSprite = this.placeObjectPreviewSprite;

            const staticProto = global.StaticObject && global.StaticObject.prototype
                ? global.StaticObject.prototype
                : null;
            if (staticProto && typeof staticProto.ensureDepthBillboardMesh === "function" && typeof previewItem.ensureDepthBillboardMesh !== "function") {
                previewItem.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
            }
            if (staticProto && typeof staticProto.updateDepthBillboardMesh === "function" && typeof previewItem.updateDepthBillboardMesh !== "function") {
                previewItem.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
            }
            if (staticProto && typeof staticProto._ensureCompositeUnderlayMesh === "function" && typeof previewItem._ensureCompositeUnderlayMesh !== "function") {
                previewItem._ensureCompositeUnderlayMesh = staticProto._ensureCompositeUnderlayMesh;
            }
            if (staticProto && typeof staticProto._destroyCompositeUnderlayMesh === "function" && typeof previewItem._destroyCompositeUnderlayMesh !== "function") {
                previewItem._destroyCompositeUnderlayMesh = staticProto._destroyCompositeUnderlayMesh;
            }

            return previewItem;
        }

        clearPlaceObjectPreview() {
            if (this.placeObjectPreviewItem) {
                if (this.placeObjectPreviewItem._depthBillboardMesh) {
                    const mesh = this.placeObjectPreviewItem._depthBillboardMesh;
                    mesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                        mesh.renderable = false;
                    }
                }
                if (this.placeObjectPreviewItem._compositeUnderlayMesh) {
                    const mesh = this.placeObjectPreviewItem._compositeUnderlayMesh;
                    mesh.visible = false;
                    if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                        mesh.renderable = false;
                    }
                }
            }
            if (this.placeObjectPreviewDisplayObject) {
                this.placeObjectPreviewDisplayObject.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewDisplayObject, "renderable")) {
                    this.placeObjectPreviewDisplayObject.renderable = false;
                }
                this.placeObjectPreviewDisplayObject = null;
            }
            if (this.placeObjectPreviewSprite) {
                this.placeObjectPreviewSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewSprite, "renderable")) {
                    this.placeObjectPreviewSprite.renderable = false;
                }
            }
            if (this.placeObjectCenterSnapGuideGraphics) {
                this.placeObjectCenterSnapGuideGraphics.clear();
                this.placeObjectCenterSnapGuideGraphics.visible = false;
            }
        }

        clearPowerupPlacementPreview() {
            if (this.powerupPlacementPreviewItem && this.powerupPlacementPreviewItem._depthBillboardMesh) {
                const mesh = this.powerupPlacementPreviewItem._depthBillboardMesh;
                mesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                    mesh.renderable = false;
                }
            }
            if (this.powerupPlacementPreviewDisplayObject) {
                this.powerupPlacementPreviewDisplayObject.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.powerupPlacementPreviewDisplayObject, "renderable")) {
                    this.powerupPlacementPreviewDisplayObject.renderable = false;
                }
                this.powerupPlacementPreviewDisplayObject = null;
            }
            if (this.powerupPlacementPreviewSprite) {
                this.powerupPlacementPreviewSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.powerupPlacementPreviewSprite, "renderable")) {
                    this.powerupPlacementPreviewSprite.renderable = false;
                }
            }
        }

        clearWallPlacementPreview() {
            if (!this.wallPlacementPreviewGraphics) return;
            this.wallPlacementPreviewGraphics.clear();
            this.wallPlacementPreviewGraphics.visible = false;
        }

        clearPrototypeSectionSeams() {
            if (!this.prototypeSectionSeamGraphics) return;
            this.prototypeSectionSeamGraphics.clear();
            this.prototypeSectionSeamGraphics.visible = false;
        }

        renderPrototypeSectionSeams(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.prototypeSectionSeamGraphics) {
                this.prototypeSectionSeamGraphics = new PIXI.Graphics();
                this.prototypeSectionSeamGraphics.name = "renderingPrototypeSectionSeams";
                this.prototypeSectionSeamGraphics.skipTransform = true;
                this.prototypeSectionSeamGraphics.interactive = false;
                this.prototypeSectionSeamGraphics.visible = false;
                layer.addChild(this.prototypeSectionSeamGraphics);
            } else if (this.prototypeSectionSeamGraphics.parent !== layer) {
                layer.addChild(this.prototypeSectionSeamGraphics);
            }

            const g = this.prototypeSectionSeamGraphics;

            const hide = () => {
                if (g.visible) {
                    g.visible = false;
                    this._sectionSeamDrawKey = null;
                }
            };

            const showSectionWorldSeams = !!(
                global.debugViewSettings
                    ? global.debugViewSettings.showSectionWorldSeams !== false
                    : global.renderingShowSectionWorldSeams !== false
            );
            if (!showSectionWorldSeams) { hide(); return; }
            if (this.getActiveBuildingInteriorRenderScope(ctx)) { hide(); return; }

            const mapRef = (ctx && ctx.map) || global.map || null;
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            if (wizardRef && typeof wizardRef.isAdventureMode === "function" && wizardRef.isAdventureMode()) {
                hide(); return;
            }

            const state = (mapRef && mapRef._prototypeSectionState) || null;
            const sectionGeometryApi = (typeof globalThis !== "undefined") ? globalThis.__sectionGeometry : null;
            if (!state || !(state.sectionAssetsByKey instanceof Map) || !sectionGeometryApi) {
                hide(); return;
            }

            // World-space edge list — recomputed only when sections are added or removed.
            const sectionCount = state.sectionAssetsByKey.size;
            if (
                !this._sectionSeamEdgeCache ||
                this._sectionSeamEdgeCache.stateRef !== state ||
                this._sectionSeamEdgeCache.sectionCount !== sectionCount
            ) {
                const edges = [];
                const seenKeys = new Set();
                for (const [, asset] of state.sectionAssetsByKey.entries()) {
                    const corners = sectionGeometryApi.getSectionHexagonCorners(asset.centerAxial, state.basis);
                    if (!Array.isArray(corners) || corners.length < 3) continue;
                    const n = corners.length;
                    for (let i = 0; i < n; i++) {
                        const ca = corners[i];
                        const cb = corners[(i + 1) % n];
                        const ax = Math.round(ca.x * 1000);
                        const ay = Math.round(ca.y * 1000);
                        const bx = Math.round(cb.x * 1000);
                        const by = Math.round(cb.y * 1000);
                        const edgeKey = (ax < bx || (ax === bx && ay <= by))
                            ? `${ax},${ay},${bx},${by}`
                            : `${bx},${by},${ax},${ay}`;
                        if (seenKeys.has(edgeKey)) continue;
                        seenKeys.add(edgeKey);
                        edges.push({ wx1: ca.x, wy1: ca.y, wx2: cb.x, wy2: cb.y });
                    }
                }
                this._sectionSeamEdgeCache = { stateRef: state, sectionCount, edges };
                this._sectionSeamDrawKey = null;
            }

            const level = (wizardRef && Number.isFinite(wizardRef.selectedFloorEditLevel))
                ? Math.round(Number(wizardRef.selectedFloorEditLevel))
                : ((typeof globalThis !== "undefined" && Number.isFinite(globalThis.selectedFloorEditLevel))
                    ? Math.round(Number(globalThis.selectedFloorEditLevel))
                    : 0);
            const worldZ = level * FLOOR_LAYER_DEFAULT_HEIGHT_UNITS;

            // Skip clear + redraw entirely when camera and level haven't changed.
            const cam = this.camera;
            const drawKey = `${cam.x},${cam.y},${cam.viewscale},${cam.xyratio},${worldZ}`;
            if (drawKey === this._sectionSeamDrawKey) {
                g.visible = true;
                return;
            }
            this._sectionSeamDrawKey = drawKey;

            g.clear();
            g.visible = true;
            g.lineStyle(2, 0xf4f4f4, 0.72);

            const screenW = Number.isFinite(cam.screenWidth) ? cam.screenWidth
                : (global.app && global.app.screen ? global.app.screen.width : 2000);
            const screenH = Number.isFinite(cam.screenHeight) ? cam.screenHeight
                : (global.app && global.app.screen ? global.app.screen.height : 2000);
            const margin = 64;

            const edges = this._sectionSeamEdgeCache.edges;
            for (let e = 0; e < edges.length; e++) {
                const edge = edges[e];
                const a = cam.worldToScreen(edge.wx1, edge.wy1, worldZ);
                const b = cam.worldToScreen(edge.wx2, edge.wy2, worldZ);
                if (
                    Math.max(a.x, b.x) < -margin || Math.min(a.x, b.x) > screenW + margin ||
                    Math.max(a.y, b.y) < -margin || Math.min(a.y, b.y) > screenH + margin
                ) continue;
                const dx = b.x - a.x;
                const dy = b.y - a.y;
                const len = Math.hypot(dx, dy);
                if (!(len > 1)) continue;
                const ux = dx / len;
                const uy = dy / len;
                let pos = 0;
                let drawing = true;
                while (pos < len) {
                    const segLen = drawing ? 8 : 6;
                    const next = Math.min(pos + segLen, len);
                    if (drawing) {
                        g.moveTo(a.x + ux * pos, a.y + uy * pos);
                        g.lineTo(a.x + ux * next, a.y + uy * next);
                    }
                    pos = next;
                    drawing = !drawing;
                }
            }
        }

        clearTerrainBrushPreview() {
            if (!this.terrainBrushPreviewGraphics) return;
            this.terrainBrushPreviewGraphics.clear();
            this.terrainBrushPreviewGraphics.visible = false;
        }

        renderTerrainBrushPreview(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.terrainBrushPreviewGraphics) {
                this.terrainBrushPreviewGraphics = new PIXI.Graphics();
                this.terrainBrushPreviewGraphics.name = "renderingTerrainBrushPreview";
                this.terrainBrushPreviewGraphics.skipTransform = true;
                this.terrainBrushPreviewGraphics.interactive = false;
                this.terrainBrushPreviewGraphics.visible = false;
                layer.addChild(this.terrainBrushPreviewGraphics);
            } else if (this.terrainBrushPreviewGraphics.parent !== layer) {
                layer.addChild(this.terrainBrushPreviewGraphics);
            }

            const g = this.terrainBrushPreviewGraphics;
            g.clear();

            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mapRef = (ctx && ctx.map) || (wizard && wizard.map) || global.map || null;
            const mousePosRef = this.getMousePosRef(ctx);
            if (
                !wizard ||
                wizard.currentSpell !== "terrainedit" ||
                !mapRef ||
                typeof mapRef.worldToNode !== "function" ||
                typeof mapRef.getGroundTerrainHexCorners !== "function" ||
                !this.camera ||
                !mousePosRef ||
                !Number.isFinite(mousePosRef.worldX) ||
                !Number.isFinite(mousePosRef.worldY)
            ) {
                g.visible = false;
                return;
            }

            const centerNode = mapRef.worldToNode(mousePosRef.worldX, mousePosRef.worldY);
            if (!centerNode) {
                g.visible = false;
                return;
            }

            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const brushRadius = (
                spellSystemRef &&
                typeof spellSystemRef.getSelectedTerrainBrushRadius === "function"
            ) ? spellSystemRef.getSelectedTerrainBrushRadius(wizard) : 1;
            const normalizedRadius = Math.max(1, Math.min(3, Math.round(Number(brushRadius) || 1)));
            let outlinePoints = [];
            if (normalizedRadius === 1) {
                outlinePoints = mapRef.getGroundTerrainHexCorners(centerNode);
                if (!Array.isArray(outlinePoints) || outlinePoints.length !== 6) {
                    throw new Error("terrain brush preview requires six center hex corners");
                }
            } else {
                if (typeof mapRef.collectGroundTerrainDeterministicBubbleNodes !== "function") {
                    throw new Error("terrain brush preview requires map.collectGroundTerrainDeterministicBubbleNodes");
                }
                const axialRadius = normalizedRadius - 1;
                const records = mapRef.collectGroundTerrainDeterministicBubbleNodes(centerNode, axialRadius);
                const recordByCoord = new Map();
                for (let i = 0; i < records.length; i++) {
                    const record = records[i];
                    if (!record || !record.node) continue;
                    recordByCoord.set(`${record.q},${record.r}`, record);
                }
                const cornerCoords = [
                    { q: axialRadius, r: 0 },
                    { q: axialRadius, r: -axialRadius },
                    { q: 0, r: -axialRadius },
                    { q: -axialRadius, r: 0 },
                    { q: -axialRadius, r: axialRadius },
                    { q: 0, r: axialRadius }
                ];
                outlinePoints = cornerCoords.map(coord => {
                    const record = recordByCoord.get(`${coord.q},${coord.r}`);
                    if (!record || !record.node) {
                        throw new Error(`terrain brush preview is missing outer corner tile ${coord.q},${coord.r}`);
                    }
                    const x = Number(record.node.x);
                    const y = Number(record.node.y);
                    if (!Number.isFinite(x) || !Number.isFinite(y)) {
                        throw new Error(`terrain brush preview outer corner ${coord.q},${coord.r} has non-finite center`);
                    }
                    return { x, y };
                });
            }

            const drawDashedPolygon = (width, color, alpha) => {
                g.lineStyle(width, color, alpha);
                for (let i = 0; i < outlinePoints.length; i++) {
                    const edgeA = outlinePoints[i];
                    const edgeB = outlinePoints[(i + 1) % outlinePoints.length];
                    const a = this.camera.worldToScreen(Number(edgeA.x), Number(edgeA.y), 0.01);
                    const b = this.camera.worldToScreen(Number(edgeB.x), Number(edgeB.y), 0.01);
                    const dx = b.x - a.x;
                    const dy = b.y - a.y;
                    const len = Math.hypot(dx, dy);
                    if (!(len > 1)) continue;
                    const ux = dx / len;
                    const uy = dy / len;
                    let pos = 0;
                    let drawing = true;
                    while (pos < len) {
                        const segLen = drawing ? 7 : 5;
                        const next = Math.min(pos + segLen, len);
                        if (drawing) {
                            g.moveTo(a.x + ux * pos, a.y + uy * pos);
                            g.lineTo(a.x + ux * next, a.y + uy * next);
                        }
                        pos = next;
                        drawing = !drawing;
                    }
                }
            };

            drawDashedPolygon(4, 0x102018, 0.72);
            drawDashedPolygon(2, 0xffffff, 0.92);
            g.visible = true;
        }

        clearRoadPlacementPreview() {
            if (this.roadPlacementPreviewContainer) {
                this.roadPlacementPreviewContainer.visible = false;
            }
            if (this.roadPlacementPreviewGraphics) {
                this.roadPlacementPreviewGraphics.clear();
                this.roadPlacementPreviewGraphics.visible = false;
            }
            if (this.roadPlacementPreviewSpriteByKey && this.roadPlacementPreviewSpriteByKey.size > 0) {
                for (const sprite of this.roadPlacementPreviewSpriteByKey.values()) {
                    if (!sprite) continue;
                    syncRoadRenderSpriteTextureRetention(sprite, null);
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                        sprite.renderable = false;
                    }
                }
            }
        }

        renderWallPlacementPreview(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.wallPlacementPreviewGraphics) {
                this.wallPlacementPreviewGraphics = new PIXI.Graphics();
                this.wallPlacementPreviewGraphics.name = "renderingWallPlacementPreview";
                this.wallPlacementPreviewGraphics.skipTransform = true;
                this.wallPlacementPreviewGraphics.interactive = false;
                this.wallPlacementPreviewGraphics.visible = false;
                layer.addChild(this.wallPlacementPreviewGraphics);
            } else if (this.wallPlacementPreviewGraphics.parent !== layer) {
                layer.addChild(this.wallPlacementPreviewGraphics);
            }

            const g = this.wallPlacementPreviewGraphics;
            g.clear();

            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mapRef = (ctx && ctx.map) || (wizard && wizard.map) || global.map || null;
            const mousePosRef = this.getMousePosRef(ctx);
            if (wizard) wizard.wallPreviewPlacement = null;
            if (
                !wizard ||
                wizard.currentSpell !== "wall" ||
                !wizard.wallLayoutMode ||
                !wizard.wallStartPoint ||
                !mapRef ||
                !mousePosRef ||
                !Number.isFinite(mousePosRef.worldX) ||
                !Number.isFinite(mousePosRef.worldY)
            ) {
                g.visible = false;
                return;
            }

            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const adjustedWallDragPoint = (
                spellSystemRef &&
                typeof spellSystemRef.getAdjustedWallDragWorldPoint === "function"
            ) ? spellSystemRef.getAdjustedWallDragWorldPoint(wizard, mousePosRef.worldX, mousePosRef.worldY) : null;
            const dragWorldX = adjustedWallDragPoint && Number.isFinite(adjustedWallDragPoint.x)
                ? adjustedWallDragPoint.x
                : mousePosRef.worldX;
            const dragWorldY = adjustedWallDragPoint && Number.isFinite(adjustedWallDragPoint.y)
                ? adjustedWallDragPoint.y
                : mousePosRef.worldY;

            const startWorld = {
                x: Number(wizard.wallStartPoint.x),
                y: Number(wizard.wallStartPoint.y)
            };
            const endWorld = { x: Number(dragWorldX), y: Number(dragWorldY) };
            if (
                !Number.isFinite(startWorld.x) ||
                !Number.isFinite(startWorld.y) ||
                !Number.isFinite(endWorld.x) ||
                !Number.isFinite(endWorld.y)
            ) {
                g.visible = false;
                return;
            }

            let segments = [];
            let previewPlan = null;
            if (
                typeof global.WallSectionUnit !== "undefined" &&
                global.WallSectionUnit &&
                typeof global.WallSectionUnit.planPlacementFromWorldPoints === "function"
            ) {
                const plan = global.WallSectionUnit.planPlacementFromWorldPoints(mapRef, startWorld, endWorld, {
                    rawStartWorld: (
                        wizard.wallDragMouseStartWorld &&
                        Number.isFinite(wizard.wallDragMouseStartWorld.x) &&
                        Number.isFinite(wizard.wallDragMouseStartWorld.y)
                    ) ? {
                        x: Number(wizard.wallDragMouseStartWorld.x),
                        y: Number(wizard.wallDragMouseStartWorld.y)
                    } : { x: startWorld.x, y: startWorld.y },
                    startFromExistingWall: !!wizard.wallStartFromExistingWall,
                    startReferenceWall: wizard.wallStartReferenceWall || null
                });
                if (plan) {
                    previewPlan = plan;
                }
                if (plan && Array.isArray(plan.segments)) {
                    segments = plan.segments.slice();
                }
            }

            if (segments.length === 0) {
                g.visible = false;
                return;
            }

            const normalizedSegments = [];
            for (let i = 0; i < segments.length; i++) {
                const seg = segments[i];
                if (!seg || !seg.start || !seg.end) continue;
                const sx = Number(seg.start.x);
                const sy = Number(seg.start.y);
                const ex = Number(seg.end.x);
                const ey = Number(seg.end.y);
                if (!Number.isFinite(sx) || !Number.isFinite(sy) || !Number.isFinite(ex) || !Number.isFinite(ey)) continue;
                normalizedSegments.push({
                    start: { x: sx, y: sy },
                    end: { x: ex, y: ey },
                    direction: Number.isFinite(seg.direction) ? Number(seg.direction) : undefined
                });
            }
            wizard.wallPreviewPlacement = {
                startWorld: { x: startWorld.x, y: startWorld.y },
                endWorld: { x: endWorld.x, y: endWorld.y },
                rawStartWorld: (
                    wizard.wallDragMouseStartWorld &&
                    Number.isFinite(wizard.wallDragMouseStartWorld.x) &&
                    Number.isFinite(wizard.wallDragMouseStartWorld.y)
                ) ? {
                    x: Number(wizard.wallDragMouseStartWorld.x),
                    y: Number(wizard.wallDragMouseStartWorld.y)
                } : { x: startWorld.x, y: startWorld.y },
                plan: previewPlan,
                segments: normalizedSegments
            };

            g.lineStyle(2, 0xff2222, 0.95);
            const wallThickness = (wizard && Number.isFinite(wizard.selectedWallThickness))
                ? wizard.selectedWallThickness : 0.1;
            const halfT = wallThickness * 0.5;
            const wallLayer = wizard && Number.isFinite(wizard.currentLayer)
                ? Math.round(Number(wizard.currentLayer))
                : 0;
            const bottomZ = wizard && Number.isFinite(wizard.currentLayerBaseZ)
                ? Number(wizard.currentLayerBaseZ)
                : this.getLayerBaseZForLevel(wallLayer);
            for (let i = 0; i < segments.length; i++) {
                const seg = segments[i];
                if (!seg || !seg.start || !seg.end) continue;
                const sx = Number(seg.start.x);
                const sy = Number(seg.start.y);
                const ex = Number(seg.end.x);
                const ey = Number(seg.end.y);
                if (!Number.isFinite(sx) || !Number.isFinite(sy) || !Number.isFinite(ex) || !Number.isFinite(ey)) continue;
                const dx = ex - sx;
                const dy = ey - sy;
                const len = Math.hypot(dx, dy);
                if (len < 1e-6) continue;
                // Perpendicular normal
                const nx = -dy / len;
                const ny = dx / len;
                // Four corners of the base rectangle
                const al = this.camera.worldToScreen(sx + nx * halfT, sy + ny * halfT, bottomZ);
                const ar = this.camera.worldToScreen(sx - nx * halfT, sy - ny * halfT, bottomZ);
                const bl = this.camera.worldToScreen(ex + nx * halfT, ey + ny * halfT, bottomZ);
                const br = this.camera.worldToScreen(ex - nx * halfT, ey - ny * halfT, bottomZ);
                // Draw closed rectangle
                g.moveTo(al.x, al.y);
                g.lineTo(bl.x, bl.y);
                g.lineTo(br.x, br.y);
                g.lineTo(ar.x, ar.y);
                g.lineTo(al.x, al.y);
            }
            g.visible = true;
        }

        renderRoadPlacementPreview(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.roadPlacementPreviewContainer) {
                this.roadPlacementPreviewContainer = new PIXI.Container();
                this.roadPlacementPreviewContainer.name = "renderingRoadPlacementPreview";
                this.roadPlacementPreviewContainer.skipTransform = true;
                this.roadPlacementPreviewContainer.interactive = false;
                this.roadPlacementPreviewContainer.visible = false;
                layer.addChild(this.roadPlacementPreviewContainer);
            } else if (this.roadPlacementPreviewContainer.parent !== layer) {
                layer.addChild(this.roadPlacementPreviewContainer);
            }
            if (!this.roadPlacementPreviewSpriteByKey) {
                this.roadPlacementPreviewSpriteByKey = new Map();
            }
            if (!this.roadPlacementPreviewGraphics) {
                this.roadPlacementPreviewGraphics = new PIXI.Graphics();
                this.roadPlacementPreviewGraphics.name = "renderingRoadPathPlacementPreview";
                this.roadPlacementPreviewGraphics.interactive = false;
                this.roadPlacementPreviewContainer.addChild(this.roadPlacementPreviewGraphics);
            } else if (this.roadPlacementPreviewGraphics.parent !== this.roadPlacementPreviewContainer) {
                this.roadPlacementPreviewContainer.addChild(this.roadPlacementPreviewGraphics);
            }

            const previewContainer = this.roadPlacementPreviewContainer;
            const previewSpriteByKey = this.roadPlacementPreviewSpriteByKey;
            const g = this.roadPlacementPreviewGraphics;
            g.clear();
            for (const sprite of previewSpriteByKey.values()) {
                if (!sprite) continue;
                syncRoadRenderSpriteTextureRetention(sprite, null);
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }
            }

            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mapRef = (ctx && ctx.map) || (wizard && wizard.map) || global.map || null;
            const mousePosRef = this.getMousePosRef(ctx);
            const RoadPathClass = (typeof global.RoadPath !== "undefined") ? global.RoadPath : null;
            const draft = wizard && wizard.roadPathDraft ? wizard.roadPathDraft : null;
            const draftPoints = draft && Array.isArray(draft.points) ? draft.points : [];
            const selectedRoadPath = (
                wizard &&
                wizard.currentSpell === "buildroad" &&
                wizard.selectedRoadPath &&
                !wizard.selectedRoadPath.gone &&
                wizard.selectedRoadPath.type === "roadPath"
            ) ? wizard.selectedRoadPath : null;
            const hasActivePlacementDraft = !!(
                wizard &&
                wizard.currentSpell === "buildroad" &&
                wizard.roadLayoutMode &&
                draftPoints.length >= 1
            );
            if (
                !wizard ||
                wizard.currentSpell !== "buildroad" ||
                (!hasActivePlacementDraft && !selectedRoadPath)
            ) {
                this.clearRoadPlacementPreview();
                return;
            }

            const renderZ = 0.001;
            const drawPointMarker = (point, color, radius = 4, strokeColor = 0x1d140c) => {
                const screen = this.camera.worldToScreen(Number(point.x), Number(point.y), renderZ);
                g.lineStyle(2, strokeColor, 0.9);
                g.beginFill(color, 0.96);
                g.drawCircle(screen.x, screen.y, radius);
                g.endFill();
            };
            const drawPathLine = (points, color, alpha = 0.95, width = 2) => {
                if (!Array.isArray(points) || points.length < 2) return;
                g.lineStyle(width, color, alpha);
                for (let i = 0; i < points.length; i++) {
                    const point = points[i];
                    if (!point || !Number.isFinite(point.x) || !Number.isFinite(point.y)) {
                        throw new Error(`Cannot draw road path edit line because point ${i} is invalid.`);
                    }
                    const screen = this.camera.worldToScreen(Number(point.x), Number(point.y), renderZ);
                    if (i === 0) g.moveTo(screen.x, screen.y);
                    else g.lineTo(screen.x, screen.y);
                }
            };
            const drawOutlinePolygon = (outline, color, alpha = 0.95, width = 2) => {
                if (!Array.isArray(outline) || outline.length < 3) {
                    throw new Error("Cannot draw selected road path without polygon outline geometry.");
                }
                g.lineStyle(width, color, alpha);
                for (let i = 0; i < outline.length; i++) {
                    const point = outline[i];
                    if (!point || !Number.isFinite(point.x) || !Number.isFinite(point.y)) {
                        throw new Error(`Cannot draw selected road path outline because point ${i} is invalid.`);
                    }
                    const screen = this.camera.worldToScreen(Number(point.x), Number(point.y), renderZ);
                    if (i === 0) g.moveTo(screen.x, screen.y);
                    else g.lineTo(screen.x, screen.y);
                }
                const first = this.camera.worldToScreen(Number(outline[0].x), Number(outline[0].y), renderZ);
                g.lineTo(first.x, first.y);
            };
            const drawSelectedRoadPathOverlay = (roadPath) => {
                const geometry = roadPath && roadPath.generatedGeometry;
                const outline = geometry && Array.isArray(geometry.outline)
                    ? geometry.outline
                    : (Array.isArray(roadPath && roadPath.outlinePolygon) ? roadPath.outlinePolygon : null);
                const points = geometry && Array.isArray(geometry.points)
                    ? geometry.points
                    : (Array.isArray(roadPath && roadPath.pathPoints) ? roadPath.pathPoints : null);
                if (!Array.isArray(points) || points.length < 2) {
                    throw new Error("Cannot draw selected road path without editable path points.");
                }
                drawOutlinePolygon(outline, 0x7bdcff, 0.95, 2);
                drawPathLine(points, 0xffffff, 0.72, 1);
                for (let i = 0; i < points.length; i++) {
                    const endpoint = i === 0 || i === points.length - 1;
                    drawPointMarker(points[i], endpoint ? 0x7bdcff : 0xf4efe5, endpoint ? 6 : 4, 0x102a36);
                }
            };

            if (selectedRoadPath) {
                drawSelectedRoadPathOverlay(selectedRoadPath);
            }

            if (
                !hasActivePlacementDraft ||
                !mapRef ||
                !RoadPathClass ||
                typeof RoadPathClass.computeGeometry !== "function" ||
                !mousePosRef ||
                !Number.isFinite(mousePosRef.worldX) ||
                !Number.isFinite(mousePosRef.worldY)
            ) {
                g.visible = !!selectedRoadPath;
                previewContainer.visible = !!selectedRoadPath;
                if (previewContainer.visible) this.promoteInteriorPresentationDisplayObject(previewContainer, ctx);
                return;
            }

            const configuredRoadWidth = Number.isFinite(draft.width)
                ? Math.max(0.25, Number(draft.width))
                : (Number.isFinite(wizard.selectedRoadWidth)
                ? Math.max(0.25, Number(wizard.selectedRoadWidth))
                : (
                    (typeof roadWidth !== "undefined" && Number.isFinite(roadWidth))
                        ? Number(roadWidth)
                        : ((Number.isFinite(global.roadWidth) ? Number(global.roadWidth) : 3))
                ));

            const wrapPreviewPoint = (x, y) => {
                let outX = Number(x);
                let outY = Number(y);
                if (!Number.isFinite(outX) || !Number.isFinite(outY)) return null;
                if (typeof mapRef.wrapWorldX === "function") outX = mapRef.wrapWorldX(outX);
                if (typeof mapRef.wrapWorldY === "function") outY = mapRef.wrapWorldY(outY);
                return { x: outX, y: outY };
            };
            const pointsMatch = (a, b, epsilon = 1e-5) => {
                if (!a || !b) return false;
                const ax = Number(a.x);
                const ay = Number(a.y);
                const bx = Number(b.x);
                const by = Number(b.y);
                if (!Number.isFinite(ax) || !Number.isFinite(ay) || !Number.isFinite(bx) || !Number.isFinite(by)) return false;
                const dx = (typeof mapRef.shortestDeltaX === "function")
                    ? mapRef.shortestDeltaX(ax, bx)
                    : (bx - ax);
                const dy = (typeof mapRef.shortestDeltaY === "function")
                    ? mapRef.shortestDeltaY(ay, by)
                    : (by - ay);
                return Math.abs(dx) <= epsilon && Math.abs(dy) <= epsilon;
            };
            const candidateNode = typeof mapRef.worldToNode === "function"
                ? mapRef.worldToNode(mousePosRef.worldX, mousePosRef.worldY)
                : null;
            const candidatePoint = candidateNode && Number.isFinite(candidateNode.x) && Number.isFinite(candidateNode.y)
                ? wrapPreviewPoint(candidateNode.x, candidateNode.y)
                : wrapPreviewPoint(mousePosRef.worldX, mousePosRef.worldY);
            if (!candidatePoint) {
                this.clearRoadPlacementPreview();
                return;
            }

            const committedPoints = draftPoints.map((point, index) => {
                const wrapped = wrapPreviewPoint(point && point.x, point && point.y);
                if (!wrapped) {
                    throw new Error(`Cannot preview road path because draft point ${index} is invalid.`);
                }
                return wrapped;
            });
            const lastPoint = committedPoints[committedPoints.length - 1];
            const previewPoints = pointsMatch(lastPoint, candidatePoint)
                ? committedPoints.slice()
                : committedPoints.concat(candidatePoint);
            const drawGeometry = (geometry, fillAlpha, strokeColor, strokeAlpha) => {
                if (!geometry || !Array.isArray(geometry.segments)) return;
                g.beginFill(0x9b8162, fillAlpha);
                g.lineStyle(2, strokeColor, strokeAlpha);
                for (let i = 0; i < geometry.segments.length; i++) {
                    const polygon = geometry.segments[i].polygon;
                    if (!Array.isArray(polygon) || polygon.length < 3) {
                        throw new Error(`Cannot preview road path segment ${i} without polygon geometry.`);
                    }
                    for (let p = 0; p < polygon.length; p++) {
                        const screen = this.camera.worldToScreen(Number(polygon[p].x), Number(polygon[p].y), renderZ);
                        if (p === 0) g.moveTo(screen.x, screen.y);
                        else g.lineTo(screen.x, screen.y);
                    }
                    const first = this.camera.worldToScreen(Number(polygon[0].x), Number(polygon[0].y), renderZ);
                    g.lineTo(first.x, first.y);
                }
                g.endFill();
            };

            let candidateWasInvalid = false;
            if (previewPoints.length >= 2) {
                try {
                    drawGeometry(RoadPathClass.computeGeometry(previewPoints, configuredRoadWidth), 0.5, 0xffd27a, 0.9);
                } catch (error) {
                    candidateWasInvalid = true;
                    if (committedPoints.length >= 2) {
                        drawGeometry(RoadPathClass.computeGeometry(committedPoints, configuredRoadWidth), 0.42, 0xffd27a, 0.75);
                    }
                }
            }
            for (let i = 0; i < committedPoints.length; i++) {
                drawPointMarker(committedPoints[i], i === committedPoints.length - 1 ? 0xffd27a : 0xf4efe5, 4);
            }
            if (candidateWasInvalid) {
                drawPointMarker(candidatePoint, 0xff5656, 5);
            } else if (!pointsMatch(lastPoint, candidatePoint)) {
                drawPointMarker(candidatePoint, 0xffd27a, 4);
            }
            g.visible = true;
            previewContainer.visible = true;
            if (previewContainer.visible) this.promoteInteriorPresentationDisplayObject(previewContainer, ctx);
        }

        clearFirewallPlacementPreview() {
            if (!this.firewallPlacementPreviewGraphics) return;
            this.firewallPlacementPreviewGraphics.clear();
            this.firewallPlacementPreviewGraphics.visible = false;
        }

        renderFirewallPlacementPreview(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.firewallPlacementPreviewGraphics) {
                this.firewallPlacementPreviewGraphics = new PIXI.Graphics();
                this.firewallPlacementPreviewGraphics.name = "renderingFirewallPlacementPreview";
                this.firewallPlacementPreviewGraphics.skipTransform = true;
                this.firewallPlacementPreviewGraphics.interactive = false;
                this.firewallPlacementPreviewGraphics.visible = false;
                layer.addChild(this.firewallPlacementPreviewGraphics);
            } else if (this.firewallPlacementPreviewGraphics.parent !== layer) {
                layer.addChild(this.firewallPlacementPreviewGraphics);
            }

            const g = this.firewallPlacementPreviewGraphics;
            g.clear();

            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const mapRef = (ctx && ctx.map) || (wizard && wizard.map) || global.map || null;
            const mousePosRef = this.getMousePosRef(ctx);
            if (
                !wizard ||
                wizard.currentSpell !== "firewall" ||
                !wizard.firewallLayoutMode ||
                !wizard.firewallStartPoint ||
                !mapRef ||
                !mousePosRef ||
                !Number.isFinite(mousePosRef.worldX) ||
                !Number.isFinite(mousePosRef.worldY)
            ) {
                g.visible = false;
                return;
            }

            const startWorld = {
                x: Number(wizard.firewallStartPoint.x),
                y: Number(wizard.firewallStartPoint.y)
            };
            const endWorld = { x: Number(mousePosRef.worldX), y: Number(mousePosRef.worldY) };
            if (
                !Number.isFinite(startWorld.x) ||
                !Number.isFinite(startWorld.y) ||
                !Number.isFinite(endWorld.x) ||
                !Number.isFinite(endWorld.y)
            ) {
                g.visible = false;
                return;
            }

            const screenStart = this.camera.worldToScreen(startWorld.x, startWorld.y, 0);
            const screenEnd = this.camera.worldToScreen(endWorld.x, endWorld.y, 0);
            if (!screenStart || !screenEnd) {
                g.visible = false;
                return;
            }

            // Draw red preview line
            g.lineStyle(3, 0xff2222, 0.9);
            g.moveTo(screenStart.x, screenStart.y);
            g.lineTo(screenEnd.x, screenEnd.y);

            // Draw tick marks along the line at emitter spacing intervals
            const dx = endWorld.x - startWorld.x;
            const dy = endWorld.y - startWorld.y;
            const dist = Math.hypot(dx, dy);
            const spacing = 0.5;
            const steps = Math.max(1, Math.ceil(dist / spacing));
            const perpScreenX = -(screenEnd.y - screenStart.y);
            const perpScreenY = (screenEnd.x - screenStart.x);
            const perpLen = Math.hypot(perpScreenX, perpScreenY);
            const tickHalfLen = 4;
            if (perpLen > 1e-6) {
                const nx = (perpScreenX / perpLen) * tickHalfLen;
                const ny = (perpScreenY / perpLen) * tickHalfLen;
                g.lineStyle(2, 0xff4444, 0.7);
                for (let i = 0; i <= steps; i++) {
                    const t = steps === 0 ? 0 : i / steps;
                    const px = screenStart.x + (screenEnd.x - screenStart.x) * t;
                    const py = screenStart.y + (screenEnd.y - screenStart.y) * t;
                    g.moveTo(px - nx, py - ny);
                    g.lineTo(px + nx, py + ny);
                }
            }

            g.visible = true;
            this.promoteInteriorPresentationDisplayObject(g, ctx);
        }

        clearTriggerAreaPlacementPreview() {
            if (!this.triggerAreaPlacementPreviewGraphics) return;
            this.triggerAreaPlacementPreviewGraphics.clear();
            this.triggerAreaPlacementPreviewGraphics.visible = false;
        }

        renderTriggerAreaPlacementPreview(ctx) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.triggerAreaPlacementPreviewGraphics) {
                this.triggerAreaPlacementPreviewGraphics = new PIXI.Graphics();
                this.triggerAreaPlacementPreviewGraphics.name = "renderingTriggerAreaPlacementPreview";
                this.triggerAreaPlacementPreviewGraphics.skipTransform = true;
                this.triggerAreaPlacementPreviewGraphics.interactive = false;
                this.triggerAreaPlacementPreviewGraphics.visible = false;
                layer.addChild(this.triggerAreaPlacementPreviewGraphics);
            } else if (this.triggerAreaPlacementPreviewGraphics.parent !== layer) {
                layer.addChild(this.triggerAreaPlacementPreviewGraphics);
            }

            const g = this.triggerAreaPlacementPreviewGraphics;
            g.clear();
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            if (
                !wizard ||
                (wizard.currentSpell !== "triggerarea" && wizard.currentSpell !== "floorshape" && wizard.currentSpell !== "floorhole" && wizard.currentSpell !== "floorstair") ||
                !spellSystemRef ||
                typeof spellSystemRef.getTriggerAreaPlacementPreview !== "function"
            ) {
                g.visible = false;
                return;
            }

            const mousePosRef = this.getMousePosRef(ctx);
            const floorShapeMouseWorldPos = (mousePosRef && Number.isFinite(mousePosRef.worldX) && Number.isFinite(mousePosRef.worldY))
                ? { x: Number(mousePosRef.worldX), y: Number(mousePosRef.worldY) }
                : null;
            const preview = wizard.currentSpell === "floorshape" && typeof spellSystemRef.getFloorShapePlacementPreview === "function"
                ? spellSystemRef.getFloorShapePlacementPreview(wizard, { mouseWorldPos: floorShapeMouseWorldPos })
                : (wizard.currentSpell === "floorhole" && typeof spellSystemRef.getFloorHolePlacementPreview === "function"
                    ? spellSystemRef.getFloorHolePlacementPreview(wizard)
                    : (wizard.currentSpell === "floorstair" && typeof spellSystemRef.getFloorStairPlacementPreview === "function"
                        ? spellSystemRef.getFloorStairPlacementPreview(wizard)
                        : spellSystemRef.getTriggerAreaPlacementPreview(wizard)));
            if (wizard.currentSpell === "floorstair") {
                if (!preview || !preview.startPoint) {
                    g.visible = false;
                    return;
                }
                const baseZ = Number.isFinite(preview.startBaseZ) ? Number(preview.startBaseZ) : 0;
                const lineColor = preview.valid ? 0x66ffaa : (preview.intent ? 0xffcc44 : 0x9ee7ff);
                const footprint = Array.isArray(preview.footprint) ? preview.footprint : [];
                if (footprint.length >= 3) {
                    const firstFoot = this.camera.worldToScreen(Number(footprint[0].x), Number(footprint[0].y), baseZ);
                    if (firstFoot && Number.isFinite(firstFoot.x) && Number.isFinite(firstFoot.y)) {
                        g.lineStyle(2, lineColor, 0.95);
                        g.beginFill(lineColor, preview.valid ? 0.18 : 0.09);
                        g.moveTo(firstFoot.x, firstFoot.y);
                        for (let i = 1; i < footprint.length; i++) {
                            const sp = this.camera.worldToScreen(Number(footprint[i].x), Number(footprint[i].y), baseZ);
                            if (sp && Number.isFinite(sp.x) && Number.isFinite(sp.y)) g.lineTo(sp.x, sp.y);
                        }
                        g.closePath();
                        g.endFill();
                    }
                }
                const startScreen = this.camera.worldToScreen(Number(preview.startPoint.x), Number(preview.startPoint.y), baseZ);
                const endPoint = preview.currentPoint || preview.startPoint;
                const endScreen = this.camera.worldToScreen(Number(endPoint.x), Number(endPoint.y), baseZ);
                if (startScreen && endScreen && Number.isFinite(startScreen.x) && Number.isFinite(endScreen.x)) {
                    g.lineStyle(3, lineColor, 0.95);
                    g.moveTo(startScreen.x, startScreen.y);
                    g.lineTo(endScreen.x, endScreen.y);
                    g.beginFill(lineColor, 0.95);
                    g.drawCircle(startScreen.x, startScreen.y, 5);
                    g.endFill();
                }
                g.visible = true;
                this.promoteInteriorPresentationDisplayObject(g, ctx);
                return;
            }
            if (!preview || !Array.isArray(preview.points) || preview.points.length === 0) {
                g.visible = false;
                return;
            }
            const points = preview.points;
            const first = points[0];
            const previewLevel = (wizard.currentSpell === "floorshape" || wizard.currentSpell === "floorhole")
                ? this.getFloorEditorOverlayLevel(ctx)
                : 0;
            const previewZ = this.getLayerBaseZForLevel(previewLevel);

            // Wall-loop snap mode: highlight the top faces of all wall sections and draw a
            // closed polygon preview at the selected floor level without a ghost cursor line.
            if (preview.isWallLoop && Array.isArray(preview.wallSections) && preview.wallSections.length > 0) {
                for (let si = 0; si < preview.wallSections.length; si++) {
                    const section = preview.wallSections[si];
                    if (!section || typeof section.getWallProfile !== "function") continue;
                    const profile = section.getWallProfile();
                    if (!profile) continue;
                    const topZ = Math.max(0, Number(section.bottomZ) || 0) + Math.max(0, Number(section.height) || 0);
                    const topFace = [
                        this.camera.worldToScreen(Number(profile.aLeft.x), Number(profile.aLeft.y), topZ),
                        this.camera.worldToScreen(Number(profile.bLeft.x), Number(profile.bLeft.y), topZ),
                        this.camera.worldToScreen(Number(profile.bRight.x), Number(profile.bRight.y), topZ),
                        this.camera.worldToScreen(Number(profile.aRight.x), Number(profile.aRight.y), topZ)
                    ];
                    if (!topFace.every(pt => Number.isFinite(pt.x) && Number.isFinite(pt.y))) continue;
                    g.lineStyle(2, 0x66ffaa, 0.55);
                    g.beginFill(0x66ffaa, 0.13);
                    g.moveTo(topFace[0].x, topFace[0].y);
                    g.lineTo(topFace[1].x, topFace[1].y);
                    g.lineTo(topFace[2].x, topFace[2].y);
                    g.lineTo(topFace[3].x, topFace[3].y);
                    g.closePath();
                    g.endFill();
                }
                // Draw closed floor polygon outline at the selected level.
                const firstScreen = this.camera.worldToScreen(Number(first.x), Number(first.y), previewZ);
                if (firstScreen && Number.isFinite(firstScreen.x) && Number.isFinite(firstScreen.y)) {
                    g.lineStyle(2, 0x66ffaa, 0.9);
                    g.moveTo(firstScreen.x, firstScreen.y);
                    for (let i = 1; i < points.length; i++) {
                        const sp = this.camera.worldToScreen(Number(points[i].x), Number(points[i].y), previewZ);
                        if (Number.isFinite(sp.x) && Number.isFinite(sp.y)) g.lineTo(sp.x, sp.y);
                    }
                    g.closePath();
                }
                g.visible = true;
                return;
            }

            const startScreen = this.camera.worldToScreen(Number(first.x), Number(first.y), previewZ);
            if (!startScreen || !Number.isFinite(startScreen.x) || !Number.isFinite(startScreen.y)) {
                g.visible = false;
                return;
            }

            const hasMouseWorld = !!(
                mousePosRef &&
                Number.isFinite(mousePosRef.worldX) &&
                Number.isFinite(mousePosRef.worldY)
            );
            const mouseScreen = hasMouseWorld
                ? this.camera.worldToScreen(Number(mousePosRef.worldX), Number(mousePosRef.worldY), previewZ)
                : null;

            const lineColor = wizard.currentSpell === "floorshape"
                ? 0xffd700
                : (wizard.currentSpell === "floorhole" ? 0xff5a5a : 0x66c2ff);
            const startColor = wizard.currentSpell === "floorshape"
                ? 0xfff0a0
                : (wizard.currentSpell === "floorhole" ? 0xffb0b0 : 0x9ee7ff);
            const drawDraftLine = (width, color, alpha) => {
                g.lineStyle(width, color, alpha);
                g.moveTo(startScreen.x, startScreen.y);
                for (let i = 1; i < points.length; i++) {
                    const pt = points[i];
                    if (!pt || !Number.isFinite(pt.x) || !Number.isFinite(pt.y)) continue;
                    const sp = this.camera.worldToScreen(Number(pt.x), Number(pt.y), previewZ);
                    if (sp && Number.isFinite(sp.x) && Number.isFinite(sp.y)) {
                        g.lineTo(sp.x, sp.y);
                    }
                }
                if (mouseScreen && Number.isFinite(mouseScreen.x) && Number.isFinite(mouseScreen.y)) {
                    g.lineTo(mouseScreen.x, mouseScreen.y);
                }
            };
            drawDraftLine(6, 0x000000, 0.72);
            drawDraftLine(3, lineColor, 0.98);

            for (let i = 0; i < points.length; i++) {
                const pt = points[i];
                if (!pt || !Number.isFinite(pt.x) || !Number.isFinite(pt.y)) continue;
                const sp = i === 0
                    ? startScreen
                    : this.camera.worldToScreen(Number(pt.x), Number(pt.y), previewZ);
                if (!sp || !Number.isFinite(sp.x) || !Number.isFinite(sp.y)) continue;
                const radius = i === 0 ? 6 : 4;
                g.lineStyle(2, 0x000000, 0.85);
                g.beginFill(i === 0 ? startColor : lineColor, 0.95);
                g.drawCircle(sp.x, sp.y, radius);
                g.endFill();
            }
            g.visible = true;
        }

        clearFloorEditorPolygonOverlay() {
            if (!this.floorEditorPolygonOverlayGraphics) return;
            this.floorEditorPolygonOverlayGraphics.clear();
            this.floorEditorPolygonOverlayGraphics.visible = false;
        }

        isLayerCutawayDebugVisible() {
            return global.renderingShowLayerCutawayDebugAreas !== false;
        }

        clearLayerCutawayDebugOverlay() {
            if (!this.layerCutawayDebugGraphics) return;
            this.layerCutawayDebugGraphics.clear();
            this.layerCutawayDebugGraphics.visible = false;
        }

        drawLayerCutawayDebugRing(g, points, baseZ) {
            const ring = normalizeFloorVisualPointList(points);
            if (!g || ring.length < 3 || !this.camera) return false;
            let started = false;
            for (let i = 0; i < ring.length; i++) {
                const screen = this.camera.worldToScreen(ring[i].x, ring[i].y, baseZ);
                if (!screen || !Number.isFinite(screen.x) || !Number.isFinite(screen.y)) return false;
                if (!started) {
                    g.moveTo(screen.x, screen.y);
                    started = true;
                } else {
                    g.lineTo(screen.x, screen.y);
                }
            }
            if (!started) return false;
            g.closePath();
            return true;
        }

        renderLayerCutawayDebugOverlay(ctx) {
            global.renderingLayerCutawayDebugState = {
                active: false,
                disabled: true,
                triggers: []
            };
            this.clearLayerCutawayDebugOverlay();
        }

        isFloorEditorToolSetActive(ctx) {
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const spellName = wizard && typeof wizard.currentSpell === "string"
                ? wizard.currentSpell
                : "";
            if (spellName === "flooredit") return true;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            if (spellSystemRef && typeof spellSystemRef.isFloorEditorToolName === "function") {
                return !!spellSystemRef.isFloorEditorToolName(spellName);
            }
            return spellName === "floorshape" || spellName === "floorhole" || spellName === "floorstair";
        }

        getFloorEditorOverlayLevel(ctx) {
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            if (wizard && Number.isFinite(wizard.selectedFloorEditLevel)) {
                return this.getLayerIndexFromValue(wizard.selectedFloorEditLevel, 0);
            }
            return this.getSelectedFloorVisualLevel();
        }

        drawFloorEditorOverlayRing(g, points, baseZ, options = {}) {
            const ring = normalizeFloorVisualPointList(points);
            if (!g || ring.length < 3) return false;
            const screenPoints = [];
            for (let i = 0; i < ring.length; i++) {
                const screen = this.camera.worldToScreen(ring[i].x, ring[i].y, baseZ);
                if (!screen || !Number.isFinite(screen.x) || !Number.isFinite(screen.y)) return false;
                screenPoints.push(screen);
            }

            const alpha = Number.isFinite(options.alpha) ? Number(options.alpha) : 0.95;
            const radius = Number.isFinite(options.radius) ? Number(options.radius) : 3.5;
            const selectedVertexIndex = Number.isInteger(options.selectedVertexIndex)
                ? options.selectedVertexIndex
                : -1;
            g.lineStyle(2, 0xff0000, alpha);
            g.moveTo(screenPoints[0].x, screenPoints[0].y);
            for (let i = 1; i < screenPoints.length; i++) {
                g.lineTo(screenPoints[i].x, screenPoints[i].y);
            }
            g.closePath();

            for (let i = 0; i < screenPoints.length; i++) {
                const selected = i === selectedVertexIndex;
                g.lineStyle(selected ? 2 : 1, selected ? 0xffffff : 0xffffff, selected ? 1 : 0.9);
                g.beginFill(selected ? 0xffffff : 0xff0000, selected ? 1 : 0.95);
                g.drawCircle(screenPoints[i].x, screenPoints[i].y, selected ? radius + 3 : radius);
                g.endFill();
            }
            return true;
        }

        renderFloorEditorPolygonOverlay(ctx) {
            const layer = this.layers && this.layers.ui ? this.layers.ui : null;
            if (!layer) return;
            if (!this.floorEditorPolygonOverlayGraphics) {
                this.floorEditorPolygonOverlayGraphics = new PIXI.Graphics();
                this.floorEditorPolygonOverlayGraphics.name = "renderingFloorEditorPolygonOverlay";
                this.floorEditorPolygonOverlayGraphics.skipTransform = true;
                this.floorEditorPolygonOverlayGraphics.interactive = false;
                this.floorEditorPolygonOverlayGraphics.visible = false;
                layer.addChild(this.floorEditorPolygonOverlayGraphics);
            } else if (this.floorEditorPolygonOverlayGraphics.parent !== layer) {
                layer.addChild(this.floorEditorPolygonOverlayGraphics);
            }

            const g = this.floorEditorPolygonOverlayGraphics;
            g.clear();
            if (!this.isFloorEditorToolSetActive(ctx)) {
                g.visible = false;
                return;
            }

            const mapRef = (ctx && ctx.map) || global.map || null;
            if (!mapRef || !(mapRef.floorsById instanceof Map)) {
                g.visible = false;
                return;
            }

            const selectedLevel = this.getFloorEditorOverlayLevel(ctx);
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const selection = (
                wizard &&
                spellSystemRef &&
                typeof spellSystemRef.getFloorEditorVertexSelection === "function"
            ) ? spellSystemRef.getFloorEditorVertexSelection(wizard) : null;
            let drawn = 0;
            for (const fragment of mapRef.floorsById.values()) {
                if (!fragment) continue;
                const level = Number.isFinite(fragment.level) ? Math.round(Number(fragment.level)) : 0;
                if (level !== selectedLevel) continue;
                if (fragment._prototypeGroundFloor === true && level === 0) continue;
                const outer = fragment.outerPolygon;
                const baseZ = Number.isFinite(fragment.nodeBaseZ)
                    ? Number(fragment.nodeBaseZ)
                    : this.getLayerBaseZForLevel(level);
                const fragmentId = typeof fragment.fragmentId === "string" ? fragment.fragmentId : "";
                const outerSelectedIndex = (
                    selection &&
                    selection.fragmentId === fragmentId &&
                    selection.ringKind === "outer"
                ) ? selection.vertexIndex : -1;
                if (this.drawFloorEditorOverlayRing(g, outer, baseZ, { alpha: 0.98, radius: 3.5, selectedVertexIndex: outerSelectedIndex })) {
                    drawn += 1;
                }
                const holes = Array.isArray(fragment.holes) ? fragment.holes : [];
                for (let h = 0; h < holes.length; h++) {
                    const holeSelectedIndex = (
                        selection &&
                        selection.fragmentId === fragmentId &&
                        selection.ringKind === "hole" &&
                        selection.holeIndex === h
                    ) ? selection.vertexIndex : -1;
                    if (this.drawFloorEditorOverlayRing(g, holes[h], baseZ, { alpha: 0.8, radius: 3, selectedVertexIndex: holeSelectedIndex })) {
                        drawn += 1;
                    }
                }
            }
            g.visible = drawn > 0;
        }

        buildPlaceObjectPreviewRenderItem(ctx) {
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            if (!wizard || wizard.currentSpell !== "placeobject" || wizard.editorPlacementActive !== true) {
                return null;
            }
            const mousePosRef = this.getMousePosRef(ctx);
            if (!mousePosRef || !Number.isFinite(mousePosRef.worldX) || !Number.isFinite(mousePosRef.worldY)) {
                return null;
            }

            const texturePath = (
                typeof wizard.selectedPlaceableTexturePath === "string" &&
                wizard.selectedPlaceableTexturePath.length > 0
            ) ? wizard.selectedPlaceableTexturePath : "/assets/images/doors/door5.png";
            const selectedCategory = (
                typeof wizard.selectedPlaceableCategory === "string" &&
                wizard.selectedPlaceableCategory.length > 0
            ) ? wizard.selectedPlaceableCategory : "doors";
            const rawAxis = (typeof wizard.selectedPlaceableRotationAxis === "string")
                ? wizard.selectedPlaceableRotationAxis.trim().toLowerCase()
                : "";
            const rotationAxis = (rawAxis === "spatial" || rawAxis === "visual" || rawAxis === "none" || rawAxis === "ground")
                ? rawAxis
                : ((selectedCategory === "doors" || selectedCategory === "windows") ? "spatial" : "visual");
            const placementRotation = Number.isFinite(wizard.selectedPlaceableRotation)
                ? Number(wizard.selectedPlaceableRotation)
                : 0;
            const effectivePlacementRotation = (rotationAxis === "none") ? 0 : placementRotation;

            if (!this.placeObjectPreviewSprite) {
                this.placeObjectPreviewSprite = new PIXI.Sprite(PIXI.Texture.from(texturePath));
                this.placeObjectPreviewSprite.anchor.set(0.5, 0.5);
                this.placeObjectPreviewSprite.alpha = 0.5;
                this.placeObjectPreviewSprite.interactive = false;
                this.placeObjectPreviewSprite.visible = false;
                this.placeObjectPreviewTexturePath = texturePath;
            } else if (this.placeObjectPreviewTexturePath !== texturePath) {
                this.placeObjectPreviewSprite.texture = PIXI.Texture.from(texturePath);
                this.placeObjectPreviewTexturePath = texturePath;
            }

            const mapRef = (ctx && ctx.map) || wizard.map || global.map || null;
            const placementLayer = this.getLayerIndexFromValue(
                Number.isFinite(wizard.currentLayer)
                    ? wizard.currentLayer
                    : (Number.isFinite(wizard.selectedFloorEditLevel) ? wizard.selectedFloorEditLevel : 0),
                0
            );
            const placementLayerBaseZ = this.getPlacementLayerBaseZForWizard(wizard, mapRef, placementLayer);
            const projectedLayerPoint = this.resolveScreenPointOnLayerPlane(
                mousePosRef.screenX,
                mousePosRef.screenY,
                placementLayerBaseZ,
                mapRef,
                wizard
            );
            const rawWorldX = projectedLayerPoint && Number.isFinite(projectedLayerPoint.x)
                ? Number(projectedLayerPoint.x)
                : mousePosRef.worldX;
            const rawWorldY = projectedLayerPoint && Number.isFinite(projectedLayerPoint.y)
                ? Number(projectedLayerPoint.y)
                : mousePosRef.worldY;
            const worldX = (mapRef && typeof mapRef.wrapWorldX === "function")
                ? mapRef.wrapWorldX(rawWorldX)
                : rawWorldX;
            const worldY = (mapRef && typeof mapRef.wrapWorldY === "function")
                ? mapRef.wrapWorldY(rawWorldY)
                : rawWorldY;
            const supportsWallSnapPlacement = selectedCategory === "windows" || selectedCategory === "doors";
            const requiresWallSnapPlacement = selectedCategory === "windows";
            const isRoofPlacement = selectedCategory === "roof";
            const roofApi = (typeof global.Roof === "function")
                ? global.Roof
                : ((typeof Roof === "function") ? Roof : null);
            const roofDiagnostics = (
                isRoofPlacement &&
                roofApi &&
                typeof roofApi.getPlacementDiagnostics === "function"
            ) ? roofApi.getPlacementDiagnostics(wizard, worldX, worldY, { maxDepth: 12 }) : null;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const snapPlacement = (
                (supportsWallSnapPlacement || isRoofPlacement) &&
                spellSystemRef &&
                typeof spellSystemRef.getPlaceObjectPlacementCandidate === "function"
            ) ? spellSystemRef.getPlaceObjectPlacementCandidate(wizard, worldX, worldY) : null;
            if (isRoofPlacement) {
                if (!roofDiagnostics || !roofDiagnostics.hoveredSection) return null;
                const previewItem = this.ensurePlaceObjectPreviewItem(mapRef);
                previewItem.roofHighlightOnly = true;
                previewItem.roofLoopSections = Array.isArray(roofDiagnostics.wallSections)
                    ? roofDiagnostics.wallSections.slice()
                    : [roofDiagnostics.hoveredSection];
                previewItem.centerSnapGuide = null;
                previewItem.traversalLayer = placementLayer;
                previewItem.level = placementLayer;
                previewItem._renderLayerBaseZ = placementLayerBaseZ;
                return previewItem;
            }
            const useSnapPlacement = !!(snapPlacement && snapPlacement.targetWall);
            const useRoofPlacement = !!(
                isRoofPlacement &&
                useSnapPlacement &&
                Number.isFinite(snapPlacement.previewX) &&
                Number.isFinite(snapPlacement.previewY) &&
                Number.isFinite(snapPlacement.previewZ)
            );
            if (requiresWallSnapPlacement) {
                if (
                    !useSnapPlacement ||
                    !Number.isFinite(snapPlacement.snappedX) ||
                    !Number.isFinite(snapPlacement.snappedY) ||
                    !Number.isFinite(snapPlacement.snappedRotationDeg) ||
                    !Number.isFinite(snapPlacement.snappedZ)
                ) {
                    return null;
                }
            }
            if (isRoofPlacement && !useRoofPlacement) return null;
            const placeableScale = Number.isFinite(wizard.selectedPlaceableScale)
                ? Number(wizard.selectedPlaceableScale)
                : 1;
            const scaleMin = Number.isFinite(wizard.selectedPlaceableScaleMin) ? wizard.selectedPlaceableScaleMin : 0.2;
            const scaleMax = Number.isFinite(wizard.selectedPlaceableScaleMax) ? wizard.selectedPlaceableScaleMax : 5;
            const clampedScale = Math.max(scaleMin, Math.min(scaleMax, placeableScale));
            const selectedSizing = (
                wizard.selectedPlaceableSizingByTexture &&
                typeof wizard.selectedPlaceableSizingByTexture === "object"
            ) ? wizard.selectedPlaceableSizingByTexture[texturePath] : null;
            const scaledDimensions = (
                typeof globalThis !== "undefined" &&
                typeof globalThis.resolvePlaceableScaledDimensions === "function"
            ) ? globalThis.resolvePlaceableScaledDimensions(selectedSizing, clampedScale) : {
                width: clampedScale,
                height: clampedScale
            };
            const selectedAnchorY = Number.isFinite(wizard.selectedPlaceableAnchorY)
                ? Number(wizard.selectedPlaceableAnchorY)
                : 1;
            const effectiveAnchorY = isRoofPlacement
                ? 0.5
                : ((useSnapPlacement && selectedCategory === "windows") ? 0.5 : selectedAnchorY);
            const yScale = Math.max(0.1, Math.abs(Number.isFinite(this.camera.xyratio) ? this.camera.xyratio : 0.66));
            const placementYOffset = (rotationAxis === "spatial" || rotationAxis === "ground" || isRoofPlacement)
                ? 0
                : (((effectiveAnchorY - 0.5) * scaledDimensions.height) / yScale);
            const spatialAnchorPlacementYOffset = (
                rotationAxis === "spatial" &&
                !useSnapPlacement &&
                !useRoofPlacement &&
                (selectedCategory === "doors" || selectedCategory === "windows")
            )
                ? (((effectiveAnchorY - 0.5) * scaledDimensions.height) / yScale)
                : 0;
            const previewX = useRoofPlacement
                ? Number(snapPlacement.previewX)
                : (useSnapPlacement ? snapPlacement.snappedX : worldX);
            let placedY = useRoofPlacement
                ? Number(snapPlacement.previewY)
                : (useSnapPlacement ? snapPlacement.snappedY : (worldY + placementYOffset + spatialAnchorPlacementYOffset));
            if (mapRef && typeof mapRef.wrapWorldY === "function") {
                placedY = mapRef.wrapWorldY(placedY);
            }
            const renderDepthOffset = Number.isFinite(wizard.selectedPlaceableRenderOffset)
                ? Number(wizard.selectedPlaceableRenderOffset)
                : 0;
            this.placeObjectPreviewSprite.tint = 0xFFFFFF;
            this.placeObjectPreviewSprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewSprite, "renderable")) {
                this.placeObjectPreviewSprite.renderable = true;
            }
            const previewItem = this.ensurePlaceObjectPreviewItem(mapRef);
            previewItem.x = previewX;
            previewItem.y = useRoofPlacement
                ? Number(snapPlacement.previewY)
                : (useSnapPlacement ? snapPlacement.snappedY : placedY);
            previewItem.z = useRoofPlacement
                ? Number(snapPlacement.previewZ)
                : (useSnapPlacement ? Number(snapPlacement.snappedZ) : 0);
            previewItem.width = scaledDimensions.width;
            previewItem.height = scaledDimensions.height;
            previewItem.renderZ = placedY + renderDepthOffset;
            previewItem.previewAlpha = 0.5;
            previewItem.texturePath = texturePath;
            previewItem.category = selectedCategory;
            previewItem.traversalLayer = placementLayer;
            previewItem.level = placementLayer;
            previewItem._renderLayerBaseZ = placementLayerBaseZ;
            
            if (wizard.selectedPlaceableCompositeLayersByTexture && wizard.selectedPlaceableCompositeLayersByTexture[texturePath]) {
                previewItem.compositeLayers = this.getRenderableDoorCompositeLayers({
                    category: selectedCategory,
                    type: selectedCategory === "doors" ? "door" : "",
                    compositeLayers: wizard.selectedPlaceableCompositeLayersByTexture[texturePath]
                });
            } else {
                previewItem.compositeLayers = null;
            }

            previewItem.placeableAnchorX = Number.isFinite(wizard.selectedPlaceableAnchorX)
                ? ((useSnapPlacement && !useRoofPlacement) ? 0.5 : Number(wizard.selectedPlaceableAnchorX))
                : 0.5;
            previewItem.placeableAnchorY = effectiveAnchorY;
            // Keep the preview sprite's Pixi anchor in sync with the item's
            // logical anchor so that updateDepthBillboardMesh (which reads
            // sprite.anchor for the standard billboard path) produces the
            // same quad geometry as the final placed object.
            if (this.placeObjectPreviewSprite && this.placeObjectPreviewSprite.anchor) {
                this.placeObjectPreviewSprite.anchor.set(
                    previewItem.placeableAnchorX,
                    previewItem.placeableAnchorY
                );
            }
            previewItem.rotationAxis = (useSnapPlacement && !useRoofPlacement) ? "spatial" : rotationAxis;
            previewItem.placementRotation = (useSnapPlacement && !useRoofPlacement)
                ? snapPlacement.snappedRotationDeg
                : effectivePlacementRotation;
            previewItem.mountedSectionId = (
                useSnapPlacement &&
                !useRoofPlacement &&
                Number.isInteger(snapPlacement.mountedSectionId)
            ) ? Number(snapPlacement.mountedSectionId) : null;
            previewItem.mountedWallLineGroupId = (
                useSnapPlacement &&
                !useRoofPlacement &&
                Number.isInteger(snapPlacement.mountedWallLineGroupId)
            ) ? Number(snapPlacement.mountedWallLineGroupId) : null;
            previewItem.mountedWallSectionUnitId = (
                useSnapPlacement &&
                !useRoofPlacement &&
                Number.isInteger(snapPlacement.mountedWallSectionUnitId)
            ) ? Number(snapPlacement.mountedWallSectionUnitId) : null;
            previewItem.mountedWallFacingSign = (
                useSnapPlacement &&
                !useRoofPlacement &&
                Number.isFinite(snapPlacement.mountedWallFacingSign)
            ) ? Number(snapPlacement.mountedWallFacingSign) : null;
            if (
                useSnapPlacement &&
                !useRoofPlacement &&
                Number.isFinite(snapPlacement.wallFaceCenterX) &&
                Number.isFinite(snapPlacement.wallFaceCenterY) &&
                Number.isFinite(snapPlacement.sectionNormalX) &&
                Number.isFinite(snapPlacement.sectionNormalY) &&
                Number.isFinite(snapPlacement.wallThickness) &&
                Number.isFinite(previewItem.mountedWallFacingSign)
            ) {
                const sign = Number(previewItem.mountedWallFacingSign) >= 0 ? 1 : -1;
                const thickness = Math.max(0, Number(snapPlacement.wallThickness));
                const nx = Number(snapPlacement.sectionNormalX);
                const ny = Number(snapPlacement.sectionNormalY);
                const frontX = Number(snapPlacement.wallFaceCenterX);
                const frontY = Number(snapPlacement.wallFaceCenterY);
                const faceEpsilon = 0.01;
                const dirX = nx * sign;
                const dirY = ny * sign;
                const backBaseX = frontX - dirX * thickness;
                const backBaseY = frontY - dirY * thickness;
                previewItem.depthBillboardFaceCenters = {
                    // Nudge both planes slightly away from wall faces to prevent preview z-fighting.
                    front: {
                        x: frontX + dirX * faceEpsilon,
                        y: frontY + dirY * faceEpsilon
                    },
                    back: {
                        x: backBaseX + dirX * faceEpsilon,
                        y: backBaseY + dirY * faceEpsilon
                    }
                };
            } else {
                previewItem.depthBillboardFaceCenters = null;
            }
            previewItem.centerSnapGuide = useSnapPlacement
                && !useRoofPlacement
                ? {
                    centerSnapActive: !!snapPlacement.centerSnapActive,
                    placementCenterX: Number(snapPlacement.placementCenterX),
                    placementCenterY: Number(snapPlacement.placementCenterY),
                    sectionCenterX: Number(snapPlacement.sectionCenterX),
                    sectionCenterY: Number(snapPlacement.sectionCenterY),
                    sectionFacingSign: Number(snapPlacement.sectionFacingSign),
                    sectionNormalX: Number(snapPlacement.sectionNormalX),
                    sectionNormalY: Number(snapPlacement.sectionNormalY),
                    sectionDirX: Number(snapPlacement.sectionDirX),
                    sectionDirY: Number(snapPlacement.sectionDirY),
                    wallFaceCenterX: Number(snapPlacement.wallFaceCenterX),
                    wallFaceCenterY: Number(snapPlacement.wallFaceCenterY),
                    placementHalfWidth: Number(snapPlacement.placementHalfWidth),
                    wallHeight: Number(snapPlacement.wallHeight) || 0,
                    wallThickness: Number(snapPlacement.wallThickness) || 0,
                    sectionFaceQuadScreenPoints: Array.isArray(snapPlacement.sectionFaceQuadScreenPoints)
                        ? snapPlacement.sectionFaceQuadScreenPoints
                        : null,
                    sectionVisiblePolygonsScreen: Array.isArray(snapPlacement.sectionVisiblePolygonsScreen)
                        ? snapPlacement.sectionVisiblePolygonsScreen
                        : null
                }
                : null;
            previewItem.roofHighlightOnly = false;
            previewItem.roofLoopSections = (
                useRoofPlacement &&
                Array.isArray(snapPlacement.wallSections)
            ) ? snapPlacement.wallSections.slice() : null;
            return previewItem;
        }

        renderPlaceObjectCenterSnapGuide(previewItem, ctx = null) {
            const layer = this.layers.ui;
            if (!layer) return;
            if (!this.placeObjectCenterSnapGuideGraphics) {
                this.placeObjectCenterSnapGuideGraphics = new PIXI.Graphics();
                this.placeObjectCenterSnapGuideGraphics.name = "renderingPlaceObjectSnapGuide";
                this.placeObjectCenterSnapGuideGraphics.skipTransform = true;
                this.placeObjectCenterSnapGuideGraphics.interactive = false;
                this.placeObjectCenterSnapGuideGraphics.visible = false;
                layer.addChild(this.placeObjectCenterSnapGuideGraphics);
            } else if (this.placeObjectCenterSnapGuideGraphics.parent !== layer) {
                layer.addChild(this.placeObjectCenterSnapGuideGraphics);
            }
            const g = this.placeObjectCenterSnapGuideGraphics;
            g.clear();
            const guide = previewItem && previewItem.centerSnapGuide ? previewItem.centerSnapGuide : null;
            const roofSections = (
                previewItem &&
                Array.isArray(previewItem.roofLoopSections)
            ) ? previewItem.roofLoopSections : null;
            let drewRoofLoop = false;
            if (Array.isArray(roofSections) && roofSections.length > 0) {
                for (let i = 0; i < roofSections.length; i++) {
                    const section = roofSections[i];
                    if (!section || typeof section.getWallProfile !== "function") continue;
                    const profile = section.getWallProfile();
                    if (!profile) continue;
                    const layerBaseZ = Number.isFinite(previewItem && previewItem._renderLayerBaseZ)
                        ? Number(previewItem._renderLayerBaseZ)
                        : null;
                    const topZ = (typeof global !== "undefined" && global.Roof && typeof global.Roof.getWallSectionTopZForLayer === "function")
                        ? global.Roof.getWallSectionTopZForLayer(section, layerBaseZ)
                        : ((Number.isFinite(section.bottomZ) ? Number(section.bottomZ) : 0) + Math.max(0, Number(section.height) || 0));
                    const topFace = [
                        this.camera.worldToScreen(Number(profile.aLeft.x), Number(profile.aLeft.y), topZ),
                        this.camera.worldToScreen(Number(profile.bLeft.x), Number(profile.bLeft.y), topZ),
                        this.camera.worldToScreen(Number(profile.bRight.x), Number(profile.bRight.y), topZ),
                        this.camera.worldToScreen(Number(profile.aRight.x), Number(profile.aRight.y), topZ)
                    ];
                    if (!topFace.every(pt => Number.isFinite(pt.x) && Number.isFinite(pt.y))) continue;
                    g.lineStyle(2, 0x66c2ff, 0.55);
                    g.beginFill(0x66c2ff, 0.12);
                    g.moveTo(topFace[0].x, topFace[0].y);
                    g.lineTo(topFace[1].x, topFace[1].y);
                    g.lineTo(topFace[2].x, topFace[2].y);
                    g.lineTo(topFace[3].x, topFace[3].y);
                    g.closePath();
                    g.endFill();
                    drewRoofLoop = true;
                }
            }
            if (
                !guide ||
                !Number.isFinite(guide.placementCenterX) ||
                !Number.isFinite(guide.placementCenterY) ||
                !Number.isFinite(guide.sectionCenterX) ||
                !Number.isFinite(guide.sectionCenterY)
            ) {
                g.visible = drewRoofLoop;
                return;
            }

            const placementCenterScreen = this.camera.worldToScreen(guide.placementCenterX, guide.placementCenterY, 0);
            const sectionCenterScreen = this.camera.worldToScreen(guide.sectionCenterX, guide.sectionCenterY, 0);
            const topCenterScreen = {
                x: sectionCenterScreen.x,
                y: sectionCenterScreen.y - (Math.max(0, Number(guide.wallHeight) || 0) * this.camera.viewscale * this.camera.xyratio)
            };
            const visiblePolygons = Array.isArray(guide.sectionVisiblePolygonsScreen)
                ? guide.sectionVisiblePolygonsScreen
                : null;
            if (Array.isArray(visiblePolygons) && visiblePolygons.length > 0) {
                for (let i = 0; i < visiblePolygons.length; i++) {
                    const poly = Array.isArray(visiblePolygons[i])
                        ? visiblePolygons[i].map(pt => ({ x: Number(pt.x), y: Number(pt.y) }))
                        : [];
                    if (poly.length < 3) continue;
                    if (!poly.every(pt => Number.isFinite(pt.x) && Number.isFinite(pt.y))) continue;
                    g.lineStyle(2, 0x4FC3FF, 0.8);
                    g.beginFill(0x4FC3FF, 0.12);
                    g.moveTo(poly[0].x, poly[0].y);
                    for (let p = 1; p < poly.length; p++) {
                        g.lineTo(poly[p].x, poly[p].y);
                    }
                    g.closePath();
                    g.endFill();
                }
            } else if (Array.isArray(guide.sectionFaceQuadScreenPoints) && guide.sectionFaceQuadScreenPoints.length >= 4) {
                const quad = guide.sectionFaceQuadScreenPoints
                    .slice(0, 4)
                    .map(pt => ({ x: Number(pt.x), y: Number(pt.y) }))
                    .filter(pt => Number.isFinite(pt.x) && Number.isFinite(pt.y));
                if (quad.length === 4) {
                    g.lineStyle(2, 0x4FC3FF, 0.8);
                    g.beginFill(0x4FC3FF, 0.12);
                    g.moveTo(quad[0].x, quad[0].y);
                    g.lineTo(quad[1].x, quad[1].y);
                    g.lineTo(quad[2].x, quad[2].y);
                    g.lineTo(quad[3].x, quad[3].y);
                    g.closePath();
                    g.endFill();
                }
            }

            const facingSign = Number.isFinite(guide.sectionFacingSign) ? Number(guide.sectionFacingSign) : 1;
            const insideWorld = {
                x: guide.sectionCenterX - (Number.isFinite(guide.sectionNormalX) ? guide.sectionNormalX : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign,
                y: guide.sectionCenterY - (Number.isFinite(guide.sectionNormalY) ? guide.sectionNormalY : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign
            };
            const insideScreen = this.camera.worldToScreen(insideWorld.x, insideWorld.y, 0);
            const topInsideScreen = {
                x: insideScreen.x,
                y: insideScreen.y - (Math.max(0, Number(guide.wallHeight) || 0) * this.camera.viewscale * this.camera.xyratio)
            };
            if (guide.centerSnapActive) {
                g.lineStyle(2, 0xFF0000, 0.5);
                g.moveTo(placementCenterScreen.x, placementCenterScreen.y);
                g.lineTo(topCenterScreen.x, topCenterScreen.y);
                g.moveTo(topCenterScreen.x, topCenterScreen.y);
                g.lineTo(topInsideScreen.x, topInsideScreen.y);
            }

            if (
                Number.isFinite(guide.wallFaceCenterX) &&
                Number.isFinite(guide.wallFaceCenterY) &&
                Number.isFinite(guide.sectionDirX) &&
                Number.isFinite(guide.sectionDirY) &&
                Number.isFinite(guide.placementHalfWidth)
            ) {
                const hx = guide.sectionDirX * guide.placementHalfWidth;
                const hy = guide.sectionDirY * guide.placementHalfWidth;
                const facingEndA = { x: guide.wallFaceCenterX - hx, y: guide.wallFaceCenterY - hy };
                const facingEndB = { x: guide.wallFaceCenterX + hx, y: guide.wallFaceCenterY + hy };
                const insideEndA = {
                    x: facingEndA.x - (Number.isFinite(guide.sectionNormalX) ? guide.sectionNormalX : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign,
                    y: facingEndA.y - (Number.isFinite(guide.sectionNormalY) ? guide.sectionNormalY : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign
                };
                const insideEndB = {
                    x: facingEndB.x - (Number.isFinite(guide.sectionNormalX) ? guide.sectionNormalX : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign,
                    y: facingEndB.y - (Number.isFinite(guide.sectionNormalY) ? guide.sectionNormalY : 0) * Math.max(0, Number(guide.wallThickness) || 0) * facingSign
                };
                const toTop = (pt) => {
                    const s = this.camera.worldToScreen(pt.x, pt.y, 0);
                    return {
                        x: s.x,
                        y: s.y - (Math.max(0, Number(guide.wallHeight) || 0) * this.camera.viewscale * this.camera.xyratio)
                    };
                };
                const topFacingA = toTop(facingEndA);
                const topInsideA = toTop(insideEndA);
                const topFacingB = toTop(facingEndB);
                const topInsideB = toTop(insideEndB);
                g.lineStyle(2, 0x000000, 0.6);
                g.moveTo(topFacingA.x, topFacingA.y);
                g.lineTo(topInsideA.x, topInsideA.y);
                g.moveTo(topFacingB.x, topFacingB.y);
                g.lineTo(topInsideB.x, topInsideB.y);
            }
            g.visible = true;
            this.promoteInteriorPresentationDisplayObject(g, ctx);
        }

        renderPlaceObjectPreview(ctx) {
            const previewItem = this.buildPlaceObjectPreviewRenderItem(ctx);
            if (!previewItem) {
                this.clearPlaceObjectPreview();
                return;
            }
            if (previewItem.roofHighlightOnly) {
                if (this.placeObjectPreviewItem) {
                    if (this.placeObjectPreviewItem._depthBillboardMesh) {
                        const mesh = this.placeObjectPreviewItem._depthBillboardMesh;
                        mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                            mesh.renderable = false;
                        }
                    }
                    if (this.placeObjectPreviewItem._compositeUnderlayMesh) {
                        const mesh = this.placeObjectPreviewItem._compositeUnderlayMesh;
                        mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                            mesh.renderable = false;
                        }
                    }
                }
                if (this.placeObjectPreviewDisplayObject) {
                    this.placeObjectPreviewDisplayObject.visible = false;
                    if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewDisplayObject, "renderable")) {
                        this.placeObjectPreviewDisplayObject.renderable = false;
                    }
                    this.placeObjectPreviewDisplayObject = null;
                }
                if (this.placeObjectPreviewSprite) {
                    this.placeObjectPreviewSprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewSprite, "renderable")) {
                        this.placeObjectPreviewSprite.renderable = false;
                    }
                }
                this.renderPlaceObjectCenterSnapGuide(previewItem, ctx);
                return;
            }
            let displayObj = null;
            // Keep preview sprite dimensions in world scale before any depth-mesh extraction.
            this.applySpriteTransform(previewItem);
            if (
                typeof previewItem.updateDepthBillboardMesh === "function"
            ) {
                const isWallMountedPreview = !!(
                    previewItem.centerSnapGuide &&
                    previewItem.depthBillboardFaceCenters &&
                    (previewItem.category === "doors" || previewItem.category === "windows" || previewItem.type === "door" || previewItem.type === "window")
                );
                const mesh = previewItem.updateDepthBillboardMesh(
                    ctx,
                    this.camera,
                    {
                        alphaCutoff: TREE_ALPHA_CUTOFF,
                        drawOnlyMountedWallSide: isWallMountedPreview,
                        forceMountedWallSide: isWallMountedPreview ? "front" : null
                    }
                );
                if (mesh) {
                    if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                        mesh.renderable = true;
                    }
                    if (Number.isFinite(previewItem.previewAlpha)) {
                        mesh.alpha = previewItem.previewAlpha;
                    }
                    displayObj = mesh;
                }
            }
            if (!displayObj) {
                displayObj = previewItem.pixiSprite || this.placeObjectPreviewSprite;
            }
            if (!displayObj) {
                this.clearPlaceObjectPreview();
                return;
            }
            const interiorPresentationActive = this.isBuildingInteriorPresentationActive(ctx);
            const container = interiorPresentationActive
                ? this.layers.ui
                : ((displayObj instanceof PIXI.Mesh)
                    ? this.layers.depthObjects
                    : this.layers.objects3d);
            if (!container) {
                this.clearPlaceObjectPreview();
                return;
            }
            const depthMesh = previewItem._depthBillboardMesh;
            if (depthMesh && depthMesh !== displayObj) {
                depthMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(depthMesh, "renderable")) {
                    depthMesh.renderable = false;
                }
            }
            if (displayObj.parent !== container) {
                container.addChild(displayObj);
            }
            displayObj.visible = true;
            if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                displayObj.renderable = true;
            }
            if (Number.isFinite(previewItem.previewAlpha)) {
                displayObj.alpha = previewItem.previewAlpha;
            }
            if (Number.isFinite(displayObj.tint)) {
                displayObj.tint = 0xFFFFFF;
            }
            if (interiorPresentationActive) {
                displayObj.zIndex = BUILDING_INTERIOR_FOREGROUND_Z;
                container.sortableChildren = true;
                if (Object.prototype.hasOwnProperty.call(container, "sortDirty")) container.sortDirty = true;
            }

            const underlayMesh = previewItem._compositeUnderlayMesh;
            const shouldShowUnderlay = !!(
                underlayMesh &&
                !underlayMesh.destroyed &&
                previewItem._compositeUnderlayShouldRender
            );
            if (shouldShowUnderlay) {
                if (underlayMesh.parent !== container) {
                    container.addChild(underlayMesh);
                }
                underlayMesh.visible = true;
                if (Object.prototype.hasOwnProperty.call(underlayMesh, "renderable")) {
                    underlayMesh.renderable = true;
                }
                if (Number.isFinite(previewItem.previewAlpha)) {
                    underlayMesh.alpha = previewItem.previewAlpha;
                }
                if (Number.isFinite(underlayMesh.tint)) {
                    underlayMesh.tint = 0xFFFFFF;
                }
            } else if (underlayMesh && !underlayMesh.destroyed) {
                underlayMesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(underlayMesh, "renderable")) {
                    underlayMesh.renderable = false;
                }
            }

            const topIndex = container.children.length - 1;
            if (topIndex >= 0) {
                const currentIndex = container.getChildIndex(displayObj);
                if (currentIndex !== topIndex) {
                    container.setChildIndex(displayObj, topIndex);
                }
                if (underlayMesh && underlayMesh.parent === container) {
                    const uIndex = container.getChildIndex(underlayMesh);
                    const newUIndex = Math.max(0, container.children.length - 2);
                    if (uIndex !== newUIndex) {
                        container.setChildIndex(underlayMesh, newUIndex);
                    }
                }
            }
            if (this.placeObjectPreviewDisplayObject && this.placeObjectPreviewDisplayObject !== displayObj) {
                this.placeObjectPreviewDisplayObject.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.placeObjectPreviewDisplayObject, "renderable")) {
                    this.placeObjectPreviewDisplayObject.renderable = false;
                }
            }
            this.placeObjectPreviewDisplayObject = displayObj;
            this.renderPlaceObjectCenterSnapGuide(previewItem, ctx);
        }

        renderBuildingPlacementPreview(ctx) {
            const layer = this.layers && this.layers.ui ? this.layers.ui : null;
            if (!layer) return;
            if (!this.buildingPlacementPreviewGraphics) {
                this.buildingPlacementPreviewGraphics = new PIXI.Graphics();
                this.buildingPlacementPreviewGraphics.name = "renderingBuildingPlacementPreview";
                this.buildingPlacementPreviewGraphics.skipTransform = true;
                this.buildingPlacementPreviewGraphics.interactive = false;
                this.buildingPlacementPreviewGraphics.visible = false;
                layer.addChild(this.buildingPlacementPreviewGraphics);
            } else if (this.buildingPlacementPreviewGraphics.parent !== layer) {
                layer.addChild(this.buildingPlacementPreviewGraphics);
            }

            const g = this.buildingPlacementPreviewGraphics;
            g.clear();
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            if (wizard && this.getWizardVisualLayerIndex(wizard, 0) < 0) {
                g.visible = false;
                if (Object.prototype.hasOwnProperty.call(g, "renderable")) {
                    g.renderable = false;
                }
                return;
            }
            const mapRef = (ctx && ctx.map) || (wizard && wizard.map) || global.map || null;
            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            if (!mapRef) {
                g.visible = false;
                return;
            }
            const spaceHeld = !!(
                (ctx && ctx.spaceHeld === true) ||
                (typeof keysPressed !== "undefined" && keysPressed && keysPressed[" "]) ||
                (global.keysPressed && global.keysPressed[" "])
            );

            const drawPolygon = (polygon, baseZ, lineColor, fillColor, fillAlpha, lineAlpha = 0.95) => {
                if (!Array.isArray(polygon) || polygon.length < 3) return false;
                const first = this.camera.worldToScreen(Number(polygon[0].x), Number(polygon[0].y), baseZ);
                if (!first || !Number.isFinite(first.x) || !Number.isFinite(first.y)) return false;
                g.lineStyle(2, lineColor, lineAlpha);
                g.beginFill(fillColor, fillAlpha);
                g.moveTo(first.x, first.y);
                for (let i = 1; i < polygon.length; i++) {
                    const sp = this.camera.worldToScreen(Number(polygon[i].x), Number(polygon[i].y), baseZ);
                    if (sp && Number.isFinite(sp.x) && Number.isFinite(sp.y)) {
                        g.lineTo(sp.x, sp.y);
                    }
                }
                g.closePath();
                g.endFill();
                return true;
            };

            let drawn = 0;
            const placements = typeof mapRef.getPrototypeBuildingPlacements === "function"
                ? mapRef.getPrototypeBuildingPlacements()
                : [];
            for (let i = 0; i < placements.length; i++) {
                const placement = placements[i];
                const exteriorCache = placement && placement.id && typeof mapRef.getPrototypeBuildingExteriorBitmap === "function"
                    ? mapRef.getPrototypeBuildingExteriorBitmap(placement.id)
                    : null;
                if (exteriorCache && exteriorCache.status === "ready") continue;
                const isError = exteriorCache && exteriorCache.status === "error";
                const polygons = Array.isArray(placement && placement.footprintPolygons)
                    ? placement.footprintPolygons
                    : [];
                for (let p = 0; p < polygons.length; p++) {
                    if (drawPolygon(
                        polygons[p],
                        0,
                        isError ? 0xff4444 : 0x66aaff,
                        isError ? 0xff4444 : 0x66aaff,
                        isError ? 0.14 : 0.06,
                        isError ? 0.9 : 0.45
                    )) drawn += 1;
                }
            }

            const previewActive = !!(
                wizard &&
                wizard.currentSpell === "placebuilding" &&
                spellSystemRef &&
                typeof spellSystemRef.getBuildingPlacementPreview === "function"
            );
            if (previewActive) {
                const mousePosRef = this.getMousePosRef(ctx);
                const preview = spellSystemRef.getBuildingPlacementPreview(wizard, {
                    spaceHeld,
                    forceActive: spaceHeld,
                    mouseWorldPos: mousePosRef && Number.isFinite(mousePosRef.worldX) && Number.isFinite(mousePosRef.worldY)
                        ? {
                            x: Number(mousePosRef.worldX),
                            y: Number(mousePosRef.worldY),
                            screenX: Number(mousePosRef.screenX),
                            screenY: Number(mousePosRef.screenY)
                        }
                        : null
                });
                if (preview && Array.isArray(preview.footprintPolygons)) {
                    const hasOverlap = Array.isArray(preview.overlappedSectionKeys) && preview.overlappedSectionKeys.length > 0;
                    const lineColor = hasOverlap ? 0x66ffaa : 0xffcc44;
                    for (let p = 0; p < preview.footprintPolygons.length; p++) {
                        if (drawPolygon(preview.footprintPolygons[p], 0, lineColor, lineColor, 0.18, 0.95)) drawn += 1;
                    }
                }
            }

            g.visible = drawn > 0;
            if (Object.prototype.hasOwnProperty.call(g, "renderable")) {
                g.renderable = g.visible;
            }
            if (g.visible) {
                this.promoteInteriorPresentationDisplayObject(g, ctx);
            }
        }

        ensurePrototypeBuildingExteriorSprite(cache) {
            if (!cache || !cache.id || !cache.texture) return null;
            if (!this.prototypeBuildingExteriorSpritesById) {
                this.prototypeBuildingExteriorSpritesById = new Map();
            }
            let sprite = this.prototypeBuildingExteriorSpritesById.get(cache.id);
            if (!sprite) {
                sprite = new PIXI.Sprite(cache.texture);
                sprite.name = `prototypeBuildingExterior:${cache.id}`;
                sprite.interactive = false;
                this.prototypeBuildingExteriorSpritesById.set(cache.id, sprite);
            } else if (sprite.texture !== cache.texture) {
                sprite.texture = cache.texture;
            }
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(
                    Number.isFinite(cache.anchorX) ? cache.anchorX : 0.5,
                    Number.isFinite(cache.anchorY) ? cache.anchorY : 1
                );
            }
            return sprite;
        }

        ensurePrototypeBuildingExteriorProxy(cache, sprite) {
            if (!cache || !cache.id || !sprite) return null;
            if (!this.prototypeBuildingExteriorProxiesById) {
                this.prototypeBuildingExteriorProxiesById = new Map();
            }
            let proxy = this.prototypeBuildingExteriorProxiesById.get(cache.id);
            if (!proxy) {
                proxy = {
                    type: "prototypeBuildingExterior",
                    category: "",
                    rotationAxis: "billboard",
                    placementRotation: 0,
                    pixiSprite: sprite,
                    x: 0,
                    y: 0,
                    z: 0,
                    width: 1,
                    height: 1,
                    depthBillboardUseVerticalAnchorY: true,
                    gone: false,
                    vanishing: false,
                    visible: true,
                    tint: 0xFFFFFF,
                    _renderLayerBaseZ: 0,
                    _renderDepthBias: 0,
                    _depthBillboardMesh: null,
                    _depthBillboardWorldPositions: null,
                    _depthBillboardLastSignature: "",
                    _depthBillboardLastUvSignature: "",
                    _depthBillboardMeshMode: ""
                };
                const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                    ? global.StaticObject.prototype
                    : null;
                if (!staticProto || typeof staticProto.updateDepthBillboardMesh !== "function") {
                    throw new Error("prototype building exterior requires StaticObject depth billboard rendering");
                }
                if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                    proxy.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                }
                if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                    proxy.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                }
                proxy.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
                this.prototypeBuildingExteriorProxiesById.set(cache.id, proxy);
            }
            proxy.pixiSprite = sprite;
            return proxy;
        }

        ensurePrototypeBuildingExteriorDoorSprite(item) {
            if (!item || typeof item.id !== "string" || item.id.length === 0) {
                throw new Error("prototype building exterior door render requires a door id");
            }
            const texturePath = typeof item.texturePath === "string" && item.texturePath.length > 0
                ? item.texturePath
                : "";
            if (!texturePath) {
                throw new Error(`prototype building exterior door ${item.id} is missing its texture`);
            }
            if (typeof PIXI === "undefined" || !PIXI.Sprite || !PIXI.Texture || typeof PIXI.Texture.from !== "function") {
                throw new Error("prototype building exterior door rendering requires Pixi texture support");
            }
            if (!this.prototypeBuildingExteriorDoorSpritesById) {
                this.prototypeBuildingExteriorDoorSpritesById = new Map();
            }
            let entry = this.prototypeBuildingExteriorDoorSpritesById.get(item.id);
            if (!entry) {
                entry = {
                    texturePath: "",
                    sprite: new PIXI.Sprite(PIXI.Texture.from(texturePath))
                };
                entry.sprite.name = `prototypeBuildingExteriorDoor:${item.id}`;
                entry.sprite.interactive = false;
                this.prototypeBuildingExteriorDoorSpritesById.set(item.id, entry);
            } else if (entry.texturePath !== texturePath) {
                entry.sprite.texture = PIXI.Texture.from(texturePath);
            }
            entry.texturePath = texturePath;
            const sprite = entry.sprite;
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(
                    Number.isFinite(item.placeableAnchorX) ? Number(item.placeableAnchorX) : 0.5,
                    Number.isFinite(item.placeableAnchorY) ? Number(item.placeableAnchorY) : 1
                );
            }
            sprite.alpha = 1;
            sprite.tint = 0xFFFFFF;
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
            return sprite;
        }

        ensurePrototypeBuildingExteriorDoorProxy(item, sprite) {
            if (!item || typeof item.id !== "string" || item.id.length === 0 || !sprite) return null;
            if (!this.prototypeBuildingExteriorDoorProxiesById) {
                this.prototypeBuildingExteriorDoorProxiesById = new Map();
            }
            let proxy = this.prototypeBuildingExteriorDoorProxiesById.get(item.id);
            if (!proxy) {
                proxy = {
                    type: "door",
                    category: "doors",
                    rotationAxis: "spatial",
                    pixiSprite: sprite,
                    x: 0,
                    y: 0,
                    z: 0,
                    width: 1,
                    height: 1,
                    gone: false,
                    vanishing: false,
                    visible: true,
                    tint: 0xFFFFFF,
                    _renderLayerBaseZ: 0,
                    _renderDepthBias: 0,
                    _depthBillboardMesh: null,
                    _depthBillboardWorldPositions: null,
                    _depthBillboardLastSignature: "",
                    _depthBillboardLastUvSignature: "",
                    _depthBillboardMeshMode: ""
                };
                const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                    ? global.StaticObject.prototype
                    : null;
                if (!staticProto || typeof staticProto.updateDepthBillboardMesh !== "function") {
                    throw new Error("prototype building exterior door requires StaticObject depth billboard rendering");
                }
                if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                    proxy.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                }
                if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                    proxy.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                }
                if (typeof staticProto._ensureCompositeUnderlayMesh === "function") {
                    proxy._ensureCompositeUnderlayMesh = staticProto._ensureCompositeUnderlayMesh;
                }
                if (typeof staticProto._destroyCompositeUnderlayMesh === "function") {
                    proxy._destroyCompositeUnderlayMesh = staticProto._destroyCompositeUnderlayMesh;
                }
                proxy.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
                this.prototypeBuildingExteriorDoorProxiesById.set(item.id, proxy);
            }
            proxy.pixiSprite = sprite;
            return proxy;
        }

        isLegacySplitDoorCompositeLayers(layers) {
            if (!Array.isArray(layers) || layers.length !== 2) return false;
            const first = layers[0] && Array.isArray(layers[0].uRegion) ? layers[0].uRegion : null;
            const second = layers[1] && Array.isArray(layers[1].uRegion) ? layers[1].uRegion : null;
            if (!first || !second || first.length < 2 || second.length < 2) return false;
            return (
                Math.abs(Number(first[0]) - 0) < 1e-6 &&
                Math.abs(Number(first[1]) - 0.5) < 1e-6 &&
                Math.abs(Number(second[0]) - 0.5) < 1e-6 &&
                Math.abs(Number(second[1]) - 1) < 1e-6
            );
        }

        getRenderableDoorCompositeLayers(item) {
            const layers = Array.isArray(item && item.compositeLayers) ? item.compositeLayers : null;
            if (!layers) return null;
            return this.isLegacySplitDoorCompositeLayers(layers) ? null : layers;
        }

        configurePrototypeBuildingExteriorDoorProxy(proxy, item, entry, trigger, ctx = null) {
            if (!proxy || !item) return null;
            const level = Number.isFinite(entry && entry.level)
                ? this.getLayerIndexFromValue(entry.level, 0)
                : this.getBuildingInteriorEntryLayer(item, entry, this.getLayerIndexFromValue(trigger && trigger.building && trigger.building.minLevel, 0));
            const sprite = proxy.pixiSprite;
            proxy.id = `${String(item.id)}:fullOpacityExteriorDoor`;
            proxy.sourceDoorItem = item;
            proxy.map = (ctx && ctx.map) || global.map || null;
            proxy.texturePath = item.texturePath;
            proxy.x = Number.isFinite(item.x) ? Number(item.x) : 0;
            proxy.y = Number.isFinite(item.y) ? Number(item.y) : 0;
            proxy.z = Number.isFinite(item.z) ? Number(item.z) : 0;
            proxy.width = Math.max(0.01, Number.isFinite(item.width) ? Number(item.width) : 1);
            proxy.height = Math.max(0.01, Number.isFinite(item.height) ? Number(item.height) : 1);
            proxy.rotationAxis = "spatial";
            proxy.placementRotation = Number.isFinite(item.placementRotation) ? Number(item.placementRotation) : 0;
            proxy.rotation = proxy.placementRotation;
            proxy.placeableAnchorX = Number.isFinite(item.placeableAnchorX) ? Number(item.placeableAnchorX) : 0.5;
            proxy.placeableAnchorY = Number.isFinite(item.placeableAnchorY) ? Number(item.placeableAnchorY) : 1;
            proxy.compositeLayers = this.getRenderableDoorCompositeLayers(item);
            proxy.isOpen = item.isOpen === true;
            proxy._doorLockedOpen = item._doorLockedOpen === true;
            proxy.isFallenDoorEffect = false;
            proxy.depthBillboardFaceCenters = item.depthBillboardFaceCenters || null;
            proxy._prototypeMountedWallSection = item._prototypeMountedWallSection || null;
            proxy.mountedWallSectionUnitId = Number.isInteger(item.mountedWallSectionUnitId)
                ? Number(item.mountedWallSectionUnitId)
                : 0;
            proxy.mountedSectionId = Number.isInteger(item.mountedSectionId) ? Number(item.mountedSectionId) : null;
            proxy.mountedWallLineGroupId = Number.isInteger(item.mountedWallLineGroupId) ? Number(item.mountedWallLineGroupId) : null;
            proxy.mountedWallFacingSign = Number.isFinite(item.mountedWallFacingSign) ? Number(item.mountedWallFacingSign) : null;
            proxy.traversalLayer = level;
            proxy.level = level;
            proxy._renderLayerIndex = level;
            proxy._renderLayerBaseZ = this.getLayerBaseZForObject(item, level);
            proxy._renderLayerFadeAlpha = 1;
            proxy._renderLayerAlpha = 1;
            proxy._renderDepthBias = Number.isFinite(item.renderDepthOffset) ? Number(item.renderDepthOffset) : 0;
            if (sprite) {
                sprite.alpha = 1;
                sprite.tint = 0xFFFFFF;
            }
            return proxy;
        }

        getPrototypeBuildingPlacementGeometricCenter(placement) {
            const polygons = Array.isArray(placement && placement.footprintPolygons)
                ? placement.footprintPolygons
                : [];
            if (polygons.length === 0) {
                throw new Error(`prototype building placement ${placement && placement.id || "(unknown)"} is missing footprint geometry for picker target`);
            }
            let weightedX = 0;
            let weightedY = 0;
            let totalArea = 0;
            let minX = Infinity;
            let minY = Infinity;
            let maxX = -Infinity;
            let maxY = -Infinity;
            let finitePoints = 0;
            for (let p = 0; p < polygons.length; p++) {
                const polygon = polygons[p];
                if (!Array.isArray(polygon) || polygon.length < 3) continue;
                let twiceArea = 0;
                let centroidXTimesArea6 = 0;
                let centroidYTimesArea6 = 0;
                for (let i = 0; i < polygon.length; i++) {
                    const a = polygon[i];
                    const b = polygon[(i + 1) % polygon.length];
                    const ax = Number(a && a.x);
                    const ay = Number(a && a.y);
                    const bx = Number(b && b.x);
                    const by = Number(b && b.y);
                    if (!Number.isFinite(ax) || !Number.isFinite(ay) || !Number.isFinite(bx) || !Number.isFinite(by)) continue;
                    minX = Math.min(minX, ax, bx);
                    minY = Math.min(minY, ay, by);
                    maxX = Math.max(maxX, ax, bx);
                    maxY = Math.max(maxY, ay, by);
                    finitePoints += 1;
                    const cross = (ax * by) - (bx * ay);
                    twiceArea += cross;
                    centroidXTimesArea6 += (ax + bx) * cross;
                    centroidYTimesArea6 += (ay + by) * cross;
                }
                const area = twiceArea * 0.5;
                if (Math.abs(area) > 0.000001) {
                    weightedX += (centroidXTimesArea6 / (6 * area)) * Math.abs(area);
                    weightedY += (centroidYTimesArea6 / (6 * area)) * Math.abs(area);
                    totalArea += Math.abs(area);
                }
            }
            if (totalArea > 0) {
                return { x: weightedX / totalArea, y: weightedY / totalArea };
            }
            if (finitePoints > 0 && Number.isFinite(minX) && Number.isFinite(minY) && Number.isFinite(maxX) && Number.isFinite(maxY)) {
                return { x: (minX + maxX) * 0.5, y: (minY + maxY) * 0.5 };
            }
            throw new Error(`prototype building placement ${placement && placement.id || "(unknown)"} footprint geometry has no finite picker target center`);
        }

        ensurePrototypeBuildingPlacementPickTarget(placement, cache, proxy, mapRef) {
            if (!placement || !placement.id || !proxy) return null;
            if (!this.prototypeBuildingExteriorPickTargetsById) {
                this.prototypeBuildingExteriorPickTargetsById = new Map();
            }
            let target = this.prototypeBuildingExteriorPickTargetsById.get(placement.id);
            if (!target) {
                target = {
                    type: "prototypeBuildingPlacement",
                    id: placement.id,
                    buildingPlacementId: placement.id,
                    buildingSaveName: placement.buildingSaveName,
                    category: "buildings",
                    rotationAxis: "ground",
                    gone: false,
                    vanishing: false,
                    visible: true,
                    isOnFire: false,
                    flamability: 1,
                    removeFromGame() {
                        const targetMap = this.map || null;
                        if (!targetMap || typeof targetMap.removePrototypeBuildingPlacement !== "function") {
                            throw new Error(`prototype building placement ${this.buildingPlacementId || this.id} cannot be removed without map.removePrototypeBuildingPlacement`);
                        }
                        const removed = targetMap.removePrototypeBuildingPlacement(this.buildingPlacementId || this.id);
                        if (!removed) {
                            throw new Error(`prototype building placement ${this.buildingPlacementId || this.id} was not removed`);
                        }
                        this.gone = true;
                        return true;
                    },
                    ignite() {
                        this.isOnFire = true;
                        return true;
                    }
                };
                this.prototypeBuildingExteriorPickTargetsById.set(placement.id, target);
            }
            const transform = placement.transform || {};
            const center = this.getPrototypeBuildingPlacementGeometricCenter(placement);
            target.id = placement.id;
            target.buildingPlacementId = placement.id;
            target.buildingSaveName = placement.buildingSaveName;
            target.map = mapRef || null;
            target.placement = placement;
            target.x = center.x;
            target.y = center.y;
            target.z = 0;
            target.rotation = Number(transform.rotation) || 0;
            target.placementRotation = target.rotation;
            target.width = Number.isFinite(proxy.width) ? Number(proxy.width) : 1;
            target.height = Number.isFinite(proxy.height) ? Number(proxy.height) : 1;
            target.pixiSprite = proxy.pixiSprite || null;
            target._renderingDisplayObject = proxy._depthBillboardMesh || null;
            target._renderingDepthMesh = proxy._depthBillboardMesh || null;
            target._depthBillboardWorldPositions = proxy._depthBillboardWorldPositions || null;
            target._renderLayerBaseZ = 0;
            target.spellTargetPoint = null;
            target.gone = false;
            target.visible = true;
            return target;
        }

        ensurePrototypeBuildingExteriorDepthMappingSupport(ctx) {
            if (this.prototypeBuildingExteriorDepthMappingSupported === true) return true;
            const renderer = (ctx && ctx.app && ctx.app.renderer) || (global.app && global.app.renderer) || null;
            const gl = renderer && renderer.gl ? renderer.gl : null;
            if (!gl) {
                throw new Error("prototype building exterior depth mapping requires a WebGL renderer");
            }
            const staticCtor = global.StaticObject || null;
            if (!staticCtor || typeof staticCtor.isWebgl2Renderer !== "function" || !staticCtor.isWebgl2Renderer(renderer)) {
                throw new Error("prototype building exterior depth mapping requires WebGL2 fragment depth");
            }
            this.prototypeBuildingExteriorDepthMappingSupported = true;
            return true;
        }

        hideUnusedPrototypeBuildingExteriors(activeIds) {
            const sprites = this.prototypeBuildingExteriorSpritesById;
            if (sprites instanceof Map) {
                sprites.forEach((sprite, id) => {
                    if (activeIds && activeIds.has(id)) return;
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
                });
            }
            const proxies = this.prototypeBuildingExteriorProxiesById;
            if (proxies instanceof Map) {
                proxies.forEach((proxy, id) => {
                    if (activeIds && activeIds.has(id)) return;
                    const mesh = proxy && proxy._depthBillboardMesh;
                    if (mesh) {
                        mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) mesh.renderable = false;
                    }
                });
            }
            const pickTargets = this.prototypeBuildingExteriorPickTargetsById;
            if (pickTargets instanceof Map) {
                pickTargets.forEach((target, id) => {
                    if (activeIds && activeIds.has(id)) return;
                    if (target) target.visible = false;
                });
            }
        }

        ensurePrototypeBuildingInteriorSprite(cache) {
            if (!cache || !cache.id || !cache.texture) return null;
            if (!this.prototypeBuildingInteriorSpritesById) {
                this.prototypeBuildingInteriorSpritesById = new Map();
            }
            let sprite = this.prototypeBuildingInteriorSpritesById.get(cache.id);
            if (!sprite) {
                sprite = new PIXI.Sprite(cache.texture);
                sprite.name = `prototypeBuildingInterior:${cache.id}`;
                sprite.interactive = false;
                this.prototypeBuildingInteriorSpritesById.set(cache.id, sprite);
            } else if (sprite.texture !== cache.texture) {
                sprite.texture = cache.texture;
            }
            if (sprite.anchor && typeof sprite.anchor.set === "function") {
                sprite.anchor.set(
                    Number.isFinite(cache.anchorX) ? cache.anchorX : 0.5,
                    Number.isFinite(cache.anchorY) ? cache.anchorY : 1
                );
            }
            return sprite;
        }

        ensurePrototypeBuildingInteriorProxy(cache, sprite) {
            if (!cache || !cache.id || !sprite) return null;
            if (!this.prototypeBuildingInteriorProxiesById) {
                this.prototypeBuildingInteriorProxiesById = new Map();
            }
            let proxy = this.prototypeBuildingInteriorProxiesById.get(cache.id);
            if (!proxy) {
                proxy = {
                    type: "prototypeBuildingInterior",
                    category: "",
                    rotationAxis: "ground",
                    placementRotation: 0,
                    pixiSprite: sprite,
                    x: 0,
                    y: 0,
                    z: 0,
                    width: 1,
                    height: 1,
                    depthBillboardUseVerticalAnchorY: true,
                    gone: false,
                    vanishing: false,
                    visible: true,
                    tint: 0xFFFFFF,
                    _renderLayerBaseZ: 0,
                    _renderDepthBias: 0,
                    _depthBillboardMesh: null,
                    _depthBillboardWorldPositions: null,
                    _depthBillboardLastSignature: "",
                    _depthBillboardLastUvSignature: "",
                    _depthBillboardMeshMode: ""
                };
                const staticProto = (typeof global.StaticObject === "function" && global.StaticObject.prototype)
                    ? global.StaticObject.prototype
                    : null;
                if (!staticProto || typeof staticProto.updateDepthBillboardMesh !== "function") {
                    throw new Error("prototype building interior requires StaticObject depth billboard rendering");
                }
                if (typeof staticProto.ensureDepthBillboardMesh === "function") {
                    proxy.ensureDepthBillboardMesh = staticProto.ensureDepthBillboardMesh;
                }
                if (typeof staticProto.updateDepthBillboardUvsForTexture === "function") {
                    proxy.updateDepthBillboardUvsForTexture = staticProto.updateDepthBillboardUvsForTexture;
                }
                proxy.updateDepthBillboardMesh = staticProto.updateDepthBillboardMesh;
                this.prototypeBuildingInteriorProxiesById.set(cache.id, proxy);
            }
            proxy.pixiSprite = sprite;
            return proxy;
        }

        hideUnusedPrototypeBuildingInteriors(activeIds) {
            const sprites = this.prototypeBuildingInteriorSpritesById;
            if (sprites instanceof Map) {
                sprites.forEach((sprite, id) => {
                    if (activeIds && activeIds.has(id)) return;
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
                });
            }
            const proxies = this.prototypeBuildingInteriorProxiesById;
            if (proxies instanceof Map) {
                proxies.forEach((proxy, id) => {
                    if (activeIds && activeIds.has(id)) return;
                    const mesh = proxy && proxy._depthBillboardMesh;
                    if (mesh) {
                        mesh.visible = false;
                        if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) mesh.renderable = false;
                    }
                });
            }
        }

        getPrototypeBuildingInteriorSourceFloorId(trigger, options = null) {
            const required = !(options && options.required === false);
            const placementId = typeof (trigger && trigger.buildingId) === "string" ? trigger.buildingId : "";
            const region = trigger && trigger.activeInteriorRegion;
            const fragment = region && region.fragment ? region.fragment : null;
            if (fragment && typeof fragment._prototypeBuildingSourceFragmentId === "string" && fragment._prototypeBuildingSourceFragmentId.length > 0) {
                return fragment._prototypeBuildingSourceFragmentId;
            }
            if (region && typeof region._prototypeBuildingSourceFragmentId === "string" && region._prototypeBuildingSourceFragmentId.length > 0) {
                return region._prototypeBuildingSourceFragmentId;
            }
            const fragmentId = typeof (region && region.fragmentId) === "string" ? region.fragmentId : "";
            const prefix = `${placementId}:floor:`;
            if (placementId && fragmentId.startsWith(prefix) && fragmentId.length > prefix.length) {
                return fragmentId.slice(prefix.length);
            }
            if (!required) return "";
            throw new Error(`prototype building interior trigger ${placementId || "(unknown)"} is missing its source floor id`);
        }

        isPrototypeBuildingInteriorBitmapSourceFloorId(floorId) {
            const text = String(floorId || "").trim();
            return !!(
                text &&
                !text.startsWith("groundFootprint:") &&
                !text.startsWith("fragment:")
            );
        }

        ensurePrototypeBuildingInteriorFloorSnapshotResources(ctx = null) {
            if (
                typeof PIXI === "undefined" ||
                !PIXI.RenderTexture ||
                !PIXI.Sprite ||
                !PIXI.Texture
            ) {
                const appRef = (ctx && ctx.app) || (typeof app !== "undefined" ? app : (global.app || null));
                if (appRef && appRef.renderer) {
                    throw new Error("prototype building interior floor snapshot requires Pixi render texture support");
                }
                return null;
            }
            const appRef = (ctx && ctx.app) || (typeof app !== "undefined" ? app : (global.app || null));
            const renderer = appRef && appRef.renderer ? appRef.renderer : null;
            if (!renderer) return null;
            const size = this.getRendererScreenSize(ctx);
            const width = size.width;
            const height = size.height;
            const sizeKey = `${width}x${height}`;
            if (
                !this.prototypeBuildingInteriorFloorSnapshotRenderTexture ||
                !this.prototypeBuildingInteriorFloorSnapshotSize ||
                this.prototypeBuildingInteriorFloorSnapshotSize.key !== sizeKey
            ) {
                if (
                    this.prototypeBuildingInteriorFloorSnapshotRenderTexture &&
                    typeof this.prototypeBuildingInteriorFloorSnapshotRenderTexture.destroy === "function"
                ) {
                    this.prototypeBuildingInteriorFloorSnapshotRenderTexture.destroy(true);
                }
                this.prototypeBuildingInteriorFloorSnapshotRenderTexture = PIXI.RenderTexture.create({
                    width,
                    height
                });
                this.prototypeBuildingInteriorFloorSnapshotSize = { key: sizeKey, width, height };
            }
            this.ensureRenderTextureDepthAttachment(
                this.prototypeBuildingInteriorFloorSnapshotRenderTexture,
                "prototype building interior floor snapshot texture"
            );
            if (!this.prototypeBuildingInteriorFloorSnapshotSprite) {
                this.prototypeBuildingInteriorFloorSnapshotSprite = new PIXI.Sprite(this.prototypeBuildingInteriorFloorSnapshotRenderTexture);
                this.prototypeBuildingInteriorFloorSnapshotSprite.name = "prototypeBuildingInteriorFloorSnapshot";
                this.prototypeBuildingInteriorFloorSnapshotSprite.interactive = false;
                this.prototypeBuildingInteriorFloorSnapshotSprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.prototypeBuildingInteriorFloorSnapshotSprite, "renderable")) {
                    this.prototypeBuildingInteriorFloorSnapshotSprite.renderable = false;
                }
            } else if (this.prototypeBuildingInteriorFloorSnapshotSprite.texture !== this.prototypeBuildingInteriorFloorSnapshotRenderTexture) {
                this.prototypeBuildingInteriorFloorSnapshotSprite.texture = this.prototypeBuildingInteriorFloorSnapshotRenderTexture;
            }
            this.applyLayerTransitionSnapshotSpriteTransform(
                this.prototypeBuildingInteriorFloorSnapshotSprite,
                width,
                height
            );
            if (PIXI.BLEND_MODES) {
                this.prototypeBuildingInteriorFloorSnapshotSprite.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            return {
                renderer,
                texture: this.prototypeBuildingInteriorFloorSnapshotRenderTexture,
                sprite: this.prototypeBuildingInteriorFloorSnapshotSprite,
                width,
                height
            };
        }

        getPrototypeBuildingInteriorFloorSnapshotSignature(ctx, placement, transition) {
            const size = this.getRendererScreenSize(ctx);
            return [
                String(placement && placement.id || ""),
                String(transition && transition.fromSourceFloorId || ""),
                String(transition && transition.toSourceFloorId || ""),
                String(Number.isFinite(transition && transition.startedAtMs) ? Number(transition.startedAtMs) : ""),
                `${size.width}x${size.height}`
            ].join("|");
        }

        withOnlyDisplayObjectVisibleForSnapshot(root, keepDisplayObject, callback) {
            if (!root || !keepDisplayObject || typeof callback !== "function") return null;
            const keepAncestors = new Set();
            let cursor = keepDisplayObject;
            while (cursor) {
                keepAncestors.add(cursor);
                cursor = cursor.parent || null;
            }
            const hidden = [];
            const visit = (displayObj) => {
                if (!displayObj || displayObj === root) {
                    const children = Array.isArray(displayObj && displayObj.children) ? displayObj.children : [];
                    for (let i = 0; i < children.length; i++) visit(children[i]);
                    return;
                }
                if (!keepAncestors.has(displayObj)) {
                    hidden.push({
                        displayObj,
                        visible: displayObj.visible,
                        renderable: displayObj.renderable
                    });
                    displayObj.visible = false;
                    if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                        displayObj.renderable = false;
                    }
                    return;
                }
                const children = Array.isArray(displayObj.children) ? displayObj.children : [];
                for (let i = 0; i < children.length; i++) visit(children[i]);
            };
            visit(root);
            try {
                return callback();
            } finally {
                for (let i = hidden.length - 1; i >= 0; i--) {
                    const entry = hidden[i];
                    if (!entry || !entry.displayObj) continue;
                    entry.displayObj.visible = entry.visible;
                    if (Object.prototype.hasOwnProperty.call(entry.displayObj, "renderable")) {
                        entry.displayObj.renderable = entry.renderable;
                    }
                }
            }
        }

        capturePrototypeBuildingInteriorFloorSnapshot(ctx, placement, cache, transition) {
            if (!placement || !cache || cache.status !== "ready") return false;
            if (!transition) return false;
            const signature = this.getPrototypeBuildingInteriorFloorSnapshotSignature(ctx, placement, transition);
            const existing = this.prototypeBuildingInteriorFloorSnapshot;
            if (
                existing &&
                existing.active === true &&
                existing.signature === signature
            ) {
                return false;
            }
            const resources = this.ensurePrototypeBuildingInteriorFloorSnapshotResources(ctx);
            if (!resources || !resources.renderer || !resources.texture || !resources.sprite) return false;
            const root = this.layers && this.layers.root ? this.layers.root : null;
            if (!root) {
                throw new Error("prototype building interior floor snapshot requires a render root");
            }
            const mesh = this.renderPrototypeBuildingInteriorBitmap(ctx, placement, cache, {
                alpha: 1,
                zIndexOffset: 0.001
            });
            if (!mesh) {
                throw new Error(`prototype building interior floor snapshot could not render source floor ${cache.floorId || cache.id || "(unknown)"}`);
            }
            const previousSnapshot = this.prototypeBuildingInteriorFloorSnapshot;
            resources.sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(resources.sprite, "renderable")) {
                resources.sprite.renderable = false;
            }
            try {
                this.withOnlyDisplayObjectVisibleForSnapshot(root, mesh, () => {
                    const cleared = this.clearBuildingCutawayCompositeRenderTarget(resources);
                    if (!cleared) {
                        throw new Error("prototype building interior floor snapshot texture could not be cleared");
                    }
                    resources.renderer.render(root, resources.texture, false);
                });
            } catch (err) {
                throw new Error(`prototype building interior floor snapshot capture failed: ${err && err.message ? err.message : err}`);
            } finally {
                this.prototypeBuildingInteriorFloorSnapshot = previousSnapshot;
                mesh.visible = false;
                if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                    mesh.renderable = false;
                }
            }
            this.prototypeBuildingInteriorFloorSnapshot = {
                active: true,
                signature,
                placementId: String(placement.id || ""),
                fromFloorId: String(transition.fromSourceFloorId || ""),
                toFloorId: String(transition.toSourceFloorId || ""),
                width: resources.width,
                height: resources.height,
                camera: this.getLayerTransitionSnapshotCameraState(ctx)
            };
            return true;
        }

        hidePrototypeBuildingInteriorFloorSnapshot() {
            const sprite = this.prototypeBuildingInteriorFloorSnapshotSprite;
            if (sprite) {
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }
                if (sprite.parent) sprite.parent.removeChild(sprite);
            }
            this.prototypeBuildingInteriorFloorSnapshot = null;
        }

        renderPrototypeBuildingInteriorFloorSnapshot(ctx, transition) {
            const snapshot = this.prototypeBuildingInteriorFloorSnapshot;
            const sprite = this.prototypeBuildingInteriorFloorSnapshotSprite;
            if (!snapshot || snapshot.active !== true || !sprite) {
                this.hidePrototypeBuildingInteriorFloorSnapshot();
                return null;
            }
            const alpha = this.getPrototypeBuildingInteriorBitmapTransitionAlphas(transition).fromAlpha;
            if (!(alpha > 0.001)) {
                this.hidePrototypeBuildingInteriorFloorSnapshot();
                return null;
            }
            const container = this.layers && this.layers.objects3d ? this.layers.objects3d : null;
            if (!container) {
                throw new Error("prototype building interior floor snapshot requires the 3D objects layer");
            }
            if (sprite.parent !== container) container.addChild(sprite);
            const offset = this.getLayerTransitionSnapshotScreenOffset(snapshot, ctx);
            this.applyLayerTransitionSnapshotSpriteTransform(
                sprite,
                snapshot.width,
                snapshot.height,
                offset.x,
                offset.y
            );
            sprite.alpha = alpha;
            sprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                sprite.renderable = true;
            }
            if (Object.prototype.hasOwnProperty.call(container, "sortableChildren")) {
                container.sortableChildren = true;
            }
            sprite.zIndex = BUILDING_INTERIOR_OVERLAY_Z - 1 + 0.001;
            if (Object.prototype.hasOwnProperty.call(container, "sortDirty")) {
                container.sortDirty = true;
            }
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                sprite.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            return sprite;
        }

        renderPrototypeBuildingInteriorBitmap(ctx, placement, cache, options = null) {
            if (!placement || !cache || cache.status !== "ready" || !cache.texture) return null;
            if (!cache.depthMetricTexture || !cache.depthMetric || !(Number(cache.depthMetric.span) > 0)) {
                throw new Error(`prototype building interior ${cache.id || placement.id} is missing its depth metric texture`);
            }
            const sprite = this.ensurePrototypeBuildingInteriorSprite(cache);
            const proxy = this.ensurePrototypeBuildingInteriorProxy(cache, sprite);
            if (!sprite || !proxy) {
                throw new Error(`prototype building interior ${cache.id || placement.id} could not create its ground projection proxy`);
            }
            const transform = placement.transform || {};
            const worldX = Number(transform.x);
            const worldY = Number(transform.y);
            if (!Number.isFinite(worldX) || !Number.isFinite(worldY)) {
                throw new Error(`prototype building interior ${cache.id || placement.id} requires a finite placement transform`);
            }
            const pixelsPerWorldUnit = Math.max(1e-6, Number(cache.pixelsPerWorldUnit) || 72);
            const sourceXyRatio = Math.max(1e-6, Number(cache.xyratio) || 0.66);
            const viewScale = Math.max(1e-6, Number(this.camera && this.camera.viewscale) || 1);
            const textureWidth = Math.max(1, Number(cache.width || (cache.texture && cache.texture.width)) || 1);
            const textureHeight = Math.max(1, Number(cache.height || (cache.texture && cache.texture.height)) || 1);
            const worldWidth = cache.bounds && Number.isFinite(cache.bounds.worldWidth)
                ? Math.max(0.01, Number(cache.bounds.worldWidth))
                : Math.max(0.01, textureWidth / pixelsPerWorldUnit);
            const worldHeight = cache.bounds && Number.isFinite(cache.bounds.worldHeight)
                ? Math.max(0.01, Number(cache.bounds.worldHeight))
                : Math.max(0.01, textureHeight / (pixelsPerWorldUnit * sourceXyRatio));
            const alpha = Math.max(0, Math.min(1, Number.isFinite(options && options.alpha) ? Number(options.alpha) : 1));
            sprite.width = worldWidth * viewScale;
            sprite.height = worldHeight * viewScale;
            sprite.alpha = alpha;
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                sprite.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            proxy.x = worldX;
            proxy.y = worldY;
            // Interior bitmaps are captured in projection space relative to the
            // building origin, including the floor's world Z. Re-projecting the
            // proxy by its floor index shifts upper floors away from movement
            // geometry.
            proxy.z = 0;
            proxy.width = worldWidth;
            proxy.height = worldHeight;
            // The interior bitmap is captured after applying the placement z-rotation to the building model.
            proxy.placementRotation = 0;
            proxy.map = (ctx && ctx.map) || (this.camera && this.camera.map) || global.map || null;
            proxy._renderLayerBaseZ = 0;
            proxy._renderDepthBias = Number.isFinite(options && options.depthBias) ? Number(options.depthBias) : 0;
            const mesh = proxy.updateDepthBillboardMesh(ctx, this.camera, {
                alphaCutoff: 0.01,
                groundPlaneVisualLift: 0,
                mazeMode: false
            });
            if (!mesh) {
                throw new Error(`prototype building interior ${cache.id || placement.id} did not produce a ground projection mesh`);
            }
            if (typeof this.flipBuildingCutawayCompositeBillboardUvs === "function") {
                this.flipBuildingCutawayCompositeBillboardUvs(mesh, sprite.texture);
            }
            const exteriorDepthState = this.ensurePrototypeBuildingExteriorDepthState();
            if (exteriorDepthState) mesh.state = exteriorDepthState;
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                mesh.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            if (!mesh.shader || !mesh.shader.uniforms) {
                throw new Error(`prototype building interior ${cache.id || placement.id} ground mesh is missing shader uniforms`);
            }
            const uniforms = mesh.shader.uniforms;
            if (!Object.prototype.hasOwnProperty.call(uniforms, "uBuildingExteriorDepthMetricUse")) {
                throw new Error(`prototype building interior ${cache.id || placement.id} shader lacks depth metric support`);
            }
            uniforms.uBuildingExteriorDepthMetricUse = 1;
            uniforms.uBuildingExteriorDepthMetricSampler = cache.depthMetricTexture;
            if (uniforms.uBuildingExteriorDepthMetricRange && uniforms.uBuildingExteriorDepthMetricRange.length >= 2) {
                uniforms.uBuildingExteriorDepthMetricRange[0] = Number(cache.depthMetric.min);
                uniforms.uBuildingExteriorDepthMetricRange[1] = 1 / Math.max(1e-6, Number(cache.depthMetric.span));
            }
            const lightFactor = Number.isFinite(options && options.lightFactor)
                ? Math.max(0, Math.min(1, Number(options.lightFactor)))
                : 1;
            if (lightFactor < 0.999) {
                if (!uniforms.uTint || uniforms.uTint.length < 4) {
                    throw new Error(`prototype building interior ${cache.id || placement.id} light tint requires uTint support`);
                }
                const tint = Number.isFinite(sprite.tint) ? Math.max(0, Math.min(0xffffff, Math.floor(sprite.tint))) : 0xffffff;
                uniforms.uTint[0] = (((tint >> 16) & 0xff) / 255) * lightFactor;
                uniforms.uTint[1] = (((tint >> 8) & 0xff) / 255) * lightFactor;
                uniforms.uTint[2] = ((tint & 0xff) / 255) * lightFactor;
            }
            const container = this.layers && this.layers.objects3d;
            if (!container) {
                throw new Error(`prototype building interior ${cache.id || placement.id} requires the 3D objects layer`);
            }
            const zIndexOffset = Number.isFinite(options && options.zIndexOffset) ? Number(options.zIndexOffset) : 0;
            if (Object.prototype.hasOwnProperty.call(container, "sortableChildren")) {
                container.sortableChildren = true;
            }
            mesh.zIndex = BUILDING_INTERIOR_OVERLAY_Z - 1 + zIndexOffset;
            if (mesh.parent !== container) container.addChild(mesh);
            if (Object.prototype.hasOwnProperty.call(container, "sortDirty")) container.sortDirty = true;
            mesh.visible = true;
            mesh.alpha = alpha;
            if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) mesh.renderable = true;
            return mesh;
        }

        renderPrototypeBuildingInteriors(ctx) {
            const mapRef = (ctx && ctx.map) || global.map || null;
            const cutawayState = (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(cutawayState && cutawayState.triggers) ? cutawayState.triggers : [];
            const renderedMeshes = new Set();
            this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame = new Set();
            this._prototypeBuildingInteriorPendingPlacementIdsThisFrame = new Set();
            this._prototypeBuildingInteriorHeldPlacementIdsThisFrame = new Set();
            const hasPrototypeInterior = triggers.some(trigger => (
                this.isPrototypeBuildingCutawayTrigger(trigger) &&
                trigger.activeInteriorRegion
            ));
            if (!hasPrototypeInterior) {
                this.hideUnusedPrototypeBuildingInteriors(new Set());
                this.hidePrototypeBuildingInteriorFloorSnapshot();
                return renderedMeshes;
            }
            if (!mapRef || typeof mapRef.requestPrototypeBuildingInteriorBitmap !== "function") {
                throw new Error("prototype building interior rendering requires map.requestPrototypeBuildingInteriorBitmap");
            }
            const activeIds = new Set();
            const resolveInteriorCache = (placement, floorId) => {
                let cache = typeof mapRef.getPrototypeBuildingInteriorBitmap === "function"
                    ? mapRef.getPrototypeBuildingInteriorBitmap(placement.id, floorId)
                    : null;
                if (cache && cache.status === "error") {
                    throw new Error(`prototype building interior ${cache.id || placement.id} bitmap failed: ${cache.error || "unknown error"}`);
                }
                if (
                    (
                        !cache ||
                        cache.status === "loading" ||
                        (cache.status === "ready" && cache.stale === true)
                    ) &&
                    typeof mapRef.requestPrototypeBuildingInteriorBitmap === "function"
                ) {
                    this.recordMoveObjectPerf("rendering.prototypeInteriorBitmap.requestCall", {
                        placementId: placement.id || "",
                        floorId,
                        previousStatus: cache && cache.status || "missing"
                    });
                    cache = mapRef.requestPrototypeBuildingInteriorBitmap(placement, floorId, {
                        app: (ctx && ctx.app) || global.app || null,
                        renderer: (ctx && ctx.app && ctx.app.renderer) || (global.app && global.app.renderer) || null,
                        pitch: Math.PI / 4,
                        pixelsPerWorldUnit: 72,
                        paddingPixels: 48,
                        maxDimension: 2048
                    });
                }
                if (cache && cache.status === "error") {
                    throw new Error(`prototype building interior ${cache.id || placement.id} bitmap failed: ${cache.error || "unknown error"}`);
                }
                return cache;
            };
            const renderInteriorFloor = (placement, floorId, options = null, cacheOverride = null) => {
                if (!this.isPrototypeBuildingInteriorBitmapSourceFloorId(floorId)) return null;
                const cache = cacheOverride || resolveInteriorCache(placement, floorId);
                if (cache && cache.status === "ready") {
                    const movePerfEnabled = !!(typeof globalThis !== "undefined" && globalThis.__moveObjectPerf);
                    const renderStartMs = movePerfEnabled ? performance.now() : 0;
                    if (cache.lowerFloorsBitmap) {
                        if (cache.lowerFloorsBitmap.status !== "ready") {
                            throw new Error(`prototype building interior ${cache.id || placement.id} lower-floors bitmap is not ready`);
                        }
                        const lowerMesh = this.renderPrototypeBuildingInteriorBitmap(ctx, placement, cache.lowerFloorsBitmap, {
                            alpha: Number.isFinite(options && options.alpha) ? Number(options.alpha) : 1,
                            zIndexOffset: -0.001,
                            depthBias: 0,
                            lightFactor: BUILDING_INTERIOR_LOWER_FLOOR_LIGHT_FACTOR
                        });
                        if (lowerMesh) {
                            activeIds.add(cache.lowerFloorsBitmap.id);
                            renderedMeshes.add(lowerMesh);
                            this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame.add(placement.id);
                        }
                    }
                    const mesh = this.renderPrototypeBuildingInteriorBitmap(ctx, placement, cache, {
                        ...(options || {}),
                        zIndexOffset: Number.isFinite(options && options.zIndexOffset)
                            ? Number(options.zIndexOffset)
                            : 0.002,
                        depthBias: Number.isFinite(options && options.depthBias)
                            ? Number(options.depthBias)
                            : BUILDING_INTERIOR_ACTIVE_FLOOR_DEPTH_BUMP_UNITS
                    });
                    if (movePerfEnabled) {
                        this.recordMoveObjectPerf("rendering.prototypeInteriorBitmap.renderMesh", {
                            placementId: placement.id || "",
                            floorId,
                            cacheId: cache.id || "",
                            hasMesh: !!mesh
                        }, performance.now() - renderStartMs);
                    }
                    if (mesh) {
                        activeIds.add(cache.id);
                        renderedMeshes.add(mesh);
                        this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame.add(placement.id);
                        if (typeof mapRef.releasePrototypeBuildingInteriorBitmapReplacement === "function") {
                            mapRef.releasePrototypeBuildingInteriorBitmapReplacement(cache);
                        }
                    }
                    return mesh;
                }
                if (cache && cache.status === "loading") {
                    this._prototypeBuildingInteriorPendingPlacementIdsThisFrame.add(placement.id);
                }
                return null;
            };
            let renderedSnapshotThisFrame = false;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!this.isPrototypeBuildingCutawayTrigger(trigger) || !trigger.activeInteriorRegion) continue;
                const placement = trigger.building && trigger.building._prototypeBuildingPlacement;
                if (!placement || !placement.id) {
                    throw new Error(`prototype building interior trigger ${trigger && trigger.buildingId || "(unknown)"} is missing its placement`);
                }
                const floorId = this.getPrototypeBuildingInteriorSourceFloorId(trigger, { required: false });
                if (!floorId) continue;
                const transition = trigger.interiorFloorTransition;
                const fromFloorId = typeof (transition && transition.fromSourceFloorId) === "string"
                    ? transition.fromSourceFloorId
                    : "";
                const progress = Number(transition && transition.progress);
                const hasOutgoingSnapshot = !!(
                    fromFloorId &&
                    fromFloorId !== floorId &&
                    this.isPrototypeBuildingInteriorBitmapSourceFloorId(fromFloorId) &&
                    Number.isFinite(progress) &&
                    progress < 1
                );
                let outgoingSnapshotReady = false;
                let fromCache = null;
                if (hasOutgoingSnapshot) {
                    const snapshotSignature = this.getPrototypeBuildingInteriorFloorSnapshotSignature(ctx, placement, transition);
                    fromCache = resolveInteriorCache(placement, fromFloorId);
                    if (fromCache && fromCache.status === "ready") {
                        this.capturePrototypeBuildingInteriorFloorSnapshot(ctx, placement, fromCache, transition);
                    }
                    const snapshot = this.prototypeBuildingInteriorFloorSnapshot;
                    outgoingSnapshotReady = !!(
                        snapshot &&
                        snapshot.active === true &&
                        snapshot.signature === snapshotSignature
                    );
                }
                const incomingCache = this.isPrototypeBuildingInteriorBitmapSourceFloorId(floorId)
                    ? resolveInteriorCache(placement, floorId)
                    : null;
                const incomingReady = !!(incomingCache && incomingCache.status === "ready");
                if (incomingCache && incomingCache.status === "loading") {
                    this._prototypeBuildingInteriorPendingPlacementIdsThisFrame.add(placement.id);
                }
                if (!incomingReady && hasOutgoingSnapshot && fromCache && fromCache.status === "ready") {
                    const heldMesh = renderInteriorFloor(placement, fromFloorId, {
                        alpha: 1,
                        zIndexOffset: 0
                    }, fromCache);
                    if (heldMesh) this._prototypeBuildingInteriorHeldPlacementIdsThisFrame.add(placement.id);
                    continue;
                }
                const snapshotTransition = incomingReady
                    ? transition
                    : (hasOutgoingSnapshot ? { ...transition, progress: 0 } : transition);
                const transitionAlphas = this.getPrototypeBuildingInteriorBitmapTransitionAlphas(snapshotTransition);
                if (!hasOutgoingSnapshot || incomingReady) {
                    renderInteriorFloor(placement, floorId, { alpha: transitionAlphas.toAlpha, zIndexOffset: 0 }, incomingCache);
                }
                if (outgoingSnapshotReady) {
                    const snapshotDisplay = this.renderPrototypeBuildingInteriorFloorSnapshot(ctx, snapshotTransition);
                    if (snapshotDisplay) {
                        renderedSnapshotThisFrame = true;
                        renderedMeshes.add(snapshotDisplay);
                        this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame.add(placement.id);
                        this._prototypeBuildingInteriorHeldPlacementIdsThisFrame.add(placement.id);
                    }
                }
            }
            this.hideUnusedPrototypeBuildingInteriors(activeIds);
            if (!renderedSnapshotThisFrame) {
                this.hidePrototypeBuildingInteriorFloorSnapshot();
            }
            return renderedMeshes;
        }

        isPrototypeBuildingExteriorHiddenByInteriorCutaway(placement, ctx = null) {
            if (!placement || typeof placement.id !== "string") return false;
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            const support = wizardRef && wizardRef.currentMovementSupport && typeof wizardRef.currentMovementSupport === "object"
                ? wizardRef.currentMovementSupport
                : null;
            const supportMembership = support && support.floorMembership && typeof support.floorMembership === "object"
                ? support.floorMembership
                : null;
            const wizardMembership = wizardRef && wizardRef._floorMembership && typeof wizardRef._floorMembership === "object"
                ? wizardRef._floorMembership
                : null;
            if (
                (
                    support &&
                    support.ownerType === "building" &&
                    support.ownerId === placement.id
                ) ||
                (
                    supportMembership &&
                    supportMembership.ownerType === "building" &&
                    supportMembership.ownerId === placement.id
                ) ||
                (
                    wizardMembership &&
                    wizardMembership.ownerType === "building" &&
                    wizardMembership.ownerId === placement.id
                )
            ) {
                return true;
            }
            const mapRef = (ctx && ctx.map) || global.map || null;
            if (mapRef && typeof mapRef.getPrototypeWorldScope === "function") {
                const scope = mapRef.getPrototypeWorldScope();
                if (
                    scope &&
                    scope.type === "building" &&
                    scope.id === placement.id
                ) {
                    return true;
                }
            }
            const state = (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (
                    trigger &&
                    trigger.buildingId === placement.id &&
                    trigger.activeInteriorRegion
                ) {
                    return true;
                }
            }
            return false;
        }

        getPrototypeBuildingExteriorCutawayAlpha(placement, ctx = null) {
            if (!placement || typeof placement.id !== "string" || placement.id.length === 0) {
                throw new Error("prototype building exterior cutaway alpha requires a placement id");
            }
            const state = (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            let alpha = 1;
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                const triggerPlacement = trigger && trigger.building && trigger.building._prototypeBuildingPlacement;
                if (!triggerPlacement || triggerPlacement.id !== placement.id) continue;
                if (trigger.activeInteriorRegion) {
                    alpha = Math.min(alpha, BUILDING_CUTAWAY_INTERIOR_ALPHA);
                    continue;
                }
                const triggerAlpha = Number.isFinite(trigger.alpha)
                    ? Math.max(0, Math.min(1, Number(trigger.alpha)))
                    : BUILDING_CUTAWAY_GHOST_ALPHA;
                alpha = Math.min(alpha, triggerAlpha);
            }
            return alpha;
        }

        shouldPickPrototypeBuildingExteriorAtAlpha(alpha) {
            const value = Number(alpha);
            if (!Number.isFinite(value)) return true;
            return value > BUILDING_CUTAWAY_GHOST_ALPHA + 0.001;
        }

        renderPrototypeBuildingExteriorBitmap(ctx, placement, cache, options = null) {
            if (!placement || !cache || cache.status !== "ready" || !cache.texture) return null;
            if (!cache.depthMetricTexture || !cache.depthMetric || !(Number(cache.depthMetric.span) > 0)) {
                throw new Error(`prototype building exterior ${cache.id || placement.id} is missing its depth metric texture`);
            }
            this.ensurePrototypeBuildingExteriorDepthMappingSupport(ctx);
            const sprite = this.ensurePrototypeBuildingExteriorSprite(cache);
            const proxy = this.ensurePrototypeBuildingExteriorProxy(cache, sprite);
            if (!sprite || !proxy) return null;
            const transform = placement.transform || {};
            const worldWidth = cache.bounds && Number.isFinite(cache.bounds.worldWidth)
                ? Math.max(0.01, Number(cache.bounds.worldWidth))
                : Math.max(0.01, Number(cache.width || 1) / Math.max(1e-6, Number(cache.pixelsPerWorldUnit) || 72));
            const worldHeight = cache.bounds && Number.isFinite(cache.bounds.worldHeight)
                ? Math.max(0.01, Number(cache.bounds.worldHeight))
                : Math.max(0.01, Number(cache.height || 1) / (Math.max(1e-6, Number(cache.pixelsPerWorldUnit) || 72) * Math.max(1e-6, Number(cache.xyratio) || 0.66)));
            sprite.width = worldWidth * Math.max(1e-6, Number(this.camera && this.camera.viewscale) || 1);
            sprite.height = worldHeight * Math.max(1e-6, Number(this.camera && this.camera.viewscale) || 1) * Math.max(1e-6, Number(this.camera && this.camera.xyratio) || 0.66);
            const alpha = Math.max(0, Math.min(1, Number.isFinite(options && options.alpha) ? Number(options.alpha) : 1));
            sprite.alpha = alpha;
            sprite.visible = false;
            if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) sprite.renderable = false;
            proxy.x = Number(transform.x);
            proxy.y = Number(transform.y);
            proxy.z = 0;
            proxy.width = worldWidth;
            proxy.height = worldHeight;
            proxy.map = (ctx && ctx.map) || (this.camera && this.camera.map) || global.map || null;
            proxy._renderLayerBaseZ = 0;
            proxy._renderDepthBias = 0;
            const mesh = proxy.updateDepthBillboardMesh(ctx, this.camera, {
                alphaCutoff: 0.01,
                mazeMode: false,
                useVerticalAnchorY: true
            });
            if (!mesh) return null;
            if (typeof this.flipBuildingCutawayCompositeBillboardUvs === "function") {
                this.flipBuildingCutawayCompositeBillboardUvs(mesh, sprite.texture);
            }
            const exteriorDepthState = alpha < 0.999
                ? this.ensurePrototypeBuildingExteriorTransparentDepthState()
                : this.ensurePrototypeBuildingExteriorDepthState();
            if (exteriorDepthState) mesh.state = exteriorDepthState;
            if (typeof PIXI !== "undefined" && PIXI.BLEND_MODES) {
                mesh.blendMode = PIXI.BLEND_MODES.NORMAL;
            }
            if (!mesh.shader || !mesh.shader.uniforms) {
                throw new Error(`prototype building exterior ${cache.id || placement.id} depth mesh is missing shader uniforms`);
            }
            const uniforms = mesh.shader.uniforms;
            if (uniforms.uTint && uniforms.uTint.length >= 4) {
                const tint = Number.isFinite(sprite.tint) ? Math.max(0, Math.min(0xffffff, Math.floor(sprite.tint))) : 0xffffff;
                uniforms.uTint[0] = (((tint >> 16) & 0xff) / 255) * alpha;
                uniforms.uTint[1] = (((tint >> 8) & 0xff) / 255) * alpha;
                uniforms.uTint[2] = ((tint & 0xff) / 255) * alpha;
                uniforms.uTint[3] = alpha;
            }
            if (!Object.prototype.hasOwnProperty.call(uniforms, "uBuildingExteriorDepthMetricUse")) {
                throw new Error(`prototype building exterior ${cache.id || placement.id} shader lacks depth metric support`);
            }
            uniforms.uBuildingExteriorDepthMetricUse = 1;
            uniforms.uBuildingExteriorDepthMetricSampler = cache.depthMetricTexture;
            if (uniforms.uBuildingExteriorDepthMetricRange && uniforms.uBuildingExteriorDepthMetricRange.length >= 2) {
                uniforms.uBuildingExteriorDepthMetricRange[0] = Number(cache.depthMetric.min);
                uniforms.uBuildingExteriorDepthMetricRange[1] = 1 / Math.max(1e-6, Number(cache.depthMetric.span));
            }
            const container = this.layers && this.layers.objects3d;
            if (!container) {
                throw new Error(`prototype building exterior ${cache.id || placement.id} requires the 3D objects layer`);
            }
            container.addChild(mesh);
            mesh.visible = true;
            mesh.alpha = alpha;
            if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) mesh.renderable = true;
            const pickTarget = this.ensurePrototypeBuildingPlacementPickTarget(
                placement,
                cache,
                proxy,
                proxy.map
            );
            if (!pickTarget) {
                throw new Error(`prototype building exterior ${cache.id || placement.id} could not create its picker target`);
            }
            if (this.shouldPickPrototypeBuildingExteriorAtAlpha(alpha)) {
                this.addPickRenderItem(pickTarget, mesh, { forceInclude: true });
            }
            return mesh;
        }

        renderPrototypeBuildingExteriorGroundDoor(ctx, item, entry, trigger, container, currentDisplayObjects = null) {
            if (!this.shouldRenderBuildingExteriorGroundDoorItem(item, entry, trigger, ctx, (ctx && ctx.wizard) || global.wizard || null)) {
                return [];
            }
            const sprite = this.ensurePrototypeBuildingExteriorDoorSprite(item);
            const proxy = this.ensurePrototypeBuildingExteriorDoorProxy(item, sprite);
            if (!proxy) return [];
            this.configurePrototypeBuildingExteriorDoorProxy(proxy, item, entry, trigger, ctx);
            const exteriorSide = this.getBuildingCutawayMountedExteriorSide(
                item,
                (ctx && ctx._renderingLayerCutawayState) || null,
                ctx,
                (ctx && ctx.map) || global.map || null
            );
            const mesh = proxy.updateDepthBillboardMesh(ctx, this.camera, {
                alphaCutoff: TREE_ALPHA_CUTOFF,
                mazeMode: false,
                forceMountedWallSide: exteriorSide === "front" || exteriorSide === "back" ? exteriorSide : null,
                drawOnlyMountedWallSide: exteriorSide === "front" || exteriorSide === "back"
            });
            if (!mesh) return [];
            const rendered = [];
            const addDisplay = (displayObj) => {
                if (!displayObj) return;
                if (displayObj.parent !== container && container && typeof container.addChild === "function") {
                    container.addChild(displayObj);
                }
                displayObj.visible = true;
                if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                    displayObj.renderable = true;
                }
                rendered.push(displayObj);
                if (currentDisplayObjects instanceof Set) currentDisplayObjects.add(displayObj);
                this.addPickRenderItem(item, displayObj, { forceInclude: true });
            };
            addDisplay(proxy._compositeUnderlayShouldRender === true ? proxy._compositeUnderlayMesh : null);
            addDisplay(mesh);
            item._renderingDepthMesh = mesh;
            item._prototypeBuildingExteriorDoorProxy = proxy;
            return rendered;
        }

        hideUnusedPrototypeBuildingExteriorGroundDoors(activeIds = null) {
            const active = activeIds instanceof Set ? activeIds : new Set();
            const proxies = this.prototypeBuildingExteriorDoorProxiesById;
            if (proxies instanceof Map) {
                proxies.forEach((proxy, id) => {
                    if (active.has(id)) return;
                    const displays = [
                        proxy && proxy._depthBillboardMesh,
                        proxy && proxy._compositeUnderlayMesh,
                        proxy && proxy.pixiSprite
                    ];
                    for (let i = 0; i < displays.length; i++) {
                        const displayObj = displays[i];
                        if (!displayObj) continue;
                        displayObj.visible = false;
                        if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                            displayObj.renderable = false;
                        }
                    }
                });
            }
            const sprites = this.prototypeBuildingExteriorDoorSpritesById;
            if (sprites instanceof Map) {
                sprites.forEach((entry, id) => {
                    if (active.has(id)) return;
                    const sprite = entry && entry.sprite;
                    if (!sprite) return;
                    sprite.visible = false;
                    if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                        sprite.renderable = false;
                    }
                });
            }
        }

        renderPrototypeBuildingExteriorGroundDoors(ctx, cutawayState, foregroundPlan, container, currentDisplayObjects = null) {
            const activeIds = new Set();
            if (!foregroundPlan || !(foregroundPlan.items instanceof Set) || !container) {
                this.hideUnusedPrototypeBuildingExteriorGroundDoors(activeIds);
                return [];
            }
            const state = cutawayState || (ctx && ctx._renderingLayerCutawayState) || this.getLayerCutawayState(ctx);
            const triggers = Array.isArray(state && state.triggers) ? state.triggers : [];
            const rendered = [];
            for (let i = 0; i < triggers.length; i++) {
                const trigger = triggers[i];
                if (!trigger || !trigger.building || trigger.activeInteriorRegion) continue;
                const cache = trigger.renderCache || null;
                const entries = Array.isArray(cache && cache.renderItems) ? cache.renderItems : [];
                for (let j = 0; j < entries.length; j++) {
                    const entry = entries[j];
                    const item = entry && entry.item;
                    if (!item || !foregroundPlan.items.has(item)) continue;
                    const displayObjects = this.renderPrototypeBuildingExteriorGroundDoor(
                        ctx,
                        item,
                        entry,
                        trigger,
                        container,
                        currentDisplayObjects
                    );
                    if (displayObjects.length > 0) {
                        activeIds.add(item.id);
                        for (let d = 0; d < displayObjects.length; d++) rendered.push(displayObjects[d]);
                    }
                }
            }
            this.hideUnusedPrototypeBuildingExteriorGroundDoors(activeIds);
            return rendered;
        }

        renderPrototypeBuildingExteriors(ctx) {
            const mapRef = (ctx && ctx.map) || global.map || null;
            if (!mapRef || typeof mapRef.getPrototypeBuildingPlacements !== "function") {
                this.hideUnusedPrototypeBuildingExteriors(new Set());
                return;
            }
            const wizardRef = (ctx && ctx.wizard) || global.wizard || null;
            if (wizardRef && this.getWizardVisualLayerIndex(wizardRef, 0) < 0) {
                this.hideUnusedPrototypeBuildingExteriors(new Set());
                return;
            }
            const placements = mapRef.getPrototypeBuildingPlacements();
            const activeIds = new Set();
            const interiorRenderedIds = this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame instanceof Set
                ? this._prototypeBuildingInteriorRenderedPlacementIdsThisFrame
                : null;
            const interiorPendingIds = this._prototypeBuildingInteriorPendingPlacementIdsThisFrame instanceof Set
                ? this._prototypeBuildingInteriorPendingPlacementIdsThisFrame
                : null;
            const interiorHeldIds = this._prototypeBuildingInteriorHeldPlacementIdsThisFrame instanceof Set
                ? this._prototypeBuildingInteriorHeldPlacementIdsThisFrame
                : null;
            const interiorScope = this.getActiveBuildingInteriorRenderScope(ctx);
            for (let i = 0; i < placements.length; i++) {
                const placement = placements[i];
                if (!placement || !placement.id) continue;
                if (interiorScope && interiorScope.active && !interiorScope.buildingIds.has(placement.id)) continue;
                const waitingForPrototypeInterior = !!(
                    interiorPendingIds &&
                    interiorPendingIds.has(placement.id)
                );
                if (
                    interiorRenderedIds &&
                    interiorRenderedIds.has(placement.id) &&
                    (!waitingForPrototypeInterior || (interiorHeldIds && interiorHeldIds.has(placement.id)))
                ) continue;
                if (this.isPrototypeBuildingExteriorHiddenByInteriorCutaway(placement, ctx)) {
                    if (!waitingForPrototypeInterior) continue;
                }
                let cache = typeof mapRef.getPrototypeBuildingExteriorBitmap === "function"
                    ? mapRef.getPrototypeBuildingExteriorBitmap(placement.id)
                    : null;
                if ((!cache || cache.status === "loading") && typeof mapRef.requestPrototypeBuildingExteriorBitmap === "function") {
                    cache = mapRef.requestPrototypeBuildingExteriorBitmap(placement, {
                        app: (ctx && ctx.app) || global.app || null,
                        renderer: (ctx && ctx.app && ctx.app.renderer) || (global.app && global.app.renderer) || null,
                        pitch: Math.PI / 4,
                        pixelsPerWorldUnit: 72,
                        paddingPixels: 48,
                        maxDimension: 2048
                    });
                }
                if (cache && cache.status === "ready") {
                    const alpha = this.getPrototypeBuildingExteriorCutawayAlpha(placement, ctx);
                    const mesh = this.renderPrototypeBuildingExteriorBitmap(ctx, placement, cache, { alpha });
                    if (mesh) activeIds.add(placement.id);
                }
            }
            this.hideUnusedPrototypeBuildingExteriors(activeIds);
        }

        buildPowerupPlacementPreviewRenderItem(ctx) {
            const wizard = (ctx && ctx.wizard) || global.wizard || null;
            if (!wizard || wizard.currentSpell !== "blackdiamond" || wizard.editorPlacementActive !== true) {
                return null;
            }
            const mousePosRef = this.getMousePosRef(ctx);
            if (!mousePosRef || !Number.isFinite(mousePosRef.worldX) || !Number.isFinite(mousePosRef.worldY)) {
                return null;
            }

            const spellSystemRef = (typeof SpellSystem !== "undefined")
                ? SpellSystem
                : (global.SpellSystem || null);
            const previewConfig = (
                spellSystemRef &&
                typeof spellSystemRef.getPowerupPlacementPreviewConfig === "function"
            ) ? spellSystemRef.getPowerupPlacementPreviewConfig(wizard) : {
                fileName: "button.png",
                imagePath: "/assets/images/powerups/button.png",
                width: 0.8,
                height: 0.8,
                radius: 0.35,
                scale: 1
            };

            const texturePath = (previewConfig && typeof previewConfig.imagePath === "string" && previewConfig.imagePath.length > 0)
                ? previewConfig.imagePath
                : "/assets/images/powerups/button.png";
            if (!this.powerupPlacementPreviewSprite) {
                this.powerupPlacementPreviewSprite = new PIXI.Sprite(PIXI.Texture.from(texturePath));
                this.powerupPlacementPreviewSprite.anchor.set(0.5, 1);
                this.powerupPlacementPreviewSprite.alpha = 0.55;
                this.powerupPlacementPreviewSprite.interactive = false;
                this.powerupPlacementPreviewSprite.visible = false;
                this.powerupPlacementPreviewTexturePath = texturePath;
            } else if (this.powerupPlacementPreviewTexturePath !== texturePath) {
                this.powerupPlacementPreviewSprite.texture = PIXI.Texture.from(texturePath);
                this.powerupPlacementPreviewTexturePath = texturePath;
            }

            const mapRef = (ctx && ctx.map) || wizard.map || global.map || null;
            const placementLayer = Number.isFinite(wizard.currentLayer)
                ? Math.round(Number(wizard.currentLayer))
                : 0;
            const placementLayerBaseZ = this.getPlacementLayerBaseZForWizard(wizard, mapRef, placementLayer);
            const projectedPoint = this.resolveScreenPointOnLayerPlane(
                mousePosRef.screenX,
                mousePosRef.screenY,
                placementLayerBaseZ,
                mapRef,
                wizard
            );
            const worldX = (projectedPoint && Number.isFinite(projectedPoint.x))
                ? projectedPoint.x
                : mousePosRef.worldX;
            const worldY = (projectedPoint && Number.isFinite(projectedPoint.y))
                ? projectedPoint.y
                : mousePosRef.worldY;
            const worldZ = 0;
            const width = Number.isFinite(previewConfig && previewConfig.width)
                ? Math.max(0.01, Number(previewConfig.width))
                : 0.8;
            const height = Number.isFinite(previewConfig && previewConfig.height)
                ? Math.max(0.01, Number(previewConfig.height))
                : 0.8;

            this.powerupPlacementPreviewSprite.tint = 0xFFFFFF;
            this.powerupPlacementPreviewSprite.visible = true;
            if (Object.prototype.hasOwnProperty.call(this.powerupPlacementPreviewSprite, "renderable")) {
                this.powerupPlacementPreviewSprite.renderable = true;
            }

            if (!this.powerupPlacementPreviewItem) {
                this.powerupPlacementPreviewItem = {
                    type: "powerupPlacementPreview",
                    map: mapRef || global.map || null,
                    gone: false,
                    vanishing: false,
                    pixiSprite: this.powerupPlacementPreviewSprite,
                    anchorX: 0.5,
                    anchorY: 1
                };
                const powerupProto = global.Powerup && global.Powerup.prototype
                    ? global.Powerup.prototype
                    : null;
                if (powerupProto && typeof powerupProto.ensureSprite === "function") {
                    this.powerupPlacementPreviewItem.ensureSprite = powerupProto.ensureSprite;
                }
                if (powerupProto && typeof powerupProto.ensureDepthBillboardMesh === "function") {
                    this.powerupPlacementPreviewItem.ensureDepthBillboardMesh = powerupProto.ensureDepthBillboardMesh;
                }
                if (powerupProto && typeof powerupProto.updateDepthBillboardMesh === "function") {
                    this.powerupPlacementPreviewItem.updateDepthBillboardMesh = powerupProto.updateDepthBillboardMesh;
                }
            }

            const previewItem = this.powerupPlacementPreviewItem;
            previewItem.map = mapRef || previewItem.map || null;
            previewItem.pixiSprite = this.powerupPlacementPreviewSprite;
            previewItem.x = worldX;
            previewItem.y = worldY;
            previewItem.z = worldZ;
            previewItem.traversalLayer = placementLayer;
            previewItem.currentLayer = placementLayer;
            previewItem.currentLayerBaseZ = placementLayerBaseZ;
            previewItem._floorBaseZ = placementLayerBaseZ;
            previewItem._renderLayerBaseZ = placementLayerBaseZ;
            previewItem.width = width;
            previewItem.height = height;
            previewItem.renderZ = worldY;
            previewItem.previewAlpha = 0.55;
            previewItem.imagePath = texturePath;
            const puAnchorX = Number.isFinite(previewConfig && previewConfig.anchorX)
                ? Number(previewConfig.anchorX) : 0.5;
            const puAnchorY = Number.isFinite(previewConfig && previewConfig.anchorY)
                ? Number(previewConfig.anchorY) : 0.5;
            previewItem.anchorX = puAnchorX;
            previewItem.anchorY = puAnchorY;
            // Sync the preview sprite's Pixi anchor to match the actual
            // powerup anchor from items.json so the depth billboard quad
            // matches the final placed powerup appearance.
            if (this.powerupPlacementPreviewSprite && this.powerupPlacementPreviewSprite.anchor) {
                this.powerupPlacementPreviewSprite.anchor.set(puAnchorX, puAnchorY);
            }
            return previewItem;
        }

        renderPowerupPlacementPreview(ctx) {
            const previewItem = this.buildPowerupPlacementPreviewRenderItem(ctx);
            if (!previewItem) {
                this.clearPowerupPlacementPreview();
                return;
            }
            if (previewItem.pixiSprite) {
                const w = Number.isFinite(previewItem.width) ? Math.max(0.01, Number(previewItem.width)) : 0.8;
                const h = Number.isFinite(previewItem.height) ? Math.max(0.01, Number(previewItem.height)) : 0.8;
                const viewScale = Number.isFinite(this.camera && this.camera.viewscale)
                    ? Number(this.camera.viewscale)
                    : 1;
                // Match live powerup render sizing exactly (see renderPowerups()).
                previewItem.pixiSprite.width = w * viewScale;
                previewItem.pixiSprite.height = h * viewScale;
            }
            let displayObj = null;
            if (typeof previewItem.updateDepthBillboardMesh === "function") {
                const mesh = previewItem.updateDepthBillboardMesh(
                    ctx,
                    this.camera,
                    { alphaCutoff: TREE_ALPHA_CUTOFF }
                );
                if (mesh) {
                    const depthContainer = this.layers.depthObjects;
                    if (depthContainer && mesh.parent !== depthContainer) {
                        depthContainer.addChild(mesh);
                    }
                    mesh.visible = true;
                    if (Object.prototype.hasOwnProperty.call(mesh, "renderable")) {
                        mesh.renderable = true;
                    }
                    if (Number.isFinite(previewItem.previewAlpha)) {
                        mesh.alpha = previewItem.previewAlpha;
                    }
                    displayObj = mesh;
                }
            }
            if (!displayObj) {
                this.applySpriteTransform(previewItem);
                displayObj = previewItem.pixiSprite || this.powerupPlacementPreviewSprite;
            }
            if (!displayObj) {
                this.clearPowerupPlacementPreview();
                return;
            }

            const interiorPresentationActive = this.isBuildingInteriorPresentationActive(ctx);
            const container = interiorPresentationActive
                ? this.layers.ui
                : ((displayObj instanceof PIXI.Mesh)
                    ? this.layers.depthObjects
                    : this.layers.objects3d);
            if (!container) {
                this.clearPowerupPlacementPreview();
                return;
            }
            if (displayObj.parent !== container) {
                container.addChild(displayObj);
            }
            displayObj.visible = true;
            if (Object.prototype.hasOwnProperty.call(displayObj, "renderable")) {
                displayObj.renderable = true;
            }
            if (Number.isFinite(previewItem.previewAlpha)) {
                displayObj.alpha = previewItem.previewAlpha;
            }
            if (Number.isFinite(displayObj.tint)) {
                displayObj.tint = 0xFFFFFF;
            }
            if (interiorPresentationActive) {
                displayObj.zIndex = BUILDING_INTERIOR_FOREGROUND_Z;
                container.sortableChildren = true;
                if (Object.prototype.hasOwnProperty.call(container, "sortDirty")) container.sortDirty = true;
            }
            const topIndex = container.children.length - 1;
            if (topIndex >= 0) {
                const currentIndex = container.getChildIndex(displayObj);
                if (currentIndex !== topIndex) {
                    container.setChildIndex(displayObj, topIndex);
                }
            }
            if (this.powerupPlacementPreviewDisplayObject && this.powerupPlacementPreviewDisplayObject !== displayObj) {
                this.powerupPlacementPreviewDisplayObject.visible = false;
                if (Object.prototype.hasOwnProperty.call(this.powerupPlacementPreviewDisplayObject, "renderable")) {
                    this.powerupPlacementPreviewDisplayObject.renderable = false;
                }
            }
            this.powerupPlacementPreviewDisplayObject = displayObj;
        }

        beginDrawPassProfiling(ctx) {
            const profiler = this.drawPassProfiler;
            if (!profiler || profiler.printed || !isDrawPassBreakdownEnabled()) return null;
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : performance.now();
            if (!Number.isFinite(profiler.startMs)) {
                profiler.startMs = nowMs;
                profiler.deadlineMs = nowMs + 60000;
            }
            return profiler;
        }

        beginFrameMetrics() {
            if (!isDrawPassBreakdownEnabled()) {
                this.currentFrameMetrics = null;
                return null;
            }
            this.currentFrameMetrics = Object.create(null);
            return this.currentFrameMetrics;
        }

        setFrameMetric(metricName, value) {
            if (!this.currentFrameMetrics || !metricName) return value;
            this.currentFrameMetrics[metricName] = value;
            return value;
        }

        recordMoveObjectPerf(name, data = null, elapsedMs = null) {
            if (
                typeof globalThis === "undefined" ||
                !globalThis.__moveObjectPerf ||
                typeof globalThis.__recordMoveObjectPerf !== "function"
            ) {
                return;
            }
            globalThis.__recordMoveObjectPerf(name, data, elapsedMs);
        }

        incrementFrameMetric(metricName, delta = 1) {
            if (!this.currentFrameMetrics || !metricName || !Number.isFinite(delta)) return 0;
            const nextValue = Number(this.currentFrameMetrics[metricName] || 0) + Number(delta);
            this.currentFrameMetrics[metricName] = nextValue;
            return nextValue;
        }

        recordDrawPassSection(sectionName, elapsedMs) {
            const profiler = this.drawPassProfiler;
            if (!profiler || profiler.printed || !sectionName || !Number.isFinite(elapsedMs)) return;
            let section = profiler.sections[sectionName];
            if (!section) {
                section = {
                    count: 0,
                    totalMs: 0,
                    maxMs: 0
                };
                profiler.sections[sectionName] = section;
            }
            section.count += 1;
            section.totalMs += elapsedMs;
            if (elapsedMs > section.maxMs) {
                section.maxMs = elapsedMs;
            }
        }

        recordDrawPassFrameMetrics(metrics) {
            const profiler = this.drawPassProfiler;
            if (!profiler || profiler.printed || !metrics) return;
            const metricNames = Object.keys(metrics);
            for (let i = 0; i < metricNames.length; i++) {
                const name = metricNames[i];
                const value = Number(metrics[name]);
                if (!Number.isFinite(value)) continue;
                let metric = profiler.metrics[name];
                if (!metric) {
                    metric = {
                        count: 0,
                        total: 0,
                        max: -Infinity,
                        last: 0
                    };
                    profiler.metrics[name] = metric;
                }
                metric.count += 1;
                metric.total += value;
                metric.last = value;
                if (value > metric.max) metric.max = value;
            }
        }

        profileDrawPassSection(sectionName, fn) {
            if (!isDrawPassBreakdownEnabled()) {
                return fn();
            }
            const t0 = performance.now();
            const result = fn();
            const elapsedMs = performance.now() - t0;
            this.recordDrawPassSection(sectionName, elapsedMs);
            if (!this.currentFrameDrawSections) {
                this.currentFrameDrawSections = Object.create(null);
            }
            this.currentFrameDrawSections[sectionName] = elapsedMs;
            return result;
        }

        summarizeCurrentDrawSections(frameElapsedMs = 0, limit = 12) {
            const sections = this.currentFrameDrawSections || null;
            if (!sections) {
                return {
                    totalMs: 0,
                    unaccountedMs: Number.isFinite(frameElapsedMs) ? frameElapsedMs : 0,
                    top: []
                };
            }
            const entries = [];
            let totalMs = 0;
            const names = Object.keys(sections);
            for (let i = 0; i < names.length; i++) {
                const name = names[i];
                const ms = Number(sections[name]);
                if (!Number.isFinite(ms) || ms <= 0) continue;
                totalMs += ms;
                entries.push({ name, ms: Number(ms.toFixed(2)) });
            }
            entries.sort((a, b) => b.ms - a.ms);
            const elapsed = Number.isFinite(frameElapsedMs) ? Number(frameElapsedMs) : 0;
            return {
                totalMs: Number(totalMs.toFixed(2)),
                unaccountedMs: Number(Math.max(0, elapsed - totalMs).toFixed(2)),
                top: entries.slice(0, Math.max(1, Math.floor(Number(limit) || 12)))
            };
        }

        maybePrintDrawPassProfileSummary(ctx) {
            const profiler = this.drawPassProfiler;
            if (!profiler || profiler.printed || !Number.isFinite(profiler.deadlineMs)) return;
            const nowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : performance.now();
            if (nowMs < profiler.deadlineMs) return;

            const sections = {};
            const sectionNames = Object.keys(profiler.sections);
            for (let i = 0; i < sectionNames.length; i++) {
                const name = sectionNames[i];
                const section = profiler.sections[name];
                if (!section) continue;
                const avgMs = section.count > 0 ? section.totalMs / section.count : 0;
                sections[name] = {
                    samples: section.count,
                    avgMs,
                    maxMs: section.maxMs,
                    totalMs: section.totalMs
                };
            }

            const metrics = {};
            const metricNames = Object.keys(profiler.metrics || {});
            for (let i = 0; i < metricNames.length; i++) {
                const name = metricNames[i];
                const metric = profiler.metrics[name];
                if (!metric) continue;
                metrics[name] = {
                    samples: metric.count,
                    avg: metric.count > 0 ? metric.total / metric.count : 0,
                    max: Number.isFinite(metric.max) ? metric.max : 0,
                    total: metric.total,
                    last: metric.last
                };
            }

            const summary = {
                durationMs: nowMs - profiler.startMs,
                frameCount: profiler.frameCount,
                avgFrameMs: profiler.frameCount > 0 ? profiler.totalFrameMs / profiler.frameCount : 0,
                maxFrameMs: profiler.maxFrameMs,
                sections,
                metrics
            };
            global.renderingDrawPassProfileSummary = summary;
            console.log("Rendering draw-pass profile (60s):", summary);
            profiler.printed = true;
        }

        renderFrame(ctx) {
            this.init(ctx);
            if (!this.initialized) return false;
            const frameStartMs = performance.now();
            this._drawFrameId = (Number(this._drawFrameId) || 0) + 1;
            this._activeDrawFrameId = this._drawFrameId;
            this.currentFrameDrawSections = isDrawPassBreakdownEnabled() ? Object.create(null) : null;
            this.beginFrameMetrics();
            this.clearBuildingInteriorForegroundPromotions();
            const frameNowMs = (ctx && Number.isFinite(ctx.renderNowMs)) ? Number(ctx.renderNowMs) : frameStartMs;
            const mazeModeSettingEnabled = this.isLosMazeModeEnabled();
            if (mazeModeSettingEnabled && (!this.lastMazeModeSettingEnabled || !Number.isFinite(this.mazeModeActivatedAtMs))) {
                this.mazeModeActivatedAtMs = frameNowMs;
                this.mazeModeSuppressRevealAnimation = true;
            } else if (!mazeModeSettingEnabled) {
                this.mazeModeActivatedAtMs = null;
                this.mazeModeSuppressRevealAnimation = false;
            }
            this.lastMazeModeSettingEnabled = mazeModeSettingEnabled;
            this.beginDrawPassProfiling(ctx);
            this.profileDrawPassSection("resetWallDepthGeometryBudget", () => {
                if (typeof global.resetWallDepthGeometryBudget === "function") {
                    global.resetWallDepthGeometryBudget();
                }
            });
            this.setLegacyLayersVisible(false);
            this.layers.root.visible = true;
            this.profileDrawPassSection("camera.update", () => {
                this.camera.update({
                    camera: ctx.camera,
                    wizard: ctx.wizard,
                    viewport: ctx.viewport,
                    viewscale: ctx.viewscale,
                    xyratio: ctx.xyratio,
                    map: ctx.map,
                    renderAlpha: ctx.renderAlpha
                });
            });
            this.profileDrawPassSection("syncLayerTransitionState", () => {
                this.syncLayerTransitionState(ctx);
            });
            this.resetPickRenderItems();
            if (this.scenePicker && this.scenePicker.publicApi) {
                global.renderingScenePicker = this.scenePicker.publicApi;
            }
            let preparedCutawayState = null;
            this.profileDrawPassSection("prepareLayerCutawayFrame", () => {
                preparedCutawayState = this.prepareLayerCutawayFrame(ctx, ctx.map || global.map || null, ctx.wizard || global.wizard || null);
            });
            const visibleNodes = this.profileDrawPassSection("collectVisibleNodes", () =>
                this.collectVisibleNodes(ctx, 4, 4)
            );
            this.profileDrawPassSection("prepareBuildingInteriorPickerFrame", () => {
                const cutawayState = preparedCutawayState || this.prepareLayerCutawayFrame(ctx, ctx.map || global.map || null, ctx.wizard || global.wizard || null);
                this.prepareBuildingInteriorPickerFrame(ctx, cutawayState);
            });
            const visibleObjects = this.profileDrawPassSection("collectVisibleObjects", () =>
                this.collectVisibleObjects(visibleNodes, ctx)
            );
            this.profileDrawPassSection("syncOnscreenObjectsCache", () => {
                this.syncOnscreenObjectsCache(ctx, visibleNodes, visibleObjects);
            });
            this.profileDrawPassSection("updateLosState", () => {
                this.updateLosState(ctx, visibleNodes, visibleObjects);
            });
            this.profileDrawPassSection("updateWallLosIlluminationTallies", () => {
                this.updateWallLosIlluminationTallies(ctx);
            });
            this.profileDrawPassSection("renderGroundTiles", () => {
                this.renderGroundTiles(ctx, visibleNodes);
            });
            this.profileDrawPassSection("renderFloorVisualPolygons", () => {
                this.renderFloorVisualPolygons(ctx);
            });
            this.profileDrawPassSection("renderPrototypeSectionSeams", () => {
                this.renderPrototypeSectionSeams(ctx);
            });
            this.profileDrawPassSection("renderClearanceOverlay", () => {
                if (typeof drawAnimalClearanceOverlay === "function") {
                    drawAnimalClearanceOverlay(this.layers.ground, this.camera);
                }
            });
            this.profileDrawPassSection("renderTileClearanceNumbers", () => {
                if (typeof drawTileClearanceNumbers === "function") {
                    drawTileClearanceNumbers(this.layers.ground, this.camera);
                }
            });
            this.profileDrawPassSection("drawMapBorder", () => {
                const debugEnabled = !!(
                    (typeof debugMode !== "undefined" && debugMode) ||
                    global.debugMode
                );
                if (debugEnabled && typeof global.drawMapBorder === "function") {
                    global.drawMapBorder();
                }
            });
            this.profileDrawPassSection("renderRoadsAndFloors", () => {
                this.renderRoadsAndFloors(ctx, visibleNodes);
            });
            this.profileDrawPassSection("renderHexGridOverlay", () => {
                this.renderHexGridOverlay(ctx);
            });
            this.profileDrawPassSection("applyMazeModeCompositor", () => {
                this.applyMazeModeCompositor(ctx);
            });
            this.profileDrawPassSection("renderLosShadowOverlay", () => {
                this.renderLosShadowOverlay(ctx);
            });
            this.profileDrawPassSection("renderObjects3D", () => {
                this.renderObjects3D(ctx, visibleNodes, visibleObjects);
            });
            this.profileDrawPassSection("renderPrototypeBuildingExteriors", () => {
                this.renderPrototypeBuildingExteriors(ctx);
            });
            this.profileDrawPassSection("renderTerrainBrushPreview", () => {
                this.renderTerrainBrushPreview(ctx);
            });
            this.profileDrawPassSection("renderWallPlacementPreview", () => {
                this.renderWallPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderRoadPlacementPreview", () => {
                this.renderRoadPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderFirewallPlacementPreview", () => {
                this.renderFirewallPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderTriggerAreaPlacementPreview", () => {
                this.renderTriggerAreaPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderFloorEditorPolygonOverlay", () => {
                this.renderFloorEditorPolygonOverlay(ctx);
            });
            this.profileDrawPassSection("renderLayerCutawayDebugOverlay", () => {
                this.renderLayerCutawayDebugOverlay(ctx);
            });
            this.profileDrawPassSection("renderPlaceObjectPreview", () => {
                this.renderPlaceObjectPreview(ctx);
            });
            this.profileDrawPassSection("renderBuildingPlacementPreview", () => {
                this.renderBuildingPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderPowerupPlacementPreview", () => {
                this.renderPowerupPlacementPreview(ctx);
            });
            this.profileDrawPassSection("renderPowerups", () => {
                this.renderPowerups(ctx);
            });
            this.profileDrawPassSection("renderWizard", () => {
                this.renderWizard(ctx);
            });
            this.profileDrawPassSection("renderCreatureTracePaths", () => {
                this.renderCreatureTracePaths(ctx);
            });
            this.profileDrawPassSection("renderProjectiles", () => {
                this.renderProjectiles(ctx);
            });
            this.profileDrawPassSection("renderLayerTransitionSnapshot", () => {
                this.renderLayerTransitionSnapshot(ctx);
            });
            this.profileDrawPassSection("renderScriptMessages", () => {
                this.renderScriptMessages(ctx);
            });
            this.profileDrawPassSection("renderWizardThoughtBubble", () => {
                this.renderWizardThoughtBubble(ctx);
            });
            if (this.scenePicker && typeof this.scenePicker.renderHoverHighlight === "function") {
                this.profileDrawPassSection("scenePicker.renderHoverHighlight", () => {
                    const spellSystemRef = (typeof SpellSystem !== "undefined")
                        ? SpellSystem
                        : (global.SpellSystem || null);
                    const mousePosRef = (typeof mousePos !== "undefined")
                        ? mousePos
                        : (global.mousePos || null);
                    const frameCountRef = (typeof frameCount !== "undefined")
                        ? frameCount
                        : (global.frameCount || 0);
                    const spaceHeldRef = !!(
                        typeof keysPressed !== "undefined" &&
                        keysPressed &&
                        keysPressed[" "]
                    );
                    this.scenePicker.renderHoverHighlight({
                        app: ctx.app || global.app || null,
                        map: ctx.map || global.map || null,
                        wizard: ctx.wizard || global.wizard || null,
                        spellSystem: spellSystemRef,
                        mousePos: mousePosRef,
                        spaceHeld: spaceHeldRef,
                        frameCount: frameCountRef,
                        viewport: ctx.viewport || null,
                        pickRenderItems: this.pickRenderItems,
                        camera: this.camera,
                        uiLayer: this.layers.ui,
                        getDisplayObjectForItem: (item) => {
                            if (!item) return null;
                            if (item._renderingDepthMesh && item._renderingDepthMesh.visible) return item._renderingDepthMesh;
                            if (item.type === "road") {
                                const roadSprite = this.roadSpriteByObject.get(item);
                                if (roadSprite && roadSprite.parent) return roadSprite;
                            }
                            if (item._renderingDisplayObject && item._renderingDisplayObject.parent) {
                                return item._renderingDisplayObject;
                            }
                            if (item.pixiSprite && item.pixiSprite.parent) return item.pixiSprite;
                            return null;
                        }
                    });
                });
            }
            if (this.hexGridContainer && this.hexGridContainer.parent === this.layers.ui) {
                const ui = this.layers.ui;
                const hasBackdrop = !!(
                    this.hexGridPickerBackdrop &&
                    this.hexGridPickerBackdrop.parent === ui
                );
                const previewSprite = (this.scenePicker && this.scenePicker.pickPreviewSprite && this.scenePicker.pickPreviewSprite.parent === ui)
                    ? this.scenePicker.pickPreviewSprite
                    : null;
                if (previewSprite) {
                    const previewTopIdx = ui.children.length - 1;
                    if (ui.getChildIndex(previewSprite) !== previewTopIdx) {
                        ui.setChildIndex(previewSprite, previewTopIdx);
                    }
                    const previewIdx = ui.getChildIndex(previewSprite);
                    const gridTargetIdx = Math.max(hasBackdrop ? 1 : 0, previewIdx - 1);
                    if (ui.getChildIndex(this.hexGridContainer) !== gridTargetIdx) {
                        ui.setChildIndex(this.hexGridContainer, gridTargetIdx);
                    }
                    if (hasBackdrop && ui.getChildIndex(this.hexGridPickerBackdrop) !== 0) {
                        ui.setChildIndex(this.hexGridPickerBackdrop, 0);
                    }
                } else {
                    const gridTargetIdx = hasBackdrop ? 1 : 0;
                    if (ui.getChildIndex(this.hexGridContainer) !== gridTargetIdx) {
                        ui.setChildIndex(this.hexGridContainer, gridTargetIdx);
                    }
                    if (hasBackdrop && ui.getChildIndex(this.hexGridPickerBackdrop) !== 0) {
                        ui.setChildIndex(this.hexGridPickerBackdrop, 0);
                    }
                }
            }
            this.profileDrawPassSection("promoteInteriorPresentationForeground", () => {
                this.promoteInteriorPresentationForeground(ctx);
            });
            const showPickerScreen = getShowPickerScreenFlag();
            setShowPickerScreenFlag(showPickerScreen);
            this.layers.ground.visible = !showPickerScreen;
            this.layers.roadsFloor.visible = !showPickerScreen;
            this.layers.groundObjects.visible = !showPickerScreen;
            this.layers.losShadow.visible = !showPickerScreen && !this.mazeModeOverlayActive;
            this.layers.depthObjects.visible = !showPickerScreen;
            this.layers.objects3d.visible = !showPickerScreen;
            if (this.layers.characters) {
                this.layers.characters.visible = !showPickerScreen;
            }
            this.layers.entities.visible = !showPickerScreen;
            this.layers.scriptMessages.visible = !showPickerScreen;
            this.layers.ui.visible = true;
            if (this.mazeModeRenderer && this.mazeModeRenderer.blackBackdropGraphics) {
                this.mazeModeRenderer.blackBackdropGraphics.visible = !showPickerScreen && this.mazeModeOverlayActive;
            }
            if (this.mazeModeRenderer && this.mazeModeRenderer.occlusionMaskGraphics) {
                this.mazeModeRenderer.occlusionMaskGraphics.visible = !showPickerScreen && this.mazeModeOverlayActive;
            }
            if (isTextureSanitizerEnabled()) {
                this.profileDrawPassSection("sanitizeDisplayTreeTextures", () => {
                    const sanitizeRoot = (ctx && ctx.app && ctx.app.stage) ? ctx.app.stage : this.layers.root;
                    const sanitizeResult = sanitizeDisplayTreeTextures(sanitizeRoot, { maxSamples: 6 });
                    if (sanitizeResult && sanitizeResult.repaired > 0) {
                        const nowMs = performance.now();
                        if (!Number.isFinite(this._lastTextureSanitizerLogAtMs) || (nowMs - this._lastTextureSanitizerLogAtMs) > 1000) {
                            this._lastTextureSanitizerLogAtMs = nowMs;
                            console.warn("[render texture sanitizer]", {
                                repaired: sanitizeResult.repaired,
                                samples: sanitizeResult.samples
                            });
                        }
                    }
                });
            }
            const frameElapsedMs = performance.now() - frameStartMs;
            this.recordDrawPassSection("renderFrame.total", frameElapsedMs);
            if (typeof globalThis !== "undefined" && globalThis.__moveObjectPerf) {
                const sections = this.currentFrameDrawSections || Object.create(null);
                const metrics = this.currentFrameMetrics || Object.create(null);
                const getMs = (name) => Number(sections[name] || 0);
                const getMetric = (name) => Number(metrics[name] || 0);
                this.recordMoveObjectPerf("rendering.frame", {
                    visibleNodes: getMetric("visibleNodes"),
                    visibleObjects: Array.isArray(visibleObjects) ? visibleObjects.length : null,
                    collectVisibleObjectsMs: getMs("collectVisibleObjects"),
                    prepareLayerCutawayFrameMs: getMs("prepareLayerCutawayFrame"),
                    prepareBuildingInteriorPickerFrameMs: getMs("prepareBuildingInteriorPickerFrame"),
                    renderObjects3DMs: getMs("renderObjects3D"),
                    renderPrototypeBuildingExteriorsMs: getMs("renderPrototypeBuildingExteriors"),
                    scenePickerHoverMs: getMs("scenePicker.renderHoverHighlight"),
                    buildingRenderCacheBuildMs: getMetric("buildingRenderCacheBuildMs"),
                    objects3dBuildingCompositeMs: getMetric("objects3dBuildingCompositeMs"),
                    buildingInteriorPlanMs: getMetric("buildingInteriorPlanMs")
                }, frameElapsedMs);
            }
            if (typeof globalThis !== "undefined") {
                globalThis.renderingLiveStats = {
                    groundCached: this.groundSpriteByNodeKey instanceof Map
                        ? this.groundSpriteByNodeKey.size
                        : 0,
                    groundVisible: this.groundVisibleNodeKeys instanceof Set
                        ? this.groundVisibleNodeKeys.size
                        : 0,
                    groundPool: Array.isArray(this.groundSpritePool)
                        ? this.groundSpritePool.length
                        : 0,
                    roadCached: this.roadSpriteByObject instanceof Map
                        ? this.roadSpriteByObject.size
                        : 0,
                    depthMeshes: this.activeDepthBillboardMeshes instanceof Set
                        ? this.activeDepthBillboardMeshes.size
                        : 0,
                    objectDisplays: this.activeObjectDisplayObjects instanceof Set
                        ? this.activeObjectDisplayObjects.size
                        : 0,
                    groundLayerChildren: this.layers && this.layers.ground && Array.isArray(this.layers.ground.children)
                        ? this.layers.ground.children.length
                        : 0,
                    roadsLayerChildren: this.layers && this.layers.roadsFloor && Array.isArray(this.layers.roadsFloor.children)
                        ? this.layers.roadsFloor.children.length
                        : 0,
                    objectsLayerChildren: this.layers && this.layers.objects3d && Array.isArray(this.layers.objects3d.children)
                        ? this.layers.objects3d.children.length
                        : 0
                };
            }
            if (typeof globalThis !== "undefined" && isDrawPassBreakdownEnabled()) {
                const sections = this.currentFrameDrawSections || Object.create(null);
                const metrics = this.currentFrameMetrics || Object.create(null);
                const getMs = (name) => Number(sections[name] || 0);
                const getMetric = (name) => Number(metrics[name] || 0);
                const visibleObjectsCount = Array.isArray(visibleObjects) ? visibleObjects.length : 0;
                let hydratedRoads = 0;
                let hydratedTrees = 0;
                for (let i = 0; i < visibleObjectsCount; i++) {
                    const item = visibleObjects[i];
                    if (!item || item.gone || item.vanishing) continue;
                    if (item.type === "road") {
                        hydratedRoads += 1;
                    } else if (item.type === "tree") {
                        hydratedTrees += 1;
                    }
                }
                globalThis.drawPerfBreakdown = {
                    lazyMs: 0,
                    prepMs: getMs("resetWallDepthGeometryBudget") + getMs("camera.update"),
                    collectMs: getMs("collectVisibleNodes") + getMs("prepareLayerCutawayFrame") + getMs("prepareBuildingInteriorPickerFrame") + getMs("collectVisibleObjects") + getMs("syncOnscreenObjectsCache"),
                    collectVisibleNodesMs: getMs("collectVisibleNodes"),
                    collectCutawayMs: getMs("prepareLayerCutawayFrame") + getMs("prepareBuildingInteriorPickerFrame"),
                    collectLayerCutawayMs: getMs("prepareLayerCutawayFrame"),
                    collectInteriorPickerMs: getMs("prepareBuildingInteriorPickerFrame"),
                    collectVisibleObjectsMs: getMs("collectVisibleObjects"),
                    collectOnscreenCacheMs: getMs("syncOnscreenObjectsCache"),
                    losMs: getMs("updateLosState") + getMs("updateWallLosIlluminationTallies"),
                    composeMs: frameElapsedMs,
                    passWorldMs:
                        getMs("renderGroundTiles") +
                        getMs("renderHexGridOverlay") +
                        getMs("renderPrototypeSectionSeams") +
                        getMs("renderClearanceOverlay") +
                        getMs("renderTileClearanceNumbers") +
                        getMs("drawMapBorder") +
                        getMs("renderRoadsAndFloors") +
                        getMs("renderFloorVisualPolygons"),
                    passWorldGroundMs: getMs("renderGroundTiles"),
                    passWorldHexMs: getMs("renderHexGridOverlay"),
                    passWorldSeamsMs: getMs("renderPrototypeSectionSeams"),
                    passWorldClearanceMs: getMs("renderClearanceOverlay"),
                    passWorldTileNumbersMs: getMs("renderTileClearanceNumbers"),
                    passWorldBorderMs: getMs("drawMapBorder"),
                    passWorldRoadsMs: getMs("renderRoadsAndFloors"),
                    passWorldFloorPolygonsMs: getMs("renderFloorVisualPolygons"),
                    passLosMs:
                        getMs("applyMazeModeCompositor") +
                        getMs("renderLosShadowOverlay"),
                    passObjectsMs:
                        getMs("renderObjects3D") +
                        getMs("renderPrototypeBuildingExteriors") +
                        getMs("renderPowerups") +
                        getMs("renderWizard") +
                        getMs("renderCreatureTracePaths") +
                        getMs("renderProjectiles") +
                        getMs("renderScriptMessages"),
                    passPostMs:
                        getMs("renderWallPlacementPreview") +
                        getMs("renderRoadPlacementPreview") +
                        getMs("renderFirewallPlacementPreview") +
                        getMs("renderTriggerAreaPlacementPreview") +
                        getMs("renderPlaceObjectPreview") +
                        getMs("renderBuildingPlacementPreview") +
                        getMs("renderPowerupPlacementPreview") +
                        getMs("scenePicker.renderHoverHighlight") +
                        getMs("drawNodeInspectorOverlay") +
                        getMs("sanitizeDisplayTreeTextures"),
                    composeMaskMs: 0,
                    composeSortMs: 0,
                    composePopulateMs: 0,
                    composeInvariantMs: 0,
                    composeWallSectionsMs: 0,
                    composeWallSectionsGroups: 0,
                    composeWallSectionsRebuilt: 0,
                    composeUnaccountedMs: 0,
                    composeInvariantSkipped: 0,
                    visibleNodes: getMetric("visibleNodes"),
                    visibleNodesWrapped: getMetric("visibleNodesWrapped"),
                    visibleNodesFallback: getMetric("visibleNodesFallback"),
                    visibleNodeFilterSkipped: getMetric("visibleNodeFilterSkipped"),
                    visibleNodeFallbackUsed: getMetric("visibleNodeFallbackUsed"),
                    visibleObjectNodeRefs: getMetric("visibleObjectNodeRefs"),
                    visibleObjectVisibilityRefs: getMetric("visibleObjectVisibilityRefs"),
                    visibleObjectDuplicateRefsSkipped: getMetric("visibleObjectDuplicateRefsSkipped"),
                    visibleFloorObjectNodes: getMetric("visibleFloorObjectNodes"),
                    visibleObjectsSkippedBuildingCutaway: getMetric("visibleObjectsSkippedBuildingCutaway"),
                    visibleObjectRoadRefsSkippedForPickerSurface: getMetric("visibleObjectRoadRefsSkippedForPickerSurface"),
                    floorObjectNodeCandidates: getMetric("floorObjectNodeCandidates"),
                    floorObjectNodeCandidatesScanned: getMetric("floorObjectNodeCandidatesScanned"),
                    floorObjectNodeSectionsScanned: getMetric("floorObjectNodeSectionsScanned"),
                    floorObjectNodeYRowsScanned: getMetric("floorObjectNodeYRowsScanned"),
                    floorObjectNodesSkippedOffscreen: getMetric("floorObjectNodesSkippedOffscreen"),
                    floorObjectNodesSkippedStale: getMetric("floorObjectNodesSkippedStale"),
                    floorObjectNodeIndexBuildMs: getMetric("floorObjectNodeIndexBuildMs"),
                    floorObjectNodeIndexRows: getMetric("floorObjectNodeIndexRows"),
                    floorObjectNodeIndexReady: getMetric("floorObjectNodeIndexReady"),
                    floorObjectNodeIndexStaleDuringBubbleShift: getMetric("floorObjectNodeIndexStaleDuringBubbleShift"),
                    layerCutawayRenderItemTests: getMetric("layerCutawayRenderItemTests"),
                    layerCutawayRenderItemFastPath: getMetric("layerCutawayRenderItemFastPath"),
                    layerCutawayWorldPointTests: getMetric("layerCutawayWorldPointTests"),
                    layerCutawayWorldPointFastPath: getMetric("layerCutawayWorldPointFastPath"),
                    visibleAnimalsAdded: getMetric("visibleAnimalsAdded"),
                    visibleAnimalsSkippedOffscreen: getMetric("visibleAnimalsSkippedOffscreen"),
                    onscreenCacheObjects: getMetric("onscreenCacheObjects"),
                    onscreenCacheRoofs: getMetric("onscreenCacheRoofs"),
                    losCandidates: getMetric("losCandidates"),
                    losBuildMs: getMetric("losBuildMs"),
                    losTraceMs: getMetric("losTraceMs"),
                    losTotalMs: getMetric("losTotalMs"),
                    losRecomputed: getMetric("losRecomputed"),
                    losVisibleObjects: getMetric("losVisibleObjects"),
                    wallLosMs: getMetric("wallLosMs"),
                    wallLosResetSections: getMetric("wallLosResetSections"),
                    wallLosIlluminatedBins: getMetric("wallLosIlluminatedBins"),
                    wallLosRangedSections: getMetric("wallLosRangedSections"),
                    wallLosEndpointLookups: getMetric("wallLosEndpointLookups"),
                    wallLosEndpointOwnersResolved: getMetric("wallLosEndpointOwnersResolved"),
                    mazeModeMaskWorldPoints: getMetric("mazeModeMaskWorldPoints"),
                    mazeModeMaskActive: getMetric("mazeModeMaskActive"),
                    roadsVisible: getMetric("roadsVisible"),
                    roadsCached: getMetric("roadsCached"),
                    roadsCreated: getMetric("roadsCreated"),
                    roadsAttached: getMetric("roadsAttached"),
                    roadsTextureRefreshes: getMetric("roadsTextureRefreshes"),
                    roadsTextureAssignments: getMetric("roadsTextureAssignments"),
                    roadsHidden: getMetric("roadsHidden"),
                    roadsDestroyed: getMetric("roadsDestroyed"),
                    roadsEvicted: getMetric("roadsEvicted"),
                    roadsSkippedForLevel0Bake: getMetric("roadsSkippedForLevel0Bake"),
                    roadsMs: getMetric("roadsMs"),
                    floorFragmentsScanned: getMetric("floorFragmentsScanned"),
                    floorFragmentsSkippedLevelIsolation: getMetric("floorFragmentsSkippedLevelIsolation"),
                    floorFragmentsSkippedUneditedLevel0: getMetric("floorFragmentsSkippedUneditedLevel0"),
                    floorFragmentsSkippedNoSurface: getMetric("floorFragmentsSkippedNoSurface"),
                    floorFragmentsSkippedInvalidOuter: getMetric("floorFragmentsSkippedInvalidOuter"),
                    floorLevel0Entries: getMetric("floorLevel0Entries"),
                    floorNonzeroEntries: getMetric("floorNonzeroEntries"),
                    floorLevel0Sections: getMetric("floorLevel0Sections"),
                    floorEntriesCollected: getMetric("floorEntriesCollected"),
                    floorLevel0BakeRequests: getMetric("floorLevel0BakeRequests"),
                    floorLevel0BakeHits: getMetric("floorLevel0BakeHits"),
                    floorLevel0BakeMisses: getMetric("floorLevel0BakeMisses"),
                    floorLevel0BakePending: getMetric("floorLevel0BakePending"),
                    floorLevel0BakeGroundTiles: getMetric("floorLevel0BakeGroundTiles"),
                    floorLevel0BakeRoads: getMetric("floorLevel0BakeRoads"),
                    floorLevel0BakePixels: getMetric("floorLevel0BakePixels"),
                    floorLevel0BakePatchRects: getMetric("floorLevel0BakePatchRects"),
                    floorLevel0ChunkBuilds: getMetric("floorLevel0ChunkBuilds"),
                    floorLevel0ChunkGroundTiles: getMetric("floorLevel0ChunkGroundTiles"),
                    floorLevel0ChunkRoads: getMetric("floorLevel0ChunkRoads"),
                    floorLevel0ChunksConsidered: getMetric("floorLevel0ChunksConsidered"),
                    floorLevel0ChunksInView: getMetric("floorLevel0ChunksInView"),
                    floorLevel0ChunkEntries: getMetric("floorLevel0ChunkEntries"),
                    floorLevel0ChunkClipMs: getMetric("floorLevel0ChunkClipMs"),
                    floorLevel0ChunkTextureMs: getMetric("floorLevel0ChunkTextureMs"),
                    floorLevel0ChunkWaterMs: getMetric("floorLevel0ChunkWaterMs"),
                    floorAnimatedWaterChunkCacheHits: getMetric("floorAnimatedWaterChunkCacheHits"),
                    floorAnimatedWaterChunkCacheMisses: getMetric("floorAnimatedWaterChunkCacheMisses"),
                    floorVisualPolygons: getMetric("floorVisualPolygons"),
                    floorVisualMeshesCreated: getMetric("floorVisualMeshesCreated"),
                    floorVisualGeometryUploads: getMetric("floorVisualGeometryUploads"),
                    floorVisualCollectMs: getMetric("floorVisualCollectMs"),
                    floorVisualMeshLookupMs: getMetric("floorVisualMeshLookupMs"),
                    floorVisualMeshCreateMs: getMetric("floorVisualMeshCreateMs"),
                    floorVisualGeometryMs: getMetric("floorVisualGeometryMs"),
                    floorVisualMeshAssignMs: getMetric("floorVisualMeshAssignMs"),
                    floorVisualMeshUpdateMs: getMetric("floorVisualMeshUpdateMs"),
                    floorVisualHideMs: getMetric("floorVisualHideMs"),
                    floorVisualTrimMs: getMetric("floorVisualTrimMs"),
                    floorVisualVertices: getMetric("floorVisualVertices"),
                    floorVisualTriangles: getMetric("floorVisualTriangles"),
                    floorVisualMeshCacheSize: getMetric("floorVisualMeshCacheSize"),
                    floorVisualChunkClipCacheHits: getMetric("floorVisualChunkClipCacheHits"),
                    floorVisualChunkClipCacheMisses: getMetric("floorVisualChunkClipCacheMisses"),
                    floorVisualChunkClipCacheSize: getMetric("floorVisualChunkClipCacheSize"),
                    floorVisualChunkClipsTrimmed: getMetric("floorVisualChunkClipsTrimmed"),
                    floorTerrainPolygonEntryCacheHits: getMetric("floorTerrainPolygonEntryCacheHits"),
                    floorTerrainPolygonEntryCacheMisses: getMetric("floorTerrainPolygonEntryCacheMisses"),
                    floorTerrainPolygonEntryCacheSize: getMetric("floorTerrainPolygonEntryCacheSize"),
                    floorTerrainPolygonEntriesTrimmed: getMetric("floorTerrainPolygonEntriesTrimmed"),
                    groundTilesSkippedForLevel0Chunks: getMetric("groundTilesSkippedForLevel0Chunks"),
                    groundTileSpritesVisible: getMetric("groundTileSpritesVisible"),
                    depthCandidates: getMetric("depthCandidates"),
                    depthMissingMountedSection: getMetric("depthMissingMountedSection"),
                    depthHiddenByScript: getMetric("depthHiddenByScript"),
                    depthDoorBottomOutlineOnly: getMetric("depthDoorBottomOutlineOnly"),
                    groundObjectSpritesRendered: getMetric("groundObjectSpritesRendered"),
                    objects3dLosBuildMs: getMetric("objects3dLosBuildMs"),
                    objects3dLosVisibleSetSize: getMetric("objects3dLosVisibleSetSize"),
                    objects3dLosVisibleWalls: getMetric("objects3dLosVisibleWalls"),
                    objects3dFilterMs: getMetric("objects3dFilterMs"),
                    objects3dTransformMs: getMetric("objects3dTransformMs"),
                    objects3dDepthMs: getMetric("objects3dDepthMs"),
                    objects3dGroundMs: getMetric("objects3dGroundMs"),
                    objects3dDisplayMs: getMetric("objects3dDisplayMs"),
                    objects3dAnimalLosHidden: getMetric("objects3dAnimalLosHidden"),
                    objects3dMissingMountedSectionFiltered: getMetric("objects3dMissingMountedSectionFiltered"),
                    objects3dBuildingCompositeMs: getMetric("objects3dBuildingCompositeMs"),
                    objects3dBuildingCompositeActive: getMetric("objects3dBuildingCompositeActive"),
                    objects3dBuildingCompositeObjects: getMetric("objects3dBuildingCompositeObjects"),
                    objects3dBuildingCompositeOriginalsHidden: getMetric("objects3dBuildingCompositeOriginalsHidden"),
                    objects3dBuildingCompositeCacheHits: getMetric("objects3dBuildingCompositeCacheHits"),
                    objects3dBuildingCompositeCacheMisses: getMetric("objects3dBuildingCompositeCacheMisses"),
                    objects3dBuildingCompositePendingTextures: getMetric("objects3dBuildingCompositePendingTextures"),
                    objects3dBuildingGhostItemsFlagged: getMetric("objects3dBuildingGhostItemsFlagged"),
                    objects3dBuildingHiddenItemsFlagged: getMetric("objects3dBuildingHiddenItemsFlagged"),
                    objects3dBuildingMaskMs: getMetric("objects3dBuildingMaskMs"),
                    objects3dBuildingMaskActive: getMetric("objects3dBuildingMaskActive"),
                    objects3dBuildingMaskEntries: getMetric("objects3dBuildingMaskEntries"),
                    objects3dBuildingMaskMeshes: getMetric("objects3dBuildingMaskMeshes"),
                    objects3dBuildingMaskVertices: getMetric("objects3dBuildingMaskVertices"),
                    objects3dBuildingMaskTriangles: getMetric("objects3dBuildingMaskTriangles"),
                    objects3dBuildingMaskCreated: getMetric("objects3dBuildingMaskCreated"),
                    objects3dBuildingMaskGeometryUploads: getMetric("objects3dBuildingMaskGeometryUploads"),
                    objects3dBuildingMaskUniformUpdates: getMetric("objects3dBuildingMaskUniformUpdates"),
                    objects3dBuildingMaskAttachOps: getMetric("objects3dBuildingMaskAttachOps"),
                    objects3dBuildingMaskHiddenMeshes: getMetric("objects3dBuildingMaskHiddenMeshes"),
                    objects3dMazeHidden: getMetric("objects3dMazeHidden"),
                    objects3dMazeHiddenWalls: getMetric("objects3dMazeHiddenWalls"),
                    objects3dMapItems: getMetric("objects3dMapItems"),
                    objects3dRoofItems: getMetric("objects3dRoofItems"),
                    objects3dRenderItems: getMetric("objects3dRenderItems"),
                    objects3dDepthRendered: getMetric("objects3dDepthRendered"),
                    objects3dGroundRendered: getMetric("objects3dGroundRendered"),
                    objects3dDisplayObjects: getMetric("objects3dDisplayObjects"),
                    objects3dVisibleAnimals: getMetric("objects3dVisibleAnimals"),
                    objects3dVisibleTrees: getMetric("objects3dVisibleTrees"),
                    mapItems: visibleObjectsCount,
                    onscreen: (typeof global.onscreenObjects !== "undefined" && global.onscreenObjects && Number.isFinite(global.onscreenObjects.size))
                        ? Number(global.onscreenObjects.size)
                        : visibleObjectsCount,
                    groundCached: this.groundSpriteByNodeKey instanceof Map
                        ? this.groundSpriteByNodeKey.size
                        : 0,
                    groundVisible: this.groundVisibleNodeKeys instanceof Set
                        ? this.groundVisibleNodeKeys.size
                        : 0,
                    groundPool: Array.isArray(this.groundSpritePool)
                        ? this.groundSpritePool.length
                        : 0,
                    roadCached: this.roadSpriteByObject instanceof Map
                        ? this.roadSpriteByObject.size
                        : 0,
                    depthMeshes: this.activeDepthBillboardMeshes instanceof Set
                        ? this.activeDepthBillboardMeshes.size
                        : 0,
                    objectDisplays: this.activeObjectDisplayObjects instanceof Set
                        ? this.activeObjectDisplayObjects.size
                        : 0,
                    groundLayerChildren: this.layers && this.layers.ground && Array.isArray(this.layers.ground.children)
                        ? this.layers.ground.children.length
                        : 0,
                    roadsLayerChildren: this.layers && this.layers.roadsFloor && Array.isArray(this.layers.roadsFloor.children)
                        ? this.layers.roadsFloor.children.length
                        : 0,
                    objectsLayerChildren: this.layers && this.layers.objects3d && Array.isArray(this.layers.objects3d.children)
                        ? this.layers.objects3d.children.length
                        : 0,
                    hydratedRoads,
                    hydratedTrees
                };
                const addTypeBreakdownMetrics = (prefix) => {
                    const suffixes = [
                        "Total",
                        "Roads",
                        "Trees",
                        "Flowers",
                        "Walls",
                        "Animals",
                        "Roofs",
                        "Doors",
                        "Windows",
                        "Furniture",
                        "Triggers",
                        "Powerups",
                        "Stairs",
                        "Parked",
                        "Other"
                    ];
                    for (let i = 0; i < suffixes.length; i++) {
                        const key = `${prefix}${suffixes[i]}`;
                        globalThis.drawPerfBreakdown[key] = getMetric(key);
                    }
                };
                addTypeBreakdownMetrics("visibleObjects");
                addTypeBreakdownMetrics("objects3dMapItems");
                addTypeBreakdownMetrics("objects3dRenderItems");
                addTypeBreakdownMetrics("buildingRenderCacheCandidates");
                globalThis.renderingFrameMetrics = { ...metrics };
                const bubbleSession = ctx && ctx.map && ctx.map._prototypeBubbleShiftSession &&
                    ctx.map._prototypeBubbleShiftSession.completed !== true
                    ? ctx.map._prototypeBubbleShiftSession
                    : null;
                if (bubbleSession) {
                    if (!bubbleSession.renderStats || typeof bubbleSession.renderStats !== "object") {
                        bubbleSession.renderStats = {
                            frames: 0,
                            composeMs: 0,
                            collectMs: 0,
                            cutawayMs: 0,
                            layerCutawayMs: 0,
                            layerCutawayStateMs: 0,
                            buildingFlagMs: 0,
                            interiorPickerMs: 0,
                            interiorPlanMs: 0,
                            cutawayHeldFrames: 0,
                            visibleObjectsMax: 0,
                            visibleNodesMax: 0,
                            maxFrameMs: 0,
                            maxFrame: null,
                            maxCutawayMs: 0,
                            maxCutawayFrame: null
                        };
                    }
                    const renderStats = bubbleSession.renderStats;
                    const collectMs = getMs("collectVisibleNodes") + getMs("prepareLayerCutawayFrame") + getMs("prepareBuildingInteriorPickerFrame") + getMs("collectVisibleObjects") + getMs("syncOnscreenObjectsCache");
                    const layerCutawayMs = getMs("prepareLayerCutawayFrame");
                    const interiorPickerMs = getMs("prepareBuildingInteriorPickerFrame");
                    const cutawayMs = layerCutawayMs + interiorPickerMs;
                    renderStats.frames += 1;
                    renderStats.composeMs += frameElapsedMs;
                    renderStats.collectMs += collectMs;
                    renderStats.cutawayMs += cutawayMs;
                    renderStats.layerCutawayMs += layerCutawayMs;
                    renderStats.layerCutawayStateMs += getMetric("layerCutawayStateMs");
                    renderStats.buildingFlagMs += getMetric("objects3dBuildingFlagMs");
                    renderStats.interiorPickerMs += interiorPickerMs;
                    renderStats.interiorPlanMs += getMetric("buildingInteriorPlanMs");
                    renderStats.cutawayHeldFrames += getMetric("layerCutawayHeldDuringBubble") > 0 ? 1 : 0;
                    renderStats.visibleObjectsMax = Math.max(renderStats.visibleObjectsMax || 0, visibleObjectsCount);
                    renderStats.visibleNodesMax = Math.max(renderStats.visibleNodesMax || 0, getMetric("visibleNodes"));
                    const buildRenderFrameSample = () => {
                        const sectionSummary = this.summarizeCurrentDrawSections(frameElapsedMs, 14);
                        return {
                            composeMs: frameElapsedMs,
                            collectMs,
                            cutawayMs,
                            layerCutawayMs,
                            layerCutawayStateMs: getMetric("layerCutawayStateMs"),
                            buildingFlagMs: getMetric("objects3dBuildingFlagMs"),
                            interiorPickerMs,
                            interiorPlanMs: getMetric("buildingInteriorPlanMs"),
                            cutawayHeldDuringBubble: getMetric("layerCutawayHeldDuringBubble"),
                            visibleNodes: getMetric("visibleNodes"),
                            visibleObjects: visibleObjectsCount,
                            drawSectionsTotalMs: sectionSummary.totalMs,
                            drawUnaccountedMs: sectionSummary.unaccountedMs,
                            topDrawSections: sectionSummary.top,
                            passWorldMs:
                                getMs("renderGroundTiles") +
                                getMs("renderHexGridOverlay") +
                                getMs("renderPrototypeSectionSeams") +
                                getMs("renderClearanceOverlay") +
                                getMs("renderTileClearanceNumbers") +
                                getMs("drawMapBorder") +
                                getMs("renderRoadsAndFloors") +
                                getMs("renderFloorVisualPolygons"),
                            passWorldGroundMs: getMs("renderGroundTiles"),
                            passWorldHexMs: getMs("renderHexGridOverlay"),
                            passWorldSeamsMs: getMs("renderPrototypeSectionSeams"),
                            passWorldClearanceMs: getMs("renderClearanceOverlay"),
                            passWorldTileNumbersMs: getMs("renderTileClearanceNumbers"),
                            passWorldBorderMs: getMs("drawMapBorder"),
                            passWorldRoadsMs: getMs("renderRoadsAndFloors"),
                            passWorldFloorPolygonsMs: getMs("renderFloorVisualPolygons"),
                            passLosMs:
                                getMs("applyMazeModeCompositor") +
                                getMs("renderLosShadowOverlay"),
                            passObjectsMs:
                                getMs("renderObjects3D") +
                                getMs("renderPrototypeBuildingExteriors") +
                                getMs("renderPowerups") +
                                getMs("renderWizard") +
                                getMs("renderCreatureTracePaths") +
                                getMs("renderProjectiles") +
                                getMs("renderScriptMessages"),
                            passPostMs:
                                getMs("renderWallPlacementPreview") +
                                getMs("renderRoadPlacementPreview") +
                                getMs("renderFirewallPlacementPreview") +
                                getMs("renderTriggerAreaPlacementPreview") +
                                getMs("renderPlaceObjectPreview") +
                                getMs("renderBuildingPlacementPreview") +
                                getMs("renderPowerupPlacementPreview") +
                                getMs("scenePicker.renderHoverHighlight") +
                                getMs("drawNodeInspectorOverlay") +
                                getMs("sanitizeDisplayTreeTextures"),
                            groundTileSpritesVisible: getMetric("groundTileSpritesVisible"),
                            groundCached: this.groundSpriteByNodeKey instanceof Map ? this.groundSpriteByNodeKey.size : 0,
                            roadCached: this.roadSpriteByObject instanceof Map ? this.roadSpriteByObject.size : 0,
                            floorVisualMeshesCreated: getMetric("floorVisualMeshesCreated"),
                            floorVisualGeometryUploads: getMetric("floorVisualGeometryUploads"),
                            floorVisualCollectMs: getMetric("floorVisualCollectMs"),
                            floorVisualMeshUpdateMs: getMetric("floorVisualMeshUpdateMs"),
                            floorVisualHideMs: getMetric("floorVisualHideMs"),
                            floorVisualTrimMs: getMetric("floorVisualTrimMs"),
                            floorVisualMeshCacheSize: getMetric("floorVisualMeshCacheSize"),
                            floorVisualChunkClipCacheHits: getMetric("floorVisualChunkClipCacheHits"),
                            floorVisualChunkClipCacheMisses: getMetric("floorVisualChunkClipCacheMisses"),
                            floorLevel0ChunkBuilds: getMetric("floorLevel0ChunkBuilds"),
                            floorLevel0ChunkGroundTiles: getMetric("floorLevel0ChunkGroundTiles"),
                            floorLevel0ChunkRoads: getMetric("floorLevel0ChunkRoads"),
                            floorLevel0ChunksConsidered: getMetric("floorLevel0ChunksConsidered"),
                            floorLevel0ChunksInView: getMetric("floorLevel0ChunksInView"),
                            floorLevel0ChunkEntries: getMetric("floorLevel0ChunkEntries"),
                            floorLevel0ChunkClipMs: getMetric("floorLevel0ChunkClipMs"),
                            floorLevel0ChunkTextureMs: getMetric("floorLevel0ChunkTextureMs"),
                            floorLevel0ChunkWaterMs: getMetric("floorLevel0ChunkWaterMs"),
                            floorAnimatedWaterChunkCacheHits: getMetric("floorAnimatedWaterChunkCacheHits"),
                            floorAnimatedWaterChunkCacheMisses: getMetric("floorAnimatedWaterChunkCacheMisses"),
                            floorLevel0ChunkCacheSize: getMetric("floorLevel0ChunkCacheSize"),
                            floorLevel0ChunksTrimmed: getMetric("floorLevel0ChunksTrimmed"),
                            floorAnimatedWaterChunkCacheSize: getMetric("floorAnimatedWaterChunkCacheSize"),
                            floorAnimatedWaterChunksTrimmed: getMetric("floorAnimatedWaterChunksTrimmed"),
                            floorVisualChunkClipCacheSize: getMetric("floorVisualChunkClipCacheSize"),
                            floorVisualChunkClipsTrimmed: getMetric("floorVisualChunkClipsTrimmed"),
                            depthMeshes: this.activeDepthBillboardMeshes instanceof Set ? this.activeDepthBillboardMeshes.size : 0,
                            objectDisplays: this.activeObjectDisplayObjects instanceof Set ? this.activeObjectDisplayObjects.size : 0,
                            objects3dRenderItems: getMetric("objects3dRenderItems"),
                            objects3dDisplayObjects: getMetric("objects3dDisplayObjects"),
                            objects3dBuildingCompositeMs: getMetric("objects3dBuildingCompositeMs"),
                            objects3dBuildingCompositeCacheMisses: getMetric("objects3dBuildingCompositeCacheMisses"),
                            visibleGlobalWallsConsidered: getMetric("visibleGlobalWallsConsidered"),
                            visibleGlobalWallsAdded: getMetric("visibleGlobalWallsAdded")
                        };
                    };
                    if (frameElapsedMs > (Number(renderStats.maxFrameMs) || 0)) {
                        renderStats.maxFrameMs = frameElapsedMs;
                        renderStats.maxFrame = buildRenderFrameSample();
                    }
                    if (cutawayMs > (Number(renderStats.maxCutawayMs) || 0)) {
                        renderStats.maxCutawayMs = cutawayMs;
                        renderStats.maxCutawayFrame = buildRenderFrameSample();
                    }
                }
            } else if (typeof globalThis !== "undefined") {
                globalThis.drawPerfBreakdown = null;
                globalThis.renderingFrameMetrics = null;
            }
            if (this.drawPassProfiler && !this.drawPassProfiler.printed) {
                this.recordDrawPassFrameMetrics(this.currentFrameMetrics);
                this.drawPassProfiler.frameCount += 1;
                this.drawPassProfiler.totalFrameMs += frameElapsedMs;
                if (frameElapsedMs > this.drawPassProfiler.maxFrameMs) {
                    this.drawPassProfiler.maxFrameMs = frameElapsedMs;
                }
            }
            this.maybePrintDrawPassProfileSummary(ctx);
            this.profileDrawPassSection("drawNodeInspectorOverlay", () => {
                if (typeof drawNodeInspectorOverlay === "function") {
                    drawNodeInspectorOverlay(this.layers.ui, this.camera);
                }
            });
            this.profileDrawPassSection("renderScriptScreenColorOverlay", () => {
                this.renderScriptScreenColorOverlay(ctx);
            });
            this._activeDrawFrameId = 0;
            return true;
        }
    }

    let singleton = null;

    const renderingApi = {
        getWizardBodyFrameIndex(wizard, ctx = {}) {
            return getWizardBodyFrameIndex(wizard, ctx);
        },
        getWizardWaterBodyRenderState(immersion = null, bodyWorldHeight = 1, viewscale = 1) {
            return getWizardWaterBodyRenderState(immersion, bodyWorldHeight, viewscale);
        },
        renderFrame(ctx) {
            if (!global.RenderingCamera || !global.RenderingLayers || typeof PIXI === "undefined") {
                return false;
            }
            if (!singleton) singleton = new RenderingImpl();
            return singleton.renderFrame(ctx || {});
        },
        isWorldPointTargetable(worldX, worldY, wizardOverride = null, mapOverride = null) {
            const wizardRef = wizardOverride || global.wizard || null;
            if (!wizardRef || !Number.isFinite(worldX) || !Number.isFinite(worldY)) return true;
            if (!singleton) return true;
            const mazeModeEnabled = typeof singleton.isLosMazeModeEnabled === "function"
                ? singleton.isLosMazeModeEnabled()
                : false;
            const omnivisionActive = typeof singleton.isOmnivisionActive === "function"
                ? singleton.isOmnivisionActive(wizardRef)
                : false;
            if (!mazeModeEnabled || omnivisionActive) return true;
            if (typeof singleton.isWorldPointInLosShadow !== "function") return true;
            return !singleton.isWorldPointInLosShadow(worldX, worldY, wizardRef, mapOverride);
        },
        getLayers() {
            return singleton && singleton.layers ? singleton.layers : null;
        },
        screenColor(color, opacity, duration, fade) {
            if (!global.RenderingCamera || !global.RenderingLayers || typeof PIXI === "undefined") {
                throw new Error("screenColor requires the Rendering runtime");
            }
            if (!singleton) singleton = new RenderingImpl();
            return singleton.startScriptScreenColorOverlay(color, opacity, duration, fade);
        },
        thoughtBubble(wizardRef, text, duration) {
            if (!global.RenderingCamera || !global.RenderingLayers || typeof PIXI === "undefined") {
                throw new Error("wizard.thoughtBubble requires the Rendering runtime");
            }
            if (!singleton) singleton = new RenderingImpl();
            return singleton.startWizardThoughtBubble(wizardRef, text, duration);
        },
        resetLevel0GroundSurfaceCaches(reason = "") {
            if (!singleton) {
                return {
                    ok: true,
                    skipped: true,
                    reason: typeof reason === "string" ? reason : ""
                };
            }
            return singleton.resetLevel0GroundSurfaceCaches(reason);
        },
        invalidateLosState() {
            if (!singleton) return false;
            singleton.currentLosState = null;
            singleton.lastLosCandidateCount = -1;
            singleton.lastLosCandidateHash = 0;
            singleton.lastLosComputeAtMs = 0;
            return true;
        },
        isBuildingInteriorPresentationActive(ctx = null) {
            if (!singleton || typeof singleton.isBuildingInteriorPresentationActive !== "function") return false;
            return !!singleton.isBuildingInteriorPresentationActive(ctx);
        },
        getBuildingInteriorVisibleFloorFragmentIds(ctx = null) {
            if (!singleton || typeof singleton.getBuildingInteriorVisibleFloorFragmentIds !== "function") return new Set();
            return singleton.getBuildingInteriorVisibleFloorFragmentIds(ctx);
        },
        disable() {
            if (!singleton) return;
            for (const item of singleton.activeDepthBillboardItems) {
                if (item && item.pixiSprite) {
                    item.pixiSprite.visible = true;
                    if (Object.prototype.hasOwnProperty.call(item.pixiSprite, "renderable")) {
                        item.pixiSprite.renderable = true;
                    }
                }
                if (item && item._renderingDepthMesh) {
                    if (item._renderingDepthMesh.parent) {
                        item._renderingDepthMesh.parent.removeChild(item._renderingDepthMesh);
                    }
                    item._renderingDepthMesh.visible = false;
                    item._renderingDepthMesh = null;
                }
            }
            singleton.activeDepthBillboardItems.clear();
            singleton.activeDepthBillboardMeshes.clear();
            for (const sprite of singleton.activePowerupDisplayObjects) {
                if (!sprite) continue;
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }
            }
            singleton.activePowerupDisplayObjects.clear();
            for (const sprite of singleton.activeProjectileDisplayObjects) {
                if (!sprite) continue;
                sprite.visible = false;
                if (Object.prototype.hasOwnProperty.call(sprite, "renderable")) {
                    sprite.renderable = false;
                }
                if (sprite.parent) {
                    sprite.parent.removeChild(sprite);
                }
            }
            singleton.activeProjectileDisplayObjects.clear();
            singleton.clearPlaceObjectPreview();
            singleton.clearPowerupPlacementPreview();
            singleton.clearWallPlacementPreview();
            singleton.clearRoadPlacementPreview();
            if (singleton.scenePicker && typeof singleton.scenePicker.hideAll === "function") {
                singleton.scenePicker.hideAll();
            }
            if (singleton.scenePicker && singleton.scenePicker.publicApi && global.renderingScenePicker === singleton.scenePicker.publicApi) {
                global.renderingScenePicker = null;
            }
            singleton.layers.root.visible = false;
            singleton.setLegacyLayersVisible(true);
        }
    };
    global.Rendering = renderingApi;
})(typeof globalThis !== "undefined" ? globalThis : window);
